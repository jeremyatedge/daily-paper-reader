---
title: "TinyDEVO: Deep Event-based Visual Odometry on Ultra-low-power Multi-core Microcontrollers"
title_zh: TinyDEVO：超低功耗多核微控制器上的深度事件视觉里程计
authors: "Alessandro Marchei, Lorenzo Lamberti, Daniele Palossi, Luca Benini"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08060v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对资源受限微控制器的深度学习模型
tldr: 本研究针对嵌入式视觉中的里程计任务，提出了TinyDEVO，这是首个在超低功耗多核微控制器（MCU）上实现的深度事件相机视觉里程计模型。针对现有模型计算和内存开销巨大的问题，TinyDEVO通过架构优化和超参数调整，在保持较高精度的同时，将内存占用降低了11.5倍，计算量减少了29.7倍。在RISC-V架构MCU上实现了1.2 FPS的运行速度和仅86 mW的功耗，证明了在资源受限设备上运行复杂事件视觉任务的可行性。
selection_source: fresh_fetch
motivation: 现有的深度事件相机视觉里程计模型因极高的内存和计算需求，难以在资源受限的超低功耗微控制器上部署。
method: 通过对DEVO模型进行神经网络架构优化和超参数微调，设计了轻量化的TinyDEVO并将其部署在9核RISC-V超低功耗MCU上。
result: TinyDEVO将内存占用降至63.8MB，计算量减至5.2G MACs，在功耗仅86mW的情况下实现了1.2 FPS的吞吐量。
conclusion: 本研究首次证明了在超低功耗设备上运行端到端深度事件相机视觉里程计流水线的可行性。
---

## 摘要
嵌入式视觉中的一项关键任务是视觉里程计（VO），它通过视觉传感器估计相机运动，是许多嵌入式功耗受限系统（从自主机器人到增强现实和虚拟现实可穿戴设备）的核心组件。最新一类 VO 系统将深度学习模型与受生物启发的事件相机相结合，这类相机对运动模糊和光照条件具有鲁棒性。然而，最先进的（SoA）基于事件的 VO 算法需要大量的内存和计算资源。例如，领先的方法 DEVO 每帧需要 733 MB 内存和 1550 亿次乘加（MAC）操作。我们提出了 TinyDEVO，这是一种专为资源受限的微控制器单元（MCU）设计的基于事件的 VO 深度学习模型。我们将 TinyDEVO 部署在超低功耗（ULP）9 核 RISC-V 微控制器上，实现了约 1.2 帧/秒的吞吐量，平均功耗仅为 86 mW。得益于我们的神经网络架构优化和超参数调优，与 DEVO 相比，TinyDEVO 将内存占用减少了 11.5 倍（至 63.8 MB），每帧操作数减少了 29.7 倍（至每帧 52 亿次 MAC），同时在三个最先进的数据集上保持了 27 cm 的平均轨迹误差，即仅比 DEVO 高出 19 cm。我们的工作首次证明了在超低功耗设备上运行基于事件的 VO 流水线的可行性。

## Abstract
A key task in embedded vision is visual odometry (VO), which estimates camera motion from visual sensors, and it is a core component in many embedded power-constrained systems, from autonomous robots to augmented and virtual reality wearable devices. The newest class of VO systems combines deep learning models with bio-inspired event-based cameras, which are robust to motion blur and lighting conditions. However, state-of-the-art (SoA) event-based VO algorithms require significant memory and computation. For example, the leading approach DEVO requires 733 MB of memory and 155 billion multiply-accumulate (MAC) operations per frame. We present TinyDEVO, an event-based VO deep learning model designed for resource-constrained microcontroller units (MCUs). We deploy TinyDEVO on an ultra-low-power (ULP) 9-core RISC-V-based MCU, achieving a throughput of approximately 1.2 frames per second with an average power consumption of only 86 mW. Thanks to our neural network architectural optimizations and hyperparameter tuning, TinyDEVO reduces the memory footprint by 11.5x (to 63.8 MB) and the number of operations per frame by 29.7x (to 5.2 billion MACs per frame) compared to DEVO, while maintaining an average trajectory error of 27 cm, i.e., only 19 cm higher than DEVO, on three state-of-the-art datasets. Our work demonstrates, for the first time, the feasibility of an event-based VO pipeline on ultra-low-power devices.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TinyDEVO** 的轻量级深度学习模型，旨在将先进的基于事件的视觉里程计（VO）算法部署在资源极度受限的超低功耗（ULP）微控制器上。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的最先进（SoA）深度事件视觉里程计模型（如 DEVO）虽然在鲁棒性和精度上表现优异，但其计算和内存开销巨大（如 DEVO 需要 733 MB 内存和每帧 155 GMAC 的计算量），导致其只能在高性能 GPU 上运行，无法应用于功耗预算低于 100 mW 的微型机器人或可穿戴设备。
*   **研究动机**：事件相机具有高动态范围、低延迟和无运动模糊的特性，非常适合嵌入式视觉。研究者希望通过模型压缩和架构优化，首次在超低功耗多核 RISC-V 微控制器上实现端到端的深度事件 VO 流水线。

### 2. 方法论
TinyDEVO 的核心思想是通过**模型架构精简**和**超参数调优**来平衡精度与资源消耗：
*   **架构优化**：
    *   **Patchifier（特征提取器）优化**：减少匹配特征（MF）和上下文特征（CF）的通道数；移除旁路连接（Bypass connections）；移除较小的 MF 张量（PYR 分支）以减小内存占用。
    *   **Update Block（更新模块）优化**：将复杂的门控循环单元（GRU）替换为轻量级的归一化层+全连接层组合；评估并移除 Softmax 聚合（SA）等高耗时操作。
*   **补丁图（Patch Graph）优化**：
    *   通过调整补丁数量（$N_{patches}$）、移除窗口（$R_w$）和补丁寿命（$PLT$）等超参数，显著减少图中的边数（$N_{edges}$），从而降低计算复杂度。
*   **混合精度部署**：在 GAP9 微控制器上，将卷积层量化为 **int8**，将几何计算块（相关性块和束调整）量化为 **FP16/BF16**，以利用硬件加速器并节省内存。

### 3. 实验设计
*   **数据集**：使用了三个主流的真实世界事件数据集：**MVSEC**（室内序列）、**HKU**（无人机序列）和 **RPG**（手持/无人机序列）。
*   **Benchmark（基准）**：以原始的 **DEVO** 模型作为主要对比对象。
*   **对比方法**：
    *   深度学习方法：DEVO。
    *   几何方法：ORB-SLAM3（基于帧的 SoA）、EVO（基于事件的 SoA）。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA GH200 GPU** 进行训练，训练迭代次数为 **180k 次**，批大小（Batch size）为 1。
*   **部署硬件**：目标平台为 **GreenWaves GAP9 SoC**（包含 1 个 RISC-V 核心作为控制器和 9 个 RISC-V 核心组成的计算集群，带有 NE16 神经网络加速器）。
*   **运行参数**：主频 370 MHz，电压 0.8 V。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的消融实验，包括通道数缩减实验、架构组件（TC, PYR, SA, GRU）移除实验，以及针对补丁图超参数的帕累托前沿（Pareto front）分析。
*   **充分性评价**：实验覆盖了不同运动尺度（10m 到 100m）和不同分辨率的传感器。通过帕累托分析确定了最佳的精度-速度平衡点（Knee point），实验设计逻辑严密，对比公平（均在相同数据集和预处理条件下进行）。

### 6. 主要结论与发现
*   **性能飞跃**：TinyDEVO 相比 DEVO，内存占用减少了 **11.5 倍**（降至 63.8 MB），计算量减少了 **29.7 倍**（降至 5.2 GMAC/frame）。
*   **精度损失可控**：在三个数据集上的平均轨迹误差（ATE）为 27 cm，仅比原始 DEVO 高出约 19 cm，且在 RPG 数据集上优于传统的几何方法（如 EVO）。
*   **功耗极低**：在 GAP9 上实现了 **1.2 FPS** 的吞吐量，平均功耗仅为 **86 mW**（含外部 RAM），每帧能耗为 79 mJ。

### 7. 优点
*   **首创性**：首次证明了在亚 100 mW 级别的超低功耗 MCU 上运行端到端深度事件 VO 的可行性。
*   **端到端优化**：不仅优化了神经网络，还对几何计算块进行了混合精度量化和硬件适配。
*   **实用性强**：针对内存瓶颈（如 L3 访问延迟）进行了 DMA 传输与计算重叠的优化。

### 8. 不足与局限
*   **实时性尚存差距**：虽然达到了 1.2 FPS，但对于高速运动的机器人（如快速飞行的无人机）来说，这一帧率仍然较低，属于“软实时”范畴。
*   **精度折损**：尽管误差增加在厘米级，但在长距离或复杂环境下，累积误差可能会比原模型更快发散。
*   **应用限制**：目前主要验证了室内和特定数据集场景，在极端动态或超大规模室外环境下的泛化能力仍需进一步验证。

（完）
