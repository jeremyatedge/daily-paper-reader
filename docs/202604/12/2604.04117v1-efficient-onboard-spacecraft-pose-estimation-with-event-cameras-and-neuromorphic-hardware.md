---
title: Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware
title_zh: 基于事件相机与类脑硬件的高效星载航天器位姿估计
authors: "Arunkumar Rathinam, Jules Lecomte, Jost Reelsen, Gregor Lenz, Axel von Arnim, Djamila Aouada"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04117v1"
tags: ["query:aiot"]
score: 9.0
evidence: 航天器类脑硬件上的高能效推理
tldr: 针对航天器交会对接中极端光照和高速运动导致的视觉感知难题，本文提出了一种结合事件相机与类脑芯片（BrainChip Akida）的6自由度位姿估计方案。研究利用SPADES数据集训练了轻量化MobileNet关键点回归网络，通过量化感知训练将其转换为脉冲神经网络（SNN），并在Akida硬件上实现了低功耗、低延迟的实时推理，为未来自主空间任务提供了高效的感知路径。
selection_source: fresh_fetch
motivation: 传统相机在空间极端光照和高速运动下易失效，需探索基于事件相机和类脑计算的高效位姿估计方案。
method: 采用轻量化MobileNet架构进行关键点回归，结合量化感知训练与脉冲神经网络转换，并在Akida V1/V2硬件上部署。
result: 在Akida硬件上成功实现了实时、低功耗的位姿估计推理，且热图模型在Akida V2上表现出更高的精度。
conclusion: 本研究首次在Akida硬件上完成了端到端的航天器位姿估计演示，证明了类脑计算在自主空间任务中的实用性。
---

## 摘要
可靠的相对位姿估计是实现自主交会对接与近距离操作的关键使能技术，然而，由于极端的照明条件、高对比度以及目标的快速运动，空间成像面临着众所周知的挑战。事件相机提供异步、变化驱动的测量，在基于帧的图像发生饱和或模糊时仍能保持信息丰富，而类脑处理器则可以利用稀疏激活实现低延迟、高能效的推理。本文提出了一种将基于事件的视觉与 BrainChip Akida 类脑处理器相结合的航天器 6 自由度（6-DoF）位姿估计流水线。利用 SPADES 数据集，我们在轻量级事件帧表示上训练了紧凑的 MobileNet 风格关键点回归网络，应用了量化感知训练（8/4 位），并将模型转换为与 Akida 兼容的脉冲神经网络。我们对三种事件表示进行了基准测试，并在 Akida V1 硬件上演示了实时、低功耗推理。此外，我们还针对 Akida V2 设计了一个基于热图的模型，并在 Akida 云端进行了评估，从而提升了位姿精度。据我们所知，这是首次在 Akida 硬件上运行航天器位姿估计的端到端演示，为未来自主空间任务实现低延迟、低功耗感知提供了一条切实可行的路径。

## Abstract
Reliable relative pose estimation is a key enabler for autonomous rendezvous and proximity operations, yet space imagery is notoriously challenging due to extreme illumination, high contrast, and fast target motion. Event cameras provide asynchronous, change-driven measurements that can remain informative when frame-based imagery saturates or blurs, while neuromorphic processors can exploit sparse activations for low-latency, energy-efficient inferences. This paper presents a spacecraft 6-DoF pose-estimation pipeline that couples event-based vision with the BrainChip Akida neuromorphic processor. Using the SPADES dataset, we train compact MobileNet-style keypoint regression networks on lightweight event-frame representations, apply quantization-aware training (8/4-bit), and convert the models to Akida-compatible spiking neural networks. We benchmark three event representations and demonstrate real-time, low-power inference on Akida V1 hardware. We additionally design a heatmap-based model targeting Akida V2 and evaluate it on Akida Cloud, yielding improved pose accuracy. To our knowledge, this is the first end-to-end demonstration of spacecraft pose estimation running on Akida hardware, highlighting a practical route to low-latency, low-power perception for future autonomous space missions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（SWaP：尺寸、重量和功耗）的航天器上，实现鲁棒且高效的相对位姿估计。
*   **研究背景**：
    *   **环境挑战**：空间环境存在极端光照（强光、深影）、高对比度和快速运动，传统基于帧的相机易产生饱和或运动模糊。
    *   **硬件限制**：航天器算力有限，传统深度学习模型功耗高、延迟大。
    *   **解决方案**：引入**事件相机**（捕捉亮度变化，动态范围高，无模糊）和**类脑（Neuromorphic）处理器**（利用稀疏激活实现低功耗推理）。

### 2. 方法论
论文提出了一个端到端的混合位姿估计流水线，核心步骤如下：
*   **事件表示（Event Representations）**：将异步事件流转化为三种轻量级张量：
    *   **E2F (Event-to-Frame)**：记录每个像素最后一次事件的极性。
    *   **2DHist (2D Histogram)**：累积像素点上的事件计数。
    *   **LNES (Locally-Normalized Event Surfaces)**：通过归一化时间戳保留粗略的时间顺序。
*   **关键点回归网络**：
    *   **Akida V1 模型**：采用 MobileNet 风格架构，直接回归 8 个关键点的 2D 坐标（16 个标量）。
    *   **Akida V2 模型**：采用编码器-解码器架构，输出 8 个关键点的空间热图（Heatmap），增强局部定位的鲁棒性。
*   **量化与转换**：
    *   使用**量化感知训练 (QAT)**，将模型压缩至 4 位（V1）或 8 位（V2）精度。
    *   利用 BrainChip 工具链将 CNN 转换为**脉冲神经网络 (SNN)**，以适配 Akida 硬件。
*   **位姿恢复**：通过 PnP 求解器结合预测的 2D 关键点与 3D 模型，计算 6-DoF 位姿。

### 3. 实验设计
*   **数据集**：使用 **SPADES 数据集**（包含 17.9 万张合成事件帧），涵盖了 300 条独特的航天器运动轨迹。
*   **基准测试（Benchmark）**：
    *   **精度指标**：平移误差 ($E_T$)、旋转误差 ($E_R$)、SPEED 综合得分、关键点正确率 (PCK)。
    *   **硬件对比**：将 Akida V1 (AKD1000 SoC) 和 Akida V2 (云端模拟) 与 **NVIDIA Jetson Orin Nano** 进行对比。
*   **对比维度**：不同事件表示（E2F vs 2DHist vs LNES）、不同精度（浮点 vs 量化）、不同架构（坐标回归 vs 热图）。

### 4. 资源与算力
*   **训练资源**：使用了多 GPU 并行策略（MirroredStrategy）进行训练。文中未明确指出具体的 GPU 型号（如 A100 或 RTX 3090），但提到了训练时长：V1 模型训练了 300 个 Epoch，而 V2 热图模型仅需 60 个 Epoch 即可收敛。
*   **推理算力**：
    *   **Akida V1**：运行频率未详述，吞吐量为 17 FPS。
    *   **Akida V2**：在 25MHz 模拟频率下为 2.43 FPS，预计在 400MHz 硬件上可达 **37.21 FPS**。
    *   **功耗**：Akida V1 的动态功耗约为 **920mW**，约为 Jetson Orin Nano (约 2500mW) 的 **1/3**。

### 5. 实验数量与充分性
*   **实验规模**：使用了超过 10 万个样本进行训练，3.5 万个样本进行测试，样本量充足。
*   **消融实验**：对比了三种不同的事件表示对量化敏感度的影响，以及 V1 和 V2 架构在处理量化噪声时的表现差异。
*   **客观性**：实验涵盖了从浮点模型到量化模型再到硬件部署的全过程，并定义了“位姿拒绝（Rejection）”标准来处理无效解，评估较为客观。

### 6. 主要结论与发现
*   **热图优于坐标回归**：在类脑硬件上，热图回归（V2）比直接坐标回归（V1）对量化噪声的容忍度更高，精度损失极小。
*   **功耗优势显著**：Akida 硬件在维持实时推理的同时，功耗比通用边缘 AI 平台（Jetson）降低了 3-4 倍。
*   **表示形式的影响**：E2F 和 2DHist 在低比特量化下比 LNES 更稳定；但在高容量模型（V2）中，LNES 能提供更丰富的时域信息。

### 7. 优点
*   **首创性**：首次在 BrainChip Akida 类脑硬件上实现了端到端的航天器位姿估计演示。
*   **硬件感知设计**：并非简单的模型移植，而是针对类脑芯片的稀疏性和低比特特性优化了网络架构（如热图分支）。
*   **实用性强**：针对航天器 SWaP 约束提供了明确的技术路径，且模型已具备一定的空间飞行遗产（Akida 曾进入近地轨道）。

### 8. 不足与局限
*   **数据局限性**：实验主要基于**合成数据**（Synthetic），虽然 SPADES 包含真实数据，但本文评估部分未包含真实场景的泛化测试。
*   **V1 性能瓶颈**：Akida V1 在 4 位量化下精度下降严重（PCK 从 93% 降至 30%），限制了其在高性能任务中的应用。
*   **接口瓶颈**：目前 Akida 芯片与主机的接口尚未优化，导致其实际推理速度（17 FPS）远低于硬件理论上限，未能充分发挥事件驱动的超低延迟优势。

（完）
