---
title: Constraint-Driven Warm-Freeze for Efficient Transfer Learning in Photovoltaic Systems
title_zh: 约束驱动的预热-冻结方法：用于光伏系统高效迁移学习
authors: "Yasmeen Saeed, Ahmed Sharshar, Mohsen Guizani"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05807v1"
tags: ["query:aiot"]
score: 9.0
evidence: 资源受限边缘控制器上深度学习的预算感知适配
tldr: 针对光伏系统边缘设备在检测网络攻击时面临的计算资源受限问题，本文提出了一种名为CDWF的预算感知型迁移学习框架。该方法通过简短的预热阶段评估模型块的重要性，并利用约束优化动态分配全量训练与LoRA适配，在保持高性能的同时显著降低了可训练参数量，为资源受限环境下的高效模型迁移提供了新方案。
selection_source: fresh_fetch
motivation: 传统的深度学习微调方法在光伏边缘控制器上计算开销过大，且现有参数高效微调方法缺乏针对硬件预算的灵活性。
method: 提出CDWF框架，通过梯度量化块重要性并求解约束优化问题，动态组合全量微调与低秩自适应（LoRA）。
result: "在视觉基准和光伏攻击数据集上，CDWF仅需1/120的可训练参数即可达到全量微调90%至99%的性能。"
conclusion: CDWF是一种高效的重要性引导型迁移学习方案，能够在严格的硬件约束下实现可靠的光伏系统网络攻击检测。
---

## 摘要
检测光伏（PV）监测和最大功率点跟踪（MPPT）控制信号中的网络攻击，需要模型对偏差、漂移和瞬态尖峰具有鲁棒性，同时又要足够轻量，以适配资源受限的边缘控制器。虽然深度学习的表现优于传统的基于物理的诊断和手工特征，但标准的微调对于边缘设备而言计算成本过高。此外，现有的参数高效微调（PEFT）方法通常采用统一的适配方式，或依赖昂贵的架构搜索，缺乏遵守严格硬件预算的灵活性。为了弥补这一差距，我们提出了约束驱动的预热-冻结（CDWF），这是一种预算感知的适配框架。CDWF 利用简短的预热阶段来量化基于梯度的块重要性，随后通过求解约束优化问题，动态地为高影响块分配完全可训练性，同时通过低秩自适应（LoRA）高效地适配其余块。我们在标准视觉基准数据集（CIFAR-10/100）和一种新型光伏网络攻击数据集上评估了 CDWF，实现了从偏差预训练到漂移和尖峰检测的迁移。实验表明，CDWF 在保留 90% 至 99% 全量微调性能的同时，将可训练参数减少了高达 120 倍。这些结果证明 CDWF 是一种有效的、基于重要性引导的解决方案，适用于严苛边缘约束下的可靠迁移学习。

## Abstract
Detecting cyberattacks in photovoltaic (PV) monitoring and MPPT control signals requires models that are robust to bias, drift, and transient spikes, yet lightweight enough for resource-constrained edge controllers. While deep learning outperforms traditional physics-based diagnostics and handcrafted features, standard fine-tuning is computationally prohibitive for edge devices. Furthermore, existing Parameter-Efficient Fine-Tuning (PEFT) methods typically apply uniform adaptation or rely on expensive architectural searches, lacking the flexibility to adhere to strict hardware budgets. To bridge this gap, we propose Constraint-Driven Warm-Freeze (CDWF), a budget-aware adaptation framework. CDWF leverages a brief warm-start phase to quantify gradient-based block importance, then solves a constrained optimization problem to dynamically allocate full trainability to high-impact blocks while efficiently adapting the remaining blocks via Low-Rank Adaptation (LoRA). We evaluate CDWF on standard vision benchmarks (CIFAR-10/100) and a novel PV cyberattack dataset, transferring from bias pretraining to drift and spike detection. The experiments demonstrate that CDWF retains 90 to 99% of full fine-tuning performance while reducing trainable parameters by up to 120x. These results establish CDWF as an effective, importance-guided solution for reliable transfer learning under tight edge constraints.

---

## 论文详细总结（自动生成）

### 论文总结：约束驱动的预热-冻结方法（CDWF）

#### 1. 核心问题与整体含义
随着光伏（PV）系统规模的扩大，针对传感器信号（如MPPT控制信号）的偏差、漂移和尖峰等网络攻击威胁日益严重。虽然深度学习在检测这些攻击方面表现优异，但其庞大的计算开销难以适配资源受限的边缘控制器。现有的参数高效微调（PEFT）方法（如LoRA）通常采用统一的适配策略，无法根据具体的硬件预算灵活调整。本文提出了 **CDWF (Constraint-Driven Warm-Freeze)** 框架，旨在在严格的硬件参数预算下，通过动态分配模型块的训练权重，实现高效且高精度的迁移学习。

#### 2. 方法论
CDWF 的核心思想是“按需分配”，即识别模型中对任务贡献最大的部分进行全量微调，其余部分则使用轻量化的 LoRA 或直接冻结。其流程包含六个阶段：
*   **简短预热（Warm-Start）：** 先进行几个 Epoch 的全量训练，以获取针对新任务的梯度信号。
*   **梯度重要性评估：** 计算每个模型块（Block）的梯度范数，量化其对目标任务的重要性。
*   **预测精度模型：** 利用预热阶段的基准精度和重要性得分，建立一个简单的线性模型来预测不同配置（哪些块全量微调、哪些用 LoRA）下的最终精度。
*   **约束优化：** 在用户定义的参数预算 $f_{max}$ 约束下，求解一个优化问题，寻找预测精度最高的配置方案。
*   **配置应用与微调：** 根据优化结果调整模型结构，对选定的高影响块保持全量训练，其余块应用 LoRA 适配，最后完成剩余的微调。

#### 3. 实验设计
*   **数据集：**
    *   **光伏网络攻击数据集：** 作者模拟生成的包含偏差（Bias）、漂移（Drift）和尖峰（Spike）攻击的 1D 时间序列数据。
    *   **视觉基准数据集：** CIFAR-10 和 CIFAR-100，用于验证方法的通用性。
*   **基准模型（Backbones）：** 1D ResNet（用于光伏数据）、ResNet-50、ResNet-18、ResNet-101 以及 ViT-B/16。
*   **对比方法：** 全量微调（Full Fine-Tuning）、统一 LoRA（Uniform LoRA）以及其他 PEFT 变体（DoRA, AdaLoRA）。

#### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA RTX 5000 Ada Generation GPU**。
*   **训练配置：** 总训练预算通常限制在 10 个 Epoch（例如 3 个预热 Epoch + 7 个微调 Epoch）。
*   **算力说明：** 论文强调了该方法在边缘设备上的适用性，通过减少可训练参数量（最高减少 120 倍）来降低内存和计算需求。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **跨领域验证：** 涵盖了 1D 时间序列（光伏）和 2D 图像（CIFAR）任务。
*   **消融实验：** 研究了 LoRA 秩（Rank）的影响、预热时长对性能的敏感性分析。
*   **架构泛化性：** 在不同深度和类型的 CNN 及 Transformer 架构上进行了测试。
*   **充分性评价：** 实验设计较为全面，通过固定随机种子和匹配参数预算，确保了与基准方法对比的公平性。

#### 6. 主要结论与发现
*   **性能保持：** CDWF 在仅使用极少量可训练参数的情况下，保留了全量微调 90%–99% 的性能。
*   **效率提升：** 在光伏尖峰攻击检测中，参数量减少了 120 倍，且 AUC 表现优于统一 LoRA。
*   **预算感知：** CDWF 能够根据设定的参数预算（如 2%、5%、10%）自动调整模型架构，证明了块级选择比单纯的秩调整更有效。
*   **预热的重要性：** 实验表明，将部分训练预算分配给预热阶段以获取准确的梯度信号，比单纯增加微调 Epoch 更有助于提升最终精度。

#### 7. 优点
*   **灵活性高：** 能够根据明确的硬件预算（参数百分比）自动定制模型。
*   **重要性引导：** 避免了盲目在所有层应用 LoRA，将有限的计算资源集中在关键的特征提取块上。
*   **通用性强：** 实验证明该方法在 CNN 和 ViT 架构上均表现稳健。

#### 8. 不足与局限
*   **校准依赖：** 预测模型需要一次离线的全量微调运行来获取参考精度（$A_{ref}$），这在某些完全未知的场景下可能增加前期开销。
*   **启发式因子：** LoRA 的效率因子 $\eta(r)$ 采用了简单的启发式设置，可能无法完美捕捉所有任务中的非线性变化。
*   **边缘硬件实测：** 虽然论文针对边缘场景设计，但实验主要在高性能 GPU 上模拟完成，缺乏在真实低功耗嵌入式芯片（如 ARM Cortex-M 系列）上的功耗和延迟实测数据。

（完）
