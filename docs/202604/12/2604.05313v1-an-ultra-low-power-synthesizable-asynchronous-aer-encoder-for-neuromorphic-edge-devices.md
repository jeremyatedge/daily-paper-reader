---
title: An Ultra-Low-Power Synthesizable Asynchronous AER Encoder for Neuromorphic Edge Devices
title_zh: 一种用于类脑边缘设备的超低功耗可综合异步 AER 编码器
authors: "Yihui Wang, Sheng-Yu Peng, Sahil Shah"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05313v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于类脑边缘设备的超低功耗AER编码器
tldr: 本文提出了一种面向神经形态边缘设备的全可综合树状异步AER编码器。该设计采用捆绑数据协议和半解耦微流水线架构，通过使用标准边沿触发寄存器替代传统锁存器，实现了与商业EDA工具流的完全兼容。在65nm CMOS工艺下，该编码器在保持435 fJ/event超低功耗的同时，达到了33 MEvent/s的高吞吐量，为可扩展神经形态系统提供了高效的异步通信解决方案。
selection_source: fresh_fetch
motivation: 针对传统异步AER编码器难以适配标准数字EDA流程及在大规模神经形态系统中扩展性差的问题。
method: 采用基于标准单元的树状架构，结合交叉耦合NAND仲裁器和边沿触发寄存器构建半解耦微流水线，实现全自动综合与布局布线。
result: 在65nm工艺下实现的8事件原型达到了33 MEvent/s的峰值吞吐量，平均延迟为50 ns，单位事件能耗仅为435 fJ。
conclusion: 该研究验证了全可综合异步电路在神经形态硬件中的高效性，实现了极低功耗与高性能的平衡。
---

## 摘要
本文提出了一种全可综合的、基于树结构的地址事件表示（AER）编码器，专为可扩展的类脑计算系统设计。为了在保持与商业 EDA 工作流严格兼容的同时实现高吞吐量，该异步设计在半解耦微流水线中采用了捆绑数据协议。该架构使用标准边沿触发触发器取代了传统的透明锁存器，从而能够使用 Cadence 工具套件进行数字综合和布局布线（PnR）。每个树节点的编码器中都嵌入了一个基于交叉耦合 NAND 的随机优先级仲裁器，以高效解决事件冲突。采用纯数字标准单元流程，在 65 nm CMOS 工艺下制造了一个 8 事件 AER 原型。流片后的芯片实测验证了该设计，结果显示其峰值吞吐量为 33 MEvent/s，平均事件延迟为 50 ns，相当于 17 ns/(event-bit) 的传播延迟。该设计每个编码事件的功耗仅为 435 fJ。

## Abstract
This paper presents a fully synthesizable, treebased Address-Event Representation (AER) encoder designed for scalable neuromorphic computing systems. To achieve high throughput while maintaining strict compatibility with commercial EDA workflows, the asynchronous design employs a bundled-data protocol within a semi-decoupled micropipeline. The architecture replaces traditional transparent latches with standard edge-triggered flip-flops, enabling digital synthesis and place-and-route (PnR) using Cadence toolkits. A cross-coupled NAND-based random-priority arbiter is embedded within the encoder of each tree node to resolve event collisions efficiently. An 8-event AER prototype is fabricated in 65 nm CMOS technology utilizing a purely digital standard-cell flow. Post-fabrication silicon measurements validate the design, demonstrating a peak throughput of 33 MEvent/s and an average event latency of 50 ns, equating to a propagation delay of 17 ns/(event-bit). The design consumes only 435 fJ per encoded event.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于类脑边缘设备的超低功耗可综合异步 AER 编码器

#### 1. 核心问题与研究动机
*   **核心问题**：在类脑（神经形态）计算中，如何高效地路由数百万神经元产生的稀疏且具有突发性的脉冲信号。
*   **研究背景**：传统的同步互连架构由于全局时钟的存在，在处理稀疏流量时功耗极高且存在延迟瓶颈。异步地址事件表示（AER）协议虽然能通过局部握手解决功耗问题，但现有的异步设计通常依赖全定制电路或实验性 EDA 工具，难以集成到标准的商业数字芯片设计流程（EDA）中，限制了其在大规模系统中的可扩展性。
*   **研究动机**：开发一种既能保持异步电路高能效优势，又能完全兼容商业 EDA 工具流（如 Cadence）的可综合 AER 编码器。

#### 2. 方法论
*   **核心思想**：采用基于标准单元的树状架构，利用“捆绑数据协议”（Bundled-data Protocol）和“半解耦微流水线”（Semi-decoupled Micropipeline）实现异步逻辑。
*   **关键技术细节**：
    *   **标准寄存器替代锁存器**：放弃了传统异步设计中常用的透明锁存器，改用标准边沿触发触发器（Flip-Flops），这使得电路可以直接通过标准数字综合工具进行逻辑综合和布局布线（PnR）。
    *   **四阶段握手协议**：采用四阶段协议，在握手信号的上升沿捕获数据，并将组合逻辑延迟隐藏在握手信号的“归零”阶段，从而提高吞吐量。
    *   **随机优先级仲裁器**：在每个树节点嵌入基于交叉耦合 NAND 门的仲裁器。当多个事件同时到达时，利用硅片的物理噪声和失配进行随机仲裁，模拟生物神经系统的随机性并防止系统死锁。
    *   **自动化时序闭合方法**：开发了一套基于 Shell 脚本的迭代时序约束方法。通过标准 SDC 约束和后仿真 SDF 反标，动态调整控制路径中的延迟单元，确保在无全局时钟的情况下满足建立时间要求。

#### 3. 实验设计
*   **实现场景**：在 **65 nm CMOS** 工艺下设计并制造了一个 **8 事件（8-event）** 的 AER 编码器原型芯片。
*   **测试平台**：
    *   使用 **Xilinx Spartan-7 FPGA** 作为激励源，向测试芯片注入高速全扫描事件流。
    *   使用 **Digilent Analog Discovery** 逻辑分析仪监测异步控制线和输出地址数据。
*   **Benchmark（对比对象）**：
    *   **Purohit (2022)**：基于自定义 EDA 的现场可编程编码器。
    *   **Ouyang (2024)**：基于标准单元的掩模拓扑异步电路（仅仿真数据）。
    *   **Bi-AER (2018)**：采用 28 nm 工艺的全定制双向 AER 转接模块。

#### 4. 资源与算力
*   **算力说明**：本文属于集成电路（IC）设计论文，不涉及深度学习模型的 GPU 训练算力。
*   **设计工具**：使用了 Cadence EDA 工具链，包括 Genus（逻辑综合）、Innovus（布局布线）、Virtuoso（版图集成）和 Xcelium（仿真）。
*   **硬件规格**：芯片核心面积为 $65.57 \mu m \times 44.6 \mu m$。

#### 5. 实验数量与充分性
*   **实验规模**：论文对制造出的 65 nm 硅片进行了实测。实验通过 1 秒的测量窗口对所有捕获事件的延迟和功耗进行了平均化处理。
*   **充分性评价**：
    *   **客观性**：提供了流片后的实测数据（Silicon Measurement），而非单纯的软件仿真，具有极高的说服力。
    *   **公平性**：在对比表中明确标注了哪些是实测数据，哪些是仿真数据，对比维度包括吞吐量、单位比特延迟和单位比特能耗，评价指标全面。

#### 6. 主要结论与发现
*   **高性能**：实测峰值吞吐量达到 **33 MEvent/s**，平均事件延迟为 **50 ns**（折合每比特地址传播延迟 17 ns）。
*   **极低功耗**：在 1.2V 电源下，总功耗仅为 **8.7 $\mu$W**，单位事件能耗低至 **435 fJ**。
*   **能效领先**：单位比特能耗（145 fJ/event-bit）显著优于现有的全定制设计（如 Bi-AER 的 420 fJ）和自定义 EDA 设计。
*   **结论**：该设计证明了通过标准数字流程实现的异步电路，在性能和能效上完全可以满足甚至超越生物及人工视觉传感器的需求。

#### 7. 优点
*   **易于集成**：完全兼容商业 EDA 流程，降低了异步电路的设计门槛，便于在大规模 SoC 中快速部署。
*   **超高能效**：435 fJ/event 的能效极佳，非常适合电池供电的边缘 AI 设备和脑机接口。
*   **可扩展性**：采用参数化 Verilog 描述和树状架构，可以轻松扩展到更多输入通道的大规模系统。

#### 8. 不足与局限
*   **原型规模较小**：本次流片仅验证了 8 事件的小规模原型，虽然理论上可扩展，但在更大规模（如数百万神经元）下的布线拥塞和时序收敛挑战尚未在实测中体现。
*   **延迟权衡**：为了实现“可综合性”而放弃了全定制晶体管优化，导致其每比特延迟（17 ns）远高于 28 nm 全定制设计（0.19 ns）。虽然对于目前的生物应用足够，但在极高性能计算场景下可能存在限制。
*   **仲裁随机性**：基于亚稳态的随机仲裁虽然具有生物合理性，但在某些需要严格确定性排序的工业应用中可能不适用。

（完）
