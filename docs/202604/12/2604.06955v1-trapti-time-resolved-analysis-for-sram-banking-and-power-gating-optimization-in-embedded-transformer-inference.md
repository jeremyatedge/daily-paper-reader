---
title: "TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference"
title_zh: TRAPTI：嵌入式 Transformer 推理中 SRAM 分体与电源门控优化的时间分辨分析
authors: "Jan Klhufek, Alberto Marchisio, Vojtech Mrazek, Lukas Sekanina, Muhammad Shafique"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06955v1"
tags: ["query:aiot"]
score: 10.0
evidence: 嵌入式Transformer推理的电源门控优化
tldr: 针对嵌入式Transformer推理中KV缓存带来的内存与功耗挑战，本文提出TRAPTI框架。该框架通过周期级仿真获取内存占用轨迹，并结合离线优化流程探索SRAM分块与电源门控策略。通过对比GPT-2 XL与DeepSeek-R1模型，验证了GQA机制在降低峰值内存占用及提升能效方面的显著优势，为嵌入式硬件设计提供了量效平衡的量化指导。
selection_source: fresh_fetch
motivation: Transformer推理中KV缓存随序列长度增长带来的内存压力，严重制约了其在资源受限嵌入式设备上的部署效率。
method: 提出一种结合周期级仿真与时间解析分析的两阶段方法，用于优化SRAM分块组织和电源门控配置。
result: 在相同加速器配置下，采用GQA的DeepSeek-R1模型比采用MHA的GPT-2 XL降低了2.72倍的峰值片上内存占用。
conclusion: 通过时间解析分析可以有效挖掘GQA等机制的节能潜力，为嵌入式AI芯片的内存架构设计提供关键优化依据。
---

## 摘要
Transformer 神经网络在语言和视觉任务中达到了最先进的准确率，但其在嵌入式硬件上的部署受到严格的面积、延迟和能耗限制的阻碍。在推理过程中，性能和效率日益受到键值（KV）缓存的主导，其内存占用随序列长度增加而增长，给片上内存利用率带来了压力。尽管与多头注意力（MHA）相比，分组查询注意力（GQA）等现有机制减少了 KV 缓存需求，但要有效利用这种减少，需要了解片上内存需求随时间演变的情况。本研究提出了 TRAPTI，这是一种两阶段方法论，它将周期级推理仿真与片上内存占用的时间分辨分析相结合，以指导设计决策。在第一阶段，该框架从仿真中获取内存占用追踪和内存访问统计数据。在第二阶段，该框架利用这些追踪数据，在离线优化流程中探索分体式内存组织和电源门控配置。我们将该方法应用于相同加速器配置下的 GPT-2 XL 和 DeepSeek-R1-Distill-Qwen-1.5B，从而实现了 MHA 和 GQA 内存特性的直接对比。分析表明，在此设置下，与 GPT-2 XL 相比，DeepSeek-R1-Distill-Qwen-1.5B 的峰值片上内存利用率降低了 2.72 倍，为电源门控优化释放了更多机会。

## Abstract
Transformer neural networks achieve state-of-the-art accuracy across language and vision tasks, but their deployment on embedded hardware is hindered by stringent area, latency, and energy constraints. During inference, performance and efficiency are increasingly dominated by the Key--Value (KV) cache, whose memory footprint grows with sequence length, straining on-chip memory utilization. Although existing mechanisms such as Grouped-Query Attention (GQA) reduce KV cache requirements compared to Multi-Head Attention (MHA), effectively exploiting this reduction requires understanding how on-chip memory demand evolves over time. This work presents TRAPTI, a two-stage methodology that combines cycle-level inference simulation with time-resolved analysis of on-chip memory occupancy to guide design decisions. In the first stage, the framework obtains memory occupancy traces and memory access statistics from simulation. In the second stage, the framework leverages the traces to explore banked memory organizations and power-gating configurations in an offline optimization flow. We apply this methodology to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under the same accelerator configuration, enabling a direct comparison of MHA and GQA memory profiles. The analysis shows that DeepSeek-R1-Distill-Qwen-1.5B exhibits a 2.72x reduction in peak on-chip memory utilization in this setting compared to GPT-2 XL, unlocking further opportunities for power-gating optimization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TRAPTI** 的两阶段框架，旨在优化嵌入式 Transformer 推理加速器中的 SRAM 分体（Banking）和电源门控（Power Gating）策略。

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 模型（尤其是大语言模型）在推理时的 KV 缓存（Key-Value Cache）随序列长度线性增长，导致巨大的片上内存压力和功耗。
*   **研究动机**：
    *   虽然 GQA（分组查询注意力）等机制减少了 KV 缓存，但现有的设计空间探索（DSE）工具通常只关注峰值内存或总量统计，缺乏**时间分辨（Time-Resolved）**的动态分析。
    *   由于内存占用随时间高度非均匀，如果能精确掌握内存占用的时间轨迹，就可以通过分体式 SRAM 并在空闲时关闭不使用的内存块（电源门控）来大幅降低静态功耗（漏电功耗）。

### 2. 方法论 (TRAPTI)
TRAPTI 采用两阶段工作流，将周期级仿真与离线优化解耦：
*   **第一阶段：仿真与内存占用分析**
    *   使用 **TransInferSim** 工具对给定的 Transformer 工作负载和加速器配置进行周期级（Cycle-level）仿真。
    *   提取**时间分辨的 SRAM 占用轨迹** $o(t)$ 和内存访问统计数据（读/写次数）。
    *   通过迭代调整确定不发生容量失效（Capacity-induced write-backs）的基准 SRAM 大小。
*   **第二阶段：SRAM 分体与电源门控探索**
    *   **分体映射**：将总容量 $C$ 划分为 $B$ 个分体。利用公式 $B_{act}(t) = \lceil \frac{o(t)}{\alpha \cdot (C/B)} \rceil$ 计算每个时刻必须保持开启的分体数量，其中 $\alpha$ 是预留余量因子（Headroom factor），用于模拟非理想的数据打包。
    *   **能耗建模**：利用 **CACTI 7** 获取不同分体配置下的访问延迟、动态能耗和漏电功率。
    *   **策略评估**：计算总能耗 $E_{tot} = E_{dyn} + E_{leak} + E_{sw}$，其中 $E_{sw}$ 是电源门控切换开销。只有当空闲时间超过“盈亏平衡点”时才执行门控。

### 3. 实验设计
*   **对比模型**：
    *   **GPT-2 XL**：代表标准的多头注意力机制（MHA）。
    *   **DeepSeek-R1-Distill-Qwen-1.5B**：代表高效的分组查询注意力机制（GQA）。
*   **硬件模板**：包含 4 个 128×128 的脉动阵列（Systolic Array），运行频率 1GHz，支持 8-bit 量化运算。
*   **实验场景**：序列长度设为 2048，对比不同 SRAM 容量（48MB 到 128MB）和分体数量（B=1 到 32）下的表现。
*   **Benchmark**：以未分体、无电源门控的配置作为基准。

### 4. 资源与算力
*   **算力说明**：论文未提及训练所需的 GPU 资源，因为其核心是**推理阶段的架构仿真**。
*   **仿真环境**：使用了 45nm 技术节点（基于 CACTI 设定）进行硬件特性评估。
*   **加速器性能**：理论峰值吞吐量为 65.5 TMAC/s。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了两种主流注意力机制（MHA vs GQA）。
    *   对分体数量（1, 2, 4, 8, 16, 32）和内存容量（16MB 步进）进行了详尽的参数扫掠。
    *   进行了多层内存层次结构（Multi-level hierarchy）的扩展实验。
*   **充分性评价**：实验设计较为充分，通过时间分辨的轨迹图直观展示了内存占用的动态变化，客观地证明了 GQA 在硬件层面的节能潜力。

### 6. 主要结论与发现
*   **内存需求差异**：在相同配置下，DeepSeek-R1 (GQA) 的峰值片上内存需求比 GPT-2 XL (MHA) 降低了 **2.72 倍**。
*   **能效提升**：通过 TRAPTI 优化的分体和电源门控，SRAM 能耗最高可降低 **78%**。
*   **GQA 的优势**：GQA 不仅减少了内存占用，还创造了更多、更长的内存空闲区间，使得电源门控的效率比 MHA 高出约 20%。
*   **分体边际效应**：当分体数量超过 16 后，能耗降低的收益开始递减，而面积开销持续增加。

### 7. 优点
*   **粒度精细**：相比传统的静态分析，时间分辨分析能捕捉到推理过程中的瞬态空闲机会。
*   **解耦设计**：两阶段方法允许在不重复进行耗时仿真的情况下，快速探索成百上千种内存组织方案。
*   **实用性强**：引入了 $\alpha$ 因子考虑实际部署中的数据打包开销，使评估结果更接近真实硬件表现。

### 8. 不足与局限
*   **调度固定**：第二阶段的优化假设第一阶段的计算调度是不变的，实际上改变内存组织可能会反过来影响调度策略。
*   **工艺节点较旧**：实验基于 45nm 工艺，虽然在学术研究中常见，但对于现代大模型推理芯片（通常采用 7nm 或更先进工艺）的漏电特性模拟可能存在偏差。
*   **模型覆盖有限**：仅对比了两个模型，未深入探讨不同稀疏化技术或更复杂的内存管理策略（如 PagedAttention）对轨迹的影响。

（完）
