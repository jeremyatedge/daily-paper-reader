---
title: "From LLM to Silicon: RL-Driven ASIC Architecture Exploration for On-Device AI Inference"
title_zh: 从大语言模型到芯片：面向端侧 AI 推理的强化学习驱动 ASIC 架构探索
authors: "Ravindra Ganti, Steve Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07526v1"
tags: ["query:aiot"]
score: 10.0
evidence: 强化学习驱动的ASIC架构与AI工作负载划分联合优化
tldr: 本研究提出一种强化学习驱动的编译器，用于联合优化3nm至28nm工艺下的端侧AI推理ASIC架构、存储层次及工作负载划分。通过将设计空间建模为统一的马尔可夫决策过程，并利用带有混合专家门控的SAC算法，实现了对网格拓扑、微架构和算子部署的自动化探索。该方法在Llama 3.1和SmolVLM上验证了其在高性能与低功耗模式下的卓越性能，且无需针对特定工艺节点进行人工调优。
selection_source: fresh_fetch
motivation: 旨在解决端侧AI推理中跨工艺节点的ASIC架构、存储层次与算子映射联合优化的复杂设计挑战。
method: 将设计空间表述为混合动作的马尔可夫决策过程，并采用带有混合专家门控机制的Soft Actor-Critic算法进行联合搜索。
result: 在3nm工艺下Llama 3.1 8B推理速度达29809 tps，而SmolVLM在各节点功耗均低于13mW，展现了极高的能效比。
conclusion: 该RL驱动框架证明了自动化探索异构ASIC架构的可行性，能够跨多个工艺节点实现性能与功耗的最优平衡。
---

## 摘要
我们提出了一种强化学习驱动的编译器，可针对 3nm 至 28nm 工艺下的 AI 推理，协同优化 ASIC 架构、存储层次结构和工作负载划分。设计空间被建模为具有离散-连续混合动作和统一功耗-性能-面积（PPA）目标的单一马尔可夫决策过程。采用带有混合专家（MoE）门控机制的软 Actor-Critic（SAC）算法，探索了 Mesh 拓扑、单核微架构和算子放置的联合空间。我们在两个工作负载上进行了验证：Llama 3.1 8B FP16（高性能模式，3nm 下每秒 29809 个 token）和 SmolVLM（低功耗模式，所有工艺节点下功耗均低于 13 mW，频率 10 MHz）。在 7 个工艺节点上，强化学习能够自动适配 Mesh 尺寸和每个 Tile 的配置，包括异构的 FETCH、VLEN 和内存分配，而无需针对特定节点进行人工重新调优。

## Abstract
We present an RL-driven compiler that jointly optimizes ASIC architecture, memory hierarchy, and workload partitioning for AI inference across 3nm to 28nm. The design space is formulated as a single Markov Decision Process with mixed discrete-continuous actions and a unified Power-Performance-Area (PPA) objective. Soft Actor-Critic (SAC) with Mixture-of-Experts gating explores the joint space of mesh topology, per-core microarchitecture, and operator placement. We validate on two workloads, Llama 3.1 8B FP16 (high-performance mode, 29809 tokens per second at 3nm) and SmolVLM (low-power mode, less than 13 mW at all nodes, 10 MHz). Across 7 process nodes, the RL automatically adapts mesh sizes and per-tile configurations, including heterogeneous FETCH, VLEN, and memory allocation without node-specific manual retuning.

---

## 论文详细总结（自动生成）

以下是对论文《From LLM to Silicon: RL-Driven ASIC Architecture Exploration for On-Device AI Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在端侧（On-Device）AI推理场景下，实现硬件架构（ASIC）与软件编译策略的**联合优化**。
*   **研究动机**：传统的ASIC设计流程通常需要数月的人工RTL编码和验证，且硬件设计与软件编译往往是孤立优化的。
*   **背景**：随着大语言模型（LLM）向端侧迁移，如何在不同工艺节点（3nm至28nm）下，针对特定模型（如Llama 3.1）快速找到最优的网格拓扑（Mesh Topology）、微架构参数（如向量长度VLEN、缓存大小）以及算子划分策略，成为提升能效比（PPA）的关键挑战。

### 2. 方法论：核心思想与技术细节
论文提出了一种**强化学习（RL）驱动的编译器框架**，将设计空间探索建模为单一的马尔可夫决策过程（MDP）。
*   **核心思想**：利用RL代理同时决定硬件配置和软件映射，实现端到端的自动化设计（从ONNX模型到生成RTL）。
*   **关键技术细节**：
    *   **算法架构**：采用**软Actor-Critic (SAC)** 算法，并引入**混合专家（MoE）门控机制**。MoE允许策略网络根据不同的算子状态（如注意力机制 vs. MLP层）切换专家策略。
    *   **状态空间（73维）**：包含工作负载特征（指令计数、算子比例）、当前硬件配置、PPA观测值、内存压力及数据冲突（Hazards）统计。
    *   **动作空间（30维连续 + 4维离散）**：离散动作控制Mesh尺寸增量；连续动作控制每个计算核心（TCC）的Fetch大小、VLEN、内存分配比例、时钟频率等。
    *   **异构Tile派生**：RL代理优化全局参数后，编译器会根据每个Tile承载的具体算子需求，自动派生异构的硬件配置（例如：内存密集型算子分配更大的权重内存WMEM）。
    *   **模型预测控制（MPC）**：在推理阶段引入世界模型（World Model）进行多步前瞻规划，辅助SAC导航复杂的参数交互空间。
    *   **PPA代理模型**：建立了一个工艺节点相关的代理模型，用于快速估计功耗、性能和面积，减少昂贵的完整仿真次数。

### 3. 实验设计
*   **数据集/场景**：
    1.  **高性能模式**：Llama 3.1 8B Instruct (FP16)，侧重吞吐量。
    2.  **低功耗模式**：SmolVLM（多模态模型），侧重极低功耗。
*   **工艺节点**：覆盖 3nm, 5nm, 7nm, 10nm, 14nm, 22nm, 28nm 共七个节点。
*   **Benchmark与对比方法**：
    *   **搜索策略对比**：将SAC与**随机搜索（Random Search）**和**网格搜索（Grid Search）**在相同时间预算下进行对比。
    *   **行业对比**：将估计结果与 NVIDIA H200/B200、Groq、SambaNova、Cerebras、Taalas 等已知推理平台的吞吐量进行对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练RL模型所使用的具体GPU型号或数量。
*   **训练时长/规模**：提到每个工艺节点的优化循环大约运行 **4,613个Episode**。单次PPA评估（包含代码生成和仿真）耗时约10ms，MPC规划过程耗时小于1ms。

### 5. 实验数量与充分性
*   **实验规模**：针对2个核心模型在7个工艺节点上进行了全量实验，总计进行了数万次Episode的探索。
*   **充分性评价**：
    *   **跨度大**：实验涵盖了从最先进的3nm到成熟的28nm工艺，验证了算法的迁移能力。
    *   **多模式**：同时验证了高性能和低功耗两种极端需求。
    *   **局限性**：实验仅针对Transformer架构模型，且缺乏多个随机种子的统计学显著性分析（如置信区间）。

### 6. 主要结论与发现
*   **卓越性能**：在3nm工艺下，Llama 3.1 8B可达到 **29,809 tok/s** 的吞吐量（基于片上ROM架构估计）。
*   **工艺增益**：从28nm迁移到3nm，吞吐量提升了约47.8倍，面积缩小了5.5倍。
*   **自动化适配**：RL能够自动为不同模型选择不同的Mesh尺寸（如Llama用41x42，SmolVLM仅用2x4），并实现异构的内存分配，显著降低了功耗。
*   **搜索效率**：SAC找到的可行配置数量是随机搜索的9.1倍，吞吐量结果高出3.5倍。

### 7. 优点与亮点
*   **全栈联合优化**：打破了硬件设计与软件编译的壁垒，实现了从模型输入到RTL输出的闭环自动化。
*   **异构性探索**：能够根据算子特征自动生成异构的Tile配置，这在传统手动设计中极难实现。
*   **工艺无关性**：通过将工艺参数参数化，同一套RL框架可直接应用于不同代际的半导体工艺。
*   **片上存储优化**：通过消除HBM（高带宽内存）并使用片上ROM，大幅降低了系统级功耗。

### 8. 不足与局限
*   **模型覆盖有限**：目前仅验证了Transformer类模型，对于CNN、扩散模型或MoE架构模型的泛化性尚待证明。
*   **拓扑限制**：目前仅支持2D Mesh拓扑，不支持环形、环面或芯粒（Chiplet）等更复杂的互连结构。
*   **偏差风险**：PPA结果基于分析模型估计，而非真实的硅片实测（Silicon-measured），可能存在模型建模误差。
*   **随机性风险**：缺乏重复实验的统计数据，RL的收敛稳定性在不同随机种子下的表现未知。

（完）
