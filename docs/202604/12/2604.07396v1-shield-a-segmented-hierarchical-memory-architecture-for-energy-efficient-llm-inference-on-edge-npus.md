---
title: "SHIELD: A Segmented Hierarchical Memory Architecture for Energy-Efficient LLM Inference on Edge NPUs"
title_zh: SHIELD：一种用于边缘端 NPU 高能效大语言模型推理的分段式分层存储架构
authors: "Jintao Zhang, Xuanyao Fong"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07396v1"
tags: ["query:aiot"]
score: 10.0
evidence: 通过存储架构在边缘NPU上实现能效LLM推理
tldr: 针对边缘端NPU在LLM推理中受限于片上内存及eDRAM高刷新功耗的问题，本文提出SHIELD架构。该架构利用BF16激活值的生命周期和位级敏感度，通过分离符号/指数位与尾数位，对瞬态QO激活值禁用尾数刷新，对持久KV缓存放宽刷新频率，在保证模型精度的前提下显著降低了能耗。
selection_source: fresh_fetch
motivation: 边缘NPU使用eDRAM存储激活值时面临巨大的定期刷新能耗，且现有研究忽视了瞬态激活值的容错特性。
method: 提出一种生命周期感知的分段eDRAM架构，将BF16数据的符号/指数与尾数分离，并根据数据类型实施差异化的刷新策略。
result: "在多个LLM场景下，SHIELD相比标准刷新基准降低了35%的eDRAM刷新能耗，且在主流基准测试中保持了模型精度。"
conclusion: 通过结合激活值的时域特性与位级敏感度，SHIELD为边缘端低功耗LLM推理提供了一种高效的片上存储优化方案。
---

## 摘要
边缘端神经网络处理器（NPU）上的大语言模型（LLM）推理从根本上受限于有限的片上存储容量。尽管高密度嵌入式动态随机存取存储器（eDRAM）在存储激活工作空间方面具有吸引力，但其周期性刷新会消耗大量能量。先前的研究主要集中在减少片外流量或优化持久性键值（KV）缓存的刷新，而瞬态且具有容错能力的查询和注意力输出（QO）激活在很大程度上被忽视了。我们提出了 SHIELD，这是一种生命周期感知的分段式 eDRAM 架构，它共同利用了 bfloat16 (BF16) 激活中的时间驻留特性和位级敏感性。SHIELD 将符号位和指数域与尾数分离，禁用了瞬态 QO 尾数的刷新，并对持久性 KV 尾数应用了松弛刷新。在多种 LLM 和推理场景下，与标准刷新基准相比，SHIELD 将 eDRAM 刷新能耗降低了 35%，同时在 WikiText-2、PIQA 和 ARC-Easy 数据集上保持了准确率。

## Abstract
Large Language Model (LLM) inference on edge Neural Processing Units (NPUs) is fundamentally constrained by limited on-chip memory capacity. Although high-density embedded DRAM (eDRAM) is attractive for storing activation workspaces, its periodic refresh consumes substantial energy. Prior work has primarily focused on reducing off-chip traffic or optimizing refresh for persistent Key-Value (KV) caches, while transient and error-resilient Query and Attention Output (QO) activations are largely overlooked. We propose SHIELD, a lifecycle-aware segmented eDRAM architecture that jointly exploits temporal residency and bit-level sensitivity in bfloat16 (BF16) activations. SHIELD isolates the sign and exponent fields from the mantissa, disables refresh for transient QO mantissas, and applies relaxed refresh to persistent KV mantissas. Across multiple LLMs and inference scenarios, SHIELD reduces eDRAM refresh energy by 35% relative to a standard-refresh baseline while preserving accuracy on WikiText-2, PIQA, and ARC-Easy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SHIELD** 的新型存储架构，旨在解决边缘端 NPU 在运行大语言模型（LLM）推理时，因 eDRAM 刷新功耗过高而导致的能效瓶颈。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：边缘端 NPU 的片上 SRAM 容量有限（通常 1-8MB），难以容纳 LLM 推理产生的大量中间激活值（如 Qwen3-8B 的 QO 空间需 32MB）。虽然高密度的 **eDRAM** 是理想替代方案，但其电荷会随时间流失，必须进行**定期刷新**，这产生了巨大的静态功耗。
*   **研究背景**：现有研究（如 Kelle）主要关注持久化的 KV Cache（键值缓存）的刷新优化，但忽视了**瞬态激活值**（Query 和 Attention Output，简称 **QO**）的刷新开销。QO 虽然生命周期短，但在推理过程中占据了显著的存储空间。

### 2. 论文提出的方法论：SHIELD 架构
SHIELD 的核心思想是**生命周期感知**与**位级敏感度**的协同设计。
*   **核心思想**：根据 BF16 数据格式中不同位域的重要性，以及激活值在时间上的驻留特性，实施差异化的存储和刷新策略。
*   **关键技术细节**：
    *   **位级分段（Bit-level Segmentation）**：将 BF16 格式（1位符号、8位指数、7位尾数）拆分。符号位和指数位对误差极度敏感，必须严格保护；尾数位则具有较高的容错性。
    *   **三级分层刷新策略**：
        1.  **标准刷新库（Standard Refresh Banks）**：存储所有激活值的**符号位和指数位**，采用标准刷新周期（45μs），确保数值稳定性。
        2.  **松弛刷新库（Relaxed Refresh Banks）**：存储持久化的 **KV 缓存尾数**。由于其生命周期长但有一定容错性，刷新周期放宽至 1216μs。
        3.  **无刷新库（Refresh-less Banks）**：存储瞬态的 **QO 尾数**。研究发现 QO 的生命周期通常小于 1.5ms，在此时间内 eDRAM 的电荷流失导致的位错误率（BER）在模型可接受范围内，因此完全禁用刷新。
*   **算法流程**：通过故障注入模拟（Algorithm 1），确定不同张量在不同 BER 下的性能表现，从而推导出最优的刷新间隔阈值。

### 3. 实验设计
*   **数据集/任务**：WikiText-2（语言建模困惑度 PPL）、PIQA（物理常识推理准确率）、ARC-Easy（科学问答准确率）。
*   **推理场景**：摘要生成（Prefill 预填充主导）、翻译（平衡）、故事创作（Decode 解码主导）。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：标准刷新的 eDRAM 架构。
    *   **Kelle**：仅针对 KV Cache 进行刷新的先前最先进（SOTA）方案。
*   **评估模型**：Qwen3 (1.7B, 4B, 8B)、Mistral-7B、Llama-3-8B。

### 4. 资源与算力
*   **算力设备**：论文提到使用单张 **NVIDIA H100 GPU** 进行激活值生命周期的系统性分析和故障注入模拟。
*   **工具软件**：使用 **DESTINY** 工具对 eDRAM 和 SRAM 的面积、漏电流及功耗进行建模。
*   **训练/推理时长**：未明确说明具体的模拟时长，但由于是推理侧的架构研究，不涉及大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流开源模型家族、3 种不同的推理负载场景以及 3 个下游任务基准测试。
*   **充分性评价**：实验设计较为充分。作者不仅验证了能效提升，还通过故障注入实验深入探讨了 BER 与模型精度之间的量化关系。对比了不同解码阶段（Prefill vs. Decode）的能效变化，证明了 SHIELD 在整个推理生命周期内的优越性。

### 6. 主要结论与发现
*   **能效提升**：相比标准刷新基准，SHIELD 将 eDRAM 刷新能耗降低了 **35%**。
*   **精度保持**：在所有测试模型和任务中，SHIELD 引起的精度下降几乎为零（$\Delta \approx 0$），表现优于更激进的刷新松弛策略。
*   **场景适应性**：在 QO 占据主导的 Prefill 阶段，SHIELD 的优势最为明显，且在 KV Cache 增长的后期依然保持高效。

### 7. 优点
*   **粒度精细**：首次将优化对象从张量级细化到位级（符号/指数 vs. 尾数），充分利用了浮点数的数值特性。
*   **软硬协同**：结合了 LLM 算法的容错特性与硬件存储器的物理特性，设计思路具有启发性。
*   **通用性强**：适用于多种 Transformer 架构的模型，不依赖特定的模型权重。

### 8. 不足与局限
*   **格式局限性**：目前仅针对 **BF16** 格式进行了优化，对于 FP8、INT8 或更低比特量化格式的适用性尚未讨论。
*   **硬件开销**：虽然降低了刷新功耗，但分段存储和位拆分（Bit-shuffling）会带来额外的存储控制器复杂度和数据移动开销，文中对此部分的硬件面积和延迟开销讨论较少。
*   **环境敏感性**：eDRAM 的电荷保持时间受温度影响极大，论文未讨论在不同工作温度下刷新策略的动态调整问题。

（完）
