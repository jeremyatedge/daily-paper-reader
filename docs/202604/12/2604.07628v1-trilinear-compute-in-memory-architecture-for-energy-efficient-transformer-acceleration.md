---
title: Trilinear Compute-in-Memory Architecture for Energy-Efficient Transformer Acceleration
title_zh: 用于高能效 Transformer 加速的三线性存内计算架构
authors: "Md Zesun Ahmed Mia, Jiahui Duan, Kai Ni, Abhronil Sengupta"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07628v1"
tags: ["query:aiot"]
score: 9.0
evidence: 使用存内计算架构实现高能效Transformer加速
tldr: 针对Transformer自注意力机制中动态操作数导致非易失性存储器（NVM）频繁重编程、降低吞吐并损耗寿命的问题，本文提出TrilinearCIM架构。该架构基于双栅极FeFET器件，利用背栅调制实现三操作数乘累加原语，无需运行时重编程即可在NVM核心内完成完整的注意力计算。实验表明，该方案在BERT和ViT模型上显著降低了功耗和延迟，是首个实现全NVM核心注意力计算的架构。
selection_source: fresh_fetch
motivation: 传统存算一体架构在处理Transformer自注意力时，动态操作数会引发高能耗且损耗器件寿命的NVM重编程循环。
method: 采用双栅极FeFET器件，通过背栅调制技术构建三操作数乘累加原语，实现无需重编程的存内注意力计算。
result: "在BERT和ViT任务中，相比传统FeFET存算架构，能耗降低达46.6%，延迟缩短20.4%，且在多数GLUE任务上表现更优。"
conclusion: TrilinearCIM通过创新的器件级调制实现了高效的全NVM存内注意力计算，有效解决了Transformer加速中的重编程瓶颈。
---

## 摘要
Transformer 中的自注意力机制会产生动态操作数，这迫使传统的存内计算（CIM）加速器进入代价高昂的非易失性存储器（NVM）重编程周期，从而降低吞吐量并加剧器件寿命压力。现有的解决方案要么通过矩阵分解或稀疏化来减少但仍保留 NVM 写入，要么以牺牲 NVM 密度为代价将注意力计算转移到数字 CMOS 中。我们提出了 TrilinearCIM，这是一种基于双栅极铁电场效应晶体管（DG-FeFET）的架构，利用背栅调制实现了用于存内注意力计算的三操作数乘累加原语，且无需动态铁电重编程。在 BERT-base (GLUE) 和 ViT-base (ImageNet 和 CIFAR) 上的评估结果显示，TrilinearCIM 在九项 GLUE 任务中的七项上表现优于传统 CIM，同时在 37.3% 的面积开销下，相比传统 FeFET CIM 实现了高达 46.6% 的能耗降低和 20.4% 的延迟改进。据我们所知，这是首个完全在 NVM 核心中执行完整 Transformer 注意力计算且无需运行时重编程的架构。

## Abstract
Self-attention in Transformers generates dynamic operands that force conventional Compute-in-Memory (CIM) accelerators into costly non-volatile memory (NVM) reprogramming cycles, degrading throughput and stressing device endurance. Existing solutions either reduce but retain NVM writes through matrix decomposition or sparsity, or move attention computation to digital CMOS at the expense of NVM density. We present TrilinearCIM, a Double-Gate FeFET (DG-FeFET)-based architecture that uses back-gate modulation to realize a three-operand multiply-accumulate primitive for in-memory attention computation without dynamic ferroelectric reprogramming. Evaluated on BERT-base (GLUE) and ViT-base (ImageNet and CIFAR), TrilinearCIM outperforms conventional CIM on seven of nine GLUE tasks while achieving up to 46.6\% energy reduction and 20.4\% latency improvement over conventional FeFET CIM at 37.3\% area overhead. To our knowledge, this is the first architecture to perform complete Transformer attention computation exclusively in NVM cores without runtime reprogramming.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **TrilinearCIM** 的创新架构，旨在解决 Transformer 模型在存内计算（CIM）加速器中面临的动态操作数重编程瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 的自注意力机制涉及 Query (Q)、Key (K) 和 Value (V) 三个矩阵的动态交互。传统的存内计算（CIM）架构基于“权重固定”假设，而自注意力中的操作数是随输入动态生成的。
*   **研究动机**：
    *   **重编程开销**：在非易失性存储器（NVM）中运行注意力计算需要频繁重写 NVM 单元，这会导致巨大的延迟、高能耗以及严重的器件寿命（Endurance）损耗。
    *   **现有方案局限**：现有方案要么将注意力计算移回数字 CMOS（牺牲了 NVM 的高密度优势），要么通过复杂的矩阵分解减少写入（但无法根除）。
    *   **目标**：实现一种“全在存储器中”（All-in-Memory）的计算模式，无需在推理过程中对 NVM 进行任何重编程。

### 2. 方法论：TrilinearCIM 架构
*   **核心思想**：利用**双栅极铁电场效应晶体管（DG-FeFET）**的物理特性，引入第三个操作数路径。
*   **关键技术细节**：
    *   **三线性原语（Trilinear Primitive）**：DG-FeFET 具有顶栅（TG）和背栅（BG）。顶栅用于存储非易失性的静态权重（如投影矩阵 $W$），漏源电压（$V_{DS}$）输入激活向量，而背栅（BG）则作为第三个操作数，通过电压调制沟道电导。
    *   **计算公式**：输出电流 $I_{DS} \approx V_{DS} \cdot G_0 \cdot (1 + \eta_{BG} \cdot V_{BG})$。其中 $G_0$ 是存储的权重，$V_{DS}$ 是输入，$V_{BG}$ 是动态调制因子。
    *   **三阶段数据流融合**：
        1.  **缩放 Query 生成**：将投影与缩放因子融合。
        2.  **分数合成（Score Synthesis）**：直接计算 $R_1 \cdot W_K \cdot X^T$，无需显式生成和存储 Key 矩阵。
        3.  **Value 聚合**：将 Softmax 结果通过背栅广播，直接与输入和 Value 权重作用。
*   **优势**：通过背栅的挥发性调制替代了铁电极化的非易失性重写，从而消除了写入延迟和寿命压力。

### 3. 实验设计
*   **模型与数据集**：
    *   **NLP 任务**：使用 BERT-base 模型，在 GLUE 基准测试（包含 CoLA, SST-2, MRPC, RTE, STS-B, WNLI, QNLI, QQP, MNLI 九项任务）上评估。
    *   **CV 任务**：使用 ViT-base 模型，在 ImageNet-1K、CIFAR-10 和 CIFAR-100 上评估。
*   **Benchmark（对比方法）**：
    *   **Digital Baseline**：理想的 8-bit 量化数字硬件。
    *   **CIM-Bilinear**：传统的单栅极 FeFET CIM 架构（推理时需频繁重写 NVM）。
*   **仿真工具**：开发了 **TransCIM** 框架（基于 NeuroSim 扩展），模拟 7nm CMOS 工艺和 22nm FeFET 器件。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练时长或型号，因为其核心贡献在于**硬件架构设计与电路级仿真**。
*   **硬件参数**：仿真采用了 7nm FinFET 逻辑工艺和 22nm FDSOI FeFET 存储工艺。配置了 4MB 的全局 SRAM 缓存，ADC 精度为 8-bit，权重为 8-bit（每个单元存储 2-bit）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 NLP 和 CV 两大领域的多个主流数据集，实验设置较为全面。
*   **消融实验**：
    *   测试了不同的子阵列（Subarray）尺寸（32x32 vs 64x64）。
    *   测试了不同的位宽组合（1b/6b 到 2b/9b 的单元/ADC 精度）。
    *   测试了序列长度（Sequence Length）对能效的影响。
*   **充分性评价**：实验设计客观、公平，通过多随机种子运行取平均值，并详细分析了面积开销与精度损失的权衡，具有较高的说服力。

### 6. 主要结论与发现
*   **能效与延迟**：相比传统 FeFET CIM，TrilinearCIM 实现了高达 **46.6% 的能耗降低**和 **20.4% 的延迟改进**。
*   **精度表现**：在 9 项 GLUE 任务中的 7 项上，TrilinearCIM 的精度优于传统 CIM 方案，证明了减少中间转换步骤有助于维持数值稳定性。
*   **吞吐量**：在 128 序列长度下，TOPS/W 提升了约 39.2%。
*   **寿命**：由于消除了推理时的 NVM 写入，理论上将器件寿命从受限状态提升到了近乎无限。

### 7. 优点与亮点
*   **首创性**：据称是首个在不进行运行时重编程的情况下，在 NVM 核心内完成完整 Transformer 注意力计算的架构。
*   **物理机制创新**：巧妙利用 DG-FeFET 的背栅调制特性解决算法中的三矩阵乘法问题，实现了硬件原语与算法逻辑的高度匹配。
*   **数据流优化**：通过操作融合减少了中间张量在全局缓冲区和 DRAM 之间的搬运，降低了约 3 倍的缓冲区压力。

### 8. 不足与局限
*   **面积开销**：由于增加了背栅驱动电路和每列 DAC，该架构带来了约 **37.3% 的额外面积负担**。
*   **视觉任务精度下降**：在 ImageNet 等视觉任务上，由于背栅量化路径对异常值（Outliers）较敏感，精度相比数字基准有明显下降（约 4.4%）。
*   **仿真依赖**：研究结果主要基于 TransCIM 仿真框架，虽然使用了实验数据校准器件参数，但仍需实际流片验证背栅调制的线性度及可靠性。
*   **应用限制**：目前主要针对 Encoder 结构优化，对于 Decoder（如 GPT 类模型）中的 KV Cache 管理尚需进一步研究。

（完）
