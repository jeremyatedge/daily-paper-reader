---
title: "DHFP-PE: Dual-Precision Hybrid Floating Point Processing Element for AI Acceleration"
title_zh: DHFP-PE：用于人工智能加速的双精度混合浮点处理单元
authors: "Shubham Kumar, Vijay Pratap Sharma, Vaibhav Neema, Santosh Kumar Vishvakarma"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04507v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于节能AI工作负载的双精度MAC单元
tldr: "针对AI和边缘计算对低功耗、高吞吐量浮点运算的需求，本文提出了一种双精度混合浮点处理单元（DHFP-PE）。该设计支持FP8和FP4格式，通过创新的位划分技术使4位乘法器可在FP8和FP4模式间灵活切换，实现了100%的硬件利用率。在28nm工艺下，该设计在面积和功耗上均显著优于现有技术，为低精度AI加速提供了高效的硬件方案。"
selection_source: fresh_fetch
motivation: AI模型向低精度演进趋势下，边缘计算急需兼具灵活性、高能效且支持多种低比特浮点格式的MAC单元。
method: 提出一种新型位划分技术，使单个4位乘法器能复用为1个4x4乘法器或2个并行的2x2乘法器，实现无逻辑冗余的多精度支持。
result: "在28nm工艺下，该引擎主频达1.94 GHz，相比最先进设计减少了60.4%的面积并节省了86.6%的功耗。"
conclusion: 该双精度混合浮点处理单元通过高效的硬件复用，在极低资源消耗下实现了高性能、高灵活性的低精度AI计算加速。
---

## 摘要
低精度算术在人工智能和边缘计算中的快速采用，对高能效且灵活的浮点乘累加（MAC）单元产生了强烈需求。本文提出了一种全流水线双精度浮点MAC处理引擎，支持FP8格式（E4M3、E5M2）和FP4格式（E2M1、E1M2），专门针对低功耗和高吞吐量的AI工作负载进行了优化。所提出的架构采用了一种新颖的位划分技术，使单个4位单元乘法器既可以作为FP8的标准4x4乘法器运行，也可以作为两个并行的2位操作数2x2乘法器运行，在不重复逻辑的情况下实现了100%的硬件利用率。该处理引擎采用28 nm工艺实现，工作频率达到1.94 GHz，面积为0.00396 mm²，功耗为2.13 mW，与现有最先进设计相比，面积减少了高达60.4%，功耗节省了86.6%。

## Abstract
The rapid adoption of low-precision arithmetic in artificial intelligence and edge computing has created a strong demand for energy-efficient and flexible floating-point multiply-accumulate (MAC) units. This paper presents a fully pipelined dual-precision floating-point MAC processing engine supporting FP8 formats (E4M3, E5M2) and FP4 formats (E2M1, E1M2), specifically optimized for low-power and high-throughput AI workloads. The proposed architecture employs a novel bit-partitioning technique that enables a single 4-bit unit multiplier to operate either as a standard 4x4 multiplier for FP8 or as two parallel 2x2 multipliers for 2-bit operands, achieving 100 percent hardware utilization without duplicating logic. Implemented in 28 nm technology, the proposed processing engine achieves an operating frequency of 1.94 GHz with an area of 0.00396 mm^2 and power consumption of 2.13 mW, resulting in up to 60.4 percent area reduction and 86.6 percent power savings compared to state-of-the-art designs.

---

## 论文详细总结（自动生成）

以下是对论文《DHFP-PE: Dual-Precision Hybrid Floating Point Processing Element for AI Acceleration》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着AI模型向边缘侧部署，传统的FP16或FP32高精度浮点运算单元在功耗、面积和内存带宽上已成为瓶颈。虽然FP8和FP4等极低精度格式逐渐兴起，但现有的硬件处理单元（PE）在处理这些细粒度数据时，往往存在硬件利用率低、逻辑冗余或无法灵活切换精度的问题。
*   **研究动机**：设计一种能够同时支持多种FP8（E4M3, E5M2）和FP4（E2M1, E1M2）格式，且在切换精度时能保持100%硬件利用率的高能效、高吞吐量MAC（乘累加）单元。

### 2. 方法论
*   **核心思想**：采用**位划分（Bit-partitioning）技术**。将一个标准的4位乘法器单元设计为可重构模式：在FP8模式下作为4×4乘法器运行；在FP4模式下，将其拆分为两个并行的2×2乘法器。
*   **关键技术细节**：
    *   **6级流水线架构**：
        *   **S0（输入处理）**：提取符号、指数和尾数，处理特殊值（如NaN、无穷大）。
        *   **S1（乘法器阵列与比较器）**：执行尾数乘法，并使用三输入比较器确定对齐基准指数。
        *   **S2（补码与对齐移位）**：进行符号校正和指数对齐。
        *   **S3（CSA加法树）**：使用4:2进位保留压缩器（CSA）进行初步累加。
        *   **S4（LZA与规格化）**：通过前导零预测器（LZA）和规格化逻辑调整结果格式。
        *   **S5（输出处理与激活函数）**：集成ReLU激活函数，增强数值稳定性。
    *   **硬件复用**：通过共享乘法器阵列和加法树，避免了为不同精度设计独立逻辑，实现了极小的硅片面积。

### 3. 实验设计
*   **实现环境**：采用 **28nm CMOS 工艺**进行 ASIC 综合，并在 **Xilinx Artix-7 FPGA** 平台上进行了资源验证。
*   **Benchmark（基准测试）**：对比了 2021 年至 2025 年间发表的 8 篇最先进（SOTA）的论文，包括 Mao et al. (DATE'21, TVLSI'22)、Li et al. (TCAS-II'24, JSSC'25) 和 Lokhande et al. (TVLSI'25) 等。
*   **对比指标**：工作频率（MHz）、功耗（mW）、面积（mm²）、吞吐量（GFLOPS）、能效比（GFLOPS/W）以及 FPGA 资源占用（LUT/FF）。

### 4. 资源与算力
*   **硬件实现**：论文侧重于硬件 IP 设计。在 28nm 工艺下，该 PE 核心面积仅为 **0.00396 mm²**，功耗为 **2.13 mW**。
*   **算力表现**：在 1.94 GHz 频率下，FP8 吞吐量为 3.88 GFLOPS，FP4 吞吐量为 7.75 GFLOPS。
*   **训练/推理说明**：文中未提及具体的 GPU 训练时长，因为其研究目标是设计底层的硬件加速器单元，而非训练特定模型。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   进行了不同工艺角（Typical Corner 和 Slow Corner）下的性能仿真，验证了设计的鲁棒性。
    *   提供了详细的各流水线阶段（S0-S5）的面积、功耗和延迟拆解分析。
    *   进行了 FPGA 资源消耗对比，显示其 LUT 占用比同类设计减少了 98% 以上。
*   **充分性评价**：实验对比了近五年的多项研究，数据维度全面（面积、功耗、频率、能效），实验结果较为客观、公平。

### 6. 主要结论与发现
*   **性能飞跃**：与现有最先进设计相比，该 PE 实现了 **60.4% 的面积缩减**和 **86.6% 的功耗节省**。
*   **频率提升**：工作频率达到 1.94 GHz，比之前最高的 SOTA 设计提升了约 31.8%。
*   **高能效**：在 FP4 模式下，能效比表现极佳，非常适合资源受限的边缘 AI 设备。

### 7. 优点
*   **极高的硬件利用率**：通过位划分技术，在 FP4 模式下实现了 100% 的硬件复用，无逻辑冗余。
*   **高度灵活性**：单单元支持四种低精度浮点格式，适应了当前 AI 推理对混合精度的需求。
*   **物理指标优异**：在 28nm 工艺下展现了极小的面积和极低的功耗，具有很强的工程实用价值。

### 8. 不足与局限
*   **精度局限**：该设计仅针对 FP8 和 FP4，不支持 FP16 或 BF16，对于需要更高动态范围的训练任务或特定算法可能不适用。
*   **舍入误差**：为了节省硬件成本，设计中采用了截断（Truncation）而非复杂的舍入（Rounding）操作，虽然减小了面积，但可能会对某些敏感模型的推理精度产生微小影响。
*   **应用场景单一**：主要针对 MAC 运算优化，未讨论在非线性函数（除 ReLU 外）或复杂内存访问模式下的表现。

（完）
