---
title: "YANA: Bridging the Neuromorphic Simulation-to-Hardware Gap"
title_zh: YANA：弥合类脑仿真到硬件之间的鸿沟
authors: "Brian Pachideh, Sven Nitzsche, Moritz Neher, Jann Krausse, Carmen Weigelt, Klaus Knobloch, Victor Pazmino Betancourt, Juergen Becker"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03432v1"
tags: ["query:aiot"]
score: 9.0
evidence: 类脑脉冲神经网络加速器的软硬件协同设计
tldr: 针对脉冲神经网络（SNN）仿真与硬件部署之间的脱节问题，本文提出了开源FPGA加速器YANA。它采用五级事件驱动流水线，支持任意拓扑结构，并利用查找表实现高效神经元更新。YANA通过集成神经形态中间表示（NIR）简化了从训练到部署的流程，在AMD Kria平台上实现了极低的资源占用，并证明了推理时间随稀疏度线性缩放，有效推动了神经形态计算的软硬件协同设计。
selection_source: fresh_fetch
motivation: 神经形态硬件的匮乏导致SNN算法创新与实际部署之间存在巨大的仿真到硬件的鸿沟。
method: 提出一种基于FPGA的五级事件驱动流水线架构，通过点对点连接支持任意拓扑，并利用查找表优化神经元漏电计算。
result: 在SHD数据集上展示了推理时间随空间和时间稀疏度近乎线性的缩放特性，且在Kria KR260平台上资源占用极低。
conclusion: YANA作为一个开源的端到端框架，通过NIR集成了现有工具链，为SNN的优化与部署提供了可扩展且易于获取的硬件解决方案。
---

## 摘要
脉冲神经网络 (SNN) 在严格功耗约束下实时处理时间稀疏数据流的应用中，展现出相较于传统人工神经网络 (ANN) 的显著优势，这一概念被称为“类脑优势”（Neuromorphic Advantage）。然而，类脑硬件的有限可用性造成了巨大的仿真到硬件鸿沟，阻碍了算法创新、软硬件协同设计以及成熟开源生态系统的发展。为应对这一挑战，我们推出了 YANA (Yet Another Neuromorphic Accelerator)，这是一款基于 FPGA 的数字 SNN 加速器，旨在通过提供易于获取的类脑计算软硬件框架来弥合这一差距。YANA 实现了五级事件驱动处理流水线，在充分利用时空稀疏性的同时，通过点对点神经元连接支持任意 SNN 拓扑。该架构采用输入预处理方案，可在无缓冲区溢出风险的情况下保持每周期处理一个事件的稳定速度，并利用查找表进行漏电流计算，实现了硬件高效的事件驱动神经元更新。通过在 Spiking Heidelberg Digits 数据集上的实验，我们展示了 YANA 的稀疏性利用能力，结果显示推理时间随空间和时间稀疏度呈近线性缩放。在易于获取的 AMD Kria KR260 平台上部署时，单个 YANA 核心仅消耗 740 个 LUT、918 个寄存器、7 个 BRAM 和 24 个 URAM，支持多达 $2^{17}$ 个突触和 $2^{10}$ 个神经元。我们将 YANA 框架作为开源项目发布，提供了一套用于 SNN 训练、优化和部署的端到端解决方案，并通过类脑中间表示 (NIR) 与现有的类脑计算工具集成。

## Abstract
Spiking Neural Networks (SNNs) promise significant advantages over conventional Artificial Neural Networks (ANNs) for applications requiring real-time processing of temporally sparse data streams under strict power constraints -- a concept known as the Neuromorphic Advantage. However, the limited availability of neuromorphic hardware creates a substantial simulation-to-hardware gap that impedes algorithmic innovation, hardware-software co-design, and the development of mature open-source ecosystems. To address this challenge, we introduce Yet Another Neuromorphic Accelerator (YANA), an FPGA-based digital SNN accelerator designed to bridge this gap by providing an accessible hardware and software framework for neuromorphic computing. YANA implements a five-stage, event-driven processing pipeline that fully exploits temporal and spatial sparsity while supporting arbitrary SNN topologies through point-to-point neuron connections. The architecture features an input preprocessing scheme that maintains steady event processing at one event per cycle without buffer overflow risks, and implements hardware-efficient event-driven neuron updates using lookup tables for leak calculations. We demonstrate YANA's sparsity exploitation capabilities through experiments on the Spiking Heidelberg Digits dataset, showing near-linear scaling of inference time with both spatial and temporal sparsity levels. Deployed on the accessible AMD Kria KR260 platform, a single YANA core utilizes 740 LUTs, 918 registers, 7 BRAMS and 24 URAMs, supporting up to $2^{17}$ synapses and $2^{10}$ neurons. We release the YANA framework as an open-source project, providing an end-to-end solution for training, optimizing, and deploying SNNs that integrates with existing neuromorphic computing tools through the Neuromorphic Intermediate Representation (NIR).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **YANA**（Yet Another Neuromorphic Accelerator）的开源、基于 FPGA 的脉冲神经网络（SNN）加速器及其配套软件框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：**“仿真到硬件的鸿沟”**（Simulation-to-Hardware Gap）。尽管 SNN 在理论上具有低功耗和实时处理稀疏数据的“类脑优势”，但由于缺乏易于获取、可编程且高效的硬件平台，许多算法创新仅停留在仿真阶段，难以在实际硬件上验证。
*   **研究背景**：现有的 FPGA 加速器大多采用时钟驱动（Clock-driven）而非事件驱动（Event-driven），且往往局限于层级化的顺序连接，无法支持复杂的递归或任意拓扑的 SNN 图结构。

### 2. 方法论：核心思想与技术细节
YANA 采用数字逻辑实现，核心是一个五级事件驱动的处理流水线，旨在充分利用时空稀疏性。
*   **五级流水线架构**：
    1.  **接收阶段 (RX)**：接收外部或内部反馈的事件。
    2.  **突触阶段 (Synapse)**：预处理输入事件，计算每个神经元的预突触权重和，并识别“活跃神经元”（Hot Neurons）。
    3.  **神经元阶段 (Neuron)**：实现 Forward-Euler LIF 模型。关键技术是**延迟更新机制**：仅在神经元接收到事件时才更新状态，利用**查找表 (LUT)** 预计算漏电（Leak）指数项，避免了复杂的幂运算。
    4.  **轴突阶段 (Axon)**：根据映射表加载并排队发放后突触事件。
    5.  **发送阶段 (TX)**：将事件路由至内部反馈路径或外部互连。
*   **关键技术**：
    *   **点对点连接**：支持任意 SNN 拓扑，不受限于层级结构。
    *   **近内存计算**：逻辑与存储紧密结合，减少冯·诺依曼瓶颈。
    *   **软件集成**：通过**神经形态中间表示 (NIR)** 实现与 Norse、PyTorch 等主流工具的无缝对接。

### 3. 实验设计
*   **数据集**：使用 **Spiking Heidelberg Digits (SHD)** 语音数据集。
*   **实验场景**：
    *   **推理延迟缩放实验**：通过均匀丢弃输入事件（增加时间稀疏度）和剪枝权重（增加空间稀疏度），观察推理时间的变化。
    *   **硬件资源评估**：在 AMD Kria KR260 平台上测量逻辑资源占用。
*   **对比基准**：主要对比不同稀疏度下的执行效率，验证硬件是否能真正从稀疏性中获益。

### 4. 资源与算力
*   **硬件平台**：AMD Kria KR260 Robotics Starter Kit (Zynq UltraScale+ SoC)。
*   **资源占用**：单个 YANA 核心仅需 **740 个 LUT**、918 个寄存器、7 个 BRAM 和 24 个 URAM。
*   **算力规模**：单核支持多达 $2^{17}$ 个突触和 $2^{10}$ 个神经元。
*   **训练细节**：文中提到使用 Norse 框架进行硬件感知训练，但未详细说明训练所用的具体 GPU 型号或训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对不同空间稀疏度（0.0 到 0.9）和时间稀疏度（0.85 到 0.96）进行了多组扫频实验，每组数据点取 20 个样本的平均值。
*   **充分性评价**：实验充分证明了 YANA 架构在处理稀疏数据时的**近线性加速特性**。虽然实验主要集中在推理延迟上，但对于验证“事件驱动”这一核心设计目标而言是客观且公平的。

### 6. 主要结论与发现
*   **稀疏性即速度**：推理时间随空间和时间稀疏度的增加而近乎线性下降。在 SHD 任务中，推理时间从 16ms 缩短至 1ms，远快于实时速度。
*   **极低资源占用**：YANA 证明了可以在极小的 FPGA 资源代价下实现支持任意拓扑的高效类脑加速器。
*   **开源生态意义**：通过提供从训练到部署的端到端开源工具链，降低了类脑硬件的研究门槛。

### 7. 优点
*   **高度灵活性**：支持点对点连接，能够运行非层级化的、复杂的递归脉冲网络。
*   **硬件效率高**：采用 LUT 优化漏电计算，实现了真正的事件驱动处理，不处理无事件的空闲状态。
*   **易用性强**：集成了 NIR 标准，方便研究人员将现有的仿真模型迁移到硬件。

### 8. 不足与局限
*   **层级支持有限**：目前软件框架（YANA deploy）主要支持全连接层，对卷积层等结构化连接的支持尚在开发中。
*   **功耗验证缺失**：虽然理论上事件驱动更省电，但论文尚未给出在 FPGA 上的实际功耗测量数据（如电流、电压波动）。
*   **多核扩展性**：目前的评估集中在单核性能，尚未展示在大规模多核 Network-on-Chip (NoC) 环境下的负载均衡和通信开销。

（完）
