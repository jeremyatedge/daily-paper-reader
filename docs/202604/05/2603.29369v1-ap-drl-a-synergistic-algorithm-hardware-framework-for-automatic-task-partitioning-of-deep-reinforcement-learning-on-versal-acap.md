---
title: "AP-DRL: A Synergistic Algorithm-Hardware Framework for Automatic Task Partitioning of Deep Reinforcement Learning on Versal ACAP"
title_zh: AP-DRL：一种用于在 Versal ACAP 上实现深度强化学习自动任务划分的算法-硬件协同框架
authors: "Enlai Li, Zhe Lin, Sharad Sinha, Wei Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29369v1"
tags: ["query:aiot"]
score: 9.0
evidence: Versal ACAP上深度强化学习任务划分的算法-硬件协同框架
tldr: 针对深度强化学习（DRL）训练中计算强度差异大和量化误差敏感的问题，本文提出AP-DRL框架。该框架利用AMD Versal ACAP的异构架构（CPU、FPGA、AIE），通过瓶颈分析、基于整数线性规划（ILP）的任务划分模型以及硬件感知的混合精度优化（FP32/FP16/BF16），实现了DRL训练的自动任务分配与加速。实验表明，该方法在保证收敛性的同时，显著提升了训练速度。
selection_source: fresh_fetch
motivation: DRL训练中算子计算强度差异大且对量化精度敏感，导致传统硬件平台和量化方案难以兼顾效率与准确性。
method: 提出一种基于ILP的任务划分模型和硬件感知混合精度算法，将DRL算子智能分配至Versal ACAP的CPU、FPGA和AIE单元。
result: 实验显示AP-DRL相比纯可编程逻辑（PL）和纯AI引擎（AIE）基准分别实现了最高4.17倍和3.82倍的加速。
conclusion: AP-DRL通过软硬件协同优化，有效解决了异构平台上DRL训练的自动任务划分与精度平衡难题。
---

## 摘要
深度强化学习在多个领域取得了显著成功。然而，训练与推理过程之间的紧密耦合使得加速 DRL 训练成为 DRL 优化的一个核心挑战。阻碍高效 DRL 训练的两个关键问题是：(1) 不同 DRL 算法之间、甚至同一算法内部不同算子之间的计算强度存在显著差异，这增加了硬件平台选择的复杂性；(2) DRL 较宽的动态范围在使用传统的 FP16+FP32 混合精度量化时，可能会导致显著的奖励误差。现有工作主要集中在针对特定计算单元加速 DRL 或优化推理阶段的量化，而我们提出了 AP-DRL 来应对上述挑战。AP-DRL 是一个自动任务划分框架，它利用 AMD Versal ACAP 的异构架构（集成了 CPU、FPGA 和 AI 引擎），通过智能硬件感知优化来加速 DRL 训练。我们的方法首先对不同 DRL 工作负载下的 CPU、FPGA 和 AIE 性能进行瓶颈分析，从而为 AP-DRL 的组件间任务划分和量化优化提供设计原则。该框架随后通过基于设计空间探索的性能分析和基于整数线性规划（ILP）的划分模型来解决平台选择挑战，根据算子的计算特性将其匹配到最优计算单元。针对量化挑战，AP-DRL 利用 Versal ACAP 对这些精度格式的原生支持，采用一种硬件感知算法来协调 FP32 (CPU)、FP16 (FPGA/DSP) 和 BF16 (AI 引擎) 操作。综合实验表明，AP-DRL 在保持训练收敛的同时，相比可编程逻辑基准可实现高达 4.17 倍的加速，相比 AI 引擎基准可实现高达 3.82 倍的加速。

## Abstract
Deep reinforcement learning has demonstrated remarkable success across various domains. However, the tight coupling between training and inference processes makes accelerating DRL training an essential challenge for DRL optimization. Two key issues hinder efficient DRL training: (1) the significant variation in computational intensity across different DRL algorithms and even among operations within the same algorithm complicates hardware platform selection, while (2) DRL's wide dynamic range could lead to substantial reward errors with conventional FP16+FP32 mixed-precision quantization. While existing work has primarily focused on accelerating DRL for specific computing units or optimizing inference-stage quantization, we propose AP-DRL to address the above challenges.   AP-DRL is an automatic task partitioning framework that harnesses the heterogeneous architecture of AMD Versal ACAP (integrating CPUs, FPGAs, and AI Engines) to accelerate DRL training through intelligent hardware-aware optimization. Our approach begins with bottleneck analysis of CPU, FPGA, and AIE performance across diverse DRL workloads, informing the design principles for AP-DRL's inter-component task partitioning and quantization optimization. The framework then addresses the challenge of platform selection through design space exploration-based profiling and ILP-based partitioning models that match operations to optimal computing units based on their computational characteristics. For the quantization challenge, AP-DRL employs a hardware-aware algorithm coordinating FP32 (CPU), FP16 (FPGA/DSP), and BF16 (AI Engine) operations by leveraging Versal ACAP's native support for these precision formats. Comprehensive experiments indicate that AP-DRL can achieve speedup of up to 4.17$\times$ over programmable logic and up to 3.82$\times$ over AI Engine baselines while maintaining training convergence.

---

## 论文详细总结（自动生成）

以下是对论文《AP-DRL: A Synergistic Algorithm-Hardware Framework for Automatic Task Partitioning of Deep Reinforcement Learning on Versal ACAP》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度强化学习（DRL）训练加速面临两大挑战：
    1.  **计算强度差异大**：不同算法（如简单的 MLP 到复杂的 CNN）以及同一算法内不同算子（如 GEMM 与激活函数）的计算强度波动剧烈，导致单一硬件平台（如纯 FPGA 或纯 GPU/AIE）难以在所有场景下达到最优效率。
    2.  **量化敏感性高**：DRL 的权重和激活值具有较宽的动态范围，传统的 FP16+FP32 混合精度量化容易引入显著的奖励误差（Reward Error），影响训练收敛。
*   **研究背景**：AMD Versal ACAP 提供了集 CPU (PS)、FPGA (PL) 和 AI 引擎 (AIE) 于一体的异构架构，为解决上述问题提供了硬件基础，但如何自动、高效地在这些单元间划分任务并保持精度仍是难题。

### 2. 方法论
AP-DRL 提出了一个算法-硬件协同框架，分为**静态划分阶段**和**动态执行阶段**：
*   **瓶颈分析与设计原则**：通过实验发现，低 FLOPs 任务在 PL 上表现更好（初始化开销低），而高 FLOPs 任务在 AIE 上更优（时钟频率高）。
*   **任务划分模型 (ILP)**：
    *   将 DRL 训练过程建模为有向无环图 (DAG)，以“层”为基本划分节点。
    *   使用**整数线性规划 (ILP)** 算法，在满足硬件资源约束和数据依赖的前提下，最小化总执行时间 $T$。
    *   利用 TAPCA 和 CHARM 工具对 PL 和 AIE 进行性能分析（Profiling），获取算子的执行时间和资源占用数据。
*   **硬件感知量化算法**：
    *   **BF16 (AIE)**：利用 AIE-ML 对 BF16 的原生支持，因其指数位与 FP32 相同，可直接处理宽动态范围数据，无需损失缩放。
    *   **FP16 (PL)**：在 PL 端使用 FP16 提高吞吐量，并引入**主权重备份 (Master Weight Backup)** 和**动态损失缩放 (Dynamic Loss Scaling)** 以保证数值稳定性。
    *   **FP32 (PS)**：在 CPU 端保持全精度，负责环境交互和部分非计算密集型任务。

### 3. 实验设计
*   **实验场景**：涵盖了 Atari 游戏、OpenAI Gym 和 MuJoCo 三大基准测试集。
*   **具体环境**：CartPole, InvertedPendulum, LunarLanderContinuous, MountainCarContinuous, Breakout, MsPacman。
*   **算法**：DQN, DDPG, A2C, PPO。
*   **对比基准 (Benchmark)**：
    1.  **AIE-Only**：仅使用 AIE 运行（基于 CHARM 优化）。
    2.  **FIXAR**：最先进的 CPU-FPGA 异构 DRL 加速框架（使用定点量化）。
    3.  **FP32 基准**：用于验证量化后的收敛精度。

### 4. 资源与算力
*   **硬件平台**：使用 **AMD Versal VEK280** 开发板（AI Edge 系列）。
*   **具体配置**：双核 Cortex-A72 CPU，304 个 AIE-ML 块，1312 个 DSP 引擎，520.7K LUT。
*   **运行模式**：实验主要通过 **Vitis 2023.2 硬件仿真 (Hardware Emulation)** 完成，性能数据由 Vitis Analyzer 分析获取。
*   **算力参数**：PL 运行频率为 245 MHz，AIE 运行频率为 1 GHz。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 6 种环境、4 种主流 DRL 算法进行了测试，并涵盖了从 16 到 4096 不等的多种 Batch Size。
*   **充分性**：
    *   **收敛性验证**：展示了所有环境下的奖励曲线，证明了混合精度方案的有效性。
    *   **消融/案例分析**：详细分析了 DDPG-LunarCont 在不同 Batch Size 下任务划分结果的变化。
    *   **性能评估**：对比了执行时间和吞吐量（Throughput）。
*   **评价**：实验设计较为全面，覆盖了从控制任务到视觉任务的不同计算强度，对比基准具有代表性。

### 6. 主要结论与发现
*   **加速效果**：AP-DRL 相比纯 PL (FIXAR) 实现高达 **4.17倍** 的加速，相比纯 AIE 实现高达 **3.82倍** 的加速。
*   **精度保持**：量化后的奖励误差控制在 **1.12% 至 4.81%** 之间，所有模型均能正常收敛。
*   **划分规律**：随着 Batch Size 和 FLOPs 的增加，框架会自动将更多层从 PL 迁移到 AIE，以利用 AIE 的高频计算能力。
*   **同步开销**：在极低 FLOPs 场景下，PL 与 AIE 间的主权重同步开销可能抵消量化带来的收益（性能下降约 22%）。

### 7. 优点
*   **自动化程度高**：通过 ILP 模型实现了层级的自动任务分配，无需人工干预复杂的异构映射。
*   **协同优化**：不仅考虑了计算加速，还针对 DRL 的数值特性设计了 BF16/FP16/FP32 协同量化方案。
*   **硬件特性利用充分**：深入挖掘了 Versal ACAP 架构中 AIE-ML 对 BF16 支持的优势。

### 8. 不足与局限
*   **仿真局限**：主要数据来源于硬件仿真，虽然具有高精度，但实际板卡上的功耗和热管理表现未详细讨论。
*   **同步瓶颈**：在小规模网络中，跨单元的数据同步（如主权重备份）仍是显著的性能瓶颈。
*   **环境步加速不足**：目前重点在于训练阶段的加速，对于某些环境模拟（Environment Step）成为瓶颈的 DRL 场景，提升空间有限。
*   **应用限制**：该框架高度依赖 AMD Versal 架构，迁移到其他异构平台（如 Intel Agilex 或 NVIDIA Orin）需要重新建模。

（完）
