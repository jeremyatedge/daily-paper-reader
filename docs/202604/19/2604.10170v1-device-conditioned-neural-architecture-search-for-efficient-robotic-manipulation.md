---
title: Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation
title_zh: 面向高效机器人操控的设备感知神经架构搜索
authors: "Yiming Wu, Huan Wang, Zhenghao Chen, Ge Yuan, Dong Xu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10170v1"
tags: ["query:aiot"]
score: 10.0
evidence: 硬件约束下的架构搜索与量化感知训练
tldr: 针对机器人视觉运动策略在异构硬件上部署效率低、通用性差的问题，本文提出DC-QFA框架。该框架通过构建包含多种架构和混合精度位宽的超网络，结合硬件感知的正则化进行训练。利用一次性训练的超网络，可针对不同设备快速搜索最优子网络，无需重复优化。此外，引入多步在线蒸馏提升低精度下的稳定性。实验证明，该方法在多种模型和硬件上实现了2-3倍加速且性能几乎无损。
selection_source: fresh_fetch
motivation: 现有的机器人策略优化方法通常针对特定设备且耗时，难以适应多样化的硬件约束和复杂的部署需求。
method: 提出DC-QFA框架，通过训练一个包含架构和量化位宽搜索空间的超网络，并结合多步在线蒸馏来减少低精度下的误差累积。
result: 在多种主流策略模型上实现了2-3倍的推理加速，且在边缘设备和云端平台上均保持了极高的任务成功率。
conclusion: DC-QFA提供了一种高效且通用的机器人策略部署方案，在保证操作稳定性的同时显著降低了跨硬件平台的适配成本。
---

## 摘要
视觉运动策略日益增长的复杂性给在异构机器人硬件约束下的部署带来了重大挑战。然而，大多数现有的机器人操控模型效率优化方法都是针对特定设备和模型的，缺乏泛化性，且在适配过程中需要耗时的逐设备优化。在这项工作中，我们提出了一个名为“设备感知全量化”（Device-Conditioned Quantization-For-All, DC-QFA）的统一框架，通过设备感知的量化感知训练和硬件约束的架构搜索来分摊部署成本。具体而言，我们引入了一个单一的超网络（supernet），其设计空间涵盖了丰富的网络架构和混合精度位宽。该超网络在逐设备查找表的指导下，利用延迟和内存感知的正则化进行优化。借助该超网络，对于每个目标平台，我们可以执行一次性的轻量级搜索来选择最优子网络，而无需任何逐设备重新优化，从而实现了在异构硬件上更具泛化性的部署，并显著缩短了部署时间。为了提高低精度下的长程稳定性，我们进一步引入了多步同策略蒸馏（multi-step on-policy distillation），以减轻闭环执行过程中的误差累积。在 DiffusionPolicy-T、MDT-V 和 OpenVLA-OFT 等三个代表性策略骨干网络上的广泛实验表明，我们的 DC-QFA 在边缘设备、消费级 GPU 和云平台上实现了 2-3 倍的加速，且任务成功率的性能下降可以忽略不计。在配备力/力矩传感器的 Inovo 机器人上进行的真实世界评估进一步验证了，即使在严重的量化情况下，我们的低比特 DC-QFA 策略仍能保持稳定且具有丰富接触的操控。

## Abstract
The growing complexity of visuomotor policies poses significant challenges for deployment with heterogeneous robotic hardware constraints. However, most existing model-efficient approaches for robotic manipulation are device- and model-specific, lack generalizability, and require time-consuming per-device optimization during the adaptation process. In this work, we propose a unified framework named \textbf{D}evice-\textbf{C}onditioned \textbf{Q}uantization-\textbf{F}or-\textbf{A}ll (DC-QFA) which amortizes deployment effort with the device-conditioned quantization-aware training and hardware-constrained architecture search. Specifically, we introduce a single supernet that spans a rich design space over network architectures and mixed-precision bit-widths. It is optimized with latency- and memory-aware regularization, guided by per-device lookup tables. With this supernet, for each target platform, we can perform a once-for-all lightweight search to select an optimal subnet without any per-device re-optimization, which enables more generalizable deployment across heterogeneous hardware, and substantially reduces deployment time. To improve long-horizon stability under low precision, we further introduce multi-step on-policy distillation to mitigate error accumulation during closed-loop execution. Extensive experiments on three representative policy backbones, such as DiffusionPolicy-T, MDT-V, and OpenVLA-OFT, demonstrate that our DC-QFA achieves $2\text{-}3\times$ acceleration on edge devices, consumer-grade GPUs, and cloud platforms, with negligible performance drop in task success. Real-world evaluations on an Inovo robot equipped with a force/torque sensor further validates that our low-bit DC-QFA policies maintain stable, contact-rich manipulation even under severe quantization.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DC-QFA**（Device-Conditioned Quantization-For-All）的框架，旨在解决大型视觉运动策略（Visuomotor Policies）在多样化机器人硬件上部署效率低下的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于 Transformer 的视觉运动策略（如 Diffusion Policy, VLA 模型）虽然性能强大，但计算和内存开销巨大，难以直接部署在资源受限的边缘设备（如嵌入式 GPU/NPU）上。
*   **研究动机**：
    *   **部署成本高**：现有的模型压缩方法（如剪枝、量化）通常是“设备特定”的，每更换一种硬件就需要重新进行耗时的训练或微调。
    *   **泛化性差**：固定压缩比例的模型难以适应异构硬件（从云端到边缘）的不同延迟和内存预算。
    *   **长程不稳定性**：在低比特量化下，闭环控制中的误差会随时间累积，导致长程操控任务失败。

### 2. 方法论
DC-QFA 的核心思想是**“一次训练，到处部署”**，通过构建一个超网络（Supernet）来覆盖多种架构和量化配置。
*   **量化超网络（Quantized Supernet）**：设计了一个包含块选择（Block Selection）、MLP 扩展比、注意力头比例以及混合精度位宽（INT4, INT8, FP4, FP8, FP16）的搜索空间。
*   **设备感知训练（Device-Conditioned Training）**：
    *   利用预先测量的硬件查找表（LUT）获取不同配置在目标设备上的延迟和内存消耗。
    *   在训练目标函数中引入**硬件正则化项**（基于 Lagrangian 松弛法），使超网络在训练过程中学习适应不同的资源约束。
*   **多步同策略蒸馏（Multi-step On-Policy Distillation, OPD）**：
    *   为了解决量化导致的误差累积，让量化后的学生模型进行多步 rollout（滚动执行）。
    *   使用全精度教师模型在学生生成的轨迹上提供监督信号，通过逐步扩大训练时界（Horizon）来增强策略的稳定性。
*   **轻量化搜索**：训练完成后，针对特定设备使用 **NSGA-II** 多目标搜索算法，在不重新训练的情况下快速找到满足约束的最优子网络。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：Push-T（平面推箱子）、CALVIN（长程语言指令操控）、LIBERO（多任务操控基准）。
    *   **真实世界**：使用 Inovo 6自由度机械臂，执行扫豆子、抓取鸡蛋、插入辣椒、放置胡萝卜等接触密集型任务。
*   **策略骨干网络（Backbones）**：DiffusionPolicy-T、MDT-V（多模态扩散 Transformer）、OpenVLA-OFT。
*   **对比方法**：
    *   **PTQ（后量化）**：SmoothQuant, AWQ。
    *   **QAT（量化感知训练）**：SQIL。
    *   **QFA（全量化搜索）**：BatchQuant。

### 4. 资源与算力
论文明确提到了训练超网络所需的时间成本：
*   **DiffusionPolicy-T**：在超网络训练阶段约需 **85 GPU 小时**。
*   **MDT-V**：约需 **156 GPU 小时**。
*   **硬件平台**：实验涵盖了 Jetson Orin NX（边缘端）、消费级 GPU 和云端平台。
*   **推理加速**：在 Jetson Orin NX 上，INT4 量化相比 BF16 实现了约 **3倍** 的加速，内存占用减少了 **3.8倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流架构、3 个仿真基准测试、4 类真实世界任务。
*   **消融实验**：详细分析了 OPD（同策略蒸馏）对长程任务稳定性的贡献，并展示了 Pareto 前沿曲线（性能 vs 参数量）。
*   **客观性**：通过在不同比特位宽（W4A4, W8A8, FP8）下与多种基准方法对比，证明了 DC-QFA 在极端压缩（4-bit）下依然能保持与全精度模型接近的性能。实验设计覆盖了从感知到动作生成的全流程，评价指标包括成功率、平均轨迹长度、延迟和内存，较为全面。

### 6. 主要结论与发现
*   **高效部署**：DC-QFA 实现了 2-3 倍的推理加速和 2-4 倍的模型尺寸缩减，且任务成功率损失极小。
*   **跨平台通用性**：通过一次超网络训练，即可通过轻量搜索适配多种硬件，显著降低了部署门槛。
*   **稳定性提升**：引入 OPD 显著缓解了低比特量化在闭环控制中的误差累积问题，尤其在 CALVIN 等长程任务中表现突出。
*   **真实世界可行性**：在配备力控传感器的机器人上验证了量化策略在处理精细、接触密集任务时的鲁棒性。

### 7. 优点（亮点）
*   **解耦训练与适配**：将昂贵的量化感知训练与设备特定的优化解耦，极大地提高了工业部署效率。
*   **混合精度搜索**：不仅搜索架构，还搜索每一层的最优位宽，实现了更精细的精度-效率平衡。
*   **关注闭环稳定性**：不同于传统的视觉量化，该方法专门针对机器人策略的“误差累积”特性设计了蒸馏方案。

### 8. 不足与局限
*   **训练开销**：虽然分摊了部署成本，但超网络的初始训练时间（如 156 小时）仍然较高。
*   **编译器依赖**：论文提到未来需要与硬件特定的编译器（如 TensorRT, TVM）进行更深度的集成，以充分发挥搜索出的非标准架构的性能。
*   **模型覆盖**：虽然测试了 OpenVLA，但对于参数量更大的（如 7B 以上）基础模型，超网络的训练稳定性和搜索空间设计可能面临更大挑战。

（完）
