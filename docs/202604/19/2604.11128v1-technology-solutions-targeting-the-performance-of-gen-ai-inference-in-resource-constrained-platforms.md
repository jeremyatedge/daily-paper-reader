---
title: Technology solutions targeting the performance of gen-AI inference in resource constrained platforms
title_zh: 针对资源受限平台中生成式人工智能推理性能的技术解决方案
authors: "Joyjit Kundu, Joshua Klein, Aakash Patel, Dwaipayan Biswas"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11128v1"
tags: ["query:aiot"]
score: 9.0
evidence: 移动设备等资源受限平台上的生成式人工智能推理
tldr: 本研究针对资源受限平台（如移动设备）在生成式AI推理中面临的内存容量与带宽压力，提出并评估了两种技术方案。通过分层Roofline分析模型，研究探讨了利用高带宽存储（HBS）支持大模型长上下文推理的性能需求，以及通过键合全局缓存芯片组优化小模型推理的策略，旨在为交互式应用提供高效的推理支持。
selection_source: fresh_fetch
motivation: 生成式AI推理（尤其是长上下文和多模态应用）在资源受限设备上引发了严重的内存带宽和容量瓶颈。
method: 采用分层Roofline分析性能模型，分别针对大模型评估高带宽存储（HBS）及针对小模型评估键合全局缓存芯片组。
result: 明确了HBS实现交互式吞吐量所需的带宽与延迟指标，并提出了优化利用全局缓存芯片组的具体方案。
conclusion: 通过针对性地引入HBS和缓存芯片组技术，可以有效缓解资源受限平台的内存压力，提升生成式AI的推理性能。
---

## 摘要
生成式人工智能工作负载（尤其是语言模型推理）的兴起，正在加剧片上和片外内存的压力。视频流或图像等多模态输入，以及问答（QA）和长文档分析等下游应用，会导致较长的上下文长度，从而需要缓存先前 token 的海量键（Key）和值（Value）状态。即使在移动设备等资源受限的设备上进行低程度的并发推理服务，也会进一步增加内存容量压力和运行时内存管理的复杂性。在本文中，我们使用基于分层 Roofline 的分析性能模型，评估了两种旨在从容量和带宽两方面缓解内存压力的新兴技术解决方案对性能的影响。对于大模型（如 13B 参数）和长上下文，我们研究了高带宽存储（HBS）对性能的影响，并概述了为实现可接受的交互吞吐量所需的带宽/延迟要求。对于小模型（如 1B 参数），我们评估了键合全局缓冲内存小芯片的优势，并提出了如何最佳利用它的方案。

## Abstract
The rise of generative AI workloads, particularly language model inference, is intensifying on/off-chip memory pressure. Multimodal inputs such as video streams or images and downstream applications like Question Answering (QA) and analysis over large documents incur long context lengths, requiring caching of massive Key and Value states of the previous tokens. Even a low degree of concurrent inference serving on resource-constrained devices, like mobiles, can further add to memory capacity pressure and runtime memory management complexity. In this paper, we evaluate the performance implications of two emerging technology solutions to alleviate the memory pressure in terms of both capacity and bandwidth using a hierarchical roofline-based analytical performance model. For large models (e.g., 13B parameters) and context lengths, we investigate the performance implications of High Bandwidth Storage (HBS) and outline bandwidth/latency requirements to achieve an acceptable throughput for interactivity. For small models (e.g., 1B parameters), we evaluate the merit of a bonded global buffer memory chiplet and propose how to best utilize it.

---

## 论文详细总结（自动生成）

这是一份关于论文《Technology solutions targeting the performance of gen-AI inference in resource constrained platforms》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在资源受限平台（如智能手机）上运行生成式人工智能（Gen-AI）面临严重的**内存墙（Memory Wall）**瓶颈。
*   **研究动机**：
    *   **内存容量压力**：大语言模型（LLM）参数量巨大（如 Llama-7B 需要约 14GB 显存），超出标准移动端 DDR 容量。
    *   **KV Cache 增长**：多模态输入和长上下文应用导致 KV Cache 线性增长，进一步挤占内存。
    *   **带宽限制**：推理过程中的 Token 生成属于内存受限型（Memory-bound）任务，数据搬运速度远低于计算速度。
    *   **技术缺口**：现有的算法优化（如量化、剪枝）虽有帮助，但仍需硬件层面的新兴技术方案来彻底缓解压力。

### 2. 方法论
论文采用了一种**基于硬件感知的分层 Roofline 分析性能模型**，核心思想是通过数学建模评估不同存储技术对推理性能的影响。
*   **核心公式**：
    执行时间由计算路径和数据传输路径中的最慢者决定：
    $$time = \max \left( \frac{\text{Total FLOPs}}{\text{Compute Throughput}}, \frac{\text{Data Traffic Volume}}{\text{Memory Throughput}} \right)$$
*   **关键技术细节**：
    *   **分层建模**：模型涵盖了 NPU Scratchpad – L2 – DDR – HBS（高带宽存储）或 Chiplet（小芯片）的多级架构。
    *   **算子分解**：将 LLM 推理分解为 GEMM/GEMV 算子（如 $QKV$ 投影、Softmax、MLP 等），并根据内存层级搜索最优分块（Tiling）策略。
    *   **技术方案 A：高带宽存储 (HBS)**。针对大模型，研究具有接近 DDR 带宽但延迟较高（微秒级）的大容量存储。
    *   **技术方案 B：键合 SRAM 缓冲芯片组 (Bonded Chiplet)**。针对小模型，研究在 NPU 旁增加一个通过定制接口连接的 SRAM 小芯片，用于缓存关键矩阵。

### 3. 实验设计
*   **场景与模型**：
    *   **大模型场景**：使用 **LLaVa1.5-13B**（多模态模型），测试不同上下文长度（200/200 到 8k/24k）。
    *   **小模型场景**：使用 **Llama3.2-1B**，测试短上下文（128/384）。
*   **Benchmark 与对比基准**：
    *   **基准内存**：LPDDR6 (173 GB/s)。
    *   **对比配置**：
        1. 标准 LPDDR6。
        2. 3倍堆叠 LPDDR6 (520 GB/s)。
        3. 引入 HBS（带宽 16-512 GB/s，延迟 2-100 μs）。
        4. 引入 SRAM Chiplet（带宽 173-1024 GB/s）。
*   **性能指标**：每秒生成的 Token 数（TPS），目标阈值为 **10 TPS**（交互式体验的最低要求）。

### 4. 资源与算力
*   **算力设定**：论文基于分析模型进行模拟，设定 NPU 的总计算吞吐量为 **35 TFLOPs**。
*   **硬件参数**：设定了 LPDDR6 延迟为 100ns，HBS 延迟在 2μs 至 100μs 之间波动。
*   **说明**：由于本文是基于性能模型的**分析性研究**，并非在物理 GPU 上进行实测训练，因此未涉及具体的 GPU 型号（如 A100/H100）或训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 HBS 进行了多维度扫描（带宽 vs 延迟），针对 DDR 进行了三种配置对比，并覆盖了从短到极长（33k）的上下文窗口。
*   **充分性评价**：
    *   **充分性**：实验涵盖了当前端侧 AI 的两个主流方向（大模型长上下文 vs 小模型极致性能），参数覆盖范围广。
    *   **客观性**：采用了工业界常用的 Roofline 模型，并考虑了算子级的分解，具有较高的参考价值。
    *   **局限性**：实验基于理论模型，未考虑实际系统中的软件栈开销、热管理或复杂的总线竞争。

### 6. 主要结论与发现
*   **HBS 瓶颈效应**：由于 HBS 延迟较高，即使其带宽略高于 DDR，HBS 仍可能是性能瓶颈。只有当 HBS 带宽比 DDR 高出 40% 以上时，瓶颈才会转移。
*   **数据放置策略**：对于 13B 模型，将 $Q, K, V$ 矩阵限制在 DDR 中（减少 HBS-DDR 流量）可获得显著加速（最高 3.1 倍），是达到 10 TPS 目标的关键。
*   **小模型优化误区**：对于 1B 级别的小模型，注意力机制（Attention）仅占总时间的 4-9%。因此，使用 Chiplet 缓存 $QKV$ 的收益有限，**更好的策略是利用 Chiplet 缓存 MLP 和投影层的权重矩阵**。
*   **上下文长度影响**：随着上下文增加，性能单调下降，但上述技术方案带来的相对增益在不同长度下保持一致。

### 7. 优点
*   **系统技术协同优化 (STCO)**：论文不仅关注算法，更从硬件架构层面提供了清晰的性能预测，为未来端侧芯片设计提供了量化指导。
*   **分层 Roofline 模型**：相比单层模型，该模型能更精确地捕捉复杂内存层级中的数据搬运瓶颈。
*   **实用性强**：明确给出了实现“交互式推理”所需的具体硬件参数指标（如 10 TPS 对应的带宽/延迟要求）。

### 8. 不足与局限
*   **模型简化**：分析模型假设数据访问模式是完全规则且可预测的，忽略了实际运行时的动态内存碎片和非线性访问开销。
*   **技术实现难度**：论文未深入讨论 HBS 或 3D 堆叠 Chiplet 在移动设备上的散热、功耗及成本挑战。
*   **覆盖范围**：仅测试了 LLaVa 和 Llama 两个系列，未探讨混合专家模型（MoE）等其他架构，MoE 架构对内存带宽的要求可能更为特殊。

（完）
