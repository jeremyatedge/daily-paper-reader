---
title: "BioTrain: Sub-MB, Sub-50mW On-Device Fine-Tuning for Edge-AI on Biosignals"
title_zh: BioTrain：面向生物信号边缘人工智能的亚兆字节、亚50毫瓦设备端微调
authors: "Run Wang, Victor J. B. Jung, Philip Wiese, Sebastian Frey, Giusy Spacone, Francesco Conti, Alessio Burrello, Luca Benin"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13359v1"
tags: ["query:aiot"]
score: 10.0
evidence: 亚 50 毫瓦功耗的生物信号边缘 AI 设备端微调
tldr: 针对生物信号在不同个体和时间下的显著差异，本文提出了BioTrain框架，旨在实现毫瓦级功耗和亚兆字节内存限制下的边缘设备全网络微调。通过优化内存分配和网络拓扑，BioTrain在GAP9 MCU上实现了低于50mW的功耗和显著的内存缩减（达8.1倍），在保护隐私的同时大幅提升了模型在实际部署中的准确率，为边缘侧生物信号AI的个性化适配提供了新方案。
selection_source: fresh_fetch
motivation: 生物信号的跨主体和跨会话变异性导致模型性能下降，而现有低功耗边缘平台难以支持全反向传播所需的内存和计算开销。
method: 提出BioTrain框架，通过高效的内存分配器和网络拓扑优化，在极低资源约束下实现全网络反向传播微调。
result: "在EEG和EOG任务中，全网络微调比未适配基准准确率提升达35%，且在GAP9平台上实现了8.1倍的内存缩减和低于50mW的运行功耗。"
conclusion: BioTrain证明了在资源受限的MCU上进行全网络微调的可行性，为边缘侧生物信号AI模型的个性化适配提供了高效可靠的解决方案。
---

## 摘要
生物信号表现出显著的跨受试者和跨会话变异性，导致严重的领域偏移，从而降低了面向边缘的小型人工智能模型在部署后的性能。因此，设备端自适应对于保护用户隐私和确保系统可靠性至关重要。然而，由于全反向传播（BP）极高的内存占用和计算成本，现有的基于微控制器（MCU）且功耗低于100毫瓦的穿戴式平台仅能支持浅层或稀疏的自适应方案。在本文中，我们提出了 BioTrain，这是一个能够在毫瓦级功耗和亚兆字节内存限制下，实现最先进生物信号模型全网络微调的框架。我们利用脑电图（EEG）和眼电图（EOG）数据集，通过离线和设备端基准测试对 BioTrain 进行了验证，涵盖了首日新受试者校准以及针对信号漂移的长期自适应。实验结果表明，在进行新受试者校准时，全网络微调相比未自适应的基准模型实现了高达 35% 的准确率提升，且比仅更新最后一层的方法高出约 7%。在 GAP9 MCU 平台上，BioTrain 在低于 50 毫瓦的功耗范围内，实现了 EEG 模型 17 样本/秒和 EOG 模型 85 样本/秒的高效设备端训练吞吐量。此外，BioTrain 高效的内存分配器和网络拓扑优化支持使用较大的批次大小，从而降低了峰值内存占用。对于 GAP9 上的全片上反向传播，与使用批归一化且批次大小为 8 的传统全网络微调相比，BioTrain 将内存占用降低了 8.1 倍，从 5.4 MB 降至 0.67 MB。

## Abstract
Biosignals exhibit substantial cross-subject and cross-session variability, inducing severe domain shifts that degrade post-deployment performance for small, edge-oriented AI models. On-device adaptation is therefore essential to both preserve user privacy and ensure system reliability. However, existing sub-100 mW MCU-based wearable platforms can only support shallow or sparse adaptation schemes due to the prohibitive memory footprint and computational cost of full backpropagation (BP). In this paper, we propose BioTrain, a framework enabling full-network fine-tuning of state-of-the-art biosignal models under milliwatt-scale power and sub-megabyte memory constraints. We validate BioTrain using both offline and on-device benchmarks on EEG and EOG datasets, covering Day-1 new-subject calibration and longitudinal adaptation to signal drift. Experimental results show that full-network fine-tuning achieves accuracy improvements of up to 35% over non-adapted baselines and outperforms last-layer updates by approximately 7% during new-subject calibration. On the GAP9 MCU platform, BioTrain enables efficient on-device training throughput of 17 samples/s for EEG and 85 samples/s for EOG models within a power envelope below 50 mW. In addition, BioTrain's efficient memory allocator and network topology optimization enable the use of a large batch size, reducing peak memory usage. For fully on-chip BP on GAP9, BioTrain reduces the memory footprint by 8.1x, from 5.4 MB to 0.67 MB, compared to conventional full-network fine-tuning using batch normalization with batch size 8.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **BioTrain** 的框架，旨在解决生物信号（如 EEG、EOG）在边缘设备上进行全网络微调的资源瓶颈问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：生物信号具有极高的**跨主体（不同人）**和**跨会话（不同时间）**变异性。预训练的 AI 模型部署到边缘设备后，由于环境、传感器位置或生理状态变化，会产生严重的“领域偏移”，导致性能大幅下降。
*   **研究背景**：现有的低功耗微控制器（MCU，功耗 < 100mW）受限于极小的内存（通常 < 2MB）和计算能力，通常只能支持简单的“线性探测”（只训练最后一层）或稀疏更新。然而，这些浅层自适应方法不足以应对复杂的生物信号波动。
*   **整体含义**：BioTrain 填补了空白，通过编译器优化和算法改进，使得在毫瓦级功耗、亚兆字节内存的 MCU 上进行**全网络反向传播（Full Backpropagation）**成为可能。

### 2. 方法论：核心思想与关键技术
BioTrain 是一个端到端的编译器框架，其核心思想是**通过内存编排和拓扑优化，将高内存需求的反向传播适配到极小内存的 MCU 上**。
*   **网络拓扑优化（Edge-FT）**：
    *   将**批归一化（Batch Normalization, BN）**替换为**组归一化（Group Normalization, GN）**。BN 依赖批次统计量，在小批次下不稳定且难以进行梯度累积；GN 仅依赖单样本统计，支持在极小内存下通过梯度累积模拟大批次训练。
*   **编译器驱动的内存管理（基于 Deeploy）**：
    *   **前端优化**：将梯度计算分解为输入梯度（grad-x）和权重梯度（grad-w），并对归一化层进行分解，以适应 L1 缓存限制。
    *   **中端平铺（Tiling）**：针对生物信号的长序列特性，采用**时间维度平铺**策略，将大数据块切分为小块，利用 DMA 引擎在 L2 和 L1 内存间高效搬运，实现计算与传输重叠。
    *   **后端支持**：集成了针对 RISC-V 优化的梯度算子库（PULPTrainLib），并针对平铺机制进行了定制修改。

### 3. 实验设计
*   **数据集**：
    1.  **EEG（脑电图）**：运动想象任务（舌头运动 vs 休息），使用 MI-BMINet 模型（7.9k 参数）。
    2.  **EOG（眼电图）**：11 类眼球运动识别，使用 EpiDeNet 模型（4.1k 参数）。
*   **实验场景**：
    *   **场景 A（首日校准）**：模拟新用户首次使用设备，利用少量标注数据进行微调。
    *   **场景 B（长期自适应）**：模拟设备在多天/多次会话中使用，应对信号随时间的漂移。
*   **对比基准（Benchmarks）**：
    *   **No-FT**：不进行任何自适应（零样本）。
    *   **LP（Linear Probing）**：仅更新最后一层分类器。
    *   **Full-FT**：传统的全网络微调（使用 BN）。
    *   **Edge-FT（BioTrain 方案）**：使用 GN 的全网络微调。

### 4. 资源与算力
*   **硬件平台**：**GAP9 MCU**（基于 RISC-V 架构，拥有 1 个控制器核心和 9 个计算核心集群）。
*   **内存占用**：通过 BioTrain 优化，EEG 模型的峰值内存从 5.4 MB 降至 **0.67 MB**（缩减 8.1 倍），完全适配 GAP9 的 1.5 MB L2 内存。
*   **功耗与能效**：
    *   运行功耗 **< 50 mW**。
    *   训练吞吐量：EEG 为 17 样本/秒，EOG 为 85 样本/秒。
    *   电池续航：使用 320mAh 电池，可支持约 211 次（EEG）或 951 次（EOG）完整的训练会话（每会话 40 epoch）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种不同的生物信号模态，每种模态包含 5 个受试者的多轮会话数据。
*   **重复性**：所有实验均使用 5 个不同的随机种子重复运行，并报告了均值和标准差。
*   **充分性评价**：实验设计较为充分，不仅对比了准确率，还深入分析了内存、延迟、功耗和能量分布。通过跨主体和跨时间的双重场景验证，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：全网络微调（Edge-FT）在 EEG 任务上比未适配基准提升了 **35%** 的准确率，比仅更新最后一层（LP）高出约 **7%**。
*   **稳定性**：在长期自适应场景中，Edge-FT 表现出比 LP 和传统 Full-FT 更稳定的收敛性，有效避免了“自适应滞后”现象。
*   **可行性**：证明了在亚兆字节内存约束下，通过编译器自动化优化，可以在低功耗 MCU 上实现复杂的全网络反向传播。

### 7. 优点
*   **内存效率极高**：通过 GN 和梯度累积技术，将内存需求降低了一个数量级，使全网络训练在廉价 MCU 上成为可能。
*   **自动化程度高**：BioTrain 作为一个编译器框架，能够自动生成 C 代码并处理复杂的内存平铺，降低了边缘 AI 开发的门槛。
*   **开源贡献**：代码已在 GitHub 开源，有助于推动 TinyML 社区在设备端学习领域的发展。

### 8. 不足与局限
*   **数值精度**：目前仅支持 **FP32（单精度浮点）** 训练。虽然 GAP9 支持浮点运算，但在更低端的定点 MCU 上，需要进一步研究量化感知训练（QAT）或定点训练。
*   **模型局限性**：实验主要集中在小型 CNN 模型上。虽然文中提到了 Transformer，但尚未在本文的生物信号任务中展示其在 MCU 上的全网络训练效果。
*   **样本量限制**：实验受试者数量（5人）相对较少，虽然符合此类原型研究的标准，但更大规模的临床数据集验证将更具说服力。

（完）
