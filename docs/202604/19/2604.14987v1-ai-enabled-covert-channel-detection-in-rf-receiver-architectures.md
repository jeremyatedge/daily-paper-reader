---
title: AI-Enabled Covert Channel Detection in RF Receiver Architectures
title_zh: 射频接收机架构中基于人工智能的隐蔽信道检测
authors: "Abdelrahman Emad Abdelazim, Alan Rodrigo Diaz-Rizo, Hassan Aboushady, Haralampos-G. Stratigopoulos"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14987v1"
tags: ["query:aiot"]
score: 9.0
evidence: "将CNN参数压缩80%以实现高效边缘部署"
tldr: "本研究针对无线芯片中隐蔽信道（CC）带来的安全威胁，提出了一种部署在RF接收端的AI防御机制。通过对原始I/Q样本进行实时监控，利用压缩后的轻量化卷积神经网络（CNN）检测嵌入在正常信号中的隐蔽信道。该模型在减少80%参数量的同时保持了高精度，并成功在FPGA上实现了高效的硬件加速器，为射频信号安全检测提供了低功耗、高性能的解决方案。"
selection_source: fresh_fetch
motivation: 旨在解决无线芯片中隐蔽信道导致敏感信息泄露的安全威胁，实现射频接收端的实时检测。
method: 提出一种压缩后的轻量化CNN模型，通过监控原始I/Q样本进行检测，并设计了专用的FPGA硬件加速器。
result: "在高信噪比下检测准确率超过97%，参数量减少80%且性能损失极小，硬件效率达107 GOPs/W。"
conclusion: 该研究证明了AI驱动的轻量化硬件加速器在射频隐蔽信道实时检测中的可行性与优越性。
---

## 摘要
无线芯片中的隐蔽信道 (CC) 构成了严重的安全威胁，因为它们能够将敏感信息从芯片泄露给外部攻击者。在这项工作中，我们提出了一种部署在射频接收机端的基于人工智能的防御机制，该模型直接监测原始 I/Q 样本，以实时检测嵌入在正常信号中的隐蔽信道。我们首先对一种先进的卷积神经网络 (CNN) 进行压缩，实现了 80% 的参数量削减，这是实现高效边缘部署的关键要求。在开源的基于硬件木马 (HT) 的隐蔽信道数据集上进行评估时，压缩后的 CNN 在信噪比 (SNR) 高于 1 dB 的平均情况下，隐蔽信道检测的平均准确率达到 90.28%，识别底层硬件木马的准确率达到 86.50%。对于 SNR > 20 dB 的实际通信场景，该模型在两项任务中的准确率均超过 97%。与基准模型相比，这些结果对应的性能下降极小，不足 2%。压缩后的 CNN 进一步与其他分类器进行了基准测试，展示了出色的准确率与模型尺寸之间的权衡。最后，我们设计了一个轻量级 CNN 硬件加速器并在 FPGA 上进行了演示，实现了极低的资源利用率和 107 GOPs/W 的效率。作为首个专门为隐蔽信道检测提出的 AI 硬件加速器，我们将其与用于调制识别等射频信号分类任务的先进 AI 加速器进行了对比，展示了其优越的性能。

## Abstract
Covert channels (CCs) in wireless chips pose a serious security threat, as they enable the exfiltration of sensitive information from the chip to an external attacker. In this work, we propose an AI-based defense mechanism deployed at the RF receiver, where the model directly monitors raw I/Q samples to detect, in real time, the presence of a CC embedded within an otherwise nominal signal. We first compact a state-of-the-art convolutional neural network (CNN), achieving an 80% reduction in parameters, which is an essential requirement for efficient edge deployment. When evaluated on the open-source hardware Trojan (HT)-based CC dataset, the compacted CNN attains an average accuracy of 90.28% for CC detection and 86.50% for identifying the underlying HT, with results averaged across SNR values above 1 dB. For practical communication scenarios where SNR > 20 dB, the model achieves over 97% accuracy for both tasks. These results correspond to a minimal performance degradation of less than 2% compared to the baseline model. The compacted CNN is further benchmarked against alternative classifiers, demonstrating an excellent accuracy-model size trade-off. Finally, we design a lightweight CNN hardware accelerator and demonstrate it on an FPGA, achieving very low resource utilization and an efficiency of 107 GOPs/W. Being the first AI hardware accelerator proposed specifically for CC detection, we compare it against state-of-the-art AI accelerators for RF signal classification tasks such as modulation recognition, showing superior performance.

---

## 论文详细总结（自动生成）

这篇论文《射频接收机架构中基于人工智能的隐蔽信道检测》（AI-Enabled Covert Channel Detection in RF Receiver Architectures）针对无线通信芯片的安全威胁，提出了一种高效的边缘侧AI防御方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：无线芯片可能被植入硬件木马（HT），通过射频（RF）发射机建立**隐蔽信道（Covert Channels, CC）**，在不干扰正常通信的情况下窃取加密密钥或AI模型权重等敏感信息。
*   **研究动机**：
    *   现有的非AI防御手段（如EVM、BER监测）通常过于复杂、仅能针对特定攻击且多为离线处理。
    *   现有的AI检测模型参数量大，难以直接部署在资源受限的射频接收机芯片上。
    *   云端检测存在高延迟、高功耗及隐私泄露风险，因此迫切需要一种**实时、通用且轻量化**的边缘侧检测方案。

### 2. 方法论
*   **核心思想**：在射频接收机的ADC输出端直接监控原始I/Q样本，将其视为“图像”输入，利用压缩后的卷积神经网络（CNN）进行实时分类（判断是否存在CC，并识别HT类型）。
*   **关键技术细节**：
    *   **LLDS模块（可学习线性下采样）**：为了减少CNN的输入维度，设计了LLDS块。它包含并行卷积滤波器（受Inception架构启发）和步长卷积，将输入维度从2×640压缩至2×128（压缩因子CF=5）。
    *   **轻量化CNN架构**：在LLDS之后连接两层卷积层和全连接层。通过消融实验确定线性激活函数在下采样阶段效果最好。
    *   **模型压缩**：相比基准模型，该方案实现了**80%的参数量削减**（从约18.4万减少至3.6万）。
    *   **硬件加速器设计**：在FPGA上实现了专用加速器，采用**权重驻留（Weight-stationary）**和**输入驻留（Input-stationary）**混合数据流，以匹配射频信号的实时吞吐量。

### 3. 实验设计
*   **数据集**：使用开源的HT-CC硬件生成数据集，包含4种主流硬件木马攻击模型：
    1.  **HT1-CC**：前导码（STS）幅度调制。
    2.  **HT2-CC**：前导码相位偏移。
    3.  **HT3-CC**：有效载荷中的“脏星座图”调制。
    4.  **HT4-CC**：射频前端的功率幅度调制。
*   **场景与基准（Benchmark）**：
    *   **信噪比（SNR）范围**：1 dB 至 29 dB。
    *   **对比方法**：基准CNN、LSTM（长短期记忆网络）、FCNN（全连接网络）、多分类SVM、单类SVM（SSCF方法）。
*   **任务类型**：二分类（检测CC）和五分类（识别具体HT类型及正常信号）。

### 4. 资源与算力
*   **硬件实现**：使用 **Xilinx Zynq UltraScale+ MPSoC (XCZU5EG)** FPGA。
*   **算力指标**：
    *   时钟频率：200 MHz。
    *   吞吐量：86.5 GOPs。
    *   能效比：**107 GOPs/W**。
    *   功耗：仅 **810 mW**。
    *   资源占用：极低（约2.7万个LUT，8246个FF）。
*   **训练说明**：文中未明确说明训练所用的具体GPU型号及训练时长，重点在于推理阶段的硬件效率。

### 5. 实验数量与充分性
*   **实验规模**：针对5种分类模型在8个不同SNR水平下进行了详尽测试，每组包含2000个固定长度的OFDM帧。
*   **消融实验**：对压缩因子（CF=2到6）和不同的激活函数（ReLU, Tanh, Sigmoid等）进行了对比实验，验证了LLDS模块的有效性。
*   **充分性评价**：实验覆盖了从极低信噪比（1dB）到实际通信信噪比（>20dB）的全场景，对比了多种主流机器学习架构，实验设计客观、公平且具有统计意义。

### 6. 主要结论与发现
*   **高准确率**：在实际通信场景（SNR > 20 dB）下，隐蔽信道检测和HT识别的准确率均**超过 97%**。
*   **鲁棒性**：即使在攻击者无法正确解码隐蔽数据的低SNR（如11-15dB）环境下，AI模型仍能以 >90% 的准确率检测到攻击。
*   **压缩优势**：参数量减少80%，但相比基准模型，准确率下降不足2%。
*   **硬件领先性**：该加速器是首个专门针对CC检测设计的AI硬件，其能效比（107 GOPs/W）优于目前最先进的射频信号分类加速器。

### 7. 优点
*   **实时性与边缘化**：直接集成在接收机架构中，无需数据上传，延迟极低。
*   **通用防御**：一种模型即可检测多种不同原理的HT-CC攻击，具有较强的泛化能力。
*   **硬件友好**：LLDS模块巧妙解决了高维射频数据与轻量化模型之间的矛盾，FPGA资源占用极低。

### 8. 不足与局限
*   **低信噪比下的误报**：在极低SNR（<10dB）时，正常信号易被误判为受感染信号（False Alarms），虽然这比漏报安全，但会影响通信效率。
*   **数据集局限**：虽然涵盖了4种主要攻击，但对于未知的、更复杂的自适应攻击（如对抗性攻击）的检测能力尚待验证。
*   **应用限制**：目前主要基于WiFi（802.11）协议验证，对于其他无线协议（如5G、蓝牙）的适配性需要进一步研究。

（完）
