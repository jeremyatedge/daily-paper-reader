---
title: "A KL Lens on Quantization: Fast, Forward-Only Sensitivity for Mixed-Precision SSM-Transformer Models"
title_zh: KL 视角下的量化：针对混合精度 SSM-Transformer 模型的快速、仅前向敏感度分析
authors: "Jason Kong, Nilesh Prasad Pandey, Flavio Ponzina, Tajana Rosing"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13440v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘设备部署的量化敏感性
tldr: 本文针对混合SSM-Transformer模型在边缘设备部署的挑战，提出一种基于KL散度的轻量级、无需反向传播的敏感度分析框架。该方法仅通过前向传播即可识别对量化敏感的组件，避免了高昂的计算成本。实验证明，KL指标优于MSE等传统度量，在Intel硬件上实现了接近FP16精度的混合精度量化，兼顾了模型尺寸与推理性能。
selection_source: fresh_fetch
motivation: 混合架构模型在边缘部署时面临严苛的资源限制，且不同组件对量化的敏感度差异显著，亟需高效的敏感度评估方法。
method: 提出一种基于KL散度的前向敏感度分析框架，无需梯度计算或重训练即可快速确定混合精度量化策略。
result: 实验证实KL指标能更准确地捕捉量化导致的性能下降，在真实硬件测试中实现了接近FP16的精度及INT4级别的推理速度。
conclusion: 该研究为资源受限设备上的复杂混合模型部署提供了一种实用且高效的量化方案，平衡了模型精度与运行效率。
---

## 摘要
在边缘设备上部署大语言模型（LLMs）面临严峻的计算和内存限制，这限制了实时处理和设备端智能。结合结构化状态空间模型（SSMs）与基于 Transformer 的 LLMs 的混合架构在效率和性能之间取得了平衡。激进的量化可以大幅减小模型尺寸并加速推理，但其对不同组件的不均匀影响需要精细化管理。在这项工作中，我们提出了一个轻量级、无反向传播、基于代理的敏感度分析框架，用于识别混合 SSM-Transformer 中最易受量化诱导性能退化影响的组件。该方法仅依赖前向传播指标，避免了昂贵的梯度计算和重新训练，使其适用于因专有权限制或隐私约束而导致领域内数据获取受限的场景。我们还提供了一项形式化分析，表明在语言建模任务中，Kullback-Leibler (KL) 散度指标比广泛采用的均方误差 (MSE) 和信号量化噪声比 (SQNR) 等替代指标能更好地捕捉量化敏感度。通过在 SSM 和混合架构上进行的大量实验，我们的消融研究证实，基于 KL 的排名与观察到的性能下降高度一致，且优于其他替代指标。该框架能够以极小的精度损失在资源受限的边缘设备上实际部署先进的混合模型。我们在 Intel Lunar Lake 硬件上通过真实的设备端性能分析进一步验证了我们的方法，证明了 KL 引导的混合精度在 CPU 和 GPU 执行模式下均实现了接近 FP16 的困惑度，且模型尺寸和吞吐量可与 Uniform INT4 媲美。代码可在 https://github.com/jasonkongie/kl-ssm-quant 获取。

## Abstract
Deploying Large Language Models (LLMs) on edge devices faces severe computational and memory constraints, limiting real-time processing and on-device intelligence. Hybrid architectures combining Structured State Space Models (SSMs) with transformer-based LLMs offer a balance of efficiency and performance. Aggressive quantization can drastically cut model size and speed up inference, but its uneven effects on different components require careful management. In this work, we propose a lightweight, backpropagation-free, surrogate-based sensitivity analysis framework to identify hybrid SSM-Transformer components most susceptible to quantization-induced degradation. Relying solely on forward-pass metrics, our method avoids expensive gradient computations and retraining, making it suitable for situations where access to in-domain data is limited due to proprietary restrictions or privacy constraints. We also provide a formal analysis showing that the Kullback-Leibler (KL) divergence metric better captures quantization sensitivity for Language modeling tasks than widely adopted alternatives such as mean squared error (MSE) and signal-to-quantization-noise ratio (SQNR). Through extensive experiments on SSM and hybrid architectures, our ablation studies confirm that KL-based rankings align with observed performance drops and outperform alternative metrics. This framework enables the practical deployment of advanced hybrid models on resource-constrained edge devices with minimal accuracy loss. We further validate our approach with real-world on-device profiling on Intel Lunar Lake hardware, demonstrating that KL-guided mixed-precision achieves near-FP16 perplexity with model sizes and throughput competitive with Uniform INT4 on both CPU and GPU execution modes. Code is available at https://github.com/jasonkongie/kl-ssm-quant.

---

## 论文详细总结（自动生成）

这篇论文针对混合架构模型在边缘设备部署中的量化难题，提出了一种高效的敏感度分析框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLMs）在边缘设备部署受限于内存和计算资源。虽然结合了状态空间模型（SSM）和 Transformer 的混合架构（如 Hymba、Zamba）在效率上有所提升，但其不同组件对量化的敏感度差异巨大。
*   **核心问题**：传统的均匀量化（Uniform Quantization）会导致严重的精度下降，而现有的混合精度量化方法通常依赖昂贵的梯度计算（反向传播）或不适用于语言建模任务的指标（如 SQNR）。
*   **研究动机**：开发一种快速、无需反向传播、仅依赖前向传播信号的敏感度分析方法，以识别混合模型中必须保留高精度的关键层。

### 2. 方法论
*   **核心思想**：利用 **KL 散度（Kullback-Leibler Divergence）** 作为代理指标，衡量单层量化后模型输出分布与原始模型输出分布之间的差异。
*   **关键技术细节**：
    *   **仅前向传播**：通过一次前向传递计算量化前后的 Logits 差异，无需计算 Hessian 矩阵或梯度，极大降低了计算开销。
    *   **KL 散度优越性**：论文通过数学证明（Lemma 1 & 2）指出，KL 散度是困惑度（Perplexity, PPL）的紧致上界，比常用的信号量化噪声比（SQNR）更能准确反映语言模型的性能退化。
    *   **非对称性分析**：发现“学生 $\to$ 教师”方向的 KL 散度（$KL_{student \to teacher}$）在捕捉量化误差方面远优于反向指标。
*   **算法流程**：
    1.  逐层对模型进行量化。
    2.  计算量化模型与原始教师模型输出之间的 KL 散度。
    3.  根据 KL 散度得分对各层进行敏感度排序。
    4.  对最敏感的 Top-k 层保留高精度（如 FP16），其余层执行低比特量化（如 INT4/INT8）。

### 3. 实验设计
*   **实验对象**：涵盖了多种规模的 SSM 和混合模型，包括 Mamba (130M, 380M, 1.4B)、Mamba2-130M、Hymba 和 Zamba。
*   **数据集**：使用 WikiText-2 评估困惑度（PPL）。
*   **Benchmark 与对比方法**：
    *   **基准**：FP16（全精度）、Uniform INT8、Uniform INT4。
    *   **对比指标**：SQNR (dB)、$KL_{teacher \to student}$、$\Delta$ 交叉熵（CE）。
*   **硬件验证**：在 **Intel Lunar Lake** 平台上进行端到端性能分析，涵盖 CPU 和 GPU 执行模式。

### 4. 资源与算力
*   **硬件型号**：实验主要在 Intel Lunar Lake 处理器（集成 CPU、iGPU 和 NPU）上完成。
*   **算力消耗**：论文未明确给出具体的训练/分析总时长，但强调了该方法是“轻量级”且“快速”的。由于不需要反向传播，其敏感度分析的开销仅相当于几次模型推理，远低于基于 Hessian 或微调的方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Hymba 模型进行了详尽的层级和参数级消融实验（32 层逐一测试）。
    *   在 6 种不同架构/规模的模型上计算了 Kendall’s $\tau$ 相关系数，验证指标的普适性。
    *   在真实硬件上进行了 10 种不同混合精度配置（p01-p10）的端到端测试。
*   **充分性评价**：实验设计较为充分，不仅有理论证明，还有跨架构的统计相关性分析以及真实硬件的部署表现（延迟、吞吐量、模型尺寸），客观地展示了方法在实际应用中的价值。

### 6. 主要结论与发现
*   **指标效能**：$KL_{student \to teacher}$ 与 PPL 下降的相关性最高（平均 Kendall’s $\tau = 0.79$），显著优于 SQNR（0.71）。
*   **敏感度分布**：混合模型中存在明显的“敏感度峰值”，例如在 Hymba 中，第 31 层贡献了超过 70% 的性能退化风险。
*   **性能表现**：
    *   在 Mamba-1.4B 上，该方法将模型从 5.2GB 压缩至 1.4GB，且无精度损失。
    *   在 GPU 上，相比 FP16 基准，推理延迟降低了高达 17.6 倍。
    *   混合精度方案在保持接近 FP16 精度的同时，达到了与 Uniform INT4 相当的吞吐量。

### 7. 优点
*   **高效性**：无需梯度，适合在资源受限或数据受限（隐私保护）的环境下快速生成量化策略。
*   **理论支撑**：为 KL 散度优于 SQNR 提供了形式化的数学证明，增强了方法的说服力。
*   **软硬结合**：不仅停留在算法层面，还针对 Intel 最新硬件架构进行了深度的端到端优化和验证。

### 8. 不足与局限
*   **数据集单一**：实验主要集中在 WikiText-2 上，未广泛测试在下游任务（如摘要、问答）中的表现。
*   **硬件依赖性**：虽然方法通用，但端到端性能数据高度依赖于 Intel OpenVINO 框架和特定硬件指令集，在其他平台（如 ARM/NVIDIA）上的表现有待验证。
*   **量化范围**：主要关注权重（Weight）量化，对激活（Activation）量化和更极端的 2-bit 量化讨论较少。

（完）
