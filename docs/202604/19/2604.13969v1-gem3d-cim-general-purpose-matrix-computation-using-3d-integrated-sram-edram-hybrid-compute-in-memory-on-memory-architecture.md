---
title: GEM3D CIM General Purpose Matrix Computation Using 3D Integrated SRAM eDRAM Hybrid Compute In Memory on Memory Architecture
title_zh: GEM3D CIM：基于 3D 集成 SRAM-eDRAM 混合“存储器上存储器”架构的通用矩阵计算
authors: "Subhradip Chakraborty, Ankur Singh, Akhilesh R. Jaiswal"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13969v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于矩阵运算的高能效存内计算架构
tldr: 针对当前存内计算（CIM）架构主要局限于点积运算、难以高效支持通用矩阵操作的问题，本文提出了一种基于3D集成SRAM-eDRAM混合架构的GEM3D CIM。该架构采用GF 22nm FDSOI工艺，通过转置架构和存内算术设计，实现了支持4位精度的通用矩阵运算（如转置、逐元素加法、乘法），在保持点积兼容性的同时，显著提升了通用矩阵处理的能效与灵活性，为AI加速和高性能计算提供了更通用的硬件支持。
selection_source: fresh_fetch
motivation: 现有的存内计算架构大多仅针对点积运算优化，缺乏对转置、逐元素加法和乘法等通用矩阵操作的高效支持。
method: 提出一种3D集成的SRAM-eDRAM混合存内计算架构，利用转置设计和外围感知电路在存储阵列内直接执行多种矩阵运算。
result: 该架构在22nm工艺下实现了4位精度的通用矩阵计算，在延迟、能效和计算密度之间取得了良好平衡，并兼容传统点积运算。
conclusion: 该研究成功将CIM的应用范围从点积扩展到通用矩阵处理，为未来AI加速器和高性能计算架构的通用化铺平了道路。
---

## 摘要
随着深度神经网络（DNN）的快速发展，存内计算（CIM）已成为一种极具前景的高能效范式，用于加速乘累加（MAC）运算。然而，目前的 CIM 架构很大程度上局限于点积计算，难以高效支持通用矩阵运算，如转置、逐元素加法和乘法。本研究提出了一种 3D 集成的“存储器上存储器”SRAM-eDRAM 混合 CIM 架构，采用 GlobalFoundries 22nm FDSOI 工艺实现，能够在存储器交叉阵列中以 4 位精度直接执行通用矩阵运算。通过利用专门的基于转置的架构、存内算术运算、外围感知设计以及 3D SRAM-eDRAM 集成，所提出的架构在保持与传统 CIM 点积架构兼容的同时，平衡了通用矩阵运算的延迟、能效和计算密度。总之，这种“存储器上存储器”CIM 框架将 CIM 的应用范围从点积扩展到了更通用的领域，实现了多功能矩阵处理，并为 AI 加速和通用高性能计算中的更广泛应用铺平了道路。

## Abstract
With the rapid growth of deep neural networks (DNNs), compute-in-memory (CIM) has emerged as a promising energy-efficient paradigm for accelerating multiply-and-accumulate (MAC) operations. Yet, current CIM architectures are largely limited to dot-product computations and struggle to efficiently support general-purpose matrix operations, such as transpose, element-wise addition, and multiplication. This work presents a 3D-integrated, memory-on-memory SRAM-eDRAM hybrid CIM architecture, implemented in GlobalFoundries 22~nm FDSOI technology, capable of performing general matrix operations directly within the memory crossbar with 4-bit precision. By leveraging a specialized transpose-based architecture, in-memory arithmetic operations, peripheral-aware design, and 3D SRAM--eDRAM integration, the proposed architecture balances latency, energy efficiency, and compute density for general purpose matrix operations while remaining compatible with the conventional CIM dot product architectures. Overall, this memory-on-memory CIM framework generalizes CIM beyond dot products, enabling versatile matrix processing and paving the way for broader applications in AI acceleration and general-purpose high performance computing.

---

## 论文详细总结（自动生成）

### 论文总结：GEM3D-CIM 3D集成混合存储通用矩阵计算架构

#### 1. 核心问题与整体含义（研究动机和背景）
传统的存内计算（CIM）架构虽然在加速深度学习中的乘累加（MAC）运算方面表现出色，但其功能过于单一，主要局限于点积运算。在处理循环神经网络（RNN/LSTM）、物理仿真及张量算法时，经常需要执行**矩阵转置、逐元素加法和逐元素乘法（Hadamard积）**。现有的CIM方案在处理这些非点积操作时，往往需要频繁的数据搬运或复杂的逻辑转换，导致并行性优势丧失。
本文提出了 **GEM3D-CIM**，这是一种基于单片3D集成（Monolithic 3D）的SRAM-eDRAM混合架构，旨在打破“存储墙”限制，在存储阵列内部直接实现通用的矩阵运算，将CIM的应用范围从单纯的AI推理扩展到通用高性能计算领域。

#### 2. 方法论：核心思想与关键技术
该架构采用分层设计，通过单片3D技术将两层存储器垂直堆叠：**Layer A（SRAM层）**和**Layer B（eDRAM层）**。

*   **核心思想：** 利用3D垂直互连（MIV）实现层间数据的高速并行传输，并重新设计存储单元使其具备计算能力。
*   **关键技术细节：**
    *   **转置操作（Transpose）：** 引入了 **T-SRAM（11T）** 和 **T-eDRAM（10T）** 单元。通过专用的字线控制和垂直/水平布线，实现三步走算法：1) 将Layer A上三角元素拷贝至Layer B；2) 两层内部同步进行对角线对称位置的交换；3) 将Layer B处理后的数据写回Layer A。
    *   **逐元素运算（Addition/Multiplication）：** 
        *   **MA-SRAM（8T）：** 充当数字模拟转换器（DAC），通过加权晶体管产生与存储值成比例的电流。
        *   **MA-eDRAM（9T）：** 充当轻量级ADC。利用**线性反馈移位寄存器（LFSR）**计数器，将模拟乘法/加法结果（通过电容耦合或电流求和获得）转换为数字信号。
    *   **混合存储优势：** 结合了SRAM的高速稳定性和eDRAM的高密度、低功耗特性。

#### 3. 实验设计
*   **技术平台：** 使用 **GlobalFoundries 22nm FDSOI** 工艺 PDK 进行仿真。
*   **场景与规模：** 针对 $32 \times 32$ 规模的矩阵（4位精度）进行了寄生参数感知的瞬态仿真。
*   **Benchmark（对比对象）：** 与多种先进的CIM架构进行对比，包括 CIMAT（基于转置SRAM）、TSRAM、CRAM（位串行逻辑）以及 FAT 等。
*   **评估指标：** 吞吐量（GOPS）、能效比（GOPS/W）、延迟及面积开销。

#### 4. 资源与算力
*   **算力工具：** 论文基于电路级仿真（Cadence Spectre），而非大规模GPU集群训练。
*   **硬件参数：** 仿真中 SRAM 工作电压为 0.8V，部分计算路径（DAC/ADC）使用 1.8V 厚氧化物器件以获得更高的信号裕度。
*   **具体性能：** 
    *   转置运算：15.51 GOPS，12.77 GOPS/W。
    *   逐元素乘法：13.93 GOPS，436.61 GOPS/W。
    *   逐元素加法：27.86 GOPS，432.25 GOPS/W。

#### 5. 实验数量与充分性
*   **实验覆盖：** 论文进行了功能验证仿真（4x4矩阵）、大规模性能评估（32x32矩阵）、以及针对工艺偏差的 **1000次蒙特卡洛（Monte Carlo）模拟**。
*   **充分性评价：** 实验设计较为充分，涵盖了从单元级（Bit-cell）到架构级（Sub-array）的验证。通过PVT（压力、电压、温度）变化分析，证明了模拟计算在4位精度下的可靠性。对比实验在相同工艺节点下进行了等效估算，确保了对比的相对公平性。

#### 6. 主要结论与发现
*   **通用性提升：** 证明了CIM架构可以高效支持除点积以外的复杂矩阵操作。
*   **3D集成的优越性：** 单片3D集成提供的细间距垂直互连（MIV）显著降低了数据搬运的能量损耗和延迟。
*   **能效表现：** 在执行算术运算时，该架构表现出极高的能效（>430 GOPS/W），远超传统的行向读取处理方式。
*   **数字化兼容：** 尽管内部采用模拟计算，但输入输出均保持数字格式，易于集成到现有的数字系统中。

#### 7. 优点（亮点）
*   **架构创新：** 首次提出将SRAM和eDRAM通过3D堆叠方式结合，专门用于通用矩阵运算。
*   **高效转置：** 巧妙利用3D层间拷贝解决了传统CIM难以处理矩阵转置的痛点。
*   **轻量化ADC：** 基于LFSR的eDRAM ADC设计减少了外围电路的面积开销。

#### 8. 不足与局限
*   **面积开销：** 由于使用了逻辑设计规则（Logic Rules）而非优化的存储规则，且增加了大量计算晶体管，单个单元面积远大于标准SRAM（T-SRAM面积约为标准6T单元的2.9倍）。
*   **精度限制：** 目前仅支持4位精度（ENOB约为4.78位），对于需要高精度浮点运算的科学计算场景可能不足。
*   **热效应挑战：** 论文虽提到3D集成的热耦合挑战，但未给出详细的热管理方案或散热模拟数据。
*   **工艺复杂性：** 单片3D集成和混合存储（SRAM+eDRAM）的制造工艺要求极高，量产成本可能较大。

（完）
