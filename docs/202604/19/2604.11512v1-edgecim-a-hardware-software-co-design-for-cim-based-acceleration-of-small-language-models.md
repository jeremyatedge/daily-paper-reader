---
title: "EdgeCIM: A Hardware-Software Co-Design for CIM-Based Acceleration of Small Language Models"
title_zh: EdgeCIM：一种用于基于存内计算的小语言模型加速的软硬件协同设计
authors: "Jinane Bazzi, Mariam Rakka, Fadi Kurdahi, Mohammed E. Fouda, Ahmed Eltawil"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11512v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对边缘小语言模型存算一体加速的软硬件协同设计
tldr: EdgeCIM是一个软硬件协同设计框架，旨在解决边缘设备上小语言模型（SLM）推理时自回归解码阶段的内存受限和能效低问题。该研究通过65nm存内计算（CIM）宏和分块映射策略，优化了端到端解码器推理。实验表明，EdgeCIM在吞吐量和能效上显著优于NVIDIA Orin Nano和Qualcomm SA8255P，为实时、低功耗的边缘SLM推理提供了高效方案。
selection_source: fresh_fetch
motivation: 现有加速器在处理边缘SLM的自回归解码时受限于内存带宽，导致资源利用率低且能耗极高。
method: 提出一种基于65nm存内计算宏的硬件架构，并结合分块映射策略来平衡流水线阶段并最大化并行度。
result: 在LLaMA3.2-1B上，EdgeCIM的吞吐量和能效分别是NVIDIA Orin Nano的7.3倍和49.59倍，且在多种SLM上表现优异。
conclusion: EdgeCIM通过软硬件协同优化，成功克服了边缘端大模型推理的瓶颈，是实现高效边缘SLM推理的有力竞争方案。
---

## 摘要
在笔记本电脑、智能手机和嵌入式平台等边缘设备上部署小语言模型（SLM）的需求日益增长，这暴露了现有加速器在基础效率上的不足。虽然 GPU 能高效处理预填充（prefill）工作负载，但自回归解码阶段主要由矩阵-向量乘法（GEMV）操作主导，这些操作本质上受限于内存带宽，导致边缘端的利用率低下且能耗极高。在本研究中，我们提出了 EdgeCIM，这是一个软硬件协同设计框架，重新思考了针对端到端仅解码器（decoder-only）推理的加速器设计。其核心是一个采用 65nm 工艺实现的存内计算（CIM）宏单元，并结合了基于分块（tile-based）的映射策略，以平衡流水线阶段，在最大化并行度的同时缓解 DRAM 带宽瓶颈。我们的模拟器支持对参数量高达 4B 的 SLM 进行设计空间探索，从而确定延迟和能效方面的帕累托最优配置。与 NVIDIA Orin Nano 相比，EdgeCIM 在 LLaMA3.2-1B 上实现了高达 7.3 倍的吞吐量提升和 49.59 倍的能效提升；在 LLaMA3.2-3B 上，其吞吐量比高通 SA8255P 高出 9.95 倍。在 TinyLLaMA-1.1B、LLaMA3.2 (1B, 3B)、Phi-3.5-mini-3.8B、Qwen2.5 (0.5B, 1.5B, 3B)、SmolLM2-1.7B、SmolLM3-3B 以及 Qwen3 (0.6B, 1.7B, 4B) 上的广泛基准测试表明，在 INT4 精度下，我们的加速器平均达到了 336.42 tokens/s 的吞吐量和 173.02 tokens/J 的能效。这些结果证明 EdgeCIM 是实现实时、高能效边缘级 SLM 推理的一种极具竞争力的解决方案。

## Abstract
The growing demand for deploying Small Language Models (SLMs) on edge devices, including laptops, smartphones, and embedded platforms, has exposed fundamental inefficiencies in existing accelerators. While GPUs handle prefill workloads efficiently, the autoregressive decoding phase is dominated by GEMV operations that are inherently memory-bound, resulting in poor utilization and prohibitive energy costs at the edge. In this work, we present EdgeCIM, a hardware-software co-design framework that rethinks accelerator design for end-to-end decoder-only inference. At its core is a CIM macro, implemented in 65nm, coupled with a tile-based mapping strategy that balances pipeline stages, maximizing parallelism while alleviating DRAM bandwidth bottlenecks. Our simulator enables design space exploration of SLMs up to 4B parameters, identifying Pareto-optimal configurations in terms of latency and energy. Compared to an NVIDIA Orin Nano, EdgeCIM achieves up to 7.3x higher throughput and 49.59x better energy efficiency on LLaMA3.2-1B, and delivers 9.95x higher throughput than Qualcomm SA8255P on LLaMA3.2-3B. Extensive benchmarks on TinyLLaMA-1.1B, LLaMA3.2 (1B, 3B), Phi-3.5-mini-3.8B, Qwen2.5 (0.5B, 1.5B, 3B), SmolLM2-1.7B, SmolLM3-3B, and Qwen3 (0.6B, 1.7B, 4B) reveal that our accelerator, under INT4 precision, achieves on average 336.42 tokens/s and 173.02 tokens/J. These results establish EdgeCIM as a compelling solution towards real-time, energy-efficient edge-scale SLM inference.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EdgeCIM** 的软硬件协同设计框架，旨在通过存内计算（CIM）技术加速边缘设备上的小语言模型（SLM）推理。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着生成式 AI 向边缘端（手机、笔记本、嵌入式设备）迁移，小语言模型（SLM，如 LLaMA 3.2-1B/3B）的需求激增。
*   **核心问题**：SLM 推理分为“预填充（Prefill）”和“解码（Decoding）”两个阶段。现有加速器（如 GPU/NPU）在处理解码阶段时面临严重瓶颈。解码阶段由**矩阵-向量乘法（GEMV）**主导，具有极低的算术强度，受限于内存带宽（Memory-bound），导致硬件利用率低且能耗极高。
*   **研究意义**：现有的 CIM 研究多关注编码器模型（如 BERT）或特定算子，缺乏针对“仅解码器（Decoder-only）”架构的端到端、全流程优化。EdgeCIM 填补了这一空白。

### 2. 方法论：核心思想与关键技术
EdgeCIM 采用软硬件协同设计，包含硬件架构设计、数据流映射和设计空间探索（DSE）三个层面：
*   **硬件架构**：
    *   **分层瓦片结构**：采用“芯片-集群（Cluster）-瓦片（Tile）-处理单元（PE）”的层次化设计。
    *   **数字 CIM 宏**：基于 65nm 工艺的 SRAM 数字存内计算单元，支持位串行（Bit-serial）计算，可灵活配置 INT4 和 INT8 精度。
*   **关键技术细节**：
    *   **活动瓦片（Active Tiles）策略**：为了平衡 DRAM 带宽和计算吞吐量，EdgeCIM 仅激活部分 Tile 进行计算，同时利用其余 Tile 预取下一组权重，实现计算与数据传输的高度重叠。
    *   **端到端映射**：不仅优化 Attention 算子，还针对投影（Projection）、线性层（Linear）和前馈网络（FFN）进行了全流程的分块映射优化。
*   **算法流程**：
    *   使用**遗传算法（GA）**进行设计空间探索。
    *   **目标函数**：最小化 $Latency^\alpha \times Energy^{(1-\alpha)}$，通过调节 $\alpha$ 平衡延迟和功耗。
    *   搜索空间包含集群数量、Tile 数量、PE 规模、总线宽度等约 310 万种配置。

### 3. 实验设计
*   **测试模型（SLMs）**：涵盖了主流的轻量级模型，包括 LLaMA3.2 (1B, 3B)、TinyLLaMA-1.1B、Phi-3.5-mini、Qwen2.5 (0.5B-3B)、SmolLM2/3 及 Qwen3 系列。
*   **对比基准（Benchmarks）**：
    *   **边缘 GPU**：NVIDIA Jetson Orin Nano, Orin Nano Super, AGX Orin。
    *   **边缘 NPU/SoC**：高通 SA8255P, Snapdragon X Elite, Snapdragon 8 Elite。
    *   **学术界 CIM 方案**：X-Former, TranCIM, ReTransformer 等。
*   **实验场景**：Batch size = 1（典型的边缘交互场景），输入/输出序列长度通常为 128/128。

### 4. 资源与算力
*   **硬件实现/模拟**：论文基于 **65nm CMOS 工艺**进行建模。
*   **算力工具**：
    *   使用 **HSPICE** 对计算组件进行电路级仿真。
    *   使用 **CACTI 6.0** 估算缓存（Buffer）的延迟、能耗和面积。
    *   开发了基于 C++ 的分析型模拟器进行系统级性能评估。
*   **训练/搜索算力**：未明确说明遗传算法运行的具体 GPU 资源，但提到 GA 运行了 50 代，种群规模为 20，属于中等规模的搜索任务。

### 5. 实验数量与充分性
*   **实验规模**：对 11 个不同的 SLM 模型进行了基准测试，涵盖了从 0.5B 到 4B 的参数规模。
*   **充分性**：
    *   **多维度评估**：不仅对比了吞吐量（Tokens/s）和能效（Tokens/J），还分析了面积开销、精度影响（INT4 vs INT8）以及不同 $\alpha$ 值下的帕累托前沿。
    *   **客观性**：对比对象包含了目前市面上最先进的商用边缘芯片（如骁龙 8 Elite），实验数据具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在 LLaMA3.2-1B 上，EdgeCIM 的吞吐量是 NVIDIA Orin Nano 的 **7.3 倍**，能效提升了 **49.59 倍**。
*   **对比高通平台**：在 LLaMA3.2-3B 上，吞吐量比高通 SA8255P 高出 **9.95 倍**。
*   **平均表现**：在 INT4 精度下，EdgeCIM 平均可达到 **336.42 tokens/s** 和 **173.02 tokens/J**。
*   **设计发现**：较小的模型倾向于更多的 Tile 以增加并行度，而较大的模型则倾向于更大的 PE 规模。

### 7. 优点
*   **软硬协同优化**：通过“活动瓦片”策略有效解决了边缘端 DRAM 带宽受限的痛点。
*   **端到端视角**：不同于只关注 Attention 的研究，该工作考虑了 SLM 推理的全流程（包括 FFN 和线性层）。
*   **高度自动化**：利用遗传算法自动寻找针对特定模型的最佳硬件配置，而非使用固定架构。

### 8. 不足与局限
*   **缺乏实测数据**：所有结果均基于分析型模拟器和电路仿真，缺乏真实的芯片流片（Tape-out）实测验证。
*   **工艺节点较旧**：采用 65nm 工艺进行建模，虽然在 CIM 研究中常见，但与商用边缘 SoC（如 3nm/4nm）相比，工艺红利未被充分体现。
*   **场景局限**：主要针对 Batch size = 1 的场景，未深入探讨多用户或高并发场景下的表现。
*   **量化损失**：虽然支持 INT4/INT8，但未详细讨论极低精度量化对模型推理准确率（Perplexity）的具体影响。

（完）
