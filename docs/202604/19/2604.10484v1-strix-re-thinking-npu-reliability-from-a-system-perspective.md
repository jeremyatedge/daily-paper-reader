---
title: "Strix: Re-thinking NPU Reliability from a System Perspective"
title_zh: Strix：从系统视角重新思考 NPU 的可靠性
authors: "Jiapeng Guan, Jie Zhang, Hao Zhou, Ran Wei, Dean You, Hui Wang, Yingquan Wang, Tinglue Wang, Xudong Zhao, Jing Li, Zhe Jiang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10484v1"
tags: ["query:aiot"]
score: 9.0
evidence: 跨越微架构和指令集的全栈 NPU 可靠性框架
tldr: Strix是一个针对NPU可靠性的全栈框架，旨在解决深度学习模型在安全关键领域中因硬件故障频发而面临的挑战。针对现有粗粒度冗余方案开销过大的问题，Strix从系统视角出发，通过重新划分NPU推理流水线并部署针对性的微架构、指令集及编程层面的保护机制，实现了亚微秒级的故障定位与纠错，且性能损耗极低。
selection_source: fresh_fetch
motivation: 随着工艺缩放和模型复杂度增加，硬件故障频发，而现有的粗粒度NPU可靠性方案性能和硬件开销过高。
method: 提出全栈框架Strix，通过在微架构、指令集和编程层面重新划分NPU流水线，并针对主要失效模式部署精细化防御机制。
result: 实现了亚微秒级的故障定位与纠错，仅带来1.04倍的性能下降，且硬件开销极小。
conclusion: Strix证明了通过系统级协同设计，可以在极低开销下显著提升NPU的可靠性，满足安全关键领域的部署需求。
---

## 摘要
深度神经网络（DNN）和大型语言模型（LLM）日益依赖硬件加速器，包括在安全关键领域，而工艺缩放和不断增长的模型复杂度使得硬件故障变得更加频繁。现有的系统级机制通常将 NPU 视为一个整体单元，采用粗粒度的复制，这会产生极高的性能和硬件开销，导致可靠性需求与可部署方案之间存在差距。为了弥补这一差距，我们提出了 Strix，这是一个基于开源 SoC 的全栈 NPU 可靠性框架，涵盖了微架构、指令集架构（ISA）和编程方法。Strix 沿系统推理流水线对 NPU 进行了重新划分，识别了主要的故障模式，并附加了针对性的保护措施，在仅有 1.04 倍减速和极低硬件开销的情况下，实现了亚微秒级的故障定位、错误检测和纠正。

## Abstract
DNNs and LLMs increasingly rely on hardware accelerators, including in safety-critical domains, while technology scaling and growing model complexity make hardware faults more frequent. Existing system-level mechanisms typically treat the NPU as a monolithic unit, using coarse-grained replication that incurs prohibitive performance and hardware overheads, leaving a gap between reliability requirements and deployable solutions. To bridge this gap, we present Strix, a full-stack NPU reliability framework on an open-source SoC, spanning micro-architecture, ISA, and programming methods. Strix re-partitions the NPU along the system inference pipeline, identifies dominant failure modes, and attaches targeted safeguards, achieving sub-micro-second fault localisation, error detection, and correction with only 1.04$\times$ slowdown and minimal hardware overhead.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Strix**，一个针对神经网络处理器（NPU）可靠性的全栈系统框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着工艺节点缩放（如 7nm 以下）和模型复杂度增加，硬件故障（如位翻转、老化、辐射干扰）变得愈发频繁。在自动驾驶等安全关键领域，NPU 的硬件错误可能导致灾难性后果。
*   **研究背景**：现有的可靠性方案存在两极分化：要么是算法级的（缺乏直接检测能力），要么是指令级/微架构级的（如三倍模块冗余 TMR，开销高达 2-3 倍）。
*   **整体含义**：论文认为不应将 NPU 视为黑盒，而应从系统流水线视角出发，通过“先分析、后精准保护”的策略，在极低性能损耗下实现端到端的可靠性保证。

### 2. 方法论：核心思想与关键技术
Strix 的核心思想是将 NPU 划分为四个关键模块，并根据其失效模式部署差异化的保护机制：
*   **寄存器保护**：针对控制流和配置寄存器，采用轻量级的 **SEC-DED ECC**（单错纠正-双错检测），以应对其高扇出、低延迟的特性。
*   **局部存储器（Local Memory）保护**：设计了基于校验和（Checksum）的保护机制。通过 **Guardpad** 存储校验和，**Guardlinker** 记录故障位置。该机制将校验逻辑与高带宽访问路径解耦，支持多位错误的检测与纠正。
*   **脉动阵列（Systolic Array）保护**：引入了 **Shield Group**（屏蔽组）。利用矩阵乘法的线性性质（受 ABFT 算法启发），在阵列旁路并行计算行/列校验和。通过流水线设计和配置策略，将校验延迟隐藏在计算任务中。
*   **非线性算子保护**：结合数学特性（如 Softmax 输出和为 1，LayerNorm 均值为 0）进行一致性检查；对于复杂算子则采用冗余备份。
*   **全栈协同**：通过扩展 ISA（指令集），允许运行时（Runtime）观察保护状态并上报故障块（`mvout_error_block`）。

### 3. 实验设计
*   **实验平台**：部署在包含 20 个节点的 FPGA 集群（AMD Virtex UltraScale+ VU19P）上，基于开源 NPU 项目 Gemmini。
*   **数据集与场景**：
    *   **DNN 任务**：使用 Tiny ImageNet 和 DBPedia-14 数据集。
    *   **LLM 任务**：使用 LAMBADA 数据集进行语言预测。
*   **Benchmark 模型**：ResNet-50, AlexNet, MobileNet-V2, BERT, Llama-3.2-1B, Qwen-2.5-3B, Mistral-7B。
*   **对比方法**：
    *   **TMR**（硬件三倍冗余）。
    *   **IR**（指令冗余/软件重复执行）。
    *   **ReaLM**（最先进的基于统计算法的可靠性方案）。

### 4. 资源与算力
*   **硬件资源**：使用了大规模 FPGA 集群进行原型验证。
*   **算力细节**：论文未详细列出具体的 GPU 训练时长（因为重点在于推理阶段的硬件可靠性验证）。对于工业级配置（如 128x128 阵列），由于 FPGA 资源限制，部分 BF16 实验采用了基于 PyTorch 和 Spike 模拟器的行为级仿真。
*   **硬件开销**：在 28nm 工艺下，Strix 仅增加约 8.7% 的面积和 16.8% 的功耗（工业级配置下）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 INT8 到 FP32/BF16 的多种数据格式，测试了从 $10^{-10}$ 到 $10^{-5}$ 的不同故障率。
*   **充分性**：每项指标均取 10 次运行的平均值。实验不仅覆盖了传统的 CNN，还包含了当前主流的 Transformer 和 LLM，并对比了面积对齐（Area-matched）后的基准方案，确保了对比的公平性和客观性。

### 6. 主要结论与发现
*   **极低开销**：Strix 的几何平均性能减速仅为 **1.04倍**，远优于 TMR（1.83x-3.68x）和 ReaLM（1.22x-1.59x）。
*   **高覆盖率**：在典型故障率下，错误检测率超过 **99%**，纠错覆盖率超过 **95%**。
*   **亚微秒级响应**：实现了亚微秒级的故障定位与检测延迟。
*   **位敏感性发现**：实验证明 LLM 对 BF16 的高 4 位（符号位和指数位）极其敏感，而对尾数位的错误具有较高容忍度，这指导了 Strix 的精细化保护策略。

### 7. 优点
*   **系统级视角**：打破了单一层面的保护局限，实现了从微架构到编程层的全栈协同。
*   **性能卓越**：通过解耦设计和流水线优化，几乎消除了可靠性机制带来的性能损失。
*   **工业实用性**：基于开源 SoC 并在 FPGA 上验证，硬件开销可控，符合 ISO-26262 等安全标准的需求。

### 8. 不足与局限
*   **高故障率下的退化**：在极端高故障率（如 $>10^{-6}$）下，由于多位错误概率激增，纠错覆盖率会出现明显下降。
*   **浮点精度风险**：在 FP32/BF16 配置下，由于硬件实现的舍入误差，校验和匹配可能存在微小的精度损失，导致检测能力略低于定点数（INT8）。
*   **应用限制**：目前主要针对脉动阵列架构的 NPU，对于非规则架构（如基于向量机的加速器）的适用性有待进一步验证。

（完）
