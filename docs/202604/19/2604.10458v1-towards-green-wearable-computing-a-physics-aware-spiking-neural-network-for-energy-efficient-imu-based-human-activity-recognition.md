---
title: "Towards Green Wearable Computing: A Physics-Aware Spiking Neural Network for Energy-Efficient IMU-based Human Activity Recognition"
title_zh: 迈向绿色可穿戴计算：一种用于高能效 IMU 人体活动识别的物理感知脉冲神经网络
authors: "Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yinze Zhou"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10458v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于边缘设备能效HAR的无乘法器SNN架构
tldr: 针对可穿戴设备中人体活动识别（HAR）的高能耗挑战，本文提出了一种物理感知的脉冲神经网络（PAS-Net）。该架构完全摒弃乘法器，通过自适应对称拓扑混合器模拟人体关节约束，并利用因果神经调制器实现动态阈值以适应运动节奏。此外，引入的早退机制使其能高效处理连续IMU流。PAS-Net在多个数据集上实现了SOTA精度，并显著降低了能耗，为绿色可穿戴计算提供了新范式。
selection_source: fresh_fetch
motivation: 传统深度学习模型在资源受限的可穿戴设备上能耗过高，且标准脉冲神经网络难以捕捉复杂的生物力学特征和长时依赖。
method: 提出PAS-Net架构，结合了模拟人体物理约束的拓扑混合器、支持动态阈值的因果神经调制器以及基于置信度的早退机制。
result: "在七个数据集上取得领先精度，将运算转化为极低功耗的稀疏整数累加，并实现高达98%的动态能耗削减。"
conclusion: PAS-Net证明了结合物理先验与神经形态计算可实现超低功耗且高性能的始终在线人体活动识别。
---

## 摘要
基于可穿戴惯性测量单元（IMU）的人体活动识别（HAR）严重依赖深度神经网络（DNN），而 DNN 面临着巨大的计算和缓冲需求负担。其高能耗的浮点运算以及处理完整时间窗口的刚性要求，严重制约了电池受限的边缘设备。虽然脉冲神经网络（SNN）具有极高的事件驱动能效，但标准架构在处理复杂的生物力学拓扑和时间梯度退化方面存在困难。为了弥补这一差距，我们提出了物理感知脉冲神经网络（PAS-Net），这是一种专门为绿色 HAR 定制的完全无乘法器架构。在空间上，一种自适应对称拓扑混合器强制执行人体关节的物理约束。在时间上，一种 $O(1)$ 内存占用的因果神经调制器产生上下文感知的动态阈值神经元，能够主动适应非平稳的运动节奏。此外，我们利用时间脉冲误差目标，为连续 IMU 流解锁了一种灵活的早期退出机制。在七个不同的数据集上的评估表明，PAS-Net 在达到最先进准确率的同时，用稀疏的 0.1 pJ 整数累加取代了密集运算。至关重要的是，其置信度驱动的早期退出能力将动态能耗大幅降低了高达 98%。PAS-Net 为全天候在线的可穿戴传感建立了一个稳健、超低功耗的类脑计算标准。

## Abstract
Wearable IMU-based Human Activity Recognition (HAR) relies heavily on Deep Neural Networks (DNNs), which are burdened by immense computational and buffering demands. Their power-hungry floating-point operations and rigid requirement to process complete temporal windows severely cripple battery-constrained edge devices. While Spiking Neural Networks (SNNs) offer extreme event-driven energy efficiency, standard architectures struggle with complex biomechanical topologies and temporal gradient degradation. To bridge this gap, we propose the Physics-Aware Spiking Neural Network (PAS-Net), a fully multiplier-free architecture explicitly tailored for Green HAR. Spatially, an adaptive symmetric topology mixer enforces human-joint physical constraints. Temporally, an $O(1)$-memory causal neuromodulator yields context-aware dynamic threshold neurons, adapting actively to non-stationary movement rhythms. Furthermore, we leverage a temporal spike error objective to unlock a flexible early-exit mechanism for continuous IMU streams. Evaluated across seven diverse datasets, PAS-Net achieves state-of-the-art accuracy while replacing dense operations with sparse 0.1 pJ integer accumulations. Crucially, its confidence-driven early-exit capability drastically reduces dynamic energy consumption by up to 98\%. PAS-Net establishes a robust, ultra-low-power neuromorphic standard for always-on wearable sensing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Green Wearable Computing: A Physics-Aware Spiking Neural Network for Energy-Efficient IMU-based Human Activity Recognition》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对可穿戴设备在人体活动识别（HAR）任务中面临的**“算力-延迟死锁”**问题展开研究。
*   **研究动机**：传统的深度神经网络（DNN）依赖高能耗的 32 位浮点乘加运算（MAC），且必须缓存完整的时间窗口（如 2 秒）才能推理，导致电池寿命短且响应延迟高。
*   **核心挑战**：虽然脉冲神经网络（SNN）具有极高的能效，但标准 SNN 存在“空间盲区”（忽略人体关节拓扑）、“神经刚性”（固定阈值难以适应多变节奏）以及“梯度退化”等问题。
*   **整体含义**：提出 PAS-Net 架构，旨在通过物理感知的脉冲动力学，实现超低功耗、低延迟且高精度的“绿色可穿戴计算”。

### 2. 方法论：核心思想与关键技术
PAS-Net 采用完全无乘法器的事件驱动架构，核心技术包括：
*   **感知前端与脉冲嵌入**：使用不变性标记器（Invariant Tokenizer）提取旋转不变特征，并通过时间感知批归一化（T-BN）将连续信号转换为二进制脉冲流。
*   **自适应对称拓扑混合器（AST-Mixer）**：在空间维度上，通过学习一个严格对称的邻接矩阵来模拟人体关节的物理约束，避免在小数据集上过拟合，并利用稀疏累加（AC）代替乘法。
*   **$O(1)$ 内存因果神经调制器**：利用指数移动平均（EMA）提取运动节奏，动态调整神经元的发放阈值。当运动剧烈时降低阈值提高灵敏度，静止时提高阈值抑制噪声。
*   **时间脉冲误差（TSE）损失与早退机制**：在训练时对每个时间步进行监督，使模型具备置信度驱动的早退能力。一旦模型在窗口内达到足够置信度，立即停止计算并输出结果。

### 3. 实验设计
*   **数据集**：涵盖 7 个公开 IMU 数据集，包括多节点复杂运动（PAMAP2, Daily-Sports, TNDA-HAR, HuGaDB）、单节点日常活动（USC-HAD）以及临床/老年人数据（HAR70+, Parkinson FOG）。
*   **Benchmark 与对比方法**：
    *   **DNN 基准**：DeepConvLSTM, ResNet-SE, Self-HAR, Uni-HAR 等 7 种 SOTA 模型。
    *   **SNN 基准**：Spikformer, SpikeTCN, SpikeDrivenTransformer 等 11 种先进脉冲网络。
*   **评估协议**：采用严格的**受试者独立（Subject-Independent）**划分，确保测试集用户在训练中从未出现，以验证真实泛化能力。

### 4. 资源与算力
*   **硬件环境**：所有模型均在 **4 块 NVIDIA V100 GPU** 上使用 PyTorch 和 SpikingJelly 框架训练。
*   **训练细节**：采用 AdamW 优化器，训练时长最高为 **220 个 epoch**，配合余弦退火调度器和线性预热（Warmup）。
*   **能效评估**：基于 45nm CMOS 硬件逻辑估算能耗，将 FP32 MAC 计为 4.6 pJ，而脉冲触发的整数累加（AC）仅计为 0.1 pJ。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个不同特征的数据集上进行了全量对比，实验组数超过 120 组（18 种对比模型 × 7 个数据集）。
*   **消融实验**：针对拓扑路由、因果调制、TSE 损失分别做了消融分析，验证了各模块的增益。
*   **可视化分析**：提供了 t-SNE 聚类可视化、层级脉冲频率分析以及学习到的物理拓扑热力图。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集验证和严格的受试者划分，排除了数据泄露风险，能效评估也考虑了端到端的系统开销。

### 6. 主要结论与发现
*   **精度突破**：PAS-Net 在多个数据集上刷新了 SNN 的纪录，甚至在 PAMAP2 和 USC-HAD 上分别超过了最佳 DNN 模型 7.24% 和 8.99% 的准确率。
*   **极致能效**：相比重型 DNN（如 Self-HAR），PAS-Net 的能耗仅为其 1/81；相比传统 SNN，通过抑制冗余脉冲，能效提升了数倍。
*   **延迟削减**：早退机制使模型在处理结构化明显的活动时，仅需观察窗口前段即可做出判断，动态能耗最高削减 98%，决策延迟降低约 70%。

### 7. 优点（亮点）
*   **物理启发**：将人体解剖学约束（对称拓扑）融入 SNN 架构，解决了脉冲网络空间建模能力弱的痛点。
*   **动态适应**：因果神经调制器模拟了生物神经元的自适应特性，使模型能“呼吸”式地处理非平稳信号。
*   **端到端绿色计算**：从无乘法器设计到早退机制，全方位考虑了可穿戴设备的资源限制。

### 8. 不足与局限
*   **硬件部署偏差**：能效数据基于 CMOS 逻辑估算，尚未在真实的类脑芯片（如 Loihi 或 TrueNorth）上进行实测。
*   **传感器位移敏感性**：虽然有不变性特征提取，但若传感器佩戴位置发生严重偏移，预定义的物理拓扑可能需要重新校准。
*   **在线学习缺失**：目前模型仍需离线训练，未来若能结合 STDP 等局部学习规则实现设备端持续学习将更具竞争力。

（完）
