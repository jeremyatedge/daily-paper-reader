---
title: Unlocking the Edge deployment and ondevice acceleration of multi-LoRA enabled one-for-all foundational LLM
title_zh: 实现支持多 LoRA 的通用基础大语言模型的边缘部署与端侧加速
authors: "Sravanth Kodavanti, Sowmya Vajrala, Srinivas Miriyala, Utsav Tiwari, Uttam Kumar, Utkarsh Kumar Mahawar, Achal Pratap Singh, Arya D, Narendra Mutyala, Vikram Nelvoy Rajendiran, Sharan Kumar Allur, Euntaik Lee, Dohyoung Kim, HyeonSu Lee, Gyusung Cho, JungBae Kim"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18655v1"
tags: ["query:aiot"]
score: 9.0
evidence: 智能手机上高效端侧LLM推理的硬件感知框架
tldr: 本研究针对智能手机端部署大语言模型的内存和延迟挑战，提出了一种硬件感知的多LoRA推理框架。该框架在三星Galaxy S24/S25上实现了基于LLaMA的多语言基础模型，通过将特定任务的LoRA作为运行时输入，支持动态任务切换。核心技术包括多流解码（实现多种风格同步生成）和动态自投机解码（DS2D），结合INT4量化，显著提升了推理速度并降低了资源消耗，为移动端生成式AI的商业化提供了可行方案。
selection_source: fresh_fetch
motivation: 解决大语言模型在智能手机端部署时面临的严苛内存限制、高延迟以及运行时灵活性不足等工程挑战。
method: 采用多LoRA动态切换架构，结合多流解码技术同步生成多种风格响应，并引入无需草稿模型的动态自投机解码（DS2D）及INT4量化优化。
result: 在三星S24/S25设备上实现了4-6倍的内存与延迟优化，解码速度提升达2.3倍，且在9种语言和8个任务中保持了高精度。
conclusion: 该框架证明了在边缘设备上部署多用途基础大模型的技术可行性，显著推动了移动端生成式AI的商业化进程。
---

## 摘要
在智能手机上部署大语言模型（LLMs）面临着巨大的工程挑战，原因在于内存、延迟和运行时灵活性方面的严格限制。在本研究中，我们提出了一种硬件感知框架，用于在分别搭载高通 SM8650 和 SM8750 芯片组的三星 Galaxy S24 和 S25 设备上，实现基于 LLaMA 的多语言基础模型的高效端侧推理，该模型支持多种应用场景。我们的方法将特定应用的 LoRA 作为运行时输入集成到单个冻结的推理图中，从而在无需重新编译或产生额外内存开销的情况下实现动态任务切换。我们进一步引入了一种多流解码机制，能够在单次前向传递中并发生成多种风格变体（如正式、礼貌或幽默的回复），将延迟降低了高达 6 倍。为了加速 Token 生成，我们应用了动态自投机解码（DS2D），这是一种基于树的策略，无需草稿模型即可预测未来的 Token，使解码时间缩短了高达 2.3 倍。结合 INT4 量化和架构级优化，我们的系统在保持 9 种语言和 8 项任务准确性的同时，在内存和延迟方面实现了 4-6 倍的整体提升。这些结果证明了在边缘设备上部署多用途 LLM 的实际可行性，推动了生成式人工智能在移动平台上的商业化进程。

## Abstract
Deploying large language models (LLMs) on smartphones poses significant engineering challenges due to stringent constraints on memory, latency, and runtime flexibility. In this work, we present a hardware-aware framework for efficient on-device inference of a LLaMA-based multilingual foundation model supporting multiple use cases on Samsung Galaxy S24 and S25 devices with SM8650 and SM8750 Qualcomm chipsets respectively. Our approach integrates application-specific LoRAs as runtime inputs to a single frozen inference graph, enabling dynamic task switching without recompilation or memory overhead. We further introduce a multi-stream decoding mechanism that concurrently generates stylistic variations - such as formal, polite, or jovial responses - within a single forward pass, reducing latency by up to 6x. To accelerate token generation, we apply Dynamic Self-Speculative Decoding (DS2D), a tree-based strategy that predicts future tokens without requiring a draft model, yielding up to 2.3x speedup in decode time. Combined with quantization to INT4 and architecture-level optimizations, our system achieves 4-6x overall improvements in memory and latency while maintaining accuracy across 9 languages and 8 tasks. These results demonstrate practical feasibility of deploying multi-use-case LLMs on edge devices, advancing the commercial viability of Generative AI in mobile platforms.

---

## 论文详细总结（自动生成）

这篇论文由三星研究院（Samsung Research）的研究团队撰写，重点介绍了如何在资源受限的智能手机端（如三星 Galaxy S24 和 S25）高效部署和加速支持多任务（Multi-LoRA）的基础大语言模型。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在移动设备（边缘端）部署大语言模型（LLM）面临内存严苛、延迟高、运行时灵活性差（难以动态切换任务）等工程瓶颈。
*   **研究动机**：传统的 LoRA 适配通常需要将权重合并到主模型或重新编译计算图，这在端侧推理中会导致巨大的内存开销和响应延迟。研究旨在构建一个“一个模型服务所有任务”（One-for-all）的框架，实现无需云端依赖、低延迟、多任务并行的端侧 AI 体验。

### 2. 方法论：核心思想与关键技术
论文提出了一个硬件感知的推理框架，包含以下核心技术：
*   **多 LoRA 动态切换（Multi-LoRA Enablement）**：
    *   将 LoRA 权重视为推理图的**运行时输入（Placeholder）**，而非静态合并。
    *   通过这种方式，单个冻结的（Frozen）基础模型图可以根据输入动态加载不同的 LoRA 权重，实现任务（如翻译、摘要、风格转换）的秒级切换，且不增加内存冗余。
*   **并发 Token 生成（CTG, Concurrent Token Generation）**：
    *   针对“智能回复”等需要同时提供多种风格（如正式、礼貌、幽默）的场景，设计了掩码解码方案。
    *   利用共享的 KV-Cache 和张量布局，在单次前向传递中并行生成 8 个不同的输出流，将此类场景的延迟降低了 6 倍。
*   **动态自投机解码（DS2D, Dynamic Self-Speculative Decoding）**：
    *   **核心思想**：无需额外的草稿模型（Draft Model），通过前缀微调（Prefix Tuning）让模型具备预测未来多个 Token 的能力。
    *   **技术细节**：采用基于树的结构进行分支预测，并利用 NPU 的并行计算能力进行验证。通过动态选择最优分支配置（如 3,2 分支），实现半自回归解码。
*   **硬件级优化与量化**：
    *   将多头注意（MHA）分解为并行单头注意（SHA），将线性层转换为 1x1 卷积以适配移动端 NPU 的算子加速。
    *   采用 **INT4 权重 + INT8 激活** 的混合精度量化（QAT），显著降低内存占用。

### 3. 实验设计
*   **设备与芯片**：三星 Galaxy S24 (高通 SM8650) 和 S25 (高通 SM8750)。
*   **模型规模**：基于 LLaMA 架构的 1B 和 3B 参数模型。
*   **任务与语言**：涵盖 9 种语言（韩、英、德、西、法、意、葡、中、日）和 8 类任务（纠错、风格转换、智能回复、摘要、健康能量评分等）。
*   **基准对比（Benchmark）**：
    *   对比了全精度（FP32/FP16）与量化后的准确度。
    *   对比了有无 DS2D 加速的解码速度（Tokens/sec）。
    *   使用 GPT-4.5 作为评估器（G-Eval）来衡量端侧量化模型的相对准确率。

### 4. 资源与算力
*   **推理算力**：明确指出利用了高通骁龙芯片的 **NPU** 进行加速。
*   **训练算力**：论文**未明确说明**训练基础模型和微调 LoRA 所使用的具体 GPU 型号、数量及训练时长。这部分属于三星内部的专有技术细节。
*   **端侧资源占用**：1B 模型量化后 ROM 占用约 600MB+120MB (LoRA)，3B 模型在 S25 上的峰值内存约为 2.4GB。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了从 1B 到 3B 的模型跨度，并在两代旗舰手机上进行了验证。
*   **充分性**：论文提供了详尽的消融实验，包括不同 LoRA 加载策略的对比、不同分支配置对投机解码效率的影响、以及 9 种语言在不同任务下的准确度损失分析。
*   **客观性**：通过 G-Eval 引入了第三方大模型作为客观评价标准，证明了量化模型在大幅压缩后仍能保持 95% 以上的相对准确度。

### 6. 主要结论与发现
*   **性能提升**：通过架构优化和量化，系统实现了 **4-6 倍** 的整体内存和延迟改进。
*   **加速效果**：DS2D 技术使解码速度提升了 **2-2.3 倍**，在 S25 上达到了约 40-50 Tokens/sec 的生成速率。
*   **商业可行性**：该框架已成功应用于三星 Galaxy S24/S25 系列的“Galaxy AI”功能中，证明了多用途基础模型在边缘端商业化的可行性。

### 7. 优点（亮点）
*   **极高的工程实用性**：不同于纯学术研究，该方案直接解决了商用手机部署中的“任务切换”和“多风格同步生成”痛点。
*   **无需草稿模型的投机解码**：DS2D 避免了额外模型带来的内存负担，非常适合内存受限的移动端。
*   **硬件深度耦合**：通过 Linear-to-Conv 等变换，充分榨取了移动端 NPU 的计算性能。

### 8. 不足与局限
*   **LoRA 维度限制**：该框架要求所有 LoRA 模块具有相同的维度（Rank），这限制了针对不同复杂度任务进行差异化微调的灵活性。
*   **预测嵌入的静态性**：DS2D 中的预测嵌入（Forecast Embeddings）在推理过程中是静态的，对于语义波动剧烈或长程依赖的输入，Token 接受率可能会下降。
*   **风格固定**：多流解码（CTG）依赖于预定义的风格变体，目前不支持用户在运行时自定义任意风格。

（完）
