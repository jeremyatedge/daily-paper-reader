---
title: "AccelCIM: Systematic Dataflow Exploration for SRAM Compute-in-Memory Accelerator"
title_zh: AccelCIM：SRAM 存内计算加速器的系统性数据流探索
authors: "Chenhao Xue, Yukun Wang, An Guo, Yuhui Shi, Jinwei Zhou, Xiping Dong, Yihan Yin, Yuanpeng Zhang, Tianyu Jia, Wei Gao, Qiang Wu, Xin Si, Jun Yang, Guangyu Sun"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17692v1"
tags: ["query:aiot"]
score: 9.0
evidence: SRAM存内计算加速器的系统数据流探索
tldr: 针对SRAM存内计算（CIM）加速器在处理大型DNN模型时面临的片内外数据迁移开销问题，本文提出AccelCIM框架。该框架系统性地构建了涵盖CIM宏配置与阵列组织的数据流设计空间，并利用周期精确仿真和布局后PPA分析进行严谨评估。通过在LLM应用上的探索，为高效CIM加速器设计提供了理论指导和实践见解。
selection_source: fresh_fetch
motivation: 现有CIM研究通常假设模型能完全装入片内，忽略了有限容量导致的大规模模型数据流优化挑战。
method: 开发了AccelCIM框架，通过系统化建模设计空间并结合架构级仿真与物理实现分析来评估性能。
result: 通过对大语言模型应用的广泛探索，量化了不同硬件配置与数据流策略对加速器效率的影响。
conclusion: AccelCIM为SRAM CIM加速器的规范化设计提供了系统性的探索工具，有助于优化大模型推理的能效。
---

## 摘要
基于 SRAM 的存内计算（CIM）为深度神经网络（DNN）加速器提供了高计算密度和能效，但其有限的容量导致大型 DNN 模型面临显著的片上/片外数据搬运开销。现有的 CIM 加速器研究通常假设 DNN 模型可以完全存储在片上，使得高效的数据流设计在很大程度上尚未得到充分探索。本文介绍了 AccelCIM，这是一个针对 SRAM CIM 加速器的系统性数据流探索框架，旨在解决先前研究的两个关键局限性：（1）它构建了一个涵盖 CIM 宏单元配置和宏单元阵列组织的系统性数据流设计空间；（2）它引入了基于周期精确架构仿真和布局后 PPA 分析的严谨设计评估。我们进行了广泛的设计空间探索，并将 AccelCIM 应用于代表性的大语言模型（LLM）应用，为 CIM 加速器的规范化设计提供了实用见解。

## Abstract
SRAM-based compute-in-memory (CIM) offers high computational density and energy efficiency for deep neural network (DNN) accelerators, but its limited capacity causes on/off-chip data movement overhead for large DNN models. Existing CIM accelerator studies typically assume that DNN models fit entirely on-chip, leaving efficient dataflow design largely untapped. This paper introduces AccelCIM, a systematic dataflow exploration framework for SRAM CIM accelerator, which addresses two key limitations of prior work. (1) It formulates a systematic dataflow design space spanning CIM macro configurations and macro-array organizations. (2) It introduces rigorous design evaluation using cycle-accurate architectural simulation and post-layout PPA analysis. We conduct an extensive design space exploration and apply AccelCIM to representative LLM applications, providing practical insights for the principled design of CIM accelerators.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AccelCIM**，一个针对 SRAM 存内计算（CIM）加速器的系统性数据流探索框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然 SRAM CIM 具有极高的计算密度和能效，但其**片上容量有限**。随着深度神经网络（DNN）模型（如 LLM）规模的激增，模型无法完全装入片内，导致频繁的片内外数据迁移。
*   **研究动机**：
    *   现有研究多假设模型能完全装入片内，忽略了权重切换（Weight Swapping）的开销。
    *   现有框架对 CIM 宏单元（Macro）的建模过于简化，缺乏对物理布局（Layout）后 PPA（功耗、性能、面积）的准确评估。
    *   缺乏对不同互连拓扑（如脉动阵列 vs. 广播）和数据流（权重固定 vs. 输出固定）的系统性对比。

### 2. 方法论
AccelCIM 框架包含三个核心组件：
*   **系统化设计空间构建**：
    *   **宏单元级（Macro）**：参数包括累加长度（AL）、局部存储长度（LSL）、并行通道数（PC）、流水线级数（PL）以及是否支持**计算与 I/O 重叠（Compute-I/O Overlap）**。
    *   **阵列级（Array）**：参数包括阵列行列大小（BR, BC）、数据流策略（权重固定 WS、输出固定 OS）和互连拓扑（广播 Broadcast、脉动 Systolic）。
*   **周期精确架构仿真**：开发了内部仿真器，精确模拟 GEMV/GEMM 操作中的计算周期、权重更新周期及停顿。
*   **物理实现评估流（Macro Array Generator）**：
    *   利用开源 CIM 编译器生成宏单元布局，通过 SPICE 仿真提取定时和功耗。
    *   使用 Cadence 工具链（Genus/Innovus）进行自动化的 RTL 合成、布局布线（P&R），以获取真实的后布局面积和功耗数据，而非简单的解析模型。

### 3. 实验设计
*   **应用场景**：针对大语言模型（LLM）推理，包括 Qwen3-0.6B、LLaMA-3-8B/70B 和 GPT-3 175B。
*   **对比基准（Benchmark）**：
    *   对比了四种组合数据流：WS-Broadcast, WS-Systolic, OS-Broadcast, OS-Systolic。
    *   对比了是否开启 Compute-I/O Overlap（OL vs. NOL）。
    *   与现有工作（如 CIM-MXU）的 Pareto 前沿进行对比。
*   **量化指标**：端到端延迟、峰值吞吐量、功耗、面积效率（TOPS/mm²）和能效（TOPS/W）。

### 4. 资源与算力
*   **硬件工艺**：所有物理布局均基于 **28nm** 工艺节点。
*   **算力消耗**：论文未明确说明运行 AccelCIM 框架本身所需的具体 GPU/CPU 算力或仿真时长。实验中假设 CIM 核心的总计算能力上限为 20 TOPS。

### 5. 实验数量与充分性
*   **实验规模**：
    *   通过贝叶斯优化（Bayesian Optimization）在广阔的设计空间中搜索 Pareto 最优点。
    *   针对不同阵列规模（1x1 到 16x16）进行了扩展性分析。
    *   针对宏单元计算容量（从 $2^7$ 到 $2^{19}$ 个位乘法器）进行了消融实验。
*   **充分性评价**：实验覆盖了从边缘到云端的多种 LLM 部署场景，并结合了真实的物理设计反馈，实验设计较为客观、全面，能够反映硬件实现的真实约束。

### 6. 主要结论与发现
*   **发现 #1（互连拓扑）**：脉动互连（Systolic）在面积效率上显著优于广播互连（Broadcast），且不会降低能效，因为广播线缆在阵列增大时会消耗大量布线资源。
*   **发现 #2（宏单元规模）**：中等规模的 CIM 宏单元在能效和面积效率之间达到了最佳平衡。过大的宏单元内部广播开销大，过小的宏单元外围逻辑占比高。
*   **发现 #3（计算-I/O 重叠）**：开启重叠功能虽然能提升带宽受限设计的面积效率，但由于增加了内部电路复杂性，会导致能效下降 25-35%。
*   **LLM 适配性**：边缘场景倾向于使用支持重叠的 OS-Systolic 数据流；而超大规模模型（如 GPT-3）在多核扩展时，WS-Systolic-NOL 因其更低的单核面积和功耗而更具优势。

### 7. 优点
*   **高保真度**：结合了后布局（Post-layout）分析，解决了传统架构研究中“纸面数据”与真实芯片实现脱节的问题。
*   **系统性强**：首次深入探讨了 Compute-I/O Overlap 这一关键功能在系统级的利弊权衡。
*   **实用性**：针对当前热门的 LLM 任务提供了具体的硬件设计建议。

### 8. 不足与局限
*   **工艺节点单一**：实验仅基于 28nm 工艺，未探讨在更先进工艺（如 7nm/5nm）下互连延迟与逻辑功耗占比的变化。
*   **存储类型局限**：主要关注 SRAM CIM，未涉及非易失性存储（如 ReRAM, MRAM）等其他 CIM 技术，后者的容量特性可能带来不同的数据流结论。
*   **软件栈假设**：假设编译器能完美调度数据，未深入讨论实际软件映射中的碎片化或非规则算子支持问题。

（完）
