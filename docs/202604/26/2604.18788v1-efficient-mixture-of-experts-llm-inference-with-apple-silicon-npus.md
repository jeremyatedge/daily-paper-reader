---
title: Efficient Mixture-of-Experts LLM Inference with Apple Silicon NPUs
title_zh: 基于 Apple Silicon NPU 的高效混合专家模型（MoE）大语言模型推理
authors: "Afsara Benazir, Felix Xiaozhu Lin"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18788v1"
tags: ["query:aiot"]
score: 9.0
evidence: 利用Apple Silicon NPU实现高效的MoE LLM推理
tldr: 本研究针对 Apple Silicon NPU 在处理 MoE 架构 LLM 时面临的动态形状、不规则算子及调度开销等挑战，提出了 NPUMoE 推理引擎。该引擎通过离线校准预估专家容量，并结合静态分层、分组执行及负载感知图驻留技术，将密集计算卸载至 NPU。实验表明，NPUMoE 在多种 MoE 模型和长文本任务中显著降低了延迟和能耗，并大幅减少了 CPU 占用。
selection_source: fresh_fetch
motivation: 旨在解决 MoE 模型在 Apple NPU 上因动态路由导致的形状不固定、算子不兼容及高调度开销等推理瓶颈。
method: 提出 NPUMoE 引擎，利用离线校准驱动的静态专家容量分层、分组执行和负载感知图驻留技术，实现 NPU 与 CPU/GPU 的协同计算。
result: 在 Apple M 系列芯片上，NPUMoE 实现了 1.32x 至 5.55x 的延迟缩减，能效提升高达 7.37x，并显著降低了 CPU 负载。
conclusion: 通过合理的软硬件协同设计，可以将 MoE 模型的密集计算有效卸载至 NPU，从而在移动端设备上实现高效且节能的 LLM 推理。
---

## 摘要
Apple 神经网络引擎（ANE）是集成在每颗 Apple Silicon 芯片中的专用神经处理单元（NPU）。混合专家（MoE）大语言模型通过稀疏激活提高了推理效率，但在三个方面对 NPU 提出了挑战：专家路由具有不可预测性，并引入了动态张量形状，这与 NPU 的形状特定约束相冲突；若干不规则算子（如 top-k、scatter/gather 等）对 NPU 并不友好；启动大量小型专家内核会产生巨大的调度和同步开销。NPU 旨在分担 CPU 和 GPU 的 AI 计算任务；我们的目标是实现 MoE 推理的此类卸载，特别是在长上下文工作负载消耗大量系统资源的预填充（prefill）阶段。本文提出了 NPUMoE，这是一种运行时推理引擎，通过将密集的静态计算卸载到 NPU，同时为动态操作保留 CPU/GPU 回退路径，从而加速 Apple Silicon 上的 MoE 执行。NPUMoE 利用离线校准来估计专家容量和受欢迎程度，从而驱动三项关键技术：(1) 用于应对动态专家路由的专家容量静态分层；(2) 用于缓解 NPU 并发限制的分组专家执行；(3) 用于减少 CPU-NPU 同步开销的负载感知专家计算图驻留。在 Apple M 系列设备上使用三种代表性 MoE 大语言模型和四种长上下文工作负载进行的实验表明，NPUMoE 始终优于基准方案，通过有效的 NPU 卸载，将延迟降低了 1.32 倍至 5.55 倍，能效提高了 1.81 倍至 7.37 倍，并将 CPU 周期使用率降低了 1.78 倍至 5.54 倍。

## Abstract
Apple Neural Engine (ANE) is a dedicated neural processing unit (NPU) present in every Apple Silicon chip. Mixture-of-Experts (MoE) LLMs improve inference efficiency via sparse activation but are challenging for NPUs in three ways: expert routing is unpredictable and introduces dynamic tensor shapes that conflict with the shape-specific constraints of NPUs; several irregular operators, e.g., top-k, scatter/gather, etc., are not NPU-friendly; and launching many small expert kernels incurs substantial dispatch and synchronization overhead. NPUs are designed to offload AI compute from CPU and GPU; our goal is to enable such offloading for MoE inference, particularly during prefill, where long-context workloads consume substantial system resources.   This paper presents NPUMoE, a runtime inference engine that accelerates MoE execution on Apple Silicon by offloading dense, static computation to NPU, while preserving a CPU/GPU fallback path for dynamic operations. NPUMoE uses offline calibration to estimate expert capacity and popularity that drives three key techniques: (1) Static tiers for expert capacity to address dynamic expert routing (2) Grouped expert execution to mitigate NPU concurrency limits (3) Load-aware expert compute graph residency to reduce CPU-NPU synchronization overhead. Experiments on Apple M-series devices using three representative MoE LLMs and four long-context workloads show that NPUMoE consistently outperforms baselines, reducing latency by 1.32x-5.55x, improving energy efficiency by 1.81x-7.37x, and reducing CPU-cycle usage by 1.78x-5.54x through effective NPU offloading.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Mixture-of-Experts LLM Inference with Apple Silicon NPUs》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在 Apple Silicon 的神经网络引擎（ANE/NPU）上高效运行混合专家模型（MoE）大语言模型。
*   **研究背景**：MoE 架构通过稀疏激活提升了模型容量，但其动态特性与 NPU 的硬件设计存在严重冲突：
    *   **动态形状冲突**：NPU 要求静态、预定义的张量形状，而 MoE 的专家路由（Routing）会导致每步处理的 Token 数量不可预测。
    *   **算子不友好**：MoE 依赖的 Top-k、Scatter/Gather 等不规则算子在 NPU 上支持极差，常导致回退（Fallback）到 CPU。
    *   **调度开销高**：MoE 包含大量小型专家内核，频繁启动这些微小任务会产生巨大的 CPU-NPU 同步和调度延迟。
*   **动机**：在长文本处理中，预填充（Prefill）阶段消耗了绝大部分资源。通过将计算卸载到 NPU，可以释放 CPU/GPU 资源用于系统响应或其他任务，同时提升能效。

### 2. 方法论：NPUMoE 推理引擎
NPUMoE 的核心思想是**将动态的 MoE 执行流转化为 NPU 友好的静态密集计算流**，主要包含以下三项关键技术：
*   **专家容量静态分层 (Static Tiers for Expert Capacity)**：
    *   通过离线校准（Offline Calibration）统计各层专家的负载分布。
    *   将专家划分为不同的容量等级（Tiers），热门专家分配较大的固定 Token 容量，冷门专家分配较小容量。
    *   若实际 Token 超过容量，采用基于显著性得分（Activation-based Saliency）的 Token 丢弃策略以维持精度。
*   **分组专家执行 (Grouped Expert Execution)**：
    *   将多个专家合并为一个单一的静态 FFN 计算图。
    *   通过一次 NPU 调用同时执行多个专家，共享启动开销，缓解 NPU 的并发限制和调度压力。
*   **负载感知计算图驻留 (Load-Aware Residency)**：
    *   根据专家受欢迎程度决定其位置：将“热专家组”常驻 NPU，而将负载极低、无法抵消调度开销的“冷专家组”保留在 CPU/GPU 执行，从而减少不必要的同步。

### 3. 实验设计
*   **硬件平台**：Apple M2 Max (64GB RAM) 和 M2 Ultra (192GB RAM)。
*   **测试模型**：
    *   Phi-3.5-MoE-Instruct (42.6B 参数)
    *   Phi-tiny-MoE-Instruct (3.8B 参数)
    *   Qwen3-30B-A3B (30.5B 参数)
*   **数据集/场景**：针对长文本预填充任务，使用了 HellaSwag (常识推理)、BoolQ (问答) 和 RULER (长文本检索与聚合)。
*   **对比基准 (Benchmarks)**：
    *   **Core ML (CPU only)**：全 CPU 执行。
    *   **Core ML (Naive)**：原生 Core ML 调度，专家 FFN 尝试在 NPU 运行但缺乏优化。
    *   **ANEMLL**：现有的针对密集 Transformer 优化的 NPU 库。

### 4. 资源与算力
*   **推理设备**：明确使用了 Apple M2 系列芯片。
*   **训练时长**：文中未涉及模型训练，因为 NPUMoE 是一个**推理引擎**，直接加载预训练好的模型权重。
*   **转换工具**：使用了自研的 ANEMLL 库进行模型转换，并基于 Swift 和 Core ML 栈构建运行时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同规模的 MoE 模型，在两种高端 Apple Silicon 芯片上进行了测试。
*   **消融实验**：详细评估了静态分层、分组执行、负载感知驻留等每一项技术对延迟和能效的贡献。
*   **充分性**：实验不仅测量了端到端延迟，还深入分析了 CPU 周期占用、能耗（EPT）、Token 丢弃率对精度的影响，以及离线校准在不同数据集间的稳定性。实验设计客观，对比基准具有代表性。

### 6. 主要结论与发现
*   **性能提升**：相比基准方案，NPUMoE 将预填充延迟降低了 **1.32x–5.55x**。
*   **能效卓越**：能效提升了 **1.81x–7.37x**，显著延长了电池续航能力。
*   **资源释放**：CPU 周期占用降低了 **1.78x–5.54x**，证明了计算任务被成功且高效地卸载到了 NPU。
*   **精度保持**：尽管存在 Token 丢弃，但在测试任务上的精度损失小于 **1.1%**，证明了静态容量设计的可行性。

### 7. 优点
*   **系统性创新**：首次系统性地解决了 MoE 动态性与移动端 NPU 静态约束之间的矛盾。
*   **软硬协同**：充分利用了 Apple Silicon 的统一内存架构，通过精细的计算图设计规避了硬件短板。
*   **实用性强**：针对长文本这一实际痛点进行优化，且在显著提升性能的同时几乎不损失精度。

### 8. 不足与局限
*   **解码阶段优化有限**：目前主要针对预填充（Prefill）阶段，由于解码（Decode）是内存受限且单 Token 执行，NPU 的调度开销更难被摊销。
*   **Token 丢弃风险**：在极端不平衡的负载下，Token 丢弃可能对某些特定任务的精度产生更大影响。
*   **平台依赖**：该方法高度依赖 Apple ANE 的特性，虽然理论可推广至其他 NPU（如高通 Hexagon），但具体实现需重新适配。

（完）
