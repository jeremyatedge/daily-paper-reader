---
title: Silicon Aware Neural Networks
title_zh: 硅感知神经网络
authors: "Sebastian Fieldhouse, Kea-Tiong Tang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19334v1"
tags: ["query:aiot"]
score: 9.0
evidence: 针对硅感知的神经网络软硬协同设计
tldr: 本文提出了一种将可微分逻辑门网络（DLGN）直接映射到定制硅芯片的方法。通过将训练好的模型转换为门级网表，并引入一种基于标准单元库面积的损失函数来优化电路面积和功耗，实现了神经网络与硬件实现的深度融合。在SkyWater 130nm工艺下的仿真显示，该方法在保持高精度的同时，实现了极高的推理速度和极低的功耗。
selection_source: fresh_fetch
motivation: 旨在解决如何将离散逻辑门神经网络高效地转化为定制数字CMOS集成电路，并优化其硬件资源占用。
method: 提出将DLGN模型一对一映射为门级网表，并利用感知标准单元面积的新型损失函数在训练中直接优化电路面积。
result: "在SkyWater 130nm工艺仿真中，该电路以97%的MNIST分类准确率实现了每秒4180万次推理，功耗仅为83.88毫瓦。"
conclusion: 该研究证明了硅感知神经网络设计能显著提升硬件效率，为超高性能、低功耗的边缘计算硬件提供了新路径。
---

## 摘要
机器学习领域的近期研究表明，深度学习可以训练由离散逻辑门函数组成的神经网络，从而在 CPU、GPU 和 FPGA 平台上以极高的速度执行简单的图像分类任务。由于这些可微分逻辑门网络（DLGN）由离散逻辑门构成，它们天然适用于定制硅片的实现。在本研究中，我们提出了一种通过将训练好的模型转换为门级网表，将 DLGN 一一映射到数字 CMOS 标准单元库的方法。我们还提出了一种新颖的损失函数，使 DLGN 能够根据目标标准单元库中标准单元的面积，通过最小化每个神经元的预期面积，来优化所得电路的面积并间接降低功耗。最后，我们首次展示了 DLGN 作为硅电路在仿真中的实现，使用 Cadence 标准单元库在 SkyWater 130nm 工艺下将 DLGN 布局为定制硬宏，并进行了布局后功耗分析。研究结果显示，我们的定制宏在 MNIST 数据集上能以 97% 的准确率每秒执行 4180 万次分类，功耗为 83.88 mW。

## Abstract
Recent work in the machine learning literature has demonstrated that deep learning can train neural networks made of discrete logic gate functions to perform simple image classification tasks at very high speeds on CPU, GPU and FPGA platforms. By virtue of being formed by discrete logic gates, these Differentiable Logic Gate Networks (DLGNs) lend themselves naturally to implementation in custom silicon - in this work we present a method to map DLGNs in a one-to-one fashion to a digital CMOS standard cell library by converting the trained model to a gate-level netlist. We also propose a novel loss function whereby the DLGN can optimize the area, and indirectly power consumption, of the resulting circuit by minimizing the expected area per neuron based on the area of the standard cells in the target standard cell library. Finally, we also show for the first time an implementation of a DLGN as a silicon circuit in simulation, performing layout of a DLGN in the SkyWater 130nm process as a custom hard macro using a Cadence standard cell library and performing post-layout power analysis. We find that our custom macro can perform classification on MNIST with 97% accuracy 41.8 million times a second at a power consumption of 83.88 mW.

---

## 论文详细总结（自动生成）

以下是对论文《Silicon Aware Neural Networks》（硅感知神经网络）的结构化深入总结：

### 1. 研究动机与背景
*   **核心矛盾**：深度学习依赖于连续可微函数以实现梯度下降收敛，而数字电路在底层运行于离散逻辑。这种差异导致传统神经网络在硬件部署时需要复杂的量化和近似处理。
*   **研究背景**：前人提出的“可微分逻辑门网络（DLGN）”通过将逻辑门函数松弛为连续函数，实现了直接训练逻辑门网络。然而，这些研究主要集中在 CPU、GPU 和 FPGA 平台，尚未有研究将其直接映射到定制硅片（ASIC）设计中。
*   **研究目标**：探索如何将 DLGN 直接转换为标准单元电路，并在训练阶段引入硬件物理属性（如面积）的约束，实现真正的“软硬协同设计”。

### 2. 方法论
*   **核心思想**：将 DLGN 的训练过程与数字集成电路的标准单元库（Standard Cell Library）深度耦合，使网络在学习分类任务的同时，学习如何选择物理面积更小的逻辑门。
*   **关键技术细节**：
    *   **逻辑门映射**：将 16 种可能的 2 输入逻辑门（如 AND, OR, XOR 等）与 SkyWater 130nm 工艺下的标准单元进行一一对应。对于没有直接对应单元的逻辑，采用组合单元（如 INV + NOR）并累加面积。
    *   **硅感知损失函数**：引入面积损失项 $L_{area}$。利用训练中的 Softmax 概率分布 $p_n$ 计算每个神经元的“预期面积” $E[area_n] = \sum p_{n,i} \cdot A_i$（$A_i$ 为对应逻辑门的物理面积）。
    *   **总损失函数**：$L_{total} = L_{CE} + \delta \cdot L_{area}$。通过超参数 $\delta$ 平衡分类准确率与电路面积。
    *   **架构调整**：为适应 130nm 工艺有限的布线资源，将原有的宽浅架构（6层×6.4万神经元）调整为窄深架构（18层×4000神经元），以提高布线成功率。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（物体分类）。
*   **Benchmark（基准）**：
    *   **内部对比**：对比了带有面积优化损失函数和不带该损失函数的 DLGN 模型。
    *   **外部对比**：与现有的 FPGA 实现（如 FINN）、65nm/40nm/28nm CMOS 工艺下的各类 AI 加速器（如 Tsetlin Machine、混合信号 CNN 等）进行性能对比。
*   **评估指标**：准确率（Accuracy）、延迟（Latency）、功耗（Power）、推理能耗（Energy per Inference）以及芯片面积。

### 4. 资源与算力
*   **训练算力**：文中**未明确说明**具体的训练硬件（如 GPU 型号或数量）及训练时长。
*   **EDA 工具**：使用了 Cadence 的标准单元库，利用 Xcelium 进行门级仿真验证，利用 Innovus 进行物理布局布线（Layout）和后仿真功耗分析。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对超参数 $\delta$ 进行了消融实验（取值从 $10^{-5}$ 到 $0.1$），验证了其对准确率和面积的影响。
    *   在两个主流数据集（MNIST, CIFAR-10）上验证了方法的通用性。
    *   完成了从算法训练到物理版图（Hard Macro）生成的全流程仿真。
*   **充分性评价**：实验设计较为完整，涵盖了从高层算法到低层物理实现的闭环。但由于 130nm 工艺较老，且仅在仿真层面完成，缺乏实际流片（Tape-out）后的实测数据支持。

### 6. 主要结论与发现
*   **面积优化显著**：引入面积损失函数后，MNIST 模型的平均神经元面积从 $9.38 \mu m^2$ 降至 $6.11 \mu m^2$（下降约 35%），而准确率仅轻微下降（从 98.04% 降至 97.66%）。
*   **极高性能**：在 SkyWater 130nm 工艺下，MNIST 宏电路实现了 **4180 万次/秒** 的吞吐量，关键路径延迟仅为 23.9 ns。
*   **高能效比**：功耗为 83.88 mW，单次推理能耗为 2.0 nJ。若等效缩放到 16nm 工艺，推理能耗预计可低至 **69 pJ**，远优于传统的混合信号或数字 CNN 加速器。

### 7. 优点
*   **端到端自动化**：提出了一种从训练好的神经网络直接生成门级网表和物理版图的清晰路径。
*   **硬件感知训练**：通过可微的方式将物理面积指标引入损失函数，打破了算法优化与硬件实现之间的壁垒。
*   **超低延迟**：由于网络完全由组合逻辑构成，推理过程极快，非常适合对实时性要求极高的边缘 AI 场景。

### 8. 不足与局限
*   **布线挑战**：在较老工艺（如 130nm）下，由于逻辑门连接的随机性，大规模网络的布线非常困难，限制了模型规模的进一步扩大。
*   **任务复杂度有限**：目前仅在 MNIST 和 CIFAR-10 等简单数据集上验证，对于更复杂的视觉任务（如 ImageNet），逻辑门网络的表达能力和可扩展性仍待证明。
*   **缺乏实测**：所有硬件数据均来自 EDA 工具仿真，实际流片中可能存在的工艺偏差、信号完整性等问题未在文中体现。

（完）
