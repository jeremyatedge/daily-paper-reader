---
title: "E2AFS: Energy-Efficient Approximate Floating Point Square Rooter for Error Tolerant Computing"
title_zh: E2AFS：面向容错计算的高能效近似浮点平方根运算器
authors: "Prateek Goyal, Jatin Kumar Reddy Mothe, Swara Rajesh Shelke, Sujit Kumar Sahoo"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16964v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于边缘AI和嵌入式系统的能效硬件
tldr: 针对边缘AI和嵌入式系统中浮点平方根运算的高能耗和高延迟问题，本文提出了一种名为E2AFS的轻量级、完全无乘法器的近似浮点平方根架构。该设计通过优化逻辑深度和减少开关活动，显著提升了硬件效率。在Artix-7 FPGA上的实验表明，E2AFS在动态功耗、延迟和功耗延迟积（PDP）方面均优于现有架构，且在Sobel边缘检测和K-means聚类应用中表现出良好的精度，适用于低功耗实时边缘计算。
selection_source: fresh_fetch
motivation: 传统的浮点平方根实现依赖乘法器或迭代流水线，导致硬件复杂度高、能耗大，难以满足边缘计算设备的低功耗需求。
method: 提出一种完全无乘法器的轻量级架构E2AFS，通过减少逻辑深度和最小化开关活动来优化硬件性能。
result: 在FPGA实现中，E2AFS实现了7.63 mW的最低动态功耗、4.639 ns的最短延迟以及35.39 pJ的最小功耗延迟积。
conclusion: E2AFS在保持高精度的同时显著降低了硬件开销，是实时边缘AI和嵌入式系统等容错计算场景的理想选择。
---

## 摘要
浮点平方根计算是边缘人工智能、信号处理和嵌入式系统中对功耗和延迟至关重要的操作。传统的实现方式通常依赖于乘法器或迭代流水线，导致硬件复杂度、开关活动和能耗增加。本研究提出了 E2AFS，这是一种轻量级且完全无乘法器的浮点平方根架构，专为高能效计算而优化。通过降低逻辑深度和最小化开关活动，所提出的设计在硬件效率和性能方面取得了显著提升。在 Artix-7 设备上的 FPGA 实现表明，与现有的 ESAS 和 CWAHA 架构相比，E2AFS 实现了最低的动态功耗 (7.63 mW)、最短的关键路径延迟 (4.639 ns) 以及最小的功耗延迟积 (35.39 pJ)。使用多种精度指标进行的误差评估以及图形分析表明，E2AFS 能够紧密近似精确的平方根函数，且偏差始终保持在较低水平。在 Sobel 边缘检测和 K-means 颜色量化中的应用级验证进一步证实了其在低功耗实时边缘和嵌入式平台上的适用性。

## Abstract
Floating-point square-root computation is a power- and delay-critical operation in edge-AI, signal-processing, and embedded systems. Conventional implementations typically rely on multipliers or iterative pipelines, resulting in increased hardware complexity, switching activity, and energy consumption. This work presents E2AFS, a lightweight and fully multiplier-free floating-point square-root architecture optimized for energy-efficient computation. By reducing logic depth and minimizing switching activity, the proposed design achieves substantial improvements in hardware efficiency and performance. FPGA implementation on an Artix-7 device demonstrates that E2AFS achieves the lowest dynamic power (7.63 mW), the shortest critical-path delay (4.639 ns), and the minimum power-delay product (35.39 pJ) compared to existing ESAS and CWAHA architectures. Error evaluation using multiple accuracy metrics, together with graphical analysis, shows that E2AFS closely approximates the exact square-root function with consistently low deviation. Application-level validation in Sobel edge detection and K-means color quantization further confirms its suitability for low-power real-time edge and embedded platforms.

---

## 论文详细总结（自动生成）

### E2AFS：面向容错计算的高能效近似浮点平方根运算器论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：在边缘人工智能（Edge-AI）、物联网（IoT）和嵌入式系统中，浮点平方根（FP Square Root）运算是功耗和延迟的主要来源。传统的硬件实现依赖复杂的乘法器或多级迭代流水线，导致硬件资源占用高、能效比低。
*   **研究动机**：针对图像处理、机器学习等具有“容错性”的应用场景，通过近似计算（Approximate Computing）牺牲极小的精度来换取显著的能效提升。论文旨在设计一种完全无乘法器、低延迟且高能效的 FP16（半精度浮点数）平方根运算架构。

#### 2. 方法论
E2AFS 的核心思想是利用二项式展开简化运算，并通过双层近似策略消除乘法器。
*   **基础公式**：将输入 $M$ 表示为 $2^r(1+Y)$，其平方根近似为 $\sqrt{M} \approx 2^{r/2}(1 + Y/2)$。
*   **奇偶指数处理**：
    *   **偶数指数**：直接通过移位实现 $2^{r/2}$。
    *   **奇数指数**：引入 $\sqrt{2} \approx 1.5$ 的过高估计，同时将尾部项改为 $(1 + Y/4)$ 进行过低估计补偿，从而抵消整体误差。
*   **双层近似策略**：
    *   以尾数 $Y=0.5$ 为分界点。
    *   当 $Y < 0.5$ 时，使用基础线性近似。
    *   当 $Y \ge 0.5$ 时，引入预定义的**常数补偿项**（如 -0.045 或 +0.333），以修正高数值区域的系统性偏差。
*   **硬件实现**：整个架构仅由归一化模块、指数奇偶检测器、尾数阈值比较器、加法器/移位器单元以及输出重组模块组成，完全不含乘法器。

#### 3. 实验设计
*   **实验平台**：使用 Xilinx Vivado 2019.2 工具，在 Artix-7 FPGA (XC7A35T) 上进行门级 Verilog 实现和综合。
*   **对比基准（Benchmarks）**：
    *   **ESAS**：基于指数级数的近似设计。
    *   **CWAHA-4/CWAHA-8**：簇类近似硬件架构的不同变体。
*   **评估指标**：
    *   **硬件性能**：查找表（LUT）数量、动态功耗（DP）、关键路径延迟（CPD）、功耗延迟积（PDP）。
    *   **精度指标**：平均误差距离（MED）、平均相对误差距离（MRED）、均方误差（MSE）等。
*   **应用场景**：
    *   **Sobel 边缘检测**：测试在 Peppers、Boat、House、Barbara 四张标准灰度图上的 PSNR 和 SSIM。
    *   **K-means 颜色量化**：评估在聚类算法中计算欧几里得距离时的表现。

#### 4. 资源与算力
*   **硬件资源**：实验在 **Xilinx Artix-7 FPGA** 上完成。
*   **算力说明**：论文未涉及 GPU 训练，因为其研究重点是数字电路逻辑设计。功耗评估基于 SAIF 文件进行后实现分析，确保了开关活动的准确性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **穷举验证**：对 FP16 的整个输入空间（$2^{16}$ 个组合）进行了详尽的仿真，计算了所有标准误差指标。
    *   **应用测试**：在 4 张不同特征的图像上进行了边缘检测对比，并在 K-means 算法中进行了视觉质量分析。
*   **充分性评价**：实验设计较为充分。通过图形化分析（误差曲线重合度）和综合性能指标（FoM）对比，客观地展示了该设计在精度与能效之间的平衡。

#### 6. 主要结论
*   **能效领先**：E2AFS 实现了最低的动态功耗（7.63 mW）和最短的关键路径延迟（4.639 ns）。
*   **PDP 优势**：其功耗延迟积（PDP）为 35.39 pJ，比对比方法（ESAS 和 CWAHA）分别提升了约 15% 至 38% 的能效。
*   **精度达标**：在边缘检测和颜色量化应用中，E2AFS 的 PSNR 和 SSIM 指标与高精度近似方案（如 CWAHA-8）非常接近，但硬件开销显著更低。

#### 7. 优点
*   **完全无乘法器**：极大地降低了逻辑深度和开关活动，适合对面积和功耗极其敏感的边缘设备。
*   **创新的误差补偿**：通过奇偶指数的“过高/过低”相互补偿机制，巧妙地解决了 $\sqrt{2}$ 处理难题。
*   **高能效比**：在保持较低误差（MRED 约 1.5%）的同时，提供了同类设计中最佳的 PDP 表现。

#### 8. 不足与局限
*   **应用范围限制**：该设计专为 FP16 优化，对于需要更高精度（如 FP32 或 FP64）的科学计算场景，其近似误差可能无法接受。
*   **常数项硬编码**：补偿项（如 -0.045）是基于统计分析确定的固定值，虽然在通用场景表现良好，但在某些极端分布的数据集下可能不是最优。
*   **硬件对比局限**：未与最新的基于查找表（LUT-based）或 CORDIC 算法的优化实现进行直接对比。

（完）
