---
title: Energy Efficient LSTM Accelerators for Embedded FPGAs through Parameterised Architecture Design
title_zh: 通过参数化架构设计实现面向嵌入式 FPGA 的高能效 LSTM 加速器
authors: "Chao Qian, Tianheng Ling, Gregor Schiele"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19293v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于资源受限嵌入式FPGA的高能效LSTM加速器
tldr: 针对嵌入式FPGA资源受限的挑战，本文提出了一种参数化、高能效的LSTM硬件加速器设计。该设计通过优化DSP资源利用和激活函数实现，显著提升了推理速度并降低了功耗。实验表明，该加速器在实时推理中达到了11.89 GOP/s/W的能效比，为嵌入式设备上的时间序列分析提供了高效且灵活的硬件支持。
selection_source: fresh_fetch
motivation: 旨在解决嵌入式FPGA在处理LSTM网络时面临的计算资源匮乏、高能耗以及实时处理能力不足的问题。
method: 提出一种可参数化的硬件架构，允许开发者根据资源限制灵活配置DSP使用策略和激活函数的硬件实现。
result: 该加速器在实时推理中实现了每秒32873个样本的处理速度，并达到了11.89 GOP/s/W的高能效比。
conclusion: 通过参数化架构设计，该方案在资源受限的嵌入式平台上成功实现了高性能与低功耗的平衡。
---

## 摘要
长短期记忆网络 (LSTM) 是一种重要的深度学习技术，适用于对嵌入式设备的本地传感器数据流进行设备端时间序列分析。在本文中，我们提出了一种专门针对资源受限的嵌入式现场可编程门阵列 (FPGA) 优化的新型 LSTM 硬件加速器设计。与相关工作相比，我们的设计提高了执行速度并降低了能耗。此外，该设计可以通过多种优化参数（例如 DSP 的使用或激活函数的实现方式）来适应不同的应用场景。我们阐述了关键的设计决策并对其性能进行了评估。在 32873 样本/秒的实时推理中，该加速器实现了 11.89 GOP/s/W 的能效。

## Abstract
Long Short-term Memory Networks (LSTMs) are a vital Deep Learning technique suitable for performing on-device time series analysis on local sensor data streams of embedded devices. In this paper, we propose a new hardware accelerator design for LSTMs specially optimised for resource-scarce embedded Field Programmable Gate Arrays (FPGAs). Our design improves the execution speed and reduces energy consumption compared to related work. Moreover, it can be adapted to different situations using a number of optimisation parameters, such as the usage of DSPs or the implementation of activation functions. We present our key design decisions and evaluate the performance. Our accelerator achieves an energy efficiency of 11.89 GOP/s/W during a real-time inference with 32873 samples/s.

---

## 论文详细总结（自动生成）

这篇论文针对在资源受限的嵌入式 FPGA 上部署 LSTM（长短期记忆网络）面临的挑战，提出了一种参数化、高能效的硬件加速器架构。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：LSTM 在处理时间序列分析方面表现优异，但其计算复杂度高，难以在资源极度受限（如低功耗、少逻辑单元）的嵌入式设备上实时运行。
*   **研究动机**：
    *   **现有方案局限**：微控制器（MCU）速度太慢，GPU 功耗太高。
    *   **FPGA 挑战**：虽然 FPGA 具有高能效和可重构性，但现有的 LSTM 加速器研究多集中在云端或边缘服务器级 FPGA，针对小型嵌入式 FPGA（如 Xilinx Spartan-7）的研究较少，且往往面临吞吐量低或过度依赖 DSP 资源的问题。
    *   **目标**：设计一个既能支持多层大模型，又能通过参数化配置优化能效和资源利用率的 RTL 级加速器。

### 2. 方法论
论文提出了一个模板化的 RTL 设计，核心技术细节包括：
*   **8位定点量化**：采用 (4, 8) 定点格式（4位小数，总宽8位），在保持模型精度的同时，允许使用 LUT（查找表）代替 DSP 实现乘法器，从而节省稀缺的 DSP 资源。
*   **激活函数优化**：
    *   使用分段线性的 **HardTanh** 和自定义的 **HardSigmoid*** 替代复杂的 Tanh 和 Sigmoid。
    *   **HardSigmoid\*** 的斜率设为 0.125（1/8），以便通过简单的位移操作实现，减少硬件开销。
    *   提供了三种实现方式：算术逻辑（Arithmetic）、全映射查找表（1to1）和步进查找表（Step），用户可根据资源需求选择。
*   **5级流水线 ALU 设计**：将乘加运算（MAC）分解为初始化、数据加载、乘法、累加、舍入输出五个阶段。这种设计显著提升了最大工作频率（从 100MHz 提升至 204MHz）。
*   **参数化架构**：支持配置隐藏层大小、输入维度、ALU 资源类型（DSP 或 LUT）、权重存储类型（BRAM 或 LUTRAM）等，使加速器能灵活适配不同规模的模型。

### 3. 实验设计
*   **数据集/场景**：使用 **PeMS-4W** 数据集进行单步交通速度预测。
*   **硬件平台**：Xilinx Spartan-7 XC7S15（极小规模 FPGA）。
*   **Benchmark（对比对象）**：
    *   Qian et al. (2022/2023) 的先前工作。
    *   Chen et al. (2021) 的 Eciton 加速器。
    *   Hasib-Al-Rashid et al. (2020) 的低功耗 LSTM 处理器。
*   **对比指标**：资源利用率（LUT/DSP/BRAM）、最大时钟频率、吞吐量（GOP/s）、功耗（mW）及能效比（GOP/s/W）。

### 4. 资源与算力
*   **算力平台**：实验主要在 **Xilinx Spartan-7 XC7S15** FPGA 上完成。
*   **训练细节**：使用 **ElasticAI-Creator** 工具进行量化感知训练（QAT），而非简单的后量化，以确保 8 位量化下的模型精度（MSE 达到 0.040）。
*   **功耗测量**：通过 Xilinx Power Estimator (XPE) 估算，并在真实硬件上进行了实测验证。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **消融实验**：对比了不同激活函数实现方式（Arithmetic vs 1to1 vs Step）对延迟和资源的影响。
    *   **扩展性实验**：测试了隐藏层大小从 20 增加到 200 时资源的消耗趋势。
    *   **资源对比**：对比了使用 DSP 和不使用 DSP 两种模式下的性能差异。
*   **充分性评价**：实验设计较为充分，不仅有仿真数据，还有真实硬件的实测数据，客观地展示了参数化设计在不同约束下的灵活性。

### 6. 主要结论与发现
*   **性能提升**：最大时钟频率达到 **204 MHz**，吞吐量提升了约 **2.04 倍**。
*   **高能效**：在真实硬件上实现了 **11.89 GOP/s/W** 的能效，比前人工作提升了约 59% 至 2.33 倍。
*   **资源优化**：LUT 利用率平均降低 29.62%，LUTRAM 降低 33.33%。
*   **模型支持**：在极小的 XC7S15 FPGA 上，该架构最高可支持 5 层 LSTM 网络（每层隐藏层大小为 60）。

### 7. 优点
*   **灵活性极高**：通过参数化设计，开发者可以在“节省 DSP”和“追求极致能效”之间做出权衡。
*   **流水线优化显著**：5 级流水线设计成功突破了嵌入式 FPGA 的频率瓶颈。
*   **软硬件协同**：结合了量化感知训练和硬件友好的激活函数修改，保证了低比特下的模型精度。

### 8. 不足与局限
*   **模型规模上限**：虽然支持多层，但在 XC7S15 这种超小型芯片上，隐藏层大小超过 180 后 BRAM 会成为瓶颈，限制了更复杂任务的处理。
*   **应用领域单一**：实验仅限于时间序列预测，未在语音识别或自然语言处理等更复杂的 LSTM 应用场景中验证。
*   **量化损失风险**：虽然 8 位定点在本文任务中表现良好，但对于某些对精度极度敏感的任务，可能需要更高位宽支持，而文中未深入讨论位宽增加对频率的具体负面影响。

（完）
