---
title: "GraphLeap: Decoupling Graph Construction and Convolution for Vision GNN Acceleration on FPGA"
title_zh: GraphLeap：解耦图构建与卷积以实现 FPGA 上的视觉图神经网络加速
authors: "Anvitha Ramachandran, Dhruv Parikh, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21290v1"
tags: ["query:aiot"]
score: 9.0
evidence: 通过解耦图构建在FPGA上加速视觉图神经网络
tldr: Vision GNN (ViG) 因每层动态构建kNN图导致计算效率低下。本文提出GraphLeap，通过解耦图构建与特征更新，实现跨层的“前瞻式”并行处理。该方法消除了层间串行依赖，并结合首个端到端FPGA加速器设计，利用流式流水线和并行计算，在保持精度的同时显著提升了推理速度，相比GPU实现高达8.5倍的加速，为实时视觉图神经网络推理提供了可行方案。
selection_source: fresh_fetch
motivation: 传统ViG中每层动态构建kNN图存在严重的计算瓶颈和串行依赖，占据了绝大部分推理时间。
method: 提出单层前瞻的解耦策略，使当前层特征更新与下一层图构建并行化，并设计了支持流式流水线的FPGA加速器。
result: 在Alveo U280 FPGA上，该方案相比CPU和GPU分别实现了最高95.7倍和8.5倍的加速，且精度可通过微调完全恢复。
conclusion: GraphLeap通过算法与硬件协同设计，有效解决了Vision GNN的动态计算瓶颈，实现了高效的端到端硬件加速。
---

## 摘要
视觉图神经网络 (ViG) 将图像表示为补丁标记 (patch tokens) 的图，从而实现自适应的、特征驱动的邻域。与具有固定网格偏差的 CNN 或具有全局标记交互的 Vision Transformer 不同，ViG 依赖于动态图卷积：在每一层中，通过对当前补丁特征进行 k-最近邻 (kNN) 搜索来构建依赖于特征的图，随后进行消息传递。这种逐层图构建是主要的瓶颈，在 CPU 和 GPU 上消耗了 50% 到 95% 的图卷积时间，其复杂度随补丁数量 $N$ 呈 $O(N^2)$ 增长，并在图构建与特征更新之间产生了顺序依赖关系。我们提出了 GraphLeap，这是一种简单的重新表述，通过跨层解耦图构建与特征更新来消除这种依赖。GraphLeap 使用基于前一层特征构建的图来执行第 $\ell$ 层的特征更新，同时利用当前层的特征为第 $\ell+1$ 层构建图。这种单层前瞻 (one-layer-lookahead) 的图构建方式实现了图构建与消息传递的并发执行。尽管使用前一层特征可能会导致轻微的精度下降，但仅需几个轮次的轻量级微调就足以恢复原始精度。基于 GraphLeap，我们展示了首个用于视觉图神经网络的端到端 FPGA 加速器。我们的流式、层流水线设计将 kNN 图构建引擎与特征更新引擎重叠，利用了节点级和通道级并行性，并实现了高效的片上数据流，无需显式的边特征实例化。在 Alveo U280 FPGA 上对各向同性和金字塔型 ViG 模型进行的评估表明，GraphLeap 相比 CPU 基准实现了高达 95.7 倍的加速，相比 GPU 基准实现了 8.5 倍的加速，证明了实时视觉图神经网络推理的可行性。

## Abstract
Vision Graph Neural Networks (ViGs) represent an image as a graph of patch tokens, enabling adaptive, feature-driven neighborhoods. Unlike CNNs with fixed grid biases or Vision Transformers with global token interactions, ViGs rely on dynamic graph convolution: at each layer, a feature-dependent graph is built via k-nearest-neighbor (kNN) search on current patch features, followed by message passing. This per-layer graph construction is the main bottleneck, consuming 50--95\% of graph convolution time on CPUs and GPUs, scaling as $O(N^2)$ with the number of patches $N$, and creating a sequential dependency between graph construction and feature updates.   We introduce GraphLeap, a simple reformulation that removes this dependency by decoupling graph construction from feature update across layers. GraphLeap performs the feature update at layer $\ell$ using a graph built from the previous layer's features, while simultaneously using the current layer's features to construct the graph for layer $\ell+1$. This one-layer-lookahead graph construction enables concurrent graph construction and message passing. Although using prior-layer features can introduce minor accuracy degradation, lightweight fine-tuning for a few epochs is sufficient to recover the original accuracy. Building on GraphLeap, we present the first end-to-end FPGA accelerator for Vision GNNs. Our streaming, layer-pipelined design overlaps a kNN graph construction engine with a feature update engine, exploits node- and channel-level parallelism, and enables efficient on-chip dataflow without explicit edge-feature materialization. Evaluated on isotropic and pyramidal ViG models on an Alveo U280 FPGA, GraphLeap achieves up to $95.7\times$ speedup over CPU and $8.5\times$ speedup over GPU baselines, demonstrating the feasibility of real-time Vision GNN inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《GraphLeap: Decoupling Graph Construction and Convolution for Vision GNN Acceleration on FPGA》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉图神经网络（Vision GNNs, ViGs）通过将图像补丁（patches）视为图节点，利用动态构建的 $k$-最近邻（kNN）图来捕捉特征驱动的邻域关系。
*   **核心瓶颈**：ViG 需要在每一层根据当前特征重新构建图。这种逐层动态构建在 CPU/GPU 上占据了 50%–95% 的推理时间，且其 $O(N^2)$ 的复杂度在图构建与特征更新之间产生了严重的**串行依赖**。
*   **研究动机**：现有的加速器多针对静态图或规则网格（如 CNN/ViT），缺乏对动态图像图构建的端到端支持。本文旨在通过算法与硬件的协同设计，消除这一串行瓶颈，实现 ViG 的实时推理。

### 2. 论文提出的方法论
论文提出了 **GraphLeap** 架构，核心包含算法重构与硬件加速两个层面：

*   **核心思想（解耦策略）**：
    *   提出“单层前瞻”（One-layer Lookahead）机制。在第 $\ell$ 层进行特征更新时，使用基于第 $\ell-1$ 层特征构建的图；与此同时，利用第 $\ell$ 层的特征并行构建用于第 $\ell+1$ 层的图。
    *   这种重构打破了“构建图 $\to$ 卷积”的顺序依赖，使得图构建引擎（GCE）和特征更新引擎（FUE）能够流水线化并行工作。
*   **关键技术细节**：
    *   **FPGA 加速器架构**：设计了首个端到端 ViG FPGA 加速器，包含流式 kNN 构建引擎（GCE）和基于脉动阵列的特征更新引擎（FUE）。
    *   **流式数据流**：通过片上缓冲区直接流转邻居索引和特征，避免了显式实例化 $O(NK)$ 规模的边特征，极大地节省了片外内存带宽。
    *   **精度恢复**：由于使用了“过期”一层的图，初始精度会有所下降。论文通过对模型进行 30 个 Epoch 的轻量级微调（Fine-tuning）来补偿精度损失。

### 3. 实验设计
*   **数据集**：ImageNet-1K（包含 1000 个类别，128 万张训练图，5 万张验证图）。
*   **模型变体**：涵盖了各向同性（Isotropic: Ti, S, B）和金字塔型（Pyramidal: Py-Ti, Py-S, Py-M, Py-B）的全系列 ViG 模型。
*   **Benchmark（基准测试）**：
    *   **CPU**：AMD EPYC 7763（64 核，利用多线程软件实现）。
    *   **GPU**：NVIDIA RTX A5000（Ampere 架构，PyTorch 2.0.1 + CUDA 12.2）。
    *   **SOTA FPGA 加速器**：对比了针对 ViT 的 DRViT 和针对混合专家模型（MoE）的 UbiMoE。

### 4. 资源与算力
*   **硬件平台**：AMD-Xilinx Alveo U280 FPGA。
*   **算力参数**：
    *   FPGA 运行频率为 300 MHz。
    *   资源利用率：LUT 67.5%，DSP 78.5%（使用了 7088 个 DSP），URAM 89.6%。
*   **训练/微调算力**：微调过程在 GPU 上完成，使用 AdamW 优化器，余弦学习率调度，共 30 个 Epoch。文中未详细说明微调的具体 GPU 数量和总时长，但强调其为“轻量级”。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 7 种不同规模和结构的 ViG 模型进行了测试，并分别在 224x224 和 448x448 两种输入分辨率下评估了延迟。
*   **充分性评价**：
    *   **多维度评估**：不仅对比了端到端延迟，还详细分析了精度恢复情况、硬件资源消耗以及与 SOTA 视觉加速器的对比。
    *   **客观性**：在 Batch=1 的严苛实时性场景下进行对比，符合边缘侧推理的实际需求。实验覆盖了 ViG 的主要家族成员，证明了方法的普适性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：在 Alveo U280 上，GraphLeap 相比 CPU 实现了最高 **95.7倍** 的加速，相比 GPU 实现了最高 **8.5倍** 的加速。
*   **延迟优势**：对于 ViG-B 模型，端到端延迟仅为 2.77ms，比同类 FPGA 上的 ViT 加速器快约 5 倍。
*   **精度保持**：通过微调，GraphLeap 能够将 Top-1 精度恢复至原始 ViG 的 1.4% 以内（例如 ViG-Py-B 达到 82.34%）。
*   **瓶颈消除**：成功将原本占据 90% 以上时间的图构建过程完全掩盖在特征更新的流水线中。

### 7. 优点
*   **算法-硬件协同**：不是单纯地加速现有算法，而是通过合理的算法重构（解耦）来适配硬件的并行特性。
*   **端到端实现**：提供了从图像输入到分类输出的完整硬件流水线，而非仅加速某个算子。
*   **高效内存管理**：流式处理和片上聚合设计有效解决了图算法中常见的内存访问不规则和带宽受限问题。

### 8. 不足与局限
*   **精度微损**：尽管经过微调，大部分模型仍存在约 0.5%–1.4% 的精度下降，对于精度极度敏感的场景可能受限。
*   **部署成本**：GraphLeap 需要对预训练模型进行额外的微调步骤，这增加了从软件模型到硬件部署的转换成本。
*   **通用性限制**：该设计高度针对 ViG 的 $k$-NN 结构，对于其他类型的动态图（如基于注意力权重的全连接图）可能需要大幅修改硬件架构。

（完）
