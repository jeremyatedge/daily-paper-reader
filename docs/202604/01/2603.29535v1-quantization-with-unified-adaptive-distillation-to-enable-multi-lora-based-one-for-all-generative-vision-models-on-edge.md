---
title: Quantization with Unified Adaptive Distillation to enable multi-LoRA based one-for-all Generative Vision Models on edge
title_zh: 结合统一自适应蒸馏的量化技术，在边缘端实现基于多 LoRA 的通用生成式视觉模型
authors: "Sowmya Vajrala, Aakash Parmar, Prasanna R, Sravanth Kodavanti, Manjunath Arveti, Srinivas Soumitri Miriyala, Ashok Senapati"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29535v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘生成式模型的量化与蒸馏
tldr: 针对移动端生成式视觉模型部署中多LoRA适配器导致的存储和运行冗余问题，本文提出了一种统一框架。该框架将LoRA权重视为运行时输入以实现动态任务切换，并引入了统一自适应蒸馏量化（QUAD）策略，使多个LoRA在共享量化配置下保持高性能。实验表明，该方案在保持视觉质量的同时，显著降低了内存占用并提升了推理速度，为边缘端多任务GenAI应用提供了高效支持。
selection_source: fresh_fetch
motivation: 现有的移动端部署方案为每个LoRA任务编译独立模型，导致严重的存储冗余和计算开销。
method: 提出QUAD量化感知训练策略，将LoRA权重作为动态输入，并利用统一蒸馏技术使多个适配器共享同一量化配置文件。
result: 在移动端NPU上实现了高达6倍的内存缩减和4倍的延迟优化，同时保持了优异的图像生成质量。
conclusion: 该研究通过统一量化与动态权重加载，为在资源受限的边缘设备上高效部署多任务生成式视觉模型提供了可行方案。
---

## 摘要
生成式人工智能（GenAI）功能（如图像编辑、物体移除和提示词引导的图像转换）正越来越多地集成到移动应用中。然而，由于高内存和计算需求，在资源受限的设备上部署用于此类任务的大型视觉模型（LVMs）仍然具有挑战性。虽然低秩适配器（LoRAs）能够实现参数高效的任务适配，但现有的移动部署流水线通常为每个 LoRA 及其基础模型副本编译单独的模型二进制文件，导致存储冗余和运行时开销增加。在这项工作中，我们提出了一个统一框架，利用单个共享模型在边缘设备上实现多任务 GenAI 推理。我们的核心思想是将 LoRA 权重视为运行时输入，而不是将其嵌入到编译后的模型图中，从而允许在运行时进行动态任务切换而无需重新编译。此外，为了支持高效的设备端执行，我们引入了 QUAD（结合统一自适应蒸馏的量化），这是一种量化感知训练策略，可在共享量化配置下对齐多个 LoRA 适配器。我们使用与移动 NPU 兼容的轻量级运行时栈实现了该系统，并在多个芯片组上进行了评估。实验结果表明，在保持多个 GenAI 任务高视觉质量的同时，内存占用和延迟分别降低了高达 6 倍和 4 倍。

## Abstract
Generative Artificial Intelligence (GenAI) features such as image editing, object removal, and prompt-guided image transformation are increasingly integrated into mobile applications. However, deploying Large Vision Models (LVMs) for such tasks on resource-constrained devices remains challenging due to their high memory and compute requirements. While Low-Rank Adapters (LoRAs) enable parameter-efficient task adaptation, existing Mobile deployment pipelines typically compile separate model binaries for each LoRA + a copy of the foundation model, resulting in redundant storage and increased runtime overhead. In this work, we present a unified framework for enabling multi-task GenAI inference on edge devices using a single shared model. Our key idea is to treat LoRA weights as runtime inputs rather than embedding them into the compiled model graph, allowing dynamic task switching at runtime without recompilation. Then, to support efficient on-device execution, we introduce QUAD (Quantization with Unified Adaptive Distillation), a quantizationaware training strategy that aligns multiple LoRA adapters under a shared quantization profile. We implement the proposed system with a lightweight runtime stack compatible with mobile NPUs and evaluate it across multiple chipsets. Experimental results demonstrate up to 6x and 4x reduction in memory footprint and latency improvements, respectively, while maintaining high visual quality across multiple GenAI tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Quantization with Unified Adaptive Distillation to enable multi-LoRA based one-for-all Generative Vision Models on edge》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在移动端部署大型视觉模型（LVMs）时，如何高效地支持多个任务（如物体移除、风格转换等）。
*   **研究动机**：
    *   **存储冗余**：传统的 LoRA 部署方式通常将适配器权重合并到基础模型中并编译为独立二进制文件。若有 $N$ 个任务，则需存储 $N$ 份基础模型副本，导致 ROM 占用巨大。
    *   **量化冲突**：不同任务的 LoRA 权重分布不同，导致各自的量化参数（缩放因子和零点）不一致，无法在同一个静态推理图中动态切换。
    *   **运行开销**：切换任务时需要重新加载整个模型图，造成显著的延迟。

### 2. 方法论
论文提出了 **QUAD（统一自适应蒸馏量化）** 框架，核心步骤包括：
*   **LoRA 作为动态输入**：修改模型架构，将 LoRA 的低秩矩阵 $A$ 和 $B$ 视为运行时的输入张量，而非固定的模型权重。这样只需编译一个通用的基础模型图。
*   **统一量化策略**：
    *   **敏感度分析 (QSS)**：计算各 LoRA 对量化的敏感得分，选择最敏感的 LoRA 作为“锚点”，确定全局共享的量化参数。
    *   **统一量化对齐**：强制所有 LoRA 共享相同的缩放因子（Scale）和零点（Zero-point）。
*   **知识蒸馏微调**：为了弥补共享量化参数带来的精度损失，以全精度（FP32）模型为教师，量化后的模型为学生，通过最小化重建损失来微调 LoRA 权重，使其适应统一的量化配置。
*   **轻量级运行时栈**：设计了一个支持动态绑定 LoRA 缓冲区的推理引擎，兼容移动端 NPU。

### 3. 实验设计
*   **基础模型**：基于 Stable Diffusion 1.5（包括 1.1B 和 0.7B 参数版本）。
*   **应用场景**：提示词引导图像转换、物体移除、文本生成图像、草图转图像、贴纸生成、人像工作室。
*   **硬件平台**：三星 Galaxy S25（搭载 Qualcomm 和 LSI 芯片）、Tab S11（搭载 MediaTek 芯片）。
*   **对比基准 (Benchmark)**：
    *   **精度对比**：服务器端 FP32 模型 vs. 设备端量化模型（指标：FID, LPIPS, PSNR, SSIM, CLIP Score）。
    *   **性能对比**：传统独立编译方案 vs. QUAD 统一框架（指标：延迟、ROM 占用、峰值 RAM）。

### 4. 资源与算力
*   **算力说明**：文中提到在 x86 服务器上运行 FP32 基准测试，并在移动端 NPU 上进行推理评估。
*   **缺失信息**：论文**未明确说明**训练/微调 LoRA 以及执行知识蒸馏时具体的 GPU 型号、数量及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流移动芯片组（高通、联发科、三星 Exynos/LSI），测试了 6 种不同的生成式任务。
*   **消融实验**：针对不同位宽（W8A16、W8A8、混合精度）进行了详尽的对比分析，探讨了精度与性能的平衡。
*   **充分性评价**：实验设计较为充分，跨芯片组的测试证明了方法的通用性，消融实验展示了量化配置的合理性，客观地反映了该方案在实际工业部署中的表现。

### 6. 主要结论与发现
*   **资源大幅节省**：在支持 10 个 LoRA 任务的场景下，内存占用（ROM）可减少 **6 倍**（从 15GB 降至 2.6GB）。
*   **推理延迟优化**：通过避免重新加载模型图，端到端推理延迟缩短了约 **1.5 秒**，整体运行速度提升高达 **4 倍**。
*   **质量保持**：通过 QUAD 蒸馏技术，量化后的模型在 FID 和 SSIM 等指标上与全精度模型保持了高度的一致性，视觉质量损失极小。

### 7. 亮点
*   **芯片组无关性**：方案不依赖特定硬件特性，在不同厂商的 NPU 上均表现良好。
*   **动态扩展性**：支持通过 OTA（云端更新）仅下发轻量级的 LoRA 权重即可增加新功能，无需更新庞大的基础模型二进制文件。
*   **工程实用性**：直接解决了 GenAI 在手机端落地时最棘手的存储和任务切换延迟问题。

### 8. 不足与局限
*   **精度瓶颈**：消融实验显示，当激活值量化到 INT8（W8A8）时，图像质量（FID）会出现剧烈退化，目前仍需依赖 W8A16（16位激活）来维持质量。
*   **训练成本**：虽然推理高效，但每个新增的 LoRA 都需要经过 QUAD 蒸馏流程进行微调，增加了离线准备阶段的工作量。
*   **任务干扰风险**：虽然文中未深入讨论，但当任务差异极大时，强制共享同一套量化参数可能会面临性能衰减的上限。

（完）
