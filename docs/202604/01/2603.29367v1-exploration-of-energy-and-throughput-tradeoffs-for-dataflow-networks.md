---
title: Exploration of Energy and Throughput Tradeoffs for Dataflow Networks
title_zh: 数据流网络中能量与吞吐量权衡的探索
authors: "Abrarul Karim, Joachim Falk, Jürgen Teich"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29367v1"
tags: ["query:aiot"]
score: 9.0
evidence: 硬件数据流网络的能效与吞吐量权衡
tldr: 本研究探讨了数据流网络中动态功耗管理带来的能量节省与吞吐量下降之间的权衡。针对硬件实现的数据流网络，作者提出了线性规划模型以确定最大吞吐量调度，并利用混合整数线性规划在满足吞吐量约束下最小化能耗。此外，提出“Hop and Skip”多目标探索策略以高效寻找帕累托最优解。实验证明，该方法在保证信号处理系统性能的同时，能显著降低能耗并缩短设计空间探索时间。
selection_source: fresh_fetch
motivation: 动态功耗管理策略在节能的同时会因开关延迟导致吞吐量下降，需解决信号处理系统中吞吐量保障与能效之间的矛盾。
method: 提出了基于LP和MILP的调度模型，并设计了名为“Hop and Skip”的多目标设计空间探索策略来寻找帕累托前沿。
result: 实验结果显示，该策略相比暴力搜索显著缩短了探索时间，并能有效识别不同功耗模式下的最优权衡点。
conclusion: 该研究为数据流网络提供了一套完整的能效与性能权衡优化框架，适用于对吞吐量有严格要求的硬件系统设计。
---

## 摘要
在数据流网络中引入动态电源管理策略（如时钟门控和电源门控），已被证明在空闲时间应用时能显著节省能量。然而，由于关断和唤醒延迟，这些策略也可能导致吞吐量下降。这种吞吐量的降低对于需要保证吞吐量的信号处理系统可能尤为不利。作为解决方案，本文首先提出了一种线性规划公式，用于寻找给定的所谓“自供电数据流网络”的周期性最大吞吐量调度，其中以硬件实现的算子在未被使能触发时允许进入睡眠状态。根据允许哪些算子关断电源，可以获得吞吐量与节能之间的权衡。作为第二个贡献，我们提出了一种混合整数线性规划公式，通过识别一组允许在空闲阶段关断电源的算子以及不建议关断的算子，来确定满足给定吞吐量并使每个周期的总能量最小化的周期性调度。最后，作为第三个贡献，我们提出了一种名为“Hop and Skip”的多目标设计空间探索策略，以高效地探索能量和吞吐量解决方案的帕累托前沿。对一组现有基准测试和随机生成图的实验评估表明，与暴力搜索相比，该策略显著减少了探索时间。最后，本文详细阐述了一个真实案例研究，并报告了相关数据流网络在以下情况下的可实现节能和吞吐量：(a) 所有算子始终处于活动状态，(b) 所有算子均为自供电，以及 (c) 由所提出的设计空间探索策略找到的所有最优能量与吞吐量权衡点。

## Abstract
The introduction of dynamic power management strategies such as clock gating and power gating in dataflow networks has been shown to provide significant energy savings when applied during idle times. However, these strategies can also degrade throughput due to shutdown and wake-up delays. Such throughput degradations might be particularly detrimental to signal processing systems that require a guaranteed throughput. As a solution, this paper first contributes a linear-program formulation for finding a periodic maximal-throughput schedule of a given so-called self-powering dataflow network where actors, realized in hardware, are allowed to go to sleep whenever not being enabled to fire. Depending on which actors are allowed to power down, tradeoffs between throughput and energy savings can be obtained. As a second contribution, we propose a mixed-integer-linear-program formulation to determine a periodic schedule that satisfies a given throughput while minimizing the overall energy per period by identifying a respective set of actors that is allowed to power down in phases of idleness and which rather not. Finally, as a third contribution, we propose a multi-objective design-space exploration strategy called "Hop and Skip" to efficiently explore the Pareto front of energy and throughput solutions. Experimental evaluations on a set of existing benchmarks and randomly generated graphs witness significant exploration time reductions over a brute-force sweep. Finally, a real-world case study is elaborated, and we report on achievable energy savings and throughputs of the related dataflow network where (a) all actors are always-active, (b) all actors are self-powered, and (c) all optimal energy and throughput tradeoff points as found by the proposed design-space exploration strategy.

---

## 论文详细总结（自动生成）

### 论文总结：数据流网络中能量与吞吐量权衡的探索

#### 1. 核心问题与整体含义
*   **研究动机**：现代物联网（IoT）设备对能效有极高要求，通常使用数据流图（DFG）建模信号处理应用。虽然动态功耗管理（DPM，如时钟门控和电源门控）能显著降低硬件算子在空闲时的功耗，但其引入的**唤醒延迟（Wake-up delay）**和**关断延迟（Shutdown delay）**会直接导致系统吞吐量下降。
*   **核心问题**：如何在保证信号处理系统实时吞吐量约束的前提下，通过优化算子的开关状态切换，找到能量消耗与吞吐量之间的最优权衡（Tradeoff）。

#### 2. 论文提出的方法论
论文构建了一个完整的数学框架和探索策略：
*   **最大吞吐量调度（LP 模型）**：提出线性规划（LP）公式，在已知每个算子是“始终激活（AA）”还是“自供电（SP）”模式的情况下，计算系统能达到的最小周期 $P$（即最大吞吐量）。
*   **最小能量调度（MILP 模型）**：提出混合整数线性规划（MILP）公式。给定一个目标周期 $P$，通过二值决策变量 $x_i$ 自动决定哪些算子应进入 SP 模式以节省能量，哪些应保持 AA 模式以避免延迟，从而在满足吞吐量的前提下最小化总能耗。
*   **“Hop and Skip”（H&S）探索策略**：
    *   **Hop（跳跃）**：利用 MILP 在当前周期下寻找能量最优的算子配置。
    *   **Skip（跳过）**：利用 LP 计算该配置下能达到的物理极限最小周期，从而直接跳过中间无需重复计算的周期点。
    *   该策略旨在高效提取**帕累托前沿（Pareto Front）**，避免了暴力搜索的指数级计算开销。

#### 3. 实验设计
*   **真实案例研究**：声学回声消除（AEC）网络，基于 28nm 工艺的门级仿真获取真实的功率、执行时间和延迟参数。
*   **基准测试（Benchmarks）**：采用 SDF3 工具包中的 7 个真实应用模型，包括 H.263 编码器、Modem、MP3 解码器（块并行与粒度并行）、采样率转换器等。
*   **随机实验**：使用随机图生成器产生 100 个复杂的同步数据流图（SDF），以验证算法的普适性。
*   **对比方法**：
    1.  **x Sweep**：对所有算子配置进行穷举（$2^{|A|}$ 种组合），用于获取绝对真实的帕累托前沿。
    2.  **P Sweep**：对所有可能的整数周期进行逐一扫描。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core i7-14700K** 处理器的机器上运行。
*   **软件工具**：使用 QuestaSim 进行门级仿真，Catapult C 进行高层综合，Synopsys Design Compiler 和 PrimePower 进行 RTL 综合与功耗分析。
*   **算力说明**：该研究属于组合优化与调度问题，不涉及 GPU 大规模训练。计算压力主要集中在 MILP 求解器的运行时间上。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个典型工业级拓扑结构和 100 组随机生成的复杂图。
*   **充分性评价**：实验设计非常充分。作者不仅对比了运行时间（Speedup），还引入了**超体积比（Hypervolume Ratio）**指标来客观衡量 H&S 算法找到的解集与真实帕累托前沿的重合度。结果显示，在 98% 以上的随机案例中，H&S 都能精准找到最优解。

#### 6. 论文的主要结论与发现
*   **节能潜力**：在 AEC 案例中，通过混合模式（Hybrid），在不损失任何吞吐量的情况下可节能 **20%**；若允许 17% 的吞吐量下降，节能可达 **55%**。
*   **算法效率**：提出的 H&S 策略在大型网络中比周期扫描（P Sweep）快达 **2300 倍**，比配置穷举（x Sweep）快几个数量级。
*   **模式权衡**：并非所有算子都适合进入自供电模式，处于“关键路径”上的算子保持始终激活状态对维持系统性能至关重要。

#### 7. 优点
*   **数学严谨性**：将复杂的硬件开关延迟问题转化为成熟的 LP/MILP 数学模型，保证了结果的最优性。
*   **探索效率高**：H&S 策略巧妙利用了 LP 和 MILP 的对偶特性（一个求周期极限，一个求能量极限），极大地缩短了设计空间探索周期。
*   **实用性强**：直接对接工业级 HLS 和 RTL 工具链，参数来源于真实硅片工艺模型。

#### 8. 不足与局限
*   **模型简化**：功耗模型主要考虑了静态和动态功耗的线性组合，未深入探讨电压频率调节（DVS）带来的非线性功耗变化。
*   **转换开销**：将同步数据流图（SDF）转换为标记图（Marked Graph）进行处理时，如果重复因子很大，会导致节点数激增，从而增加 MILP 求解的复杂度。
*   **应用限制**：目前主要针对周期性调度的静态数据流，对于具有高度不确定性的动态数据流（Dynamic Dataflow）支持有限。

（完）
