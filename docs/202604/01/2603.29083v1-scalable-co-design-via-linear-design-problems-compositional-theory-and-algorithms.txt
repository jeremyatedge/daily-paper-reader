Title: Scalable Co-Design via Linear Design Problems: Compositional Theory and Algorithms

URL Source: https://arxiv.org/pdf/2603.29083v1

Published Time: Wed, 01 Apr 2026 00:22:27 GMT

Number of Pages: 17

Markdown Content:
# Scalable Co-Design via Linear Design Problems: Compositional Theory and Algorithms 

Yubo Cai, Yujun Huang, Meshal Alharbi, Gioele Zardini 

Abstract — Designing complex engineered systems requires managing tightly coupled trade-offs between subsystem capabil-ities and resource requirements. Monotone co-design provides a compositional language for such problems, but its generality does not by itself reveal which problem classes admit exact and scalable computation. This paper isolates such a class by introducing linear design problems (LDPs): design problems whose feasible functionality–resource relations are polyhedra over Euclidean posets. We show that queries on LDPs reduce exactly to Multi-Objective Linear Programs (MOLPs), thereby connecting monotone co-design semantics with polyhedral mul-tiobjective optimization. We further prove that LDPs are closed under the fundamental co-design interconnections, implying that any interconnection of linear components induces a system-level LDP. To compute the resulting feasible sets, we develop two complementary constructions: a monolithic lifted formulation that preserves block-angular sparsity, and a compositional formulation that incrementally eliminates internal variables through polyhedral projection. Beyond the exact linear setting, we show that convex co-design resource queries admit arbitrar-ily accurate polyhedral outer approximations, with recession-cone error identically zero for standard nonnegative resource cones. Numerical studies on synthetic series-chain benchmarks, a gripper, and a rover co-design validate the theory. 

I. I NTRODUCTION 

Designing complex engineered systems requires navigat-ing tightly coupled trade-offs across heterogeneous subsys-tems. In autonomy, robotics, and cyber-physical systems, choices in sensing, computation, actuation, energy, and phys-ical structure jointly determine achievable performance, cost, and safety margins. As a result, optimizing each subsystem in isolation can yield architectures that are globally infeasible or far from Pareto optimal. This motivates co-design : the joint design of interacting subsystems under coupled functionality and resource constraints [1]–[3]. A principled foundation for this viewpoint is monotone co-design , where each component is modeled as a design prob-lem (DP) : a monotone relation between partially ordered sets (posets) of functionalities and resources [1], [4], [5]. Its key advantage is the separation of structure from semantics : the architecture is encoded by an interconnection graph, while each component contributes only its local feasibility relation. Series, parallel, intersection, and feedback then induce a           

> The authors are with the Laboratory for Information and Decision Systems, Massachusetts Institute of Technology, 02139 Cambridge (MA), USA, {yubocai,yujun233,meshal,gzardini }@mit.edu
> This material is based upon work supported by the Defense Advanced Research Projects Agency (DARPA) under Award No. D25AC00373. The views and conclusions contained in this document are those of the authors and should not be interpreted as representing the official policies, either expressed or implied, of the U.S. Government.

system-level DP compositionally from the component DPs, enabling modular design and reuse across domains [6]–[10]. The generality of monotone co-design is both its strength and its limitation. It provides a domain-agnostic semantics for compositional design, but does not by itself identify which structured subclasses admit exact and scalable com-putation. In many engineering applications, functionality– resource trade-offs are naturally linear or polyhedral, yet current co-design frameworks do not isolate this case as a tractable subclass with its own closure theory, query semantics, and algorithms. This paper addresses that gap by introducing linear design problems (LDPs) , whose feasible functionality–resource sets are polyhedra over Euclidean posets. Within monotone co-design, LDPs play a role anal-ogous to linear programming in mathematical optimiza-tion: they isolate a tractable structural core, admit exact algorithmic treatment, and provide a natural backbone for approximating broader convex models. 

A. Related Work 

This work sits at the intersection of three lines of research. First, system-level design and multidisciplinary design opti-mization emphasize that engineering decisions must be co-ordinated across interacting disciplines and components [2], [3], [11]. Second, monotone co-design provides a composi-tional and order-theoretic language for such problems, with exact semantics based on monotone relations and fixed-point computation [4], [5], [12]. Third, our computational viewpoint connects co-design to multi-objective and vec-tor linear optimization, where efficient-frontier computation, polyhedral projection, and Benson-type algorithms are well developed [13]–[17]. Existing co-design tools are intentionally general. For instance, MCDPL can represent broad classes of monotone relations and handle continuous specifications through dis-cretization and consistent relaxations [1], [5]. However, it does not exploit linear or polyhedral structure explicitly; thus even simple linear trade-offs may be treated through generic antichain propagation or discretization rather than exact polyhedral algorithms. What is missing is a structural linear theory within the co-design formalism itself. 

B. Statement of Contribution 

The contributions of the paper are as follows. First, we define LDPs and show that their natural queries reduce to multi-objective linear programmings (MOLPs)/vector linear programmings (VLPs), establishing a direct bridge between 

> arXiv:2603.29083v1 [math.OC] 30 Mar 2026

monotone co-design and polyhedral multi-objective opti-mization. Second, we prove that LDPs are closed under the main co-design interconnections; hence any linear co-design problem (LCDP) assembled from linear components induces a system-level LDP. Third, we develop two complementary feasible-set construction methods for LCDPs: (i) a mono-lithic lifted formulation that preserves block-angular sparsity, and (ii) a compositional formulation that eliminates internal variables through graph-guided projection. Fourth, we extend the framework beyond the exact linear case by showing that convex co-design resource queries admit arbitrarily accurate polyhedral outer approximations; for the standard resource cone Rn

> ≥0

, the recession-cone error is structurally exact (δ = 0 ), so accuracy is governed solely by the bounded-part error ε. Finally, we validate the theory on three benchmarks: a synthetic series-chain family that isolates the trade-off between sparsity-preserving and elimination-based construc-tions, a rigid gripper LDP illustrating the exact polyhedral regime, and a nonlinear rover case study in which tangent-plane outer approximations yield accurate (ε, 0) surrogates and substantial runtime gains over MCDPL. II. L INEAR CO-D ESIGN THEORY 

We formalize LDPs, i.e., DPs whose feasible sets are polyhedra over Euclidean posets. We first recall the order-theoretic and convex-geometric notation used throughout, then introduce LDPs and their query semantics. 

A. Mathematical preliminaries 1) Background on orders: 

Definition 1 (Poset) . A poset is a tuple P = ⟨P, ⪯P ⟩, where 

P is a set and ⪯P is a partial order (a reflexive, transitive, and antisymmetric relation). If clear from context, we use P

for a poset, and ⪯ for its order. 

Definition 2 (Opposite poset) . The opposite of a poset P =

⟨P, ⪯P ⟩ is the poset Pop def 

= ⟨P, ⪯op  

> P

⟩ with the same elements and reversed ordering: xP ⪯op  

> P

yP ⇔ yP ⪯P xP .

Definition 3 (Product poset) . Given posets ⟨P, ⪯P ⟩ and 

⟨Q, ⪯Q⟩, their product ⟨P × Q, ⪯P×Q ⟩ is the poset with 

⟨xP , x Q⟩ ⪯ P×Q ⟨yP , y Q⟩ ⇔ (xP ⪯P yP ) ∧ (xQ ⪯Q yQ).

Definition 4 (Upper closure) . Let P be a poset. The upper closure of a subset XP ⊆ P contains all elements of P that are greater or equal to some yP ∈ XP :

↑ XP def 

= {xP ∈ P | ∃ yP ∈ XP : yP ⪯P xP }.

Definition 5 (Upper set) . A subset XP ⊆ P of a poset is called an upper set if it is upwards closed: ↑ XP = XP .We write U(P) for the set of upper sets of P. We regard 

U(P) as a poset with U ⪯ U ′ ⇔ U ⊇ U ′. Thus larger upper sets are considered smaller in this order (encoding weaker constraints). 

2) Convex geometry: Throughout, ∥ · ∥ denotes the Eu-clidean norm. 

Definition 6 (Set operations) . For a set C ⊆ Rn, define the 

convex hull , conical hull , and closure by 

conv( C) def 

=

nPki=1 λici | k ∈ N, c i ∈ C, λ i ≥ 0, P 

> i

λi = 1 

o

,

cone( C) def 

=

nPki=1 λici | k ∈ N, c i ∈ C, λ i ≥ 0

o

,

cl( C) def 

= x ∈ Rn | ∃ (xk)k≥1 ⊆ C, x k → x .

These are the smallest convex set, convex cone, and closed set containing C, respectively. A point v ∈ P is a vertex 

(extreme point) of a set P if it cannot be expressed as v = 

> 12

(x + y) for distinct x, y ∈ P . We write vert( P ) for the set of vertices of a polyhedron P .

Definition 7 (Basic convex sets) . The following sets are basic examples of convex sets in Rn:(1) Linear subspace: L def 

= {x ∈ Rn | Ax = 0 } for some matrix A ∈ Rm×n.(2) Affine subspace: M def 

= L + x0, where L is a linear subspace and x0 ∈ Rn. Equivalently, M = {x ∈ Rn |

Ax = b} with b = Ax 0.(3) Hyperplane: H def 

= {x ∈ Rn | a⊤x = b}, where a ∈ Rn

and b ∈ R. It is an (n − 1) -dimensional affine subspace. (4) Halfspace: H+ def 

= {x ∈ Rn | a⊤x ≥ b} and H− def 

= {x ∈

Rn | a⊤x ≤ b}, which are (by default) closed halfspaces determined by the hyperplane H.

Definition 8 (Polyhedron) . A polyhedron P is the intersec-tion of a finite number of halfspaces. That is: 

P =

> m

\

> i=1

H+

> i
> def

=

> m

\

> i=1

x ∈ Rn : a⊤ 

> i

x ≥ bi = {x ∈ Rn : Ax ≥ b} ,

where a⊤ 

> i

form the rows of A and bi are the components of b. This is called the H-representation of a polyhedron. A polyhedron can also be written in V-representation as 

P = conv {v1, . . . , v p} + cone {r1, . . . , r q },

where {v1, . . . , v p} are vertices and {r1, . . . , r q } are extreme rays. A bounded polyhedron ( q = 0 ) is called a polytope .

Remark (H/V representations and conversion) . For polyhedra in finite-dimensional spaces, H- and V-representations are equivalent (Minkowski–Weyl theorem). Converting H → V

amounts to vertex/ray enumeration, while converting V →

H amounts to facet enumeration; both can be exponential in the worst case. In this paper, feasible sets are constructed primarily in H-representation from linear constraints, and 

V-information is recovered through polyhedral projection or MOLP/VLP solvers when needed [15], [16], [18]. 

B. Background on co-design and queries 

Co-design provides a compositional perspective for ana-lyzing complex systems by making explicit the trade-offs between what a subsystem can provide and what it must consume. As a motivating example, consider the design of unmanned aerial vehicles (UAVs) (see [12, Fig 1(a)]), in which perception is a critical sub-module. Holding the remaining system conditions fixed, the perception unit can be viewed as delivering a target level of detection accu-racy at the cost of computation power , under prescribed 

weather conditions . Although accuracy and computation can be naturally represented by ordered real vectors, weather conditions are often only partially ordered (and may contain incomparable scenarios). For example, a clear night and a foggy day induce qualitatively different sensing regimes, so a design tailored to one condition may not transfer to the other. Monotone co-design formalizes these heterogeneous trade-offs by representing components as DPs defined over posets of functionalities and resources. 

Definition 9 (DP) . Given posets F and R of functionalities 

and resources , a DP is an upper set of F op × R. We denote the set of such DPs by DP {F , R }. Given a DP dp , a pair ⟨xF , x R⟩ of functionality xF and resource xR is feasible 

if ⟨xF , x R⟩ ∈ dp . We order DP {F , R } by inclusion: dp a ⪯

dp b ⇔ dp a ⊆ dp b. Note that this is the opposite of the ordering used for upper sets, i.e., larger feasible sets correspond to “better” DPs. The upper set condition captures the following intuition: if a resource xR suffices to provide functionality xF , then it also suffices for any worse functionality x′ 

> F

⪯ xF . Moreover, any better resource x′ 

> R

⪰ xR should also suffice to provide 

xF .A core tenet of co-design is to compose systems out of simpler sub-systems. Such composites are formalized as multi-graphs of DPs called co-design problems (CDPs) . We summarize the main composition operations in Definition 10, with some shown diagrammatically in Fig. 1. 

Definition 10 (Composition operations for DPs) . The fol-lowing operations construct new DPs from old: 

Series : Given DPs dp a ∈ DP {P , Q} and dp b ∈

DP {Q , R} , their series connection dp a # dp b ∈ DP {P , R} 

is defined as 

{⟨ xP , x R⟩ | ∃ xQ : ⟨xP , x Q⟩ ∈ dp a and ⟨xQ, x R⟩ ∈ dp b}.

This models situations where dp a uses the functionalities provided by dp b as its resources. 

Parallel : For dp a ∈ DP {P , Q} , dp ′ 

> a

∈ DP {P ′, Q′}, their parallel connection dp a ⊗ dp ′ 

> a

∈ DP {P × P ′, Q × Q ′} is 

{ ⟨xP , x ′P ⟩ , xQ, x ′Q | ⟨ xP , x Q⟩ ∈ dp a, x′P , x ′Q ∈ dp ′

> a

},

representing two non-interacting systems. 

Intersection : For dp a, dp b ∈ LDP {P , Q} , their intersec-tion dp a ∧ dp b ∈ DP {P , Q} is 

{⟨ xP , x Q⟩ | ⟨ xP , x Q⟩ ∈ dp a, ⟨xP , x Q⟩ ∈ dp b}.

representing a conjunction of constraints. 

Feedback/Trace : For dp ∈ DP {P × R , Q × R} , its trace Tr (dp) ∈ DP {P , Q} is defined as 

{⟨ xP , x Q⟩ | ∃ xR : ⟨⟨ xP , x R⟩ , ⟨xQ, x R⟩⟩ ∈ dp }.

This models the case where functionalities provided by dp 

are used as its own resources. These compositions allow the construction of a DP from CDPs, such as Figures 3 and 5 below. 

Definition 11 (CDPs) . We call ⟨F , R, ⟨V , E⟩⟩ a CDP, where 

F = ΠnF

> i=1

F i and R = ΠnR

> j=1

Rj are the system-level functionalities and resources, and ⟨V , E⟩ is a multi-graph where: (i) Each node in V is a DP d with functionality F d =Πnd,F  

> i=1

F d,i and resource Rd = Π nd,R  

> j=1

Rd,j .(ii) Each edge is a pair ⟨⟨ d1, i 1⟩ , ⟨d2, j 2⟩⟩ with the cor-responding posets Rd1,i 1 and F d2,j 2 being the same, representing the inequality xRd1,i 1 ⪯ xF d 2,j 2 (the i1-th resource required by d1 ∈ V is at most the j2-th functionality provided by d2 ∈ V ). (iii) The system-level functionality/resource is the product of the unconnected functionalities/resources. With these modeling primitives in hand, we can ask for optimal solutions to DPs, formalized as queries .

Definition 12 (Querying DPs) . Given a DP dp ∈ DP {F , R },we define two basic queries: 1) Fix functionalities minimize resources : for a fixed xF ∈

F , return the upper set of resources xR ∈ R that make 

⟨xF , x R⟩ feasible with respect to dp . We can view this query as a monotone map q : F → U(R).2) Fix resources maximize functionalities : For a fixed xR ∈

R, return the upper set of functionalities xF ∈ F that make ⟨xF , x R⟩ feasible with respect to dp . We can view this query as a monotone map q′ : R → U(F op ).In applications we are typically interested in the minimal 

resources in q(xF ) and the maximal functionalities in q′(xR).When F and R are general posets, there might be a Pareto front of optimal values and solutions to the queries, instead of a unique one. Using the compositional structure, one can derive efficient algorithms to calculate the query results for complex DPs defined by a multi-graph of sub-systems [1]. 

C. Linear co-design problems and queries 

We next focus on DPs endowed with additional geometric structure. Based on Definition 9, we introduce LDPs. 

Definition 13 (Euclidean poset and sub-poset) . For n ∈ N,the Euclidean poset Rn

> ≥0

, ⪯ is the set Rn

> ≥0
> def

= {x ∈ Rn |

xi ≥ 0, i = 1 , . . . , n } equipped with the componentwise order x ⪯ y ⇔ xi ≤ yi for all i. A subset S ⊆ Rn 

> ≥0

with the restricted order is called a Euclidean sub-poset .

Definition 14 (LDP) . Let F ⊆ ⟨ RnF 

> ≥0

, ⪯⟩ , R ⊆ ⟨ RnR 

> ≥0

, ⪯⟩ 

be Euclidean sub-posets. A LDP is a DP dp ∈ DP {F , R }

for which the set of all feasible pairs ⟨xF , x R⟩ forms a polyhedron in the underlying Euclidean space. Equivalently, there exist AF ∈ Rm×nF , AR ∈ Rm×nR , b ∈ Rm such that 

ldp = 

n

⟨xF , x R⟩ ∈ F op × R AF xF + ARxR ≥ b

o

. (1) DP     

> resources functionalities
> (a) A DP.
> de⪯
> (b) Series ( #).
> d
> e
> PQ
> RS
> (c) Parallel ( ⊗).
> d
> e
> PQ
> ∧
> (d) Intersection ( ∧).
> d
> ⪰
> (e) Loop ( Tr ).
> Fig. 1: A DP is a monotone relation between posets of functionalities and resources (a), and can be composed via various operations (b)–(e).

We denote the set of all such LDPs by LDP {F , R }. For conciseness, we can write the condition as Aldp xldp ≥ b,where Aldp = [ AF | AR] and xldp = x⊤ 

> F

, x ⊤

> R

⊤.

Remark. Since F and F op share the same underlying set, Eq. (1) is written over F × R and the order reversal appears only in the requirement that dp be an upper set in 

F op × R. Not every polyhedron in F × R therefore defines an LDP: we additionally require the monotonicity encoded by Definition 9. Following the query formalism in Definition 12 (see [1]), the fix functionalities minimize resources query asks, for a fixed xF ∈ F , for the set of minimal (Pareto) resources xR

such that ⟨xF , x R⟩ is feasible; the fix resources maximize functionalities query asks, for a fixed xR ∈ R, for the set of maximal (Pareto) functionalities xF such that ⟨xF , x R⟩ is feasible. For LDPs, these become MOLPs. 

Definition 15 (MOLP) . A MOLP in standard form is the optimization problem 

Min ⪯ Cx subject to Ax ≥ b, x ≥ 0,

where C ∈ Rq×n, A ∈ Rm×n, b ∈ Rm, the variable x ∈ Rn,and ⪯ is the componentwise partial order on Rq . We call the set of feasible points X := {x ≥ 0 : Ax ≥ b} the feasible set of the MOLP. 

Definition 16 (Efficient point) . A feasible point x∗ ∈ X

is called efficient (or Pareto optimal ) for the MOLP in Definition 15 if there is no feasible x ∈ X with Cx ⪯ Cx ∗

and Cx ̸ = Cx ∗. Equivalently, Cx ∗ /∈ CX + Rq 

> ≥0

\ { 0},where CX := {Cx | x ∈ X}. The set of all efficient points is denoted XE , and its image CX E is the efficient frontier 

(or Pareto front ). 

Definition 17 (Upper image of a MOLP) . For the MOLP in Definition 15, the upper image is P def 

= cl  CX + Rq

> ≥0

.In the polyhedral linear case here, P is a polyhedron whose lower boundary coincides with the efficient frontier CX E .

Remark (Vector linear program) . A vector linear program 

(VLP) generalizes MOLP by replacing the componentwise ordering cone Rq 

> ≥0

with an arbitrary polyhedral cone K ⊆

Rq [16]: 

Min K Cx subject to Ax ≥ b, x ≥ 0,

where y ⪯K z ⇔ z − y ∈ K. When K = Rq

> ≥0

, the VLP reduces to the standard MOLP. The Bensolve tools and solver [16], [19] computes V- and H-representations of the upper image P for VLPs. 

Lemma 1 (Queries of LDP are equivalent to MOLP) .

Consider an LDP dp ∈ LDP {F , R } with feasible set ldp = 

{⟨ xF , x R⟩ ∈ F × R : AF xF + ARxR ≥ b}.(i) Fix functionalities minimize resources : For fixed ¯xF ∈

F , the set of minimal resources that make ⟨¯xF , x R⟩

feasible coincides with the set of efficient solutions of the MOLP 

Min ⪯ xR s.t. ARxR ≥ b − AF ¯xF , xR ≥ 0.

(ii) Fix resources maximize functionalities : For fixed ¯xR ∈

R, the set of maximal functionalities that make 

⟨xF , ¯xR⟩ feasible coincides with the set of efficient solutions of the MOLP 

Max ⪯ xF s.t. AF xF ≥ b − AR ¯xR, xF ≥ 0,

which is equivalent to Min ⪯ (−xF ) subject to the same constraints (efficient points are unchanged). (iii) Conversely, every MOLP of the form Min ⪯ x subject to Ax ≥ b, x ≥ 0 is the fix-functionalities -minimize-

resources query of the LDP with functionality space 

F = {0} ⊆ R≥0 (singleton), resource space R = Rn

> ≥0

,and feasible set ldp = {⟨ 0, x ⟩ : Ax ≥ b, x ≥ 0} (i.e., 

AF = 0 , AR = A). 

Proof. We prove the three statements in order. (i) Fix 

¯xF ∈ F and consider the associated feasible resource set 

R(¯ xF ) def 

= {xR ∈ R | ⟨ ¯xF , x R⟩ ∈ ldp }. By the polyhedral representation of ldp , a pair ⟨¯xF , x R⟩ is feasible if and only if 

ARxR ≥ b − AF ¯xF , xR ∈ R ⊆ RnR 

> ≥0

.

Hence R(¯ xF ) coincides with the feasible set of the vector optimization problem 

Min ⪯ xR s.t. ARxR ≥ b − AF ¯xF , xR ≥ 0,

where ⪯ is the componentwise order on RnR . By Def-inition 12, the query answer at ¯xF is precisely the set of ⪯–minimal elements of R(¯ xF ). These are exactly the efficient (Pareto–optimal) solutions of the above MOLP with objective matrix C = I, establishing (i). 

(ii) The statement for the “fix resources, maximize func-tionalities” query is proved by an order–duality argument entirely analogous to (i). For fixed ¯xR the feasible function-ality set is the polyhedron F (¯ xR) = {xF ∈ F | AF xF ≥

b−AR ¯xR, x F ≥ 0}. Taking ⪯–maximal elements of F (¯ xR)

is equivalent to taking ⪯–minimal elements of −F (¯ xR), so exactly as in (i) one obtains that the query result coincides with the efficient set of the MOLP in (ii). 

(iii) Let a MOLP of the form 

Min ⪯ x subject to Ax ≥ b, x ≥ 0be given, where x ∈ Rn 

> ≥0

and ⪯ is the componentwise order. Define an LDP with functionality space F def 

= {0} ⊆ R≥0,resource space R def 

= Rn

> ≥0

, and feasible set 

ldp def 

= {⟨ 0, x ⟩ ∈ F × R | Ax ≥ b}.

This is an LDP, since ldp is a polyhedron in R1 × Rn and admits the representation 

ldp = {⟨ xF , x R⟩ | AF xF + ARxR ≥ b},

with AF = 0 and AR = A. The fix-functionalities -minimize-

resources query evaluated at the unique functionality ¯xF = 0 

asks for the set of ⪯–minimal xR such that ⟨0, x R⟩ ∈

ldp , that is, {xR ∈ Rn 

> ≥0

| Ax R ≥ b} together with its 

⪯–minimal elements. By definition, these minimal elements are exactly the efficient solutions of the original MOLP, which proves (iii). We are now ready to define LCDPs. 

Definition 18 (LCDP) . Let ⟨F , R, ⟨V , E⟩⟩ be a CDP in the sense of Definition 11. We call ⟨F , R, ⟨V , E⟩⟩ a linear co-design problem (LCDP) if the following conditions hold: (i) ( Linear component design problems ) For each node 

d ∈ V , the associated DP d = ⟨F d, R d, ldp d⟩ has func-tionality and resource posets F d ⊆ ⟨ R nF d 

> ≥0

, ⪯⟩ , Rd ⊆⟨R nRd 

> ≥0

, ⪯⟩ , and its feasible set ldp d ⊆ F d × Rd is a polyhedron (there exist AF d ∈ Rmd×nF d , ARd ∈

Rmd×nRd , bd ∈ Rmd satisfying Eq. (1)). (ii) ( Linear interconnection ) Each edge e ∈ E is a pair 

e = ⟨⟨ d1, i 1⟩ , ⟨d2, j 2⟩⟩ that links the i1-th resource component of node d1 ∈ V to the j2-th functionality component of node d2 ∈ V , with the two components identified by inequality of the corresponding coordi-nate projections, i.e. πi1 (Rd1 ) ≤ πj2 (F d2 ).In other words, an LCDP is a CDP whose component design problems are all LDPs and interconnections are element-wise inequalities. Consequently, the system-level function-ality/resource are Euclidean posets. The following lemma shows LDPs are closed under the compositions in Definition 10. 

Lemma 2 (Closure of LDPs under composition) . Let 

P, Q, R, P′, Q′ be Euclidean sub-posets (Definition 13). Then the following hold: (a) Series. For any dp a ∈ LDP {P , Q} and dp b ∈

LDP {Q , R} , their series composition dp a # dp b satisfies 

dp a # dp b ∈ LDP {P , R} .(b) Parallel. For any dp a ∈ LDP {P , Q} and dp ′ 

> a

∈

LDP {P ′, Q′}, their parallel composition dp a ⊗ dp ′

> a

satisfies dp a ⊗ dp ′ 

> a

∈ LDP {P × P ′, Q × Q ′}.(c) Intersection. For any dp a, dp b ∈ LDP {P , Q} , their intersection dp a ∧dp b satisfies dp a ∧dp b ∈ LDP {P , Q} .(d) Feedback. For any dp ∈ LDP {P × R , Q × R} , its trace Tr (dp) satisfies Tr (dp) ∈ LDP {P , Q} .

Proof. By Definition 14, for Euclidean sub-posets F , R aDP dp ∈ DP {F , R } is an LDP if and only if its feasible set is a polyhedron in the underlying Euclidean space. For each composition we give the feasible set of the composite DP, show that it is a polyhedron, and conclude that the composite is an LDP. Throughout, we use that polyhedra are closed under Cartesian products and linear images. 

Series. Let dp a ∈ LDP {P , Q} and dp b ∈ LDP {Q , R} be LDPs with feasible sets 

ldp a = {⟨ xP , x Q⟩ | AP xP + AQxQ ≥ ba},

ldp b = {⟨ xQ, x R⟩ | BQxQ + BRxR ≥ bb},

for suitable matrices ⟨AP , A Q, b a⟩ and ⟨BQ, B R, b b⟩. By Definition 10, the series connection dp a # dp b ∈ DP {P , R} 

has feasible set 

ldp series = {⟨ xP , x R⟩ | ∃ xQ : AP xP + AQxQ ≥ ba,BQxQ + BRxR ≥ bb}. (2) In the extended variable space x def 

= [x⊤P , x ⊤Q, x ⊤R]⊤, the constraints in (2) can be written as 

AP AQ 00 BQ BR



x ≥

"ba

bb

#

,

which defines a polyhedron in ⟨xP , x Q, x R⟩. The feasible set ldp series is its projection onto the ⟨xP , x R⟩ coordinates, hence is again a polyhedron. Since the external functionality and resource posets are still P and R, which are Euclidean sub-posets by assumption, dp a # dp b is an LDP. 

Parallel. Let dp a ∈ LDP {P , Q} and dp ′ 

> a

∈ LDP {P ′, Q′}

be LDPs with feasible sets 

ldp a = {⟨ xP , x Q⟩ | AP xP + AQxQ ≥ ba},

ldp ′ 

> a

= {⟨ xP′ , x Q′ ⟩ | AP′ xP′ + AQ′ xQ′ ≥ b′

> a

}.

By Definition 10, their parallel connection dp a ⊗ dp ′ 

> a

∈

DP {P × P ′, Q × Q ′} has feasible set 

ldp par =

n

⟨⟨ xP , x P′ ⟩ , ⟨xQ, x Q′ ⟩⟩ 

⟨xP , x Q⟩ ∈ ldp a, ⟨xP′ , x Q′ ⟩ ∈ ldp ′

> a

o

.

(3) The right-hand side is the Cartesian product ldp a × ldp ′

> a

of two polyhedra in disjoint variables, hence a polyhedron. Moreover, P × P ′ and Q × Q ′ are Euclidean sub-posets (products of subsets of Euclidean spaces with component-wise order), so dp a ⊗ dp ′ 

> a

is an LDP. 

Intersection. Let dp a, dp b ∈ LDP {P , Q} be LDPs with feasible sets 

ldp a = {⟨ xP , x Q⟩ | AP xP + AQxQ ≥ ba},

ldp b = {⟨ xP , x Q⟩ | BP xP + BQxQ ≥ bb},

for suitable ⟨AP , A Q, b a⟩ and ⟨BP , B Q, b b⟩. By Defini-tion 10, the intersection composition dp a ∧dp b ∈ DP {P , Q} 

has feasible set 

ldp int = {⟨ xP , x Q⟩ | AP xP + AQxQ ≥ ba,BP xP + BQxQ ≥ bb}. (4) The constraints in Eq. (4) can be written as a single system of linear inequalities by stacking the two descriptions: 

AP AQ

BP BQ

 "xP

xQ

#

≥

"ba

bb

#

,which defines a polyhedron directly in ⟨xP , x Q⟩. Since P

and Q are Euclidean sub-posets, dp a ∧ dp b is an LDP. 

Feedback. Let dp ∈ LDP {P × R , Q × R} be an LDP with feasible set 

ldp = 

n

xP , x in  

> R

, xQ, x out 

> R

AP xP + AQxQ + Ain 

> R

xin  

> R

+ Aout  

> R

xout  

> R

≥ b

o

.

By Definition 10, Tr (dp) ∈ DP {P , Q} has feasible set 

ldp fb = {⟨ xP , x Q⟩ | ∃ xR : ⟨⟨ xP , x R), (xQ, x R⟩⟩ ∈ ldp }.

(5) Equivalently, ⟨xP , x Q⟩ ∈ ldp fb if and only if there exists 

xR such that  Ain  

> R

+ Aout 

> R

xR + AP xP + AQxQ ≥ b. 

The set of triples ⟨xP , x Q, x R⟩ satisfying this inequality is a polyhedron in the extended space; ldp fb is its projection onto ⟨xP , x Q⟩, hence a polyhedron. Since P and Q remain Euclidean sub-posets, Tr (dp) is an LDP. We now show that every LCDP induces an LDP at the system level, so that its feasible set is a polyhedron and queries can be cast as MOLP. 

Theorem 3 (An LCDP induces an LDP) . Let ⟨F , R, ⟨V , E⟩⟩ 

be an LCDP in the sense of Definition 18. The CDP obtained by composing the component DPs along the multigraph 

⟨V , E⟩ is an LDP in DP {F , R }. In particular, its feasible set ldp tot ⊆ F × R is a polyhedron. 

Proof. A LDP is constructed from a LCDP by applying compositions in Definition 10 to the components according to ⟨V , E⟩ , adding splitter/merger DPs of the form 

dp s 

> P,k def

= { xP , x′P,1, · · · , x ′P,k | xP ⪯ x′P,i , ∀i},

dp m 

> P,k def

= {⟨⟨ xP,1, · · · , x P,k ⟩ , x ′P ⟩ | xP,i ⪯ x′P , ∀i}

when necessary [1]. Since composing LDPs gives LDPs, we only need to prove these splitters/mergers DPs are LDPs, which follows by their definitions. III. F EASIBLE -S ET CONSTRUCTION AND QUERY 

SOLVING FOR LCDP S

Theorem 3 implies that any LCDP induces a system-level LDP, and therefore inherits the MOLP-based query semantics of Theorem 1. Computationally, this gives rise to two tasks: (i) query evaluation , i.e., computing Pareto-minimal resources (or Pareto-maximal functionalities) for fixed external ports, and (ii) explicit feasible-set construction ,i.e., producing an H-representation of the induced system-level polyhedron ldp tot ⊆ F × R for reuse across queries or further analysis. Both tasks reduce to polyhedral calculus. An LCDP can be represented as the Cartesian product of the component polyhedra intersected with a sparse linear subspace enforcing the wiring equalities; the system-level feasible set is then obtained by projection onto the ex-ternal ports. This yields two complementary computational strategies. The monolithic strategy (Section III-A) assembles the lifted block-angular polyhedron once and answers each query as a single MOLP in the full variable space, avoiding explicit projection. The compositional strategy (Section III-B) follows the co-design graph and incrementally eliminates internal wiring variables, producing a lower-dimensional H-representation in external coordinates at the cost of possible inequality growth. Lemmas 4 and 5 formalize the global polyhedral encoding and the equivalence between projected and lifted query solving. 

Lemma 4 (Global polyhedral encoding of an LCDP) . Con-sider an LCDP ⟨F , R, ⟨V , E⟩⟩ in the sense of Definition 18. For each node d ∈ V , let ldp d = {zd ∈ Rnd | Mdzd ≥

md} be an H-representation of its component feasible set (including the port-domain constraints zd ∈ F d × Rd). Let 

zglobal = [ z⊤ 

> 1

, . . . , z ⊤ 

> N

]⊤ and define 

Mblk def 

= block-diag( M1, . . . , M N ),mblk def 

= [ m⊤ 

> 1

, . . . , m ⊤ 

> N

]⊤.

Let Ez global = 0 be the collection of all wiring equalities induced by the edges E (each row of E has exactly one +1 

and one −1). Then the full-space system-level feasible set of the LCDP is the polyhedron 

ldp full tot =

n

zglobal Mblk zglobal ≥ mblk , Ez global = 0 

o

. (6) 

Remark. Writing inequalities in Fig. 1 as equalities in Lemma 4 produces the same system-level feasible set, since for any feasible zglobal where one constraint xP < x P is strict, assigning, for instance, ˆxP def 

= xP , results in another feasible global variable because every component is mono-tone. It also provides and requires the same functionality and resource at the system level. If Sext is a selection matrix extracting the external ports zext = [x⊤ 

> F

, x ⊤ 

> R

]⊤ from zglobal , then the induced 

external-port system-level feasible set is the projection 

ldp tot =

n

zext ∃zglobal ∈ ldp full tot s.t. zext = Sext zglobal 

o

.

(7) 

Proof. By definition of an LCDP (Definition 18), a system design is feasible if and only if each component design zd

satisfies its local feasibility constraints and all interconnec-tions identify the corresponding connected port coordinates by equality. Stacking the local H-representations yields the block-diagonal system Mblk zglobal ≥ mblk , while all port identifications yield the linear equalities Ez global = 0,giving (6). Finally, the system-level feasible set retains only the external ports, i.e., it is the image of the full-space set under the coordinate projection encoded by Sext , which is (7). 

Lemma 5 (Lifted MOLP queries equal projected DP queries) . Let ldp tot and ldp full tot be as in Lemma 4. Let SF

and SR be selection matrices such that xF = SF zglobal and 

xR = SRzglobal . Fix an external functionality ¯xF ∈ F and consider the two feasible resource sets 

Rext (¯ xF ) def 

= {xR | ⟨ ¯xF , x R⟩ ∈ ldp tot },

Rlift (¯ xF ) def 

= {SRzglobal | zglobal ∈ ldp full tot , S F zglobal = ¯ xF }.Then Rext (¯ xF ) = Rlift (¯ xF ), and hence the Pareto-minimal (efficient) resources returned by the DP query coincide with the efficient outcomes of the lifted MOLP 

Min ⪯ SRzglobal 

s.t. Mblk zglobal ≥ mblk , Ez global = 0 , S F zglobal = ¯ xF .

(8) An analogous statement holds for the “fix resources, maxi-mize functionalities” query. 

Proof. By Lemma 4, ⟨¯xF , x R⟩ ∈ ldp tot holds if and only if there exists a full assignment zglobal such that zglobal ∈

ldp full tot , SF zglobal = ¯ xF , and SRzglobal = xR. This is exactly the equality Rext (¯ xF ) = Rlift (¯ xF ). Since both optimization problems compare resource vectors under the same compo-nentwise order ⪯ and have identical attainable resource sets, their efficient outcomes coincide. The second query follows by the same argument with (SF , S R) exchanged and the order reversal accounted for as in Theorem 1. 

A. Monolithic formulation 

The monolithic strategy implements the lifted encoding of Lemma 4 by introducing all component port variables as decision variables, enforcing (i) each component’s local fea-sibility constraints and (ii) all wiring equalities. This yields the full-space polyhedron ldp full tot in (6), whose constraint ma-trix is block-angular : a block-diagonal part Mblk (one block per component) plus a very sparse coupling part E (each wiring row has exactly two nonzeros). For query evaluation, no explicit projection onto external ports is required: by Lemma 5, the projected DP query is equivalent to solving the lifted MOLP (8) (and its dual) in the full variable space. This is typically the most efficient route for single-shot queries, because it avoids polyhedral projection and lets the solver handle internal feasibility implicitly. Algorithm 1 details the construction of the lifted constraint system in practice. 

Remark (Monolithic formulation and Dantzig–Wolfe de-composition) . The lifted feasibility system in (6) is block-angular, the classical structural setting for Dantzig–Wolfe 

(DW) decomposition. For scalar-objective instances (e.g., minimize a single weighted sum of resources), DW can exploit the separation between local component constraints and global couplings to solve large instances via a master problem plus one subproblem per node [20]. For MOLP queries, DW is not directly applicable as a complete Pareto-front method, but the same sparsity remains valuable: many MOLP algorithms (including Benson-type outer approxima-tion) reduce computation to solving sequences of LPs, each inheriting the same coupling structure. This suggests that DW-style decomposition can still be used inside Pareto-front algorithms to accelerate the repeated LP solves. 

B. Compositional approach and variable elimination 

The monolithic polyhedron (6) is built in a lifted space containing all component port variables, including many internal wiring coordinates that are not of direct system-level interest. When one needs an explicit system-level feasible set ldp tot ⊆ F × R (e.g., to amortize repeated queries 

Algorithm 1 Monolithic feasible set for composite LCDP 

Require: LCDP ⟨F , R, ⟨V , E⟩⟩ ; for each d ∈ V (|V| =

N ), component LDP with ldp d = {⟨ xF ,d , x R,d ⟩ :

AF ,d xF ,d + AR,d xR,d ≥ bd}

Ensure: System-level feasible set ldp full tot in H-representation Mblk zglobal ≥ mblk (full-space variables)  

> 1:

Local formulation: For each node d ∈ V , set zd ←

[x⊤

> F ,d

, x ⊤

> R,d

]⊤, Md ← [AF ,d AR,d ], md ← bd 

> 2:

Aggregation: Form zglobal ← [z⊤ 

> 1

, . . . , z ⊤ 

> N

]⊤, Mblk ←

block-diag( M1, . . . , M N ), mblk ← [m⊤ 

> 1

, . . . , m ⊤ 

> N

]⊤ 

> 3:

System Mblk zglobal ≥ mblk is block-diagonal (one block per node)  

> 4:

Edge constraints: For each edge e ∈ E (series or feedback, Definition 18), the edge links one functionality and one resource. In zglobal (with zd = [ x⊤

> F ,d

, x ⊤

> R,d

]⊤), find the indices if , ir of these two entries. Append to 

Mblk one row with +1 at if , −1 at ir, zeros elsewhere, and append 0 to mblk (so zif − zir ≥ 0; add a second row −zif + zir ≥ 0 to enforce equality). Thus each edge adds one (or two) rows to the constraint matrix.  

> 5:

return ldp full tot = {zglobal : Mblk zglobal ≥ mblk }

or to export a reduced model), it can be advantageous to eliminate internal variables and work directly in external coordinates. The compositional approach does exactly this by traversing the co-design graph and applying the same polyhedral operations that appear in the closure proof (The-orem 2): Cartesian products (parallel), constraint stacking (intersection), linear coupling constraints (series/feedback), and projections (variable elimination). 

1) Composition as polyhedral calculus: At the level of feasible sets, the four DP interconnections can be written uniformly as: (i) take a Cartesian product of the participating polyhedra, (ii) intersect with a linear subspace enforcing port identifications, and (iii) project out the internal coordinates. For example, suppose an edge identifies one resource coor-dinate of subsystem u with one functionality coordinate of subsystem v; write this equality as a⊤[z⊤ 

> u

, z ⊤ 

> v

]⊤ = 0 with a sparse row a⊤ containing one +1 and one −1. Then the 

series contraction of u and v is 

Puv = Π keep 



(Pu × Pv ) ∩ {⟨ zu, z v ⟩ | a⊤[z⊤ 

> u

, z ⊤ 

> v

]⊤ = 0 }



,

(9) where Πkeep removes the internal connected coordinate (and possibly other internal ports that cease to be boundary variables after contraction). Parallel composition is simply 

Pu × Pv in disjoint coordinates. Intersection of two compo-nents Pa, P b sharing the same external ports is even simpler: the composed feasible set is Pa ∩ Pb, realized by vertically stacking the two inequality systems without introducing any new variables or projections. Feedback (trace) is the special case where both coordinates live in the same polyhedron P :intersect with an equality subspace identifying the two ports, then project out the identified internal variable. 

2) Graph-guided elimination schedule: The goal is to choose an elimination order that keeps intermediate poly-hedra manageable. We use the multigraph ⟨V , E⟩ to guide a 

projection schedule : 

> •

Intersection preprocessing: Whenever two or more com-ponents share the same external ports (parallel edges in the multigraph), their inequality systems are stacked to form a single merged node before any series or feedback step, a projection-free operation that just concatenates constraints.  

> •

Parallel decomposition: Connected components of the underlying undirected graph are independent and therefore compose in parallel.  

> •

Series-first skeleton: Within each connected component, choose a spanning forest (e.g., via DFS). The forest edges define a series contraction skeleton that reduces the component to a single composite node.  

> •

Feedback-late edges: The remaining edges (those not in the spanning forest) are precisely the cycle-closing constraints. After the series skeleton is contracted, each such edge becomes an internal wiring equality within the composite node and is handled by a feedback (trace) elimination. This is algorithmically convenient: intersection merges re-duce the node count without any projection, series contrac-tions primarily reduce the graph size, while feedback elimi-nations remove the remaining internal degrees of freedom. 

3) Projection primitives and practical considerations: 

Each contraction step requires polyhedral projection. In low dimensions, Fourier–Motzkin elimination (FME) is simple and effective; however, it can generate up to p·q new inequal-ities when eliminating one variable, where p and q are the numbers of inequalities with positive/negative coefficients in that variable. Therefore, practical implementations typically combine projection with: (i) redundancy removal (LP-based checks to delete implied inequalities), (ii) variable scal-ing/normalization to reduce numerical sensitivity, and (iii) 

hybrid cutoffs that switch to the monolithic representation once the inequality count exceeds a budget. 

Lemma 6 (Correctness of compositional elimination) . Al-gorithm 2 returns a polyhedron in the external coordinates whose feasible set equals the system-level feasible set ldp tot 

defined by the LCDP semantics (equivalently, the projection in (7)). 

Proof. Fix a connected component C. Any components shar-ing identical external ports are first merged by stacking their inequality systems; this realizes DP intersection (Defi-nition 10), since the stacked system [Ma; Mb] z ≥ [ma; mb]

defines exactly Pa ∩ Pb. Each subsequent series contraction step in Algorithm 2 replaces two neighboring subsystems by their series interconnection, implemented as “product, wiring equality, and projection” as in (9); this operation is exactly the feasible-set realization of DP series composition in Definition 10. Thus, after contracting all edges of a spanning forest, the resulting composite node represents the feasible set of the parallel-free, cycle-free composition of all subsystems in C.Each remaining (non-forest) edge enforces an additional wiring equality that closes a cycle; imposing that equality and projecting out the internal identified port is precisely the feasible-set realization of DP feedback/trace (Definition 10). Therefore, after all cycle-closing edges are processed, the component polyhedron PC equals the feasible set induced by composing all DPs in C and restricting to its external ports. Finally, distinct connected components share no wiring constraints, so their feasible sets compose by Cartesian product, matching DP parallel composition. Hence Q 

> k

PCk

equals the system-level feasible set in external coordi-nates ldp tot .

Remark (Relation between monolithic and composi-tional representations) . The monolithic formulation con-structs ldp full tot in (6) and then (optionally) projects once onto the external ports. The compositional formulation performs the same projection incrementally during graph reduction. Both methods therefore compute the same external feasible set; they differ only in the elimination, trading off dimen-sionality (fewer variables) against potential inequality growth under projection. 

Remark (MOLP solution via Benson’s algorithm) . Each query on an LDP reduces to a polyhedral MOLP (The-orem 1), or more generally a VLP (see the remark fol-lowing Definition 17). For polyhedral MOLP, the Benson outer-approximation algorithm is finite [17, Theorem 3.1] and recovers efficient extreme outcomes [17, Theorem 3.2]; further analysis shows recovery of weakly efficient outcomes as well [21, Theorem 3.3]. IV. C ONVEX FEASIBLE -SET APPROXIMATION 

The monolithic and compositional strategies of Sec-tions III-A and III-B exploit that every component is an LDP, so that the induced system-level feasible set is a polyhedron and every query reduces to an MOLP. In practice, however, subsystem models often involve nonlinear couplings that re-main convex but are not polyhedral (e.g., second-order cone constraints, semidefinite constraints, or the convex bilinear inequality p m ≥ c over p, m ≥ 0 that appears in Section V-C). In such cases, the induced system-level feasible set is still a closed convex upper set, but it is not an LDP. This section shows how convex co-design models can still be handled by the LDP pipeline via polyhedral outer approximation. 

1) From convex DPs to approximate LDPs: We say that a DP dp ∈ DP {F , R } is convex if its feasible set dp ⊆

F op × R ⊆ RnF +nR is nonempty, closed, and convex (in the ambient Euclidean topology). 1 Convex sets admit polyhedral approximations. 

Definition 19 (Polyhedral outer approximation) . Let C ⊆ Rn

be nonempty. A polyhedron P ⊆ Rn is a polyhedral outer approximation of C if C ⊆ P . If, in addition, C is an upper 

> 1Linear images (projections) of closed convex sets need not be closed in general. When this matters, one can either assume regularity guaranteeing closedness of the induced projections, or work with the closure of the induced set (a convention in convex vector optimization when defining upper images).

Algorithm 2 Compositional feasible set via graph traversal 

Require: LCDP ⟨F , R, ⟨V , E⟩ ; for each d ∈ V , component LDP with (Md, m d) s.t. ldp d = {zd : Mdzd ≥ md}

Ensure: System-level feasible set ldp tot in H-representation 

MGzext ≥ mG (external-port variables)  

> 1:

Classify edges: Run DFS on the directed graph (resource 

→ functionality). Partition E = Eser ∪ Efb (tree/forward = series, back edges = feedback). Find connected com-ponents of the underlying undirected graph for parallel grouping.  

> 2:

Merge intersections: For every group of nodes sharing the same external ports (parallel edges in the multi-graph), stack their inequality systems [Ma; Mb] z ≥

[ma; mb] into a single node. No projection is needed.  

> 3:

Process series: Within each component, compute atopological order of nodes (DAG from removing back-edges). In this order, maintain a current composite LDP; for each series edge ⟨u, v ⟩, compose with v in series as in the proof of Theorem 2: form the stacked polyhedron in 

⟨xP , x Q, x R⟩, then apply FME to project onto ⟨xP , x R⟩.Replace u, v by the composite node and update the graph.  

> 4:

Process parallel: The previous step yields one com-posite LDP per connected component. Compose these in parallel as in Theorem 2: Cartesian product (block-diagonal M , stacked m), no shared variables.  

> 5:

Process feedback: For each remaining back edge (feed-back loop), apply the trace as in Theorem 2: impose equality of the linked resource and functionality, then project out that variable via FME. Update the composite.  

> 6:

return ldp tot = {zext : MGzext ≥ mG}

set in a cone-ordered space ⟨K, ⪯K ⟩ (i.e., C +K ⊆ C), then 

P is an upper-set outer approximation if P + K ⊆ P .An outer approximation yields an approximate LDP (hence MOLP-solvable queries), but its accuracy must be quantified in a way that remains meaningful for unbounded 

sets. Indeed, co-design query-feasible resource sets are inher-ently unbounded. By the upper-set property of DPs (Defini-tion 9), if xR is feasible for ¯xF , then any x′ 

> R

⪰ xR is feasible as well. For unbounded closed convex sets, the classical Hausdorff distance between the set and its approximation is usually infinite unless their recession cones coincide exactly; see [22, Proposition 3.5]. We therefore adopt the (ε, δ )-approximation framework of [22], which separates the error on the bounded part, measured by ε, from the asymptotic error in the recession cone, measured by δ. We recall the main notions from [22]. 

Definition 20 (Recession cone, line-freeness, self-bounded-ness) . Let C ⊆ Rn be nonempty, closed, and convex. Its 

recession cone is 

0+C def 

= {d ∈ Rn | x + td ∈ C, ∀x ∈ C, ∀t ≥ 0}.C is line-free if 0+C ∩ (−0+C) = {0} (i.e., the recession cone is pointed). If 0+C̸ = {0}, the set C is self-bounded 

if there exists y ∈ Rn such that C ⊆ y + 0 +C [22, Definition 3.2]. For nonempty sets C1, C 2 ⊆ Rn, the excess of C1 over C2

is 

e[C1, C 2] def 

= sup 

> c1∈C1

inf 

> c2∈C2

∥c1 − c2∥.

The Hausdorff distance is 

dH(C1, C 2) def 

= max {e[C1, C 2], e [C2, C 1]}

For nonempty closed convex cones K1, K 2, the truncated Hausdorff distance is 

¯dH(K1, K 2) def 

= dH(K1 ∩ B1(0) , K 2 ∩ B1(0)) .

Definition 21 ((ε, δ )-approximation [22, Definition 4.2]) .

Let C ⊆ Rn be nonempty, closed, convex, and line-free. A line-free polyhedron P ⊆ Rn is an (ε, δ )-approximation 

of C if: (i) e[vert P, C ] ≤ ε, (ii) ¯dH(0 +P, 0+C) ≤ δ, and (iii) C ⊆ P .We now collect two results from [22] that we will use. 

Theorem 7 (Existence in Hausdorff distance and conver-gence of approximations) . Let C ⊆ Rn be nonempty and closed. (a) (Hausdorff-approximability) If C is convex, 0+C is polyhedral, and e[C, 0+C] < ∞, then for every ε > 0

there exists a finite set V such that dH

 C, conv V +0+C ≤ ε [22, Theorem 3.1]. In particular, the approx-imating polyhedra can be chosen with 0+P = 0 +C.(b) (Convergence) If C is additionally line-free and P ν is an (εν , δ ν )-approximation of C with (εν , δ ν ) → (0 , 0) ,then P ν → C in the sense of Painlev´ e–Kuratowski [22, Theorem 4.9]. 

2) Specialization to co-design resource queries: While the approximation in Definition 21 is generally described by two parameters, a useful simplification arises for co-design 

resource queries . Because feasible resource sets are upper sets in a cone-ordered resource space, their recession cone is necessarily equal to the resource cone. Consequently, for the standard Euclidean resource poset R = RnR 

> ≥0

, the recession-cone part of the approximation can be matched exactly (i.e., 

δ = 0 ), and the approximation task reduces to controlling ε.Fix a DP dp ∈ DP {F , R } over Euclidean resource coordinates, and fix a functionality ¯xF ∈ F . Define the 

feasible resource R(¯ xF ) set and the corresponding query graph G¯xF :

R(¯ xF ) def 

= {xR ∈ R | ⟨ ¯xF , x R⟩ ∈ dp },

G¯xF

> def

= {⟨ ¯xF , x R⟩ | xR ∈ R (¯ xF )}.

By the upper-set property of DPs (Definition 9), R(¯ xF ) is an upper set in the resource poset. 

Lemma 8 (Query-graph recession structure) . Let dp ∈

DP {F , R } have nonempty, closed, convex feasible set in 

F op × R. Fix ¯xF ∈ F and assume R(¯ xF )̸ = ∅. Then: (a) 0+G¯xF = {0}nF × 0+R(¯ xF ).(b) If R = R1 × · · · × Rk is a product of nonempty closed convex cones and R(¯ xF ) is an upper set in R,then 0+R(¯ xF ) = 0 +R = R. 

Proof. (a) By definition, G¯xF fixes the functionality coordi-nates at ¯xF . A direction ⟨dF , d R⟩ belongs to 0+G¯xF if and only if for all t ≥ 0, ⟨¯xF , x R⟩ + t ⟨dF , d R⟩ ∈ G ¯xF , which forces ¯xF + td F = ¯ xF for all t ≥ 0 and hence dF = 0 . The resource direction dR is then feasible for all t exactly when 

dR ∈ 0+R(¯ xF ). This proves the identity. 

(b) We show both inclusions. 

(i) R ⊆ 0+R(¯ xF ). Let d ∈ R and xR ∈ R (¯ xF ). Since R

is a cone, td ∈ R for all t ≥ 0 and xR + td ⪰ xR. Because 

R(¯ xF ) is an upper set in R, xR + td ∈ R (¯ xF ) for all t ≥ 0.Hence d ∈ 0+R(¯ xF ).

(ii) 0+R(¯ xF ) ⊆ R. Let d ∈ 0+R(¯ xF ) and pick any 

xR ∈ R (¯ xF ) ⊆ R. Then xR+td ∈ R (¯ xF ) ⊆ R for all t ≥ 0.Since R is a cone, for every t > 0 we have (xR + td )/t =

d + xR/t ∈ R. Letting t → ∞ and using closedness of R

yields d ∈ R.Finally, since R is a closed convex cone, 0+R = R.

Lemma 9 (Recession cone of an upper feasible resource set) .

Let K ⊆ Rn be a nonempty closed convex cone ordered by 

x ⪯K y ⇔ y − x ∈ K. Let C ⊆ K be nonempty and an upper set in K (i.e., C + K ⊆ C). Then 0+C = K.

Proof. This is exactly Lemma 8(b) with R = K and C =

R(¯ xF ), but we repeat the short argument for completeness. First, K ⊆ 0+C: for any d ∈ K and x ∈ C, x + td ∈

x + K ⊆ C for all t ≥ 0. For the reverse inclusion, let 

d ∈ 0+C and choose x ∈ C ⊆ K. Then x + td ∈ C ⊆ K

for all t ≥ 0. Since K is a cone, d + x/t ∈ K for all t > 0;letting t → ∞ and using closedness gives d ∈ K.

Theorem 10 (Natural δ = 0 guarantee for convex co-design resource queries) . Assume the system resource poset is the standard Euclidean cone R = RnR 

> ≥0

. Fix ¯xF ∈ F and let 

C def 

= R(¯ xF ) be the feasible resource set, assumed nonempty, closed, and convex. Then: (a) 0+C = RnR 

> ≥0

and C is line-free and self-bounded. (b) For every ε > 0 there exists a polyhedron P ⊇ C with 

0+P = 0 +C such that P is an (ε, 0) -approximation of 

C in the sense of Definition 21. (c) Any sequence of (εν , 0) -approximations with εν → 0

converges to C in the sense of Painlev´ e–Kuratowski. 

Proof. (a) Since C is an upper set in RnR 

> ≥0

, Lemma 9 yields 

0+C = RnR 

> ≥0

. The cone RnR 

> ≥0

is pointed, hence 0+C ∩

(−0+C) = {0} and C is line-free. Self-boundedness holds because C ⊆ RnR 

> ≥0

= 0 + 0 +C.

(b) We apply Theorem 7(a). Here 0+C = RnR 

> ≥0

is poly-hedral. Moreover, since C ⊆ 0+C, we have e[C, 0+C] = 0 < ∞. Hence for any ε > 0 there exists a polyhedron 

P = conv V + 0 +C with dH(C, P ) ≤ ε and 0+P = 0 +C.Then e[vert P, C ] ≤ e[P, C ] ≤ dH(C, P ) ≤ ε, so P is an 

(ε, 0) -approximation. 

(c) This is Theorem 7(b) with δν = 0 .

Lemma 11 (From ε-outer approximation to guaranteed feasibility by inflation) . Let C ⊆ Rn 

> ≥0

be a nonempty upper set. (a) If P ⊇ C satisfies e[P, C ] ≤ ε, then every p ∈ P admits a certified-feasible inflated point psafe def 

= p + ε 1 ∈ C. 

(b) If P ⊇ C satisfies only e[vert P, C ] ≤ ε, then the same conclusion holds for every vertex v ∈ vert P : v + ε1 ∈

C.

Proof. We prove (a); (b) is the same argument restricted to 

p = v ∈ vert P . Fix p ∈ P . By e[P, C ] ≤ ε there exists 

c ∈ C such that ∥p − c∥ ≤ ε. Hence |pi − ci| ≤ ε for each coordinate i, so c ⪯ p + ε1 componentwise. Since C is an upper set, p + ε1 ∈ C.

Remark (Practical implications for LDP pipeline) . Theo-rem 10 explains why, for standard nonnegative resource coordinates, the recession-cone part of (ε, δ )-accuracy can be made exact ( δ = 0 ): the unbounded directions of every convex query-feasible resource set are fixed by monotonic-ity and equal RnR 

> ≥0

. Thus accuracy control reduces to the bounded-part error ε.Operationally, one can build polyhedral outer approxima-tions by adding supporting halfspaces (e.g., tangent planes from subgradients of convex constraints), yielding an ap-proximate LDP that can be queried via MOLP. The obtained resource vectors are optimistic in general, but Proposition 11 shows how an ε bound can be converted into a simple, explicit feasibility margin. In the rover case study (Section V-C), the linearization parameter N (number of tangent-plane cuts) controls the tightness of the outer approximation; em-pirically ε decreases as N grows, and the limiting behavior is captured by Theorem 10(c). 

3) Approximation under co-design composition: So far, we have discussed how a single convex DP can be ap-proximated by an LDP. We now extend this idea to co-design problems involving multiple convex design problems. Replacing each convex component feasible set by a poly-hedral outer approximation yields a polyhedral DP, i.e., an LDP. Importantly, outer approximations propagate through co-design composition. 

Lemma 12 (Outer approximations are preserved by com-position) . Consider a finite co-design interconnection built from classic operations (Definition 10). Let {Cd}d∈V be the true component feasible sets and {Pd}d∈V polyhedral outer approximations with Cd ⊆ Pd for all d. Let Ctot and Ptot be the induced system-level feasible sets obtained by composing 

{Cd} and {Pd} along the same interconnection graph. Then 

Ctot ⊆ Ptot . Moreover, if each Pd is an upper-set outer approximation with respect to the corresponding resource cone, then Ptot is an upper-set outer approximation of Ctot 

with respect to the system resource cone. 

Proof. At the feasible-set level, each of the three intercon-nections is a combination of: (i) Cartesian products, (ii) intersection with a linear subspace (wiring equalities), and (iii) linear projection (elimination of internal coordinates); see Section IV-.4. Each of these operations is monotone with respect to set inclusion: if A ⊆ B then A × D ⊆ B × D,

A ∩ S ⊆ B ∩ S, and Π( A) ⊆ Π( B). Applying these facts along a finite composition sequence yields Ctot ⊆ Ptot .For the upper-set statement, note that if each component approximation satisfies Pd + Kd ⊆ Pd for its cone Kd,then Cartesian products preserve this property with product cones, intersections with linear subspaces (since Minkowski addition commutes with affine subspaces), and projections since Π( P + K) = Π( P ) + Π( K) and Π( K) is the projected cone governing the remaining resource coordinates. 

Remark (Outer vs. inner approximation and conservative-ness) . An outer approximation P ⊇ C is conservative for 

infeasibility certification (if r /∈ P then certainly r /∈

C), but it is generally optimistic for minimization queries (minimizing over P can return resources that are not feasible for C). The (ε, δ ) framework below quantifies this optimism; additionally, for upper sets one can convert an ε-accurate outer point into a guaranteed-feasible point by a simple inflation margin (see Proposition 11). 

4) Recession-cone calculus for co-design interconnections: The feasible-set realizations of parallel/series/feedback interconnections are built from Cartesian products, affine intersections (wiring equalities), and coordinate projections; see Section IV-.4. The following lemma records the corresponding recession-cone calculus with explicit proofs. A key subtlety is that, for general convex sets, projection can enlarge the recession cone ;hence we state the strongest always-true inclusion and, separately, the equality that holds for polyhedra. 

Lemma 13 (Recession cones under product, affine intersec-tion, and projection) . Let C1, C 2 ⊆ Rn be nonempty, closed, and convex. Then: (i) 0+(C1 × C2) = 0 +C1 × 0+C2.(ii) If S def 

= {x | Ax = b} and C ∩S̸ = ∅, then 0+(C ∩S) = 0+C ∩ ker A. 

(iii) For any linear map L, one always has L(0 +C) ⊆

0+(L(C)) . If, in addition, C is a polyhedron, then 0+(L(C)) = L(0 +C), and in particular 0+(L(C)) 

is polyhedral whenever 0+C is polyhedral. 

Proof. We repeatedly use the following characterization valid for nonempty closed convex sets D:

d ∈ 0+D ⇐⇒ ∃¯x ∈ D s.t. ¯x + td ∈ D ∀t ≥ 0. (10) The implication “ ⇒” is immediate. For “ ⇐”, fix x ∈ D and 

t ≥ 0. Since ¯x + ntd ∈ D for all n ∈ N and D is convex, 

xn def 

=



1 − 1

n



x + 1

n (¯ x + ntd ) = x + td + ¯x − xn ∈ D. 

By closedness, xn → x + td implies x + td ∈ D, hence 

d ∈ 0+D.

(i) Let ⟨d1, d 2⟩ ∈ 0+(C1 × C2). Then for all ⟨x1, x 2⟩ ∈ 

C1 × C2 and t ≥ 0, ⟨x1 + td 1, x 2 + td 2⟩ ∈ C1 × C2, so 

x1 +td 1 ∈ C1 and x2 +td 2 ∈ C2. Thus d1 ∈ 0+C1 and d2 ∈

0+C2, proving 0+(C1 × C2) ⊆ 0+C1 × 0+C2. Conversely, if di ∈ 0+Ci (i = 1 , 2), then for any ⟨x1, x 2⟩ ∈ C1 × C2

and t ≥ 0, ⟨x1 + td 1, x 2 + td 2⟩ ∈ C1 × C2, so ⟨d1, d 2⟩ ∈ 

0+(C1 × C2). Hence equality holds. 

(ii) Let S = C ∩ { x | Ax = b}̸ = ∅. If d ∈ 0+S, then by (10) there exists ¯x ∈ S with ¯x + td ∈ S for all t ≥ 0. Since 

S ⊆ C, we have ¯x + td ∈ C for all t ≥ 0, hence d ∈ 0+C.Moreover, A(¯ x + td ) = b and A¯x = b imply Ad = 0 ,so d ∈ ker A. Thus 0+S ⊆ 0+C ∩ ker A. Conversely, if 

d ∈ 0+C ∩ ker A, then for any x ∈ S and t ≥ 0, x + td ∈ C

(since d ∈ 0+C) and A(x + td ) = Ax + tAd = b (since 

x ∈ S and Ad = 0 ), so x + td ∈ S. Hence d ∈ 0+S and 

0+C ∩ ker A ⊆ 0+S, proving equality. 

(iii) inclusion. Let d ∈ 0+C and take any y = Lx ∈

L(C). Then for all t ≥ 0, y + tLd = L(x + td ) ∈ L(C),so Ld ∈ 0+(L(C)) . Thus L(0 +C) ⊆ 0+(L(C)) .

(iii) equality for polyhedra. Assume C is a polyhedron, and write it in H-representation as C = {x | Gx ≥ h}.Its recession cone is 0+C = {d | Gd ≥ 0}. Consider the image L(C) = {y | ∃ x : y = Lx, Gx ≥ h}, which is again a polyhedron. A direction w is in 0+(L(C)) if and only if for every feasible ⟨y, x ⟩ with y = Lx , one has y + tw ∈ L(C) for all t ≥ 0, i.e., there exist xt

with Lx t = Lx + tw and Gx t ≥ h. By polyhedrality, this is equivalent to the existence of some direction d with 

Gd ≥ 0 and Ld = w (one can see this by applying the standard characterization of recession directions to the lifted polyhedron {⟨ x, y ⟩ | Gx ≥ h, y = Lx } and then projecting onto y). Hence w ∈ L(0 +C), proving 0+(L(C)) ⊆ L(0 +C),and thus equality. 

Remark (How DP interconnections map to set operations) .

The three interconnections in Definition 10 are built from the operations of Theorem 13: (a) Parallel. For feasible sets Ca ⊆ P ×Q and Cb ⊆ P ′ ×Q′,

Cpar = Ca × C b.(b) Series. Writing the two intermediate variables separately as qa, q b,

bCser def 

= ( Ca × C b) ∩ {⟨ p, q a, q b, r ⟩ | qa = qb},

Cser = Π p,r 

 bCser 



.

(c) Feedback. For C ⊆ (P × Rin ) × (Q × Rout ),

bCfb def 

= C ∩ {⟨ p, r in , q, r out ⟩ | rin = rout },

Cfb = Π p,q 

 bCfb 



.

Thus every finite composition graph built from paral-lel/series/feedback applies only these set operations. V. N UMERICAL EXPERIMENTS 

We evaluate the computational pipeline developed in Sec-tion III and its convex extension from Section IV. We consider three complementary benchmarks: (i) a synthetic series-chain family that isolates the trade-off between lifted sparsity and variable elimination for exact LCDPs; (ii) a rigid gripper co-design instance that is natively polyhedral after an exact reparameterization ( exact LCDP regime); and (iii) a rover co-design instance with a bilinear coupling that yields DP 0 DP 1 · · · DP m-1                

> ⪯
> ⪯
> f1
> fk
> r1
> rk
> Fig. 2: Series-chain benchmark with mnodes. Each DP ihas k-dimensional functionality and resource ports, and the resource of DP ifeeds the functionality of DP i+1 .

a convex but nonpolyhedral feasible resource set, exercising the outer-approximation pipeline. Unless stated otherwise, all reported runtimes are end-to-end wall-clock times for a single query instance, including model construction and solver calls but excluding plotting. 2

All polyhedral queries are solved with B ENSOLVE [16]. As a baseline, we also compare against MCDPL [5], [12], a general solver for DPs. 

Remark (Discretization in MCDPL) . For any DP dp whose antichains have infinite cardinality (e.g. a continuous rela-tion such as f1 ≤ r1 + r2), MCDPL replaces the exact relation with a pair of finite-cardinality DPs ⟨dp UR, dp LR⟩

parameterized by a resolution R ≥ 1. The upper-bound DP 

dp UR consists of R points sampled on the Pareto boundary of the feasible resource set and serves as a pessimistic (inner) approximation of the true relation; the lower-bound DP dp LR is constructed by taking pairwise meets of R + 1 

successive boundary samples and serves as an optimistic (outer) approximation. The solver uses Van der Corput sam-pling [23, Section 5.2], so dp LR+1 ⪯ dp LR and dp UR ⪯ dp UR+1 ,guaranteeing monotone convergence to the exact relation as 

R → ∞ .

A. Series-chain LCDP scalability benchmark 1) Benchmark topology: The benchmark (Fig. 2) is a series chain of m nodes DP 0, . . . , DP m−1. Each node has k-dimensional functionality and k-dimensional resource ports. Consecutive nodes are interconnected in series by identifying the resource of DP i with the functionality of DP i+1 , com-ponentwise. The external ports are the functionality of DP 0,fixed to fext = c ∈ Rk, and the resource of DP m−1, which is minimized in the posetal sense. This acyclic topology isolates series composition and is therefore a natural stress test for the projection-based construction of Section III-B. 

2) Component generation: Each node is a synthetic LDP with k functionality variables f ∈ Rk

> ≥0

, k resource vari-ables r ∈ Rk

> ≥0

, and c def 

= k + 1 linear inequalities. The first k

rows are pass-through constraints 

fj ≤ γj rj + dj , j = 1 , . . . , k, (11) with γj ∼ Unif(0 .5, 1.5) . The remaining row is a cross-coupling trade-off constraint 

a f j⋆ ≤

> k

X

> i=1

biri + e, (12) with a, b i ∼ Unif(0 .5, 2.0) and fixed j⋆ = 1 . This produces nontrivial Pareto frontiers while keeping the per-node com-plexity fixed as m varies. All random coefficients are drawn   

> 2All experiments were run on a macOS Tahoe 26.2 machine with an Apple M2 Pro CPU (10 cores, 3.2 GHz) and 16 GB of RAM. TABLE I: Series-chain benchmark ( k=2 ,c=3 , fixed fext =1). “Vars” and “Cstr” count scalar decision variables and inequalities in the feasible-set representation. “ ✓” denotes exact agreement between the monolithic and hybrid pipelines in Pareto vertex/ray count.

Monolithic Hybrid FME MCDPL 

m Vars Cstr Time Vars Cstr Time Time Pts Agree 3 12 17 0.018 4 12 0.328 11.1 27 ✓

4 16 24 0.011 4 14 0.584 11.2 34 ✓

6 24 38 0.010 4 26 1.680 47.2 39 ✓

8 32 52 0.024 4 20 3.083 19.0 22 ✓

10 40 66 0.020 4 29 5.598 27.0 44 ✓

15 60 101 0.031 4 36 31.697 43.5 44 ✓

20 80 136 0.044 4 40 61.240 67.3 44 ✓

30 120 206 0.090 64 151 33.612 127.0 44 ✓

with a fixed seed. Intercepts are chosen with a feasibility margin μ = 0 .1, ensuring that the tested query is feasible. We further require that each inequality contain at most one functionality variable on the left-hand side. This matches the most compact MCDPL encoding used here; more general left-hand-side couplings can be represented by auxiliary compositions, but substantially increase the symbolic size of the baseline model and would confound the scaling comparison. 

3) Compared methods and parameters: We compare three methods. First, in the monolitic (exact) , we assemble the lifted block-angular polyhedron of Section III-A and solve the resulting MOLP exactly with B ENSOLVE [16]. Second, using hybrid compositional elimination (exact) , we apply series contractions using FME with redundancy removal; if the intermediate inequality count exceeds a threshold ¯c, we fall back to the monolithic representation for the remaining subsystem. Finally, with MCDPL (approximate) , encode the same chain in the current state of the art MCDPL and compute optimistic/pessimistic Pareto approximations at res-olution R [1], [12]. Unless stated otherwise, we use k =2, c = k + 1 = 3 constraints per node, seed = 42 , FME cutoff ¯c = 50 , MCDPL resolution R = 5 , and timeout 300 s .

a) Results: For a chain of length m, the monolithic model has nvars = 2 km , nineq = cm + 2 k(m − 1) , since each node contributes 2k port variables and c local inequal-ities, and each of the m − 1 interconnections contributes k

equalities written as two inequalities. For k = 2 and c = 3 ,this gives nvars = 4 m and nineq = 7 m−4, matching Table I. The monolithic pipeline is consistently fastest, with end-to-end time essentially constant at the scale tested. Its advantage here comes from preserving the sparse block-angular structure of the lifted representation and avoiding any projection step. The hybrid FME pipeline eliminates all internal ports and reduces the final query dimension to the 2k external variables, but its preprocessing cost grows with m due to repeated projection and redundancy removal. The non-monotone final constraint counts in Table I reflect the fact that many generated inequalities are subsequently removed as redundant. At the chosen cutoff ¯c = 50 , all re-ported instances remained fully compositional; the “hybrid” safeguard is included for robustness on larger chains. At fixed resolution R = 10 , MCDPL is one to three orders of magnitude slower than the algebraic pipelines and returns resolution-dependent Pareto point sets rather than the exact polyhedral frontier. For all tested m, the monolithic and hybrid pipelines return identical Pareto vertex/ray counts, confirming that the compositional elimination preserves the exact feasible set (Lemma 6). 

B. Rigid gripper co-design: exact polyhedral regime 

The series-chain benchmark isolates algorithmic scaling on synthetic data. We next consider a small but physically grounded multi-objective co-design problem that is an exact 

LCDP after suitable reparameterization, characterizing the zero-approximation-error regime of the proposed framework. 

1) System and query: We study a tendon-driven rigid gripper with four conceptual design sub-problems: a mo-tor (maps actuator investment ⟨mmot , c mot ⟩ to achievable tendon force fgrasp ), a material-selection module (given a mass and cost budget ⟨mf , c f ⟩, determines achievable segment lengths ⟨LAl , L CF ⟩), a gripper-geometry module (maps lengths ⟨LAl , L CF ⟩ to structural workspace and grasp-ing capacity), and an arm-cost aggregation module (maps workspace requirement fws to arm mounting cost carm ). For a co-design diagram, refer to Fig. 3. The system-level functionality is q = ⟨fgrasp , f ws ⟩, where fgrasp is the required grasping force and fws is the required workspace. The system-level resources are r = ⟨rcost , r mass ⟩, which are minimized in the Pareto sense. We use the query fgrasp ≥

100 N , fws ≥ 80 mm .

2) LCDP model formulation: The gripper LCDP is parameterized by two continuous design variables, ⟨LAl , L CF ⟩ ∈ R2

> ≥0

, representing the aluminum and carbon-fibre finger lengths. These variables are the main coupling medium across the architecture, as they jointly determine workspace, structural feasibility, finger mass, and material cost. The resulting model has nf = 2 functionality dimensions, nr = 8 resource dimensions, and seven linear inequalities grouped by subsystem. 

a) Actuation (motor sub-problem): The available grasp force increases with both motor mass and motor cost. A heavier motor accommodates a larger rotor, while a costlier motor can achieve higher torque density. We capture these two effects with the supporting halfspace 

fgrasp ≤ αf mmot + βf cmot , (13a) where αf = 400 N /kg and βf = 0 .6 N /USD .

b) Kinematics and structure (finger sub-problem): 

Finger length determines both reach and structural feasibility. The usable workspace grows linearly with total finger length, 

fws ≤ kw

 LAl + LCF 

, kw = 0 .35 , (13b) while sustaining a grasping force requires a minimum total length, 

LAl + LCF ≥ γL fgrasp , γL = 0 .5 mm /N. (13c) Thus both functionality requirements couple to the same design pair ⟨LAl , L CF ⟩.Gripper                               

> length AL length CF workspace grasping force
> Material
> finger mass finger cost
> Motor
> motor mass motor cost
> Arm arm cost ++ total mass total cost
> Fig. 3: Gripper co-design architecture. The query ⟨fgrasp , f ws ⟩is propa-gated to subsystem design choices; the motor and material subsystems are coupled through the shared design variables ⟨LAl , L CF ⟩; and contributions are aggregated into system objectives ⟨rcost , r mass ⟩.TABLE II: Exact Pareto vertices for the rigid gripper LCDP (monolithic solver). Query: fgrasp ≥100 N ,fws ≥80 mm .V1–V3are the only Pareto-optimal solutions; no approximation is introduced. Vertex Dominant material mix rcost (USD) rmass (kg)
> V1Pure aluminum (min cost / max mass) 5 371.06 3.0696
> V2Mixed Al / CF (balanced trade-off) 5 518.10 2.7429
> V3Pure carbon fibre (min mass / max cost) 9 633.00 0.7677

c) Material physics (material sub-problem): The finger lengths also determine mass and raw material cost: 

mf ≥ ρAl LAl + ρCF LCF , (13d) 

cf ≥ pAl LAl + pCF LCF . (13e) We use ρAl = 0 .012 kg /mm , ρCF = 0 .004 kg /mm , pAl =2.0 USD /mm , and pCF = 8 .0 USD /mm . Hence carbon fibre is lighter but more expensive per unit length, which is the main source of the Pareto trade-off. 

d) System-level aggregation: The system-level objec-tives aggregate subsystem resources. Total mass is 

rmass ≥ mmot + mf , (13f) and total cost combines motor cost, arm payload penalties, finger material cost, and a workspace-dependent term: 

rcost ≥ cmot + kAP mmot + cf + kAP mf + kAW fws + cA0 ,

(13g) with kAP = 200 USD /kg , kAW = 50 USD /mm , and cA0 =300 USD . Substituting (13d)–(13e) into (13g) yields effective per-length coefficients pAl +kAP ρAl = 4 .4 USD /mm , pCF +

kAP ρCF = 8 .8 USD /mm , which compactly combine ma-terial price and arm load-bearing penalty. The density–cost asymmetry between aluminum and carbon fiber drives the Pareto trade-off between total mass and total cost. 

3) Results: We compare: (i) the monolithic exact LCDP pipeline, which solves the induced bi-objective MOLP with BENSOLVE ; and (ii) MCDPL optimistic approximations at resolutions R ∈ { 5, 10 , 20 , 40 , 60 , 85 }. Because the model is already polyhedral, this benchmark cleanly separates the ex-act LCDP regime from discretization-based approximation. Table II reports the three exact Pareto vertices returned by the monolithic solver; Table III summarizes timing and approximation quality. The right panel of Fig. 4 visualises the accuracy–runtime trade-off. The monolithic LCDP solve enumerates the exact frontier (3 vertices) in approximately 48 ms end-to-end. In contrast, MCDPL optimistic runs require approximately 9–15 s per resolution and produce increasingly dense discretized fronts 5000 6000 7000 8000 9000 10000            

> Total cost [USD]
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> Total mass [kg]
> Rigid Gripper LCDP Pareto Front
> (grasping_force 100.0 N, workspace 80.0 mm)
> Monolithic (exact)
> MCDPL R=5 (25 pts)
> MCDPL R=10 (84 pts)
> MCDPL R=20 (207 pts)
> MCDPL R=40 (430 pts)
> 810 12 14 16
> Computational Time [s]
> 0.00
> 0.05
> 0.10
> 0.15
> 0.20
> IGD (normalised)
> R=5
> R=10
> R=20
> R=40
> R=60
> R=85
> Accuracy vs Runtime
> MCDPL optimistic
> Monolithic (exact, IGD = 0)

Fig. 4: Pareto-front comparison for the rigid gripper case study. (Left) Monolithic exact frontier (3 vertices) versus MCDPL optimistic approximations at resolutions R ∈ { 5, 10 , 20 , 40 }. (Right) IGD convergence versus computational time: the normalised Inverted Generational Distance of MCDPL optimistic solutions decreases monotonically with increasing resolution, while the exact monolithic solver achieves IGD = 0. TABLE III: Rigid gripper: monolithic exact solver vs. MCDPL optimistic approximations. IGD is the Inverted Generational Distance computed against the densified exact frontier (200 reference points), normalised to [0 , 1] 2

using the ideal–nadir range. Method R #Pts Time (s) IGD Monolithic (exact) — 3 4.8 × 10 −2 0MCDPL opt. 5 25 9.59 2.15 × 10 −1

MCDPL opt. 10 84 9.03 9.09 × 10 −2

MCDPL opt. 20 207 9.54 4.16 × 10 −2

MCDPL opt. 40 430 10 .09 2.26 × 10 −2

MCDPL opt. 60 642 11 .69 1.64 × 10 −2

MCDPL opt. 85 944 14 .96 1.31 × 10 −2

(from 25 points at R = 5 to 944 points at R = 85 ). Ap-proximation quality is measured by the normalised Inverted Generational Distance (IGD), computed against a densified reference front (200 points interpolated along the exact piecewise-linear Pareto curve) and normalised to [0 , 1] 2 using the ideal–nadir range. The IGD decreases monotonically from 2.15 × 10 −1 at R = 5 to 1.31 × 10 −2 at R =85 , confirming theoretical convergence of the optimistic approximation. Even at R = 85 , the exact monolithic pipeline remains about 300 × faster while delivering zero approximation error. 

C. Rover co-design case study with bilinear coupling 

The rigid gripper benchmark illustrates the exact poly-hedral regime. We now consider a small but representative example that falls outside the exact LCDP class, i.e., a rover co-design problem with a bilinear battery coupling. For the fixed query studied here, the induced feasible resource set is closed and convex but not polyhedral, making it a natural test case for the outer-approximation framework of Section IV. 

1) Model: The rover architecture in Fig. 5 couples a chassis subsystem and a battery subsystem. The system-level functionality query is f = ⟨mpay , v req ⟩, and the system-level resource vector to be minimized is r = ⟨csys , m sys ⟩.3

> 3

In Fig. 5, these correspond to ⟨extra payload , velocity ⟩ and 

⟨overall cost , total mass ⟩, respectively. 

Fig. 5: Rover co-design architecture. The query ⟨mpay , v req ⟩ propagates to chassis and battery choices; battery and chassis are coupled through power and mass; and the system-level objectives are ⟨csys , m sys ⟩.

a) Chassis: The chassis provides total load ℓ and velocity v, and requires power pch and total mass mtot :

pch ≥ p0 + κ(ℓ + ms)v, mtot ≥ ℓ + ms, (14) with ms = 770 kg , p0 = 10 W , and κ = 1 W · s/(m · kg) .

b) Battery: The battery provides power pbat and re-quires battery mass mb and cost c:

pbat ≤ αm b + βc + γm bc, (15) with α = 0 .975 W /kg , β = 0 .02733 W /USD , and γ =0.0005 W /(kg · USD) . The bilinear term γm bc is the sole source of nonpolyhedrality. 

c) Interconnection: The two subsystems are wired through 

mpay + mb ≤ ℓ, pch ≤ pbat , vreq ≤ v, csys ≥ c, msys ≥ mtot . (16) We use the query mpay = 0 .1 kg and vreq = 0 .05 m /s,and minimize the bi-objective resource vector ⟨csys , m sys ⟩.

2) Compared methods: We compare four solution routes. 

a) Method I: high-resolution reference frontier: We densely sample the objective plane ⟨csys , m sys ⟩, test nonlin-ear feasibility of (14)–(16), and retain nondominated feasible samples. This produces a high-resolution reference frontier used for error reporting. b) Method II: MCDPL : We solve the same query with MCDPL at resolutions R ∈ { 5, 10 , 20 , 40 , 60 , 80 }, obtaining optimistic and pessimistic Pareto approximations. 

c) Method III: tangent-plane outer approximation + monolithic MOLP: For the fixed query, the required chassis power is 

preq (mb) = p0 + κ ms + mpay + mb

 vreq . (17) Combining (15) with pch ≤ pbat yields 

c ≥ ϕ(mb) def 

= preq (mb) − αm b

β + γm b

. (18) Writing ϕ(mb) = ( a + bm b)/(β + γm b) with a = p0 +

κ(ms + mpay )vreq and b = κ v req − α, one verifies that 

ϕ′′ (mb) = 2γ γa − βb 

(β + γm b)3 > 0

for the parameter values; hence ϕ is convex on mb ≥ 0. We select anchor points {m(i)b }Ni=1 and tangent cuts 

c ≥ ϕ(m(i)b ) + ϕ′(m(i)b )( mb − m(i)b ), i = 1 , . . . , N. 

(19) Because tangents under-estimate a convex function, (19) defines a polyhedral outer approximation of the feasible set. Together with the linear chassis and wiring constraints, this yields an N -dependent LCDP solved via the monolithic MOLP pipeline. 

d) Method IV: direct cutting-plane refinement in re-source space: Starting from a coarse outer polyhedron in 

⟨csys , m sys ⟩, we iteratively add supporting half-space cuts at boundary points and re-solve the resulting MOLP. This produces a sequence of polyhedral outer approximations in resource space. 

3) Metrics: We report both efficiency and accuracy. Effi-ciency is measured by end-to-end runtime and, for Methods III–IV, the number of inequalities in the polyhedral surrogate. Accuracy is measured against the reference frontier using directional one-sided excesses on a fixed normalization box induced by the reference frontier. For outer approximations, we report e[P, C ] and e[C, P ]. For inner approximations, the primary directional metric is e[C, P −] (coverage of the reference by the inner front), rather than e[P −, C ]. To reduce discretization-induced oscillations and obtain a scale-aware indicator for inner fronts, we additionally report the normalized hypervolume deficit 

∆HV −

> rel
> def

= HV (C) − HV (P −)

HV (C) .

For optimistic fronts we report the dual hypervolume excess 

∆HV +rel = ( HV (P +)−HV (C)) /HV (C). For Methods III– IV, the recession-cone discrepancy is δ = 0 by Theorem 10. We also report the maximum and mean mass gap (kg) for physical interpretability. 

4) Results: Figures 6 and 7 and Table IV summarize the rover results. Both LDP-based outer-approximation pipelines improve monotonically as the number of cuts grows, consis-tent with the ⟨ε, 0⟩ theory of Section IV. For Method III (tangent-plane surrogate), at N = 200 we obtain e[P, C ] = 2.36 × 10 −4, e[C, P ] = 5 .37 × 10 −3, ∆HV − 

> rel

= 3 .75 × 10 −3

with end-to-end runtime 2.53 ×10 −2 s. Method IV reaches a comparable vertex excess at N = 200 (e[P, C ] = 2 .37 ×

10 −4), but has larger coverage excess ( e[C, P ] = 1 .33 ×

10 −2) and is much slower ( 3.38 × 10 1 s) because each refinement step requires an additional MOLP solve. Under the new inner-approximation metrics, MCDPL pes-simistic fronts now exhibit the expected smooth directional improvement: e[C, P −] decreases from 3.74 ×10 −1 at R = 5 

to 1.13 × 10 −1 at R = 80 , and ∆HV − 

> rel

decreases from 

4.93 × 10 −1 to 1.54 × 10 −1. However, even at the best tested MCDPL setting ( R = 80 ), the inner-front errors remain far above Method III while requiring 2.45 × 10 3 s of solve time. Thus, the LDP outer-approximation pipeline still offers the strongest accuracy–time trade-off by a wide margin. 

D. Discussion 

The benchmarks separate the exact and approximate regimes of the framework. For polyhedral LCDPs, the mono-lithic lifted formulation is best suited to single-shot queries: it preserves sparsity, avoids projection, and computes exact Pareto frontiers quickly. Compositional elimination is also exact, but its cost is driven by intermediate projection and redundancy removal, making it most useful when a reduced model is needed for repeated use. For convex but nonpoly-hedral models, polyhedral outer approximations extend the same MOLP-based pipeline beyond the exact linear regime. The rover benchmark shows that directional inner-front met-rics give a clearer view of pessimistic convergence, while accurate (ε, 0) outer approximations remain computationally cheap. VI. C ONCLUSIONS AND FUTURE WORK 

This paper introduced LDPs, a polyhedral subclass of monotone co-design problems whose feasible functionality– resource relations are described by linear inequalities over Euclidean posets. We showed that LDPs are closed under classic co-design interconnections, so any LCDP built from linear components induces a system-level LDP whose queries reduce exactly to MOLPs/VLPs, yielding a bridge between monotone co-design and polyhedral multiobjective optimiza-tion. We then developed two exact computational pipelines: a 

monolithic formulation that preserves lifted block-angular structure and is effective for single-shot queries, and a

compositional formulation that eliminates internal variables to produce a reduced external-coordinate model. Beyond the exact linear case, we showed that convex co-design resource queries admit arbitrarily accurate polyhedral outer approximations; for standard nonnegative resource cones, the recession-cone error is structurally exact ( δ = 0 ), so Fig. 6: Pareto-front comparison for the rover case study. (a) LDP tangent-plane approximation at several N vs. ground truth; (b) zoomed view highlighting convergence in the cost range 1000–1200 USD; (c) MCDPL optimistic approximations at resolutions R ∈ { 5, 10 , 20 , 40 , 60 , 80 }; (d) best available result from each method compared against the ground-truth frontier. 

Fig. 7: Convergence and efficiency comparison. (a) Directional excess metrics versus precision parameter: e[P, C ] and e[C, P] for LDP, e[P+, C ] for MCDPL optimistic, and e[C, P−] for MCDPL pessimistic; (b) the same directional excess metrics plotted against computation time, showing that LDP achieves markedly lower error at a fraction of the MCDPL runtime. TABLE IV: Rover case study: accuracy–time comparison across methods. For Methods III–IV, δ = 0 holds by Theorem 10. The column ∆HV rel reports a one-sided normalized hypervolume gap with respect to the reference frontier C: hypervolume deficit (HV (C) − HV (P −)) /HV (C) for inner fronts and hypervolume excess (HV (P +) − HV (C)) /HV (C) for optimistic fronts. For MCDPL pessimistic rows, the coverage column corresponds to e[C, P−].Method Parameter nineq Pts e[P, C ] e[C, P] ∆HV rel Max gap (kg) Mean gap (kg) Time (s) Tangent-plane linearization (Method III) 

N = 2 11 4 3.83 × 10 −2 3.44 × 10 −1 2.69 × 10 −1 2.53 7.59 × 10 −1 2.65 × 10 −2

N = 5 14 7 7.47 × 10 −3 1.71 × 10 −1 1.40 × 10 −1 4.59 × 10 −1 1.30 × 10 −1 7.44 × 10 −3

N = 10 19 12 2.05 × 10 −3 9.57 × 10 −2 7.57 × 10 −2 1.22 × 10 −1 3.60 × 10 −2 7.43 × 10 −3

N = 20 29 22 5.84 × 10 −4 5.09 × 10 −2 3.91 × 10 −2 3.40 × 10 −2 1.23 × 10 −2 7.85 × 10 −3

N = 50 59 52 2.52 × 10 −4 2.12 × 10 −2 1.58 × 10 −2 1.30 × 10 −2 5.67 × 10 −3 1.00 × 10 −2

N = 100 109 102 2.38 × 10 −4 1.07 × 10 −2 7.80 × 10 −3 1.06 × 10 −2 4.72 × 10 −3 1.49 × 10 −2

N = 200 209 202 2.36 × 10 −4 5.37 × 10 −3 3.75 × 10 −3 1.06 × 10 −2 4.48 × 10 −3 2.53 × 10 −2

Convex approx. + MOLP (Method IV) 

N = 2 11 4 6.37 × 10 −2 3.81 × 10 −1 — 3.86 8.65 × 10 −1 1.54 × 10 −2

N = 5 14 7 1.05 × 10 −2 2.07 × 10 −1 — 6.95 × 10 −1 1.53 × 10 −1 1.90 × 10 −2

N = 10 19 12 1.05 × 10 −2 1.55 × 10 −1 — 6.94 × 10 −1 9.92 × 10 −2 5.27 × 10 −2

N = 20 29 22 7.95 × 10 −4 5.72 × 10 −2 — 5.25 × 10 −2 1.37 × 10 −2 1.89 × 10 −1

N = 50 59 52 2.73 × 10 −4 2.84 × 10 −2 — 1.75 × 10 −2 6.22 × 10 −3 1.26 × 10 0

N = 100 109 102 2.73 × 10 −4 2.64 × 10 −2 — 1.69 × 10 −2 5.06 × 10 −3 6.33 × 10 0

N = 200 209 201 2.37 × 10 −4 1.33 × 10 −2 — 1.06 × 10 −2 4.55 × 10 −3 3.38 × 10 1

MCDPL (optimistic) 

R = 5 — 3 5.02 × 10 −1 7.60 × 10 −1 5.90 × 10 −1 4.10 × 10 1 3.82 × 10 1 8.94 × 10 0

R = 10 — 4 4.48 × 10 −1 7.60 × 10 −1 5.59 × 10 −1 3.98 × 10 1 3.13 × 10 1 1.13 × 10 1

R = 20 — 4 2.33 × 10 −1 4.51 × 10 −1 3.01 × 10 −1 2.36 × 10 1 1.66 × 10 1 2.30 × 10 1

R = 40 — 7 2.33 × 10 −1 4.51 × 10 −1 2.76 × 10 −1 2.36 × 10 1 1.33 × 10 1 9.15 × 10 1

R = 60 — 7 2.33 × 10 −1 4.51 × 10 −1 2.58 × 10 −1 2.36 × 10 1 1.23 × 10 1 2.82 × 10 2

R = 80 — 14 1.30 × 10 −1 2.44 × 10 −1 1.65 × 10 −1 1.28 × 10 1 7.10 × 10 0 2.45 × 10 3

MCDPL (pessimistic) 

R = 5 — 4 1.18 × 10 −1 3.74 × 10 −1 4.93 × 10 −1 8.73 5.78 8.94 × 10 0

R = 10 — 6 1.55 × 10 −1 2.11 × 10 −1 3.72 × 10 −1 1.03 × 10 1 6.20 1.13 × 10 1

R = 20 — 9 1.55 × 10 −1 2.11 × 10 −1 3.49 × 10 −1 1.03 × 10 1 6.20 2.30 × 10 1

R = 40 — 8 1.18 × 10 −1 1.91 × 10 −1 3.11 × 10 −1 8.73 5.78 9.15 × 10 1

R = 60 — 15 9.35 × 10 −2 1.13 × 10 −1 1.77 × 10 −1 7.96 3.51 2.82 × 10 2

R = 80 — 14 9.35 × 10 −2 1.13 × 10 −1 1.54 × 10 −1 7.96 2.65 2.45 × 10 3approximation quality is controlled entirely by the bounded-part error ε.The numerical results validate both regimes. On syn-thetic series-chain LCDPs, the monolithic pipeline is fastest, while the compositional pipeline preserves the exact Pareto frontier with lower final query dimension. On the rigid gripper benchmark, the exact LCDP model computes the true frontier with zero approximation error and large runtime gains over MCDPL. On the rover benchmark, tangent-plane outer approximations produce accurate (ε, 0) surro-gates and a substantially better accuracy–time trade-off than the discretization-based baseline. Overall, linear co-design emerges as both a rigorous subclass of monotone co-design and a practical backbone for exact and approximate Pareto-set computation. Future work includes extending the theory to broader con-vex design classes, exploiting the monolithic block-angular structure through decomposition-based multiobjective algo-rithms, integrating uncertainty-aware co-design [24], [25], and studying richer interconnection topologies and hybrid monolithic/compositional schemes. REFERENCES [1] G. Zardini, “Co-design of complex systems: From autonomy to future mobility systems,” Ph.D. dissertation, ETH Zurich, 2023. [2] J. R. R. A. Martins and A. B. Lambe, “Multidisciplinary design optimization: A survey of architectures,” AIAA Journal , vol. 51, no. 9, pp. 2049–2075, Sep. 2013. [3] S. A. Seshia, S. Hu, W. Li, and Q. Zhu, “Design automation of cyber-physical systems: Challenges, advances, and opportunities,” IEEE Transactions on Computer-Aided Design of Integrated Circuits and Systems , vol. 36, no. 9, pp. 1421–1434, 2017. [4] A. Censi, J. Lorand, and G. Zardini, Applied Compositional Thinking for Engineering , 2024, work-in-progress book. [Online]. Available: https://bit.ly/3qQNrdR [5] A. Censi, “A mathematical theory of co-design,” arXiv preprint arXiv:1512.08055 , 2015. [6] G. Zardini, A. Censi, and E. Frazzoli, “Co-design of autonomous systems: From hardware selection to control synthesis,” in 2021 European Control Conference (ECC) , 2021, pp. 682–689. [7] G. Zardini, Z. Suter, A. Censi, and E. Frazzoli, “Task-driven modular co-design of vehicle control systems,” in 2022 IEEE 61st Conference on Decision and Control (CDC) . IEEE, 2022, pp. 2196–2203. [8] D. Milojevic, G. Zardini, M. Elser, A. Censi, and E. Frazzoli, “Codei: Resource-efficient task-driven co-design of perception and decision making for mobile robots applied to autonomous vehicles,” IEEE Transactions on Robotics , vol. 41, pp. 2727–2748, 2025. [9] M.-P. Neumann, G. Fieni, F. Furia, A. Cerofolini, V. Ravaglioli, C. H. Onder, and G. Zardini, “Strategic co-design in formula 1: Balancing physical configuration and race tactics,” 2024. [10] G. Zardini, N. Lanzetti, A. Censi, E. Frazzoli, and M. Pavone, “Co-design to enable user-friendly tools to assess the impact of future mobility solutions,” IEEE Transactions on Network Science and Engineering , vol. 10, no. 2, pp. 827–844, 2022. [11] P. Karkus, B. Ivanovic, S. Mannor, and M. Pavone, “DiffStack: A differentiable and modular control stack for autonomous vehicles,” in 

Conference on robot learning . PMLR, 2023, pp. 2170–2180. [12] A. Censi, “Uncertainty in monotone codesign problems,” IEEE Robotics and Automation Letters , vol. 2, no. 3, pp. 1556–1563, 2017. [13] M. Ehrgott, Multicriteria optimization . Springer, 2005. [14] A. L¨ ohne, Vector optimization with infimum and supremum . Springer Science & Business Media, 2011. [15] A. L¨ ohne and B. Weißing, “Equivalence between polyhedral projec-tion, multiple objective linear programming and vector linear program-ming,” Mathematical Methods of Operations Research , vol. 84, no. 2, pp. 411–426, Oct. 2016. [16] ——, “The vector linear program solver Bensolve – notes on theo-retical background,” European Journal of Operational Research , vol. 260, no. 3, pp. 807–813, Aug. 2017. [17] H. P. Benson, “An outer approximation algorithm for generating all efficient extreme points in the outcome set of a multiple objective linear programming problem,” Journal of Global Optimization , vol. 13, no. 1, pp. 1–24, 1998. [18] K. Fukuda, “Lecture: Polyhedral computation, spring 2016,” Institute for Operations Research and Institute of Theoretical Computer Science, ETH Zurich. https://inf. ethz. ch/personal/fukudak/lect/pclect/notes2015/PolyComp2015. pdf , 2016. [19] D. Ciripoi, A. L¨ ohne, and B. Weißing, “Calculus of convex polyhedra and polyhedral convex functions by utilizing a multiple objective linear programming solver,” Optimization , 2019. [20] F. Vanderbeck and M. W. Savelsbergh, “A generic view of Dantzig– Wolfe decomposition in mixed integer programming,” Operations Research Letters , vol. 34, no. 3, pp. 296–306, 2006. [21] H. P. Benson, “Further analysis of an outcome set-based algorithm for multiple-objective linear programming,” Journal of Optimization Theory and Applications , vol. 97, no. 1, pp. 1–10, 1998. [22] D. D¨ orfler, “On the approximation of unbounded convex sets by polyhedra,” Journal of Optimization Theory and Applications , vol. 194, pp. 265–287, 2022. [23] S. M. LaValle, Planning algorithms . Cambridge university press, 2006. [24] Y. Huang, M. Furter, and G. Zardini, “On composable and parametric uncertainty in systems Co-Design,” in 2025 IEEE 64th Conference on Decision and Control (CDC) . IEEE, 2025, pp. 3049–3056. [25] Y. Huang and G. Zardini, “Distributional uncertainty and adaptive decision-making in system co-design,” arXiv preprint arXiv:2603.14047 , 2026.