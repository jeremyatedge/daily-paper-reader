---
title: "OneComp: One-Line Revolution for Generative AI Model Compression"
title_zh: OneComp：生成式人工智能模型压缩的一行代码革命
authors: "Yuma Ichikawa, Keiji Kimura, Akihiro Yoshida, Yudai Fujimoto, Hiroki Tokura, Yamato Arai, Yoshiyuki Ishii, Yusei Kawakami, Genki Shikada, Achille Jacquemond, Yoshihiko Fujisawa, Katsuki Fujisawa, Takumi Honda, Akira Sakai"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28845v1"
tags: ["query:aiot"]
score: 8.0
evidence: 生成式AI模型压缩的资源自适应流水线
tldr: 针对生成式AI模型部署面临的内存和延迟挑战，本文推出了开源压缩框架OneComp。该框架将复杂的模型压缩流程简化为自动化、资源自适应的流水线，支持从层级到全局的渐进式量化。其核心贡献在于通过混合精度分配和硬件感知策略，将前沿压缩算法转化为生产级工具，确保模型质量随计算资源投入而提升，有效填补了算法创新与实际部署之间的空白。
selection_source: fresh_fetch
motivation: 现有的生成式AI模型压缩流程因算法碎片化和硬件依赖性而极其复杂，阻碍了高性能模型的快速部署。
method: 开发了OneComp开源框架，通过自动模型检查、混合精度规划及从层到全局的渐进式量化阶段实现高效压缩。
result: 该框架成功将专家级压缩工作流转化为可复现的自动化流水线，并利用“部署枢轴”策略确保压缩质量的持续优化。
conclusion: OneComp通过提供可扩展且硬件感知的压缩方案，显著降低了生成式AI模型从研究到生产部署的门槛。
---

## 摘要
基础模型的部署日益受到内存占用、延迟和硬件成本的限制。训练后压缩可以通过降低模型参数的精度来缓解这些瓶颈，且不会显著降低性能；然而，由于从业者需要应对量化算法、精度预算、数据驱动的校准策略以及依赖硬件的执行方案等碎片化现状，其实际应用仍然具有挑战性。我们提出了 OneComp，这是一个开源压缩框架，它将这种专家级工作流转化为可复现且资源自适应的流水线。给定模型标识符和可用硬件，OneComp 会自动检查模型，规划混合精度分配，并执行从层级压缩到块级细化再到全局细化的渐进式量化阶段。一个关键的架构选择是将第一个量化检查点视为可部署的基准点，确保后续的每个阶段都在改进同一个模型，并且随着计算投入的增加，模型质量也随之提升。通过将最先进的压缩研究转化为可扩展、开源且感知硬件的流水线，OneComp 弥合了算法创新与生产级模型部署之间的鸿沟。

## Abstract
Deploying foundation models is increasingly constrained by memory footprint, latency, and hardware costs. Post-training compression can mitigate these bottlenecks by reducing the precision of model parameters without significantly degrading performance; however, its practical implementation remains challenging as practitioners navigate a fragmented landscape of quantization algorithms, precision budgets, data-driven calibration strategies, and hardware-dependent execution regimes. We present OneComp, an open-source compression framework that transforms this expert workflow into a reproducible, resource-adaptive pipeline. Given a model identifier and available hardware, OneComp automatically inspects the model, plans mixed-precision assignments, and executes progressive quantization stages, ranging from layer-wise compression to block-wise refinement and global refinement. A key architectural choice is treating the first quantized checkpoint as a deployable pivot, ensuring that each subsequent stage improves the same model and that quality increases as more compute is invested. By converting state-of-the-art compression research into an extensible, open-source, hardware-aware pipeline, OneComp bridges the gap between algorithmic innovation and production-grade model deployment.

---

## 论文详细总结（自动生成）

以下是对论文《OneComp: One-Line Revolution for Generative AI Model Compression》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）部署面临巨大的内存占用、高延迟和高硬件成本。虽然训练后量化（PTQ）是有效的解决方案，但现有的量化算法极其碎片化（如 GPTQ, AWQ, SmoothQuant 等），且精度预算分配、校准策略和硬件依赖性复杂，导致非专家用户难以在特定硬件预算下获得最优的压缩模型。
*   **研究动机**：旨在打破算法创新与生产部署之间的鸿沟，通过一个“一行代码”式的开源框架，实现自动化的、资源自适应的模型压缩流水线。

### 2. 方法论与关键技术
OneComp 的核心思想是将量化过程视为一个**渐进式细化流水线**，主要包含以下关键技术：
*   **AutoBit（自动混合精度规划）**：利用整数线性规划（ILP）在给定的显存（VRAM）预算下，自动为不同层分配最优比特数。它通过激活感知的二阶泰勒展开来估计各层的量化敏感度。
*   **等效变换预处理**：集成 SmoothQuant（通道缩放）和 SpinQuant（学习旋转矩阵），通过数学等效变换重新分布权重和激活值中的离群值（Outliers），降低量化难度。
*   **层级 PTQ 优化（Layer-wise）**：
    *   **QEP（量化误差传播）**：通过修正当前层的量化目标来补偿前序层累积的误差。
    *   **LPCD（子模块感知坐标下降）**：将优化范围从单层扩展到功能耦合的子模块（如 Q/K 投影对），进行联合优化。
*   **JointQ**：针对 3-4 bit 制度，通过局部搜索算法联合优化量化缩放因子（Scales）和整数权重，优于传统的交替优化。
*   **MDBF（多包络双二进制分解）**：针对 1-2 bit 极端量化，采用结构化二进制因子格式，通过多秩量级包络提升表达能力。
*   **资源自适应流水线**：根据可用算力，模型从层级量化（Layer-wise）起步，逐步进阶到块级细化（Block-wise，基于 MSE 蒸馏）和全局细化（Global，基于 KL 散度蒸馏）。

### 3. 实验设计
*   **数据集/场景**：
    *   **校准集**：使用 C4 或 WikiText-2 数据集（通常为 512-1024 个样本）。
    *   **评估指标**：WikiText-2 的困惑度（Perplexity）；下游任务零样本（Zero-shot）准确率（包括 ARC-Challenge/Easy, PIQA, WinoGrande）。
*   **Benchmark 模型**：Llama-3 (8B, 70B), Qwen3 (8B, 14B), Llama-3.2-1B, TinyLlama-1.1B 等。
*   **对比方法**：标准 GPTQ、均匀比特分配（Uniform）、DBF（双二进制分解）等主流基准。

### 4. 资源与算力
*   **算力需求**：论文强调了“资源自适应性”。
    *   **层级量化**：可在单块消费级 GPU 上完成（仅需加载单层权重和 Hessian 统计量）。
    *   **块级/全局量化**：需要更多显存以同时容纳教师模型和学生模型。
*   **训练时长**：文中提到块级补偿通常进行 5 个 Epoch，块级蒸馏使用 Adam 优化器配合余弦退火。具体总耗时未给出统一表格，但指出层级量化在分钟到小时级别。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 70B 不同参数规模的模型，测试了从 1.0 bit 到 4.0 bit 的多种压缩率。
*   **消融实验**：针对 AutoBit 的分配策略、QEP/LPCD 的修正效果、JointQ 的优化增益均做了专项对比。
*   **充分性评价**：实验设计较为全面，不仅验证了语言建模能力（PPL），还验证了实际任务表现（ACC），且跨越了多个模型家族，客观展示了在极端低比特下的性能衰减与恢复过程。

### 6. 主要结论与发现
*   **混合精度优于均匀精度**：AutoBit 自动分配比特能显著提升 Llama-3 等敏感模型的表现。
*   **误差补偿至关重要**：QEP 和 LPCD 能有效缓解层级量化中的误差累积，使单 GPU 量化效果逼近昂贵的全局优化。
*   **单调提升特性**：随着投入的计算资源增加（从层级到块级再到全局），模型质量呈现明显的单调上升趋势。
*   **极端量化可行性**：通过 MDBF 格式，模型在 1.5 bit 左右仍能保持相当比例的下游任务准确率。

### 7. 优点与亮点
*   **易用性**：将复杂的量化专家知识封装进“一行代码”接口，极大降低了部署门槛。
*   **灵活性**：支持从 1 bit 到 8 bit 的全频谱量化，且兼容 QLoRA 微调。
*   **理论创新**：提出的 QEP 和 LPCD 算法在不增加硬件负担的前提下，显著提升了层级 PTQ 的精度上限。
*   **开源贡献**：提供了一个可扩展的框架，允许研究者集成新的量化算子或细化器。

### 8. 不足与局限
*   **功能完整性**：部分高级功能（如全局 PTQ 细化、MDBF 格式）在论文发表时标注为“计划在未来版本发布”，当前版本的完整能力尚待验证。
*   **量化对象限制**：目前核心研究集中在**权重（Weight-only）量化**，虽然提及了激活值和 KV 缓存量化，但实验部分对此覆盖较少。
*   **校准集依赖**：PTQ 效果高度依赖校准数据的质量和分布，对于特定垂直领域的泛化能力未做深入探讨。

（完）
