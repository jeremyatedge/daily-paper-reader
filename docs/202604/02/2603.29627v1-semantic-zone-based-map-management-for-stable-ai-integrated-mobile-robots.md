---
title: Semantic Zone-Based Map Management for Stable AI-Integrated Mobile Robots
title_zh: 面向稳定集成人工智能移动机器人的基于语义区域的地图管理
authors: "Huichang Yun, Seungho Yoo"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29627v1"
tags: ["query:aiot"]
score: 8.0
evidence: 严格内存预算下边缘机器人的地图管理
tldr: 针对边缘机器人内存受限下难以同时运行大模型和稠密地图的问题，本文提出一种基于语义区域的地图管理方法。该方法将关键帧与室内语义区域（如房间、走廊）关联，在内存限制下优先调度空间相关的地图内容。实验表明，该方法在NVIDIA Jetson Orin Nano上显著提升了VLM运行效率，减少了延迟并消除了内存溢出风险，确保了定位稳定性和AI任务的鲁棒性。
selection_source: fresh_fetch
motivation: 在内存受限的边缘设备上，同时部署大型AI模型和稠密3D地图会导致关键帧加载延迟，进而影响定位稳定性和模型性能。
method: 提出一种基于语义区域的关键帧管理策略，通过将地图划分为房间或走廊等语义区，按区域优先级进行内存调度以减少加载频率。
result: "在Jetson Orin Nano上配合Qwen3.5测试，吞吐量提升3.3 tokens/s，延迟降低21.7%，并成功消除了内存溢出和执行停滞问题。"
conclusion: 该语义区域管理方法有效解决了内存受限机器人中稠密地图与AI模型共存的挑战，显著提升了系统的稳定性和交互效率。
---

## 摘要
大型人工智能模型（如 VLM 和 LLM）的最新进展以及 3D 稠密地图的联合应用，使移动机器人能够基于丰富的空间上下文提供更强大且更具交互性的服务。然而，在严格的内存预算下，在边缘机器人上同时部署大型 AI 模型和稠密地图面临巨大挑战。当超出内存预算时，所需的关键帧可能无法及时加载，从而降低位置估计的稳定性并干扰模型性能。我们提出了一种基于语义区域的地图管理方法，旨在内存受限的情况下稳定稠密地图的利用。我们将关键帧与室内语义区域（如房间和走廊）相关联，并在语义区域级别进行关键帧管理，在遵守内存限制的同时优先处理空间相关的地图内容。这降低了关键帧的加载与卸载频率以及内存占用。我们在大规模模拟室内环境以及 NVIDIA Jetson Orin Nano 平台上，在 SLAM 与 VLM 并行执行的场景下对该方法进行了评估。实验结果显示，在使用 Qwen3.5:0.8b 模型时，与几何地图管理策略相比，该方法将吞吐量提高了 3.3 tokens/s，延迟降低了 21.7%。此外，几何策略在内存压力下会出现内存溢出故障和执行停滞，而本方法消除了这些问题，保持了定位稳定性并确保了 VLM 的鲁棒运行。这些结果证明，该方法能够为内存受限的集成 AI 移动机器人实现高效的稠密地图利用。代码开源地址：https://github.com/huichangs/rtabmap/tree/segment。

## Abstract
Recent advances in large AI models (VLMs and LLMs) and joint use of the 3D dense maps, enable mobile robots to provide more powerful and interactive services grounded in rich spatial context. However, deploying both heavy AI models and dense maps on edge robots is challenging under strict memory budgets. When the memory budget is exceeded, required keyframes may not be loaded in time, which can degrade the stability of position estimation and interfering model performance. We proposes a semantic zone-based map management approach to stabilize dense-map utilization under memory constraints. We associate keyframes with semantic indoor regions (e.g., rooms and corridors) and keyframe management at the semantic zone level prioritizes spatially relevant map content while respecting memory constraints. This reduces keyframe loading and unloading frequency and memory usage. We evaluate the proposed approach in large-scale simulated indoor environments and on an NVIDIA Jetson Orin Nano under concurrent SLAM-VLM execution. With Qwen3.5:0.8b, the proposed method improves throughput by 3.3 tokens/s and reduces latency by 21.7% relative to a geometric map-management strategy. Furthermore, while the geometric strategy suffers from out-of-memory failures and stalled execution under memory pressure, the proposed method eliminates both issues, preserving localization stability and enabling robust VLM operation. These results demonstrate that the proposed approach enables efficient dense map utilization for memory constrained, AI-integrated mobile robots. Code is available at: https://github.com/huichangs/rtabmap/tree/segment

---

## 论文详细总结（自动生成）

### 论文总结：面向稳定集成人工智能移动机器人的基于语义区域的地图管理

#### 1. 核心问题与研究动机
*   **研究背景**：现代移动机器人正集成大型 AI 模型（如 VLM 和 LLM）与 3D 稠密地图，以实现更高级的空间理解和交互服务。
*   **核心问题**：边缘机器人（如 Jetson 系列）内存资源极其有限。稠密地图的关键帧（Keyframes）与庞大的 AI 模型会激烈竞争内存。
*   **动机**：传统的地图管理策略（基于几何邻近或时间先后）在内存压力下会导致关键帧加载延迟或失败，进而引发回环检测失效、定位漂移，甚至系统因内存溢出（OOM）而崩溃。

#### 2. 方法论
*   **核心思想**：将地图管理单元从“单个关键帧”提升到“语义区域（Semantic Zone）”级别。利用机器人运动的连续性和室内环境的结构化特征（如房间、走廊），按需批量调度地图数据。
*   **关键技术细节**：
    *   **语义分区**：将环境划分为有限的语义区域集合 $Z = \{z_1, z_2, \dots, z_K\}$，并将每个关键帧关联至特定区域。
    *   **语义区域管理器**：根据机器人当前位姿确定“当前活跃区域”，并预测即将进入的区域。
    *   **内存管理器**：设定严格的关键帧数量阈值（$MemoryThr$）。在加载新区域前，先计算预计总数，若超标则按“最近最少使用（LRU）”原则批量卸载非活跃区域的关键帧。
    *   **批量操作**：通过区域级的批量加载/卸载，减少数据库事务频率，降低系统开销。

#### 3. 实验设计
*   **实验场景**：使用 NVIDIA Isaac Sim 构建的大规模室内医院模拟环境。
*   **基准方法（Benchmark）**：RTAB-Map 默认的基于几何邻近的关键帧检索策略（标记为 Basic）。
*   **对比配置**：
    *   Basic vs. Semantic（本文方法）。
    *   独立运行 SLAM vs. SLAM 与 VLM 并行运行。
*   **测试模型**：Qwen3-vl (2B/4B), Qwen3.5 (0.8B/2B), Gemma3 (4B)。
*   **评估指标**：内存占用曲线、更新频率（FPS）、回环检测数量、绝对轨迹误差（ATE RMSE）、VLM 吞吐量（TPS）及延迟。

#### 4. 资源与算力
*   **硬件平台**：NVIDIA Jetson Orin Nano，具备 8GB 统一内存（典型的受限边缘设备）。
*   **算力使用**：实验侧重于边缘端的推理与实时调度，未提及模型训练时长。所有实验均在同一台 Jetson 设备上完成 SLAM 与 VLM 的并发执行。

#### 5. 实验数量与充分性
*   **实验规模**：在医院场景下进行了多组对比实验，涵盖了不同参数规模的 VLM 模型（从 0.8B 到 4B）。
*   **充分性评价**：实验设计较为充分，不仅对比了定位精度（SLAM 视角），还详细分析了 AI 模型的推理性能（VLM 视角）以及系统级的稳定性（内存压力测试）。通过轨迹图和内存曲线直观展示了改进效果，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **消除系统故障**：在 Basic 策略下，并发运行 VLM 频繁导致 OOM 崩溃或执行停滞；Semantic 策略成功消除了这些问题，确保了全流程稳定运行。
*   **提升 SLAM 稳定性**：在并发压力下，Semantic 策略的定位误差（ATE RMSE）比 Basic 策略降低了约 39%，回环检测成功率显著提高。
*   **优化 AI 性能**：配合 Qwen3.5:0.8b 使用时，VLM 吞吐量提升了 3.3 tokens/s，端到端延迟降低了 21.7%。
*   **内存可控性**：通过区域级调度，内存使用更加平滑，避免了频繁的细粒度内存分配带来的开销。

#### 7. 优点
*   **实用性强**：直接针对边缘机器人部署大模型这一痛点，提供了系统级的解决方案。
*   **逻辑合理**：利用语义信息（房间/走廊）进行地图调度符合机器人实际作业逻辑，比单纯的几何距离更具预测性。
*   **开源贡献**：提供了基于 RTAB-Map 的代码实现，便于社区复现和改进。

#### 8. 不足与局限
*   **人工依赖**：目前的语义区域划分仍需人工预定义，缺乏自动化的空间分割机制。
*   **策略单一**：区域卸载仅采用了简单的 LRU 策略，未考虑任务优先级或复杂的路径预测。
*   **场景局限**：主要在模拟医院环境中测试，在高度动态或非结构化（如野外）环境中的表现尚待验证。
*   **扩展性**：当单个语义区域本身极大（超过内存阈值）时，该方法的有效性可能会受限。

（完）
