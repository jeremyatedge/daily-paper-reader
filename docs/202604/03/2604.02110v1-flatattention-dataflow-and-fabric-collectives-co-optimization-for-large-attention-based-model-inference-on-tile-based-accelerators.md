---
title: "FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators"
title_zh: FlatAttention：基于瓦片加速器的大型注意力模型推理数据流与织构集合通信协同优化
authors: "Chi Zhang, Luca Colagrande, Renzo Andri, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02110v1"
tags: ["query:aiot"]
score: 8.0
evidence: 针对瓦片式加速器模型推理的数据流与架构协同优化
tldr: 针对大模型（如MoE）推理中Attention计算占比增加的问题，本文提出FlatAttention，一种专为瓦片式（Tile-based）加速器设计的优化数据流。该方法通过将集合通信原语集成到片上网络中，显著减少了高带宽内存（HBM）访问，在多种Attention变体上实现了极高的硬件利用率，相比FlashAttention-3和GH200在性能和带宽利用上均有显著提升。
selection_source: fresh_fetch
motivation: 大模型推理中Attention计算占比日益增加，且传统架构在处理瓦片间通信时面临严重的HBM带宽瓶颈。
method: 提出FlatAttention数据流，利用片上网络集成的集合通信原语优化瓦片间数据交换，从而最小化昂贵的HBM访问。
result: "实验显示其利用率高达92.3%，比FlashAttention-3快4.1倍，并在DeepSeek-v3推理中实现1.9倍吞吐量提升。"
conclusion: FlatAttention通过数据流与片上通信的协同优化，有效解决了瓦片式加速器在大模型推理中的存储访问瓶颈。
---

## 摘要
在混合专家（MoE）模型的推理过程中，注意力机制在总计算量中所占的比例日益增大，使得高效加速变得至关重要。用于大模型推理的新兴领域专用加速器正转向芯片级和晶圆级基于瓦片（tile-based）的架构。瓦片包含大型矩阵和向量引擎，并通过片上互连进行连接，支持瓦片间通信以缓解瓦片到主存之间的流量瓶颈。因此，数据流管理对于实现高利用率至关重要。我们提出了 FlatAttention，这是一种适用于基于瓦片加速器的现代注意力变体的数据流。FlatAttention 通过利用集成在片上网络织构（fabric）中的集合原语，最大限度地减少了昂贵的高带宽内存（HBM）访问，实现了高达 92.3% 的利用率，相比 FlashAttention-3 提速 4.1 倍，并降低了 16 倍的 HBM 流量。在峰值性能与 NVIDIA GH200 相当的 32x32 瓦片配置下，FlatAttention 可推广至多种注意力变体，在计算受限型注意力中实现了平均 86% 的利用率，在内存受限型中实现了 78% 的 HBM 带宽利用率，相比 GH200 上的注意力实现平均提速 1.9 倍。最后，我们在晶圆级多芯片系统上评估了使用 FlatAttention 的端到端 DeepSeek-v3 FP8 解码，尽管系统峰值性能比现有最先进方案低 1.5 倍，但仍实现了 1.9 倍的系统吞吐量提升和 1.4 倍的单用户 Token 输出延迟降低。

## Abstract
Attention accounts for an increasingly dominant fraction of total computation during inference for mixture-of-experts (MoE) models, making efficient acceleration critical. Emerging domain-specific accelerators for large model inference are shifting toward chip-scale and wafer-scale tile-based architectures. Tiles contain large matrix and vector engines and are connected through on-chip interconnects, which support tile-to-tile traffic to reduce the tile-to-main-memory traffic bottleneck. Hence, dataflow management is crucial to achieve high utilization. We propose FlatAttention, a dataflow for modern attention variants on tile-based accelerators. FlatAttention minimizes expensive high-bandwidth memory (HBM) accesses by exploiting collective primitives integrated into the on-chip network fabric, achieving up to 92.3% utilization, 4.1x speedup over FlashAttention-3, and 16x lower HBM traffic. On a 32x32 tile configuration with peak performance comparable to NVIDIA GH200, FlatAttention generalizes across multiple attention variants, achieving an average of 86% utilization for compute-bound attentions and 78% HBM bandwidth utilization for memory-bound ones, resulting in an average 1.9x speedup over attention implementations on GH200. Finally, we evaluate end-to-end DeepSeek-v3 FP8 decoding with FlatAttention on a wafer-scale multi-die system, achieving a 1.9x improvement in system throughput and a 1.4x reduction in per-user token output latency, despite operating with 1.5x lower peak system performance compared to the state-of-the-art solution.

---

## 论文详细总结（自动生成）

以下是对论文《FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators》的结构化深入总结：

### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）从传统的解码器架构转向混合专家模型（MoE，如 DeepSeek-v3），注意力机制（Attention）在推理计算中的占比显著增加（在长文本解码中可达 71%）。然而，现有的 GPU 架构在处理这些计算时，硬件利用率往往仅为峰值的 26%-64%。
**核心问题**：新兴的“瓦片式”（Tile-based）加速器（如晶圆级芯片）虽然具有极高的峰值算力，但面临严重的 HBM（高带宽内存）访问瓶颈和瓦片间通信效率低下的挑战。
**整体含义**：本文提出了 **FlatAttention**，一种通过协同优化数据流与片上网络（NoC）集合通信原语的方法，旨在最小化 HBM 访问并提升瓦片式加速器在处理各种 Attention 变体时的执行效率。

### 2. 方法论
*   **核心思想**：将多个计算瓦片视为一个统一的计算单元（Group），利用瓦片组的聚合 L1 缓存来存储更大的注意力块，从而通过增加片上数据复用来减少昂贵的 HBM I/O。
*   **关键技术细节**：
    *   **织构集合原语（Fabric Collectives）**：在 NoC 路由器硬件中直接支持多播（Multicast）和归约（Reduction）操作，避免了传统软件模拟集合通信带来的高延迟。
    *   **异步调度（Asynchronous Scheduling）**：通过双缓冲技术，将矩阵引擎（GEMM）的计算与向量引擎（Softmax）的操作以及 DMA 数据传输（HBM 访问）在时间上重叠，使矩阵引擎保持近乎 100% 的活跃度。
    *   **通用化设计**：支持多种 Attention 变体。对于 **MLA（多头潜在注意力）**，利用“权重吸收”（Weight-absorption）技巧将其转化为多查询注意力（MQA）模式进行高效处理。
    *   **平铺策略（Tiling Strategy）**：提出了一种平衡策略，先确保单个瓦片的矩阵引擎利用率，再根据序列长度和硬件拓扑决定“扁平化”（Flattening）的规模，以防止“过度扁平化”导致的同步开销过大。

### 3. 实验设计
*   **模拟环境**：使用基于事件的 **GVSoC** 模拟器，并针对 RedMulE 矩阵引擎和 FlooNoC 片上网络进行了 RTL 级的周期精确校准（误差在 0.17% - 12% 之间）。
*   **硬件配置**：模拟了一个 32×32 的瓦片阵列，峰值性能（988 TFLOPS FP16）和 HBM 带宽（2-4 TB/s）与 NVIDIA GH200 相当。
*   **Benchmark 与对比方法**：
    *   **算子级**：对比了 FlashAttention-2、FlashAttention-3 以及针对 MLA 优化的 FlashMLA。
    *   **硬件级**：对比了 NVIDIA GH200 GPU 的实际运行表现。
    *   **系统级**：在晶圆级多芯片系统上评估了 DeepSeek-v3-671B 的端到端解码性能，对比对象为 96 块 NVIDIA H800 GPU 组成的集群。

### 4. 资源与算力
*   **算力规格**：论文评估的单芯片加速器在 965MHz 下提供约 1 PFLOPS (FP16) 算力；在端到端 DeepSeek-v3 评估中，频率提升至 1.9GHz 以匹配 FP8 峰值性能。
*   **系统规模**：使用了 64 个此类加速器组成的晶圆级系统。
*   **训练时长**：本文专注于**推理加速**，未涉及模型训练过程，因此未提及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从微基准测试（集合原语延迟）、算子级消融实验（不同瓦片组规模）、多种 Attention 变体（MHA, GQA, MLA）到端到端全模型推理。
*   **充分性与公平性**：实验在相同的峰值算力和带宽条件下对比了 FlatAttention 与 FlashAttention 系列，并考虑了预填充（Prefill）和解码（Decode）两个阶段。通过 RTL 校准确保了模拟数据的可靠性，实验设计客观且覆盖面广。

### 6. 主要结论与发现
*   **性能提升**：在相同硬件上，FlatAttention 比 FlashAttention-3 提速高达 **4.1 倍**，HBM 流量减少了 **16 倍**。
*   **硬件利用率**：在计算受限场景下实现平均 **86%** 的利用率，在内存受限场景下实现 **78%** 的带宽利用率。
*   **端到端优势**：在 DeepSeek-v3 解码任务中，尽管系统峰值算力比 H800 集群低 1.5 倍，但凭借 FlatAttention，系统吞吐量提升了 **1.9 倍**，单用户延迟降低了 **1.4 倍**。

### 7. 优点
*   **软硬协同**：不仅优化了算法数据流，还深入探讨了硬件 NoC 原语对算法性能的支撑作用。
*   **高度灵活**：能够自适应处理从标准 MHA 到复杂的 MLA 等多种现代注意力机制。
*   **解决瓶颈**：精准打击了瓦片式架构在大模型推理中的“注意力瓶颈”，证明了片上通信换取片外带宽的可行性。

### 8. 不足与局限
*   **硬件依赖性**：该方法高度依赖于具有硬件级集合通信支持的特定 NoC 架构，在缺乏此类支持的通用 GPU 上难以直接复现相同增益。
*   **短序列挑战**：在序列长度非常短的情况下，由于“过度扁平化”，同步开销可能会抵消计算收益。
*   **互连拓扑限制**：端到端评估主要基于 2D Mesh 拓扑，对于更复杂的晶圆级互连拓扑（如 Torus 或全连接）的泛化性讨论较少。

（完）
