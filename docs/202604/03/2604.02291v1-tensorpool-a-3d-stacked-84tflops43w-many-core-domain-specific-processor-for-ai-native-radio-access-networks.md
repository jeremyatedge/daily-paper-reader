---
title: "TensorPool: A 3D-Stacked 8.4TFLOPS/4.3W Many-Core Domain-Specific Processor for AI-Native Radio Access Networks"
title_zh: TensorPool：一种用于 AI 原生无线接入网的 3D 堆叠 8.4TFLOPS/4.3W 多核领域专用处理器
authors: "Marco Bertuletti, Yichao Zhang, Diyou Shen, Alessandro Vanelli-Coralli, Frank K. Gürkaynak, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02291v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于AI原生无线接入网的多核领域专用处理器
tldr: 针对6G AI原生无线接入网（RAN）对高算力和低功耗的双重需求，本文提出了TensorPool处理器。该处理器采用TSMC N7工艺，集成256个RISC-V核心和16个高性能张量引擎，并通过3D堆叠技术优化存储访问。实验表明，其在AI-RAN任务中实现了8.4 TFLOPS的算力和极高的能效比，显著提升了处理性能与面积效率，为实时AI物理层处理提供了可行方案。
selection_source: fresh_fetch
motivation: 6G物理层AI模型的高计算复杂度和严苛的实时性、功耗限制，要求基带处理器在保持灵活性的同时大幅提升张量计算能效。
method: 设计了集成256个RISC-V核心与16个张量引擎的领域专用多核架构，并采用3D堆叠技术优化计算单元与L1存储间的布线。
result: 在TSMC N7工艺下实现8.4 TFLOPS算力，张量运算效率提升6倍，能效比提升9.1倍，且3D堆叠使芯片面积缩小2.32倍。
conclusion: TensorPool通过领域专用加速与3D集成技术，为满足未来6G AI原生无线通信的高吞吐与低功耗需求提供了高效的硬件支撑。
---

## 摘要
6G 无线接入网 (RAN) 物理层 (PHY) 中即将引入的 AI 集成，将能够在具有挑战性的传输场景中提供更高的服务质量。然而，与传统基带相比，深度优化的 AI 原生 PHY 模型带来了更高的计算复杂度，这对现代 PHY 典型的亚毫秒级实时约束下的部署提出了挑战。此外，随着向太赫兹载波的扩展，即将到来的 6G 基站密集化进一步限制了基站的功耗，从而约束了可用于计算的预算（≤ 100W）。为了确保长期可持续性所需的灵活性，以及在主导 AI 原生 PHY 的高吞吐量张量计算上实现必要的能效提升，可以通过多核可编程基带处理器的领域专用化来实现。遵循领域专用化策略，我们提出了 TensorPool，这是一个由 256 个 RISCV32IMAF 可编程核心组成的集群，由 16 个 256 MACs/周期 (FP16) 的张量引擎加速，并可低延迟访问 4MiB 的 L1 便签存储器 (scratchpad) 以实现最大程度的数据复用。TensorPool 采用台积电 (TSMC) N7 工艺实现，在 AI-RAN 的张量运算中达到了 3643 MACs/周期（张量单元利用率为 89%），比没有张量加速的纯核心集群高出 6 倍，同时将 GOPS/W/mm² 效率提升了 9.1 倍。此外，我们展示了通过对 TensorPool 的计算模块进行 3D 堆叠，以更好地展开张量引擎到 L1 内存的布线，与 2D 实现相比，在不降低频率的情况下实现了 2.32 倍的面积利用率提升。

## Abstract
The upcoming integration of AI in the physical layer (PHY) of 6G radio access networks (RAN) will enable a higher quality of service in challenging transmission scenarios. However, deeply optimized AI-Native PHY models impose higher computational complexity compared to conventional baseband, challenging deployment under the sub-msec real-time constraints typical of modern PHYs. Additionally, following the extension to terahertz carriers, the upcoming densification of 6G cell-sites further limits the power consumption of base stations, constraining the budget available for compute ($\leq$ 100W). The desired flexibility to ensure long term sustainability and the imperative energy-efficiency gains on the high-throughput tensor computations dominating AI-Native PHYs can be achieved by domain-specialization of many-core programmable baseband processors. Following the domain-specialization strategy, we present TensorPool, a cluster of 256 RISCV32IMAF programmable cores, accelerated by 16 256 MACs/cycle (FP16) tensor engines with low-latency access to 4MiB of L1 scratchpad for maximal data-reuse. Implemented in TSMC's N7, TensorPool achieves 3643~MACs/cycle (89% tensor-unit utilization) on tensor operations for AI-RAN, 6$\times$ more than a core-only cluster without tensor acceleration, while simultaneously improving GOPS/W/mm$^2$ efficiency by 9.1$\times$. Further, we show that 3D-stacking the computing blocks of TensorPool to better unfold the tensor engines to L1-memory routing provides 2.32$\times$ footprint improvement with no frequency degradation, compared to a 2D implementation.

---

## 论文详细总结（自动生成）

### 论文总结：TensorPool —— 用于 AI 原生无线接入网的 3D 堆叠多核领域专用处理器

#### 1. 核心问题与研究动机
随着 6G 通信技术的发展，AI 原生无线接入网（AI-Native RAN）要求在物理层（PHY）集成深度学习模型以提升服务质量。然而，这面临三大挑战：
*   **计算复杂度极高**：AI 模型（如 ResNet、Attention）的计算量远超传统基带处理。
*   **实时性严苛**：物理层任务必须在 1ms 内完成。
*   **功耗受限**：6G 基站密度增加，要求数字信号处理功耗控制在 100W 以内，边缘部署环境对能效比要求极高。
现有的 CPU、FPGA 或数据中心 GPU 难以同时满足高灵活性、低延迟、低功耗和高吞吐量的需求。

#### 2. 方法论与关键技术
论文提出了 **TensorPool** 架构，其核心思想是通过“领域专用化（Domain-Specialization）”和“3D 堆叠”来突破计算与访存瓶颈：
*   **异构多核架构**：集成 256 个可编程 RISC-V 核心（处理非规则逻辑）和 16 个高性能张量引擎（TE，处理 GEMM 密集运算）。
*   **高性能张量引擎 (TE)**：每个 TE 包含 256 个 FP16 MAC 单元，采用 32x8 的 FMA 阵列，支持 512-bit 宽内存接口。
*   **延迟容忍互连设计**：
    *   **重排序缓冲区 (ROB)**：在 TE 流水线中集成 ROB 以应对非均匀内存访问延迟。
    *   **突发传输支持 (Burst Support)**：引入 Burst-Grouper 和 Burst-Distributor，减少分层互连中的地址握手开销，提升有效带宽。
*   **3D 逻辑堆叠**：采用晶圆级混合键合（Wafer-to-Wafer Hybrid Bonding）技术，将 4 个计算组（Groups）垂直堆叠。通过 Z 轴布线减少 2D 平面的布线拥塞，缩短全局连线长度。
*   **内存平衡理论**：基于 Kung's Principle 分析并优化了 L1/L2 缓存带宽，确保计算单元不会因等待数据而停顿。

#### 3. 实验设计与 Benchmark
*   **实验场景**：涵盖了 AI-Native PHY 的核心算子（GEMM、Softmax、LayerNorm、ReLU、深度可分离卷积）以及传统无线信号处理任务（CFFT、LS 信道估计、MIMO-MMSE 检测）。
*   **对比基准 (Benchmark)**：
    *   **TeraPool**：同系列的纯 RISC-V 核心集群（1024 核，无张量加速）。
    *   **商业平台**：NVIDIA Aerial RAN 系列（Blackwell/L4 GPU）、Qualcomm Hexagon HTA。
*   **评估指标**：峰值算力 (TFLOPS)、能效比 (TFLOPS/W)、面积效率 (GOPS/mm²)、利用率及 PPA（功耗、性能、面积）。

#### 4. 资源与算力
*   **工艺节点**：台积电 (TSMC) 7nm (N7) FinFET。
*   **算力表现**：峰值算力达 **8.4 TFLOPS (FP16)**。
*   **功耗与面积**：总功耗约 **4.32W**，2D 面积为 26.65 mm²。
*   **仿真工具**：使用 QuestaSim 进行周期精确的 RTL 仿真，Synopsys Fusion Compiler 进行物理设计，3DIC Compiler 进行 3D 堆叠分析，PrimeTime 进行功耗评估。
*   **训练/运行说明**：论文侧重于推理与实时处理性能，未提及模型训练时长。

#### 5. 实验数量与充分性
*   **实验覆盖面**：
    *   进行了单 TE 与多 TE 并行的 GEMM 性能扩展实验。
    *   针对多种 AI 算子进行了 PEs（核心）、TEs（张量引擎）与 DMA 的并发协同实验。
    *   对比了 2D 与 3D 实现的布线拥塞与面积效率。
*   **充分性评价**：实验设计较为全面，不仅有理论上的内存平衡分析，还有基于真实 RTL 的周期精确仿真和物理后端布局布线数据。对比对象涵盖了学术界前沿和工业界主流方案，结果具有较高的客观性和说服力。

#### 6. 主要结论
*   **性能飞跃**：TensorPool 在张量运算上的吞吐量比纯核心集群（TeraPool）高出 **6 倍**。
*   **能效卓越**：综合能效比（GFLOPS/W/mm²）提升了 **9.1 倍**。
*   **高利用率**：通过互连优化，在执行 GEMM 时 FMA 利用率高达 **89%**。
*   **3D 优势**：3D 堆叠在不损失频率的前提下，使芯片足迹（Footprint）缩小了 **2.32 倍**，布线通道面积减少了 **67%**。

#### 7. 论文亮点
*   **领域专用互连**：针对多核共享 L1 存储的架构，设计了专门的突发传输和延迟容忍机制，解决了大规模并行下的访存冲突。
*   **3D 集成创新**：首次展示了逻辑-逻辑（Logic-on-Logic）3D 堆叠在提升共享 L1 存储多核处理器性能方面的巨大潜力。
*   **软硬协同**：不仅提供硬件，还展示了如何将复杂的 AI-PHY 任务（如 MHA 模块）映射到异构核心上并行执行。

#### 8. 不足与局限
*   **仿真局限**：所有数据基于 RTL 仿真和后端设计工具估算，尚未经过流片（Silicon-Proven）验证，实际散热和 3D 键合良率可能存在挑战。
*   **精度单一**：目前主要针对 FP16 精度，对于更低精度（如 INT8/INT4）或更高精度（FP32）的支持和优化讨论较少。
*   **热管理挑战**：虽然提到了 3D 堆叠，但对于逻辑-逻辑堆叠带来的热点（Hotspot）问题和散热方案未进行深入的定量热仿真分析。

（完）
