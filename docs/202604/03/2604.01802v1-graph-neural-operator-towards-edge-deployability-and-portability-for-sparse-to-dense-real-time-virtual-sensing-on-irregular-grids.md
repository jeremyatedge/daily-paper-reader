---
title: "Graph Neural Operator Towards Edge Deployability and Portability for Sparse-to-Dense, Real-Time Virtual Sensing on Irregular Grids"
title_zh: 面向不规则网格上稀疏到密集实时虚拟传感的边缘可部署且具可移植性的图神经营算子
authors: "William Howes, Jason Yoo, Kazuma Kobayashi, Subhankar Sarkar, Farid Ahmed, Souvik Chakraborty, Syed Bahauddin Alam"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01802v1"
tags: ["query:aiot"]
score: 8.0
evidence: 资源受限系统上实时传感的边缘部署能力
tldr: "针对资源受限环境下高密度物理场感知的难题，本文提出VIRSO，一种用于不规则几何形状稀疏到稠密重建的图神经算子。通过结合V-KNN变连接算法与时空分析，VIRSO在保持高精度的同时显著降低了计算延迟和功耗。在核热工水力基准测试中，其重建误差低于1%，且在边缘设备上实现了亚秒级延迟和低功耗运行，为实时虚拟传感提供了高效的边缘部署方案。"
selection_source: fresh_fetch
motivation: 传统的物理求解器和现有神经算子因计算延迟高、功耗大，难以满足资源受限环境下实时、高密度的物理场监测需求。
method: 提出VIRSO架构，结合了基于网格信息的V-KNN变连接图构建算法，通过谱空间与空间分析实现高效的稀疏到稠密重建。
result: "在多个核能基准测试中，VIRSO以更少的参数实现了低于1%的误差，并将能量延迟积降低了约20倍，在边缘设备上表现出极佳的性能。"
conclusion: VIRSO证明了计算感知型算子学习在边缘部署中的可行性，为不可达或资源受限环境下的实时虚拟传感奠定了基础。
---

## 摘要
对空间分布物理场的准确感知通常需要密集的仪器布置，但由于成本、可访问性和环境限制，这在现实系统中往往是不可行的。基于物理的求解器通过对控制方程进行直接数值积分来解决这一问题，但其计算延迟和功耗需求阻碍了其在资源受限的监测与控制系统中的实时应用。在此，我们介绍了 VIRSO（虚拟不规则实时稀疏算子），这是一种用于不规则几何形状上稀疏到密集重建的图神经营算子，以及一种用于网格感知图构建的可变连接算法——可变 KNN (V-KNN)。与以往将硬件可部署性视为次要因素的神经营算子不同，VIRSO 将推理重新定义为测量：频谱分析与空间分析的结合提供了准确的重建，且没有以往扩展性较差的图方法所具有的高延迟和高功耗，使 VIRSO 成为边缘受限实时虚拟传感的潜在候选方案。我们在三个几何和多物理场复杂度逐渐增加的核热工水力基准测试上评估了 VIRSO，重建比例涵盖 47:1 到 156:1。VIRSO 实现了低于 1% 的平均相对 $L_2$ 误差，在参数量更少的情况下优于其他基准算子。在 NVIDIA H200 上，完整的 10 层配置将能量延迟积 (EDP) 从图算子基线的约 206 J·ms 降低到了 10.1 J·ms。在 NVIDIA Jetson Orin Nano 上实现时，VIRSO 的所有配置均提供了低于 10 W 的功耗和亚秒级的延迟。这些结果确立了 VIRSO 的边缘可行性和硬件可移植性，并将计算感知型算子学习展示为在不可访问和资源受限环境中进行实时传感的新范式。

## Abstract
Accurate sensing of spatially distributed physical fields typically requires dense instrumentation, which is often infeasible in real-world systems due to cost, accessibility, and environmental constraints. Physics-based solvers address this through direct numerical integration of governing equations, but their computational latency and power requirements preclude real-time use in resource-constrained monitoring and control systems. Here we introduce VIRSO (Virtual Irregular Real-Time Sparse Operator), a graph-based neural operator for sparse-to-dense reconstruction on irregular geometries, and a variable-connectivity algorithm, Variable KNN (V-KNN), for mesh-informed graph construction. Unlike prior neural operators that treat hardware deployability as secondary, VIRSO reframes inference as measurement: the combination of both spectral and spatial analysis provides accurate reconstruction without the high latency and power consumption of previous graph-based methodologies with poor scalability, presenting VIRSO as a potential candidate for edge-constrained, real-time virtual sensing. We evaluate VIRSO on three nuclear thermal-hydraulic benchmarks of increasing geometric and multiphysics complexity, across reconstruction ratios from 47:1 to 156:1. VIRSO achieves mean relative $L_2$ errors below 1%, outperforming other benchmark operators while using fewer parameters. The full 10-layer configuration reduces the energy-delay product (EDP) from ${\approx}206$ J$\cdot$ms for the graph operator baseline to $10.1$ J$\cdot$ms on an NVIDIA H200. Implemented on an NVIDIA Jetson Orin Nano, all configurations of VIRSO provide sub-10 W power consumption and sub-second latency. These results establish the edge-feasibility and hardware-portability of VIRSO and present compute-aware operator learning as a new paradigm for real-time sensing in inaccessible and resource-constrained environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Graph Neural Operator Towards Edge Deployability and Portability for Sparse-to-Dense, Real-Time Virtual Sensing on Irregular Grids》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在核反应堆、工业热交换等复杂物理系统中，由于极端环境（高辐射、高温）和成本限制，无法布置高密度的物理传感器。传统的数值模拟（如 CFD）虽然精确，但计算延迟极高，无法满足实时监测和控制的需求。
*   **核心问题**：如何在资源受限的边缘计算设备上，利用极少数边界传感器（稀疏输入）实时、准确地重建整个内部复杂几何区域的物理场（密集输出，如温度、压力、流速）。
*   **整体含义**：论文提出了 **VIRSO** 框架，将神经营算子从单纯的“加速替代模型”转变为一种“虚拟传感仪器”，强调硬件感知设计，使其能在单位数瓦特功耗的边缘设备上运行。

### 2. 论文提出的方法论
*   **核心思想**：结合图谱分析（全局一致性）和空间聚合（局部校准），构建一种硬件友好的图神经营算子。
*   **关键技术细节**：
    *   **VIRSO 架构**：采用“嵌入-处理-投影”结构。首先通过 FCN 或 LSTM 将稀疏边界信号映射到潜在空间，再通过多个“谱-空间协作块”进行迭代处理。
    *   **谱路径（Spectral Path）**：利用图拉普拉斯算子的特征分解，在频域执行卷积。这能捕捉全局物理相关性，且计算上属于计算密集型（矩阵乘法），易于硬件加速。
    *   **空间路径（Spatial Path）**：利用带门控机制的图神经网络（GNN）进行局部特征提取，用于校准边界层和高梯度区域。
    *   **V-KNN（可变 KNN）算法**：一种网格感知的图构建策略。根据网格密度动态调整节点的连接数（K值），在几何复杂区域增加连接，在平滑区域减少连接，从而在提升精度的同时减少 34% 的边数量。
    *   **残差跳跃连接**：在谱卷积后保留高频物理信息，防止谱截断导致的细节丢失。

### 3. 实验设计
*   **数据集/场景**：
    1.  **盖驱动流（LDC）**：规则网格上的瞬态多物理场重建。
    2.  **压水堆（PWR）子通道**：不规则几何，跨域传感（边界输入与内部输出几何不重合）。
    3.  **波纹嵌件热交换器**：极高几何复杂度，重建比例高达 **156:1**。
*   **Benchmark（基准模型）**：
    *   **Geo-FNO**：几何感知傅里叶神经营算子。
    *   **NOMAD**：非线性流形解码器。
    *   **GNO**：标准图神经营算子。
*   **对比维度**：预测精度（相对 $L_2$ 误差）、参数量、推理延迟、功耗、能量延迟积（EDP）。

### 4. 资源与算力
*   **训练与高性能推理**：使用 **NVIDIA H200 GPU**（Delta 超算集群）。
*   **边缘部署测试**：使用 **NVIDIA Jetson Orin Nano (8GB)** 嵌入式平台。
*   **训练细节**：Adam 优化器，最高 500 epoch，Batch Size 为 16，使用学习率衰减和早停机制。
*   **数值基准生成**：使用 ANSYS Fluent 在 AMD EPYC 7763 CPU 上生成高保真参考数据（LDC 案例单样本生成需 36 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：针对三个不同复杂度的基准测试进行了完整评估，样本量从 1500 到 5000 不等。
*   **消融实验**：非常充分。分别验证了嵌入层、跳跃连接、谱路径 vs 空间路径的贡献，以及不同图构建策略（KNN, Radius, V-KNN）的影响。
*   **硬件测试**：不仅对比了算力峰值，还详细测量了边缘设备上的板级功耗（tegrastats）和内存占用。
*   **客观性**：通过在相同参数量级下对比不同模型，并引入“功率归一化精度”指标，确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **高精度重建**：在所有基准测试中，VIRSO 的平均相对 $L_2$ 误差均低于 **1%**，优于所有对比模型。
*   **极致加速**：相比传统 CFD 求解器，推理速度提升了 **4 个数量级**以上。
*   **边缘可行性**：在 Jetson Orin Nano 上实现了 **<10W** 的功耗和亚秒级延迟（纯谱模式可达 17.0 样本/秒）。
*   **硬件特性解耦**：发现谱路径是计算受限型（易移植），而空间路径是内存带宽受限型（在边缘设备上延迟增加 9.6 倍），这为根据硬件资源调整模型配置提供了理论依据。

### 7. 优点
*   **硬件感知设计**：不同于大多数只关注精度的论文，本文深入探讨了算子结构与硬件架构（计算受限 vs 带宽受限）的匹配关系。
*   **V-KNN 创新**：将有限元分析中的“网格加密”思想引入图构建，有效平衡了计算效率与物理表达能力。
*   **跨域处理能力**：成功解决了输入传感器域与输出物理场域不重合的严苛传感问题。

### 8. 不足与局限
*   **稳态限制**：虽然 LDC 涉及瞬态，但主要实验仍集中在稳态物理场的重建，对于剧烈随时间演化的动态过程（如事故工况）尚需进一步验证。
*   **跨几何泛化**：论文尚未展示在一个几何形状上训练后，直接在另一个完全不同的几何形状上进行推理的能力（Zero-shot Generalization）。
*   **边缘加速瓶颈**：在嵌入式设备上，空间路径的延迟仍然较高，未来可能需要算子融合或 TensorRT 深度优化。

（完）
