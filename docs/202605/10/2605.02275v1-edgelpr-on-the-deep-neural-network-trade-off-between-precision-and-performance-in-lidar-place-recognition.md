---
title: "EdgeLPR: On the Deep Neural Network trade-off between Precision and Performance in LiDAR Place Recognition"
title_zh: EdgeLPR：关于激光雷达场景识别中深度神经网络精度与性能权衡的研究
authors: "Pierpaolo Serio, Hetian Wang, Zixiang Wei, Vincenzo Infantino, Lorenzo Gentilini, Lorenzo Pollini, Valentina Donzella"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02275v1"
tags: ["query:aiot"]
score: 9.0
evidence: 针对边缘AI的量化和轻量级网络
tldr: 本研究针对自动驾驶中激光雷达场景识别在边缘设备部署难的问题，提出了一种基于鸟瞰图（BEV）表示的轻量级方案。通过对多种代表性深度学习架构进行基准测试，并对比FP32、FP16及INT8量化下的表现，揭示了精度与性能间的权衡。研究发现FP16能在保持精度的同时显著提升效率，而INT8的影响则因架构而异，为未来边缘AI的量化研究提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在解决深度学习模型在资源受限的边缘计算平台上进行激光雷达场景识别时，难以兼顾高精度与高性能的挑战。
method: 利用鸟瞰图表示法将激光雷达数据转化为轻量级图像网络输入，并采用统一的描述符方案对不同架构在FP32、FP16和INT8量化下进行评估。
result: 实验显示FP16量化在降低资源消耗的同时能维持与FP32相当的精度，而INT8量化带来的性能下降则表现出明显的架构依赖性。
conclusion: 该研究为边缘AI部署中针对特定用例的神经网络量化策略提供了坚实的实验基础和指导建议。
---

## 摘要
场景识别对于长期自主导航至关重要，它能够实现回环检测和一致性建图。尽管深度学习提高了性能，但在资源受限的平台上部署此类模型仍然具有挑战性。本研究通过利用鸟瞰图（Bird's Eye View）表示来实现轻量级基于图像的网络，探索了面向边缘人工智能（EdgeAI）的高效激光雷达场景识别。我们使用基于全局池化和线性投影的统一描述符方案，对不含聚合头的代表性架构进行了基准测试，并评估了在 FP32、FP16 和 INT8 量化下的性能。实验揭示了准确性、鲁棒性和效率之间的权衡：FP16 在成本较低的情况下达到了与 FP32 相当的水平，而 INT8 则引入了与架构相关的性能下降。总之，所呈现的结果为未来关于边缘部署中“用例感知”神经网络量化的研究奠定了坚实基础。

## Abstract
Place recognition is essential for long-term autonomous navigation, enabling loop closure and consistent mapping. Although deep learning has improved performance, deploying such models on resource-constrained platforms remains challenging. This work explores efficient LiDAR-based place recognition for EdgeAI by leveraging Bird's Eye View representations to enable lightweight image-based networks. We benchmark representative architectures without aggregation heads using a unified descriptor scheme based on global pooling and linear projection, and evaluate performance under FP32, FP16, and INT8 quantization. Experiments reveal trade-offs between accuracy, robustness, and efficiency: FP16 matches FP32 with lower cost, while INT8 introduces architecture-dependent degradation. Overall, the presented results are a strong basis for future research on 'use-case'-aware quantisation of Neural Networks for Edge deployment.

---

## 论文详细总结（自动生成）

以下是对论文《EdgeLPR: On the Deep Neural Network trade-off between Precision and Performance in LiDAR Place Recognition》的结构化总结：

### 1. 研究动机与核心问题
*   **研究背景**：场景识别（Place Recognition）是机器人自主导航中回环检测和一致性建图的关键。虽然深度学习显著提升了识别精度，但模型日益复杂，导致在资源受限的边缘设备（EdgeAI）上部署困难。
*   **核心问题**：如何在保证激光雷达（LiDAR）场景识别精度的同时，降低计算和内存开销？论文重点探讨了轻量级神经网络架构在不同数值精度（FP32、FP16、INT8）下的性能权衡。

### 2. 方法论
*   **核心思想**：将 3D 激光雷达点云投影为 2D 鸟瞰图（BEV），从而能够利用成熟且轻量级的图像处理网络，避免直接处理高维无序点云带来的巨大算力消耗。
*   **关键技术流程**：
    1.  **表示转换**：将原始点云投影为 256x256 的 BEV 图像（使用强度信息作为像素值）。
    2.  **轻量级骨干网络**：选用 MobileNetV3、ShuffleNetV2 和 ResNet18 作为特征提取器。
    3.  **统一描述符方案**：取消复杂的聚合头（如 NetVLAD），采用“全局平均池化（GAP）+ 线性投影层”的简洁结构，将特征映射为固定维度的全局描述符。
    4.  **量化策略**：采用静态后训练量化（PTQ），通过校准集最小化均方误差（MSE）来确定量化参数，支持 FP32、FP16 和 INT8 三种精度。
    5.  **损失函数**：使用三元组损失（Triplet Loss）进行端到端微调，拉近空间邻近帧的描述符距离，推开远处帧的距离。

### 3. 实验设计
*   **数据集**：
    *   **KITTI**：用于评估短期视角鲁棒性（训练使用序列 00，测试使用 02、05、06）。
    *   **NCLT**：用于评估长期季节性和环境变化下的不变性（跨越数月的多次采集）。
*   **基准测试（Benchmark）**：
    *   对比了三种不同设计范式的轻量级架构：MobileNetV3（高效搜索）、ShuffleNetV2（通道洗牌）、ResNet18（残差连接）。
    *   评估指标包括：Recall@k (1, 5, 10, 20)、最大 F1 分数、PR 曲线下面积（AUC）和平均倒数排名（MRR）。

### 4. 资源与算力
*   **算力平台**：论文提到使用了比萨大学（University of Pisa）提供的计算服务（computing@unipi）。
*   **硬件细节**：实验中 FP32 和 FP16 在 GPU 上运行，而 INT8 在 CPU 后端执行以模拟边缘推理环境。
*   **具体参数**：文中未明确给出具体的 GPU 型号（如 RTX 系列）或总训练时长，但提供了详细的推理延迟（Latency）和吞吐量（Throughput）数据（见表 V）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种架构、3 种精度等级、2 个主流数据集（包含多个子序列），并针对每个指标运行了 3 个随机种子以获取均值和标准差。
*   **充分性评价**：实验设计较为客观、公平。通过统一描述符维度和池化策略，排除了聚合层对结果的干扰，专注于骨干网络和量化本身的影响。消融实验体现在不同精度和不同数据集的交叉对比上。

### 6. 主要结论与发现
*   **FP16 的优越性**：在所有架构中，FP16 的精度几乎与 FP32 持平，但内存占用减半，是边缘部署的“免费午餐”。
*   **INT8 的架构敏感性**：INT8 量化对不同架构影响差异巨大。MobileNetV3 性能下降最明显；ShuffleNetV2 表现稳定；ResNet18 表现出最强的量化鲁棒性。
*   **环境挑战**：在 NCLT 等长期变化场景下，所有模型性能均有下降，但 ResNet18 在量化后的泛化能力相对更好。
*   **效率悖论**：参数量最少的模型（如 ShuffleNetV2）在某些硬件（如 CPU）上的 INT8 推理吞吐量提升并不总是线性，说明架构设计需与硬件特性深度耦合。

### 7. 优点
*   **系统性强**：首次针对激光雷达场景识别任务，系统地对比了轻量级网络在量化后的表现。
*   **实用性高**：为边缘机器人系统提供了明确的硬件设计指南（如：追求极致内存选 ShuffleNetV2+INT8，追求精度选 MobileNetV3+FP16）。
*   **方案简洁**：摒弃了复杂的特征聚合模块，证明了简单的 BEV + 轻量级 CNN 也能在场景识别中取得不错的效果。

### 8. 不足与局限
*   **量化方法单一**：仅采用了后训练量化（PTQ），未探讨量化感知训练（QAT），后者可能进一步提升 INT8 的表现。
*   **架构覆盖有限**：虽然选取的架构具有代表性，但未包含最新的 Vision Transformer (ViT) 轻量化变体。
*   **BEV 局限性**：BEV 投影虽然高效，但可能会丢失部分 3D 空间垂直结构信息，在某些极端垂直环境下的表现未做深入探讨。

（完）
