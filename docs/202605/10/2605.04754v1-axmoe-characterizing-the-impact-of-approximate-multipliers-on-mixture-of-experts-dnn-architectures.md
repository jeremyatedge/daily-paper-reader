---
title: "AxMoE: Characterizing the Impact of Approximate Multipliers on Mixture-of-Experts DNN Architectures"
title_zh: AxMoE：表征近似乘法器对混合专家（MoE）深度神经网络架构的影响
authors: "Omkar B Shende, Marcello Traiola, Gayathri Ananthanarayanan"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04754v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于边缘DNN推理的低功耗近似乘法器
tldr: "本研究首次探讨了近似乘法器对混合专家（MoE）架构的影响，提出了AxMoE框架。通过在CNN和ViT模型上评估多种MoE变体（Hard, Soft, Cluster），分析了在不同近似计算精度下模型的鲁棒性。研究发现，经过近似感知重训练后，MoE在特定场景下（如ViT）比密集模型更具优势，为软硬件协同设计提供了新思路。"
selection_source: fresh_fetch
motivation: 旨在探索近似计算与MoE架构结合在提升边缘端深度学习推理效率方面的潜力及相互影响。
method: 在ResNet、VGG和ViT架构上对比了三种MoE变体与密集基准，并使用EvoApproxLib中的多种8位近似乘法器进行评估。
result: 未重训练时密集模型更稳健，但重训练后，Hard MoE在ViT上表现优于密集模型，而CNN的恢复程度随架构和乘法器而异。
conclusion: MoE架构与近似计算的结合具有可行性，且特定路由策略在激进近似下能提供更好的性能与功耗平衡。
---

## 摘要
边缘侧的深度神经网络（DNN）推理要求在准确率、计算效率和能耗方面同时得到提升。近似计算和混合专家（MoE）架构各自作为实现高效推理的独立路径被研究，前者通过用低功耗近似乘法器替换精确算术，后者通过将输入路由到专门的专家子网络以实现条件计算。然而，它们之间的相互作用仍完全未被探索。本文提出了 AxMoE，这是首个研究近似乘法对 MoE DNN 架构影响的工作。我们在 CIFAR-100 数据集上的三种 CNN 架构（ResNet-20、VGG11_bn、VGG19_bn）以及 Tiny ImageNet-200 数据集上的视觉 Transformer（ViT-Small）上，评估了三种 MoE 变体（Hard MoE、Soft MoE 和 Cluster MoE）与密集（Dense）基准模型的对比，并使用了来自 EvoApproxLib 库的八个 8 位有符号乘法器（包括一个精确基准）。结果表明，在不进行重训练的情况下，Dense 基准在所有 CNN 架构中是鲁棒性最强的拓扑结构，而在 ViT-Small 上，无论采用何种路由策略，所有拓扑结构的性能下降速率都相当。在进行近似感知重训练后，不同架构、拓扑和乘法器的恢复情况差异显著。ResNet-20 在整个乘法器范围内实现了完全恢复，而 VGG 架构在适度近似的乘法器下可以恢复，但在激进近似下则不可逆地失效（除了 VGG11_bn 上的 Cluster MoE）；在 ViT-Small 上，在相等的归一化推理成本下，Hard MoE 在激进近似下的表现优于 Dense。这些结果为未来近似 MoE 软硬件协同设计策略铺平了道路。

## Abstract
Deep neural network (DNN) inference at the edge demands simultaneous improvements in accuracy, computational efficiency, and energy consumption. Approximate computing and Mixture-of-Experts (MoE) architectures have each been studied as independent routes towards efficient inference, the former by replacing exact arithmetic with low-power approximate multipliers, the latter by routing inputs through specialized expert sub-networks to enable conditional computation. However, their interaction remains entirely unexplored. This paper presents AxMoE, the first study of the impact of approximate multiplication on MoE DNN architectures. We evaluate three MoE variants: Hard MoE, Soft MoE, and Cluster MoE against dense baselines across three CNN architectures (ResNet-20, VGG11_bn, VGG19_bn) on CIFAR-100 and a Vision Transformer (ViT-Small) on Tiny ImageNet-200 dataset, using eight 8-bit signed multipliers (including one exact baseline) from the EvoApproxLib library. Results show that, without retraining, the Dense baseline is the most resilient topology across all CNN architectures, whereas on ViT-Small, all topologies degrade at comparable rates regardless of routing strategy. After approximate-aware retraining, recovery varies substantially across architectures, topologies, and multipliers. ResNet-20 achieves full recovery across the entire multiplier range, whereas VGG architectures recover at moderate multipliers but fail irreversibly at aggressive ones for all topologies except Cluster MoE on VGG11_bn; on ViT-Small, Hard MoE outperforms Dense under aggressive approximation at equal normalized inference cost. These results pave the way for future approximate MoE hardware-software co-design strategies.

---

## 论文详细总结（自动生成）

这篇论文名为《AxMoE: Characterizing the Impact of Approximate Multipliers on Mixture-of-Experts DNN Architectures》，是首个系统性研究**近似计算（Approximate Computing）**与**混合专家架构（MoE）**结合对深度学习推理影响的工作。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在边缘设备上部署深度神经网络（DNN）面临严峻的功耗和计算资源挑战。
*   **研究动机**：目前存在两种独立的高效推理路径：
    1.  **硬件层面**：近似计算（AxC），通过降低算术电路（如乘法器）的精度来换取能效提升。
    2.  **架构层面**：混合专家架构（MoE），通过动态路由实现条件计算，仅激活部分参数。
*   **研究空白**：尽管两者都已成熟，但它们的相互作用（例如，MoE的动态路由如何影响近似误差的传播）此前完全未被探索。

### 2. 论文提出的方法论
*   **核心思想**：提出 **AxMoE** 框架，将多种 MoE 路由策略集成到主流 CNN 和 ViT 架构中，并使用多种近似乘法器模拟硬件层面的精度损失，评估其准确率与功耗的权衡。
*   **关键技术细节**：
    *   **三种 MoE 变体**：
        *   **Hard MoE**：Top-1 路由，每个输入仅激活一个专家，计算成本低。
        *   **Soft MoE**：所有专家按权重参与计算，训练更稳定但计算开销大。
        *   **Cluster MoE**：在图像级别进行一次性路由，每个专家是完整模型的副本。
    *   **近似模拟**：使用基于查找表（LUT）的仿真技术，模拟 8 位有符号近似乘法器。
    *   **功耗归一化公式**：定义了 $P_{norm}$，综合考虑了有效乘累加操作（Effective MACs）、近似操作占比以及特定乘法器的相对功耗，以便跨架构对比。
    *   **近似感知重训练（Approximate-aware Retraining）**：在训练循环中嵌入近似算术，使模型权重适应硬件误差。

### 3. 实验设计
*   **数据集**：
    *   **CNN 实验**：使用 CIFAR-100。
    *   **ViT 实验**：使用 Tiny ImageNet-200。
*   **基准模型（Benchmark）**：
    *   **架构**：ResNet-20、VGG11_bn、VGG19_bn、ViT-Small。
    *   **对比方法**：Dense（密集模型，无 MoE）作为主要基准，对比 Hard、Soft、Cluster 三种 MoE 拓扑。
*   **近似乘法器**：从 EvoApproxLib 库中选择了 8 种 8 位有符号乘法器，涵盖从精确（KV6）到极度近似（L2L，功耗降低 52.9%）的范围。

### 4. 资源与算力
*   **硬件设备**：使用 NVIDIA RTX A6000 和 RTX 4000 Ada GPU。
*   **训练时长**：
    *   **CNN**：每个 epoch 耗时 6-8 分钟，完整 96 种配置的扫描约耗费 48-64 GPU 小时。
    *   **ViT**：每个 epoch 耗时 45-50 分钟。
*   **重训练配置**：每个模型进行 5 个 epoch 的近似感知重训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的参数扫描，涵盖 4 种架构 × 4 种拓扑（Dense + 3 MoE）× 8 种乘法器 × 2 种状态（重训练前后），总计超过 100 组实验配置。
*   **充分性与公平性**：
    *   实验覆盖了卷积层（CNN）和线性层（ViT）的近似处理。
    *   通过归一化功耗指标确保了不同计算量模型之间的公平对比。
    *   **局限性**：重训练仅设定为 5 个 epoch（受限于 LUT 仿真的高昂时间成本），虽然对多数模型足够，但可能未完全挖掘某些复杂模型的恢复潜力。

### 6. 论文的主要结论与发现
*   **内在鲁棒性（未重训练）**：在 CNN 中，Dense 模型比 MoE 更耐受近似误差；而在 ViT 中，所有拓扑的下降速度相似，因为自注意力层的近似误差占主导地位。
*   **重训练恢复力**：
    *   **ResNet-20**：表现出极强的韧性，在所有近似级别下均能完全恢复准确率。
    *   **VGG**：在激进近似下（如 KVA, L2L）会出现不可逆的准确率崩溃。
    *   **ViT-Small**：在激进近似下，**Hard MoE 的表现优于 Dense 模型**，且功耗相同。
*   **Pareto 最优性**：
    *   对于 CNN，Dense + 中度近似乘法器（如 L2J）通常是帕累托最优解。
    *   对于 ViT，Hard MoE 在低功耗区间展现出比 Dense 更好的准确率平衡。

### 7. 优点
*   **开创性**：首次填补了近似计算与动态 MoE 架构结合的研究空白。
*   **多维度评估**：不仅关注准确率，还通过详细的功耗模型评估了硬件效率。
*   **架构多样性**：涵盖了残差网络、无残差网络以及 Transformer 架构，结论具有普适性。

### 8. 不足与局限
*   **任务单一**：实验仅限于图像分类任务，未涉及目标检测或自然语言处理（NLP）等 MoE 应用更广泛的领域。
*   **重训练预算**：5 个 epoch 的重训练可能不足以让某些在激进近似下失效的模型（如 VGG）完全收敛。
*   **硬件假设**：研究基于仿真，实际硬件部署中路由器的开销和存储器访问模式可能会对总功耗产生额外影响。

（完）
