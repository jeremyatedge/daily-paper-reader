---
title: LLM-Driven Design Space Exploration of FPGA-based Accelerators
title_zh: LLM驱动的基于FPGA的加速器设计空间探索
authors: "Vinamra Sharma, Xingjian Fu, Jude Haris, José Cano"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05920v1"
tags: ["query:aiot"]
score: 9.0
evidence: 基于FPGA加速器的软硬件协同设计
tldr: 针对FPGA加速器设计空间探索（DSE）中手动工作量大、专业门槛高的问题，本文提出SECDA-DSE框架。该框架将大语言模型（LLM）集成到SECDA生态系统中，通过结合结构化探索器、检索增强生成（RAG）和思维链（CoT）提示词，实现了自动化的硬件配置生成与优化。实验证明，该方法能有效生成符合时序和资源约束的加速器设计，显著提升了设计效率。
selection_source: fresh_fetch
motivation: 现代AI负载下的FPGA加速器设计空间极其复杂，传统方法依赖大量人工干预和领域知识，导致开发周期长且资源消耗高。
method: 提出SECDA-DSE框架，利用LLM结合RAG、CoT及反馈循环机制，自动化地进行硬件架构参数、数据流和存储层次的探索。
result: 在Zynq-7000 FPGA上的高层次综合评估表明，生成的加速器设计成功满足了时序和资源约束。
conclusion: 将LLM引入FPGA设计空间探索是可行的，能有效自动化复杂硬件的设计过程并持续优化性能。
---

## 摘要
为现代人工智能工作负载设计基于现场可编程门阵列（FPGA）的加速器，需要在一个包含架构参数、数据流策略和存储层级的庞大且复杂的硬件设计空间中进行探索，这使得该过程既耗时又耗费资源。虽然 SECDA 方法论能够通过 SystemC 仿真和 FPGA 执行实现加速器的快速软硬件协同设计，但确定最优加速器配置仍需要大量的体力劳动和领域专业知识。本研究提出了 SECDA-DSE，这是一个将大语言模型（LLM）集成到 SECDA 生态系统中的框架，包含围绕 SECDA 构建的工具，旨在自动化基于 FPGA 的加速器的设计空间探索（DSE）。SECDA-DSE 结合了一个用于生成加速器配置的结构化 DSE 探索器（DSE Explorer），以及一个利用检索增强生成和思维链提示进行推理引导探索的 LLM 栈（LLM Stack），同时还包含一个支持强化微调以实现持续改进的反馈循环。我们通过对生成的加速器设计进行初步的基于高层次综合的评估，证明了 SECDA-DSE 的可行性，该设计在 Zynq-7000 FPGA 上满足了综合时序和资源约束。

## Abstract
Designing field-programmable gate array (FPGA)-based accelerators for modern artificial intelligence workloads requires navigating a large and complex hardware design space encompassing architectural parameters, dataflow strategies, and memory hierarchies, making the process time-consuming and resource-intensive. While the SECDA methodology enables rapid hardware-software co-design of accelerators through SystemC simulation and FPGA execution, identifying optimal accelerator configurations still requires substantial manual effort and domain expertise. This work presents SECDA-DSE, a framework that integrates Large Language Models (LLMs) into the SECDA ecosystem, comprising tools built around SECDA to automate the design space exploration (DSE) of FPGA-based accelerators. SECDA-DSE combines a structured DSE Explorer for generating accelerator configurations with an LLM Stack that performs reasoning-guided exploration using retrieval-augmented generation and chain-of-thought prompting, alongside a feedback loop that enables reinforced fine-tuning for continuous improvement. We demonstrate the feasibility of SECDA-DSE through an initial high-level synthesis based evaluation of a generated accelerator design that meets synthesis timing and resource constraints on an Zynq-7000 FPGA.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-Driven Design Space Exploration of FPGA-based Accelerators》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：现代AI工作负载对FPGA加速器提出了高性能和高能效的要求。然而，FPGA的设计空间（包括架构参数、数据流策略、存储层次等）极其庞大且复杂。
*   **核心问题**：传统的设计空间探索（DSE）过程高度依赖人工经验，且高层次综合（HLS）和评估迭代周期长、资源消耗大。现有的SECDA方法虽然简化了软硬件协同设计，但寻找最优配置仍需大量手动干预。
*   **研究动机**：利用大语言模型（LLM）的推理能力来自动化DSE过程，降低对领域专家的依赖，缩短开发周期。

### 2. 论文提出的方法论：SECDA-DSE 框架
该框架集成了LLM与SECDA生态系统，主要由两个核心组件构成：
*   **DSE Explorer（探索器）**：
    *   负责根据目标AI负载、FPGA设备约束和架构指令生成硬件配置。
    *   生成符合SECDA规范的架构模板，并将其转化为可评估的硬件设计（包括源代码、RTL和FPGA映射设计）。
*   **LLM Stack（LLM 栈）**：
    *   **检索增强生成（RAG）**：通过向量数据库索引SECDA-TFLite代码库，为LLM提供必要的上下文，解决Token限制问题。
    *   **思维链（CoT）提示**：引导LLM进行多步推理，分析架构权衡，而非直接生成结果。
    *   **强化微调（LoRA）**：利用探索过程中收集的硬件数据点（延迟、资源利用率等）对模型进行低秩自适应微调，使其更偏向生成高性能且可行的配置。
    *   **反馈循环**：通过SystemC仿真和HLS评估结果建立闭环，不断优化后续的设计迭代。

### 3. 实验设计
*   **实验场景**：验证框架将自然语言规范转化为可综合硬件设计的能力。具体任务是设计一个“元素级向量乘法加速器”（$Z_i = X_i \odot Y_i$）。
*   **目标平台**：Zynq-7000 FPGA (xc7z020-clg400-1 SoC)。
*   **评估工具**：Vivado HLS 2019.2。
*   **对比/基准**：主要验证生成的代码是否能通过HLS工具链，并满足时序（200 MHz）和资源约束。

### 4. 资源与算力
*   **LLM推理**：使用了开源框架 **Ollama** 进行本地设备端推理，支持灵活切换不同的开源LLM（如Llama、Qwen）。
*   **算力细节**：文中**未明确说明**具体使用的GPU型号、数量或微调的具体时长。实验重点在于HLS综合结果的验证。

### 5. 实验数量与充分性
*   **实验规模**：目前处于初步验证阶段（Preliminary Results），主要展示了一个向量乘法算子的完整生成到综合的过程。
*   **充分性评价**：作为研讨会（Workshop）论文，其实验展示了端到端流程的可行性。但由于目前仅针对单一简单算子进行了测试，尚未在复杂的CNN或大模型（LLM）负载上进行大规模Benchmark对比，实验的广泛性和客观性仍有待后续研究补充。

### 6. 论文的主要结论与发现
*   **可行性验证**：SECDA-DSE能够成功将自然语言描述转化为符合SECDA标准的SystemC描述、软件驱动和构建集成文件。
*   **性能达标**：生成的加速器设计在200 MHz时钟约束下成功通过了HLS综合，关键路径延迟为3.950 ns，资源利用率（BRAM、DSP、LUT、FF）均在芯片物理限制内。
*   **自动化潜力**：LLM能够通过推理引导设计空间的搜索，减少无效设计的尝试。

### 7. 优点
*   **生态集成**：并非孤立使用LLM，而是将其深度嵌入成熟的SECDA软硬件协同设计流中。
*   **推理增强**：结合了RAG、CoT和LoRA微调，针对硬件设计的严谨性要求对LLM进行了多维度优化。
*   **闭环优化**：引入了硬件性能反馈机制，使LLM能够从失败或低效的设计中学习。

### 8. 不足与局限
*   **任务复杂度较低**：目前的实验仅限于简单的向量乘法，尚未证明其在处理复杂多级流水线或复杂数据流策略时的有效性。
*   **计算成本**：虽然仿真比硬件执行快，但在大规模设计空间中，频繁调用LLM和进行仿真依然存在较高的计算开销。
*   **局部最优风险**：LLM引导的推理可能陷入局部最优解，或者在未探索区域产生次优设计。
*   **数据依赖性**：RAG和微调的效果高度依赖于高质量硬件数据点的积累。

（完）
