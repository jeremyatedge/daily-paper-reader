---
title: Real Image Denoising with Knowledge Distillation for High-Performance Mobile NPUs
title_zh: 面向高性能移动端 NPU 的知识蒸馏真实图像去噪
authors: "Faraz Kayani, Sarmad Kayani, Asad Ahmed, Radu Timofte, Dmitry Ignatov"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03680v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对移动设备的NPU感知硬件算法协同设计
tldr: 本研究针对移动端NPU在图像去噪中面临的算子不兼容和内存开销问题，提出了一种硬件感知的高性能协同设计方案。通过知识蒸馏技术，将高容量教师模型的知识迁移到仅含1.96M参数的轻量级学生模型LiteDenoiseNet中。该模型完全采用NPU原生算子，在保持高保真度的同时，在联发科和高通旗舰芯片上实现了极速推理，显著缩小了轻量化模型与大型模型间的性能差距，为移动端实时高保真去噪提供了新思路。
selection_source: fresh_fetch
motivation: 深度学习图像修复模型在移动端NPU部署时，常因算子不兼容和内存访问开销导致推理效率低下。
method: 采用硬件感知协同设计，利用高比例知识蒸馏将大模型能力迁移至仅使用NPU原生算子的轻量级学生模型。
result: "LiteDenoiseNet在主流旗舰芯片上实现了毫秒级推理，并以极小的参数量恢复了教师模型99.8%的去噪质量。"
conclusion: 硬件感知的蒸馏策略是实现高保真去噪性能与移动端NPU实际部署效率统一的有效手段。
---

## 摘要
虽然基于深度学习的图像修复已达到前所未有的保真度，但在移动端神经网络处理器（NPU）上的部署仍受限于算子不兼容和内存访问开销。我们提出了一种面向 NPU 的硬件-算法协同设计方法，用于移动端 NPU 上的真实场景图像去噪。该方法采用高容量教师网络来监督轻量级学生网络，该学生网络专为利用现代移动端 SoC 的平铺内存（tiled-memory）架构而设计。通过优先使用 NPU 原生算子（标准 3x3 卷积、ReLU 激活和最近邻上采样）并采用渐进式上下文扩展策略（裁剪尺寸高达 1024x1024），该模型在 Mobile AI 2026 挑战赛的全分辨率（2432x3200）验证基准上达到了 37.66 dB PSNR / 0.9278 SSIM，在预留测试基准上达到了 37.58 dB PSNR / 0.9098 SSIM。根据挑战赛官方规则，推理运行时间在标准全高清（1088x1920）协议下测量，在联发科天玑 9500 上运行时间为 34.0 ms，在骁龙 8 Elite NPU 上为 46.1 ms。我们进一步揭示了“推理反转”（Inference Inversion）效应，即严格遵守 NPU 兼容操作可使专用 NPU 的执行速度比集成移动端 GPU 快 3.88 倍。拥有 1.96M 参数的学生网络通过高 alpha 知识蒸馏（alpha = 0.9）恢复了教师网络 99.8% 的修复质量，在实现 21.2 倍参数缩减的同时，将 PSNR 差距从 1.63 dB 缩小至仅 0.05 dB。这些结果证明，硬件感知蒸馏是将高保真去噪与跨多种移动端 NPU 架构的实际部署相结合的有效策略。所提出的轻量级学生模型（LiteDenoiseNet）及其训练统计数据已在 NN 数据集中提供，访问地址为 https://github.com/ABrain-One/NN-Dataset。

## Abstract
While deep-learning-based image restoration has achieved unprecedented fidelity, deployment on mobile Neural Processing Units (NPUs) remains bottlenecked by operator incompatibility and memory-access overhead. We propose an NPU-aware hardware-algorithm co-design approach for real-world image denoising on mobile NPUs. Our approach employs a high-capacity teacher to supervise a lightweight student network specifically designed to leverage the tiled-memory architectures of modern mobile SoCs. By prioritizing NPU-native primitives -- standard 3x3 convolutions, ReLU activations, and nearest-neighbor upsampling -- and employing a progressive context expansion strategy (up to 1024x1024 crops), the model achieves 37.66 dB PSNR / 0.9278 SSIM on the validation benchmark and 37.58 dB PSNR / 0.9098 SSIM on the held-out test benchmark at full resolution (2432x3200) in the Mobile AI 2026 challenge. Following the official challenge rules, the inference runtime is measured under a standardized Full HD (1088x1920) protocol, where it runs in 34.0 ms on the MediaTek Dimensity 9500 and 46.1 ms on the Qualcomm Snapdragon 8 Elite NPU. We further reveal an "Inference Inversion" effect, where strict adherence to NPU-compatible operations enables dedicated NPU execution up to 3.88x faster than the integrated mobile GPU. The 1.96M-parameter student recovers 99.8% of the teacher's restoration quality via high-alpha knowledge distillation (alpha = 0.9), achieving a 21.2x parameter reduction while closing the PSNR gap from 1.63 dB to only 0.05 dB. These results establish hardware-aware distillation as an effective strategy for unifying high-fidelity denoising with practical deployment across diverse mobile NPU architectures. The proposed lightweight student model (LiteDenoiseNet) and its training statistics are provided in the NN Dataset, available at https://github.com/ABrain-One/NN-Dataset.

---

## 论文详细总结（自动生成）

这篇论文针对移动端高性能 NPU 上的真实图像去噪任务，提出了一种硬件感知的知识蒸馏方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：深度学习图像去噪模型虽然在 PC 端表现优异，但在移动端 NPU（神经网络处理器）部署时面临两大瓶颈：**算子不兼容**（如自注意力、转置卷积常导致 CPU 回退）和**内存访问开销大**。
*   **研究背景**：现有的轻量化模型往往为了速度牺牲过多精度，而高性能模型又无法在移动端实时运行。作者旨在通过“硬件-算法协同设计”，在保证 NPU 原生高效运行的前提下，实现接近大模型的去噪质量。

### 2. 核心方法论
*   **硬件感知协同设计**：
    *   **教师网络（Teacher）**：高容量 U-Net 架构（41.6M 参数），使用 PReLU 激活和转置卷积，追求极致保真度（37.71 dB PSNR）。
    *   **学生网络（LiteDenoiseNet）**：超轻量级架构（1.96M 参数），严格限制算子：仅使用标准 **3x3 卷积**、**ReLU 激活**和**最近邻上采样**（避免转置卷积触发的驱动回退）。采用 16 通道基础宽度，适配移动端 SIMD 向量化执行。
*   **高 $\alpha$ 知识蒸馏**：
    *   使用总损失函数 $L_{total} = \lambda_{gt} L_{gt} + \lambda_{distill} L_{distill} + \lambda_{L1} L_{L1}$。
    *   设置 $\alpha = 0.9$（即 $\lambda_{distill}$ 权重极高），强制学生模型模拟教师模型的去噪流形，防止过拟合于原始数据中的残留噪声。
*   **渐进式上下文扩展（Progressive Context Expansion）**：
    *   训练初期使用 256x256 裁剪图，微调阶段逐步扩大至 512x512 甚至 1024x1024。这弥补了轻量级模型感受野受限的问题，提升了全分辨率下的结构一致性。

### 3. 实验设计
*   **数据集**：使用 Mobile AI 2026 图像去噪挑战赛数据集（650 对训练图，50 对验证图）。
*   **Benchmark**：
    *   **质量指标**：在全分辨率（2432x3200，约 8MP）下评估 PSNR 和 SSIM。
    *   **性能指标**：在全高清（1088x1920，FHD）分辨率下，于物理设备（骁龙 8 Elite 和天玑 9500）上测量 FP16 推理延迟。
*   **对比方法**：对比了挑战赛中其他团队（如 TeamPAK, Fanis, TLG 等）的方法，并进行了 5 个阶段的消融实验。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **时长**：完整训练和微调流程大约需要 **3-6 小时**（取决于 Batch Size 和裁剪尺寸）。
*   **部署工具**：使用 AI Edge Torch 将 PyTorch 模型转换为 TensorFlow Lite (TFLite) 格式。

### 5. 实验数量与充分性
*   **消融实验**：共进行了 5 组系统性实验，涵盖了从注意力增强基准、高容量教师模型到不同通道宽度的学生模型，以及最终的蒸馏模型。
*   **充分性评价**：实验设计较为充分。作者不仅提供了平均指标，还展示了 50 张验证图的 PSNR 差异分布（95% 置信区间），并跨高通和联发科两大主流旗舰平台进行了实测，确保了结果的客观性和跨平台公平性。

### 6. 主要结论与发现
*   **蒸馏效果显著**：1.96M 参数的学生模型恢复了教师模型 **99.8%** 的修复质量，将 PSNR 差距从 1.63 dB 缩小到 0.05 dB，同时参数量减少了 **21.2 倍**。
*   **推理反转（Inference Inversion）效应**：由于严格遵守 NPU 原生算子，模型在 NPU 上的执行速度比在移动端 GPU 上快 **2.86x 至 3.88x**。这打破了“GPU 是移动端加速首选”的传统认知。
*   **性能表现**：在天玑 9500 NPU 上仅需 34.0 ms，在骁龙 8 Elite 上需 46.1 ms，满足实时处理需求。

### 7. 优点
*   **落地导向**：并非盲目追求 SOTA 精度，而是将硬件兼容性作为第一优先级，具有极强的工业部署参考价值。
*   **协同设计思路**：通过渐进式上下文扩展解决了轻量化模型感受野不足的数学缺陷。
*   **开源贡献**：提供了 LiteDenoiseNet 模型及训练统计数据，便于社区复现。

### 8. 不足与局限
*   **算子过于保守**：为了绝对的稳定性，完全放弃了可能提升性能的复杂算子（如动态卷积或轻量化注意力），在未来更先进的 NPU 驱动下可能显得过于保守。
*   **分辨率差异**：推理延迟是在 FHD 下测量的，而质量评估是在 8MP 下进行的，虽然符合挑战赛规则，但未直接给出 8MP 下的端到端实时延迟。
*   **数据集规模**：训练集仅 650 对图像，对于真实场景的泛化能力仍有待在更大规模、更多样化的传感器数据上验证。

（完）
