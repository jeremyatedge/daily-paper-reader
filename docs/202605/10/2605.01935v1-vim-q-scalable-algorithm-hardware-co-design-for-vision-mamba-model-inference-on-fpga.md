---
title: "ViM-Q: Scalable Algorithm-Hardware Co-Design for Vision Mamba Model Inference on FPGA"
title_zh: ViM-Q：面向 FPGA 上 Vision Mamba 模型推理的可扩展算法-硬件协同设计
authors: "Shengzhe Lyu, Yuhan She, Patrick S. Y. Hung, Ray C. C. Cheung, Weitao Xu"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01935v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对边缘端FPGA的Vision Mamba推理算法-硬件协同设计
tldr: Vision Mamba (ViM) 模型因线性复杂度在视觉任务中表现出色，但在 FPGA 部署上面临量化困难和内存访问不匹配等挑战。本文提出 ViM-Q，一种软硬协同设计方案。算法上采用动态逐 token 激活量化与 4-bit APoT 权重量化；硬件上设计了可运行时配置的加速器，包含基于查找表的线性引擎和细粒度流水线 SSM 引擎。在 ZCU102 FPGA 上，ViM-Q 相比 RTX 3090 GPU 实现了显著的加速和能效提升，为边缘设备部署 ViM 提供了可行路径。
selection_source: fresh_fetch
motivation: 针对 Vision Mamba 模型在 FPGA 部署时面临的激活值离群点、低比特量化精度损失以及 SSM 内存访问不匹配等问题。
method: 提出一种结合动态激活量化、APoT 权重量化与可运行时配置 FPGA 加速器的软硬协同设计方案。
result: 在 AMD ZCU102 FPGA 上，ViM-tiny 推理速度比量化后的 RTX 3090 GPU 快 4.96 倍，能效提升 59.8 倍。
conclusion: 该研究证明了通过软硬协同优化，可以在资源受限的边缘设备上高效实现 Vision Mamba 模型推理。
---

## 摘要
Vision Mamba (ViM) 模型通过利用状态空间模型 (SSM) 的线性复杂度，展现出优于 Transformer 的显著效率优势，但在 FPGA 上实现高效部署仍具挑战。线性层难以应对动态激活离群值，导致静态量化失效，而均匀量化在低位宽下无法有效捕捉权重分布。此外，虽然关联扫描在 GPU 上能加速 SSM，但其内存访问模式与 FPGA 所需的流式数据流不匹配。为解决这些挑战，我们提出了 ViM-Q，一种用于边缘端端到端 ViM 推理的可扩展算法-硬件协同设计。我们引入了一种硬件感知的量化方案，结合动态逐 token 激活量化和逐通道平滑以缓解离群值影响，并采用了定制的 4 位逐块加法幂次 (APoT) 权重量化。模型部署在运行时可参数化的 FPGA 加速器上，该加速器包含一个利用查找表 (LUT) 单元将乘法替换为移位加法操作的线性引擎，以及一个在保持顺序递归的同时并行化状态维度的细粒度流水线 SSM 引擎。关键在于，该硬件支持运行时配置，能够适应 ViM 系列中不同的维度和输入分辨率。在 AMD ZCU102 FPGA 上的实验表明，对于 ViM-tiny 的低批次推理，ViM-Q 相比量化后的 NVIDIA RTX 3090 GPU 基准实现了平均 4.96 倍的加速和 59.8 倍的能效提升。该协同设计为在资源受限的边缘设备上部署 ViM 模型提供了一条可行路径。

## Abstract
Vision Mamba (ViM) models offer a compelling efficiency advantage over Transformers by leveraging the linear complexity of State Space Models (SSMs), yet efficiently deploying them on FPGAs remains challenging. Linear layers struggle with dynamic activation outliers that render static quantization ineffective, while uniform quantization fails to capture the weight distribution at low bit-widths. Furthermore, while associative scan accelerates SSMs on GPUs, its memory access patterns are misaligned with the streaming dataflow required by FPGAs. To address these challenges, we present ViM-Q, a scalable algorithm-hardware co-design for end-to-end ViM inference on the edge. We introduce a hardware-aware quantization scheme combining dynamic per-token activation quantization and per-channel smoothing to mitigate outliers, alongside a custom 4-bit per-block Additive Power-of-Two (APoT) weight quantization. The models are deployed on a runtime-parameterizable FPGA accelerator featuring a linear engine employing a Lookup-Table (LUT) unit to replace multiplications with shift-add operations, and a fine-grained pipelined SSM engine that parallelizes the state dimension while preserving sequential recurrence. Crucially, the hardware supports runtime configuration, adapting to diverse dimensions and input resolutions across the ViM family. Implemented on an AMD ZCU102 FPGA, ViM-Q achieves an average 4.96x speedup and 59.8x energy efficiency gain over a quantized NVIDIA RTX 3090 GPU baseline for low-batch inference on ViM-tiny. This co-design shows a viable path for deploying ViM models on resource-constrained edge devices.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ViM-Q**，这是一个针对 Vision Mamba (ViM) 模型在边缘端 FPGA 上进行高效推理的算法-硬件协同设计框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：Transformer 模型在视觉任务中表现优异，但其自注意力机制的计算复杂度随序列长度呈平方增长。状态空间模型（SSM），特别是 Vision Mamba (ViM)，凭借线性时间复杂度和强大的表征能力成为替代方案。
*   **核心挑战**：
    1.  **量化困难**：ViM 存在动态激活离群值（Outliers），传统的静态量化会导致精度大幅下降；且低比特下的均匀量化无法有效捕捉权重分布。
    2.  **硬件不匹配**：GPU 常用的关联扫描（Associative Scan）算法在 FPGA 上会产生严重的内存访问冲突，不符合 FPGA 的流式数据流需求。
    3.  **可扩展性需求**：ViM 家族有多种尺寸和分辨率，硬件需要具备运行时可重构能力以适应不同配置。

### 2. 方法论
ViM-Q 采用了**算法-硬件协同设计**的方法：
*   **硬件感知量化方案（算法层）**：
    *   **动态逐 Token 激活量化**：实时计算每个 Token 的绝对最大值以确定缩放因子，有效应对动态离群值。
    *   **逐通道平滑（Smoothing）**：通过离线缩放权重和激活值，将量化难度从激活转移到权重。
    *   **4-bit 逐块 APoT 权重量化**：采用加法幂次（Additive Power-of-Two）非均匀量化，将权重限制为 2 的幂次之和，从而在硬件上用移位加法代替昂贵的乘法。
*   **可扩展 FPGA 加速器架构（硬件层）**：
    *   **统一线性引擎**：利用查找表（LUT）预计算技术。针对每个输入 Tile 预计算 8 种可能的移位结果，PE 单元只需根据 4-bit 权重索引进行查表和累加，消除了冗余计算。
    *   **细粒度流水线 SSM 引擎**：放弃关联扫描，采用三阶段宏流水线（状态更新、状态投影、融合输出）。在保持 Token 顺序递归的同时，在状态维度（State Dimension）上实现高度空间并行。
    *   **运行时参数化**：支持在不重新编译硬件的情况下，通过控制参数调整特征维度、补丁数量等，适配 ViM-tiny/small/base 等不同变体。

### 3. 实验设计
*   **数据集**：ImageNet-1k 图像分类基准。
*   **模型变体**：ViM-tiny (ViM-t), ViM-small (ViM-s), ViM-base (ViM-b)。
*   **Benchmark（基准）**：
    *   **算法对比**：对比了 FP16 基准、均匀 W8A8 量化、标准幂次（PoT）量化以及不同粒度（逐通道 vs 逐块）的量化效果。
    *   **硬件对比**：对比了 NVIDIA RTX 3090 GPU（包括 FP16 和量化后的 INT8 版本）。
*   **评估指标**：Top-1/Top-5 准确率、推理延迟（Latency）、功耗（Power）、能效比（Energy Efficiency）。

### 4. 资源与算力
*   **FPGA 平台**：AMD ZCU102 开发板（16nm 工艺），工作频率 350 MHz。
*   **GPU 平台**：NVIDIA RTX 3090（8nm 工艺），使用 CUDA 融合算子加速 SSM。
*   **算力消耗**：FPGA 动态功耗约 11.96W，静态功耗 0.81W。GPU 功耗测量排除了 50W 的闲置板载功耗以确保公平。
*   **训练/量化时长**：论文未详细说明 PTQ（后训练量化）的具体耗时，但通常 PTQ 仅需少量校准数据，计算开销极低。

### 5. 实验数量与充分性
*   **实验覆盖面**：
    *   涵盖了 ViM 家族的三种主要尺寸。
    *   进行了详尽的**消融实验**，验证了激活平滑、动态量化、APoT 权重、预移位技术等每个组件对精度和硬件性能的贡献。
    *   进行了**设计空间探索**，研究了不同位宽（3/4/5 bit）和块大小（16/32/64）对精度的影响。
*   **充分性评价**：实验设计较为充分，不仅有端到端的性能对比，还有针对单个线性引擎优化阶段的细化分析。对比 GPU 基准时考虑了不同输入分辨率（96 到 256），证明了其在低批次（Batch size = 1）边缘场景下的优势。

### 6. 主要结论与发现
*   **精度保持**：ViM-Q 在 W4A8 精度下表现稳健。ViM-t 达到 74.2% Top-1 准确率，ViM-b 仅比 FP16 下降 0.21%。
*   **性能优势**：在 ViM-t 模型上，FPGA 相比量化后的 RTX 3090 GPU 实现了 **4.96 倍的平均加速**和 **59.8 倍的能效提升**。
*   **硬件效率**：LUT 预计算技术显著降低了逻辑资源消耗（约 77K LUTs），同时通过内存对齐的权重重排消除了非顺序访问延迟。

### 7. 优点
*   **软硬协同深度高**：APoT 量化与硬件 LUT 引擎的结合非常巧妙，真正实现了“以移位代乘法”。
*   **解决动态离群值**：通过硬件内置的流式动态量化器，在不增加延迟的前提下解决了 ViM 部署中最棘手的精度退化问题。
*   **灵活性强**：支持运行时配置，解决了 FPGA 部署中常见的“一模型一编译”的痛点。

### 8. 不足与局限
*   **工艺代差**：实验使用了较旧的 16nm FPGA 与 8nm GPU 对比，虽然结果占优，但若能对比同代工艺的边缘 ASIC 或更先进的 FPGA 会更具说服力。
*   **任务局限**：目前仅在 ImageNet 分类任务上进行了验证，尚未展示在目标检测或语义分割等更长序列任务中的表现。
*   **SSM 精度**：为了避免递归误差累积，SSM 模块仍保留在较高精度（未完全量化到 4-bit），这在一定程度上限制了存储空间的进一步压缩。

（完）
