---
title: "CoVSpec: Efficient Device-Edge Co-Inference for Vision-Language Models via Speculative Decoding"
title_zh: CoVSpec：通过投机解码实现视觉语言模型的高效端边协同推理
authors: "Yuanyuan Jia, Shunpu Tang, Qianqian Yang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02218v1"
tags: ["query:aiot"]
score: 9.0
evidence: 视觉语言模型的端云协同推理
tldr: 针对视觉语言模型（VLM）在移动端部署受限的问题，本文提出CoVSpec框架，通过端云协同的投机解码实现高效推理。该框架引入免训练的视觉Token裁剪、自适应草稿策略及并行分支机制，在保证模型准确率的前提下，显著提升了推理吞吐量并大幅降低了端云间的通信开销。
selection_source: fresh_fetch
motivation: 现有的端云协同投机解码在处理VLM时，面临视觉Token计算负担重和通信开销过高导致的效率瓶颈。
method: 提出了一种包含视觉Token裁剪、自适应草稿长度调整以及解耦验证纠错的并行分支机制的协同推理框架。
result: "实验显示CoVSpec比仅在边缘端推理的吞吐量提升高达2.21倍，且通信开销较基准方案降低了96%以上。"
conclusion: CoVSpec通过优化视觉Token处理和协同机制，为在资源受限的移动设备上高效部署大型VLM提供了有效的解决方案。
---

## 摘要
视觉语言模型（VLM）在多模态感知和推理方面展现出了强大的能力。然而，由于巨大的计算和内存需求，在移动设备上部署大型 VLM 仍然具有挑战性。一种实际的替代方案是端边协同推理，即移动设备上的轻量级草稿 VLM 通过投机解码与边缘服务器上较大的目标 VLM 进行协作。然而，由于过多的视觉 Token 计算和高昂的通信开销，直接将投机解码扩展到 VLM 会导致严重的效率低下。为了应对这些挑战，我们提出了 CoVSpec，这是一个用于 VLM 推理的高效协作投机解码框架。具体而言，我们首先开发了一个无需训练的视觉 Token 缩减框架，通过综合考虑查询相关性、Token 活跃度和低秩依赖性，剪枝移动设备上的冗余视觉 Token。此外，我们设计了一种自适应草稿策略，动态调整验证频率和草稿长度。此外，我们引入了一种具有解耦验证-修正功能的并行分支机制，以提高目标端验证期间草稿端的利用率，并减少与修正相关的传输开销。在多个基准测试上的实验表明，CoVSpec 的吞吐量比仅目标端推理高出多达 2.21 倍，且与基准方案相比，在不损失任务准确性的情况下，通信开销降低了 96% 以上。

## Abstract
Vision-language models (VLMs) have demonstrated strong capabilities in multimodal perception and reasoning. However, deploying large VLMs on mobile devices remains challenging due to their substantial computational and memory demands. A practical alternative is device-edge co-inference, where a lightweight draft VLM on the mobile device collaborates with a larger target VLM on the edge server via speculative decoding. Nevertheless, directly extending speculative decoding to VLMs suffers from severe inefficiency due to excessive visual-token computation and high communication overhead. To address these challenges, we propose CoVSpec, an efficient collaborative speculative decoding framework for VLM inference. Specifically, we first develop a training-free visual token reduction framework that prunes redundant visual tokens on the mobile device by jointly considering query relevance, token activity, and low-rank dependency. Moreover, we design an adaptive drafting strategy that dynamically adjusts both the verification frequency and the draft length. In addition, we introduce a parallel branching mechanism with decoupled verification-correction to improve draft-side utilization during target-side verification and reduce correction-related transmission overhead. Experiments on multiple benchmarks show that CoVSpec achieves up to 2.21x higher throughput than target-only inference and reduces communication overhead by more than 96% compared with baselines, without compromising task accuracy.

---

## 论文详细总结（自动生成）

以下是对论文《CoVSpec: Efficient Device-Edge Co-Inference for Vision-Language Models via Speculative Decoding》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：视觉语言模型（VLM）虽然强大，但在移动设备上部署面临两难：小型模型推理能力不足，大型模型计算和内存开销过大。
*   **核心问题**：现有的“端-边协同推理”（Device-Edge Co-Inference）通常采用投机解码（Speculative Decoding），但直接应用于VLM时存在两大瓶颈：
    1.  **视觉Token冗余**：大量的视觉Token导致移动端计算负担沉重。
    2.  **通信开销高**：频繁的端边验证和修正导致无线链路上的数据传输延迟，甚至可能使协同推理速度慢于纯服务器推理。

### 2. 核心方法论：CoVSpec 框架
CoVSpec 的核心思想是让移动端草稿模型在**精简的视觉Token集**上运行，而边缘端目标模型在**全量Token**上验证，并通过自适应机制优化通信。

*   **免训练视觉Token缩减**：
    *   **查询感知评分（Query-Aware）**：利用文本查询中的关键词与视觉Token的余弦相似度，筛选语义相关的Token。
    *   **活跃度评分（Activity-Aware）**：测量视觉Token在预填充层中的层间变化幅度，变化大的Token被认为对预测贡献更大。
    *   **低秩冗余消除**：对初步筛选的Token进行奇异值分解（SVD），保留子空间能量最大的代表性Token。
*   **自适应草稿策略**：
    *   **置信度门控（Margin-Based Gating）**：根据草稿模型输出的前两名概率差值判断不确定性，高置信度时跳过边缘验证，减少交互。
    *   **动态草稿长度调整**：基于历史接受率的指数移动平均（EMA）和当前信道状况，动态缩放草稿长度。
*   **并行分支与解耦验证-修正**：
    *   **并行分支**：在边缘端验证时，移动端预先计算可能的后续分支，减少等待空闲。
    *   **解耦修正**：将残差采样（Correction Sampling）从边缘端移至移动端，边缘端仅返回目标Logits，大幅降低上行链路的Logits传输开销。

### 3. 实验设计
*   **数据集**：VQAv2（视觉问答）、MMMU（多模态理解）、MMBench（综合基准）。
*   **模型配置**：草稿模型为 InternVL2.5-4B，目标模型为 InternVL2.5-78B（INT8量化）。
*   **对比基准（Benchmarks）**：
    *   **Edge-only**：仅边缘服务器推理。
    *   **Device-only**：仅移动端推理。
    *   **Vanilla SD**：标准端边投机解码。
    *   **U-HLM**：一种现有的不确定性感知混合推理框架。

### 4. 资源与算力
*   **移动端（Device）**：配备 **Apple M5 芯片** 的 MacBook Pro。
*   **边缘端（Edge Server）**：配备 **2 张 NVIDIA RTX 4090 GPU**。
*   **网络环境**：带宽 B = 5 MHz，信噪比 SNR = 10 dB。
*   **训练时长**：由于该方法是**免训练（Training-free）**的，因此不涉及模型训练算力，主要关注推理阶段的延迟和吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上各随机抽取 50 个样本，每个样本生成 1024 个新 Token。
*   **消融实验**：针对视觉Token缩减、门控机制、自适应长度、并行分支、解耦修正等每个组件都进行了详细的消融对比。
*   **评价指标**：涵盖了加速比（Speedup）、通信开销（Comm.）、准确率（Acc.）和 API 成本降低率（Cost Red.）。
*   **充分性评价**：实验设计较为全面，涵盖了性能、成本和通信三个维度，对比了多种 SOTA 方法，实验结果具有较强的说服力。但样本量（每数据集50个）相对较小，可能存在一定的统计偏差。

### 6. 主要结论
*   **性能提升**：CoVSpec 在 VQAv2 上实现了高达 **2.21倍** 的加速比（相对于纯边缘推理）。
*   **通信优化**：相比传统投机解码，通信开销降低了 **96% 以上**（从数百MB降至20MB以下）。
*   **成本与精度**：在保持与大模型相当的准确率前提下，推理成本降低了约 15%~30%。
*   **视觉缩减有效性**：证明了即使草稿模型只使用 64/768 个视觉 Token，也能通过投机解码机制维持高精度。

### 7. 优点
*   **系统性优化**：不仅关注算法层面的加速，还深入考虑了无线通信环境和端边算力分配的实际约束。
*   **免训练特性**：无需对现有 VLM 进行微调，具有很强的通用性和易部署性。
*   **通信感知**：引入信道状况作为草稿长度调整的因子，更符合移动边缘计算的实际场景。

### 8. 不足与局限
*   **样本覆盖面**：实验样本量较小（每数据集50例），可能无法完全覆盖长尾场景。
*   **模型依赖**：实验仅基于 InternVL 系列模型，尚未验证在其他架构（如 LLaVA 或 Qwen-VL）上的泛化表现。
*   **硬件特定性**：移动端使用了性能极强的 M5 芯片，在更低端的移动设备（如中低端手机）上的表现尚待观察。

（完）
