---
title: Resource Utilization of Differentiable Logic Gate Networks Deployed on FPGAs
title_zh: 部署在 FPGA 上的可微分逻辑门网络的资源利用率研究
authors: "Stephen Wormald, Gilon Kravatsky, Damon Woodard, Domenic Forte"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.04109v1"
tags: ["query:aiot"]
score: 10.0
evidence: FPGA资源利用与功耗感知逻辑门网络
tldr: 本研究探讨了可微分逻辑门网络（LGN）在FPGA上的硬件实现特性。针对边缘机器学习对低延迟和低功耗的需求，LGN虽能提供纳秒级推理，但其架构参数与硬件资源间的权衡尚不明确。本文通过实验分析了LGN深度和宽度对功耗、资源利用率及速度的影响，发现最后一层规模是优化关键，并为工程师提供了在特定LUT限制下选择最优架构的指导。
selection_source: fresh_fetch
motivation: 旨在解决边缘机器学习中模型智能化与电路小型化、低功耗之间的矛盾，填补LGN架构参数与FPGA硬件性能权衡关系的空白。
method: 通过在FPGA上综合不同深度和宽度的LGN模型，定量研究其在功耗、资源占用、推理速度和准确度方面的表现。
result: "实验表明LGN的最后一层是决定时序和资源消耗的关键（可降低28%资源），较窄的末层允许构建更深更宽的网络。"
conclusion: 研究揭示了LGN架构设计的关键准则，为在受限的FPGA资源下优化逻辑门网络性能提供了重要的工程参考。
---

## 摘要
边缘机器学习（ML）通常力求在最小化推理所需的电路尺寸和功耗的同时，最大化小型模型的智能程度。为了满足这些需求，可微分逻辑门网络（LGN）已展示出纳秒级的预测速度，且与传统的二值神经网络相比，减少了所需的资源。尽管有这些优势，但 LGN 参数与最终硬件综合特性之间的权衡关系尚未得到充分表征。因此，本文研究了在改变为现场可编程门阵列（FPGA）综合的 LGN 深度和宽度时，功耗、资源利用率、推理速度和模型准确性之间的权衡。结果表明，LGN 的最后一层对于最小化时序和资源消耗（即减少 28%）至关重要，因为该层决定了求和操作的逻辑规模。在受时序和布线约束的情况下，当最后一层较窄时，可以为 FPGA 综合更深、更宽的 LGN。本文还提出了进一步的权衡方案，以帮助机器学习工程师为具有固定数量查找表（LUT）的 FPGA 选择基准 LGN 架构。

## Abstract
On-edge machine learning (ML) often strives to maximize the intelligence of small models while miniaturizing the circuit size and power needed to perform inference. Meeting these needs, differentiable Logic Gate Networks (LGN) have demonstrated nanosecond-scale prediction speeds while reducing the required resources as compares to traditional binary neural networks. Despite these benefits, the trade-offs between LGN parameters and resulting hardware synthesis characteristics are not well characterized. This paper therefore studies the tradeoffs between power, resource utilization, inference speed, and model accuracy when varying the depth and width of LGNs synthesized for Field Programmable Gate Arrays (FPGA). Results reveal that the final layer of an LGN is critical to minimize timing and resource usage (i.e. 28\% decrease), as this layer dictates the logic size of summing operations. Subject to timing and routing constraints, deeper and wider LGNs can be synthesized for FPGA when the final layer is narrow. Further tradeoffs are presented to help ML engineers select baseline LGN architectures for FPGAs with a set number of Look Up Tables (LUT).

---

## 论文详细总结（自动生成）

这是一份关于论文《Resource Utilization of Differentiable Logic Gate Networks Deployed on FPGAs》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然可微分逻辑门网络（LGN）在边缘机器学习中展现出纳秒级推理速度和极高的资源效率，但其架构参数（如深度、宽度、位精度）与FPGA硬件指标（如查找表LUT占用、功耗、延迟）之间的定量权衡关系尚不明确。
*   **研究动机**：工程师在将LGN部署到FPGA时，缺乏选择网络参数的指导准则。本文旨在填补这一空白，系统研究LGN设计变量与硬件性能之间的关系，并提供一套选择基准架构的方法论。

### 2. 方法论
*   **核心思想**：通过改变LGN的层宽（LW）、层深（LD）以及引入“端盖（Cap）”策略，观察其对硬件资源的影响。
*   **关键技术细节**：
    *   **资源分解**：将FPGA资源占用分为输入流逻辑（$LUT_{input}$）、组合决策逻辑（$LUT_{logic}$）和分类求和逻辑（$LUT_{sum}$）。
    *   **架构变体**：
        *   **Baseline**：恒定层宽。
        *   **Front Cap**：在输入端添加窄层。
        *   **End Cap**：在输出端添加窄层（减少求和操作的规模）。
        *   **Dual Cap**：同时添加前后窄层。
    *   **转换流程**：使用DiffLogic库训练模型，通过Xilinx Vitis HLS将模型转换为C++及Verilog HDL，最后在Vivado中进行综合与布线。

### 3. 实验设计
*   **数据集**：MNIST、FashionMNIST、CIFAR10。
*   **参数范围**：
    *   位深度（b）：1至8。
    *   层深（LD）：1至8。
    *   层宽（LW）：1,000至64,000。
*   **硬件平台**：Xilinx Alveo U200（包含XCU200 FPGA，拥有89.2万个LUT）。
*   **对比指标**：准确率、综合LUT/FF使用量、硬件功耗、计算周期数（Latency）。

### 4. 资源与算力
*   **训练算力**：文中提到使用Adam优化器，批大小为64，学习率为0.01，并采用了早停机制。**未明确说明具体的GPU型号或训练总时长**。
*   **硬件综合算力**：实验在Alveo U200加速卡上进行，由于模型巨大，部分大模型（$L_{end} > 100,000$ 门）在预综合阶段失败或耗时极长。

### 5. 实验数量与充分性
*   **实验规模**：共训练并综合了**390个LGN变体模型**（包括280个基准模型和110个带“盖”的模型）。其中122个模型成功完成了完整的硬件布线并用于功耗分析。
*   **充分性评价**：实验覆盖了从小型到超大型的LGN架构，样本量充足。通过5折交叉验证确保了准确率的可靠性。实验设计考虑了SSI（堆叠硅片互连）技术带来的布线拥塞问题，具有较高的工程实践参考价值。

### 6. 主要结论与发现
*   **末层关键性**：LGN的最后一层（$L_{end}$）是决定资源占用和时序的关键。通过使用**End Cap（末层减窄）**，可以在准确率仅损失0-2%的情况下，减少约**28%的LUT占用**和**10%的寄存器（FF）占用**，并缩短1.4至4个时钟周期。
*   **深度与资源无关**：层深（LD）对资源占用和延迟几乎没有影响，因为LGN主要由组合逻辑实现。这意味着增加深度是提升准确率的“廉价”手段。
*   **功耗波动小**：在所研究的范围内，不同规模LGN的功耗差异小于2W（约12.5W-13.5W），在FPGA设计中功耗约束相对宽松。
*   **位深度影响**：较大的位深度（b）往往导致资源消耗剧增，但并不一定能显著提升准确率。

### 7. 优点
*   **工程指导性强**：首次提出了针对FPGA资源限制（LUT数量）来反推LGN架构参数的系统化方法论。
*   **发现瓶颈**：精准识别出求和操作（$LUT_{sum}$）是LGN硬件化的主要资源瓶颈。
*   **实用策略**：提出的“End Cap”策略为在小型FPGA上部署大型智能模型提供了可行路径。

### 8. 不足与局限
*   **平台局限性**：实验数据高度依赖于Alveo U200和Xilinx工具链，不同厂商或不同架构的FPGA（如LUT3 vs LUT4）可能产生不同的资源映射结果。
*   **数据集局限**：在CIFAR10等复杂数据集上，前馈式LGN的准确率仍然较低，限制了其在高复杂度视觉任务中的应用。
*   **功耗评估**：功耗数据来源于Vivado仿真工具而非实测，且未深入探讨不同时钟频率对功耗的动态影响。

（完）
