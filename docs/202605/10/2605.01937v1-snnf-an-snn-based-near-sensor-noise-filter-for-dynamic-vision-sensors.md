---
title: "SNNF: An SNN-based Near-Sensor Noise Filter for Dynamic Vision Sensors"
title_zh: SNNF：一种基于 SNN 的动态视觉传感器近传感器噪声滤波器
authors: "Yahan Yang, Pradeep Kumar Gopalakrishnan, Chang Chip Hong, Arindam Basu"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01937v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于低功耗边缘视觉的近传感器 SNN 噪声滤波器
tldr: 针对动态视觉传感器（DVS）中背景活动（BA）噪声导致的计算开销问题，本文提出了一种名为SNNF的近传感器噪声滤波器。该方案结合了紧凑的事件二进制图像表示、并行存储架构和单层脉冲神经网络（SNN）分类器。SNNF通过脉冲计算取代了高能耗的乘法器，显著降低了硬件资源消耗。实验表明，SNNF在保持高过滤精度的同时，大幅减少了内存和功耗，非常适合资源受限的边缘部署。
selection_source: fresh_fetch
motivation: 动态视觉传感器产生的背景活动噪声会增加不必要的计算负担，亟需一种高效的近传感器过滤方案。
method: 提出一种基于单层脉冲神经网络的滤波器，利用事件二进制图像表示和并行存储架构实现低功耗、无时间戳依赖的信号识别。
result: 在FPGA和ASIC实现中，SNNF的资源占用和功耗仅为现有方案的极小比例，同时实现了高达44.4 Meps的吞吐量。
conclusion: SNNF在过滤精度与硬件效率之间取得了卓越平衡，是资源受限环境下近传感器部署的理想选择。
---

## 摘要
动态视觉传感器 (DVS) 具有卓越的动态范围和低功耗特性，使其成为视频物联网 (IoVT) 边缘应用的理想选择。然而，其输出经常受到伪背景活动 (BA) 噪声的影响，导致不必要的计算开销。本文提出了 SNNF，这是一种近传感器 BA 噪声滤波器，它集成了紧凑的基于事件的二值图像 (EBBI) 表示、并行存储架构和单层脉冲神经网络 (SNN) 分类器。在代表性 DVS 数据上训练后，该 SNN 在标准数据集上区分信号事件与噪声的 AUC 达到 0.89。基于二值数组的 EBBI 消除了对时间戳的依赖，显著减少了内存占用。此外，SNN 基于脉冲的计算用简单的累加逻辑取代了高功耗的乘法器，并最小化了神经元间的数据宽度，从而实现了极高的硬件效率。FPGA 实现结果表明，SNNF 的内存和逻辑资源分别减少到现有先进滤波器的约 11% 和 40%，同时实现了每秒 2900 万个事件 (Meps) 的吞吐量。在 65 nm CMOS ASIC 实现中，SNNF 达到了 44.4 Meps，其面积和功耗仅为相应基于人工神经网络 (ANN) 设计的约 13% 和不到 5%。这些结果表明，SNNF 在滤波精度和硬件效率之间取得了极佳的平衡，非常适合资源受限的近传感器部署。

## Abstract
Dynamic Vision Sensors (DVS) exhibit exceptional dynamic range and low power consumption, making them ideal for edge applications in the Internet of Video Things (IoVT). However, their output is often degraded by spurious Background Activity (BA) noise, leading to unnecessary computational overhead. This paper proposes SNNF, a near-sensor BA noise filter that integrates a compact Event-Based Binary Image (EBBI) representation, a parallel memory architecture, and a single-layer Spiking Neural Network (SNN) classifier. Trained on representative DVS data, the SNN distinguishes signal events from noise with an AUC of 0.89 on standard datasets. The binary-array-based EBBI eliminates timestamp dependency, significantly reducing memory footprint. Moreover, the SNN's spike-based computation replaces power-hungry multipliers with simple accumulation logic and minimizes inter-neuron data width, resulting in an extremely hardware-efficient design. FPGA implementation results show that SNNF reduces memory and logic resources to approximately 11% and 40%, respectively of state-of-the-art filters, while achieving a throughput of 29 Mega events per second (Meps). In a 65 nm CMOS ASIC implementation, SNNF achieves 44.4 Meps with an area and power consumption of only ~13% and <5% of the corresponding ANN-based designs. These results demonstrate that SNNF provides an excellent balance between filtering accuracy and hardware efficiency, making it highly suitable for resource-constrained, near-sensor deployment.

---

## 论文详细总结（自动生成）

以下是对论文《SNNF: An SNN-based Near-Sensor Noise Filter for Dynamic Vision Sensors》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
动态视觉传感器（DVS）因其高动态范围和低功耗特性，被广泛用于边缘计算和视频物联网（IoVT）。然而，DVS 容易产生**背景活动（BA）噪声**（由传感器物理缺陷如漏电流引起），这些伪事件会占用宝贵的带宽并增加下游处理的计算负担。
*   **核心挑战**：现有的滤波器（如 STCF 或基于 ANN 的 MLPF）要么精度不足，要么内存占用过高（需存储高位宽时间戳），难以在资源极度受限的“近传感器”（Near-Sensor）环境中部署。
*   **研究目标**：开发一种既能保持高过滤精度，又能极度压缩内存和功耗的硬件友好型滤波器。

### 2. 核心方法论
论文提出了 **SNNF**（基于脉冲神经网络的滤波器），其核心思想结合了数据表示的简化与计算架构的革新：
*   **EBBI（事件二值图像）表示**：放弃存储 16-32 位的原始时间戳，转而使用 1 位的二值图像。通过固定时间窗口（如 25ms）或固定事件数累积事件，仅记录像素是否有事件发生。
*   **并行存储架构**：为了解决局部块（Patch）提取的瓶颈，设计了多行并行存储库（Memory Banks）。将图像划分为 $n$ 个库，支持在 2 个时钟周期内提取 $5 \times 5$ 的时空特征块。
*   **单层脉冲神经网络（SNN）**：
    *   使用 **LIF（Leaky Integrate-and-Fire）** 神经元模型。
    *   **计算优势**：利用脉冲的二值特性，将传统神经网络中的高能耗乘法器替换为简单的**累加逻辑**。
    *   **架构**：输入层接收扁平化的 EBBI 特征，隐藏层包含约 30 个神经元，输出层进行二分类（信号或噪声）。
*   **流水线设计**：硬件实现分为地址生成、块提取、SNN 处理和分类四个阶段，实现了确定的低延迟（9 个时钟周期）。

### 3. 实验设计
*   **数据集**：
    1.  **DND21 Driving**：模拟车载摄像头在城市行驶的场景，混合了真实的漏电流噪声和散粒噪声。
    2.  **Hotel-bar**：典型的监控场景数据集。
*   **Benchmark（基准）**：对比了多种硬件友好型滤波器，包括 BAF、STCF、MLPF（基于 ANN）、ONF、HashHeat 和 HAST。
*   **评估指标**：AUC（ROC 曲线下面积）、内存占用（KB）、每事件能耗（pJ）、吞吐量（Meps）。

### 4. 资源与算力
*   **训练阶段**：使用 Python 的 `snnTorch` 框架进行训练。**论文未明确说明具体的 GPU 型号、数量或训练时长**，但考虑到单层 SNN 规模极小（仅 30 个隐藏神经元），其训练算力需求极低。
*   **硬件实现**：
    *   **FPGA**：部署在 Xilinx Zynq XC7Z100 和 Zynq UltraScale+ ZU3CG 上。
    *   **ASIC**：采用 **TSMC 65nm CMOS** 工艺进行后端设计和仿真。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的超参数搜索（消融实验），包括 EBBI 堆栈数量（$N_{EBBI}$）、EBBI 生成触发方式（时间 vs 事件数）、SNN 隐藏层神经元数量、权重位宽量化等。
*   **充分性评价**：实验设计较为充分。通过在不同场景（驾驶 vs 监控）下验证，证明了方法的通用性。对比实验涵盖了从传统启发式算法到现代深度学习滤波器的全频谱，对比维度包括算法精度和底层硬件指标，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **精度领先**：SNNF 在驾驶数据集上的 AUC 达到 **0.89**，优于基于 ANN 的 MLPF（0.87）和传统的 STCF（0.82）。
*   **硬件效率极高**：
    *   **内存**：仅需 MLPF 内存的 **11%** 左右（得益于 1-bit EBBI）。
    *   **功耗**：ASIC 实现功耗仅 **1.48 mW**，不到 MLPF 的 5%。
    *   **吞吐量**：在 ASIC 上达到 **44.4 Meps**，足以处理高分辨率 DVS 的高速事件流。
*   **结论**：SNN 结合二值图像表示是实现近传感器实时去噪的最优权衡方案。

### 7. 优点（亮点）
*   **极简的数据表示**：通过 1-bit EBBI 彻底解决了 DVS 滤波器中时间戳存储导致的内存瓶颈。
*   **计算与存储深度耦合**：并行存储库设计巧妙解决了 Patch 提取的带宽问题，使系统能以单周期吞吐运行。
*   **真正的脉冲优势**：不同于许多仅在算法层面讨论 SNN 的论文，本文通过 ASIC 布局布线展示了脉冲计算在消除乘法器、降低面积和功耗方面的真实收益。

### 8. 不足与局限
*   **时间精度损失**：EBBI 丢弃了精确的时间戳信息，虽然 SNN 通过时空累积弥补了这一点，但在极高动态或需要微秒级时序特征的特定任务中，可能会存在性能上限。
*   **内存线性扩展**：虽然每像素仅 1-bit，但总内存仍随传感器分辨率线性增长。对于超高分辨率（如 8K）传感器，可能仍需进一步优化。
*   **噪声类型限制**：主要针对背景活动（BA）噪声，未讨论对其他类型干扰（如光照剧烈变化引起的伪事件）的抑制效果。

（完）
