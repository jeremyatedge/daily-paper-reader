---
title: "On-Device Vision Training, Deployment, and Inference on a Thumb-Sized Microcontroller"
title_zh: 拇指大小微控制器上的设备端视觉训练、部署与推理
authors: Jeremy Ellis
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.23012v1"
tags: ["query:aiot"]
score: 9.0
evidence: 微控制器上的端侧CNN训练与推理
tldr: 本研究在仅需15-40美元的拇指级微控制器（如ESP32-S3）上，实现了一个完整的端到端视觉机器学习流水线。该系统不依赖外部ML库，仅通过约1750行C++代码便完成了从数据采集、两层CNN训练（含Adam优化）到实时推理的全过程。其核心贡献包括梯度累积、预计算缩放查找表及三级权重管理系统，显著提升了边缘设备的自主学习能力。
selection_source: fresh_fetch
motivation: 旨在消除对云端基础设施的依赖，在资源极度受限的微控制器上实现透明且独立的端到端机器学习生命周期。
method: 利用纯C++开发了轻量级CNN框架，集成了Adam优化器、PSRAM感知内存管理以及支持SD卡和内置头文件的双重权重部署机制。
result: 在ESP32-S3硬件上实现了64x64分辨率的三分类任务，训练过程仅需约9分钟，实时推理速度可达6.3 FPS。
conclusion: 该研究证明了在极低成本硬件上进行完整视觉模型训练与部署的可行性，为边缘AI应用提供了一个高效、开源的参考实现。
---

## 摘要
本文介绍了一个完整的、端到端的设备端视觉机器学习流水线，涵盖了数据采集、采用 Adam 优化的双层 CNN 训练以及实时推理，且完全在成本仅为 15-40 美元的微控制器级设备上运行。与需要外部基础设施并对从业者隐藏计算流水线的云端工作流不同，该系统在大约 1,750 行可读的 C++ 代码中实现了核心机器学习生命周期的每一个步骤，使用 Arduino IDE 编译时间不足一分钟，且不依赖任何外部机器学习库。该固件运行在 Seeed Studio ESP32-S3 XIAO ML Kit（具备 8 MB PSRAM）上，每次训练运行约 9 分钟即可完成三类 64x64 图像分类，实时推理速度可达 6.3 FPS。主要贡献包括：正确的批次级梯度累积；用于推理的预计算图像缩放查找表；支持无需 SD 卡的内置部署的双格式权重导出；在启动时自动解析的三级权重优先级系统（SD 二进制文件 > 内置头文件 > He 初始化）；单常量网络重配置接口；以及适用于微控制器约束的 PSRAM 感知内存管理。所有源代码和参考数据集均已在 MIT 许可证下发布，地址为 https://github.com/webmcu-ai/on-device-vision-ai。

## Abstract
This paper presents a complete, end-to-end on-device vision machine learning pipeline, comprising data acquisition, two-layer CNN training with Adam optimization, and real-time inference, executing entirely on a microcontroller-class device costing $15-40 USD. Unlike cloud-based workflows that require external infrastructure and conceal the computational pipeline from the practitioner, this system implements every step of the core ML lifecycle in approximately 1,750 lines of readable C++ that compiles in under one minute using the Arduino IDE, with no external ML dependencies. Running on the Seeed Studio ESP32-S3 XIAO ML Kit (8 MB PSRAM), the firmware achieves three-class 64x64 image classification in approximately 9 minutes per training run, with real-time inference at 6.3 FPS. Key contributions include: correct batch-level gradient accumulation; pre-computed resize lookup tables for inference; dual-format weight export for SD-free baked-in deployment; a three-tier weight priority system (SD binary > baked-in header > He-initialization) resolved automatically at boot; a single-constant network reconfiguration interface; and PSRAM-aware memory management suited to microcontroller constraints. All source code and reference datasets are released under the MIT License at https://github.com/webmcu-ai/on-device-vision-ai

---

## 论文详细总结（自动生成）

### 论文总结：拇指大小微控制器上的设备端视觉训练、部署与推理

#### 1. 核心问题与整体含义（研究动机和背景）
当前机器学习（ML）的主流工作流高度依赖云端：数据上传至数据中心训练，再将模型下发至终端。这种模式存在**隐私泄露风险、高昂的订阅成本以及计算过程不透明**（黑盒化）等问题。
本文旨在打破这一现状，证明在极低成本（15-40美元）、资源受限的微控制器（MCU）上，可以独立完成从数据采集、模型训练到实时推理的全生命周期。其核心意义在于为教育、隐私敏感型应用和小型工业原型提供一个**完全透明、无云端依赖、单文件实现**的轻量级视觉AI框架。

#### 2. 方法论：核心思想与关键技术
论文实现了一个约1,750行C++代码的单文件Arduino工程，核心技术细节包括：
*   **神经网络架构**：采用两层卷积神经网络（CNN），包含卷积层（Leaky ReLU激活）、最大池化层、展平层和全连接层（Softmax输出）。
*   **优化算法**：实现了标准的**Adam优化器**，并包含偏置校正。
*   **梯度管理**：严格区分了批次级（Batch-level）梯度累积和单样本传播缓冲区的清理逻辑，确保小批量梯度下降的数学正确性。
*   **内存管理**：针对ESP32-S3的8MB PSRAM进行优化，采用全局预分配RGB缓冲区（172 KB）避免堆碎片，并使用`ps_malloc`管理大型张量。
*   **推理优化**：
    *   **预计算查找表**：预先计算图像缩放的索引，消除推理时的浮点除法。
    *   **乘法替代除法**：将像素归一化（/255.0）替换为预计算的倒数乘法（*0.003921569f）。
*   **权重优先级系统**：系统启动时按“SD卡二进制文件 > 内置头文件 > He初始化随机权重”的顺序自动加载，支持“训练-导出-固化-再微调”的闭环。

#### 3. 实验设计
*   **数据集**：使用了三个自定义的设备端采集数据集：
    *   **Dataset A**：2类×9张图（冒烟测试）。
    *   **Dataset B**：3类（空白、杯子、笔）×约30张图（核心实验）。
    *   **Dataset C**：6类×约60张图（扩展性测试）。
*   **Benchmark（对比对象）**：
    *   **官方库**：Espressif的ESP-DL和ESP-NN（仅支持推理，不支持训练）。
    *   **第三方框架**：AIfES（代码量大且复杂）和TinyTorch（主要针对Python教学）。
*   **评估指标**：训练时长、推理帧率（FPS）、内存占用、功耗（电流曲线）。

#### 4. 资源与算力
*   **硬件平台**：Seeed Studio XIAO ESP32-S3 Sense（Xtensa LX7双核 @ 240MHz，8MB PSRAM）。
*   **算力使用**：
    *   **训练**：在MCU上运行20个Epoch（90张图像），耗时约**9分钟**。
    *   **推理**：在64x64输入下，推理速度为**6.3 FPS**（159ms/帧）。
    *   **内存**：训练峰值占用约750 KB PSRAM（约占总量的9.4%）。
*   **GPU/外部算力**：**完全未使用**任何GPU或外部计算机，所有计算均在MCU端完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对不同输入尺寸（48, 64, 96, 128）进行了消融实验，对比了参数量、内存和耗时的变化。
*   **充分性评价**：
    *   **客观性**：通过Nordic PPK2功率分析仪记录了详细的电流波动，展示了计算密集阶段（反向传播）与IO阶段（SD卡写入）的功耗差异。
    *   **局限性**：实验主要集中在小规模分类任务（3-6类），未在大规模标准数据集（如CIFAR-10）上进行长时间验证，且验证集规模较小（每类仅3张图作为Hold-out）。

#### 6. 主要结论与发现
*   **可行性确认**：证明了在亚瓦特（sub-watt）功耗的微控制器上进行端到端CNN训练是完全可行的。
*   **性能表现**：6.3 FPS的推理速度足以满足许多实时边缘监测场景。
*   **教育价值**：透明的代码结构使学生能够观察每一个梯度和激活值的变化，这是云端黑盒框架无法提供的。
*   **迭代能力**：系统支持通过SD卡保存权重并在后续继续训练，实现了增量学习的初步形态。

#### 7. 优点
*   **极简性**：单文件实现，无需复杂的构建系统或外部依赖库，极易移植和分发。
*   **透明度**：代码可读性高，所有数学逻辑（卷积、反向传播、Adam）均直接暴露，适合教学。
*   **部署灵活**：创新的三级权重管理系统，支持无SD卡的固化部署，也支持带SD卡的现场微调。
*   **功耗透明**：提供了详尽的端到端训练能耗数据（约0.07 Wh/次训练）。

#### 8. 不足与局限
*   **算法限制**：目前仅支持单核运行，未利用ESP32-S3的第二个核心；缺乏数据增强（Data Augmentation）和Dropout等正则化手段，易过拟合。
*   **性能差距**：由于未使用SIMD指令集优化（如ESP-NN），推理速度低于官方优化库。
*   **精度风险**：仅使用Float32，未涉及INT8量化，虽然保证了精度但牺牲了速度和存储效率。
*   **应用范围**：目前仅限于简单的图像分类，尚未扩展到目标检测（如FOMO）或更复杂的视觉任务。

（完）
