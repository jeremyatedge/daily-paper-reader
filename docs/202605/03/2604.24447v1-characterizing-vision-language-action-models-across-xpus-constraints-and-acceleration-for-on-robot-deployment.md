---
title: "Characterizing Vision-Language-Action Models across XPUs: Constraints and Acceleration for On-Robot Deployment"
title_zh: 跨 XPU 的视觉-语言-动作（VLA）模型特性分析：机器人端部署的约束与加速
authors: "Kaijun Zhou, Qiwei Chen, Da Peng, Zhiyang Li, Xijun Li, Jinyu Gu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24447v1"
tags: ["query:aiot"]
score: 10.0
evidence: 在严格的成本和能源预算下，使用边缘加速器进行机器人部署
tldr: 本研究针对视觉-语言-动作（VLA）模型在机器人边缘部署中的实时推理难题，系统分析了不同硬件加速器（GPU/XPU/NPU）的性能表现。通过构建CET（成本、能耗、时间）排行榜，揭示了VLA推理中计算密集型VLM与访存密集型Action Expert的阶段性特征。基于此，提出了DP-Cache和V-AEFusion优化方案，在保证任务成功率的同时，显著提升了推理速度，为低成本高效能机器人部署提供了新路径。
selection_source: fresh_fetch
motivation: 旨在解决VLA模型在受限的边缘计算环境下，因高推理延迟和能耗导致的实时控制瓶颈。
method: 建立了跨硬件评估体系，并针对推理中的计算与访存不平衡问题，提出了减少冗余和异步流水线并行的优化技术。
result: 在边缘NPU上获得最高6倍的加速效果，在GPU上获得2.9倍加速，且对模型性能影响极小。
conclusion: 协同优化模型与硬件特性是实现低成本、高效率通用机器人控制的关键。
---

## 摘要
视觉-语言-动作（VLA）模型在通用机器人控制方面具有广阔前景，但受限于严格的成本和能耗预算，其在机器人端的部署面临实时推理的瓶颈。先前的评估大多依赖于桌面级 GPU，这掩盖了异构边缘加速器（GPU/XPU/NPU）所提供的权衡与机遇。我们通过模型-硬件协同特性分析，对低成本 VLA 部署进行了系统性研究。首先，我们构建了一个跨加速器排行榜，并在 CET（成本、能耗、时间）指标下评估了模型-硬件组合，结果表明，在满足控制频率约束的前提下，尺寸合适的边缘设备比旗舰级 GPU 具有更高的成本和能效比。其次，通过深入的性能分析，我们发现了一种一致的两阶段推理模式：计算受限（compute-bound）的 VLM 主干网络后接内存受限（memory-bound）的动作专家（Action Expert），这导致了阶段性的利用率不足和硬件低效。最后，在这些见解的指导下，我们提出了 DP-Cache 和 V-AEFusion，旨在减少扩散冗余并实现异步流水线并行，在仅有轻微成功率下降的情况下，在 GPU 上实现了高达 2.9 倍的加速，在边缘 NPU 上实现了 6 倍的加速。示例排行榜网站见：https://vla-leaderboard-01.vercel.app/。

## Abstract
Vision-Language-Action (VLA) models are promising for generalist robot control, but on-robot deployment is bottlenecked by real-time inference under tight cost and energy budgets. Most prior evaluations rely on desktop-grade GPUs, obscuring the trade-offs and opportunities offered by heterogeneous edge accelerators (GPUs/XPUs/NPUs). We present a systematic analysis for low-cost VLA deployment via model-hardware co-characterization. First, we build a cross-accelerator leaderboard and evaluate model-hardware pairs under CET (Cost, Energy, Time), showing that right-sized edge devices can be more cost-/energy-efficient than flagship GPUs while meeting control-rate constraints. Second, using in-depth profiling, we uncover a consistent two-phase inference pattern: a compute-bound VLM backbone followed by a memory-bound Action Expert, which induces phase-dependent underutilization and hardware inefficiency. Finally, guided by these insights, we propose DP-Cache and V-AEFusion to reduce diffusion redundancy and enable asynchronous pipeline parallelism, achieving up to 2.9x speedup on GPUs and 6x on edge NPUs with only marginal success degradation. The example leaderboard website is available at: https://vla-leaderboard-01.vercel.app/.

---

## 论文详细总结（自动生成）

这篇论文《Characterizing Vision-Language-Action Models across XPUs: Constraints and Acceleration for On-Robot Deployment》深入探讨了视觉-语言-动作（VLA）模型在机器人端部署的实际挑战，并提出了针对性的优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然 VLA 模型在通用机器人控制上表现出色，但其部署受限于实时性要求、高昂的硬件成本和有限的能源预算。
*   **核心问题**：现有的评估多基于高端桌面 GPU（如 RTX 4090），忽略了边缘加速器（NPU/XPU）的潜力。论文旨在回答：如何为机器人选择“尺寸合适”的计算平台？VLA 推理的瓶颈究竟在哪里？如何通过软硬件协同优化实现加速？
*   **整体含义**：通过系统性分析异构硬件上的 VLA 表现，为低成本、高效率的具身智能部署提供理论指导和技术工具。

### 2. 方法论
论文提出了一个模型-硬件协同特性的分析框架，包含以下关键技术：
*   **CET 评估体系**：从成本（**C**ost）、能耗（**E**nergy）、时间（**T**ime/延迟）三个维度量化部署效率，而非单一的推理速度。
*   **双阶段特性分析**：利用 Roofline 模型发现 VLA 推理具有明显的阶段性：
    1.  **VLM 主干阶段**：计算密集型（Compute-bound），硬件利用率高。
    2.  **动作专家（Action Expert）阶段**：访存密集型（Memory-bound），受限于内存带宽，且因迭代去噪导致硬件利用率极低。
*   **DP-Cache（扩散策略缓存）**：针对扩散模型中的时间冗余，在去噪过程的稳定阶段缓存中间结果并广播，跳过不必要的重复计算。
*   **V-AEFusion（异步流水线并行）**：利用观察流的时间相干性，在 VLM 处理当前帧时，让动作专家利用上一帧的特征先行开始去噪（Stale steps），实现两个阶段的并行执行。

### 3. 实验设计
*   **硬件平台**：涵盖了 NVIDIA（RTX 4090, Jetson Thor, AGX Orin）、Intel（B60 Pro XPU, i7 CPU）、华为（Ascend 310P, 310B NPU）。
*   **模型对象**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、SmolVLA、Gr00t、Diffusion Policy 等主流模型。
*   **Benchmark 与场景**：
    *   **仿真**：LIBERO 基准测试（包含空间、物体、目标、长程四类任务）。
    *   **真实机器人**：Franka 机械臂（桌面清理任务）、JAKA S5 双臂机器人（杯子/方块放置任务）。
*   **对比方法**：将提出的优化方案与原始模型、量化（4-bit）、Cache-VLA、Spec-VLA 等加速技术进行对比。

### 4. 资源与算力
*   **推理算力**：详细列出了各平台的 FP16/BF16 峰值算力（如 RTX 4090 为 330 TFLOPS，Ascend 310B 为 10 TFLOPS）及内存带宽。
*   **训练算力**：论文重点在于**推理部署**，未详细说明模型的训练时长和训练用 GPU 数量，但提到使用了模型编译技术（如 Torch.compile, ATC）来优化推理图。

### 5. 实验数量与充分性
*   **实验规模**：跨越了 7 种异构硬件和 5 种以上模型架构，涵盖了从 1B 以下到 7B 以上的不同参数规模。
*   **充分性**：实验不仅包含仿真环境下的成功率测试，还包含真实机器人的 50 次重复实验，并使用物理功率计测量实际能耗。
*   **客观性**：通过消融实验验证了“过期步数（Stale steps）”对成功率的影响，找出了速度与精度的平衡点，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **模型大小误区**：模型参数量并非延迟的唯一决定因素，扩散模型的迭代步数对实时性的影响往往超过参数规模。
*   **硬件选型**：RTX 4090 并非总是最优选。在成本和能效优先的场景下，Jetson Thor 或 Ascend 310P 等边缘设备表现出更高的 CET 综合效率。
*   **瓶颈转移**：VLA 推理存在从“计算受限”到“访存受限”的阶段性转换，导致硬件资源在动作生成阶段严重闲置。
*   **加速效果**：DP-Cache 和 V-AEFusion 在 GPU 上实现 2.9 倍加速，在边缘 NPU 上实现高达 6 倍加速，且任务成功率仅有轻微下降（约 0.3% - 6%）。

### 7. 优点
*   **协同视角**：打破了单纯优化算法或单纯堆砌硬件的局限，从软硬件适配的角度解决部署难题。
*   **实用性极高**：提供的交互式排行榜（Leaderboard）和选型指南对工业界机器人部署具有直接参考价值。
*   **训练无关（Training-free）**：提出的加速方法不需要重新训练模型，易于迁移和快速部署。

### 8. 不足与局限
*   **精度权衡**：虽然成功率下降较小，但在极高精度要求的任务中，缓存和异步并行可能引入不确定性。
*   **硬件覆盖**：虽然涵盖了主流芯片，但对于一些新兴的 RISC-V 架构或更低功耗的微控制器（MCU）级加速器尚未涉及。
*   **应用限制**：主要针对基于扩散或流匹配的 VLA 模型，对于纯自回归生成的模型加速效果可能有限。

（完）
