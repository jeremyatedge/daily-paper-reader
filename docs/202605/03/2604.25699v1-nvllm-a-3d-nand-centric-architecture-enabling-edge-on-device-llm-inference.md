---
title: "NVLLM: A 3D NAND-Centric Architecture Enabling Edge on-Device LLM Inference"
title_zh: NVLLM：一种支持边缘端设备 LLM 推理的以 3D NAND 为中心的架构
authors: "Mingbo Hao, Changwei Yan, Haoyu Cui, Zhihao Yan, Yizhi Ding, Zhangrui Qian, Weiwei Shan"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25699v1"
tags: ["query:aiot"]
score: 10.0
evidence: 面向边缘设备LLM推理的3D NAND中心化架构
tldr: NVLLM 是一种以 3D NAND 为中心的推理架构，旨在解决边缘设备上大语言模型（LLM）推理受限于内存带宽和容量的问题。它通过晶圆堆叠技术将 FFN 计算卸载到闪存中，并与计算流水线、ECC 和缓存深度集成，实现了无需经过 DRAM 的页面级权重访问。该架构在保持低面积开销的同时，显著提升了推理速度，为边缘端运行大规模模型提供了高效方案。
selection_source: fresh_fetch
motivation: 边缘设备上 LLM 推理受限于内存带宽，且现有的 GPU 离核推理或类 SSD 加速器存在 DRAM 传输瓶颈和存储访问粒度低的问题。
method: 提出 NVLLM 架构，利用晶圆堆叠技术将 FFN 计算集成到 3D NAND 中，通过乱序执行的计算单元直接处理闪存读取的数据，并配合 KV 缓存感知调度器。
result: "在 OPT 和 LLaMA 模型上，NVLLM 相比 A800 离核推理实现了 16.7 至 37.9 倍的加速，相比类 SSD 设计提升达 4.7 倍，且 CMOS 面积开销仅为 2.7%。"
conclusion: NVLLM 通过将计算与存储深度融合，有效解决了边缘端 LLM 推理的存储瓶颈，证明了 3D NAND 中心化架构在处理大规模模型时的优越性。
---

## 摘要
大语言模型（LLM）的快速增长要求在资源受限的边缘设备上进行高吞吐量、内存容量密集型的推理，而单批次解码在根本上仍受限于内存带宽。现有的基于 GPU 的核外加速器和类 SSD 加速器受限于受 DRAM 限制的权重移动以及低效的存储访问粒度。我们提出了 NVLLM，这是一种以 3D NAND 为中心的推理架构，它将前馈网络（FFN）计算卸载到闪存中，同时在带有外部 DRAM 的轻量级 CMOS 逻辑上执行注意力机制。通过晶圆对晶圆堆叠，NVLLM 将多平面 3D NAND 与计算流水线、纠错码（ECC）单元和缓冲区紧密集成，实现了无需经过 DRAM 的页面级 FFN 权重访问。所有的 GEMM/GEMV 操作被分解为由乱序 PE 通道执行的点积原语，直接在集成了 ECC 的原始 NAND 读取数据上运行。注意力权重保留在 DRAM 中，且 KV 缓存感知调度器在上下文长度增加时仍能维持吞吐量。在参数量高达 30B 的 OPT 和 LLaMA 模型上进行的评估表明，NVLLM 相比基于 A800 的核外推理实现了 16.7 倍至 37.9 倍的加速，相比类 SSD 设计实现了高达 4.7 倍的加速，且仅有 2.7% 的 CMOS 面积开销。

## Abstract
The rapid growth of LLMs demands high-throughput, memory-capacity-intensive inference on resource-constrained edge devices, where single-batch decoding remains fundamentally memory-bound. Existing out-of-core GPU-based and SSD-like accelerators are limited by DRAM-bound weight movement and inefficient storage access granularity. We present NVLLM, a 3D NAND-centric inference architecture that offloads feed-forward network (FFN) computation into the Flash while executing attention on lightweight CMOS logic with external DRAM. Through wafer-to-wafer stacking, NVLLM tightly integrates multi-plane 3D NAND with compute pipelines, error correction code (ECC) units, and buffers, enabling page-level FFN weight access without DRAM traversal. All GEMM/GEMV operations are decomposed into dot-product primitives executed by out-of-order PE lanes, operating directly on raw NAND reads with integrated ECC. Attention weights remain in DRAM, and a KV-cache-aware scheduler sustains throughput as the context length grows. Evaluated on OPT and LLaMA models with up to 30B parameters, NVLLM achieves a 16.7$\times$--37.9$\times$ speedup over A800-based out-of-core inference and up to 4.7$\times$ speedup over SSD-like designs, with only 2.7\% CMOS area overhead.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NVLLM** 的新型架构，旨在通过以 3D NAND 闪存为中心的设计，解决边缘设备在大语言模型（LLM）推理过程中面临的内存瓶颈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：边缘设备（如手机、嵌入式设备）在运行 LLM 时，受限于内存容量和带宽。由于边缘端推理通常是单批次（Single-batch）且以解码（Decoding）为主，计算强度低，性能瓶颈主要在于从存储中读取权重的速度（Memory-bound）。
*   **研究背景**：
    *   现有的 GPU 离核（Out-of-core）方案受限于 PCIe 和 DRAM 带宽。
    *   类 SSD 加速器存在存储访问粒度粗、数据搬运开销大的问题。
    *   LLM 中前馈网络（FFN）占据了约 70% 的参数，且访问模式规整，适合近存储计算。

### 2. 方法论：核心思想与关键技术
NVLLM 的核心思想是将 **FFN 计算卸载到 3D NAND 内部**，而将注意力机制（Attention）保留在 DRAM 和轻量级 NPU 中。
*   **晶圆级集成（W2W Stacking）**：利用晶圆对晶圆键合技术，将 CMOS 逻辑电路直接堆叠在 3D NAND 阵列下方，实现极高带宽的页面级（Page-level）权重访问。
*   **集群平面-通道协同设计（Clustered Plane-Lane Co-Design）**：将多个闪存平面（Plane）分组，使其数据输出宽度与处理单元（PE）的计算宽度对齐，消除复杂的缓冲和布线开销。
*   **容错点积引擎（ERDPE）**：
    *   **核心算法**：针对 NAND 原始读取存在位错误（RBER）的问题，设计了**乱序纠错点积算法（OoO-ECDP）**。
    *   **技术细节**：将错误检测与纠错解耦。如果检测到错误，该数据段被送入纠错器，而计算流水线继续处理后续无错数据（乱序执行），从而在维持高吞吐量的同时容忍闪存错误。
*   **KV 缓存感知调度器**：随着上下文长度增加，注意力机制开销增大。调度器根据实时负载动态调整 NPU 和 NAND 之间的计算分配，确保吞吐量稳定。

### 3. 实验设计
*   **数据集/场景**：使用了 ShareGPT、UltraChat 和 OASST1 等真实对话数据集来分析推理模式（短提示、长生成）。
*   **测试模型**：OPT 系列（1.3B 到 30B 参数）以及 LLaMA 系列模型。
*   **对比基准（Benchmarks）**：
    *   **GPU 方案**：NVIDIA A800 GPU 分别配合 DRAM、SSD 以及混合存储（FlexGen 方案）。
    *   **类 SSD 方案**：Cambricon-LLM 和 AiF（现有的近存储计算研究）。
*   **评估指标**：每秒生成 Token 数（TPS）、端到端延迟、能效比（J/Token）以及面积开销。

### 4. 资源与算力
*   **硬件仿真/合成**：
    *   使用 **TSMC 28nm** 工艺对 NPU 和 NAND CMOS 逻辑进行综合。
    *   使用 **3D-FPIM** 框架模拟 3D NAND 阵列。
    *   使用 **Ramulator2** 和 **DRAMPower** 模拟 LPDDR5X DRAM。
*   **基准算力**：对比对象为 NVIDIA A800（80GB 显存）。NVLLM 自身的逻辑电路运行频率在 350MHz-500MHz 之间。
*   **训练时长**：本文侧重于推理架构设计，未涉及模型训练过程。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.3B 到 30B 不同参数规模的模型，验证了架构的可扩展性。
*   **消融实验**：针对 KV 缓存感知调度器进行了消融实验，证明了其在长文本场景下的有效性。
*   **鲁棒性测试**：通过注入随机位翻转模拟 NAND 的原始比特错误率（RBER），验证了纠错引擎的性能。
*   **评价**：实验设计较为全面，对比了当前主流的软件优化方案（FlexGen）和前沿的硬件加速研究，数据对比客观。

### 6. 主要结论与发现
*   **性能提升**：相比 A800 GPU 的离核推理，NVLLM 实现了 **16.7× 至 37.9×** 的吞吐量提升；相比类 SSD 架构提升达 **4.7×**。
*   **延迟与能效**：端到端延迟降低了最高 28.2×，数据搬运能耗降低了 **5.6×**。
*   **面积开销极低**：新增的 CMOS 逻辑仅占 NAND 芯片面积的 **2.7%**，具有极高的商用可行性。
*   **核心发现**：将 FFN 留在闪存内计算、Attention 留在 DRAM 的解耦策略，是解决边缘端 LLM 存储瓶颈的最优解之一。

### 7. 优点与亮点
*   **架构创新**：通过 W2W 技术真正实现了存储与计算的深度融合，突破了传统存储接口的带宽限制。
*   **容错设计巧妙**：乱序执行纠错机制解决了近闪存计算中“纠错延迟高”的痛点，保证了流水线不间断。
*   **软硬协同**：不仅有硬件改进，还配合了动态调度算法，适应 LLM 推理过程中 KV Cache 动态增长的特性。

### 8. 不足与局限
*   **制造复杂度**：晶圆对晶圆（W2W）堆叠技术虽然先进，但相比传统封装，其制造工艺要求更高，成本和良率控制是实际量产的挑战。
*   **应用范围限制**：该架构主要针对推理（Inference）优化，对于需要频繁写入权重的模型微调（Fine-tuning）或训练场景，受限于 NAND 的写入寿命和速度，表现可能受限。
*   **硬件依赖性**：性能高度依赖于特定的 3D NAND 结构设计，通用性可能不如纯软件优化方案。

（完）
