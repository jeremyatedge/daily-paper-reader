---
title: "DECOFFEE: Decentralized Reinforcement Learning for Time-critical Workload Offloading and Energy Efficiency across the Computing Continuum"
title_zh: DECOFFEE：跨计算连续体的时间关键型工作负载卸载与能效的去中心化强化学习
authors: "Anastasios Giannopoulos, Sotirios Spantideas, Panagiotis Trakadas"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24507v1"
tags: ["query:aiot"]
score: 9.0
evidence: 物联网应用的负载卸载与能效
tldr: 针对物联网应用中延迟敏感和能耗受限的挑战，本文提出DECOFFEE框架，利用去中心化强化学习优化边缘-云环境下的任务卸载。该框架结合Double Dueling DQN与LSTM预测，使边缘节点能自主决策任务执行位置，在降低系统延迟和能耗的同时，显著减少任务丢弃率，提升了大规模分布式架构的运行效率。
selection_source: fresh_fetch
motivation: 随着物联网应用对延迟和能耗要求的提高，在动态且异构的边缘-云环境中实现高效的任务调度和资源管理变得至关重要。
method: 提出一种基于多智能体强化学习的去中心化框架，采用结合LSTM预测的Double Dueling DQN算法来制定最优卸载策略。
result: 实验结果表明，DECOFFEE在各种流量和网络条件下，在降低延迟、能耗及任务丢弃率方面均优于传统的启发式和规则化策略。
conclusion: 该研究证明了结合预测机制的去中心化强化学习能有效解决复杂计算连续体中的实时任务卸载与能效优化问题。
---

## 摘要
对延迟敏感且受电池限制的物联网 (IoT) 应用的快速普及，加剧了对跨边缘-云计算连续体的智能工作负载放置机制的需求。在此类环境中，远端边缘节点必须动态决定是在本地执行工作负载，还是将其卸载到邻近节点或云端，同时考虑执行延迟、能耗和严格的超时限制。然而，由于随机到达、异构计算能力和随时间变化的网络条件，大规模分布式基础设施中的工作负载放置是一个高度动态且非凸的优化问题。本文提出了 DECOFFEE，这是一个用于跨计算连续体的时间关键型工作负载卸载和能效运行的去中心化强化学习框架。所提出的多智能体学习方案通过自适应放置决策，共同优化系统延迟、能耗和工作负载丢弃率。每个边缘智能体作为一个自主学习实体运行，根据局部系统观测和预测的网络条件推导出最优策略。工作负载放置过程被建模为并行马尔可夫决策过程，并使用增强了长短期记忆 (LSTM) 预测的双重竞争深度 Q 网络 (Double Dueling Deep Q-Network, DQN) 架构来解决，以预测未来的负载情况。广泛的仿真表明，DECOFFEE 及其变体始终优于传统的基于规则和启发式的放置策略，在不同的流量和网络条件下显著降低了延迟、能耗和工作负载丢弃率。

## Abstract
The rapid proliferation of latency-sensitive and battery-constrained Internet-of-Things (IoT) applications has intensified the need for intelligent workload placement mechanisms across the Edge-Cloud computing continuum. In such environments, far-edge nodes must dynamically decide whether to execute workloads locally or offload them to neighboring nodes or the cloud, while accounting for execution delay, energy consumption, and strict timeout constraints. However, workload placement in large-scale distributed infrastructures is a highly dynamic and non-convex optimization problem due to stochastic arrivals, heterogeneous computing capacities, and time-varying network conditions. This paper proposes DECOFFEE, a decentralized reinforcement learning framework for time-critical workload offloading and energy-efficient operation across the computing continuum. The proposed multi-agent learning scheme jointly optimizes system delay, energy consumption, and workload drop rate through adaptive placement decisions. Each edge agent operates as an autonomous learning entity that derives an optimal policy from local system observations and predicted network conditions. The workload placement process is formulated as parallel Markov Decision Processes and solved using a Double Dueling Deep Q-Network (DQN) architecture enhanced with Long Short-Term Memory (LSTM) forecasting to anticipate future load conditions. Extensive simulations demonstrate that DECOFFEE and its variants consistently outperform conventional rule-based and heuristic placement strategies, achieving significant reductions in delay, energy consumption, and workload drop rate under varying traffic and network conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《DECOFFEE: Decentralized Reinforcement Learning for Time-critical Workload Offloading and Energy Efficiency across the Computing Continuum》的结构化分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：随着延迟敏感型和电池受限型物联网（IoT）应用的激增，如何在“边缘-云”计算连续体中实现智能的工作负载放置成为关键。
*   **核心挑战**：由于任务到达的随机性、计算资源的异构性以及网络条件的动态变化，工作负载放置是一个高度动态且非凸的优化问题。传统的中心化方案在可扩展性、实时响应和局部能效感知方面存在不足。
*   **研究目标**：提出一种去中心化的强化学习框架（DECOFFEE），使边缘节点能够自主决定任务是在本地执行、水平卸载（至邻近边缘节点）还是垂直卸载（至云端），以平衡延迟、能耗和任务丢弃率。

### 2. 方法论
*   **核心思想**：将卸载问题建模为一组**并行且智能体特定的马尔可夫决策过程（Parallel MDPs）**。每个边缘智能体（EA）根据局部观测和预测的全局遥测数据独立学习最优策略。
*   **关键技术细节**：
    *   **状态空间增强**：引入 **LSTM（长短期记忆网络）** 模块，利用历史负载轨迹预测未来负载状态，使决策从“反应式”转变为“主动式”。
    *   **算法架构**：采用 **Double Dueling Deep Q-Network (DQN)**。Dueling 架构将状态价值与动作优势分离，Double 机制减少了 Q 值的过高估计。
    *   **多目标代价函数**：定义了一个综合代价函数 $c_n(t)$，加权整合了执行延迟、等待延迟、传输能耗、计算能耗以及任务超时的惩罚项（Drop Penalty）。
    *   **工作负载堆栈（WS）**：每个节点维护私有堆栈（本地任务）和多个公共堆栈（接收其他节点的任务），采用完全公平调度（CFS）原则分配计算资源。

### 3. 实验设计
*   **场景设置**：模拟了一个三层架构，包含 20 个边缘智能体（EA）和一个云智能体（CA）。边缘节点间通过有线链路（30 Mbps）互联，边缘至云端通过广域网（10 Mbps）连接。
*   **数据集/任务模型**：采用应用无关的任务模型，任务大小在 2-5 Mbits 之间，计算密度为 0.297 gigacycles/Mbit。
*   **对比基准（Benchmarks）**：
    1.  **RP (Random)**：随机放置。
    2.  **LOP (Local-Only)**：仅本地处理。
    3.  **COO (Cloud-Only)**：全部卸载至云端。
    4.  **EEO (Edge-to-Edge)**：仅在边缘节点间水平卸载。
    5.  **RRO (Round-Robin)**：轮询调度。
    6.  **MLEO (Min-Latency Estimation)**：基于最小延迟估计的启发式算法（强基准）。
    7.  **DECOFFEE 变体**：包括延迟优先（DA）、能效优先（EA）和平衡型（B）。

### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：AMD Ryzen 7 7800X3D (8核, 4.20 GHz)。
    *   **GPU**：**NVIDIA RTX 4080**。
    *   **内存**：64 GB DDR5 RAM。
*   **软件框架**：Python 3.10, PyTorch v2.0.1, CUDA v11.8。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到每个智能体训练了 2000 个 Episode，每个 Episode 包含 110 个时间步。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **收敛性分析**：测试了不同学习率（$\alpha_{lr}$）和折扣因子（$\gamma$）对收敛的影响。
    *   **敏感性分析**：针对任务到达概率（$P$）、智能体数量（$N$）、CPU 频率、任务超时阈值（$T_{max}$）和水平数据速率进行了多组实验。
    *   **消融实验**：对比了包含与不包含 LSTM 预测模块的性能差异。
*   **充分性评价**：实验设计较为全面，覆盖了从算法超参数调优到环境参数变化的多个维度。通过 200 次推理运行取平均值，保证了结果的统计客观性。

### 6. 主要结论与发现
*   **性能优越性**：DECOFFEE 在延迟、能耗和丢弃率方面均优于所有基准算法。例如，在重负载下，延迟比 MLEO 降低约 8%，比 COO 降低超过 50%。
*   **LSTM 的价值**：在负载中等（$P \in [0.3, 0.7]$）时，LSTM 预测带来的增益最明显，能将系统代价降低 10%-16%。
*   **灵活性**：通过调整权重系数（$w_d, w_e$），DECOFFEE 能在“极速响应”和“极致省电”模式间平滑切换。
*   **可扩展性**：随着节点数量增加，去中心化架构表现出良好的稳定性，且更多的边缘节点提供了更多的水平卸载机会，有助于降低整体能耗。

### 7. 优点
*   **主动式决策**：结合 LSTM 预测未来负载，有效解决了任务卸载中的决策滞后问题。
*   **去中心化架构**：每个智能体仅依赖局部观测和少量遥测数据，避免了单点故障，降低了通信开销，适合大规模边缘场景。
*   **多目标平衡**：不仅关注延迟，还深入建模了复杂的能耗组件（等待、传输、处理能耗）。

### 8. 不足与局限
*   **任务原子性假设**：论文假设任务是不可分割的（Atomic），未考虑任务拆分或具有依赖关系的 DAG（有向无环图）任务流。
*   **环境简化**：实验主要在仿真环境中完成，虽然参数设定参考了现实标准，但未在真实的物理边缘测试床（如 KubeEdge 或 StarlingX）上验证。
*   **拓扑固定性**：虽然提到可以支持动态拓扑，但实验中主要使用了固定的连接矩阵，未深入探讨节点移动性（如车载边缘场景）带来的挑战。
*   **推理开销**：虽然声称轻量，但每个节点运行 LSTM + Dueling DQN 在资源极度受限的微型传感器节点上可能仍有压力。

（完）
