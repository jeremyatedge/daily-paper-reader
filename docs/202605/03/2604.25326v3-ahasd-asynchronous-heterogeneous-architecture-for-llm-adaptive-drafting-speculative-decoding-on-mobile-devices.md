---
title: "AHASD: Asynchronous Heterogeneous Architecture for LLM Adaptive Drafting Speculative Decoding on Mobile Devices"
title_zh: AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构
authors: "Ma Zirui, Fan Zhihua, Li Wenxing, Wu Haibin, Zhang Fulin, Ye Xiaochun, Li Wenming"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25326v3"
tags: ["query:aiot"]
score: 9.0
evidence: 移动端NPU-PIM系统的异步异构架构
tldr: 针对移动设备上大语言模型投机采样推理效率低的问题，本文提出AHASD架构。该架构利用NPU与PIM（存内计算）构建任务级异步异构体系，通过解耦草稿生成与验证过程，并引入熵历史感知控制和预验证机制，有效减少了计算浪费和空闲开销。实验表明，AHASD在吞吐量和能效比上显著优于现有GPU及GPU+PIM方案，且硬件开销极低。
selection_source: fresh_fetch
motivation: 传统移动端投机采样在同步执行时存在空闲开销，而在异步执行时因草稿长度波动导致大量无效计算。
method: 提出一种任务级异步异构架构，在PIM上并行生成草稿并在NPU上验证，结合动态草稿控制与硬件加速单元优化调度。
result: 相比GPU基准，吞吐量提升达4.2倍，能效提升5.6倍；相比最先进的GPU+PIM方案，吞吐量和能效分别提升1.5倍和1.24倍。
conclusion: AHASD通过软硬件协同设计，为移动端大模型的高效投机采样推理提供了一种低开销、高性能的异构加速方案。
---

## 摘要
投机解码通过使用小型草稿语言模型（DLM）生成草稿并使用大型目标语言模型（TLM）进行批量验证，提升了大语言模型（LLM）的推理效率。然而，在移动端单 NPU-PIM 系统上，由于草稿长度的波动，自适应草稿推理在传统的算子级同步执行中存在空闲开销，在异步执行中则存在计算浪费。本文提出了 AHASD，一种用于投机解码的任务级异步移动 NPU-PIM 异构架构。AHASD 通过任务级 DLM-TLM 解耦，实现了 PIM 上的并行草稿生成与单 NPU 上的验证。具体而言，它引入了熵历史感知草稿控制和时间感知预验证控制，以动态管理自适应草稿算法的执行与预验证时机，从而抑制基于低置信度草稿的无效草稿生成。此外，AHASD 在 LPDDR5-PIM 中集成了注意力算法单元和门控任务调度单元，实现了 PIM 侧的注意力链路本地化和亚微秒级任务切换。针对不同 LLM 和自适应草稿算法的实验结果表明，与纯 GPU 基准相比，AHASD 的吞吐量和能效分别提升了高达 4.2 倍和 5.6 倍；与最先进的 GPU+PIM 基准相比，吞吐量和能效分别提升了 1.5 倍和 1.24 倍，且硬件开销低于 DRAM 面积的 3%。

## Abstract
Speculative decoding enhances the inference efficiency of large language models (LLMs) by generating drafts using a small draft language model (DLM) and verifying them in batches with a large target language model (TLM). However, adaptive drafting inference on a mobile single-NPU-PIM system faces idle overhead in traditional operator-level synchronous execution and wasted computation in asynchronous execution due to fluctuations in draft length. This paper introduces AHASD, a task-level asynchronous mobile NPU-PIM heterogeneous architecture for speculative decoding. Notably, AHASD achieves parallel drafting on the PIM and verification on a single NPU through task-level DLM-TLM decoupling and specifically, it incorporates Entropy-History-Aware Drafting Control and Time-Aware Pre-Verification Control to dynamically manage adaptive drafting algorithm execution and pre-verification timing, suppressing invalid drafting based on low-confidence drafts. Additionally, AHASD integrates Attention Algorithm Units and Gated Task Scheduling Units within LPDDR5-PIM to enable attention link localization and sub-microsecond task switching on the PIM side. Experimental results for different LLMs and adaptive drafting algorithms show that AHASD achieves up to 4.2$\times$ in throughput and 5.6$\times$ in energy efficiency improvements over a GPU-only baseline, and 1.5$\times$ in throughput and 1.24$\times$ in energy efficiency gains over the state-of-the-art GPU+PIM baseline, with hardware overhead below 3% of the DRAM area.

---

## 论文详细总结（自动生成）

### AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在移动端的推理受限于内存带宽和计算资源。**投机解码**通过小模型（DLM）生成草稿、大模型（TLM）验证的方式提升效率，但在移动端 NPU-PIM（存内计算）异构系统中面临两大挑战：
*   **同步开销**：传统的算子级同步执行在面对“自适应草稿长度”时，由于计算负载波动，会导致 NPU 或 PIM 频繁处于空闲等待状态。
*   **无效计算**：在异步执行模式下，DLM 可能会基于低置信度的 Token 盲目“超前生成”，导致大量草稿被 TLM 拒绝，浪费计算资源和能效。

#### 2. 方法论：核心思想与关键技术
AHASD 提出了一种**任务级异步异构架构**，核心思想是将 DLM 和 TLM 在任务层面解耦，分别映射到最适合的硬件单元上。
*   **任务级解耦**：将访存密集型的 DLM 草稿生成任务分配给 PIM，将计算密集型的 TLM 验证任务分配给 NPU，通过异步队列进行通信。
*   **熵历史感知草稿控制（EDC）**：通过硬件模块在线学习草稿的 Softmax 熵（Entropy）与接受率之间的关系。当预测到当前草稿置信度较低且领先验证进度过多时，自动抑制后续生成，减少无效功耗。
*   **时间感知预验证控制（TVC）**：建立 NPU 和 PIM 的双向延迟模型。在 NPU 验证期间，若 PIM 有空闲时间，TVC 会调度 PIM 进行小批量预验证，以提前过滤错误草稿，确保 NPU 始终有高质量草稿可验证。
*   **硬件增强设计**：在 LPDDR5-PIM 中集成**注意力算法单元（AAU）**处理非线性算子，并利用**门控任务调度单元**实现亚微秒级的任务切换。

#### 3. 实验设计
*   **模型配置**：涵盖了 OPT (1.3B/6.7B)、LLaMA2 (7B/13B) 和 PaLM-like (8B/30B) 三种规模。
*   **算法场景**：测试了四种主流自适应草稿算法（SpecDec++、SVIP、AdaEDL、BanditSpec）。
*   **数据集**：使用 Alpaca 指令遵循数据集。
*   **Benchmark（对比方法）**：
    1.  **GPU-Only**：在 NVIDIA RTX 4090 (Laptop) 上顺序执行。
    2.  **SpecPIM**：目前最先进的算子级并行 GPU+PIM 异构加速方案。

#### 4. 资源与算力
*   **模拟环境**：使用了周期精确（Cycle-accurate）的模拟器组合：ONNXim（模拟 NPU）和 SAITPublic-PIMSimulator（模拟 PIM）。
*   **硬件参数**：NPU 提供 16 TOPS (INT8) 算力；PIM 基于 LPDDR5，提供 102.4 GOPS 算力，带宽 51.2 GB/s。
*   **训练/运行说明**：论文未提及模型训练时长（属于推理加速研究），实验侧重于端到端的推理延迟和能效评估。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **消融实验**：逐步验证了异步架构、AAU 单元、EDC 模块和 TVC 模块对吞吐量和接受率的贡献。
*   **跨模型/算法验证**：在 3 种模型规模和 4 种算法下均进行了测试，证明了方案的通用性。
*   **硬件开销分析**：利用 CACTI 和 Yosys/OpenROAD 工具进行了面积和功耗的详细评估。
实验设计较为全面，对比基准选择了高性能 GPU 和最新的学术前沿方案，体现了客观性和公平性。

#### 6. 主要结论与发现
*   **性能提升**：相比纯 GPU 基准，AHASD 实现了最高 **4.2×** 的吞吐量提升和 **5.6×** 的能效提升。
*   **超越前沿**：相比 SpecPIM，吞吐量提升 **1.5×**，能效提升 **1.24×**。
*   **低开销**：新增的硬件模块（EDC, TVC, AAU 等）总面积开销仅占 DRAM 芯片面积的 **2.68%**，非常适合集成在移动端 SoC 中。
*   **有效性**：EDC 成功抑制了异步模式下的接受率下降问题，将平均接受率恢复了约 24.6%。

#### 7. 优点
*   **软硬协同创新**：不仅是架构改进，还结合了 LLM 推理过程中的统计学特征（熵）进行硬件控制。
*   **解决动态波动**：通过异步化和延迟建模，有效解决了自适应草稿长度带来的负载不均衡问题。
*   **高能效比**：充分利用了 PIM 的高内部带宽和 NPU 的高计算密度，显著降低了数据搬运功耗。

#### 8. 不足与局限
*   **模拟器依赖**：所有结果均基于模拟器得出，实际芯片制造中的工艺偏差、热管理及复杂的总线竞争可能影响最终表现。
*   **量化限制**：实验主要基于 INT8 量化，对于更低比特（如 FP4/INT4）或混合精度推理的支持情况未详细讨论。
*   **应用范围**：目前主要针对单 NPU-PIM 系统，对于多 NPU 或更复杂的移动端异构拓扑（如包含 DSP/GPU 的协同）尚待探索。

（完）
