---
title: Hardware-Efficient Softmax and Layer Normalization with Guaranteed Normalization for Edge Devices
title_zh: 面向边缘设备的具有归一化保证的硬件高效 Softmax 和层归一化
authors: "Dawon Choi, Hana Kim, Ji-Hoon Kim"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23647v1"
tags: ["query:aiot"]
score: 10.0
evidence: 边缘设备的高效硬件非矩阵乘法运算
tldr: 针对Transformer模型中Softmax和LayerNorm等非GEMM操作在边缘设备上硬件成本高的问题，本文提出了一种保证归一化特性的硬件高效近似方法。该设计在满足NLP和生成式AI对数值精度需求的同时，通过硬件友好的近似算法显著降低了面积开销。实验表明，该方案在保持极高任务精度的前提下，相比现有技术实现了高达11至14倍的面积缩减。
selection_source: fresh_fetch
motivation: 现有的非GEMM近似方法主要针对排序任务，难以满足边缘NLP和生成式AI对归一化数值精度的严格要求。
method: 提出了一种在保持归一化特性（Softmax和为1，LayerNorm标准差为1）的前提下，采用硬件友好型近似计算的架构。
result: 在GLUE和SQuAD等任务上实现了极低的精度损失，且Softmax和LayerNorm的硬件面积分别仅为942μm²和1199μm²。
conclusion: 该研究为边缘设备上的Transformer模型提供了一种兼顾高精度与极低硬件开销的非GEMM操作优化方案。
---

## 摘要
在 Transformer 模型中，非 GEMM（非通用矩阵乘法）操作——特别是 Softmax 和层归一化 (LayerNorm)——由于其非线性特性，往往占据了主要的硬件成本。为了解决这一问题，以往的近似研究主要针对面向排名的任务，这对于分类任务是可以接受的。然而，边缘自然语言处理 (NLP) 应用和边缘生成式 AI 很大程度上是基于面向分数的任务进行评估的，因此具有归一化保证的非 GEMM 操作至关重要。我们提出了一种面向边缘设备的、具有归一化保证的硬件高效 Softmax 和 LayerNorm。我们的设计采用了硬件高效的近似方法，同时保留了归一化特性（Softmax: $\sum p = 1$，LayerNorm: $σ= 1$）。我们的架构使用 Verilog HDL 描述，并采用三星 28nm CMOS 工艺进行综合。在精度评估中，我们实现了高精度且性能下降极小：GLUE 提升 0.07%，SQuAD 下降 0.01%，困惑度 (perplexity) 下降 0.09%。实现结果表明，我们的架构非常小巧：Softmax 为 $942\,μm^2$，LayerNorm 为 $1199\,μm^2$。与现有技术相比，我们的面积分别减少了高达 11 倍和 14 倍。

## Abstract
In Transformer models, non-GEMM (non-General Matrix Multiplication) operations -- especially Softmax and Layer Normalization (LayerNorm) -- often dominate hardware cost due to their nonlinear nature. To address this, previous approximation studies mainly target rank-oriented tasks, which is acceptable for classification. However, edge Natural Language Processing (NLP) applications and edge generative AI are largely evaluated based on score-oriented tasks, so normalization-guaranteed non-GEMM operations are essential. We propose a hardware-efficient Softmax and LayerNorm with Guaranteed Normalization for Edge devices. Our design employs hardware-efficient approximation methods while preserving the normalization (Softmax: $\sum p = 1$, LayerNorm: $σ= 1$). Our architecture is described in Verilog HDL and synthesized using the Samsung 28nm CMOS process. In accuracy evaluation, we achieve high accuracy with minimal degradation: GLUE +0.07%, SQuAD -0.01%, perplexity -0.09%. Implementation results show that our architecture is small: $942\,μm^2$ for Softmax, $1199\,μm^2$ for LayerNorm. Compared to the state of the art, we achieve up to 11x and 14x reduction in area, respectively.

---

## 论文详细总结（自动生成）

这篇论文针对边缘设备上的 Transformer 模型推理，提出了一种兼顾硬件效率与数值精度的 Softmax 和层归一化（LayerNorm）实现方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在 Transformer 架构中，非矩阵乘法（non-GEMM）操作（如 Softmax、LayerNorm）虽然计算量占比不如矩阵乘法，但由于涉及指数、除法和平方根等非线性运算，往往成为硬件实现的瓶颈。
*   **核心问题**：现有的硬件近似方法大多针对“面向排名（Rank-oriented）”的任务（如文本分类），只要相对顺序正确即可。但在“面向分数（Score-oriented）”的任务（如问答系统 SQuAD、生成式 AI 的困惑度计算）中，近似导致的“归一化误差”（即 Softmax 输出和不为 1，或 LayerNorm 标准差不为 1）会严重破坏模型精度。
*   **研究目标**：设计一种既能大幅减少硬件面积，又能保证严格归一化特性（$\sum p = 1, \sigma = 1$）的硬件架构。

### 2. 核心方法论
论文提出了两种关键的硬件友好型算法及其对应的电路架构：

*   **硬件高效 Softmax（无乘法器/除法器设计）**：
    *   **指数近似**：采用“两级查找表（LUT）分解法”。将输入值分解为粗略项（Coarse）和残差项（Residual），利用公式 $e^{-q} = e^{-R \cdot frac} \cdot e^{-rem}$，通过两个极小的 LUT（分别仅 7 和 8 个条目）实现高精度指数运算。
    *   **归一化除法**：开发了一种基于二进制移位-减法算法的定点除法器（FxP Div），利用归一化条件近似倒数缩放因子，避免了昂贵的通用除法器。
*   **硬件高效 LayerNorm（牛顿迭代法）**：
    *   **平方根倒数近似**：采用基于前导零检测（LOD）的初始值估计，结合重构的牛顿迭代法（Newton Method），仅需 2 个周期即可完成高精度的平方根倒数计算。
    *   **架构优化**：通过将除法转换为乘法操作，进一步压缩了硬件面积。

### 3. 实验设计
*   **数据集与场景**：
    *   **GLUE 基准测试**：评估分类任务（面向排名）。
    *   **SQuAD v1.1**：评估问答任务（面向分数）。
    *   **WikiText**：评估语言模型的困惑度（Perplexity，对归一化误差极度敏感）。
*   **模型基准**：使用 BERT-base 和 GPT-Neo 1.3B 作为评估模型。
*   **对比对象**：
    *   **算法对比**：FP32 基准以及多种现有的近似方法（如 Softermax、RMSNorm 等）。
    *   **硬件对比**：与近年来发表在 TCAS-I、TCAS-II、SCIS 等顶级期刊/会议上的 4 种先进硬件架构进行面积和频率对比。

### 4. 资源与算力
*   **硬件实现**：使用 Verilog HDL 描述，采用 **Samsung 28nm CMOS** 工艺进行综合。
*   **性能参数**：目标工作频率为 250 MHz（最高可达 1.0 GHz）。
*   **算力说明**：论文主要关注推理阶段的硬件电路综合结果（面积、功耗、延迟），未详细说明模型训练所使用的 GPU 资源，因为其核心贡献在于硬件架构设计而非模型训练。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从判别式模型（BERT）到生成式模型（GPT）的多种任务，实验设置较为全面。
*   **对比维度**：不仅对比了最终的任务精度（F1 分数、困惑度等），还详细分析了归一化误差的分布情况（图 5 显示 100% 的 LayerNorm 误差处于极低范围）。
*   **客观性**：通过在不同工艺节点和不同精度的现有技术之间进行横向对比，证明了其在面积缩减上的显著优势。

### 6. 主要结论与发现
*   **精度保持**：在 GLUE 任务上精度甚至提升了 0.07%，在 SQuAD 上仅下降 0.01%，在困惑度上仅下降 0.09%。相比之下，不保证归一化的现有方法在困惑度上会下降 13.68%。
*   **硬件优势**：
    *   **Softmax 面积**：仅 $942\,\mu m^2$，比对比方法缩小了高达 **11 倍**。
    *   **LayerNorm 面积**：仅 $1199\,\mu m^2$，比对比方法缩小了高达 **14 倍**。
*   **归一化重要性**：实验证明，保证归一化特性是边缘设备运行生成式 AI 和复杂 NLP 任务的关键。

### 7. 优点与亮点
*   **理论与实践结合**：深刻揭示了“归一化误差”与“任务类型（排名 vs 分数）”之间的关系。
*   **极高的面积效率**：通过巧妙的数学分解（指数分解）和迭代算法（牛顿法），在极小的硅片面积上实现了高性能运算。
*   **通用性**：该设计不依赖于特定的模型权重，具有较强的通用性，适用于各种 Transformer 变体。

### 8. 不足与局限
*   **延迟权衡**：为了保证归一化精度，其延迟为 $N+1$ 个周期，虽然在可接受范围内，但在极高性能要求的场景下仍有优化空间。
*   **工艺依赖性**：实验数据主要基于 28nm 工艺，在更先进的先进制程（如 5nm/3nm）下的功耗和热密度表现未做进一步讨论。
*   **应用范围**：主要针对推理端优化，未讨论该近似方法是否适用于硬件加速的训练过程。

（完）
