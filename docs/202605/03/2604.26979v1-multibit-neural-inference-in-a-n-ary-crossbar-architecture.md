---
title: Multibit neural inference in a N-ary crossbar architecture
title_zh: N 进制交叉阵列架构中的多位神经网络推理
authors: "Anatole Moureaux, Anthony Lopes Temporao, Flavio Abreu Araujo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26979v1"
tags: ["query:aiot"]
score: 9.0
evidence: 通过存内计算实现能效神经推理
tldr: "本研究针对内存计算（IMC）中的能效问题，提出了一种用于N进制交叉阵列架构的仿真框架，旨在通过模拟矩阵向量乘法（MVM）实现高效的神经网络推理。研究利用具有4种状态的磁隧道结（MTJ）阵列，在XOR和MNIST分类任务中验证了该架构的有效性，其中MNIST准确率达到94.48%。通过分析系统非理想性和随机噪声，研究确定了权重量化是主要的误差来源，并揭示了单元状态数与推理精度之间的平衡关系，为优化多位神经推理提供了理论指导。"
selection_source: fresh_fetch
motivation: 利用内存计算技术在交叉阵列中执行模拟矩阵向量乘法，以提升神经网络推理的能效。
method: 提出一种N进制交叉阵列仿真框架，并采用多状态磁隧道结（MTJ）器件进行建模与推理验证。
result: "在MNIST分类中取得94.48%的准确率，并发现阵列平均效应使得随机噪声的影响小于系统性误差。"
conclusion: 权重量化是主要误差源，通过平衡量化误差与电阻分辨率可找到最小化推理误差的最佳单元状态数。
---

## 摘要
存内计算 (IMC) 通过在存储交叉阵列中计算模拟矩阵-向量乘法 (MVM)，实现了高能效的神经网络推理。在这项工作中，我们提出了一个针对 N 进制交叉阵列架构的仿真框架，该框架能在最小化实现假设的情况下获取 MVM 结果。使用由 (4x4) 个 4 态磁隧道结 (MTJ) 组成的模拟交叉阵列，成功推理了 XOR 和 MNIST 分类任务。MNIST 准确率达到 94.48%（软件基准为 97.56%）。通过使用 PCA 降维，进一步缩小了软硬件性能差距。我们确定权重量化是主要的误差源，并研究了其与系统非理想性和随机噪声共同产生的影响。我们发现，由于阵列的平均效应，特定单元的随机噪声比系统误差的危害更小。最后，我们展示了每个单元的最佳状态数，该数量在量化误差与电阻状态分辨率之间取得平衡，从而最小化总 MVM 误差。

## Abstract
In-memory computing (IMC) enables energy-efficient neural network inference by computing analog matrix-vector multiplications (MVM) in memory crossbar arrays. In this work we present a simulation framework for N-ary crossbar architectures that retrieves MVM results with minimal implementation assumptions. The XOR and MNIST classification tasks were successfully inferred using a simulated crossbar array of (4x4) 4-states magnetic tunnel junctions (MTJ). MNIST accuracy reached 94.48% (vs. 97.56% software baseline). The software-hardware performance gap was further reduced using PCA dimensionality reduction. We identified weight quantization as the primary error source, and studied its impact alongside systematic nonidealities and random noise. We find that cell-specific random noise is less detrimental than systematic errors due to averaging across the array. Finally, we demonstrate an optimal number of states per cell that balances quantization error against resistance state resolution to minimize total MVM error.

---

## 论文详细总结（自动生成）

这篇论文《N 进制交叉阵列架构中的多位神经网络推理》由比利时鲁汶大学的研究团队撰写，探讨了如何利用多态存储器件提升存内计算（IMC）的效率与精度。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统冯·诺依曼架构面临“冯·诺依曼瓶颈”和“存储墙”限制，导致 AI 推理能效低下。存内计算（IMC）通过在交叉阵列中直接执行模拟矩阵向量乘法（MVM）来解决此问题，但面临**模拟信号转换误差、器件非理想性（噪声）以及权重量化损失**等挑战。
*   **研究动机**：目前大多数 IMC 研究集中在二进制或三进制权重，限制了计算密度。本文旨在开发一个通用的仿真框架，支持多位（N 进制）推理，并深入探讨在存在噪声和非理想性的情况下，如何优化多态器件的使用。

### 2. 核心方法论
*   **N 进制交叉阵列框架**：提出一种通用的数学框架，将权重矩阵编码为 $N$ 个等间距的电阻水平。
*   **结果检索算法**：推导出了一个关键公式（公式 13），仅需输入信号和测得的输出电压，即可通过已知的线性缩放因子和偏移量（$a_R, a_I, b_R, b_I$）还原 MVM 结果。这种方法无需改变阵列内部架构，具有高度通用性。
*   **权重量化策略**：采用基于 Powell 最小化方法的优化算法，将全精度权重映射到 $N$ 个等间距的量化状态，类似于约束化的 K-means 聚类。
*   **降维优化**：引入主成分分析（PCA）对输入数据进行降维，以减少在有限尺寸交叉阵列上进行分块计算（复用）所需的子操作次数。

### 3. 实验设计
*   **任务与数据集**：
    *   **XOR 逻辑运算**：用于验证框架的基本非线性分类能力。
    *   **MNIST 手写数字识别**：用于评估复杂任务下的推理准确率。
*   **硬件模拟对象**：基于实验数据建模的 4 态磁隧道结（M2TJ）器件，模拟 (4×4) 的交叉阵列。
*   **Benchmark（基准）**：全精度软件推理结果（Software Baseline）。
*   **对比维度**：对比了不同状态数 $N$（1 到 64）、系统性非理想性（如磁层对齐误差）以及单元特定随机噪声对 RMSE（均方根误差）的影响。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该研究主要基于 Python（Scipy 库）进行的仿真模拟，而非大规模深度学习训练，其对算力的需求相对较低，重点在于物理特性的数学建模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 MNIST 进行了 10,000 个样本的测试。
    *   在误差评估中，每组实验平均运行了 2000 次随机 MVM 以确保统计显著性。
    *   进行了关于状态数 $N$、系统误差 $\sigma_{NL}$ 和随机噪声 $\sigma_{\perp}$ 的多维度消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从理想状态到各种非理想环境的演变，客观地展示了量化误差与环境噪声之间的权衡。

### 6. 主要结论与发现
*   **准确率表现**：在 4 态（2-bit）配置下，MNIST 推理准确率达到 **94.48%**（软件基准为 97.56%）。
*   **误差来源**：**权重量化**是主要的误差来源，误差随状态数 $N$ 的增加呈 $1/N$ 趋势下降。
*   **噪声鲁棒性**：由于阵列的**平均效应**，单元特定的随机噪声对准确率的影响小于相同幅度的系统性误差。
*   **最佳状态数**：发现存在一个“最佳状态数 $N_{opt}$”。在噪声较大的环境下，减少状态数反而能提高鲁棒性，因为状态间的电阻分辨率更高。
*   **PCA 的作用**：PCA 不仅减少了 88.69% 的计算子操作，还通过滤除输入噪声缩小了软硬件之间的性能差距。

### 7. 优点与亮点
*   **通用性强**：提出的 MVM 检索框架不依赖于特定的物理器件特性，可推广至 RRAM、PCM 等多种 memristor 技术。
*   **数学严谨**：通过严谨的线性代数推导解决了模拟信号还原问题，简化了外围电路的设计逻辑。
*   **洞察深刻**：揭示了“状态数 vs 噪声分辨率”的权衡关系，为未来多态存储器件的硬件设计提供了理论指导。

### 8. 不足与局限
*   **阵列规模较小**：实验主要基于 (4×4) 的微型阵列模拟，虽然通过复用技术处理了大矩阵，但未充分探讨超大规模阵列中的寄生电阻（IR drop）和漏电流问题。
*   **仿真局限**：研究完全基于数字孪生仿真，尚未在真实的物理交叉阵列硬件上进行流片验证。
*   **假设前提**：框架假设电阻状态是完美等间距的，而在实际器件制造中，实现高精度的等间距多态电阻仍是一个巨大的工艺挑战。

（完）
