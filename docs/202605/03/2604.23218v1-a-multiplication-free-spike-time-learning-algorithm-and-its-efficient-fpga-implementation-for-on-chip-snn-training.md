---
title: A Multiplication-Free Spike-Time Learning Algorithm and its Efficient FPGA Implementation for On-Chip SNN Training
title_zh: 一种无乘法脉冲时间学习算法及其用于片上 SNN 训练的高效 FPGA 实现
authors: "Maryam Mirsadeghi, Mojtaba Mirbagheri, Saeed Reza Kheradpisheh"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23218v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于片上SNN训练的无乘法算法及FPGA实现
tldr: 针对脉冲神经网络（SNN）片上训练面临的硬件挑战，本文提出一种无乘法器的脉冲时间学习算法及其FPGA实现方案。该方法通过消除浮点运算和梯度存储，构建了全事件驱动的数字训练流水线。在Xilinx Artix-7 FPGA上的实验表明，该架构在保持高准确率的同时，显著降低了资源消耗和功耗，为边缘环境下的实时SNN学习提供了高效、可扩展的硬件解决方案。
selection_source: fresh_fetch
motivation: 脉冲神经网络的直接片上监督训练在硬件实现上存在计算复杂度和资源消耗高的挑战。
method: 提出一种基于脉冲时间的无乘法器学习算法，通过消除浮点运算和显式梯度存储实现全事件驱动的FPGA训练流水线。
result: "在Artix-7 FPGA上实现了低资源占用和高运行速度，并在MNIST和Fashion-MNIST数据集上分别达到96.5%和84.8%的准确率。"
conclusion: 该框架为边缘计算环境提供了一种实用且高效的片上SNN实时学习硬件解决方案。
---

## 摘要
脉冲神经网络 (SNN) 为低功耗、事件驱动的智能提供了生物启发式基础，但其直接片上有监督训练仍是主要的硬件挑战。本文提出了一种专为高效 FPGA 实现而设计的无乘法、基于脉冲时间的学习算法。该方法消除了浮点运算和显式梯度存储，实现了完全事件驱动的数字训练流水线。该架构在 Xilinx Artix-7 FPGA 上实现，在保持具有竞争力的准确率的同时，实现了高运行速度和极低的资源占用。结果表明，该学习算法能有效地映射到可重构硬件上，兼顾了计算效率和能效。软件仿真进一步验证了其可扩展性，在 MNIST 和 Fashion-MNIST 数据集上分别达到了 96.5% 和 84.8% 的准确率。凭借其脉冲驱动和无乘法操作，所提出的框架为边缘环境下的实时片上 SNN 学习提供了一种实用且可扩展的硬件解决方案。

## Abstract
Spiking Neural Networks (SNNs) offer a biologically inspired foundation for low-power, event-driven intelligence, yet their direct on-chip supervised training remains a key hardware challenge. This paper presents a multiplication-free, spike-time-based learning algorithm specifically designed for efficient FPGA realization. The proposed approach eliminates floating-point arithmetic and explicit gradient storage, enabling a fully event-driven, digital training pipeline. Implemented on a Xilinx Artix-7 FPGA, the architecture achieves high operating speed and minimal resource usage while maintaining competitive accuracy. These results demonstrate that the learning algorithm effectively maps onto reconfigurable hardware, achieving both computational and energy efficiency. Software simulations further validate scalability, with 96.5\% and 84.8\% accuracy on MNIST and Fashion-MNIST. With its spike-driven and multiplier-free operation, the proposed framework delivers a practical and scalable hardware solution for real-time, on-chip SNN learning in edge environments.

---

## 论文详细总结（自动生成）

以下是对论文《A Multiplication-Free Spike-Time Learning Algorithm and its Efficient FPGA Implementation for On-Chip SNN Training》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）虽然在推理阶段具有极高的能效比，但其**片上有监督训练**（On-chip Supervised Training）面临巨大的硬件挑战。
*   **研究动机**：
    *   **计算开销高**：传统的反向传播算法依赖大量的浮点乘法和复杂的梯度存储，这与资源受限的边缘设备（如FPGA、ASIC）不兼容。
    *   **现有方案局限**：现有的神经形态芯片（如Loihi、TrueNorth）在推理上表现出色，但在支持深度SNN的在线、有监督学习方面能力有限。
    *   **硬件不匹配**：冯·诺依曼架构（CPU/GPU）处理事件驱动的脉冲数据效率低下。
*   **目标**：开发一种完全**无乘法器（Multiplication-Free）**、基于脉冲时间的学习算法，并实现高效的FPGA流水线。

### 2. 方法论
该论文提出了一种统一了算法简洁性与硬件效率的框架，核心思想是将梯度信息完全编码在脉冲延迟（Spike Latencies）中。

*   **核心思想**：
    *   **单脉冲时间编码（Single-spike Temporal Coding）**：每个神经元在每个样本处理周期内最多只发射一个脉冲，信息由脉冲发射的时间（延迟）承载。
    *   **无乘法训练**：通过定点数运算、基于加法器的计算树以及位移操作，彻底消除了训练过程中的浮点乘法。
*   **关键技术细节**：
    *   **前向传播**：采用非漏电集成火（Non-leaky IF）神经元。输入图像通过强度-延迟转换变为脉冲流。神经元累加权重，当膜电位超过阈值时记录发射时间 $t$。
    *   **反向传播（脉冲时间位移）**：定义时间误差为目标发射时间与实际发射时间的差值。
    *   **梯度编码为脉冲**：将梯度值（$\delta$）线性映射为“反向脉冲时间”（$\tau$）。正梯度产生正向符号脉冲，负梯度产生负向符号脉冲。
    *   **硬件实现**：在FPGA上设计了模块化的处理单元，利用BRAM存储权重，通过并行加法树处理突触输入，不使用任何DSP切片（DSP Slices）。

### 3. 实验设计
*   **数据集**：
    *   **MNIST**：标准手写数字识别。
    *   **Fashion-MNIST**：更复杂的服装分类任务。
    *   **8×8 Digits**：用于硬件验证的小型数据集。
*   **Benchmark（基准方法）**：
    *   对比了多种先进的SNN训练算法：S4NN、STiDi-BP、BS4NN、Mostafa等。
    *   硬件对比对象：**HaSiST**（一种基于速率编码和浮点运算的FPGA SNN引擎）。
*   **评估指标**：分类准确率、FPGA资源占用（LUT、寄存器）、最高运行频率（$F_{max}$）、吞吐量（FeaPS）。

### 4. 资源与算力
*   **硬件平台**：Xilinx Artix-7 FPGA。
*   **算力细节**：
    *   **权重精度**：12位定点数（Q5.7格式）。
    *   **资源消耗**：前向传播每突触仅需0.66个寄存器和1.68个LUT；反向传播每突触需1.33个寄存器和5.1个LUT。
    *   **未明确说明的部分**：论文未详细列出软件仿真阶段使用的具体GPU型号及训练总时长，重点放在了FPGA的硬件性能参数上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对MNIST和Fashion-MNIST进行了多层架构（如784-400-10, 784-400-400-10）的软件仿真。
    *   针对8×8 Digits数据集完成了完整的FPGA RTL级实现与验证。
*   **充分性评价**：实验设计较为充分。作者不仅验证了算法在标准数据集上的准确率（算法有效性），还通过与HaSiST的直接对比证明了硬件实现的优越性（硬件高效性）。涵盖了从二层到三层网络的扩展性分析，实验结果具有客观性。

### 6. 主要结论与发现
*   **准确率竞争力**：在MNIST上达到**96.5% - 97.4%**，在Fashion-MNIST上达到**84.8%**，与需要大量乘法运算的浮点SNN算法性能相当。
*   **硬件效率飞跃**：
    *   **无DSP使用**：完全不依赖DSP切片，这在深度学习硬件实现中非常罕见。
    *   **资源节省**：相比HaSiST，反向传播阶段的LUT占用减少了约**7.4倍**，寄存器占用减少了**2倍**。
    *   **速度提升**：训练阶段的最高频率达到105.3 MHz，比HaSiST（50 MHz）提升了**110.6%**。
*   **稀疏性优势**：MNIST前向传播中平均只有约75%的突触处于活跃状态，进一步降低了功耗。

### 7. 优点
*   **真正的无乘法化**：通过将梯度转化为脉冲时间，巧妙地避开了SNN训练中最耗资源的乘法运算。
*   **全事件驱动流水线**：前向和反向传播均基于脉冲事件，充分利用了SNN的异步和稀疏特性。
*   **硬件资源极低**：极低的资源占用意味着可以在更廉价、更小型的FPGA芯片上部署更复杂的网络。

### 8. 不足与局限
*   **数据集规模限制**：硬件实现目前仅在8×8 Digits小型数据集上进行了完整验证，对于超大规模图像（如ImageNet）的片上训练表现尚待观察。
*   **伪脉冲开销**：为了处理不发射脉冲的神经元，系统会发送“默认时间包”（Placeholder spikes），这在一定程度上削弱了完全事件驱动的节能潜力。
*   **时间分辨率权衡**：采用16个时间步的离散化处理虽然提高了速度，但可能在处理极高精度要求的时序任务时产生量化误差。

（完）
