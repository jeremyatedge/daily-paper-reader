---
title: LLM-Guided Runtime Parameter Optimization for Energy-Efficient Model Inference
title_zh: LLM引导的能效模型推理运行时参数优化
authors: "Katelyn Crumpacker, Dimitrios Nikolopoulos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27032v1"
tags: ["query:aiot"]
score: 9.0
evidence: 通过参数优化实现能效模型推理
tldr: 随着大语言模型（LLM）广泛应用，其推理过程的高能耗成为严峻挑战。传统运行时参数优化方法耗时且依赖专业知识。本文提出一种人机协作流程，利用LLM辅助进行运行时参数优化。通过特定的反馈提示词，LLM能比传统搜索方法更快地找到节能的推理参数，并能灵活适应不同硬件环境。实验表明，该方法在收敛速度和节能效果上均优于基准方法和Sobol采样。
selection_source: fresh_fetch
motivation: 针对大语言模型推理能耗高且传统参数优化方法效率低下的问题，寻求一种更快速、智能的节能优化方案。
method: 提出一种基于LLM辅助的人机协作优化流程，通过迭代反馈提示词引导LLM搜索最优运行时参数。
result: 增强型提示词模板平均仅需3.4次提示即可收敛，优于基准的5.2次，且在单位Token能耗和收敛速度上均超过Sobol采样。
conclusion: 利用LLM进行运行时参数优化是一种高效且灵活的方法，能显著提升模型推理的能源效率并缩短优化周期。
---

## 摘要
大语言模型（LLM）已成为许多现实世界工作流中不可或缺的一部分。然而，LLM消耗大量能源，在这些工具需求规模不断扩大的背景下，这已成为一个重大问题。随着LLM被集成到不同的工作流中，出现了各种应用程序来应对这些工具的推理运行挑战。这引发了另一个问题，即如何选择这些服务的运行时参数值，以最小化能源消耗。这通常需要对应用程序有深入的了解，或者使用传统的优化方法，而这些方法可能需要数天时间才能找到最优值。在这项工作中，我们创建了一个包含LLM辅助运行时参数优化的人机回环（human-in-the-loop）流程，以解决这一问题。通过人工创建的特定反馈提示方法，基于聊天的LLM可以比传统搜索方法更快地迭代寻找高能效的推理参数。LLM还可以针对不同的硬件设置量身定制解决方案，并轻松考虑其他系统约束。与基准相比，增强型提示模板能够以平均3.4次提示收敛至阈值以下，而基准平均需要5.2次提示，且始终能实现更低的单Token最终能耗。增强型提示模板在收敛速度上也优于Sobol采样。

## Abstract
Large Language Models (LLMs) have become an integral part of many real-world workflows. However, LLMs consume a lot of energy, which becomes a large concern in the scale of the demand for these tools. As LLMs become integrated into different workflows, different applications have arisen to deal with the challenge of running inference for these tools. This raises another issue of choosing the runtime parameter values for these services in order to minimize the energy consumption. Oftentimes this requires deep knowledge of the application or traditional optimization methods that can take days to find optimal values. In this work, we created a human-in-the-loop flow with LLM-assisted runtime parameter optimization in order to solve this issue. With human-created, specific feedback prompting methods, chat-based LLMs can iteratively find energy-efficient inference parameters faster than traditional search methods. LLMs can also tailor their solutions to different hardware setups and easily take into account other system constraints. The enhanced prompt template was able to converge below the threshold at an average of 3.4 prompts compared to the baseline, which converged in an average of 5.2 prompts, and consistently achieved lower final energy per token. The enhanced prompt template also outperformed Sobol sampling in convergence speed.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-Guided Runtime Parameter Optimization for Energy-Efficient Model Inference》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）推理过程能耗极高，而优化推理引擎的运行时参数（如批处理大小、内存利用率、功率限制等）以降低能耗通常需要深厚的领域知识，或依赖耗时数日的传统优化算法。
*   **研究动机**：
    *   **能效挑战**：随着LLM集成到各类工作流，规模化推理带来的电力负担成为不可忽视的问题。
    *   **传统方法局限**：网格搜索或贝叶斯优化等黑盒优化方法在覆盖高维参数空间时效率较低，且难以灵活适应不同的硬件约束。
    *   **LLM潜力**：探索是否可以利用LLM作为智能代理，通过理解系统反馈来快速引导参数搜索，从而实现更高效的节能优化。

### 2. 方法论
论文提出了一种**LLM引导的运行时参数优化框架**，核心思想是利用外部LLM（如Claude）作为决策代理，通过“人机回环”（Human-in-the-loop）模式进行迭代优化。
*   **核心流程**：
    1.  **初始化**：人工设定搜索空间和初始提示词。
    2.  **执行与测量**：在目标硬件上运行推理任务，利用NVML（NVIDIA管理库）实时采集能耗（J）、耗时（s）、吞吐量（tokens/s）等指标。
    3.  **反馈编码**：将实验结果（如单Token能耗）填入提示词模板。
    4.  **LLM建议**：LLM根据历史表现建议下一组参数。
    5.  **迭代**：重复上述过程直至达到预设的能耗阈值或收敛。
*   **关键技术细节（提示词工程）**：
    *   **增强型提示词（Enhanced Prompt）**：包含背景信息（硬件规格、参数含义）、结构化任务框架、重复强调目标以及搜索过程引导。
    *   **基准提示词（Baseline Prompt）**：仅提供最基本的参数范围和反馈数据，不提供背景知识。

### 3. 实验设计
*   **实验场景**：
    1.  **文本推理**：使用 vLLM 引擎运行 Llama-3.2-3B 模型。
    2.  **多模态推理**：使用 PyTorch 运行 Qwen3-VL-4B-Instruct 模型，处理图像+文本输入。
*   **数据集**：
    *   文本：固定1,000条提示词的数据集。
    *   多模态：500张图像配以随机采样的问答对。
*   **对比基准（Benchmark）**：
    *   **默认配置**：推理引擎的初始默认参数。
    *   **Sobol采样**：一种经典的准随机序列采样方法，用于均匀覆盖参数空间。
    *   **提示词消融**：对比“增强型”与“基准型”提示词模板的效果。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备一颗 **NVIDIA Tesla V100 PCIe GPU (16GB)**。
*   **算力消耗**：
    *   **Sobol采样**：在vLLM场景下评估30个配置需耗时约24小时；在PyTorch场景下评估64个配置需约6小时。
    *   **LLM优化过程**：增强型提示词平均仅需3.4次迭代（每次迭代运行一次数据集推理）即可收敛。
    *   **外部LLM开销**：估算每次调用外部LLM（Claude）消耗约3Wh电能，计算得出在运行约6个工作负载后，优化带来的节能收益即可抵消优化本身的电耗开销。

### 5. 实验数量与充分性
*   **实验规模**：针对每种提示词策略进行了**5次独立重复实验**，以消除LLM输出随机性的影响。
*   **充分性评价**：
    *   **统计显著性**：使用了配对t检验（p=0.0086）和Cohen's d效应量分析，证明了增强型提示词在收敛速度上的显著优势。
    *   **跨平台验证**：实验覆盖了vLLM和原生PyTorch两个后端，以及纯文本和多模态两种任务，验证了方法的通用性。
    *   **对比客观性**：LLM与Sobol采样在相同的搜索空间和评估环境下运行，对比具有说服力。

### 6. 主要结论与发现
*   **收敛速度快**：增强型提示词平均只需**3.4次**提示即可达到节能阈值，显著快于基准提示词（5.2次）和Sobol采样（需完成全部采样）。
*   **节能效果显著**：在vLLM/Llama场景下，单Token能耗从默认的3.1 J降至约1.7 J（降低约45%）；在多模态场景下，能耗降低幅度甚至超过80%。
*   **性能意外提升**：虽然仅针对“能耗”进行优化，但优化后的配置使吞吐量从33.5 tokens/s提升至59 tokens/s（**增长76%**），实现了能效与性能的帕累托改进。
*   **提示词工程至关重要**：提供硬件背景知识和结构化引导能显著降低LLM建议无效配置（如导致显存溢出OOM）的概率。

### 7. 优点
*   **高效性**：相比于需要数十次甚至上百次运行的传统搜索算法，LLM引导的方法极大地缩短了优化周期。
*   **易用性与通用性**：无需开发者具备深厚的底层内核知识，且框架不依赖于特定的推理引擎。
*   **多目标潜力**：实验证明该方法能同时改善能耗和吞吐量，显示出处理复杂系统权衡的能力。

### 8. 不足与局限
*   **硬件覆盖有限**：实验仅在单节点、单GPU（V100）环境下完成，未测试多节点分布式推理等更复杂的场景。
*   **人机回环依赖**：目前仍需要人工构建提示词和监控实验过程，尚未实现完全自动化的闭环优化。
*   **随机性风险**：尽管有提示词工程约束，LLM的随机输出仍可能导致优化过程的不稳定。
*   **应用限制**：对于极小规模的任务，优化过程本身的电耗（调用外部LLM）可能超过节省的电能，需达到一定任务规模（如文中提到的6个工作负载以上）才具备经济效益。

（完）
