---
title: "AHASD: Asynchronous Heterogeneous Architecture for LLM Adaptive Drafting Speculative Decoding on Mobile Devices"
title_zh: AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构
authors: "Ma Zirui, Fan Zhihua, Li Wenxing, Wu Haibin, Zhang Fulin, Ye Xiaochun, Li Wenming"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25326v2"
tags: ["query:aiot"]
score: 9.0
evidence: 移动端NPU-PIM系统上LLM的异步异构架构
tldr: 针对移动端大模型推理中投机采样存在的同步开销和计算浪费问题，本文提出AHASD架构。该架构采用任务级异步异构设计，在LPDDR5-PIM上进行草稿生成，在NPU上进行验证。通过引入熵历史感知和时间感知控制机制，动态优化草稿长度与验证时机，并集成专用硬件单元提升调度效率。实验表明，AHASD显著提升了推理吞吐量和能效比，且硬件开销极低。
selection_source: fresh_fetch
motivation: 传统移动端投机采样在同步执行时存在空闲开销，而在异步执行时因草稿长度波动导致计算资源浪费。
method: 提出一种任务级异步异构架构，通过解耦DLM与TLM任务，并结合动态草稿控制与PIM硬件优化实现高效并行。
result: "相比现有GPU+PIM方案，AHASD在吞吐量和能效上分别提升了1.5倍和1.24倍，且DRAM面积开销低于3%。"
conclusion: AHASD通过软硬件协同设计，有效解决了移动端大模型投机采样的效率瓶颈，是实现高性能移动端AI推理的有效方案。
---

## 摘要
投机解码通过使用小型草稿语言模型（DLM）生成草稿并由大型目标语言模型（TLM）进行批量验证，提升了大语言模型（LLM）的推理效率。然而，在移动端单 NPU-PIM 系统上，由于草稿长度的波动，传统的算子级同步执行面临空闲开销，而异步执行则存在计算浪费。本文提出了 AHASD，一种用于投机解码的任务级异步移动 NPU-PIM 异构架构。AHASD 通过任务级 DLM-TLM 解耦，实现了 PIM 上的并行草稿生成与单 NPU 上的验证。具体而言，它引入了熵历史感知草稿控制和时间感知预验证控制，以动态管理自适应草稿算法的执行与预验证时机，从而抑制基于低置信度草稿的无效生成。此外，AHASD 在 LPDDR5-PIM 中集成了注意力算法单元和门控任务调度单元，实现了 PIM 端的注意力链路本地化和亚微秒级任务切换。针对不同 LLM 和自适应草稿算法的实验结果表明，与纯 GPU 基准相比，AHASD 实现了高达 4.2 倍的吞吐量提升和 5.6 倍的能效提升；与最先进的 GPU+PIM 基准相比，吞吐量和能效分别提升了 1.5 倍和 1.24 倍，且硬件开销低于 DRAM 面积的 3%。

## Abstract
Speculative decoding enhances the inference efficiency of large language models (LLMs) by generating drafts using a small draft language model (DLM) and verifying them in batches with a large target language model (TLM). However, adaptive drafting inference on a mobile single-NPU-PIM system faces idle overhead in traditional operator-level synchronous execution and wasted computation in asynchronous execution due to fluctuations in draft length. This paper introduces AHASD, a task-level asynchronous mobile NPU-PIM heterogeneous architecture for speculative decoding. Notably, AHASD achieves parallel drafting on the PIM and verification on a single NPU through task-level DLM-TLM decoupling and specifically, it incorporates Entropy-History-Aware Drafting Control and Time-Aware Pre-Verification Control to dynamically manage adaptive drafting algorithm execution and pre-verification timing, suppressing invalid drafting based on low-confidence drafts. Additionally, AHASD integrates Attention Algorithm Units and Gated Task Scheduling Units within LPDDR5-PIM to enable attention link localization and sub-microsecond task switching on the PIM side. Experimental results for different LLMs and adaptive drafting algorithms show that AHASD achieves up to 4.2$\times$ in throughput and 5.6$\times$ in energy efficiency improvements over a GPU-only baseline, and 1.5$\times$ in throughput and 1.24$\times$ in energy efficiency gains over the state-of-the-art GPU+PIM baseline, with hardware overhead below 3\% of the DRAM area.

---

## 论文详细总结（自动生成）

### AHASD：面向移动设备大语言模型自适应草稿投机解码的异步异构架构总结

#### 1. 核心问题与研究动机
*   **核心问题**：在移动端（如手机、嵌入式设备）部署大语言模型（LLM）时，受限于内存带宽和算力，推理速度较慢。投机解码（Speculative Decoding）虽能加速推理，但在移动端 NPU-PIM（近存计算）异构系统中面临两大挑战：
    1.  **同步开销**：传统的算子级同步执行在面对“自适应草稿长度”时，会导致 NPU 或 PIM 频繁出现空闲等待。
    2.  **计算浪费**：异步执行时，若草稿模型（DLM）盲目超前生成低质量草稿，会被目标模型（TLM）大量拒绝，造成无效能耗。
*   **研究动机**：设计一种任务级异步架构，通过软硬件协同优化，在保证生成质量的同时，最大化移动端异构硬件的并行效率。

#### 2. 方法论：核心思想与关键技术
AHASD 提出了一种**任务级异步异构执行框架**，核心思想是将内存密集型的草稿生成（DLM）卸载到 PIM，将计算密集型的验证（TLM）交给 NPU，并实现两者的解耦运行。
*   **熵历史感知草稿控制（EDC）**：
    *   通过硬件模块实时计算草稿 Token 的 Softmax 熵值。
    *   结合历史接受率模式（Pattern History Table），动态判断当前草稿的置信度。若置信度过低，则停止超前生成，避免无效计算。
*   **时间感知预验证控制（TVC）**：
    *   建立 NPU 和 PIM 的双向延迟模型，实时预测两端的执行周期。
    *   在 NPU 验证空档期，利用 PIM 插入小批量“预验证”任务，确保 NPU 始终有高概率被接受的草稿可供验证，减少 NPU 空闲。
*   **硬件增强设计**：
    *   **注意力算法单元（AAU）**：在 LPDDR5-PIM 内部集成非线性算子和归约运算单元，减少中间数据跨芯片传输。
    *   **门控任务调度单元**：支持亚微秒级的任务切换，实现 DLM 和 TLM 任务在 PIM 端的快速转换。

#### 3. 实验设计
*   **数据集与场景**：使用 **Alpaca** 指令遵循数据集，模拟移动端常见的单批次（Batch Size = 1）推理场景。
*   **模型配置**：涵盖了从小到大三种规模：
    *   Small: OPT-1.3B (DLM) + OPT-6.7B (TLM)
    *   Medium: LLaMA2-7B (DLM) + LLaMA2-13B (TLM)
    *   Large: PaLM-Like-8B (DLM) + PaLM-Like-30B (TLM)
*   **对比算法**：集成了四种主流自适应草稿算法（SpecDec++、SVIP、AdaEDL、BanditSpec）。
*   **Benchmark（基准）**：
    1.  **GPU-Only**：在移动端 GPU 上交替执行 DLM 和 TLM。
    2.  **SpecPIM**：目前最先进的（SOTA）基于 GPU+PIM 的算子级并行加速方案。

#### 4. 资源与算力
*   **模拟平台**：实验基于周期精确的模拟器（ONNXim 和 SAITPublic-PIMSimulator）进行。
*   **硬件参数**：
    *   **NPU**：16 TOPS (INT8) 矩阵算力，8.2 TOPS 向量算力，8MB 片上存储。
    *   **PIM**：基于 LPDDR5，16 个 PIM 单元，总算力 102.4 GOPS，带宽 51.2 GB/s。
*   **基准 GPU**：使用 NVIDIA GeForce RTX 4090 (Laptop) 作为 GPU-Only 基准的参考硬件。
*   **训练/推理时长**：文中未提及模型训练时长（属于推理加速研究），推理长度设定为 1024 Token。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对 3 种模型组合、4 种自适应算法进行了交叉验证，并进行了详细的消融实验（逐步加入异步架构、AAU、EDC、TVC）。
*   **充分性评价**：实验设计较为充分，涵盖了吞吐量、能效比、接受率、硬件面积开销及功耗分析等多个维度。通过与 SOTA 方案（SpecPIM）的对比，证明了任务级异步设计的优越性。实验过程考虑了硬件实现的物理约束（如 28nm 工艺下的面积估算），具有较高的客观性。

#### 6. 主要结论与发现
*   **性能提升**：AHASD 相比纯 GPU 方案，吞吐量提升高达 **4.2倍**，能效提升 **5.6倍**。
*   **对比 SOTA**：相比 SpecPIM，吞吐量提升 **1.5倍**，能效提升 **1.24倍**。
*   **硬件友好**：额外的硬件逻辑（EDC, TVC, AAU 等）仅占 LPDDR5-PIM 芯片面积的 **2.68%**，非常适合集成到移动端 SoC 中。
*   **关键发现**：任务级异步虽然会略微降低草稿接受率，但通过 EDC 和 TVC 的动态调控，可以显著弥补这一损失，并大幅提升系统整体的有效吞吐量。

#### 7. 优点与亮点
*   **软硬协同**：不仅是架构设计，还深入到了 PIM 内部的逻辑电路设计（如 AAU），解决了数据搬运的瓶颈。
*   **动态自适应**：引入了基于熵和历史记录的在线学习机制（EDC），使硬件能够“感知”模型推理的状态。
*   **解决负载失衡**：通过异步队列解耦，有效解决了自适应投机解码中常见的 NPU/PIM 负载波动问题。

#### 8. 不足与局限
*   **模拟器依赖**：所有结果均来自模拟器，尚未在真实的 NPU-PIM 物理芯片上进行流片验证，实际环境中的热管理、总线竞争等因素可能影响表现。
*   **应用限制**：该方案高度依赖于 PIM（近存计算）硬件的支持，对于不具备 PIM 功能的常规移动设备无法直接应用。
*   **量化偏差**：实验主要基于 INT8 量化，未讨论更低比特（如 INT4）或混合精度对熵感知准确性的影响。

（完）
