---
title: Sparsity-Aware Event-Driven Impulse Radio Transceivers for Reliable Neuromorphic Inference
title_zh: 用于可靠类脑推理的稀疏感知事件驱动脉冲无线电收发信机
authors: "Zhengzhong Guan, Jiaying Li, Kanghua Li, Bojun Cheng, Hong Xing"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23559v1"
tags: ["query:aiot"]
score: 9.0
evidence: 基于物联网的AI应用和边缘智能的类脑推理
tldr: 针对资源受限的边缘AI应用，本文提出了一种结合事件感知与超宽带（UWB）通信的脉冲无线电收发器方案。通过利用脉冲稀疏性的双时间尺度重复编码，以及数字和模拟两种脉冲编码推理方案，实现了低延迟、高可靠的神经形态推理。研究揭示了不同信噪比下两种方案的性能权衡，为边缘智能提供了高效的通信与计算集成框架。
selection_source: fresh_fetch
motivation: 传统收发器在处理多用户神经形态推理时面临高能耗、高延迟以及衰落和干扰的挑战。
method: 提出一种利用脉冲稀疏性的双时间尺度重复编码，并设计了基于数字脉冲编码和模拟脉冲编码的两种推理方案。
result: 实验表明两种方案存在信噪比相关的性能交叉，模拟编码在高中信噪比下表现更优，而数字编码在低信噪比下更具鲁棒性。
conclusion: 该研究通过整合事件驱动感知与超宽带通信，为实现可靠且能效高的分布式边缘神经形态推理提供了有效途径。
---

## 摘要
部署在资源受限网络边缘的物联网（IoT）人工智能（AI）应用日益增多，这要求从分布式前端传感器到远程推理单元具备超可靠且低延迟的数据处理流水线。同时，以脉冲神经网络（SNN）为特征的类脑神经形态计算已成为高效能 AI 推理的新范式。然而，用于对抗衰落和多用户干扰的高复杂度收发信机所产生的巨大能量和时间开销，阻碍了边缘智能中多用户类脑推理的实现。为应对这一挑战，本文考虑了一种宽带多用户远程推理系统，该系统集成了基于事件的传感和基于跳时（TH）开关键控（OOK）的超宽带（UWB）通信，以实现可靠的类脑推理。具体而言，我们提出了一种新型的双时间尺度重复编码，利用帧内脉冲稀疏性来实现低延迟重复。我们还开发了两种类脑推理方案：(i) 数字脉冲编码，通过基于 SNN 的稀疏性估计器进行阈值自适应检测，从而恢复事件帧的每个像素；(ii) 模拟脉冲编码，将接收端的含噪声相关器输出转换为模拟值输入，用于端到端（E2E）分类。最后，数值结果验证了所提编码方案的有效性，并揭示了两种推理方案之间存在依赖于信噪比（SNR）的性能交叉，表明基于模拟脉冲编码的方案在中高信噪比下更具优势，而基于数字脉冲编码的方案在低信噪比区域保持稳健。

## Abstract
The growing number of Internet-of-Things (IoT) based artificial intelligence (AI) applications deployed at resource-constrained network edge call for ultra-reliable and low-latency data processing pipelines from distributed front-end sensors to remote inference units. Meanwhile, brain-inspired neuromorphic computing featuring spiking neural networks (SNNs) have arisen as a new paradigm for energy-efficient AI inference. However, significant energy and time expenses incurred in high-complexity transceivers that combat fading and multi-user interference hinder implementations of multi-user neuromorphic inference for edge intelligence. To address this challenge, we consider in this paper a broadband multi-user remote inference system that integrates event-based sensing and time-hopping (TH) on-off keying (OOK) based ultra-wideband (UWB) communications for reliable neuromorphic inference. Specifically, we propose a novel two-timescale repetition coding that leverages intra-frame pulse sparsity for low-latency repetition. We also develop two neuromorphic inference schemes based on: (i) digital spike encoding that recovers each pixel of the event-frame by threshold-adaptive detection via an SNN based sparsity estimator; and (ii) analog spike encoding that converts noisy correlator outputs at the receiver into analog-valued inputs for end-to-end (E2E) classification. Finally, numerical results validate the effectiveness of the proposed coding schemes, and reveal a signal-to-noise ratio (SNR)-dependent performance crossover between the two inference schemes, indicating that analog spike encoding based schemes are preferable with mild or high SNR while digital spike encoding based schemes remain robust in low SNR regime.

---

## 论文详细总结（自动生成）

这是一份关于论文《Sparsity-Aware Event-Driven Impulse Radio Transceivers for Reliable Neuromorphic Inference》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着物联网（IoT）和边缘智能的发展，在资源受限的边缘设备上部署 AI 推理面临严峻挑战。虽然类脑神经形态计算（如脉冲神经网络 SNN）和事件驱动传感器（如 DVS 摄像头）提供了高能效的解决方案，但当传感器与推理单元非同地部署时，无线信道的多径衰落和多用户干扰会严重破坏脉冲流的传输，导致推理精度下降。
**核心问题：** 如何设计一种既能利用事件流稀疏性，又能对抗复杂无线环境干扰的高效、可靠的超宽带（UWB）收发信机架构，以支持远程类脑推理。

### 2. 方法论：核心思想与关键技术
论文提出了一种集成“感知-通信-推理”的跨层设计方案：
*   **TH-OOK 调制：** 采用跳时开关键控（Time-Hopping On-Off Keying）。由于事件数据极其稀疏（0 远多于 1），OOK 仅在有脉冲时发射能量，显著降低了功耗并减少了多用户碰撞概率。
*   **双时间尺度重复编码（Two-timescale Repetition Coding）：** 
    *   在传统的帧间重复（Inter-frame）基础上，引入帧内脉冲重复（Intra-frame）。
    *   利用 OOK 带来的空闲时隙，在不增加额外帧延迟的前提下增加重复次数 $N_p$，从而提高处理增益。
*   **两种神经形态推理方案：**
    *   **数字脉冲编码（Digital）：** 采用模型驱动方法。利用一个轻量级 SNN 估计当前帧的稀疏度 $p$，结合最大后验概率（MAP）准则计算自适应检测阈值 $\eta$，通过多数投票恢复二进制比特，再输入预训练的 SNN 分类器。
    *   **模拟脉冲编码（Analog）：** 采用端到端（E2E）数据驱动方法。直接将相关器的软输出（经 Sigmoid 归一化）作为 SNN 的模拟输入，让网络自行学习如何从噪声中提取特征。

### 3. 实验设计
*   **数据集：** DVS128 Gesture（11 类手势识别），包含 1176 个训练样本和 166 个测试样本。
*   **通信场景：** 模拟 IEEE 802.15.4a 标准下的 UWB 多径衰落信道（CM1 模型），考虑 16 个并发用户。
*   **Benchmark（基准）：**
    *   **PPM-D/A：** 基于脉冲位置调制（PPM）的数字/模拟方案。
    *   **Baseline：** 理想信道（无损传输）下的 SNN 推理精度。
*   **对比维度：** 不同信噪比（SNR）、不同的重复次数（$N_p$）、不同的调制方式（OOK vs PPM）。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确提及**具体的硬件资源（如 GPU 型号、数量）以及具体的训练时长。
*   **模型细节：** 提到了使用 PLIF 神经元和 DVSGestureNet 架构，并使用弧切线（Arctangent）替代梯度进行训练。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 500 次蒙特卡洛（Monte-Carlo）试验以获取平均测试精度，涵盖了从 -15dB 到 10dB 的宽范围 SNR。
*   **充分性：** 实验对比了 OOK 与传统 PPM 的性能差异，并详细探讨了数字与模拟两种路径的优劣。
*   **客观性：** 实验设置考虑了真实的多径信道模型和多用户干扰，而非简化的 AWGN 信道，具有较高的实际参考价值。

### 6. 主要结论与发现
*   **稀疏性优势：** TH-OOK 方案在所有测试条件下均显著优于 TH-PPM，证明了利用事件稀疏性减少碰撞的有效性。
*   **性能交叉点（Crossover）：** 
    *   在**低 SNR 区域**，数字编码方案更优，因为它通过 MAP 准则和硬决策有效地抑制了噪声。
    *   在**中高 SNR 区域**，模拟编码方案表现更好，因为它保留了更多软信息，允许 SNN 进行端到端的特征提取。
*   **编码增益：** 增加帧内重复次数 $N_p$ 能显著提升精度，使其在较高 SNR 下逼近理想信道的基准性能。

### 7. 优点
*   **跨层优化：** 将物理层的调制编码与应用层的 SNN 推理深度结合，而非孤立设计。
*   **自适应性：** 引入 SNN 稀疏性估计器，解决了事件流动态变化导致的检测阈值难确定问题。
*   **低延迟：** 双时间尺度编码在不增加帧数（即不增加时间延迟）的情况下提升了可靠性。

### 8. 不足与局限
*   **应用单一：** 实验仅基于手势识别数据集，对于更复杂的视觉任务（如目标检测或高速避障）的泛化能力有待验证。
*   **理想化假设：** 假设接收端已知完美的信道状态信息（CSI），在实际超宽带通信中，多径参数的精确估计本身具有很大挑战。
*   **算力开销：** 虽然推理是低功耗的，但数字方案中增加了一个额外的 SNN 稀疏性估计器，其带来的额外计算开销未做详细量化分析。

（完）
