---
title: Deployment-Aligned Low-Precision Neural Architecture Search for Spaceborne Edge AI
title_zh: 面向星载边缘人工智能的部署对齐低精度神经架构搜索
authors: "Parampuneet Kaur Thind, Vaibhav Katturu, Giacomo Zema, Roberto Del Prete"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24492v1"
tags: ["query:aiot"]
score: 10.0
evidence: 边缘AI的低精度神经架构搜索
tldr: 针对星载边缘AI设备中低精度部署导致的精度下降问题，本文提出了一种部署对齐的低精度硬件感知神经架构搜索（NAS）框架。该方法在NAS的微调和评估阶段直接引入FP16数值约束，使架构优化与实际部署环境保持一致。在Intel Myriad X VPU上的船舶分割任务实验表明，该方法在不增加模型复杂度的前提下，显著缩小了部署精度差距，提升了模型的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的硬件感知NAS通常在全精度假设下优化，导致模型在低精度边缘硬件部署时因数值不匹配而出现显著的性能退化。
method: 将部署时的FP16数值约束直接集成到NAS的微调和评估环节，在不改变搜索空间的情况下实现架构效率与数值鲁棒性的联合优化。
result: 在Intel Myriad X VPU的船舶分割实验中，该方法将部署精度从0.78 mIoU提升至0.826 mIoU，成功恢复了约三分之二的精度损失。
conclusion: 在硬件感知NAS中引入部署一致的数值约束，能有效提升资源受限边缘AI模型的部署性能和优化对齐度。
---

## 摘要
在边缘加速器上设计满足严格延迟和精度约束的深度网络，越来越依赖于硬件感知优化，包括由设备级指标引导的神经架构搜索 (NAS)。然而，大多数硬件感知 NAS 流水线仍是在全精度假设下优化架构，并仅在搜索后应用低精度适配，这导致了优化时的行为与低精度硬件上的部署时执行之间存在不匹配，从而可能显著降低精度。我们通过将部署对齐的低精度训练直接集成到硬件感知 NAS 中来解决这一局限性。候选架构在微调和评估过程中处于 FP16 数值约束下，从而在不修改搜索空间或进化策略的情况下，实现了架构效率和数值鲁棒性的联合优化。我们在面向星载海事监测的船舶分割任务上评估了所提框架，目标硬件为 Intel Movidius Myriad X 视觉处理单元 (VPU)。虽然训练后精度转换将设备端性能从 0.85 mIoU 降低到 0.78 mIoU，但部署对齐的低精度训练在相同架构（95,791 个参数）下实现了 0.826 mIoU 的设备端性能，在不增加模型复杂性的情况下恢复了约三分之二的部署诱导精度差距。这些结果表明，将部署一致的数值约束纳入硬件感知 NAS，显著提高了资源受限边缘人工智能 (AI) 在优化与部署之间的鲁棒性和对齐性。

## Abstract
Designing deep networks that meet strict latency and accuracy constraints on edge accelerators increasingly relies on hardware-aware optimization, including neural architecture search (NAS) guided by device-level metrics. Yet most hardware-aware NAS pipelines still optimize architectures under full-precision assumptions and apply low-precision adaptation only after the search, leading to a mismatch between optimization-time behavior and deployment-time execution on low-precision hardware that can substantially degrade accuracy. We address this limitation by integrating deployment-aligned low-precision training directly into hardware-aware NAS. Candidate architectures are exposed to FP16 numerical constraints during fine-tuning and evaluation, enabling joint optimization of architectural efficiency and numerical robustness without modifying the search space or evolutionary strategy. We evaluate the proposed framework on vessel segmentation for spaceborne maritime monitoring, targeting the Intel Movidius Myriad X Visual Processing Unit (VPU). While post-training precision conversion reduces on-device performance from 0.85 to 0.78 mIoU, deployment-aligned low-precision training achieves 0.826 mIoU on-device for the same architecture (95,791 parameters), recovering approximately two-thirds of deployment-induced accuracy gap without increasing model complexity. These results demonstrate that incorporating deployment-consistent numerical constraints into hardware-aware NAS substantially improves robustness and alignment between optimization and deployment for resource-constrained edge Artificial Intelligence (AI).

---

## 论文详细总结（自动生成）

这是一份关于论文《Deployment-Aligned Low-Precision Neural Architecture Search for Spaceborne Edge AI》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的硬件感知神经架构搜索（HW-NAS）通常在全精度（FP32）环境下进行搜索，仅在部署阶段才将模型转换为低精度（如 FP16 或 INT8）。这种“搜索与部署脱节”的模式会导致模型在边缘硬件（如卫星载荷）上运行时，因数值精度损失而出现显著的性能退化。
*   **研究背景**：星载边缘 AI（如海事监测、灾害响应）对延迟和功耗有极高要求，必须使用低功耗加速器。然而，这些加速器通常运行在低精度模式下，导致优化时的精度假设与实际部署时的执行行为不匹配。

### 2. 方法论
*   **核心思想**：提出“部署对齐的 NAS”，将低精度数值约束直接引入 NAS 的评估循环中。在搜索过程中，候选架构不仅要经过 FP32 训练，还要进行“部署对齐”的微调，使其在搜索阶段就“感知”到目标硬件的数值特性。
*   **关键技术细节**：
    *   **两阶段训练**：每个候选架构先进行 10 个 Epoch 的 FP32 训练，随后进行 10 个 Epoch 的 FP16 感知微调。
    *   **数值投影算子**：在微调的前向传播中注入投影算子 $P(\cdot)$，模拟 FP16 的尾数精度和舍入行为。
    *   **直通估计器（STE）**：在反向传播中使用 STE 解决舍入函数不可导的问题，确保梯度更新稳定。
    *   **硬件在环评估**：将微调后的模型导出为 OpenVINO FP16 格式，直接在 Intel Myriad X VPU 硬件上测量真实的吞吐量（FPS）和精度（mIoU）。
    *   **适应度函数**：结合了设备端 FPS 和任务精度（mIoU），并使用指数项增强对高精度方案的敏感度。

### 3. 实验设计
*   **数据集与任务**：使用 **HRSC2016** 数据集进行星载海事监测中的**船舶分割**任务（二分类分割）。
*   **目标硬件**：Intel Movidius Myriad X VPU（一种典型的低功耗空间边缘加速器）。
*   **对比基准（Benchmark）**：
    *   **PTQ Baseline**：在 FP32 下训练并搜索，部署时直接转换为 FP16（训练后量化风格）。
    *   **Deployment-Aligned（本文方法）**：在搜索过程中集成 FP16 感知微调。
*   **评估指标**：平均交并比（mIoU）、每秒帧数（FPS）、参数量。

### 4. 资源与算力
*   **训练算力**：使用配备 **AMD Radeon GPU** 的工作站进行模型训练和微调。
*   **部署算力**：使用 **Intel Movidius Myriad X VPU** 进行设备端推理评估。
*   **搜索规模**：进化算法运行 10 代，种群大小为 16，每代保留 1 个精英架构并注入 6 个随机架构。
*   **时长说明**：文中未明确给出总搜索时长，但提到每个候选架构共训练 20 个 Epoch（10 FP32 + 10 FP16）。

### 5. 实验数量与充分性
*   **实验规模**：总共评估了约 160 个候选架构（10 代 × 16 种群）。
*   **充分性评价**：
    *   **客观性**：实验采用了“硬件在环”测试，数据来源于真实物理设备而非模拟器，具有较高的可信度。
    *   **对比公平性**：对比组（PTQ）与实验组使用了完全相同的搜索空间、进化策略和初始化权重，变量控制严格。
    *   **局限性**：仅在一个数据集（HRSC2016）和一个硬件平台（Myriad X）上进行了验证，实验多样性略显不足。

### 6. 主要结论与发现
*   **精度恢复**：传统的 PTQ 转换使模型精度从 0.85 降至 0.78 mIoU；而本文方法在相同架构下实现了 **0.826 mIoU**，成功恢复了约 **2/3** 的部署精度损失。
*   **鲁棒性提升**：低精度感知训练改变了 NAS 的搜索动态，使算法倾向于选择那些不仅结构高效、而且对数值舍入更具鲁棒性的架构。
*   **定性改善**：可视化结果显示，该方法显著减少了低精度部署中常见的边缘破碎、内部空洞和漏检现象。

### 7. 优点
*   **部署对齐**：解决了 NAS 领域长期存在的“训练-部署”数值不一致问题，对星载等高可靠性场景极具价值。
*   **无需修改搜索空间**：该方法作为一种评估策略，可以无缝集成到现有的各种进化 NAS 框架中。
*   **真实硬件反馈**：通过真实的 VPU 测量延迟和精度，避免了基于代理模型（Proxy）评估的不准确性。

### 8. 不足与局限
*   **计算开销增加**：由于每个候选架构都需要额外的低精度微调，搜索阶段的计算成本比传统 FP32 NAS 更高。
*   **精度覆盖有限**：目前仅针对 FP16 进行了优化，尚未探讨更极端的 INT8 或混合精度量化。
*   **硬件单一性**：虽然 Myriad X 具有代表性，但不同边缘加速器（如 TPU, FPGA）的数值行为可能不同，方法的通用性有待在更多硬件上验证。

（完）
