Title: Introducing the Self-Stabilizing SLEEPING Model

URL Source: https://arxiv.org/pdf/2608.23044v1

Published Time: Tue, 25 Aug 2026 01:53:03 GMT

Number of Pages: 25

Markdown Content:
# Introducing the Self-Stabilizing SLEEPING Model 

# Tistou Fages #

LIP, ENS de Lyon, France 

# Colette Johnen # 

LaBRI, CNRS, Bordeaux, France 

# Mikaël Rabie # 

Université Paris Cité, IRIF, CNRS, Paris, France 

Abstract 

The SLEEPING LOCAL model introduces a new complexity parameter, the awake complexity, to make distributed algorithms energy-efficient. In the synchronous LOCAL model, nodes can now decide to be awake or asleep in each round. In a round, only awake nodes can communicate to share information, which consumes energy. The awake complexity is the maximum number of times a node is activated to produce an output. In particular, it often comes at the cost of the total number of rounds required to solve a problem, compared with algorithms in which every node is awake in every round. In this article, we adapt the notion of awaken rounds to the context of self-stabilization, introducing the Self-Stabilizing SLEEPING model. Nodes are no longer required to remain awake at all times. However, in self-stabilization, nodes must be activated infinitely often to detect any issue in the system’s current state. In this model, the complexities are: How many synchronous rounds are needed to reach a legitimate configuration? How many times does a node need to be awake to reach this configuration? How often does a node need to be awake once this configuration is reached? The goal is to minimize those three metrics, and we can expect different trade-offs. We present energy-efficient algorithms to solve the problems of finding a (∆ + 1)-coloring, a Maximal Independent Set, and a Maximal Matching, thanks to new ad hoc sleeping techniques that reduce the awake complexity (i.e., energy consumption) during the convergence phase. We also propose two transformers that adapt silent self-stabilizing algorithms to the SLEEPING setup. The first transformer is pretty simple and deals with low-complexity algorithms. The second is more elaborate and is more energy-efficient when it transforms slow self-stabilizing algorithms. 

2012 ACM Subject Classification Theory of computation → Distributed algorithms; Mathematics of computing → Discrete mathematics 

Keywords and phrases Distributed Computing Energy Complexity Sleeping Model, fault-tolerance, transient faults self-stabilization 

Funding This research was funded, in whole or in part, by the Agence Nationale de la Recherche (ANR), grant ANR-24-CE48-7768-01 (project ENEDISC) and grant ANR-22-CE48-0001 (projet TEMPOGRAL) 

Acknowledgements The authors are thankful to Lélia Blin and Sylvain Gay for the preliminary discussions about the model, as well as Johanne Cohen and Laurence Pilard for the discussions on the algorithms. arXiv:2608.23044v1 [cs.DC] 24 Aug 2026 Fages, Johnen and Rabie 1

# 1 Introduction 1.1 Motivation 

In a long-lived system that runs forever, one should expect some failures to occur (e.g., memory corruption or network modification). To address these failures, one solution is to run self-stabilizing algorithms that can recover from any problematic configuration. However, detecting one of those failures entails a high energy cost: all nodes must run their detection systems continuously. In the synchronous LOCAL model, the SLEEPING variant was recently introduced. In this variant, nodes decide when they are actually awake. In each round, only awake nodes are allowed to communicate with their (awake) neighbors. It allows for saving energy and permits nodes to operate only when needed. In particular, we obtain algorithms in which nodes have fewer awake rounds than the total number of rounds in the classical LOCAL model. However, it usually comes at a time cost: the total number of rounds needed before the system reaches its final state is usually considerably larger. What if we introduce the notion of SLEEPING in Self-Stabilizing algorithms? In particular, it would permit nodes to be non-continuously awake for detecting system failures, thereby saving a considerable amount of energy. On the other hand, we would want the system to be fast enough to recover from a failure once it is detected. A considerable challenge is to determine what constitutes a good definition of a SLEEPING version of self-stabilizing algorithms. The first reflex is to add, as in the LOCAL version, an internal clock to determine when the node should be activated next. Do we want the internal clock to be public or private? Making it public allows two neighbors to synchronise their clocks. However, it increases the number of public states and requires each node to communicate its updated clock in each round. In this paper, we consider the case where the clock is internal: a node can report how many rounds it will be sleeping, but its neighbors cannot deduce the exact time at which they will be awake again. Some algorithms operate in an asynchronous scheduler in which a daemon determines which nodes are activated in each round. This becomes challenging in the case of sleeping nodes: should their clock be updated when they are scheduled, or in each round? In the SLEEPING LOCAL model, the advantage of being sleeping is to wait for some event to happen in the meantime. However, in an asynchronous setting, we lose this guarantee. For that reason, we consider a synchronous setting in this article and leave open the question of how to adapt the model to an asynchronous setting. In the SLEEPING LOCAL model, information can only go through an edge if both endpoints are awake. Several techniques are developed in this model to coordinate activation among neighbors to be energy-efficient while passing information at the appropriate time. In particular, having access to a common clock and starting computation synchronously in round one facilitates coordination. In an asynchronous setting, if both endpoints of an edge have to be awake at the same time to communicate, it would force nodes to be awake at the same time infinitely often to detect an issue. It would impose an additional layer of constraints on the internal clocks. In this article, we consider the state model, in which nodes have access to their neighbors’ latest state when they are awake. This allows balancing, given that the public state does not allow nodes to know when their neighbor will next activate (unless the neighbor is awake in every round). For a classical self-stabilizing algorithm, the usual complexity metric is the number of rounds needed for the system to reach, from any initial configuration, a good configuration. However, unlike the LOCAL model, nodes cannot reach a state in which they are done and 2 Introducing the Self-Stabilizing SLEEPING Model 

stop updating themselves during self-stabilization. One important metric is how often a node is awake over time, particularly when a legitimate configuration is reached. Note that this frequency directly affects the total number of rounds required for the system to converge to a legitimate configuration. A node can start in this low-frequency state, even though some update must be performed. As a consequence, the later the nodes wake up for the first time, the longer the system takes to detect and broadcast that a configuration update needs to be computed. On the other hand, we want the system to update itself fast enough. As in the SLEEPING LOCAL model, this will introduce interesting trade-offs in complexity. 

# 1.2 Related Work 

The LOCAL model, introduced in 1987 [25, 28], aims to capture computation on graphs, in which nodes (or processors) coordinate synchronously to solve a problem locally. In particular, LCL problems [27] - problems where a solution can be checked locally, such as coloring, maximal independent set, and maximal matching - are intensively studied. Distributed graph coloring is one of the most fundamental and extensively studied problems in the LOCAL model [4, 14]. In [4] is presented a locally-iterative (∆ + 1)-coloring algorithm converging in O(∆ + log ∗ n) synchronous rounds. A general challenge is to find the best complexity to solve a given problem with regard to ∆ (the maximal degree of the graph) and n (the size of the network). See [30] for a survey on algorithms in the LOCAL model. In 2020, the SLEEPING LOCAL model was introduced in [6] to account for energy consumption. Nodes decide when to be active (or awake) and when to sleep, with the aim of minimizing the energy consumption (i.e., the number of awake rounds for each node). Thanks to the results of [1, 5], it is known that any global problem has awake complexity Θ(log n), compared to Θ( n) in the LOCAL model. In [3], an algorithm is presented to solve (∆ + 1)-coloring and Maximal Independent set, deterministically, with an awake complexity of O(log ∗ n√log n). Randomized algorithms with constant average complexity to compute a Maximal Matching or a Maximal Independent Set are provided in [13, 15, 16]. One of the current challenges in this model is studying the awake complexity of the deterministic maximal matching algorithm. Does the Ω(∆) lower bound in the LOCAL model [2] hold for the awake complexity? or an algorithm in o(∆) awake rounds can be designed? This is one of the current main open questions in this model. Energy consumption through choosing when to be awake has been considered in wireless sensor networks. In [33, 32, 22], nodes are periodically active. During a round, a node can only communicate (and know about) neighbor nodes that are also active in that round. In this context, the main challenge is to ensure that each node identifies all of its neighbors and that they coordinate their active and inactive cycles to enable periodic communication in their local neighborhood. See [29] for a survey. There are bridges between the LOCAL model, LCL problems, and self-stabilization. Various self-stabilizing algorithms to solve LCL problems have been proposed (see [19] for a survey). Some are designed for specific graph topologies, such as planar graphs [17] and bipartite graphs [23]. Greedy problems have also been considered [8, 21, 18]. More generally, any (silent) algorithm in the LOCAL model converging into T synchronous rounds can be transformed into a self-stabilizing algorithm that converges into 3 T + 2 rounds [24, 10] at the expense of the memory space. Fages, Johnen and Rabie 3

# 1.3 Contribution 

We propose three self-stabilizing SLEEPING algorithms that solve well-known distributed problems: (∆ + 1)-coloring (Section 3), maximal independent set (Section 4), and maximal matching (Section 5). We work in networks where nodes have unique identifiers and share knowledge of an upper bound M on the network size. Figure 1 summarizes these algorithms by presenting the round complexity, the awake complexity before convergence, and the awake frequency after convergence. Nodes share an upper bound M on the larger identifier in the network, and ∆ is the maximum degree of the graph. The round complexity is, of course, important for the three algorithms, but their awake complexities, which depend only on ∆, are close to the step complexity of the best self-stabilizing algorithms without any sleeping nodes [31, 26, 19]. For (∆+1)-coloring and the maximal independent set problem, a node’s wake-up frequency depends on whether it has a conflict. On the other hand, for maximal matching, the frequency remains the same, as a node might need to change its state depending on information at distance 2 (for example, if itself and another node are matched to the same node). The awake frequency before convergence, in the three algorithms, is a unique prime number for each node, depending on their identifier, ∆, and even M for the maximal matching algorithm. Those prime numbers ensure that a mutual exclusion will happen every (∆ + 1) awake round of a node, allowing them to do a step without any interference from one of their neighbor. The round complexities, which depend on M , are based on the Prime Number Theo-rem [20], which allows us to state that the k-th prime number is O(k log k).                                       

> SS SLEEPING awake awake Algorithm round complexity frequency complexity before after convergence convergence ∆ + 1 coloring - Algorithm 1 O(∆ Mlog M)∆ + O(1) O(1 /(∆ Mlog M)) Maximal independent set - Algorithm 2 O(∆ Mlog M)2∆ + O(1) O(1 /(∆ Mlog M)) Maximal matching - Algorithm 3 O(∆ 2Mlog M)O(∆ 2)O(1 /(Mlog M))
> Figure 1 Our Self-Stabilizing SLEEPING Algorithms.

We also propose two transformers (Section 6) that build, from a silent self-stabilizing algorithm, called A, a silent self-stabilizing SLEEPING algorithm solving the same problem as A. In both algorithms, the round complexity depends on T , the convergence time of 

A, and some arbitrary constant SP . The first transformer (that is very simple) requires that A converges under the weakly fair distributed scheduler. The second only requires that A converges under the synchronous scheduler. Both transformers have the same awake frequency after convergence; however, the convergence time differs. In particular, using the second transformer becomes more interesting if A has a complexity super-linear in D, the diameter of the graph. Note that, in [11], there is a transformer that turns any silent synchronous algorithm into an asynchronous self-stabilizing one with the same complexity. By applying either of the two transformers in Figure 2, we can turn any silent synchronous algorithm into a self-stabilizing SLEEPING algorithm. 4 Introducing the Self-Stabilizing SLEEPING Model                     

> SS SLEEPING round complexity awake complexity awake frequency Scheduler transformer before convergence after convergence requirement Algorithm 4 T.SP T1/SP weakly fair distributed Algorithm 5 3T+ (3 D+ 2) .SP 3T+ (3 D+ 2) .SP 1/SP synchronous
> Figure 2 Our Self-Stabilizing SLEEPING tranformers.

# 2 Model 2.1 Self-Stabilizing Algorithms in Static Graphs 

Communication and computation models 

We consider a distributed system modeled by a non-oriented connected graph G. Two nodes are neighbors in G(V, E ) if and only if they can communicate with each other. The number of nodes is denoted by n, D denotes the diameter of G, and ∆ is the maximal degree of G.The set of neighbors of a node u ∈ V is denoted by N (u); the closed neighborhood of u (i.e., 

N (u) ∪ { u} is denoted by N [u]. In that paper, we study identified networks, meaning that every node u has a unique identifier, denoted by id u.Every node has a finite set of variables . Some variables, such as identifier, are constant 

inputs provided by the system. Each node can read its own variables and variables owned by the neighboring nodes, but can write only to its own (non-constant) variables. The state of a node u designates the current value of all its local variables. An action by a node u is the updating of some non-constant variables of u according to its algorithm, its state, and the state of its neighbors. 

Configuration, Execution, Asynchrony. 

A configuration of an algorithm A for V is a vector ( s1, . . . , s n), where s1 to sn represent the states of all nodes of G. A node is said to be enabled in a configuration γ if and only if it can perform an action in γ. If no node is enabled in a configuration γ, then γ is a terminal 

configuration. We model the asynchrony of the system by a schedule: a function that takes as an input the algorithm and a sequence of configurations γ0, . . . , γ t, and returns the empty set if γt

is terminal, and a non-empty subset of the enabled nodes in γt otherwise. A scheduler is a class of schedules that share common properties, such as the level of fairness they guarantee regarding when nodes are scheduled. A taxonomy of schedulers has been proposed in [12]. In this paper, we only consider two schedulers. The synchronous scheduler ensures that every enabled node updates its variables at each computation step. The weakly fair distributed scheduler ensures that a node cannot be enabled in every configuration of an execution without eventually being scheduled; there exists a computation step in which the node is either scheduled or neutralized. Given a schedule D and an algorithm A, an execution of A under D is a finite or infinite sequence of configuration γ0, γ 1, . . . such that ∀i > 0, γ i is the configuration obtained from 

γi−1 after the execution of one simultaneous action by all nodes of D(A, γ 0, γ 1, . . . , γ i−1), the scheduled nodes. An execution is maximal if it is infinite or the last configuration is terminal. A computing step of an execution γ0, γ 1, . . . is any tuple γiγi+1 for i ≥ 0. If a node u has a variable var, we note var ru its value in configuration γr , or just var u if it is unambiguous. Fages, Johnen and Rabie 5Self-stabilization. 

Let A be a distributed algorithm, S be a specification (i.e., a predicate over graphs and configuration sequences), and D be a scheduler. 

▶ Definition 1 (Self-stabilization) . Algorithm A is self-stabilizing for S under D if for any graph G, there exists a subset of configurations L(G), called legitimate configurations, such that 

1. for every configuration γ of A in G, any maximal execution of A in G under D starting from γ contains a legitimate configuration γ′ ∈ L (G) (Convergence), and 

2. for every legitimate configuration γ ∈ L (G), any execution ϵ of A in G under D starting from γ satisfies S(G, ϵ ) (Correctness). 

If L(G) is the set of terminal configurations then Algorithm A is a silent algorithm. 

Convergence time. 

A node u is neutralized in the computing step γ → γ′ if u is enabled in γ and not enabled in 

γ′, but does not execute any action between these two configurations. Neutralization occurs when some neighbors of u change their state between γ and γ′, and this change brings u into a situation where A applied to u does not change its state. To evaluate the stabilization time, the classical time unit used in the literature is the Asynchronous round [9]. The first asynchronous round of an execution ϵ, noted ϵ′, is the minimal prefix of ϵ in which every node that is enabled in the initial configuration either executes an action or becomes neutralized. The second asynchronous round of ϵ is the first round of the suffix of ϵ starting from the last configuration of ϵ′, and so on. In the case of the synchronous scheduler, each computation step corresponds to a new round. 

# 2.2 SLEEPING Self-Stabilizing Algorithm 

In the SLEEPING Self-Stabilizing model, nodes still have access to their neighbors’ current states, even when sleeping. It differs from the SLEEPING LOCAL model, in which both nodes must be awake to share their current state. In that model, knowledge of a shared round 0, in which all nodes can be awake and aware of their neighbors, allows for the development of elaborate algorithms that cannot be easily adapted to self-stabilization. Self-stabilizing algorithms, in which both nodes must be awake simultaneously to share their state (or even be aware of each other), have been extensively studied in the wireless sensor network model. To be able to have a notion of awake rounds in our model, we add the following elements: 

Internal clock. 

Each node has a local variable cl , called the internal clock, that cannot be read by its neighbors. It is a natural number representing the number of rounds the node waits before activating itself. In each round, the node will first decrement cl by 1. Then, if cl > 0, the node does not update any of its variables and is not enabled. Otherwise, it is awake. So it executes an action if enabled, based on the current state of its neighborhood. It then updates 

cl to the number of rounds the node must sleep before its next activation ( cl is set to 1 if the node gets to be awake in the next round). Note that to describe a configuration, we need to know the value of each internal clock. An algorithm is considered silent if, after a legitimate configuration has been reached, every node variable except the clock remains unchanged. 6 Introducing the Self-Stabilizing SLEEPING Model Synchrony. 

In this model, we consider synchronous steps. In each step, each node updates itself, first by decreasing its clock and then by applying an action if that clock hits 0. In particular, it means that each step is an actual (asynchronous) round in the Self-Stabilizing model. In the rest of the paper, we will directly talk about rounds for each synchronous step the system goes through. Configuration γr corresponds to the states of the system after round r. We say that a node is awake or activated in round r if, in γr−1, its clock has value 1, and will execute the algorithm. 

Complexities. 

In this model, for a given algorithm A, we are considering three complexities: Round complexity: the maximal number of (synchronous) rounds needed, from any configuration, to reach a legitimate configuration. Awake complexity before convergence: From a given execution ϵ = γ0, γ 1 · · · , given a node v, aϵv corresponds to how many times v is awake before a legitimate configuration 

γl is reached for the first time during ϵ. The awake complexity before convergence of A is the maximal value among all executions and all nodes of aϵv .Frequency after convergence: let ϵ be an execution starting from a legitimate configuration. During ϵ, the frequency of activation of a node v is the average duration between two consecutive rounds where v is awake. The frequency after convergence of A is the maximal value among all executions starting from a legitimate configuration and all nodes of their frequencies of activation. 

# 3 (∆ + 1) -Coloring 

In this section, we introduce a SLEEPING Self-Stabilizing algorithm that computes a (∆ + 1)-coloring in O(∆) awake rounds. 

# 3.1 Algorithm 

The idea of Algorithm 1 is, for each node, to find a round in which it will be the only one to change its color. To that end, every node u picks a unique prime number p(id u) large enough, based on its identifier id u and ∆. As long as a node u is in conflict with one of its neighbors, it will wake up every p(id u) rounds to select the smallest available color. As each node is using a different prime number, we prove that, during ∆ + 1 consecutive attempts of 

u to set up a proper color, there are not 2 rounds in which a neighbor v of u changes also its color. Hence, after at most ∆ + 1 attempts, u will change its color while all of its neighbors are sleeping. From then on, u keeps its color forever. From that point on, u will wake up every SP rounds, for some sufficiently large shared constant SP .

# 3.2 Analysis 

To prove that Algorithm 1 is correct and its complexity, we use the following key lemma that ensures that if a node has a conflict, it will be solved: 

▶ Lemma 2. Let γ0 be a configuration in which some node u is such that predicate conflict(u) 

is satisfied. In the rounds numbered from 1 to r = SP + ∆ p(id u), node u is awake at least once with predicate conflict(u) unverified, or being the only node of its neighborhood to be awake. Fages, Johnen and Rabie 7 

> Algorithm 1 Energy-efficient self-stabilizing (∆ + 1)-coloring algorithm

Input: 

id u: identifier of uM : a strict upper bound on the largest identifier ∆: the maximum degree of the graph 

SP : an integer being the awake frequency, strictly greater than ∆ p( M )

Macro: 

p( x): the x-th prime number strictly greater than ∆ + 1 

Shared variables: 

color u: a color in [0 , ∆] 

Predicates: 

conflict( u) ≡ ∃ v ∈ N (u), color u = color v

Upon waking:  

> 1

if conflict( u) then  

> 2

color u := min[0 , ∆] \ { color v |v ∈ N (u)} 

> 3

Sleep for p(id u) rounds.  

> 4

else  

> 5

Sleep for SP rounds. 

Proof. Suppose that every time u is awake between rounds 1 and r, conflict( u) is True, and at least one of its neighbors is awake. First, observe that u will be awake at least once before or at round SP , as it sleeps each time for at most SP rounds. Since conflict( u) is at True each time it is awake, it will each time go to sleep for p(id u) rounds. Hence, it will be awake at least ∆ + 1 times. Since u has at most ∆ neighbors, one of them, say v, is awake at least twice at the same round as u among the ∆ + 1 rounds in which u is awake. Let’s call r1 the first round in which both u and v are awake at the same time, and r2 the next. Note that r1 ≤ r2 ≤ r1 + ∆ p(id u). Since ∆ p(id u) < T , node v also does not go sleeping for T rounds between r1 and r2, so it is awake every p(id v ) rounds. Hence, r1 = r2 + LCM(p(id u), p(id v )), where LCM( x, y ) is the Lowest Common Multiple of x and y.However, p(id u) and p(id u) are two different prime numbers, so LCM(p(id u), p(id v )) = p(id u) p(id v ). Since p(id v ) > ∆ + 1, this leads to a contradiction with r2 ≤ r1 + ∆ p(id u). ◀▶ Lemma 3. Let γ0 be a configuration and u such that conflict( u) = False . We get that 

conflict( u) = False on any configuration γr with r ∈ N.

Proof. Assume by contradiction that there exists some r such that conflict( u) = True in γr .Let us choose the smallest r. It means that in round r, a node v ∈ N [u] has changed its color, and we have color ru = color rv .We cannot have v = u, as u was not in conflict in γr−1, by the choice of r. In round r, v

has applied line 2 of the algorithm. It means that v took a color different from color r−1 

> u

. As 

u did not change its color, we get that color ru = color r−1 

> u

, which leads to a contradiction. ◀

The following lemma allows us to get the complexity of p( M ): 

▶ Lemma 4. For any k ∈ N large enough, the kth prime number is O(k log k).8 Introducing the Self-Stabilizing SLEEPING Model 

Proof. Let π(x) be the number of prime numbers smaller than or equal to x. By the Prime Number Theorem [20], we have: 

π(x) = x

log x + o

( x

log x

)

In particular, π(2 k log k) = 2k log k  

> log k

+ o

( 2k log k 

> log k

)

= 2 k + o(k). It implies that for k large enough, the kth prime number is at most 2 k log k. ◀▶ Theorem 5. Algorithm 1 computes a (∆+1) -coloring in SP +∆ p( M ) = SP +O(∆ M log M )

rounds. Each node in those rounds is awake at most ∆ + 2 = ∆ + O(1) times. The awake frequency after convergence is 1/SP .

Proof. The computation of a (∆ + 1)-coloring comes from the fact that each node will reach a configuration in which it has no conflict (Lemma 2), from which it will never be in conflict again (Lemma 3). Lemma 2 tells us that a node will apply lines 3 at most ∆ + 1 times each and otherwise sleep each time for T rounds, which gives the awake complexity and awake frequency after convergence. By choosing SP = ∆ p( M ), we get a convergence time of O(∆ p( M )) and an awake frequency after convergence of 1 /(∆ p( M )). Let M > ∆ + 1 be an upper bound on the larger identifier of a node in the network. Lemma 4 allows us to conclude that p( M ) = O(M log M ), giving us the complexities of Figure 1. ◀

# 4 Maximal Independent Set 

In this section, we introduce a SLEEPING Self-Stabilizing algorithm that computes a Maximal Independent Set in O(∆) awake rounds. 

# 4.1 Algorithm 

To build a maximal independent set, nodes must be either in or out of it. In our algorithm, when both a node u and one of its neighbors are in the independent set, u can freely leave the set. However, in the case where neither u nor any of its neighbors is in the independent set, we must ensure that u joins the set only if none of its neighbors will do so at the same time, as otherwise we could create a loop where a node will alternately join and leave the set. To that end, the action to join the independent set takes two rounds. First, the node sets the variable justWokeUp to True. If during the next round, none of its neighbors has this variable set to True, it joins the independent set; otherwise, u will sleep for p(id u −1) before considering joining the independent set again. This technique ensures that nodes join the independent set via a mutual exclusion process: no pair of neighbors can join simultaneously. The idea of Algorithm 2 is similar to the (∆ + 1)-coloring algorithm: we use prime numbers to ensure that during a round, a node is the only one in its neighborhood to set the variable justWokeUp to True. 

# 4.2 Analysis 

If a node u wakes up with the variable justWokeUp at True, we say that u is actively awake .

Observation : Let γ be a configuration, and let u and v be two neighboring nodes. We cannot have both u and v setting their respective variable inMIS to True during the round Fages, Johnen and Rabie 9 

> Algorithm 2 Energy-efficient self-stabilizing maximal independent set algorithm

Input: 

id u: identifier of uM : a strict upper bound on the largest identifier ∆: the maximum degree of the graph 

SP : an integer being the awake frequency, strictly greater than ∆ p( M )

Macro: 

p( x): the x-th prime number strictly greater than ∆ + 1 

Shared variables: 

justWokeUp u: True if u just woke up on this round, else False inMIS u: True if u is in the maximal independent set, else False 

Predicates: 

mustLeave( u) ≡ inMIS u ∧∃ v ∈ N (u), inMIS v

canJoin( u) ≡ ¬ inMIS u ∧∀ v ∈ N (u), ¬ inMIS v

awakeAlone( u) ≡ justWokeUp u ∧∀ v ∈ N (u), ¬ justWokeUp v

Upon waking:  

> 1

if canJoin( u) then  

> 2

if justWokeUp u then  

> 3

justWokeUp u := False  

> 4

if awakeAlone( u) then  

> 5

inMIS u := True  

> 6

Sleep for p(id u) − 1 rounds.  

> 7

else  

> 8

justWokeUp u := True  

> 9

Sleep for 1 round.  

> 10

else  

> 11

if justWokeUp( u) then  

> 12

justWokeUp u := False  

> 13

if mustLeave( u) then  

> 14

inMIS u := False  

> 15

Sleep for SP rounds. 10 Introducing the Self-Stabilizing SLEEPING Model 

from γ. Indeed, to do so, we would have awakeAlone( u) = awakeAlone( v) = True, which is a contradiction of the definition of the predicate awakeAlone. 

▶ Lemma 6. Let γ0 be a configuration in which some node u verifies the predicate canJoin(u) .In the rounds numbered from 1 to r = SP + ∆ p(id u), node u is awake at least once with predicate canJoin(u) unverified, or being the only node of its neighborhood to be actively awake. 

Proof. Suppose that every time u is awake between rounds 1 and r, canJoin( u) is at True, and awakeAlone( u) is at False in the rounds in which u is actively awake (i.e., in each configuration where justWokeUp u = True, u has a neighbor v such that justWokeUp v = True). First, observe that u will be awake at least once before round SP . Since canJoin( u) is at True each time it is awake, it never goes sleeping for SP rounds and will be actively awake every p(id u) rounds (by construction of the algorithm). By the choice of r, this will happen at least ∆ + 1 times. Since u has at most ∆ neighbors, one of them, say v, is actively awake at least twice at the same round as u among the ∆ + 1 rounds in which u is actively awake. Let’s call r1 the first round in which both u and v are awake at the same time, and r2 the next. Note that 

r1 ≤ r2 ≤ r1 + ∆ p(id u). Since ∆ p(id u) < SP , node v also does not go sleeping for SP rounds between r1 and r2,so it is actively awake up every p(id v ) rounds. Hence, r1 = r2 + LCM(p(id u), p(id v )). However, p(id u) and p(id u) are two different prime numbers, so LCM(p(id u), p(id v )) = p(id u) p(id v ). Since p(id v ) > ∆ + 1, this leads to a contradiction with r2 ≤ r1 + ∆ p(id u). ◀▶ Lemma 7. Let u be a node. Let γ0, γ 1, · · · be an execution starting from a configuration where inMIS u = False . We have mustLeave( u) = False on any configuration γr with r ≥ 0.

Proof. Assume that in γr we have mustLeave( u) = True. Then, in γr, u has a neighbor v

such that inMIS v = True. By definition of mustLeave( u), the variable inMIS u = True in γr. Let’s call r1 the last round before r in which inMIS u = False. Note that r1 ≥ 0. On round r1, u sets the value of inMIS u to True, so u is the only node in its neighborhood to be actively awake, and every neighbor of u has its variable inMIS set to False. This is in particular the case for v. Hence, there is a round r2 between r1 and r where v changes the value inMIS v from False to True. In the configuration before that round, we have canJoin( v) = True. However, in that same configuration, we have inMIS u = True, so canJoin( v) = False. This leads to a contradiction. ◀▶ Lemma 8. From any configuration γ0, for any r ≥ SP , the set {u| inMIS u = True } in γr

is independent. 

Proof. Suppose there exist two adjacent nodes u and v such that inMIS u = inMIS v = True on a round r ≥ SP . Then mustLeave( u) = mustLeave( v) = True on round r.By Lemma 7, the value of inMIS is True for both nodes on every configuration before round r. However, both u and v were awake at least once before round r, as a node can sleep for at most SP rounds. Out of generality, assume that u was awake first in some round 

r1 < r . In that round, it must have applied the action from line 13 and set its variable inMIS u to False. This contradicts the fact that mustLeave( u) remains True up to round r.This implies that from round SP on, there is no pair of adjacent nodes with both of them having their variable inMIS at True, which concludes the proof. ◀Fages, Johnen and Rabie 11 

▶ Lemma 9. Let γ0 be a round in which {u| inMIS u = True } is an independent set, and let u be a node such that inMIS u = True in that configuration. For any r > 0, we have 

inMIS ru = True .

Proof. Suppose there exists r > 0 such that inMIS u = False. Without loss of generality, let’s say r is the smallest possible choice. Then inMIS u = True on any round before, and in particular, node u changes the value of inMIS u to False on round 

r. It implies that u has a neighbor v with inMIS v = True on this round. By Lemma 8, {u| inMIS u = True } is an independent set in configuration γr−1, but we have inMIS u = inMIS v = True. This is a contradiction. ◀▶ Lemma 10. Let γ0 be a round in which {u| inMIS u = True } is an independent set. For any r > SP + ∆ p( M ), the set {u| inMIS u = True } in γr is maximal independent. 

Proof. Let r > SP + ∆ p( M ). By Lemma 8, the set {u| inMIS u = True } is independent in 

γi with i ≤ r. Suppose this set is not maximal. It implies that there exists a node u such that canJoin( u) = True in this configuration. Then, each node v in the closed neighborhood of u verifies inMIS rv = False. Lemma 9 implies directly that inMIS v = False in any configuration from γ0 to γr. Hence, in γ0,canJoin(u) is verified. By Lemma 6, there exists some round r1 ≤ r in which either canJoin(u) is unverified (case 1) or u was the only actively awake node (case 2). In case 1, it implies that one node v ∈ N [u] (which can be u) has inMIS r1 

> v

= True. Lemma 9 ensures that inMIS v remains True forever. This implies that currently, in γr,canJoin( u) = False. In case 2, in round r1, u sets inMIS u to True, and by the same reasoning, this variable will never change its value and canJoin( u)r = False. ◀▶ Theorem 11. Algorithm 2 computes a Maximal Independent Set in 2SP + ∆ p( M ) rounds. Each node in those rounds is awake at most 2∆ + 4 = O(∆) times. The awake frequency after convergence is 1/SP .

Proof. The computation of a Maximal Independent Set comes from Lemmas 8 and 10. Lemma 6 tells us that a node will apply lines 6 and 8 at most ∆ + 1 times each and otherwise sleep each time for SP rounds, which gives the awake complexity and awake frequency after convergence. By choosing SP = ∆ p( M ), we get a convergence time of O(∆ p( M )) and an awake frequency after convergence of 1 /(∆ p( M )), matching the complexities of Figure 1. ◀

# 5 Maximal Matching 

This section presents an energy-efficient self-stabilizing algorithm for computing a maximal matching. 

# 5.1 Algorithm 

The idea of Algorithm 3 is inspired by the constructions from [7]. We use Algorithm 2 as a subroutine to compute a maximal independent set IS , which will be used to perform some mutual exclusion. Each node u in IS attempts to match with an unmatched node v via a seducing step. Either v accepts, and both nodes are matched and ignored by the independent set subroutine, or v accepts another seducing node, and u tries to match itself with another 12 Introducing the Self-Stabilizing SLEEPING Model 

Algorithm 3 Energy-efficient self-stabilizing maximal matching algorithm 

Input: 

id u: identifier of uM : a strict upper bound on the largest identifier ∆: the maximum degree of the graph 

Macro: 

B(M ): the smallest integer such that the interval [ B(M ), 2B(M )] contains M

prime number pM (x): the x-th prime number in [ B(M ), 2B(M )] 

Shared variables: 

justWokeUp u: True if u just woke up on this round, else False status u ∈ { inMIS , outMIS , matched }: the current status of the node partner u ∈ N (u): the partner of u, only relevant if u is matched seducing u ∈ N (u): the node seduced by u, only relevant if u is inside of the MIS 

Predicates: 

awakeAlone( u) ≡ justWokeUp u ∧∀ v ∈ N (u), ¬ justWokeUp v

canJoinMIS( u) ≡ status u = outMIS ∧∀ v ∈ N (v), status v̸ = inMIS mustLeaveMIS( u) ≡ status u = inMIS ∧∃ v ∈ N (v), status v = inMIS married( u) ≡ status u = status partner u = matched ∧ partner partner u = u

waiting( u) ≡ status u = matched ∧ status partner u = inMIS ∧ seducing partner u = u

badMatching( u) ≡ status u = matched ∧¬ married( u) ∧ ¬ waiting( u)gotMatch( u) ≡ status u = inMIS ∧ status seducing u = matched ∧ partner seducing u = u

gotSeduced( u) ≡ status u = outMIS ∧∃ v ∈ N (u), status v = inMIS ∧ seducing v = u

canSeduce( u) ≡ status u = inMIS ∧ status seducing u̸ = outMIS 

∧∃ v ∈ N (u), status v = outMIS 

Upon waking:  

> 1

if canJoinMIS( u) then  

> 2

if justWokeUp u then  

> 3

if awakeAlone( u) then status u := inMIS  

> 4

justWokeUp u := False; Sleep for p M (id u) − 1 rounds.  

> 5

else justWokeUp u := True; Sleep for 1 round.  

> 6

else  

> 7

if justWokeUp u then justWokeUp u := False  

> 8

if mustLeaveMIS( u) ∨ badMatching( u) then status u := outMIS  

> 9

else  

> 10

if gotMatch( u) then  

> 11

partner u := seducing u ; status u := matched  

> 12

if gotSeduced( u) then  

> 13

partner u := argmin v∈N (u){id v , status v = inMIS ∧ seducing v = u} 

> 14

status u := matched  

> 15

if canSeduce( u) then  

> 16

seducing u := argmin v∈N (u){id v , status v = outMIS } 

> 17

Sleep for p M (id u) rounds. Fages, Johnen and Rabie 13 

node. If all of its attempts are rejected, it implies that all of its neighbors are matched. A node in IS leaves the independent set once it is matched. After that, the subroutine from Algorithm 2 tries to add new nodes in IS . That way, an unmatched node will eventually join IS and try to seduce its unmatched neighbors. To ensure that the seducing process is not too long, we need to ensure that a node from 

IS does not wait too long for an answer. To that end, we add a restriction to the prime numbers we use. They must belong to a set [ B, 2B], for some B large enough (this set must contain at least M prime numbers; the proof of existence of such B can be found in Lemma 13). With this restriction, we know that for any pair of neighbors u and v, if u (resp. 

v) wakes up every p M (id u) (resp. p M (id v )) rounds, v will be awake at least once between 3 consecutive awake rounds of u.In this algorithm, each node u wakes up every p M (id u) rounds. In each of those rounds, we say that the node is commonly awake . A node can also be awake in one more round every pM (id u) rounds, for the justWokeUp part of the maximal independent set subroutine. This is necessary as computing nodes need feedback from their neighbor. This system ensures some pseudo-synchrony. Contrarily to Algorithms 1 and 2, the frequency of waking up of a node does not change after having finished its computation. This is due to the fact that in maximal matching, an edge can be in the independent set if both endpoints are happy about it. 

# 5.2 Analysis 5.2.1 Discussions around synchrony and B

The following lemma is key to our construction: it allows us to ensure some pseudo-synchrony in the nodes’ awakening schedules. 

▶ Lemma 12. If a node u is commonly awake during round r, every neighbor of u will be commonly awake at least once between rounds r + 1 and r + 2 p M (id u) − 1.

Proof. Any neighbor v of u is commonly awake with period p M (id v ). Since all primes assigned by function p M lie in [ B(M ), 2B(M )], we have p M (id u) ≥ B(M ) and p M (id v ) < 2B(M ), so pM (id v ) < 2B(M ) < 2 p M (id u). In any interval of 2 p M (id u) consecutive rounds, v is awake at least once. ◀

The following Lemma allows us to argue that B(M ) always exists, and gives us some approximation of its value: 

▶ Lemma 13. For any M , there exists some B such that the number of prime numbers in 

[B, 2B] is at least M . Moreover, we can find B such as B = M log M + o(M log M ).

Proof. Let π(x) be the number of prime numbers smaller than or equal to x. By the Prime Number Theorem [20], which states: 

π(x) = x

log x + o

( x

log x

)

Note that in [ B, 2B], we have π(2 B) − π(B) prime numbers. In particular, for B = M 2, we get: π(2 B) − π(B) = (2 M )2  

> log (2 M)2

+ o

( (2 M )2 

> log(2 M)2

)

− M 2   

> log M2

− o

( M 2  

> log M2

)

= O

( M 2 

> log M

)

.It implies that such a B indeed exists and has value at least M (as there are fewer than 

M prime numbers smaller than M ). Let B(M ) be the smallest value possible of B. We have: 

M = π(2 B(M )) − π(B(M )) = 2B(M )log(2 B(M )) − B(M )log( B(M )) + o

( B(M )log( B(M )) 

)14 Introducing the Self-Stabilizing SLEEPING Model 

M = B(M )log( B(M )) + o

( B(M )log( B(M )) 

)

This equality first implies that B(M ) = M log( B(M )) + o(B(M )). Secondly, by taking the logarithm of this equality, we obtain: log M = log( B(M )) + o(log( B(M ))) which implies that log( B(M )) = log M + o(log( B(M ))). This allows us to finally deduce: 

B(M ) = M log( B(M )) + o(B(M )) = M log M + o(M log M ). ◀

# 5.2.2 Safe configurations 

Note that in the algorithm, each node u wakes up every p M (id u) rounds. They can also wake up right after one of those rounds if they enter in line 5 of the algorithm. In the first situation (when justWokeUp u = False), we say that u is commonly awake , and in the second situation, u is justly awake .

▶ Lemma 14. Let γ0 be a configuration in which mustLeaveMIS( u) evaluates to False for some node u. In any configuration γr with r ∈ N, mustLeaveMIS( u) also evaluates to False .

Proof. Suppose, for contradiction, that there exists a configuration γr in which we have mustLeaveMIS( u) = True. Let choose r to be minimal, i.e., mustLeaveMIS( u) = False in 

γr−1.There exists some v ∈ N (u) such that status ru = status rv = inMIS. Since mustLeaveMIS( u) = False in γr−1, either u or v has changed its status to inMIS in round r. This means that 

u or v has applied line 3 of the algorithm. However, similarly to Algorithm 2, this can happen only if both u and v have status outMIS in γr−1, and it would imply that both had awakeAlone verified, which is impossible. This is a contradiction. ◀▶ Lemma 15. Let γ0 be a configuration in which married( u) evaluates to True for some node u. In any configuration γr with r ∈ N, married( u) also evaluates to True .

Proof. Suppose married( u) evaluates to True in γ0, and suppose for contradiction that there exists a configuration γr in which married( u) = False. Let choose r to be minimal, meaning that married( u) = True in γr−1.In configuration γr−1, we have status r−1 

> u

= status r−1 

> v

= matched, partner r−1 

> u

= v and partner r−1 

> v

= u. Hence, predicates canJoinMIS, mustLeaveMIS, badMatching, gotMatch and gotSeduced all evaluate to False. A node can modify its status and its partner only through the rules associated with these predicates, which contradicts married( u) = False in configuration γr . ◀▶ Lemma 16. Let γ0 be a configuration in which badMatching( u) evaluates to False for some node u. In any configuration γr with r ∈ N, badMatching( u) also evaluates to False .

Proof. Suppose badMatching( u) evaluates to False in γ0, and suppose for contradiction that there exists a configuration γr in which badMatching( u) = True. Let choose r to be minimal, i.e., badMatching( u) switched to False in round r. In γr, we have status u = matched and both predicates married( u) and waiting( u) evaluate to False. Since badMatching( u) = False in γr−1, either status r−1

> u

̸ = matched, married r−1(u) = True or waiting r−1(u) = True. Let’s suppose status r−1

> u

̸ = matched. Then u transitions to matched in round r, which can only happen if either gotSeduced( u) or gotMatch( u) is verified. If gotSeduced( u) is verified, then in round r, u sets partner u to some neighbor v satisfying status v = inMIS and seducing v = u. Hence, v can only update the variable justWokeUp( v)during round r, contradicting waiting( u) = False in γr .Fages, Johnen and Rabie 15 

If gotMatch( u) is verified, then u sets partner u to v = seducing u and status u to matched in round r. By the definition of gotMatch, the predicate waiting( v) is verified in γr−1. Hence, in round r, v can only update the variable justWokeUp v , contradicting married( u) = False in γr .Hence status r−1 

> u

= matched. By Lemma 15, predicate married r−1(u) must be False, which implies that waiting r−1(u) = True. Let’s call v = partner r−1 

> u

.Since status r−1 

> u

= matched, predicates gotMatch( u), gotSeduced( u) and canSeduce( u)evaluate to False in γr−1, so partner ru = v and status ru = matched. So, as predicate waiting( u)to changes between γr−1 and γr, node v must change the values of some its variables in round r.By definition of predicate waiting, gotMatch( v) is verified in γr−1. Therefore, if v updates its variables in round r, it will set partner v to seducing v = u, and status v to matched, i. e., u and v will end up married in γr, which contradicts badMatching( u) = True in that configuration. ◀▶ Definition 17. A configuration is safe if both predicates mustLeaveMIS and badMatching 

evaluate to False on any node. 

▶ Corollary 18. From any configuration, after the pM (M ) rounds, a safe configuration is reached. The set of safe configurations is closed. 

Proof. Since id u < M , we have p M (id u) ≤ pM (M ), so u is awake at least once within the first p M (M ) rounds. If mustLeaveMIS( u) or badMatching( u) held at the beginning of this round, u set status u := outMIS, making both predicates immediately False (mustLeaveMIS requires status u = inMIS, and badMatching requires status u = matched). By Lemmas 14 and 16, both predicates remain False on every subsequent round. ◀

# 5.2.3 Convergence from a safe configuration 

In this section, we study executions starting from a safe configuration. Let’s define the predicate isolated: isolated( u) ≡ status u = inMIS ∧∀ v ∈ N (u), status v = matched ∧ partner v̸ = u

We will establish that any execution reaches a configuration where every node is either married or isolated. We will say (and prove) that such a configuration is stable .

▶ Lemma 19. If in a safe configuration γ, a node u is isolated, then it stays isolated along any execution from γ.

Proof. From Corollary 18, we know that mustLeaveMIS and badMatching are not verified by any node in any configuration reachable from γ. We conclude that u and its neighbors will never change their variable status nor partner. ◀▶ Lemma 20. Let γ0 be a safe configuration and a node u such that u is commonly awake in round 1, and we have justWokeUp 0 = False , status 0 

> u

= inMIS and isolated 0(u) = False .Let r = 2 p M (id u) + 1 . There exists some v ∈ N (u) such that status 0 

> v

= outMIS and 

status rv = matched (i.e., v changes its status to matched). Moreover, v keeps the status matched in any configuration after γr .16 Introducing the Self-Stabilizing SLEEPING Model 

Proof. Since γ0 is safe, mustLeaveMIS( u) = False, so no neighbor of u has status = inMIS. Since isolated( u) = False, not every neighbor is matched with a partner other than u, so there exists a neighbor w with status w = outMIS. Consequently canSeduce( u) holds in γ0. As u is awake in round 1, there exists some v ∈

N (u) such that seducing 1 

> u

= v. By Lemma 12, v is awake during a round r′ ∈ [2 , 2 p M (id u)+1]. At round r′, gotSeduced( v) holds (since status u = inMIS and seducing u = v). Moreover, since γ0 is safe, we have mustLeaveMIS( v) = badMatching( v) = False, so v executes lines 13 and 14 of the algorithm, setting partner v to one of its neighbors (possibly u) and status v to matched. As γ0 is safe, status v stays unchanged after the round r′. ◀▶ Corollary 21. Let γ0 be a safe configuration and a node u such that status u = inMIS .Then u is either married or isolated in any configuration r with r ≥ (2∆ + 2) p M (id u) + ∆ .

Proof. As γ0 is safe, a node married (resp. isolated) stays married (resp. isolated). If isolated( u) already holds in γ0, the claim is immediate. Otherwise, we apply Lemma 20 inductively. As long as u remains inMIS and not isolated, every 2 p M (id u) + 1 rounds (after the first time u was commonly awake), some neighbor of u

sets its status from outMIS to matched, and keeps this status forever. Since u has at most ∆ neighbors, after (2∆ + 1) p M (id u) + ∆ rounds, either all neighbors of u are matched with partner ̸ = u, i.e., isolated( u) = True, or some neighbor v has status v = matched and partner v = u, so gotMatch( u) is verified and u gets married with v

during the next awake round. ◀▶ Lemma 22. Let γ0 be a safe configuration and a node u commonly awake in round 1 such that status 0 

> u

= outMIS , and for each neighbor v of u, we have status 0

> v

̸ = inMIS .Let F be the set of neighbors of u with status = outMIS in γ0. In γr, with r =2 + ∆ p M (id u), either u or some node in F has a different status than in γ0.

Proof. This proof is similar to that of Lemma 6 for the MIS algorithm. Since status u =outMIS and no neighbor has status = inMIS, predicate canJoinMIS( u) holds in γ0. Suppose, for contradiction, that u and all nodes in F still have status outMIS in γr. Since γ0 is safe, no node can change its status to outMIS; this implies that the status of every node in F is unchanged. So canJoinMIS( u) holds from rounds 1 to r.Before round r, u will be justly awake (i.e., awake with justWokeUp u = True) ∆ + 1 times at rounds 2 + i pM (id u) with 0 ≤ i ≤ ∆. If awakeAlone( u) holds at any of these ∆ + 1 rounds, u joins the MIS, contradicting our assumption. Hence awakeAlone( u) = False every time u is justly awake, i. e., some neighbor of u is also justly awake. Since u has at most ∆ neighbors, by the pigeonhole principle, some neighbor v is justly awake simultaneously with 

u two times. Let’s call r1 the first round in which both u and v are justly awake at the same time, and r2 the next. Note that r1 ≤ r2 ≤ r1 + ∆ p M (id u). Note that, by construction of Algorithm 3, if a node w is justly awake in two different rounds s1 and s2, we have that s1 − s2 is a multiple of p M (id w). We get that r2 − r1 is both a multiple of p M (id u) and p M (id v ), which are different prime numbers. Hence, r2 − r1 = k pM (id u) p M (id v ) for some k ≥ 1. However, we have pM (id v ) > ∆, which brings a contradiction to r2 ≤ r1 + ∆ p M (id u). ◀▶ Lemma 23. Let γ0 be a safe configuration and a node u such that status u = outMIS in γ0.Let r = 2 + (5∆ + 4) p M (id u) + ∆ . In γr , either isolated( u) holds or the number of matched nodes in N [u] has strictly increased. Fages, Johnen and Rabie 17 

Proof. Assume that, in γr , u is not isolated and that the number of matched nodes in N [u]has not increased. Since γ0 is safe, no matched node in γ0 has changed its status in γr.Hence, no node in N [u] takes the status matched before γr .Suppose some neighbor v has status v = inMIS in γ0. By applying Corollary 21, v is either isolated or married in γr .If v is married: status v = matched, so the number of matched nodes has increased. Contradiction. If v is isolated: every neighbor of v (including u) has status = matched with a partner ̸ = v.In particular, u is matched, so the number of matched nodes has increased. Contradiction. Hence, no neighbor of u has status = inMIS in γ0.By Lemma 22, by round r1 = 2 + ∆ p M (id u), a node v ∈ N [u] changed its status from outMIS. If v has taken the status matched, then the number of matched nodes has increased. Hence, v has taken the status inMIS. By applying Corollary 21 to v, we reach the same contradictions as previously after (2∆+2) p M (id v )+∆ more rounds. As p M (id v ) < 2 p M (id u), in round r > 2 + ∆ p M (id u) + (2∆ + 2) p M (id v ) + ∆, the number of matched nodes must have increased. ◀▶ Corollary 24. After at most r = p M (M )[∆(5∆ + 4) + 1] + ∆(∆ + 2) rounds, any node is either isolated or married. 

Proof. After p M (M ) rounds, by Corollary 18, a safe configuration has been reached. Let us assume that in γpM (M ), there exists some node u that is neither isolated nor married. If status u = inMIS, by Corollary 21, u is isolated or married in γr. Otherwise, we can apply Lemma 23 iteratively (at most ∆ times) until either: 

u gets matched All the neighbors of u get matched. In both scenarios, u ends up married or isolated, and this happens before γr , as p M (id u) ≤

pM (M ). ◀

# 5.2.4 Stable configurations 

A configuration is said to be stable if every node is either isolated or married. 

▶ Lemma 25. The set of stable configurations is closed. 

Proof. In a stable configuration, the predicates canJoinMIS, mustLeaveMIS, badMatching, gotMatch, gotSeduced and canSeduce evaluate at False on every node. ◀▶ Theorem 26. In a stable configuration, the set of edges {u, v } such that status u = status v =matched , partner u = v, and partner v = u defines a maximal matching. 

Proof. Suppose that it is possible to add one more edge {u, v } to the matching so that it remains a legal matching. Hence, both u and v are not matched yet so married( u) = married( v) = False. Since the configuration is terminal, predicates isolated( u) and isolated( v)must evaluate to True. But by definition, two neighbors cannot simultaneously verify the predicate isolated. Contradiction. ◀▶ Theorem 27. Algorithm 3 computes a Maximal Matching in O(∆ 2M log M ) rounds. Each node is awake at most O(∆ 2) times during the convergence phase. The awake frequency after convergence is O(1 /(M log M )) .18 Introducing the Self-Stabilizing SLEEPING Model 

Proof. A stable configuration is reached after O(p M (M )∆ 2) rounds according to corollary 24. In a stable configuration, a Maximal Matching is computed, thanks to Lemma 25 and Theorem 26, forever. By definition, p(M ) ≤ 2B. We have B = O(M log M ) (Lemma 13). By the choice of B, a node is awake at least once and at most four times in p M (M ) rounds, so the awake complexity of a node before convergence of the system is at most O(∆ 2), and the awake frequency after convergence, for each node, is O(1 / pM (M )). ◀

# 6 Two Automatic Transformers 

In this section, we introduce two ways to transform a self-stabilizing algorithm to make it more energy-efficient by lowering the frequency with which a node is awake. Algorithm 4 simply forces each node to be awake once every SP rounds, for some constant SP . Algorithm 5 is more elaborate and tries to converge faster when a node detects that some computation is required. In this section, we will denote the initial algorithm by A.

# 6.1 A Simple Transformer: Algorithm 4 

Algorithm 4 is a simple transformer that converts a self-stabilizing algorithm into a self-stabilizing SLEEPING Algorithm. The transformed algorithm, denoted by A, only needs to converge under the weakly fair scheduler. If A converges in T rounds then the obtained algorithm converges in T.SP synchronous rounds, for any arbitrary SP . The wake-up frequency is always the same 1 /SP . Therefore, during the convergence time, each node is awake during T synchronous rounds.  

> Algorithm 4 Simple transformer to make a self-stabilizing algorithm energy efficient

Inputs: 

A : a self-stabilizing algorithm under the weakly fair scheduler SP : an integer being the awake frequency 

Upon waking:  

> 1

u executes an action of A if it is enabled; Sleep for SP rounds; 

# 6.2 A More Elaborate Transformer: Algorithm 5 

Algorithm 5 is a transformer that allows converting a silent self-stabilizing algorithm under the synchronous scheduler into a silent self-stabilizing SLEEPING Algorithm. If A converges in T rounds under the synchronous scheduler, then the obtained algorithm converges in (3 D + 2) .SP + 3 T synchronous rounds, where D is the diameter of the graph. The idea of the algorithm is to (1) detect that a node has not converged, (2) broadcast the information to the whole graph, (3) have T synchronous rounds where every node is active to converge, and (4) go back to sleeping with a 1 /SP awake frequency (where every node actually wake up at the same time every SP rounds). More formally, the first node, u, to locally detect that A has diverged stays awake for 

D.SP + T consecutive rounds. More specifically, u sets its variable ect (elapsed convergence time) to 1. The first period of D.SP rounds is used to wake up all nodes. In the second period of T rounds, every node executes the algorithm A. By the definition of T , a terminal Fages, Johnen and Rabie 19 

configuration of A is reached at the end of this period. During the final period of at most D.SP + T rounds, every node ends up setting its ect variable to 0 to reach a silent configuration.  

> Algorithm 5 Elaborate transformer to make a self-stabilizing algorithm energy efficient

Inputs: 

A : a self-stabilizing algorithm under the weakly fair scheduler SP : an integer being the awake frequency after convergence D : an upper bound on the diameter of the graph T : an upper bound on the maximum number of rounds needed for the convergence of A

Local variable: 

sleepDuration u : integer taking value in {1, SP }

Shared variable: 

ect u : integer taking value in [0 , D.SP + T ]

macro "Elapsed Convergence Time": 

MSPect u := 

{ 0 if ∀v ∈ N [u], ect v = 0 1 + min of { ect v | v ∈ N [u] and ect v > 0} otherwise 

Upon waking: 

/* Regularly wake up to detect an anomaly. */  

> 1

if u is disabled in A and MSPect u = 0 then sleepDuration u := SP ; 

> 2

else  

> 3

if u is enabled in A then u executes an action of A ; 

> 4

if MSPect u < D.SP + T then  

> 5

ect u := max(1 , MSPect u); sleepDuration u := 1;  

> 6

else ect u := 0; sleepDuration u := SP ; 

> 7

Sleep for sleepDuration u rounds. To prove that our transformer works, we first need the following predicate that ensures that nodes are sleeping only if their variable ect is set to 0, and prove that this predicate is and remains true after most SP rounds. Predicate pseudoCorrect( u) : sleepDuration u = SP ⇔ ect u = 0 

▶ Lemma 28. After SP rounds, every node verifies the predicate pseudoCorrect . The predicate pseudoCorrect is closed. 

Proof. The variable sleepDuration takes values in {1, SP }, meaning that every node is active at least once every SP rounds. An awake node updates its two variables sleepDuration and ect in such a way that it verifies the predicate pseudoCorrect at the end of the current round. ◀

We introduce, for any configuration γ, GMSPect γ as the minimum value of ect among 20 Introducing the Self-Stabilizing SLEEPING Model 

all the nodes. Formally, GMSPect γ =

{ 0 if ∀u ∈ V, ect u = 0 min of { ect u | ect u > 0} otherwise 

# 6.2.1 Executions from a pseudoCorrect configuration 

A configuration is said to be pseudoCorrect if all nodes verify the predicate pseudoCorrect. In this section, we show that from such a configuration, a terminal configuration of A is reached. 

▶ Lemma 29. Let γ0 be a pseudoCorrect configuration. Let us assume that there exists some node u such that ect 0 

> u

≤ (D − 1) SP .In round γSP , we have ect SP v ∈ [1 , ect 0 

> u

+SP ] if v is u or a neighbor of u.

Proof. As long as ect u < D.SP + T , the value ect u can only increase by at most one by Algorithm 5 before reaching (if ever) the value 0. Hence, for any configuration γl with 

l ∈ [1 , SP ], we get that 1 ≤ ect lu ≤ ect 0 

> u

+l.Let v ∈ N (u). There exists i ∈ [0 , SP − 1] such that v is awake in configuration γi. We have MSPect iv ∈ [2 , ect iu +1]. Hence, ect i+1  

> v

= MSPect iv . As MSPect v remains positive and increases by at most 1 during [ i, SP ], we can conclude that ect SP v ∈ [1 , ect 0 

> u

+SP ]. ◀▶ Lemma 30. Let γ0 be a pseudoCorrect configuration. Let us assume that there exists some node u such that ect u = 1 . All nodes are active during the T consecutive rounds from γD.SP 

to γD.SP +T and γD.SP +T is a terminal configuration of A.

Proof. First, we observe that, if ect v = k > 0 for some node v, then this value increases by at most 1 before reaching D.SP + T . Hence, v will be awake for at least D.SP + T − k

consecutive rounds. Let’s prove by induction on d that nodes at distance d from u are active in configurations 

γd.SP to γD.SP +T . For d = 0, it is immediate, as u is the only node at distance 0 from u. By applying Lemma 29, we immediately get the inductive step. Since all nodes are at a distance of at most D from u, all nodes are active in round D.SP 

with their ect variable at values in [1 , D.SP ]. From this round, they will all be active for the next T consecutive rounds. ◀▶ Lemma 31. Let γ0 be a pseudoCorrect configuration of Algorithm 5. Let u be a node. Assume that ect 1

> u

̸ = 1 . After a round from γ0, either ect 1 

> u

= 0 or the value of ect 1 

> u

>

GMSPect 0.

Proof. We study the behavior of u during the round from γ0. Assume that u executes line 5 during the round from γ0. As MSPect 1

> u

̸ = 0 we have MSPect 1 

> u

≥ GMSPect 0 + 1. Hence, the value of ect 1 

> u

> GMSPect 0.Assume that u executes line 6 during the round from γ0. After the round, ect u = 0. Assume that u is awake and does not execute line 5 or 6 during the round from γ0. So, MSPect 0 

> u

= 0 and the value of ect u is unchanged during this round. Assume that u is not awake. As the predicate pseudoCorrect is verified in γ0; after the round, ect u = 0. ◀

Predicate correct( u) : pseudoCorrect( u) & ect u = 0 Fages, Johnen and Rabie 21 

▶ Lemma 32. Let ϵ be an execution from γ0, a pseudoCorrect configuration. Assume during the first D.SP + T configurations of ϵ, there is not node u such that ect u = 1 . There is a configuration among the first D.SP + T configurations of ϵ such that every node verifies the predicate correct .

Proof. Any reached configuration of ϵ is pseudoCorrect (Lemma 28). So it is enough to establish that a configuration where every ect variable has the value 0 is reached. If in γ0, every node u verifies ect u = 0 then γ0 is the requested configuration. Assume that in γ0 a node verifies ect u > 0. So, GMSPect 0 ≥ 2 by hypothesis. We assume that during the first D.SP + T configurations of ϵ, there is not node u such that ect u = 1. So, the repeated use of lemma 31 establishes that during the first D.SP + T − GMSPect 0 + 1 rounds, the value GMSPect increases to reach D.SP +T +1. After D.SP +T −GMSPect 0 +1 rounds, on every node, ect has the value 0. We conclude that every node verifies the predicate correct. ◀▶ Lemma 33. Let ϵ be an execution from γ0 a pseudoCorrect configuration. After (D +1) .SP + T rounds, a terminal configuration of A is reached or there is a node u such that 

ect u = 1 during one of the first (D + 1) .SP + T configurations of ϵ.

Proof. Assume that no node u verifies ect u = 1 during the first D.SP + T configurations of 

ϵ. According to Lemma 32, there is a configuration among the first D.SP + T configurations of ϵ such that every node verifies the predicate correct. Let γ be that configuration. Assume that γ is not a terminal configuration of A. Let r be the first round along ϵ

from γ where an enabled node for A, u, is awake. We get that ect ru = 1: u has executed line 5 when all nodes verify the predicate correct. Since each node wakes up with a frequency greater than or equal to 1/SP, r ≤ SP . ◀

# 6.2.2 Executions from a terminal pseudoCorrect configuration 

We say that a configuration is legitimate when every node u verifies the predicate correct( u)and u is disabled for A. We show in this section that a legitimate configuration is always reached. 

▶ Lemma 34. Let γ0 be a pseudoCorrect configuration of Algorithm 5 where a terminal configuration of A is reached. Let u be a node. After a round from γ0, either ect u = 0 or the value of ect u > GMSPect 0.

Proof. Let u be a node. We study the behavior of u during the round from γ0. Assume that 

u executes line 5 during the round from γ0. By hypothesis, in γ0, u is not enabled for A; so MSPect u̸ = 0 in γ0. Thus MSPect u ≥ GMSPect 0 + 1. Hence, the value of ect u > GMSPect 0,after the round from γ0.Assume that u executes line 6 during the round from γ0. After the round, ect u = 0. Assume that u is awake and does not execute line 5 or 6 during the round from γ0. So, MSPect u = 0 in γ0 and the value of ect u is unchanged during this round. Therefore, the value of ect u is still zero after the round. Assume that u is not awake. As the predicate pseudoCorrect is verified in γ0; after the round, ect u = 0. ◀▶ Corollary 35. Let γ0 a pseudoCorrect configuration of Algorithm 5 where a terminal configuration of A is reached. After at most D.SP + T rounds, a legitimate configuration is reached. The set of legitimate configurations is closed. 22 Introducing the Self-Stabilizing SLEEPING Model 

Proof. Let γ0 be a configuration. If GMSPect 0 = 0 then γ0 is a legitimate configuration -every node u verifies ect u = 0 and sleepDuration u := SP . Only line 1 is executed by the awake nodes. So, the reached configuration is legitimate. Assume that GMSPect 0 > 0. Let γ1 be the configuration reached from γ0. According to lemma 34, GMSPect 1 > GMSPect 0 or γ1 is a legitimate configuration. As GMSPect γ is bounded by D.SP + T . After D.SP + T − GMSPect 0 rounds, every node u verifies ect u = 0 and sleepDuration u := SP . ◀▶ Theorem 36. Let ϵ = γ0, γ 1, · · · be an execution of Algorithm 5. After 3( D.SP + T ) + 2 SP 

rounds, a legitimate configuration is reached. 

Proof. From γ0, a configuration γr where every node verifies the predicate pseudoCorrect is reached after at most SP rounds. First case. There is a node u such that ect u = 1 at γr′ where γr′ is one of the first (D + 1) .SP + T configurations of e after reaching γr. According to Lemma 30, γr′′ with 

r′′ = r′ + D.SP + T ≤ 2( D.SP + T + SP ) is a terminal configuration of A.Second case. No node u verifies ect u = 1 during the first ( D + 1) .SP + T configurations of e after reaching γr. According to Lemma 33, γr′ is a terminal configuration of A with 

r′ = r + ( D + 1) SP + T ≤ D.SP + T + 2 SP .To conclude, after s = 2( D.SP + SP + T ) rounds, a terminal configuration of A is reached. According to Corollary 35, γs′ with s′ = s + D.SP + T ≤ 3( D.SP + T ) + SP is a silent legitimate configuration. ◀

# 7 Conclusion 

In this paper, we have introduced a new model that incorporates energy efficiency into self-stabilization by allowing nodes to be inactive. We have introduced several algorithms to solve classical problems, with new techniques that revolve in particular around the use of prime numbers to simulate asynchrony, mutual exclusion, and pseudo-synchrony. We have also implemented two transformers to automatically make any self-stabilizing algorithm more energy-efficient, at the cost of some time-complexity overhead. Upcoming work can focus on developing new techniques to improve the complexities of the algorithms we provide and solve other problems, such as leader election or spanning trees. 

References              

> 1John Augustine, William K Moses Jr, and Gopal Pandurangan. Awake complexity of dis-tributed minimum spanning tree. In International Colloquium on Structural Information and Communication Complexity , pages 45–63. Springer, 2024.
> 2Alkida Balliu, Sebastian Brandt, Juho Hirvonen, Dennis Olivetti, Mikaël Rabie, and Jukka Suomela. Lower bounds for maximal matchings and maximal independent sets. Journal of the ACM (JACM) , 68(5):1–30, 2021.
> 3Alkida Balliu, Pierre Fraigniaud, Dennis Olivetti, and Mikaël Rabie. Solving sequential greedy problems distributedly with sub-logarithmic energy cost. In 44th ACM Symposium on Principles of Distributed Computing, (PODC 2025) , pages 417–427. ACM, 2025. doi: 10.1145/3732772.3733499 .
> 4Leonid Barenboim, Michael Elkin, and Uri Goldenberg. Locally-iterative distributed (∆ + 1)-coloring and applications. J. ACM , 69(1):5:1–5:26, 2022. doi:10.1145/3486625 .
> 5Leonid Barenboim and Tzalik Maimon. Deterministic logarithmic completeness in the dis-tributed sleeping model. In 35th International Symposium on Distributed Computing (DISC 2021) , pages 10–1. Schloss Dagstuhl–Leibniz-Zentrum für Informatik, 2021.

Fages, Johnen and Rabie 23 

6 Soumyottam Chatterjee, Robert Gmyr, and Gopal Pandurangan. Sleeping is efficient: Mis in o (1)-rounds node-averaged awake complexity. In Proceedings of the 39th Symposium on Principles of Distributed Computing , pages 99–108, 2020. 

7 Johanne Cohen, George Manoussakis, and Laurence Pilard. From state to link-register model: A transformer for self-stabilizing distributed algorithms. In 2023 25th International Symposium on Symbolic and Numeric Algorithms for Scientific Computing (SYNASC) , pages 114–121. IEEE, 2023. 

8 Johanne Cohen, Laurence Pilard, Mikaël Rabie, and Jonas Sénizergues. Making self-stabilizing algorithms for any locally greedy problem. In 2nd Symposium on Algorithmic Foundations of Dynamic Networks (SAND 2023) , pages 11–1. Schloss Dagstuhl–Leibniz-Zentrum für Informatik, 2023. 

9 Alain Cournier, Ajoy Kumar Datta, Stéphane Devismes, Franck Petit, and Vincent Villain. The expressive power of snap-stabilization. Theor. Comput. Sci. , 626:40–66, 2016. doi: 10.1016/j.tcs.2016.01.036 .

10 Stéphane Devismes, David Ilcinkas, Colette Johnen, and Frédéric Mazoit. Asynchronous self-stabilization made fast, simple, and energy-efficient. In the 43rd ACM Symposium on Principles of Distributed Computing, (PODC 2024) , pages 538–548, 2024. doi:10.1145/ 3662158.3662803 .

11 Stéphane Devismes, David Ilcinkas, Colette Johnen, and Frédéric Mazoit. Asynchronous self-stabilization made fast, simple, and energy-efficient. In 43rd ACM Symposium on Principles of Distributed Computing, (PODC 2024) , pages 538–548. ACM, 2024. doi:10.1145/3662158. 3662803 .

12 Swan Dubois and Sébastien Tixeuil. A taxonomy of daemons in self-stabilization. arXiv: 1110.0334 , 2011. 

13 Fabien Dufoulon, William K Moses Jr, and Gopal Pandurangan. Distributed mis in o(log log n) awake complexity. In Proceedings of the 2023 ACM Symposium on Principles of Distributed Computing , pages 135–145, 2023. 

14 Xinyu Fu, Yitong Yin, and Chaodong Zheng. Locally-iterative (∆8239 + 8239)-coloring in sublinear (in ∆) rounds. Theoretical Computer Science , 1052:115456, 2025. URL: https: //doi.org/10.1016/j.tcs.2025.115456 , doi:10.1016/J.TCS.2025.115456 .

15 Mohsen Ghaffari and Julian Portmann. Average awake complexity of mis and matching. In 

Proceedings of the 34th ACM Symposium on Parallelism in Algorithms and Architectures ,pages 45–55, 2022. 

16 Mohsen Ghaffari and Julian Portmann. Distributed MIS with low energy and time complexities. In Proceedings of the 2023 ACM Symposium on Principles of Distributed Computing , pages 146–156, 2023. 

17 Sukumar Ghosh and Mehmet Hakan Karaata. A self-stabilizing algorithm for coloring planar graphs. Dist. Computing , 7(1):55–59, 1993. doi:10.1007/BF02278856 .

18 Maria Gradinariu and Sébastien Tixeuil. Self-stabilizing vertex coloration and arbitrary graphs. In 4th International Conference on Principles of Distributed Systems, (OPODIS 2000) , Studia Informatica Universalis, pages 55–70, 2000. 

19 Nabil Guellati and Hamamache Kheddouci. A survey on self-stabilizing algorithms for independence, domination,coloring, and matching in graphs. JPDC , 70(4):406–415, 2010. URL: https://doi.org/10.1016/j.jpdc.2009.11.006 , doi:10.1016/J.JPDC.2009.11.006 .

20 Jacques Hadamard. Sur la distribution des zéros de la fonction ζ(s) et ses conséquences arithmétiques. Bulletin de la Societé mathematique de France , 24:199–220, 1896. 

21 Stephen T. Hedetniemi, David Pokrass Jacobs, and Pradip K. Srimani. Linear time self-stabilizing colorings. Information Processing Letters , 87(5):251–255, 2003. doi:10.1016/ S0020-0190(03)00299-0 .

22 Ted Herman, Sriram Pemmaraju, Laurence Pilard, and Morten Mjelde. Temporal partition in sensor networks. In Symposium on Self-Stabilizing Systems , pages 325–339. Springer, 2007. 24 Introducing the Self-Stabilizing SLEEPING Model 

23 Adrian Kosowski and Lukasz Kuszner. Self-stabilizing algorithms for graph coloring with improved performance guarantees. In 8th International Conference Artificial Intelligence and Soft Computing, (ICAISC 2006) , volume LNCS 4029, pages 1150–1159, 2006. doi: 10.1007/11785231\_120 .

24 Christoph Lenzen, Jukka Suomela, and Roger Wattenhofer. Local algorithms: Self-stabilization on speed. In 11th International Symposium on Stabilization, Safety, and Se-curity of Distributed Systems,(SSS 2009) , volume LNCS 5873, pages 17–34, 2009. doi: 10.1007/978-3-642-05118-0\_2 .

25 Nathan Linial. Distributive graph algorithms global solutions from local data. In 28th Annual Symposium on Foundations of Computer Science (sfcs 1987) , pages 331–335. IEEE, 1987. 

26 Fredrik Manne, Morten Mjelde, Laurence Pilard, and Sébastien Tixeuil. A new self-stabilizing maximal matching algorithm. Theor. Comput. Sci. , 410(14):1336–1345, 2009. URL: https: //doi.org/10.1016/j.tcs.2008.12.022 , doi:10.1016/J.TCS.2008.12.022 .

27 Moni Naor and Larry Stockmeyer. What can be computed locally? In Proceedings of the twenty-fifth annual ACM symposium on Theory of computing , pages 184–193, 1993. 

28 David Peleg. Distributed computing: a locality-sensitive approach . SIAM, 2000. 

29 Wei Sun, Zheng Yang, Xinglin Zhang, and Yunhao Liu. Energy-efficient neighbor discovery in mobile ad hoc and wireless sensor networks: A survey. IEEE Communications Surveys & Tutorials , 16(3):1448–1459, 2014. 

30 Jukka Suomela. Survey of local algorithms. ACM Computing Surveys (CSUR) , 45(2):1–40, 2013. 

31 Volker Turau. Linear self-stabilizing algorithms for the independent and dominating set problems using an unfair distributed scheduler. Inf. Process. Lett. , 103(3):88–93, 2007. URL: 

https://doi.org/10.1016/j.ipl.2007.02.013 , doi:10.1016/J.IPL.2007.02.013 .

32 Tijs Van Dam and Koen Langendoen. An adaptive energy-efficient mac protocol for wireless sensor networks. In Proceedings of the 1st international conference on Embedded networked sensor systems , pages 171–180, 2003. 

33 Wei Ye, John Heidemann, and Deborah Estrin. An energy-efficient mac protocol for wireless sensor networks. In Proceedings. Twenty-first annual joint conference of the IEEE computer and communications societies , volume 3, pages 1567–1576. IEEE, 2002.