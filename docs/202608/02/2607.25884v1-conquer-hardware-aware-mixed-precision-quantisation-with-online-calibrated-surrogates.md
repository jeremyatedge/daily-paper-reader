---
title: "CONQuER: Hardware-Aware Mixed-Precision Quantisation with Online-Calibrated Surrogates"
title_zh: CONQuER：基于在线校准代理的硬件感知混合精度量化
authors: "Aidan Dakhama, Ajitha Rajan"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25884v1"
tags: ["query:aiot"]
score: 9.0
evidence: 基于在线校准替代模型的硬件感知混合精度量化
tldr: "当前混合精度量化（MPQ）常作为与硬件无关的预处理步骤，独立于编译器，导致位宽映射到张量核等异构单元时严重失配，且穷举硬件在环测试不可行。CONQuER将量化集成进编译器TOSA流水线，基于NSGA-II进化算法搜索，引入双代理预筛选（缓存界限+特征各向同性）丢弃不可行解，并通过IREE在硬件上实测最强候选，在线校准代理模型。在移动/笔记本CPU和服务器GPU上，CONQuER发现的Pareto最优配置实现最高12.19倍推理加速，top-1精度仅比未量化基线低1.44%，同时证明最优量化策略高度依赖目标硬件。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25884v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1496, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25884v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1873, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25884v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1396, \"height\": 981, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25884v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25884v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 858, \"height\": 197, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25884v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 695, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25884v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 936, \"height\": 902, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25884v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1880, \"height\": 555, \"label\": \"Table\"}]"
motivation: 现有MPQ与编译器分离，位宽无法匹配异构硬件，穷举硬件在环测试不可行，导致推理运行时惩罚严重。
method: CONQuER将量化移入编译器TOSA流水线，用NSGA-II进化搜索结合双代理预筛选（缓存上界与特征各向同性），并通过在线校准器用硬件实测反馈修正代理模型。
result: "在移动/笔记本CPU和GPU上，CONQuER实现最高12.19倍加速，top-1精度损失在1.44%以内，并发现最优配置随硬件变化。"
conclusion: 将量化与编译器lowering及物理执行耦合可实现硬件感知MPQ，表明最优量化策略硬件相关，为部署工具链提供统一方案。
---

## 摘要
在资源受限硬件上部署深度神经网络依赖于混合精度量化（MPQ）。当前的部署工具链严重割裂了这一过程。量化通常作为前端框架中与硬件无关的预处理步骤，与生成物理机器码的下游编译器相分离。这种分离导致配置次优，所分配的位宽与目标机器的异构硬件执行块（如张量核心和可变宽度向量单元）映射不佳，从而产生严重的运行时执行惩罚。此外，由于搜索空间呈指数级增长，通过穷举式硬件在环（HIL）测试来评估这些配置是难以处理的。我们提出了CONQuER，一个用于硬件感知MPQ的统一编译器集成基础设施。CONQuER将量化转移到TOSA级别的编译器流水线中，基于编译器支持实现智能配置处理。为了在实际编译预算内评估不同模型层的组合搜索空间，CONQuER将NSGA-II进化算法与双代理预筛选引擎相结合。该引擎评估理论缓存内存边界和特征空间各向同性，以丢弃不可行的配置。随后，CONQuER通过IREE仅对最强的候选策略在硬件上执行，并将执行指标输入在线校准器。该校准器在NSGA-II进化搜索过程中将代理模型与真实硬件行为对齐。在移动和笔记本电脑CPU以及服务器GPU上的评估表明，最优量化策略是硬件相关的。通过将量化与编译器降级和物理执行相结合，CONQuER发现帕累托最优配置，推理速度最高提升12.19倍，top-1准确率与未量化基线相差在1.44%以内。

## Abstract
Deploying deep neural networks on resource-constrained hardware relies on mixed-precision quantisation (MPQ). current deployment toolchains severely fragment this process. Quantisation typically occurs as a hardware-agnostic preprocessing step in front-end frameworks, disconnected from the downstream compilers that generate the physical machine code. This separation leads to suboptimal configurations where assigned bit-widths map poorly to the target machine's heterogeneous hardware execution blocks such as tensor cores and variable-width vector units, incurring severe runtime execution penalties. Furthermore, evaluating these configurations via exhaustive hardware-in-the-loop (HIL) testing is intractable due to the exponentially large search space. We present CONQuER, a unified compiler-integrated infrastructure for hardware-aware MPQ. CONQuER shifts quantisation into the compiler pipeline at the TOSA level, enabling intelligent configuration handling based on compiler support. To evaluate this combinatorial search space of different of model layers within practical compilation budgets, CONQuER couples an NSGA-II evolutionary algorithm with a dual-surrogate prescreening engine. This engine evaluates theoretical cache memory bounds and feature space isotropy to discard non-viable configurations. CONQuER then executes only the strongest candidate policies on hardware via IREE, feeding the execution metrics into an online calibrator. This calibrator aligns the surrogate models with the true hardware behaviour during an NSGA-II evolutionary search. Evaluation across mobile and laptop CPUs, and server GPUs demonstrates that optimal quantisation policies are hardware-dependent. By coupling quantisation with compiler lowering and physical execution, CONQuER discovers Pareto-optimal configurations up to 12.19x faster inference with top-1 accuracy within 1.44% of the unquantised baseline.

---

## 论文详细总结（自动生成）

# CONQuER 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **核心问题**：深度神经网络在资源受限硬件上的部署严重依赖混合精度量化（MPQ），但当前部署工具链高度碎片化。量化通常在前端框架中作为与硬件无关的预处理步骤执行，与下游编译器分离，导致：
  - 分配的位宽与目标硬件的异构执行单元（如张量核、可变宽度向量单元）映射不佳，造成严重运行时性能损失；
  - 穷举式硬件在环（HIL）测试由于搜索空间的指数增长而不可行；
  - 理论有效的 MPQ 策略常因目标编译器的后端不支持而无法实际部署。
- **整体含义**：量化决策必须与编译器降级（lowering）过程及真实硬件执行紧密结合，最优量化策略是硬件相关的，这为统一部署工具链提供了新的研究范式。

## 2. 论文提出的方法论

### 核心思想
将混合精度量化从外部前端脚本转移到 **MLIR 编译器中间表示（IR）的 TOSA 方言层**，实现编译器原生的量化支持，并通过**双代理预筛选 + NSGA-II 进化搜索 + 在线校准**的方式高效搜索 Pareto 最优混合精度策略。

### 关键技术细节

**（1）原生 TOSA 量化基础设施（conquer-opt）**
- 在 MLIR TOSA 方言层原生生成和操作量化图；
- 框架无关，与前端框架解耦，保证所有探索的配置可被下游编译器支持；
- 预校准阶段使用 256 张验证集图像计算激活范围（min/max）与激活协方差矩阵。

**（2）双代理预筛选引擎**
- **硬件代理（Hardware Surrogate）**：基于 roofline 模型估算节点延迟：
  - 总延迟 \(C_{total} = max(C_{compute}, C_{memory}) + C_{dispatch}\)；
  - 计算界（\(C_{compute}\)）由 SIMD 宽度推导，内存界（\(C_{memory}\)）对照目标缓存容量映射；
  - 违反目标设备物理内存约束的配置被立即剪枝。
- **精度代理（Accuracy Surrogate）**：基于特征空间各向同性（feature space isotropy）估计精度敏感性：
  - 对层激活的协方差矩阵进行特征值分解，计算归一化香农熵：
    \[
    H = -\frac{1}{\ln(C)} \sum_{i=1}^{C} p_i \ln(p_i), \quad p_i = \frac{|\lambda_i|}{\sum_j |\lambda_j|}
    \]
  - 熵越高表示特征空间越各向同性，层对量化噪声越脆弱；
  - 结合一阶泰勒近似与理论量化噪声幅度（\(\approx 2^{-b}\)），估计节点的总精度退化惩罚。

**（3）在线校准的进化搜索**
- 采用 NSGA-II 多目标进化算法，优化目标为最小化延迟和精度退化；
- 每代中，仅最有前景的候选配置通过 IREE 编译并在目标硬件上实际执行（使用 512 张独立搜索图像）；
- 测量结果通过在线对数校准器 \(y = a\ln(x) + b\) 在滑动窗口内持续对齐代理模型与真实硬件行为；
- 延迟评估采用 3 次 warm-up + 10 次计时运行。

## 3. 实验设计

### 数据集与场景
- **数据集**：ImageNet，严格的数据隔离：
  - 256 张图像：conquer-opt 初始校准与敏感性分析；
  - 512 张图像：遗传算法中的 HIL 评估（搜索分割）；
  - 其余未见过的验证集图像：最终 Pareto 最优配置的独立测试。
- **目标硬件**：移动处理器（Snapdragon 8 Elite）、笔记本 CPU（Intel I5 1145g7）、服务器 GPU（NVIDIA A100 80GB）。
- **模型**：MobileNetV2、ResNet-18、ResNet-50、EfficientNet。

### 对比基线
| 方法 | 类型 | 说明 |
|------|------|------|
| FP32 基线 | 未量化 | 准确率上界与延迟基线 |
| SeQTO | HIL 方法 | 贪心逐层调优，预算上限 1,440 次硬件推理 |
| InfoQ | 分析型方法 | 基于全局信息流 + ILP 分配位宽，评估 4×、6×、“high”三档压缩目标 |

### 主要研究问题
- **RQ1**：与 SOTA 方法（硬件感知与硬件无关）相比，Pareto 折衷（延迟、准确率、内存）的改进；
- **RQ2**：跨硬件目标迁移的泛化能力/性能退化；
- **RQ3**：双代理系统的消融分析（No Proxy、Accuracy-Only、Latency-Only、Dual）。

## 4. 资源与算力

- 论文执行环境包括 **NVIDIA A100 80GB**（服务器 GPU）、**Intel I5 1145g7**（笔记本 CPU）、**Snapdragon 8 Elite**（移动 CPU）。
- 文中未明确说明使用了多少张 GPU、CPU 核心数等具体算力配置。
- 仅提及 InfoQ 的 QAT 训练预算限制为 **12 小时的训练时长**，以匹配 CONQuER 搜索的平均墙钟时间；并说明 CONQuER 的 12 小时搜索开销主要依赖标准 CPU 并发编译能力。

## 5. 实验数量与充分性

### 实验数量
- **RQ1（对比实验）**：CONQuER 对比 SeQTO 和 InfoQ，覆盖 3 种硬件 × 3 个模型，各重复 5 次；
- **RQ2（迁移实验）**：从每种源硬件 Pareto 前沿提取 3 种代表性策略（延迟最优、精度最优、平衡策略），部署到失配目标上进行评估；
- **RQ3（消融实验）**：在 NVIDIA A100 上对 4 个模型 × 4 种代理配置进行 5 次重复实验。

### 充分性与公平性评估
- **优点**：使用了多次重复（5 次随机种子），报告均值和方差；严格数据隔离防止过拟合；对 SeQTO 的评估预算做了严格控制（1,440 次硬件推理与 CONQuER 一致）。
- **不足**：
  - InfoQ 在 ARM 设备上无法运行（依赖 x86 的 fbgemm 算子），其延迟只能在 PyTorch 运行时评估，而非 IREE 编译栈，这使得与 CONQuER 的比较存在工具链不对等；
  - InfoQ 的 QAT 修复与 CONQuER 的 PTQ 搜索不完全等效（前者需要专用训练硬件）；
  - 实验仅覆盖 CNN 图像分类模型，未涉及 Transformer 等动态架构。

## 6. 论文的主要结论与发现

- **量化策略高度硬件相关**：最优混合精度策略在不同硬件之间不可迁移，跨目标部署通常导致 1.05×~1.79× 的延迟惩罚。
- **CONQuER 显著超越基线**：
  - 在 NVIDIA A100 上，ResNet-50 达到 **12.19× 加速**，top-1 精度仅下降 **1.44%**；
  - 在 Snapdragon 8 Elite 上，ResNet-18 达到 3.31× 加速且精度无损失；
  - SeQTO 的贪心逐层方法易于陷入局部最优；InfoQ 在 PTQ 设置下几乎无法获得加速，需要 QAT 才能恢复精度。
- **工具链集成的价值**：SeQTO 和 InfoQ 生成的模型存在无法通过 IREE 编译器降低的结构性问题，验证了在编译器内部进行优化的必要性。
- **双代理消融**：双代理系统收敛最快（平均 34.45 代）、Hypervolume 最高、Pareto 前沿质量最优；单代理会导致目标偏斜；无引导搜索效率极低。

## 7. 优点

- **编译器集成创新**：首次将混合精度量化原生嵌入 MLIR TOSA 层，解决工具链碎片化问题，确保所有配置可执行。
- **高效搜索机制**：双代理预筛选 + 在线校准的 NSGA-II 搜索，兼顾搜索效率和代理可靠性。
- **硬件在环反馈**：仅执行有前景的候选配置，在线校准代理模型与真实硬件行为对齐。
- **严格的实验规范**：数据隔离（校准/搜索/测试分离）、多次重复、统计报告（均值±标准差）、预算控制。
- **跨硬件全面评估**：覆盖移动、笔记本、服务器 GPU 等多样化部署场景。

## 8. 不足与局限

- **模型覆盖有限**：仅评估 CNN 图像分类模型（MobileNetV2、ResNet、EfficientNet），未涉及 Transformer 等动态架构。
- **原型实现限制**：conquer-opt 是原型，部分量化节点的降级可能非最优，可能人为惩罚某些配置。
- **基线比较不对等**：InfoQ 在 ARM 上无法运行，延迟评估使用 PyTorch 运行时而非 IREE，导致对比结果可能受到工具链差异的影响。
- **热管理与测量噪声**：虽然已采取措施（固定性能配置），但边缘设备的热节流仍可能存在残余影响。
- **代理可靠性风险**：静态代理可能受运行时异常影响，在线校准虽然缓解但不能完全消除。
- **搜索开销较高**：相比分析型方法（如 InfoQ 分钟级），CONQuER 需要数小时编译和硬件执行，虽然这个投资在大规模部署中可回收。
- **小模型在强 GPU 上收益受限**：NVIDIA A100 上紧凑模型受限于调度开销，量化带来的延迟收益被压缩。

（完）
