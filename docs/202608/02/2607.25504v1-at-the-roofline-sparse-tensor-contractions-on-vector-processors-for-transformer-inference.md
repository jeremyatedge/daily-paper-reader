---
title: At-the-Roofline Sparse Tensor Contractions on Vector Processors for Transformer Inference
title_zh: 面向Transformer推理的向量处理器上达到Roofline的稀疏张量收缩
authors: "Bowen Wang, Chi Zhang, Diyou Shen, Renzo Andri, Navaneeth Kunhi Purayil, Luca Benini"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25504v1"
tags: ["query:aiot"]
score: 9.0
evidence: 面向向量处理器上 Transformer 推理的稀疏张量收缩硬件加速
tldr: "针对Transformer推理中稀疏张量收缩在向量处理器上因缺乏原生支持而远低于roofline的问题，提出Ventaglio运行时配置稀疏执行单元与RVV扩展，通过索引聚集-累加-散射加速。在12nm实现，较优化RVV基线加速6.9-7.4倍，仅3.1%面积开销。配合DuoGPT剪枝的LLaMA-3-8B，在40-60%双稀疏下prefill和decoding分别加速2.40-5.25倍和2.06-3.16倍。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 657, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 924, \"height\": 427, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 926, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 935, \"height\": 315, \"label\": \"Table\"}]"
motivation: 现有RVV架构缺乏对Gustavson数据流原生支持，稀疏张量收缩因软件索引解码和L1索引访存而远低于roofline性能。
method: 提出Ventaglio，含运行时配置稀疏执行单元和RVV扩展，实现索引化聚集-累加-散射以高效支撑双稀疏。
result: "在12nm实现，较RVV基线加速6.9-7.4倍，面积开销3.1%；在LLaMA-3-8B上prefill和decoding加速达2.40-5.25倍和2.06-3.16倍。"
conclusion: 证实原生稀疏执行支持可将向量处理器上稀疏Transformer推理推向roofline，且开销极小。
---

## 摘要
细粒度权重剪枝和激活稀疏化已成为降低Transformer模型推理计算和内存成本的有效方法。在中等稀疏度区间，Gustavson数据流通过元数据驱动的索引累加，为在向量处理器上同时利用激活和权重稀疏性提供了一种自然的执行模型。然而，现有的RVV架构缺乏对该模式的原生支持，迫使内核依赖软件索引解码和基于L1的索引内存操作，使得稀疏张量收缩远低于其roofline性能界限。我们提出了Ventaglio，一个运行时可配置的稀疏执行单元，结合RVV ISA扩展，通过索引聚集-累加-散播支持驱动稀疏张量收缩接近其roofline。集成到开源向量处理集群并用12nm FinFET实现后，Ventaglio将稀疏张量收缩内核加速了$6.9\text{--}7.4\times$，相较于优化后的RVV基线，且仅造成紧密L1耦合向量处理单元集群$3.1\%$的面积开销。我们构建了Ventaglio扩展的性能精确指令级模型，针对RTL实现进行校准，并利用其在大规模$4\times4$多集群系统上进行扩展性能分析。使用经DuoGPT剪枝的LLaMA-3-8B模型，在实用$40\text{--}60\%$双重稀疏度下，Ventaglio在预填充和自回归解码阶段分别实现了相对于密集基线$2.40\text{--}5.25\times$和$2.06\text{--}3.16\times$的加速。

## Abstract
Fine-grained weight pruning and activation sparsification have emerged as effective approaches for reducing the compute and memory cost of inference for Transformer models. In the moderate-sparsity regime, Gustavson's dataflow provides a natural execution model for exploiting both activation and weight sparsity on vector processors through metadata-driven indexed accumulation. However, existing RVV architectures lack native support for this pattern, forcing kernels to rely on software index decoding and L1-backed indexed memory operations that keep sparse tensor contractions far below their roofline performance bound. We present Ventaglio, a runtime-configurable sparse execution unit coupled with RVV ISA extensions that drives sparse tensor contractions toward their roofline through indexed gather-accumulate-scatter support. Integrated into an open-source vector processing cluster and implemented in 12nm FinFET, Ventaglio accelerates sparse tensor contraction kernels by $6.9\text{--}7.4\times$ over optimized RVV baselines, with only $3.1\%$ area overhead for a cluster of tightly-L1 coupled vector processing elements. We build a performance-accurate instruction-level model of the Ventaglio extension, calibrate it against RTL implementation, and leverage it for scale-out performance analysis on a large $4\times4$ multi-cluster system. Using a DuoGPT-pruned LLaMA-3-8B model with practical $40\text{--}60\%$ dual sparsity, Ventaglio achieves $2.40\text{--}5.25\times$ and $2.06\text{--}3.16\times$ speedup over dense baselines during prefill and autoregressive decoding, respectively.

---

## 论文详细总结（自动生成）

# 详细论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景动机**：Transformer架构的成功与扩展定律紧密相关，模型参数从数十亿增长到数万亿，带来巨大的内存占用和计算成本。细粒度权重剪枝和激活稀疏化是降低推理成本的有效手段。
- **核心问题**：在中等稀疏度（MS，零元素占比40–75%）区间，Gustavson数据流为向量处理器同时利用激活和权重稀疏性提供了自然的执行模型（非零激活触发计算，权重元数据决定索引累加位置）。然而，现有RISC-V Vector（RVV）架构缺乏对元数据驱动的索引累加模式的原生支持，导致稀疏张量收缩内核只能依赖：
  1. 软件索引解码；
  2. 基于L1的索引内存操作（gather/scatter）。
  
  这两者使稀疏内核算力利用率远低于roofline性能上限。
- **整体含义**：该问题在自回归解码阶段尤为突出——该阶段以低强度的矩阵-向量运算为主，矩阵中心化的稀疏加速方案（NVIDIA Tensor Core、AMD CDNA、Arm SME2等）容易利用率不足，而向量处理器虽然灵活，却缺乏必要的稀疏执行原语。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将元数据驱动的索引累加提升为原生RVV执行原语，通过硬件-软件协同设计消除稀疏内核的软件开销。
- **Ventaglio架构**：运行时可配置的稀疏执行单元，与VAU并行工作，包含：
  - **多通道SCM内存单元**：1R1W端口与交错寻址，C个可配置通道，总容量1KiB（C=4配置），为稀疏累加状态提供高带宽扩展窗口，避免与VRF争用端口；
  - **模块化gather/scatter数据通路**：多通道宽读转换为紧凑操作数（gather），写回时逆映射（scatter），同一时间仅激活一种稀疏模式，非活跃通路被门控以降低功耗；
  - **beat计数器**：管理打包元数据消费，在当前字最后有效beat预取下一元数据字。
- **ISA扩展**（关键指令）：

  | 指令 | 功能 |
  |---|---|
  | `vlx.v` | 从L1 SPM加载打包索引元数据到VRF |
  | `vfxmacc.vf` | 融合索引乘累加（fused indexed multiply-accumulate），5-bit idx字段选择索引上下文 |
  | `p.vlx.v / p.vle.v / p.fl` | 带地址后增量（post-increment）的标量和向量加载 |
  | CSR映射 | 动态将选中的VRF寄存器映射到Ventaglio内存单元，VRF旁路通道将访问重定向 |

- **算法基础**：针对门控激活向量 $\tilde{x} = g(x) \in \mathbb{R}^N$ 与紧致存储的权重矩阵 $\hat{W} \in \mathbb{R}^{N \times P_c}$，计算 $y = \tilde{x}W$，其中对每个非零激活 $x_i$，按元数据指示的输出坐标集合 $I_i$ 执行 `yj ← yj + xi · wij`。
- **三个关键洞察**：① 稀疏元数据解释应硬件化而非软件反复重构；② 索引化gather/scatter应在核内处理而非L1内存；③ scatter/gather与运算应融合并跨稀疏更新流水化。

## 3. 实验设计：数据集 / 场景 / Benchmark / 对比方法

- **内核级别（Kernel Benchmark）**：
  - 三种稀疏配置：50%稀疏权重（2:4格式）、75%稀疏权重（1:4格式）、50%稀疏激活+60%稀疏权重（bitmap格式）；
  - 覆盖矩阵-矩阵和矩阵-向量两类工作负载；
  - 逻辑内核尺寸为 256(1)×256×256；
  - 三种硬件配置对比：基线Spatz、Ventaglio（vfxmacc）、Ventaglio+地址后增量；
  - 使用QuestaSim 2022.3进行RTL仿真。
- **端到端系统级评估**：
  - 在GVSoC框架中构建Ventaglio指令级模型（与RTL最大偏差2.3%）；
  - 扩展到4×4多集群系统，2D NoC互联，1024-bit网络链路，支持多播和归约；
  - 片外内存使用DRAMSys建模为4×2 HBM2通道，聚合峰值带宽512 GB/s；
  - 模型：DuoGPT剪枝的LLaMA-3-8B，FP16精度；
  - 场景：prefill（S=512, 1K, 2K）和自回归解码（S=1; KV=512, 1K, 2K）；
  - 对比：密集基线 vs 40–60%双重稀疏性（权重和激活同时稀疏）。

## 4. 资源与算力

- **未明确说明的内容**：论文未提及任何GPU型号、数量或大规模训练算力消耗，因为其贡献聚焦于硬件架构，不涉及模型训练。
- **实际使用的硬件/工具链资源**：
  - 12nm FinFET工艺综合、布局布线（目标1 GHz，TT/0.80V/25°C）；
  - QuestaSim 2022.3 RTL仿真；
  - GVSoC全平台仿真器 + DRAMSys内存建模；
  - 实现结果：集群面积4.21 MGE，较基线Spatz的开销仅3.1%。

## 5. 实验数量与充分性

- **实验组数**：
  - 内核级：3种稀疏配置 × 3种硬件配置（其中地址增量变体在roofline分析中重点评估）；
  - 端到端：prefill和decoding各3种序列长度场景；
  - 额外的面积开销分析、性能模型校准（最大偏差2.3%）、与5种现有SOTA设计（NVIDIA Blackwell、AMD MI300、Arm SME2、Arm Ethos-U85、IndexMAC、SCG）的定性对比。
- **充分性评估**：
  - **优点**：覆盖从内核级到端到端、从RTL到系统仿真的多层级验证；roofline分析具有理论支撑；对比维度（性能、面积、利用率）较为全面。
  - **局限**：内核benchmark仅覆盖单一逻辑尺寸（256×256×256）；端到端只评估一个模型（LLaMA-3-8B）、一种剪枝方法（DuoGPT）；与现有硬件方案的对比是定性的（表I），缺乏与其他设计的定量竞争性比较；缺少能量/功耗评估。

## 6. 论文的主要结论与发现

- 内核级：Ventaglio将稀疏张量收缩内核性能提升6.9–7.4倍（vs 优化RVV基线），VAU利用率达89.3–97.4%，使各测试工作负载均达到roofline。
- 硬件开销：12nm实现，3.1%集群级面积开销，无新增关键路径。
- 端到端：
  - Prefill阶段：2.40–5.25倍加速（矩阵乘法计算密集，双稀疏直接降低FLOP成本）；
  - 解码阶段：2.06–3.16倍加速（内存受限，权重稀疏减少HBM流量、激活感知的SUMMA数据流减少无效传输）。
- 剩余差距的根因：更稀疏权重矩阵需维护更多扩展累加状态，压缩LMUL空间并增加循环开销；稀疏激活提取需额外控制指令。
- 总体结论：低开销的硬件增强配合ISA支持，可显著提升向量处理器上稀疏张量计算的性能与效率。

## 7. 优点：方法与实验设计的亮点

- **硬件-软件协同设计**：直接针对现有RVV指令集的架构缺口设计，而非另起炉灶，保持了架构兼容性和可编程性。
- **优雅的VRF旁路机制**：通过CSR动态映射向量寄存器到内存单元，避免了侵入式scoreboard修改，设计优雅。
- **统一支持多种稀疏格式**（1:4、2:4、bitmap）和统一处理激活与权重稀疏性，普适性强。
- **开源可见性**：基于开源Spatz集群集成，代码开源（github.com/pulp-platform/spatz），可复现性好。
- **方法学严谨**：从RTL综合到指令级模型校准到端到端系统仿真，形成了完整的验证链；
- **Roofline分析贯穿始终**：用理论模型解释性能差距并定位瓶颈，分析有据可依。

## 8. 不足与局限

- **稀疏度覆盖有限**：仅验证中等稀疏度（40–75%）区间，未探索高稀疏度或超低稀疏度的行为边界。
- **扩展累加单元容量受限**：1KiB容量限制M维度展开深度，压缩LMUL预算，长向量性能可能受制约。
- **模型评估范围窄**：仅用LLaMA-3-8B + DuoGPT剪枝，未涵盖更多Transformer架构（编码器、多模态）、不同精度（INT8等）或更大模型。
- **与SOTA硬件对比仅停留在定性层面**：缺少与NVIDIA/AMD等商业方案在相同工作负载下的量化对比，实际竞争力不易判定。
- **缺少功耗与能效分析**：论文只报告了面积开销，没有提供能量效率（如TOPS/W）数据——这是边缘部署中与性能同等重要的指标。
- **解码阶段带宽利用率偏低**：稀疏访问模式导致DMA突发短且不连续，可能仍有优化空间（如重新设计数据局部性），但论文未深入探索。
- **缺乏软件生态讨论**：对于编译器支持、auto-vectorization框架或编程模型扩展未作说明，限制了实际采用便利性评估。

（完）
