---
title: Multi-DNN Inference of Sparse Models on Edge SoCs
title_zh: 边缘级片上系统上的稀疏模型多深度神经网络推理
authors: "Jiawei Luo, Di Wu, Simon Dobson, Blesson Varghese"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09642v1"
tags: ["query:aiot"]
score: 10.0
evidence: 利用模型缝合在边缘SoC上进行多DNN推理优化
tldr: 针对边缘SoC上多DNN推理系统效率低、SLO违规率高的问题，本文提出了一种名为“模型缝合”的新技术。该技术通过在无需重新训练的情况下，重组稀疏模型的子图来生成多种模型变体，从而更好地匹配异构处理器。实验表明，所提出的SparseLoom系统显著降低了SLO违规率，提升了吞吐量并减少了内存开销。
selection_source: fresh_fetch
motivation: 现有多DNN推理系统因模型变体有限，难以在异构处理器上实现高效匹配，导致服务水平目标（SLO）违规率高。
method: 提出模型缝合技术，通过在不重新训练的情况下重组稀疏模型的子图来创建模型变体，并开发了SparseLoom演示系统。
result: "与现有系统相比，SparseLoom将SLO违规率降低了高达74%，吞吐量提升了2.31倍，内存开销平均降低了28%。"
conclusion: 模型缝合技术能有效提升边缘SoC上多DNN推理的灵活性和效率，是优化异构计算资源利用的有效手段。
---

## 摘要
现代边缘应用日益需要多深度神经网络（multi-DNN）推理系统在异构处理器上执行任务，从而通过并发执行以及将每个模型与最合适的加速器匹配来获得性能提升。然而，现有系统每个任务仅支持单个模型（或少数稀疏变体），这阻碍了这种匹配的效率，并导致较高的服务水平目标（SLO）违规率。我们为多深度神经网络推理系统引入了模型缝合（model stitching）技术，该技术通过在无需重新训练的情况下重组稀疏模型的子图来创建模型变体。我们展示了一个名为 SparseLoom 的演示系统，证明了模型缝合可以部署到片上系统（SoCs）。实验结果表明，与最先进的多深度神经网络推理系统相比，SparseLoom 将 SLO 违规率降低了高达 74%，吞吐量提高了高达 2.31 倍，并将内存开销平均降低了 28%。

## Abstract
Modern edge applications increasingly require multi-DNN inference systems to execute tasks on heterogeneous processors, gaining performance from both concurrent execution and from matching each model to the most suited accelerator. However, existing systems support only a single model (or a few sparse variants) per task, which impedes the efficiency of this matching and results in high Service Level Objective violation rates. We introduce model stitching for multi-DNN inference systems, which creates model variants by recombining subgraphs from sparse models without re-training. We present a demonstrator system, SparseLoom, that shows model stitching can be deployed to SoCs. We show experimentally that SparseLoom reduces SLO violation rates by up to 74%, improves throughput by up to 2.31x, and lowers memory overhead by an average of 28% compared to state-of-the-art multi-DNN inference systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SparseLoom** 的系统，旨在优化边缘片上系统（SoC）上多个深度神经网络（Multi-DNN）的并行推理效率。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代边缘应用（如增强现实 AR）需要同时运行多个 DNN 任务（如图像分类、语音识别等）。现有的多 DNN 推理系统通常只为每个任务提供极少数的模型变体（如仅一个密集模型或几个稀疏模型），这导致系统在面对动态变化的 **服务水平目标（SLO）**（即精度和延迟要求）以及异构硬件（CPU、GPU、NPU）时，难以实现最优的任务匹配，从而导致高 SLO 违规率和低吞吐量。
*   **研究动机**：通过增加模型变体的多样性，可以更精细地平衡精度与延迟。然而，重新训练大量变体成本太高，且在异构处理器上调度这些变体存在巨大的性能评估和内存开销挑战。

### 2. 方法论：模型缝合与 SparseLoom 系统
*   **核心思想——模型缝合（Model Stitching）**：这是一种**无需重新训练**的变体生成技术。它将同一基础模型的不同稀疏版本（如 80% 剪枝、INT8 量化、密集模型）划分为多个层对齐的子图，然后通过重新组合这些子图来创建全新的“缝合变体”。
*   **关键技术模块**：
    *   **性能评估器（Performance Profiler）**：针对缝合变体数量呈指数级增长的问题，利用 **XGBoost 回归器**预测精度，并利用子图延迟累加模型预测端到端延迟，大幅降低离线测试成本。
    *   **稀疏感知优化器（Sparsity-Aware Optimizer）**：联合优化**处理器放置顺序**（如 NPU→GPU→CPU）和**变体选择**。它会遍历所有可能的放置组合，为所有任务选择一个全局最优的执行顺序，以最大化吞吐量。
    *   **热点子图预加载器（Hot-Subgraph Preloader）**：引入“热度（Hotness）”指标（结合子图的使用频率和唯一性），在有限的内存预算内优先加载最关键的子图，减少运行时切换模型的加载延迟。

### 3. 实验设计
*   **数据集与模型**：
    *   图像分类：ImageNet-1K + ResNet101
    *   情感分类：SST-2 + BERT-Base
    *   活动识别：HAR + ViT-Small
    *   语音识别：LibriSpeech + Wav2vec2
*   **硬件平台**：
    *   台式机（Intel Core Ultra 7 265K，含 CPU/GPU/NPU）
    *   笔记本（Intel Core Ultra 5 135U）
    *   嵌入式设备（NVIDIA Jetson AGX Orin）
*   **对比方法（Benchmark）**：构建了 6 种基准线，涵盖了单变体（SV）/多变体（AV）、精度优先（AO）/延迟优先（LO）、是否进行子图划分（P/NP）的各种组合（如 SV-AO-P, AV-NP 等）。

### 4. 资源与算力
*   **推理算力**：使用了 Intel 的 AI Boost NPU、Arc GPU 以及 NVIDIA 的 Ampere GPU。
*   **训练/准备算力**：论文强调该方法是 **Training-free（无需训练）** 的。模型稀疏化使用了 OpenVINO 的 NNCF 工具包和 ONNX Runtime，通常在几分钟内即可完成变体准备。
*   **评估开销**：通过评估器，将原本需要 468 分钟的穷举测试缩短至 5 分钟（在笔记本平台上）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的硬件架构上进行了测试，涵盖了视觉、语言、音频等 4 类典型任务。
*   **SLO 配置**：为每个任务生成了 25 种不同的精度-延迟 SLO 组合，总计进行了数百组并行推理实验。
*   **充分性**：实验不仅对比了端到端性能，还进行了详细的消融实验（评估器准确性、放置顺序影响、内存预算影响），验证了系统在不同资源约束下的鲁棒性，实验设计客观且公平。

### 6. 主要结论与发现
*   **SLO 违规率降低**：相比最先进的自适应变体系统，SparseLoom 将 SLO 违规率降低了高达 **74%**。
*   **吞吐量提升**：通过优化处理器放置顺序和增加变体选择，吞吐量提升了 **1.53x 至 2.31x**。
*   **内存效率**：在仅使用 55% 内存预算的情况下，即可达到接近全量预加载的 SLO 达成率，平均减少 **28%** 的内存开销。
*   **帕累托前沿优化**：模型缝合生成的变体在精度-延迟空间中形成了比原始稀疏模型更好的帕累托前沿。

### 7. 优点
*   **高效性**：无需重新训练即可获得数千个模型变体，极大地扩展了配置空间。
*   **系统性**：不仅提出了算法，还解决了离线评估、在线调度和内存管理的工程痛点。
*   **跨平台**：在 Intel 和 NVIDIA 两大主流边缘 SoC 平台上均表现出显著增益。

### 8. 不足与局限
*   **通信开销**：跨处理器执行子图会带来约 5% 的延迟开销（数据传输和格式转换），虽然在 SoC 上较小，但在非统一内存架构上可能会放大。
*   **模型范围**：目前主要针对边缘侧的中小型 DNN（CNN, Transformer），对于超大规模基础模型（如 LLM）的适用性尚未讨论（LLM 的瓶颈通常在内存带宽而非计算单元匹配）。
*   **动态环境**：虽然考虑了 SLO 变化，但对于硬件频率动态调整（DVFS）的实时响应机制仍有优化空间。

（完）
