---
title: "PrototypeNAS: Rapid Design of Deep Neural Networks for Microcontroller Units"
title_zh: "PrototypeNAS: 面向微控制器的深度神经网络快速设计"
authors: "Mark Deutel, Simon Geis, Axel Plinge"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15106v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于微控制器上高效 DNN 推理的零样本神经架构搜索
tldr: PrototypeNAS 是一种针对微控制器（MCU）的零样本神经架构搜索（NAS）方法，旨在解决传统 NAS 资源消耗大且未考虑硬件约束的问题。它通过解耦设计与训练、结合多种架构类型及剪枝量化配置、利用零样本代理集成和超体积子集选择，在几分钟内即可为不同 MCU 自动定制高效 DNN，在图像、时间序列和目标检测任务中均表现优异。
selection_source: fresh_fetch
motivation: 传统 NAS 方法在为资源受限的 MCU 设计专用 DNN 时，存在训练成本极高且难以兼顾硬件约束的局限性。
method: 提出一种三阶段零样本搜索方法，结合了多架构搜索空间、零样本代理集成以及基于超体积的帕累托前沿蒸馏技术。
result: 在 12 个数据集上的实验表明，该方法能在数分钟内找到可部署于商用 MCU 且准确率媲美大型模型的轻量化架构。
conclusion: PrototypeNAS 实现了高效、自动化的 DNN 跨平台定制，显著降低了边缘设备深度学习应用的开发门槛。
---

## 摘要
在具有不同硬件约束的边缘设备上实现高效的深度神经网络（DNN）推理是一项具有挑战性的任务，通常需要针对每个设备分别进行 DNN 架构的专门化。为了避免巨大的手动工作量，可以使用神经架构搜索（NAS）。然而，许多现有的 NAS 方法资源密集且耗时，因为它们需要从头开始训练许多不同的 DNN。此外，它们没有考虑到目标系统的资源约束。为了解决这些缺点，我们提出了 PrototypeNAS，这是一种零样本（zero-shot）NAS 方法，旨在加速并自动化针对不同目标微控制器（MCU）的 DNN 选择、压缩和专门化。我们提出了一种新颖的三步搜索方法，将给定目标平台的 DNN 设计和专门化与 DNN 训练解耦。首先，我们提出了一个新颖的搜索空间，它不仅从单个大型架构中裁剪出较小的 DNN，还结合了多种架构类型的结构优化，以及它们的剪枝和量化配置优化。其次，我们探索了在优化过程中使用零样本代理集成而非单一代理的方法。第三，我们建议使用超体积子集选择（Hypervolume subset selection）从多目标优化的帕累托前沿（Pareto front）中提取 DNN 架构，这些架构代表了准确率与 FLOPs 之间最有意义的权衡。我们在三个不同任务（图像分类、时间序列分类和目标检测）的 12 个不同数据集上评估了 PrototypeNAS 的有效性。我们的结果表明，PrototypeNAS 能够在几分钟内识别出足够小、可部署在现成 MCU 上的 DNN 模型，且仍能达到与大型 DNN 模型性能相当的准确率。

## Abstract
Enabling efficient deep neural network (DNN) inference on edge devices with different hardware constraints is a challenging task that typically requires DNN architectures to be specialized for each device separately. To avoid the huge manual effort, one can use neural architecture search (NAS). However, many existing NAS methods are resource-intensive and time-consuming because they require the training of many different DNNs from scratch. Furthermore, they do not take the resource constraints of the target system into account. To address these shortcomings, we propose PrototypeNAS, a zero-shot NAS method to accelerate and automate the selection, compression, and specialization of DNNs to different target microcontroller units (MCUs). We propose a novel three-step search method that decouples DNN design and specialization from DNN training for a given target platform. First, we present a novel search space that not only cuts out smaller DNNs from a single large architecture, but instead combines the structural optimization of multiple architecture types, as well as optimization of their pruning and quantization configurations. Second, we explore the use of an ensemble of zero-shot proxies during optimization instead of a single one. Third, we propose the use of Hypervolume subset selection to distill DNN architectures from the Pareto front of the multi-objective optimization that represent the most meaningful tradeoffs between accuracy and FLOPs. We evaluate the effectiveness of PrototypeNAS on 12 different datasets in three different tasks: image classification, time series classification, and object detection. Our results demonstrate that PrototypeNAS is able to identify DNN models within minutes that are small enough to be deployed on off-the-shelf MCUs and still achieve accuracies comparable to the performance of large DNN models.

---

## 论文详细总结（自动生成）

以下是对论文《PrototypeNAS: Rapid Design of Deep Neural Networks for Microcontroller Units》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源极度受限的微控制器（MCU）上，快速且自动化地设计、压缩并部署高效的深度神经网络（DNN）。
*   **研究动机**：
    *   **硬件多样性**：不同 MCU 的内存（RAM/ROM）和算力约束各异，手动为每个平台定制模型耗时耗力。
    *   **传统 NAS 缺陷**：现有的神经架构搜索（NAS）通常需要训练数百个候选模型，计算资源消耗巨大；且许多方法仅针对单一超网络（Supernet）进行裁剪，搜索空间有限。
    *   **零样本代理的局限**：单一的零样本（Zero-shot）代理指标往往存在偏差，与实际准确率的相关性不稳定。

### 2. 论文提出的方法论
PrototypeNAS 采用了一种**三阶段**的零样本搜索框架，将架构设计与最终训练解耦：
1.  **架构原型探索（Architectural Prototype Exploration）**：
    *   **多目标优化（MOO）**：同时优化 FLOPs（最小化）和四个零样本代理指标（MeCo, ZiCo, NASWOT, SNIP，最大化）。
    *   **硬件约束搜索空间**：不仅包含模型宽度和剪枝率，还引入了多种基准架构选择（如 MobileNetV2, ResNet, SqueezeNet 等）以及结构化参数（层深度、卷积核大小、步长）。
    *   **约束处理**：将目标 MCU 的 RAM 和 ROM 限制作为硬性约束，过滤不可部署的架构。
2.  **超体积子集选择（Hypervolume Subset Selection）**：
    *   从优化得到的帕累托前沿（Pareto front）中，利用进化算法筛选出 $k$ 个（通常为 3-5 个）最具代表性的模型。这些模型在准确率与资源消耗之间提供了最有意义的权衡（Trade-offs）。
3.  **数据集评估器（Dataset Evaluator）**：
    *   仅对筛选出的少量原型进行完整的训练、剪枝和量化（PTQ 或 QAT），显著降低了总计算成本。

### 3. 实验设计
*   **任务与数据集**：
    *   **图像分类**：CIFAR10/100, GTSRB, Flowers, Birds, Cars, Pets, ArxPhotos314（共 8 个）。
    *   **时间序列分类**：Daliac, MAFULDA, BitBrain Sleep（共 3 个）。
    *   **目标检测**：COCO 人的检测子集（1 个）。
*   **基准对比（Benchmarks）**：
    *   **TinyNAS (MCUNet)**：针对 MCU 的主流硬件感知 NAS。
    *   **NATS-Bench**：包含数万个预训练模型的 NAS 基准测试集。
*   **目标硬件**：ARM Cortex-M7 (iMXRT1062) 微控制器。

### 4. 资源与算力
*   **搜索耗时**：论文指出搜索过程在**几分钟内**即可完成，因为零样本代理不需要训练模型。
*   **搜索规模**：每项任务执行 500 次试验（Trials）。
*   **训练配置**：最终选出的 5 个候选模型在目标数据集上训练 100 个 epoch。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但强调了该方法相比传统 NAS 极大地减少了训练需求（从训练数百个模型减少到仅 3-5 个）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 类任务、12 个数据集上进行了验证，涵盖了从图像到传感器数据的多种模态。
*   **消融与分析**：
    *   对 4 种零样本代理指标进行了 Kendall’s $\tau$ 相关性分析，证明了集成代理的必要性。
    *   对比了不同基准架构在搜索空间中的表现。
*   **客观性**：通过与 MCUNet 和 NATS-Bench 的直接对比，证明了其在相同资源约束下具有更高的准确率（CIFAR10 上平均高出 5%）。实验设计较为全面且客观。

### 6. 论文的主要结论与发现
*   **高效性**：PrototypeNAS 能够快速找到符合 MCU 严苛内存限制（如数百 KB RAM）的高性能模型。
*   **代理集成优势**：单一零样本代理在不同数据集上表现不稳定，而通过多目标优化集成多个代理可以有效抵消偏差。
*   **搜索空间广度**：结合多种基准架构和剪枝/量化配置的搜索空间，比单一超网络搜索更能发现“知识密度”高的模型。
*   **线性相关性**：实验证实 FLOPs 与 MCU 上的推理延迟和能耗呈强线性相关，是有效的硬件代理指标。

### 7. 优点
*   **极速搜索**：零样本机制避免了昂贵的训练过程，适合快速原型开发。
*   **跨任务通用性**：不仅限于图像分类，还成功扩展到了时间序列和目标检测任务。
*   **硬件感知**：直接将 C 代码转换后的 ROM/RAM 占用作为约束，确保了搜索结果的 100% 可部署性。
*   **决策支持**：通过超体积子集选择，自动为开发者提供最优的几个权衡方案，而非单一解。

### 8. 不足与局限
*   **代理相关性波动**：尽管使用了集成方法，但零样本代理与最终量化准确率之间的相关性（Kendall’s $\tau$）在某些数据集上仍然较低。
*   **依赖基准架构**：搜索空间基于预定义的基准架构（如 MobileNet），而非从最底层的算子（Primitive Ops）开始构建，这可能限制了发现完全创新架构的可能性。
*   **量化策略手动性**：在 PTQ 和 QAT 的选择上仍需一定的先验知识（如文中提到时间序列任务必须用 QAT）。

（完）
