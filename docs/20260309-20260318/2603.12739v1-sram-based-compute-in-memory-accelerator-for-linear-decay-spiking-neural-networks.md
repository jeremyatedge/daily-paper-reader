---
title: SRAM-Based Compute-in-Memory Accelerator for Linear-decay Spiking Neural Networks
title_zh: 基于 SRAM 的线性衰减脉冲神经网络存内计算加速器
authors: "Hongyang Shang, Shuai Dong, Yahan Yang, Junyi Yang, Peng Zhou, Arindam Basu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12739v1"
tags: ["query:aiot"]
score: 9.0
evidence: 基于SRAM的脉冲神经网络存内计算加速器
tldr: 针对脉冲神经网络（SNN）中神经元状态更新导致的串行计算瓶颈，本文提出了一种基于SRAM的存内计算（CIM）加速器。通过将复杂的指数衰减简化为线性衰减，并结合存内并行更新架构，实现了神经元状态的就地更新。该方案在保持高精度的同时，显著降低了能耗并提升了处理效率，为低功耗实时神经形态处理提供了新思路。
selection_source: fresh_fetch
motivation: 传统SNN加速器中神经元状态更新的串行处理（O(N)复杂度）已成为制约系统吞吐量和能效的主要瓶颈。
method: 算法上采用线性衰减LIF模型替代指数衰减以简化计算，硬件上设计了支持存内并行更新的SRAM架构。
result: "实验显示该方法在仅损失约1%准确率的情况下，SOP能耗降低了1.1至16.7倍，能效提升了15.9至69倍。"
conclusion: 优化存内计算架构中的状态更新动力学是实现可扩展、低功耗和实时神经形态处理的关键。
---

## 摘要
脉冲神经网络 (SNN) 作为一种受生物启发的传统深度网络替代方案，提供了事件驱动和高能效的计算。然而，其吞吐量仍受限于神经元膜电位状态的串行更新。虽然许多硬件加速器和存内计算 (CIM) 架构能够高效地并行化突触操作 (W x I)，使矩阵向量乘法达到 O(1) 复杂度，但随后的状态更新步骤仍需 O(N) 时间来刷新所有神经元的膜电位。这种不匹配使得状态更新成为 SNN 推理中主要的延迟和功耗瓶颈。为应对这一挑战，我们提出了一种针对线性衰减漏电积分发放 (LD-LIF) 神经元 SNN 的基于 SRAM 的存内计算架构，实现了算法与硬件的协同优化。在算法层面，我们用线性衰减近似取代了传统的指数膜电位衰减，将昂贵的乘法运算转化为简单的加法，而准确率仅下降约 1%。在架构层面，我们引入了一种存内并行更新方案，直接在 SRAM 阵列内执行原位衰减，消除了对全局顺序更新的需求。在基准 SNN 工作负载上的评估结果显示，所提方法将 SOP 功耗降低了 1.1 倍至 16.7 倍，同时能效提升了 15.9 倍至 69 倍，且相对于原始衰减模型，准确率损失可忽略不计。本研究强调，除了加速 (W x I) 计算外，优化存内计算架构中的状态更新动力学对于实现可扩展、低功耗和实时的类脑处理至关重要。

## Abstract
Spiking Neural Networks (SNNs) have emerged as a biologically inspired alternative to conventional deep networks, offering event-driven and energy-efficient computation. However, their throughput remains constrained by the serial update of neuron membrane states. While many hardware accelerators and Compute-in-Memory (CIM) architectures efficiently parallelize the synaptic operation (W x I) achieving O(1) complexity for matrix-vector multiplication, the subsequent state update step still requires O(N) time to refresh all neuron membrane potentials. This mismatch makes state update the dominant latency and energy bottleneck in SNN inference. To address this challenge, we propose an SRAM-based CIM for SNN with Linear Decay Leaky Integrate-and-Fire (LD-LIF) Neuron that co-optimizes algorithm and hardware. At the algorithmic level, we replace the conventional exponential membrane decay with a linear decay approximation, converting costly multiplications into simple additions while accuracy drops only around 1%. At the architectural level, we introduce an in-memory parallel update scheme that performs in-place decay directly within the SRAM array, eliminating the need for global sequential updates. Evaluated on benchmark SNN workloads, the proposed method achieves a 1.1 x to 16.7 x reduction of SOP energy consumption, while providing 15.9 x to 69 x more energy efficiency, with negligible accuracy loss relative to original decay models. This work highlights that beyond accelerating the (W x I) computation, optimizing state-update dynamics within CIM architectures is essential for scalable, low-power, and real-time neuromorphic processing.

---

## 论文详细总结（自动生成）

### 基于 SRAM 的线性衰减脉冲神经网络存内计算加速器论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）虽然具有生物启发的高能效特性，但在硬件实现中存在**计算不匹配**的问题。虽然存内计算（CIM）可以将突触权重乘累加（W×I）操作并行化至 $O(1)$ 复杂度，但神经元膜电位（Membrane Potential）的状态更新通常仍需串行处理，导致 $O(N)$ 的延迟。
*   **研究动机**：传统的漏电积分发放（LIF）模型采用指数衰减，涉及复杂的乘法或查找表操作，这成为了 SNN 推理中的能效和延迟瓶颈。本文旨在通过算法与硬件的协同设计，消除这一瓶颈，实现全并行的状态更新。

#### 2. 方法论
*   **算法优化（LD-LIF 模型）**：
    *   将传统的**指数衰减**（Exponential Decay）简化为**线性衰减**（Linear Decay）。
    *   公式表达：$V_{mem}[t+1] = V_{mem}[t] - \beta + W \cdot Input[t]$。其中 $\beta$ 是一个**可学习的衰减参数**，通过梯度下降在训练过程中针对不同层进行优化。
    *   这种简化将昂贵的乘法操作转化为简单的加减法，便于硬件实现。
*   **硬件架构设计**：
    *   **存内并行更新**：设计了一种基于 SRAM 的 CIM 宏，将衰减和更新操作直接集成在存储阵列中。
    *   **8T SRAM 单元**：使用两个 8T SRAM 块（VMEM A 和 VMEM B）存储 10 位膜电位，利用其独立的读写路径实现高效数据流。
    *   **乒乓更新机制**：通过三个时钟周期的“乒乓”操作完成一次更新：
        1.  计算 $V_{init} + MAC$ 并存入临时块。
        2.  减去衰减值和阈值（$-(DCY + TH)$），利用符号位判断是否发放脉冲（Spike）。
        3.  根据判断结果重置电位或写回最终值。
    *   **硬件加速器组成**：包含 MAC 模块（32个 SRAM 块）、缩放器（Scaler）和 LD-LIF 神经元模块。

#### 3. 实验设计
*   **数据集与场景**：
    *   **N-MNIST**：使用 MLP-1 网络（2312-256-128-10）。
    *   **SHD (Spiking Heidelberg Digits)**：使用 MLP-2 网络（140-128-128-128-10）。
    *   **DVS Gesture**：使用 CNN 架构（卷积层+线性层）。
*   **Benchmark 与对比方法**：
    *   **算法对比**：以传统的 Vanilla LIF (v-LIF) 指数衰减模型为基准。
    *   **硬件对比**：对比了近年来发表在 JSSC'24、VLSI'23 和 TCASAI'25 上的先进 SNN 处理器。
    *   **量化实验**：研究了权重和激活值量化（如 W4A6）对准确率的影响。

#### 4. 资源与算力
*   **硬件仿真**：使用 **TSMC 65nm CMOS 工艺**进行电路仿真。
*   **运行参数**：工作电压 0.9V（MAC 模块 0.72V，Scaler 1.2V），时钟频率 142MHz。
*   **训练资源**：文中**未明确说明**具体使用的 GPU 型号或训练时长，但提到使用了梯度下降法来学习 $\beta$ 参数。

#### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了三种不同类型的 SNN 任务（静态图像转脉冲、语音脉冲、手势识别），涉及 MLP 和 CNN 两种架构。
*   **充分性评价**：实验设计较为充分，不仅验证了算法的准确率损失（约 1%），还通过硬件仿真给出了详细的功耗、延迟和面积效率数据。对比实验涵盖了数字、混合信号等多种主流技术路径，具有较强的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：与传统数字实现的 v-LIF 相比，该架构在 0.9V 下完成 32 个神经元并行更新仅需 21ns，延迟降低了约 30 倍。
*   **能效表现**：实现了 **20.7 TOPS/W** 的超高能效，SOP（突触操作）能耗仅为 **0.09 pJ/SOP**。
*   **准确率保持**：线性衰减模型在三个数据集上的准确率损失极小（0.38% - 1.11%），证明了线性近似在 SNN 中的可行性。
*   **学习特性**：发现不同层学习到的 $\beta$ 值差异显著，甚至在某些层出现负值（增加膜电位），这表明线性衰减能有效捕捉数据的时序特征。

#### 7. 优点
*   **协同优化**：成功将算法层面的简化（线性化）转化为硬件层面的并行优势。
*   **原位计算**：消除了膜电位数据在存储和计算单元之间的频繁搬运，极大地降低了功耗。
*   **高兼容性**：基于成熟的 SRAM 工艺，比 RRAM 或 MRAM 等新兴非易失性存储器更具可靠性和 CMOS 兼容性。

#### 8. 不足与局限
*   **硬件开销**：为了实现并行更新，每个膜电位单元使用了复杂的 8T SRAM 和额外的加法器逻辑，相比传统 6T SRAM 阵列，其面积开销（Area Overhead）会显著增加。
*   **量化限制**：虽然讨论了量化，但 10 位膜电位的存储在超大规模神经元集成时可能会面临存储容量的挑战。
*   **应用范围**：目前主要在较小规模的网络（MLP/小型 CNN）上验证，在超深层残差网络或 Transformer 等复杂架构上的表现尚待观察。

（完）
