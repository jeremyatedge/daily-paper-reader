---
title: "HyperCroc: End-to-End Open-Source RISC-V MCU with a Plug-In Interface for Domain-Specific Accelerators"
title_zh: HyperCroc：具有领域专用加速器插件接口的端到端开源 RISC-V MCU
authors: "Philippe Sauter, Thomas Benz, Paul Scheffler, Luca Benini"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12308v1"
tags: ["query:aiot"]
score: 9.0
evidence: 带有领域专用加速器插件接口的 RISC-V 微控制器
tldr: HyperCroc是一个基于RISC-V的开源端到端微控制器（MCU）平台，旨在解决开源MCU在处理机器学习和信号处理任务时面临的大规模数据传输和高带宽访问瓶颈。它通过集成HyperBus控制器和DMA引擎，实现了高达400 MB/s的片外存储访问带宽，并提供了灵活的插件接口以支持领域特定加速器。该项目基于IHP 130nm开源工艺，支持快速物理实现，并通过首个芯片流片验证了其在72MHz频率下的功能可靠性。
selection_source: fresh_fetch
motivation: 现有的开源微控制器通常缺乏高效的大规模数据移动能力和高带宽存储访问，难以满足机器学习等领域特定加速器的需求。
method: 在开源Croc SoC基础上集成了HyperBus控制器和DMA引擎，并设计了标准化的插件接口以支持领域特定加速器的无缝集成。
result: 实现了高达400 MB/s的片外存储带宽，且全芯片物理实现流程在普通工作站上仅需不到一小时，首颗流片芯片在1.2V下达到72MHz。
conclusion: HyperCroc为领域特定加速提供了一个实用、高效且完全开源的硬件平台，验证了从设计到流片的完整开源工具链的可行性。
---

## 摘要
针对机器学习和信号处理的领域专用架构需要高效的大容量数据移动和对大型数据集的高带宽访问。极简的开源微控制器（MCU）通常缺乏此类功能。我们提出了 HyperCroc，它是对端到端开源 RISC-V Croc 片上系统（SoC）的扩展，集成了经过硅验证的 HyperBus 控制器（用于片外 DRAM 和 Flash 存储器访问）以及 DMA 引擎，提供了一个实用的 MCU 级平台，并为领域专用加速提供了流线型的插件支持。HyperBus 提供了一个低引脚数的 PSDRAM 接口，速率高达 400 MB/s，实现了带宽扩展的数据集访问，而 DMA 引擎则可以在无需 CPU 干预的情况下实现自主、高吞吐量的数据传输。HyperCroc 保留了 Croc 针对 IHP 开源 130 nm 工艺设计套件（PDK）的开源综合和物理实现流程；整个芯片可以在消费级工作站上于一小时内完成实现。我们进一步报告了来自 MLEM（首个 Croc 流片）的初步硅片测量结果，确认该芯片在 72 MHz @ 1.2 V 下功能完全正常，并验证了端到端流程。

## Abstract
Domain-Specific architectures with accelerators for machine learning and signal processing require efficient bulk data movement and high-bandwidth access to large datasets. Such capabilities are often absent from minimal open-source microcontrollers (MCUs). We present HyperCroc, an extension to the end-to-end open-source RISC-V Croc system-on-chip (SoC) integrating a silicon-proven HyperBus controller for off-chip DRAM and Flash memory access and a DMA engine, providing a practical MCU-class platform with streamlined plug-in support for domain-specific acceleration. HyperBus offers a low-pin-count PSDRAM interface at up to 400 MB/s, enabling bandwidth-scaled dataset access, while the DMA engine enables autonomous, high-throughput transfers without CPU intervention. HyperCroc preserves Croc's open-source synthesis and physical implementation flow targeting IHP's open 130 nm process design kit (PDK); the full chip can be implemented in under one hour on a consumer-grade workstation. We further report first silicon measurements from MLEM, the first Croc tapeout, confirming that the silicon is fully functional at 72 MHz @ 1.2 V and validating the end-to-end flow.

---

## 论文详细总结（自动生成）

以下是对论文《HyperCroc: End-to-End Open-Source RISC-V MCU with a Plug-In Interface for Domain-Specific Accelerators》的结构化总结：

### 1. 论文的核心问题与整体含义
论文针对当前开源硬件领域的一个关键瓶颈：**极简开源微控制器（MCU）缺乏高效的大规模数据移动能力和高带宽存储访问接口**。这使得它们难以支持机器学习（ML）和信号处理等需要处理大型数据集的领域专用加速器（DSA）。
**HyperCroc** 的提出旨在通过集成高性能存储接口和 DMA 引擎，扩展现有的开源 RISC-V SoC（Croc），为研究人员和开发者提供一个从 RTL 到 GDS（芯片物理掩模）全流程开源、且具备实用带宽能力的 MCU 级开发平台。

### 2. 论文提出的方法论
HyperCroc 在原始 Croc SoC 的基础上进行了关键架构增强：
*   **核心架构**：采用 3 级流水线的 Ibex RISC-V 内核（RV32IMCB），通过 32 位 OBI（Open Bus Interface）总线连接。
*   **HyperBus 控制器**：集成了经过硅验证的 HyperBus 接口，支持连接片外 PSDRAM（高达 256 MiB）和 Flash（高达 2 GiB）。其 PHY 运行在独立时钟域（最高 200 MHz），单 PHY 带宽达 400 MB/s，双 PHY 配置可达 800 MB/s。
*   **iDMA 引擎**：引入自主数据搬运引擎，支持在片外存储、片上 SRAM 和加速器之间进行高吞吐量的突发传输，无需 CPU 干预。
*   **插件式接口（User Domain）**：设计了一个专门的“用户域”接口，允许领域专用加速器以极低的工作量集成到 SoC 主总线上。
*   **开源工具链**：针对 IHP 130 nm 开源 PDK，提供完整的 RTL-to-GDS 物理实现流程。

### 3. 实验设计
*   **验证对象**：
    1.  **MLEM 芯片**：Croc 的首个流片版本，用于验证基础架构和开源流程的成熟度。
    2.  **HyperCroc 设计**：作为 Croc 的增强版，通过仿真和设计指标进行评估。
*   **Benchmark 与对比**：
    *   对比了 **Croc（实测硅片数据）** 与 **HyperCroc（设计目标）** 的关键指标。
    *   主要对比参数包括：时钟频率、内外存储容量、外部带宽。
*   **测试场景**：通过软件驱动的测试验证了所有外设、内核和 SRAM 的功能完整性。

### 4. 资源与算力
*   **物理实现效率**：论文强调该平台的物理实现流程非常高效，在**消费级工作站**上，全芯片的 RTL-to-GDS 流程可在 **1 小时内**完成。
*   **算力细节**：文中未提及具体的 GPU 或高性能计算集群需求，重点在于其对低算力环境（普通工作站）的友好性。

### 5. 实验数量与充分性
*   **实验规模**：报告了 MLEM 芯片的实测数据（72 MHz @ 1.2 V），验证了端到端流程的闭环。
*   **充分性评价**：作为一篇短论文（Extended Abstract），其实验侧重于“功能验证”和“流程可行性”。虽然缺乏大规模应用负载（如复杂 AI 模型）的对比实验，但通过首颗硅片的实测结果，有力地证明了该开源平台在 130nm 工艺下的可靠性。

### 6. 论文的主要结论与发现
*   **硅片验证成功**：首个基于 Croc 的流片 MLEM 在 1.2V 下达到 72 MHz，证明了开源 EDA 工具链和 IHP 130nm PDK 的实用性。
*   **带宽突破**：HyperCroc 通过 HyperBus 实现了高达 800 MB/s 的外部带宽，解决了 MCU 级 SoC 访问大型数据集的瓶颈。
*   **降低门槛**：该平台通过提供文档、教学材料和快速实现流程，显著降低了领域专用芯片设计的准入门槛。

### 7. 优点
*   **全栈开源**：从处理器内核、总线架构到物理实现流程完全开源，不依赖商业 EDA 工具。
*   **高性能存储支持**：在 130nm 这种成熟工艺上实现了极高的存储带宽（400-800 MB/s），对嵌入式 AI 研究极具价值。
*   **易用性**：插件式接口设计简化了加速器的集成过程，且物理实现时间极短。

### 8. 不足与局限
*   **工艺限制**：采用 130nm 工艺虽然开源友好，但在能效比和逻辑密度上远落后于先进工艺，限制了其在高性能计算场景的应用。
*   **HyperCroc 尚未实测**：论文中的 HyperCroc 数据多为设计目标和仿真结果，其最终硅片表现需等待 2026 年流片后的验证。
*   **软件生态**：论文重点在于硬件架构和流程，对于配套的软件编译器、库支持以及如何高效编程利用 iDMA 提及较少。

（完）
