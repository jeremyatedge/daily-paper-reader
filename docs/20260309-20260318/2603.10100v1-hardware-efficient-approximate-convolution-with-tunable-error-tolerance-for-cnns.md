---
title: Hardware Efficient Approximate Convolution with Tunable Error Tolerance for CNNs
title_zh: 针对卷积神经网络的具有可调容错性的硬件高效近似卷积
authors: "Vishal Shashidhar, Anupam Kumari, Roy P Paily"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10100v1"
tags: ["query:aiot"]
score: 10.0
evidence: 边缘CNN的硬件高效近似卷积与功耗节省
tldr: 针对卷积神经网络在边缘设备部署时计算开销大的问题，本文提出一种“软稀疏”范式。该方法利用硬件高效的最高有效位（MSB）代理来跳过对结果影响较小的非零乘法，并将其集成到自定义RISC-V指令中。实验表明，在LeNet-5模型上，该技术在保持零精度损失的同时，显著减少了ReLU和Tanh激活下的乘累加操作，性能优于传统零跳过技术5倍，有效降低了推理功耗。
selection_source: fresh_fetch
motivation: 传统的硬稀疏技术在处理深层网络或平滑激活函数时效果不佳，难以满足边缘设备对高效推理的需求。
method: 提出一种基于最高有效位（MSB）代理的软稀疏方法，通过跳过可忽略的非零乘法运算来降低计算复杂度。
result: "在LeNet-5上实现ReLU和Tanh操作分别减少88.42%和74.87%，且无精度损失，功耗降低约30%至35%。"
conclusion: 该近似卷积方法通过硬件层面的优化显著提升了资源受限环境下的CNN推理效率，是边缘计算的有效解决方案。
---

## 摘要
现代卷积神经网络（CNN）的高计算需求阻碍了其在边缘端的部署，因为传统的“硬”稀疏性（跳过数学上的零值）在深层网络或使用 Tanh 等平滑激活函数时会失效。我们提出了一种“软稀疏”范式，利用硬件高效的最高有效位（MSB）代理来跳过可忽略的非零乘法。该方法作为自定义 RISC-V 指令集成，并在 LeNet-5 (MNIST) 上进行了评估；在零精度损失的情况下，该方法将 ReLU 的 MACs 减少了 88.42%，将 Tanh 的 MACs 减少了 74.87%，性能优于零跳过（zero-skipping）5 倍。通过对非活动乘法器进行时钟门控，我们估计 ReLU 的功耗可降低 35.2%，Tanh 的功耗可降低 29.96%。尽管内存访问使得功耗降低与操作节省之间呈亚线性关系，但该方法显著优化了资源受限下的推理。

## Abstract
Modern CNNs' high computational demands hinder edge deployment, as traditional ``hard'' sparsity (skipping mathematical zeros) loses effectiveness in deep layers or with smooth activations like Tanh. We propose a ``soft sparsity'' paradigm using a hardware efficient Most Significant Bit (MSB) proxy to skip negligible non-zero multiplications. Integrated as a custom RISC-V instruction and evaluated on LeNet-5 (MNIST), this method reduces ReLU MACs by 88.42% and Tanh MACs by 74.87% with zero accuracy loss--outperforming zero-skipping by 5x. By clock-gating inactive multipliers, we estimate power savings of 35.2\% for ReLU and 29.96\% for Tanh. While memory access makes power reduction sub-linear to operation savings, this approach significantly optimizes resource-constrained inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hardware Efficient Approximate Convolution with Tunable Error Tolerance for CNNs》的结构化深度总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现代卷积神经网络（CNN）计算量巨大且功耗高，难以在资源受限的边缘设备上部署。
*   **研究动机**：
    *   **传统方法的局限性**：现有的硬件加速技术主要利用“硬稀疏性”（即跳过数学上的零值）。然而，在深层网络中，ReLU 产生的零值比例会显著下降（仅剩 20-50%）；若使用 Tanh 等平滑激活函数，则几乎没有零值，导致传统跳过技术失效。
    *   **软稀疏性的需求**：论文提出一种“软稀疏”范式，即即使操作数非零，如果其对最终输出的贡献微乎其微，也可以有选择地省略这些乘法运算。

### 2. 论文提出的方法论
*   **核心思想**：利用**最高有效位（MSB）**作为对数幅度的低成本硬件代理，在不执行实际乘法的情况下，评估乘积的相对大小，并根据可调阈值跳过不重要的计算。
*   **关键技术细节**：
    1.  **MSB 代理对数**：对于正整数 $x$，其 MSB 的位置 $k$ 近似于 $\log_2(x)$。
    2.  **乘积幅度估算**：两个数 $a$ 和 $b$ 乘积的幅度可以通过其 MSB 位置之和来估算：$M(a \cdot b) \approx MSB(a) + MSB(b)$。
    3.  **动态剪枝策略**：在卷积窗口内，首先找到所有乘积项中最大的 MSB 和（$MSB_{max}$）。对于任一乘积项 $P_i$，如果 $(MSB_{max} - MSB_i) \ge T$（$T$ 为可调阈值），则认为该项对总和贡献极小，直接跳过该乘法。
    4.  **硬件集成**：将该算法实现为自定义指令 `conv_approx()`，集成到 32 位 RISC-V（RI5CY 核心）处理器的执行阶段，通过 5 阶段有限状态机（FSM）控制。

### 3. 实验设计
*   **数据集**：MNIST 手写数字识别数据集。
*   **模型架构**：LeNet-5。
*   **Benchmark（基准）**：
    *   精确卷积（Exact Convolution）。
    *   传统的硬零跳过（Hard-zero skipping）范式。
*   **对比维度**：乘累加（MAC）操作减少量、推理准确率、功耗降低比例、硬件面积与延迟。
*   **场景变量**：分别测试了 ReLU 和 Tanh 两种激活函数下的表现。

### 4. 资源与算力
*   **硬件实现**：使用 **65nm CMOS 工艺**进行综合分析。
*   **处理器核心**：基于 PULPino 平台的 RI5CY 核心（4 阶段流水线）。
*   **算力说明**：文中未明确提及模型训练所使用的具体 GPU 型号或训练时长，研究重点在于**推理阶段**的硬件优化和自定义指令集的周期/功耗表现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 MNIST 的 10,000 张测试图像进行了推理实验。
    *   针对不同的误差阈值 $T$（如 0.03, 0.06, 0.1, 0.25 等）进行了多组消融实验。
    *   对比了 ReLU 和 Tanh 两种不同稀疏特性的激活函数。
*   **充分性评价**：实验在 LeNet-5 这一经典模型上较为充分，涵盖了准确率、计算量和硬件开销的权衡分析。但由于仅使用了 MNIST 数据集，对于更深层、更复杂的现代网络（如 ResNet）和大规模数据集（如 ImageNet）的泛化能力尚待验证。

### 6. 论文的主要结论与发现
*   **计算量大幅减少**：在零精度损失的前提下，ReLU 模型减少了 **88.42%** 的 MAC 操作；Tanh 模型减少了 **74.87%** 的乘法操作。
*   **优于传统方法**：相比传统的硬零跳过技术，该方法在 Tanh 激活函数下的乘法减少量提升了 **5 倍**。
*   **功耗优化**：通过时钟门控（Clock Gating）闲置乘法器，估计 ReLU 和 Tanh 模型的推理功耗分别降低了 **35.2%** 和 **29.96%**。
*   **硬件开销**：自定义硬件块在 65nm 工艺下增加了约 108% 的面积（相对于原始 RI5CY 核心），但没有增加关键路径延迟。

### 7. 优点
*   **突破硬稀疏限制**：成功解决了平滑激活函数（如 Tanh）缺乏零值而无法优化的问题。
*   **硬件效率极高**：利用 MSB 位运算代替复杂的对数或浮点运算，决策成本极低。
*   **灵活性强**：提供可调阈值 $T$，允许开发者根据应用场景在“计算精度”和“能效”之间进行动态权衡。
*   **指令集集成**：通过自定义 RISC-V 指令实现，易于与现有嵌入式软件栈集成。

### 8. 不足与局限
*   **数据集单一**：仅在 MNIST/LeNet-5 上验证，该任务本身容错性较高，在更敏感的任务（如目标检测或医疗影像）中表现未知。
*   **功耗收益递减**：由于内存访问是功耗的主要贡献者（占 50% 以上），乘法操作的减少与总功耗降低之间呈亚线性关系，优化存在瓶颈。
*   **硬件面积翻倍**：虽然逻辑简单，但自定义加速块使处理器核心面积增加了一倍以上，这在极度空间受限的芯片设计中需权衡。
*   **误差累积风险**：在极深的网络中，多层近似卷积可能导致误差逐层放大，论文未深入探讨长链条下的误差传播。

（完）
