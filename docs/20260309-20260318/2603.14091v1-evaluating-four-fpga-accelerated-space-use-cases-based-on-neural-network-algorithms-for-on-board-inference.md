---
title: Evaluating Four FPGA-accelerated Space Use Cases based on Neural Network Algorithms for On-board Inference
title_zh: 评估四个基于神经网络算法的星载推理FPGA加速空间应用案例
authors: "Pedro Antunes, Muhammad Ihsan Al Hafiz, Jonah Ekelund, Ekaterina Dineva, George Miloshevich, Panagiotis Gonidakis, Artur Podobas"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14091v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于星载推理的FPGA加速神经网络推理
tldr: 本研究针对空间任务中传感器数据量过大的挑战，在AMD ZCU104 FPGA平台上评估了四种神经网络应用。通过对比Vitis AI和Vitis HLS两种实现方式，展示了FPGA在提升推理速度（最高34.16倍）和降低能耗方面的显著优势，证明了星载FPGA加速神经网络在缓解下行链路压力方面的巨大潜力。
selection_source: fresh_fetch
motivation: 空间任务中高保真传感器产生的数据量巨大，超出了星载存储和下行链路的传输能力。
method: 在AMD ZCU104平台上，利用Vitis AI和Vitis HLS工具链对四种空间应用场景的神经网络推理性能进行量化评估。
result: Vitis AI实现的推理速率比ARM CPU高出34.16倍，而HLS设计在实现加速的同时支持了DPU不支持的自定义算子。
conclusion: FPGA加速神经网络可实现高效的星载数据处理，显著降低能耗并缓解未来空间任务的通信链路压力。
---

## 摘要
太空任务越来越多地部署高保真传感器，其产生的数据量超过了星载缓冲和下行链路容量。本研究在AMD ZCU104开发板上评估了针对四个空间应用案例的神经网络（NN）FPGA加速。我们利用Vitis AI (AMD DPU) 和 Vitis HLS 实现推理，量化吞吐量和能耗，并揭示了与部署相关的工具链和架构约束。Vitis AI 的推理速率比嵌入式 ARM CPU 基准高出多达 34.16 倍，而定制的 HLS 设计实现了高达 5.4 倍的加速，并增加了对 DPU 中缺失算子（如 sigmoid、3D 层）的支持。对于这些实现，测得的 MPSoC 推理功耗在 1.5-6.75 W 之间，在所有应用案例中均降低了单次推理能耗（相较于 CPU 执行）。这些结果表明，神经网络 FPGA 加速能够实现星载过滤、压缩和事件检测，从而缓解未来任务中的下行链路压力。

## Abstract
Space missions increasingly deploy high-fidelity sensors that produce data volumes exceeding onboard buffering and downlink capacity. This work evaluates FPGA acceleration of neural networks (NNs) across four space use cases on the AMD ZCU104 board. We use Vitis AI (AMD DPU) and Vitis HLS to implement inference, quantify throughput and energy, and expose toolchain and architectural constraints relevant to deployment. Vitis AI achieves up to 34.16$\times$ higher inference rate than the embedded ARM CPU baseline, while custom HLS designs reach up to 5.4$\times$ speedup and add support for operators (e.g., sigmoids, 3D layers) absent in the DPU. For these implementations, measured MPSoC inference power spans 1.5-6.75 W, reducing energy per inference versus CPU execution in all use cases. These results show that NN FPGA acceleration can enable onboard filtering, compression, and event detection, easing downlink pressure in future missions.

---

## 论文详细总结（自动生成）

以下是对论文《Evaluating Four FPGA-accelerated Space Use Cases based on Neural Network Algorithms for On-board Inference》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代空间探测任务中的高保真传感器产生的数据量巨大，远超星载存储和下行链路的传输能力。
*   **研究动机**：
    *   **带宽瓶颈**：深空探测和近地观测任务急需在星载端进行实时数据处理（如过滤、压缩、事件检测），以仅传输“提炼后”的信息。
    *   **硬件局限**：传统的抗辐射通用处理器（CPU）在处理计算密集型的神经网络（NN）时，性能和能效比均无法满足严苛的空间环境要求。
    *   **解决方案**：利用FPGA的可重构逻辑和并行处理能力，通过专用加速器实现高效的星载AI推理。

### 2. 论文提出的方法论
论文采用了两种互补的FPGA实现路径，针对四种不同的空间应用模型进行加速：
*   **Vitis AI (基于DPU)**：
    *   **核心思想**：使用AMD/Xilinx提供的深度学习处理器单元（DPU）IP核。
    *   **技术细节**：通过Vitis AI工具链进行模型量化（INT8）、编译和部署。主要针对标准卷积神经网络（CNN），具有极高的计算效率。
*   **Vitis HLS (高层次综合)**：
    *   **核心思想**：将C/C++代码直接转换为RTL硬件描述语言。
    *   **技术细节**：用于支持DPU不支持的特殊算子（如3D卷积、3D池化、Sigmoid激活函数等）。该路径保留了IEEE-754单精度浮点（FP32）运算，保证了数值精度。
*   **应用案例**：
    1.  **VAE Encoder**：用于太阳矢量磁图数据的压缩（压缩比达1:16,384）。
    2.  **CNetPlusScalar**：结合多模态太阳图像预测X射线通量。
    3.  **ESPERTA**：用于早期太阳高能粒子（SEP）事件预测。
    4.  **MMS Neural Networks**：处理3D离子能量分布数据，分类地球昼侧等离子体区域。

### 3. 实验设计
*   **硬件平台**：AMD ZCU104 开发板（搭载 ZU7EV MPSoC，包含四核 ARM Cortex-A53 CPU 和 FPGA 逻辑）。
*   **Benchmark（基准）**：嵌入式 ARM Cortex-A53 CPU 上的纯软件执行性能。
*   **对比维度**：
    *   **吞吐量**：每秒帧数（FPS）和每秒操作数（MOP/s）。
    *   **延迟**：单次推理所需时间。
    *   **功耗与能效**：分别测量整板功耗和MPSoC核心功耗，计算单次推理能耗（Energy per Inference）。
    *   **资源占用**：LUT、FF、DSP、BRAM、URAM的利用率。

### 4. 资源与算力
*   **推理硬件**：使用 AMD ZCU104 FPGA。
    *   **DPU配置**：B4096 架构，运行频率为 75/100/300/600 MHz。
    *   **HLS配置**：运行频率统一为 100 MHz。
*   **训练算力**：文中未明确说明模型训练所使用的具体GPU型号或训练时长，研究重点在于**推理阶段**的硬件加速评估。

### 5. 实验数量与充分性
*   **实验覆盖**：针对4种完全不同的空间科学模型（涵盖1D标量、2D图像、3D张量输入）进行了详尽的测试。
*   **数据量**：在测量功耗和性能时，使用了1,000到1,000,000个输入样本进行平均化处理，以确保结果的稳定性。
*   **充分性评价**：实验设计较为充分，不仅对比了CPU与FPGA，还深入探讨了DPU与HLS两种工具链的优劣，并提供了详细的动态功耗随时间变化的曲线，具有较高的客观性和参考价值。

### 6. 论文的主要结论与发现
*   **性能飞跃**：Vitis AI (DPU) 表现最强，在 CNetPlusScalar 模型上实现了 **34.16倍** 的加速；VAE Encoder 实现了 **24.06倍** 加速。
*   **灵活性优势**：HLS 成功实现了 DPU 无法处理的 3D 卷积和特殊激活函数，虽然在深度网络（如 BaselineNet）中因缺乏并行优化导致速度慢于 CPU，但在轻量级模型（如 ESPERTA）中仍有 **5.33倍** 的加速。
*   **能效显著**：所有 FPGA 实现均降低了单次推理能耗。MPSoC 推理功耗仅为 **1.5W - 6.75W**，远低于空间平台的功耗上限。
*   **精度权衡**：DPU 的 INT8 量化会带来一定的精度损失，而 HLS 的 FP32 实现与 CPU 结果几乎完全一致（误差 $\le 10^{-10}$）。

### 7. 优点
*   **实战导向**：选取的四个案例均来自真实的在研空间任务（如 SDO 卫星、MMS 任务），具有极强的工程应用背景。
*   **工具链对比**：清晰地界定了 Vitis AI 和 HLS 的适用边界，为未来空间任务的硬件选型提供了决策依据。
*   **全方位评估**：不仅关注速度，还深入分析了星载任务最关心的功耗、能效以及 FPGA 编程（Bitstream 加载）过程中的瞬态功耗。

### 8. 不足与局限
*   **HLS 优化不足**：论文中的 HLS 实现采用了较为“原生”的翻译方式，未充分利用 HLS 的流水线（Pipeline）和展开（Unroll）指令进行深度并行优化，导致在复杂模型上性能不佳。
*   **量化影响分析较浅**：虽然提到 DPU 的 INT8 量化导致精度下降，但未给出具体的精度损失数值或量化感知训练（QAT）后的恢复效果。
*   **环境模拟缺失**：实验在实验室开发板上完成，未考虑空间环境特有的单粒子翻转（SEU）等辐射效应对 FPGA 逻辑可靠性的影响。

（完）
