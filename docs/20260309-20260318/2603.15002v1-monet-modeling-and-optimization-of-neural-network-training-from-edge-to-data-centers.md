---
title: "MONET: Modeling and Optimization of neural NEtwork Training from Edge to Data Centers"
title_zh: MONET：从边缘到数据中心的神经网络训练建模与优化
authors: "Jérémy Morlier, Robin Geens, Stef Cuyckens, Arne Symons, Marian Verhelst, Vincent Gripon, Mathieu Léonardon"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15002v1"
tags: ["query:aiot"]
score: 9.0
evidence: 针对数据流加速器上神经网络训练的软硬件协同设计
tldr: 针对现有工具在神经网络训练建模方面的不足，本文提出MONET框架。该框架扩展了Stream推理建模工具，专门针对训练过程中的反向传播复杂性和内存占用进行建模。通过在ResNet-18和GPT-2上的实验，MONET展示了其在异构加速器上优化层融合和激活检查点策略的能力。研究证明了软硬件协同设计在提升训练效率和寻找最优架构方面的重要性。
selection_source: fresh_fetch
motivation: 现有的神经网络建模工具主要针对推理阶段，无法有效处理训练过程中特有的反向传播复杂性和巨大的内存需求。
method: 提出了MONET框架，在Stream的基础上引入了训练工作负载建模，并利用遗传算法优化层融合与激活检查点。
result: 通过对ResNet-18和GPT-2的分析，MONET成功识别了更优的硬件架构配置，并揭示了激活检查点中的关键性能权衡。
conclusion: MONET为跨平台的神经网络训练提供了全面的建模与优化手段，是实现高效深度学习软硬件协同设计的关键工具。
---

## 摘要
虽然软硬件协同设计显著提高了神经网络推理的效率，但对训练阶段的建模仍然是一个关键且尚未得到充分探索的挑战。训练工作负载施加了独特的约束，特别是在内存占用和反向传播复杂性方面，而现有的专注于推理的工具无法捕捉到这些约束。本文介绍了 MONET，这是一个旨在对异构数据流加速器上的神经网络训练进行建模的框架。MONET 基于 Stream 构建，Stream 是一个经过实验验证的框架，用于对具有层融合的异构数据流加速器上的神经网络推理进行建模。利用 MONET，我们探索了 ResNet-18 和小型 GPT-2 的设计空间，展示了该框架建模训练工作流并寻找更优硬件架构的能力。随后，我们进一步研究了由于设计空间更大而在神经网络训练中变得更加复杂的问题，例如确定最佳的层融合配置。此外，我们借助遗传算法，利用该框架在激活检查点（activation checkpointing）中寻找有趣的权衡。我们的研究结果强调了整体软硬件协同设计方法对于可扩展且高效的深度学习部署的重要性。

## Abstract
While hardware-software co-design has significantly improved the efficiency of neural network inference, modeling the training phase remains a critical yet underexplored challenge. Training workloads impose distinct constraints, particularly regarding memory footprint and backpropagation complexity, which existing inference-focused tools fail to capture. This paper introduces MONET, a framework designed to model the training of neural networks on heterogeneous dataflow accelerators. MONET builds upon Stream, an experimentally verified framework that that models the inference of neural networks on heterogeneous dataflow accelerators with layer fusion. Using MONET, we explore the design space of ResNet-18 and a small GPT-2, demonstrating the framework's capability to model training workflows and find better hardware architectures. We then further examine problems that become more complex in neural network training due to the larger design space, such as determining the best layer-fusion configuration. Additionally, we use our framework to find interesting trade-offs in activation checkpointing, with the help of a genetic algorithm. Our findings highlight the importance of a holistic approach to hardware-software co-design for scalable and efficient deep learning deployment.

---

## 论文详细总结（自动生成）

以下是对论文《MONET: Modeling and Optimization of neural NEtwork Training from Edge to Data Centers》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的神经网络硬件加速器建模工具（如 Timeloop, ZigZag, Stream）主要针对**推理（Inference）**阶段，而忽视了**训练（Training）**阶段的特殊性。
*   **研究动机**：训练过程比推理复杂得多，涉及反向传播、梯度计算、权重更新以及巨大的内存开销（激活值存储、优化器状态）。
*   **整体含义**：论文提出了 **MONET** 框架，旨在填补这一空白，通过对异构数据流加速器（HDA）上的训练工作负载进行建模，实现软硬件协同设计，优化从边缘设备到数据中心的训练效率。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：基于已验证的推理建模框架 **Stream** 进行扩展，通过 ONNX 转换将完整的训练图（前向+反向+优化器）映射到异构硬件模型上。
*   **关键技术细节**：
    *   **训练图生成**：利用 ONNX Runtime Training 将 PyTorch 模型导出为包含梯度算子的图，并开发了专门的 ONNX Pass 将复合算子（如 `ConvGrad`）分解为 Stream 可识别的细粒度原语。
    *   **层融合（Layer Fusion）优化**：提出一种基于**约束优化（整数规划，IP）**的算法。通过广义优先搜索（BFS）寻找候选子图，并在内存容量、平铺（Tiling）兼容性和算子类型约束下，自动寻找最优的融合配置，以减少访存开销。
    *   **激活检查点（Activation Checkpointing）**：针对训练中内存占用最高的激活值存储问题，引入了**遗传算法（NSGA-II）**。由于层融合环境下重计算成本具有非线性特征，该算法能有效搜索内存节省与额外延迟/功耗之间的 Pareto 最优权衡。

### 3. 实验设计
*   **场景与模型**：
    *   **边缘计算场景**：使用 **ResNet-18** 模型，目标硬件为类 **Edge TPU** 的异构加速器。
    *   **数据中心场景**：使用小型 **GPT-2** 模型，目标硬件为 **FuseMax**（一种针对 Attention 优化的加速器）。
*   **Benchmark 与对比对象**：
    *   对比了“逐层执行（Base）”、“手动设计融合（Manual）”以及 MONET 自动生成的融合策略。
    *   对比了推理与训练在相同硬件配置下的性能分布差异。
    *   在激活检查点实验中，对比了全量存储与不同重计算策略的开销。

### 4. 资源与算力
*   **建模工具开销**：MONET 本身是一个分析/仿真框架，不涉及大规模集群训练。
*   **提及的硬件**：文中提到在 **NVIDIA RTX 3090** 上测量了 ResNet-50 的实际内存崩溃情况作为动机参考。
*   **仿真算力**：实验中模拟了多种配置的 Edge TPU（PE 阵列从 1x1 到 8x8）和 FuseMax 架构。具体的仿真运行时间未详细列出，但提到其启发式算法旨在提高迭代速度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Edge TPU 进行了大规模设计空间探索（DSE），涉及 PE 数量、SIMD 单元、内存大小等多个维度的组合。
    *   针对层融合策略，对比了不同子图长度限制（Limit 4/6/8）的效果。
    *   针对激活检查点，通过遗传算法演化了多代（Gen 0-3）以寻找 Pareto 前沿。
*   **充分性评价**：实验覆盖了视觉（CNN）和语言（Transformer）两大主流领域，且涵盖了从硬件参数到软件调度策略的多个层次，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **推理与训练的硬件需求不一致**：实验证明，在推理上表现最优的硬件配置（如 PE 粒度）在训练任务中往往不是最优的。
*   **自动融合的优越性**：MONET 自动生成的层融合策略在延迟和功耗上均优于手动设计的配置。
*   **非线性权衡**：在层融合架构下，激活检查点的重计算成本并非简单的线性累加，使用遗传算法能发现比传统线性模型更优的内存/性能平衡点。

### 7. 优点
*   **全流程建模**：首次在异构数据流加速器上实现了包含优化器更新在内的端到端训练建模。
*   **模块化设计**：基于 ONNX 的 Pass 设计使其具有良好的通用性，可以适配不同的深度学习框架。
*   **算法创新**：将整数规划和遗传算法引入调度优化，解决了训练任务中搜索空间过大的问题。

### 8. 不足与局限
*   **控制逻辑简化**：作为数据流建模工具，MONET 主要关注计算和访存，对 GPU 等复杂架构中的指令发射、分支预测等动态控制逻辑建模较少。
*   **硬件验证局限**：虽然 Stream 框架本身经过实验验证，但 MONET 针对训练生成的复杂调度在真实流片硬件上的周期精确度（Cycle-accuracy）仍有待进一步实测验证。
*   **应用限制**：目前主要针对单机异构加速器，对于超大规模分布式训练（如跨节点的流水线并行）的通信建模尚不深入。

（完）
