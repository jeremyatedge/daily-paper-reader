---
title: Covariance-Guided Resource Adaptive Learning for Efficient Edge Inference
title_zh: 协方差引导的资源自适应学习，用于高效边缘推理
authors: "Ahmad N. L. Nabhaan, Zaki Sukma, Rakandhiya D. Rachmanto, Muhammad Husni Santriaji, Byungjin Cho, Arief Setyanto, In Kee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14577v1"
tags: ["query:aiot"]
score: 9.0
evidence: 高效边缘推理硬件配置的在线优化
tldr: 针对边缘设备深度学习推理中硬件配置优化难的问题，本文提出CORAL，一种无需离线分析的在线优化方法。它利用距离协方差捕捉硬件设置与性能间的非线性关系，将任务建模为吞吐量与功耗的协同优化问题。在NVIDIA Jetson设备上的实验表明，CORAL能以极低的探索成本找到接近最优的配置，有效满足功耗预算和吞吐量目标。
selection_source: fresh_fetch
motivation: 边缘设备硬件配置复杂且性能功耗比差异大，现有的静态预设或离线分析方法效率低下且难以跨设备迁移。
method: 提出基于距离协方差的在线优化方法CORAL，通过统计建模硬件参数间的非线性依赖关系，实现吞吐量与功耗的协同优化。
result: "在多种目标检测模型和设备上，CORAL达到了穷举搜索最优性能的96%-100%，并在双重约束场景下表现优于基准方法。"
conclusion: CORAL证明了通过在线统计学习可以高效解决边缘推理的资源自适应问题，无需昂贵的预处理即可实现高性能与低功耗的平衡。
---

## 摘要
在边缘设备上进行深度学习推理时，达到相同吞吐量的硬件配置在功耗上可能相差 2 倍，但操作人员往往难以在不进行详尽分析的情况下找到高效的配置。现有方法通常依赖于低效的静态预设，或者需要昂贵的离线分析，且必须针对每个新模型或设备重复进行。为了解决这一问题，我们提出了 CORAL，这是一种无需离线分析即可发现近乎最优配置的在线优化方法。CORAL 利用距离协方差（distance covariance）从统计学角度捕捉硬件设置（如 DVFS 和并发级别）与性能指标之间的非线性依赖关系。与以往的工作不同，我们将该挑战明确表述为一个吞吐量-功耗联合优化问题，以同时满足功耗预算和吞吐量目标。我们在两个 NVIDIA Jetson 设备上，针对从轻量级到重量级的三个目标检测模型对 CORAL 进行了评估。在单目标场景下，CORAL 达到了详尽搜索所发现的最优性能的 96% – 100%。在基准方法失效或超出功耗预算的严格双重约束场景下，CORAL 能够以极小的探索开销持续在线找到合适的配置。

## Abstract
For deep learning inference on edge devices, hardware configurations achieving the same throughput can differ by 2$\times$ in power consumption, yet operators often struggle to find the efficient ones without exhaustive profiling. Existing approaches often rely on inefficient static presets or require expensive offline profiling that must be repeated for each new model or device. To address this problem, we present CORAL, an online optimization method that discovers near-optimal configurations without offline profiling. CORAL leverages distance covariance to statistically capture the non-linear dependencies between hardware settings, e.g., DVFS and concurrency levels, and performance metrics. Unlike prior work, we explicitly formulate the challenge as a throughput-power co-optimization problem to satisfy power budgets and throughput targets simultaneously. We evaluate CORAL on two NVIDIA Jetson devices across three object detection models ranging from lightweight to heavyweight. In single-target scenarios, CORAL achieves 96% $\unicode{x2013}$ 100% of the optimal performance found by exhaustive search. In strict dual-constraint scenarios where baselines fail or exceed power budgets, CORAL consistently finds proper configurations online with minimal exploration.

---

## 论文详细总结（自动生成）

### 论文总结：Covariance-Guided Resource Adaptive Learning for Efficient Edge Inference (CORAL)

#### 1. 核心问题与整体含义（研究动机和背景）
在边缘设备（如无人机、智能监控）上部署深度学习推理时，面临**严格的功耗预算**和**实时吞吐量要求**的双重挑战。现代边缘硬件（如 NVIDIA Jetson）拥有复杂的配置空间（CPU/GPU 频率、核心数、内存频率、并发级别等），不同配置下，即便吞吐量相同，功耗差异可达 2 倍。
*   **现有痛点**：
    *   **静态预设**：厂家提供的预设模式（Power Modes）覆盖面窄，难以平衡特定应用的性能与功耗。
    *   **离线分析（Profiling）**：如 ALERT 等方法需要针对每个模型和设备进行数小时的详尽测试，缺乏灵活性。
    *   **强化学习**：在线学习方法通常需要大量训练迭代，收敛缓慢。
*   **研究目标**：开发一种**无需离线分析**、能够**在线快速收敛**并同时满足功耗与吞吐量约束的优化方法。

#### 2. 方法论：核心思想与技术细节
论文提出了 **CORAL**，其核心思想是利用统计学中的**距离协方差（Distance Covariance）**来引导硬件配置的搜索。
*   **核心技术细节**：
    1.  **距离协方差应用**：不同于皮尔逊相关系数（仅能捕捉线性关系），距离协方差能够捕捉硬件参数与性能指标（吞吐量、功耗）之间的**非线性依赖关系**。
    2.  **协同优化建模**：将问题定义为在满足 $\tau(s) \ge \tau_{target}$（吞吐量）且 $p(s) \le p_{budget}$（功耗）的前提下，最大化效率 $\eta = \tau/p$。
    3.  **算法流程**：
        *   **奖励评估**：执行推理并测量指标。满足双约束则奖励效率比，否则给予负值惩罚并加入禁止列表。
        *   **相关性分析**：利用最近 $W$ 次观察结果，计算各硬件参数与吞吐量、功耗的距离相关系数（$dCor$）。
        *   **配置搜索**：根据相关系数确定各参数的修改权重（高相关性参数调整幅度大），并根据当前是否达标决定搜索方向（增加性能或降低功耗）。
        *   **迭代优化**：通常在 10 次迭代内即可收敛。

#### 3. 实验设计
*   **硬件平台**：NVIDIA Jetson Xavier NX (Volta 架构) 和 Jetson Orin Nano (Ampere 架构)。
*   **模型选择**：涵盖轻量级到重量级的三个目标检测模型：YOLOv5-N (1.9M 参数)、FRCNN-MobileNetV3 (19.4M)、RetinaNet-ResNet50 (38M)。
*   **对比基准（Baselines）**：
    *   **ORACLE**：通过穷举搜索获得的全局最优解（理论上限）。
    *   **ALERT**：基于离线分析和卡尔曼滤波的状态空间方法。
    *   **ALERT-Online**：ALERT 的在线随机探索版本。
    *   **厂家预设**：Max-power（最大性能）和 Default（默认模式）。
*   **场景设置**：单目标优化（仅吞吐量）和严格的双重约束优化（功耗限制 + 吞吐量目标）。

#### 4. 资源与算力
*   **推理设备**：使用了两款主流边缘 SoC（Xavier NX 和 Orin Nano）。
*   **算力开销**：CORAL 是一种**轻量级在线算法**。它不需要在云端或本地进行大规模预训练。
*   **搜索成本**：算法被限制在 **10 次迭代**内完成搜索，每次迭代仅涉及一次推理测量（约 1 秒），总搜索时间极短，适合动态环境。

#### 5. 实验数量与充分性
*   **实验规模**：论文对 Xavier NX 上的 2,160 种配置和 Orin Nano 上的 1,600 种配置进行了**穷举测试**，以建立完整的搜索空间基准（Ground Truth）。
*   **充分性**：实验覆盖了不同架构的 GPU、不同规模的模型以及不同的约束强度。通过与 ORACLE（全局最优）的对比，证明了其搜索效率。
*   **客观性**：使用了标准的 COCO 数据集和真实的交通监控视频流，评估指标包括 FPS 和 mW，数据采集使用了系统级工具（tegrastats），实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能接近最优**：在单约束场景下，CORAL 达到了 ORACLE 性能的 **96% – 100%**。
*   **双约束优势显著**：在严格的功耗和吞吐量限制下，厂家预设和 ALERT 往往会违反其中一项约束（如功耗超标或帧率不足），而 CORAL 能够稳定找到可行解。
*   **模型泛化性**：随着模型变大（如 RetinaNet），可行配置空间变窄，CORAL 相比基准方法的领先优势更加明显。
*   **收敛速度**：仅需 10 次在线尝试即可找到近优配置，远快于传统的强化学习或离线分析方法。

#### 7. 优点（亮点）
*   **无需先验知识**：完全摆脱了对离线分析（Offline Profiling）的依赖，实现了“即插即用”。
*   **非线性建模能力**：引入距离协方差有效解决了硬件参数间复杂的非线性交互问题。
*   **协同优化**：明确处理了吞吐量与功耗的权衡，而非单一指标优化，更符合工业界实际部署需求。
*   **极低开销**：算法逻辑简单，计算开销几乎可以忽略不计。

#### 8. 不足与局限
*   **设备覆盖有限**：实验仅限于 NVIDIA Jetson 系列，未在其他边缘平台（如树莓派、瑞芯微或专用 NPU 平台）上验证。
*   **固定迭代次数**：算法设定了固定的 10 次迭代，在参数空间更庞大的设备上可能需要更复杂的收敛判定机制。
*   **多模型并发**：论文主要关注单模型推理优化，未深入探讨多个不同模型在同一设备上竞争资源时的动态调度问题。
*   **环境剧烈波动**：虽然是在线方法，但如果输入负载（如视频复杂度）发生剧烈且频繁的瞬时变化，10 次迭代的响应延迟可能仍显不足。

（完）
