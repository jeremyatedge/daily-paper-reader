---
title: "SNAP-V: A RISC-V SoC with Configurable Neuromorphic Acceleration for Small-Scale Spiking Neural Networks"
title_zh: SNAP-V：一种面向小规模脉冲神经网络且具有可配置类脑加速功能的 RISC-V SoC
authors: "Kanishka Gunawardana, Sanka Peeris, Kavishka Rambukwella, Thamish Wanduragala, Saadia Jameel, Roshan Ragel, Isuru Nawinne"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11939v1"
tags: ["query:aiot"]
score: 10.0
evidence: 具有边缘 SNN 神经形态加速功能的 RISC-V SoC
tldr: SNAP-V是一款基于RISC-V架构的神经形态SoC，专为小规模脉冲神经网络（SNN）推理优化。针对传统SoC的存储瓶颈和大型神经形态硬件在小规模应用中的低效问题，该研究提出了Cerebra-S（总线型）和Cerebra-H（NoC型）两种加速器变体。通过集成RISC-V核心、并行处理节点和分布式存储，SNAP-V在45nm工艺下实现了极低的能耗（1.05 pJ/SOP）和高精度的推理，为实时边缘计算提供了高效解决方案。
selection_source: fresh_fetch
motivation: 传统SoC架构存在存储-处理器瓶颈，而现有大型神经形态硬件在处理小规模SNN应用时效率低下且资源浪费。
method: 设计了集成RISC-V核心与两种可配置加速器（总线型和NoC型）的SoC，利用并行处理节点和分布式存储优化推理。
result: "实验显示硬件推理精度与软件高度一致（偏差仅2.62%），且在45nm工艺下实现了每突触操作1.05 pJ的极低能耗。"
conclusion: SNAP-V为实时边缘应用提供了一个高精度、低功耗且灵活的小规模脉冲神经网络硬件加速方案。
---

## 摘要
脉冲神经网络 (SNN) 因其低功耗和高计算效率在边缘计算领域受到了广泛关注。然而，现有的实现方式要么采用受限于存算瓶颈的传统片上系统 (SoC) 架构，要么采用对于小规模 SNN 应用而言效率低下且资源浪费的大规模类脑硬件。本研究提出了 SNAP-V，这是一款基于 RISC-V 的类脑 SoC，包含两种加速器变体：Cerebra-S（基于总线）和 Cerebra-H（基于片上网络 (NoC)）。这两者均针对小规模 SNN 推理进行了优化，集成了一个用于管理任务的 RISC-V 核心，且两种加速器都具有并行处理节点和分布式存储。实验结果表明，软件与硬件推理结果高度一致，在多种网络配置下的平均准确率偏差仅为 2.62%，在 45 nm CMOS 工艺下，每次突触操作 (SOP) 的平均突触能量为 1.05 pJ。这些结果表明，所提出的解决方案能够实现准确、高效的 SNN 推理，适用于实时边缘应用。

## Abstract
Spiking Neural Networks (SNNs) have gained significant attention in edge computing due to their low power consumption and computational efficiency. However, existing implementations either use conventional System on Chip (SoC) architectures that suffer from memory-processor bottlenecks, or large-scale neuromorphic hardware that is inefficient and wasteful for small-scale SNN applications. This work presents SNAP-V, a RISC-V-based neuromorphic SoC with two accelerator variants: Cerebra-S (bus-based) and Cerebra-H (Network-on-Chip (NoC)-based) which are optimized for small-scale SNN inference, integrating a RISC-V core for management tasks, with both accelerators featuring parallel processing nodes and distributed memory. Experimental results show close agreement between software and hardware inference, with an average accuracy deviation of 2.62% across multiple network configurations, and an average synaptic energy of 1.05 pJ per synaptic operation (SOP) in 45 nm CMOS technology. These results show that the proposed solution enables accurate, energy-efficient SNN inference suitable for real-time edge applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SNAP-V** 的基于 RISC-V 指令集架构的片上系统（SoC），专门用于小规模脉冲神经网络（SNN）的硬件加速。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：现有的 SNN 实现存在两极分化：传统 SoC 架构受限于冯·诺依曼瓶颈（内存与处理器分离）；而大型类脑硬件（如 Intel Loihi、SpiNNaker）虽然强大，但对于仅需数十到数千个神经元的小规模边缘应用（如机器人运动控制、简单传感器处理）来说，资源冗余度极高（利用率常低于 0.000002%），导致严重的静态功耗浪费。
*   **研究动机**：填补大规模类脑芯片与通用嵌入式处理器之间的空白，设计一种既能处理通用计算任务，又能高效执行小规模 SNN 推理的集成化 SoC。

### 2. 方法论：核心思想与关键技术
*   **双核 RISC-V 架构**：采用 RocketChip 基础，包含 **MainCore**（负责系统控制、初始化和通用任务）和 **SpikeCore**（通过 RoCC 接口专门负责加速器的编排与通信）。
*   **加速器变体设计**：
    *   **Cerebra-S (Small)**：基准设计，采用全局标记总线（Global Tagged Bus）连接 1024 个神经元，使用中心化存储。
    *   **Cerebra-H (High-performance)**：改进设计，采用**分层片上网络 (H-NoC)**。神经元被分为 32 个簇（每簇 32 个神经元），簇内通过 L1 路由器通信，簇间通过 L2 路由器通信。
*   **神经元模型**：实现可配置的 **LIF（Leaky Integrate-and-Fire）** 模型。Cerebra-H 将复杂的乘法衰减优化为算术右移操作，显著降低了功耗。
*   **分布式存储**：Cerebra-H 将权重存储分布在簇组级别，每 4 个簇共享一个单端口权重存储器，有效缓解了内存访问瓶颈。
*   **硬件编码/解码器**：内置专用硬件单元，将传感器数据直接转换为脉冲序列（频率编码），并能将输出脉冲解码为控制指令，减少 CPU 负担。

### 3. 实验设计
*   **数据集**：使用 **MNIST** 手写数字识别作为主要功能验证基准。
*   **Benchmark（基准）**：使用 PyTorch 和 snnTorch 实现的软件参考模型。
*   **对比维度**：
    *   **功能准确性**：对比软件推理与 RTL 级硬件仿真（Synopsys VCS）的分类准确率。
    *   **物理特性**：在 **45nm CMOS** 工艺下，评估功耗、面积、最高工作频率和每突触操作能量（SOP）。
    *   **架构对比**：对比 Cerebra-S 与 Cerebra-H 的性能提升。

### 4. 资源与算力
*   **EDA 工具**：使用了 Synopsys VCS（仿真）、RTL Architect（综合与面积评估）、PrimePower（功耗分析）。
*   **硬件工艺**：45nm CMOS OpenNand 库。
*   **算力说明**：文中未明确提及训练软件模型所用的 GPU 型号或训练时长，因为研究重点在于**硬件推理加速器**的实现与验证，而非训练算法本身。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **80 组** 实验。涵盖了 5 种隐藏层规模（16 到 256 个神经元）以及 16 种不同的训练/推理时间步长组合（T=25, 50, 75, 100）。
*   **充分性评价**：实验设计较为充分。通过跨时间步长的配置验证了硬件的鲁棒性，并提供了详细的子系统级功耗分解（权重存储、神经元簇、NoC 等），客观地展示了硬件性能。

### 6. 主要结论与发现
*   **高保真度**：硬件推理与软件模型高度一致，平均准确率偏差仅为 **2.62%**。在 256 个神经元的配置下，偏差降至 **0.63%**。
*   **能效显著**：在 45nm 工艺下，计算路径的平均突触能量仅为 **1.05 pJ/SOP**。
*   **性能提升**：Cerebra-H 的最高工作频率达到 **96.24 MHz**，比 Cerebra-S 提升了约 9.46 倍，证明了 H-NoC 架构在消除通信瓶颈方面的有效性。
*   **存储主导功耗**：功耗分析发现，**95.97% 的系统功耗来自权重存储器**，而神经元计算和通信逻辑仅占不到 4%。

### 7. 优点与亮点
*   **高度集成**：将 RISC-V 通用核心与专用 SNN 加速器集成在同一 SoC 内，消除了外部微控制器或主机通信的延迟。
*   **架构优化**：Cerebra-H 的分层 NoC 和分布式存储设计，在保持小规模的同时，解决了传统总线架构的拥塞问题。
*   **灵活性**：支持运行时配置神经元参数（衰减率、阈值、重置模式），并能同时运行多个独立的 SNN 模型。

### 8. 不足与局限
*   **存储瓶颈依然存在**：尽管采用了分布式设计，但 SRAM 存储仍占据了绝大部分功耗和面积，这限制了其在极低功耗环境下的进一步扩展。
*   **工艺节点较旧**：实验基于 45nm 工艺，虽然具有参考价值，但与目前主流边缘芯片采用的 22nm 或更先进工艺相比，其绝对功耗数据可能偏高。
*   **应用场景单一**：目前仅在 MNIST 数据集上进行了验证，对于更复杂的时序数据（如 DVS 相机输入或实时机器人控制反馈）的实测表现尚待进一步研究。

（完）
