---
title: Multi-Objective Load Balancing for Heterogeneous Edge-Based Object Detection Systems
title_zh: 异构边缘目标检测系统的多目标负载均衡
authors: "Daghash K. Alqahtani, Maria A. Rodriguez, Muhammad Aamir Cheema, Adel N. Toosi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15400v1"
tags: ["query:aiot"]
score: 9.0
evidence: 具有能效特征的异构边缘目标检测负载均衡
tldr: 针对物联网边缘计算中硬件异构和环境动态变化的挑战，本文提出一种多目标负载均衡方法。该方法针对目标检测任务，通过两阶段决策机制（准确率过滤与延迟-能耗加权评分），在异构设备-模型对之间动态分配任务。实验表明，该方法在仅损失少量准确率的情况下，显著降低了能耗和端到端延迟，提升了边缘系统的整体效率。
selection_source: fresh_fetch
motivation: 边缘计算环境中硬件异构性及动态变化的请求与场景复杂度，使得在资源受限设备上实现高效的负载均衡变得困难。
method: 采用两阶段决策机制，先通过准确率感知过滤候选设备-模型对，再利用延迟与能耗的加权评分函数选择最优执行目标。
result: "与准确率优先的基准相比，该方法将能耗降低了一半，端到端延迟减少了80%，而准确率下降控制在10%以内。"
conclusion: 该多目标负载均衡策略能有效平衡边缘目标检测系统的准确率、延迟和能耗，适用于动态异构的物联网环境。
---

## 摘要
物联网（IoT）和智能应用的快速普及导致分布式传感设备产生的数据量激增。边缘计算是处理这些数据的一种主流方法，它将计算推向更靠近数据源的地方，通常是单板计算机（SBC）等资源受限的设备。在此类环境中，硬件和软件不可避免的异构性使得有效的负载均衡变得尤为具有挑战性。在本文中，我们提出了一种专为异构边缘目标检测系统量身定制的多目标负载均衡方法。我们研究了一种场景，其中多个“设备-模型”对表现出不同的准确率、延迟和能耗特性，同时请求强度和场景复杂度随时间波动。为了应对这种动态变化的环境，我们的方法采用了一种两阶段决策机制：首先进行准确率感知过滤，以识别在可接受范围内提供准确率的合适设备-模型候选者，然后对预期延迟和能耗应用加权和评分函数，以选择最终的执行目标。我们通过在真实数据集上进行的大量实验评估了所提出的负载均衡器，并与广泛使用的基准策略进行了比较。结果表明，与以准确率为中心的基准相比，所提出的多目标负载均衡方法使能耗减半，端到端延迟降低了 80%，而检测准确率仅适度下降了最多 10%。

## Abstract
The rapid proliferation of the Internet of Things (IoT) and smart applications has led to a surge in data generated by distributed sensing devices. Edge computing is a mainstream approach to managing this data by pushing computation closer to the data source, typically onto resource-constrained devices such as single-board computers (SBCs). In such environments, the unavoidable heterogeneity of hardware and software makes effective load balancing particularly challenging. In this paper, we propose a multi-objective load balancing method tailored to heterogeneous, edge-based object detection systems. We study a setting in which multiple device-model pairs expose distinct accuracy, latency, and energy profiles, while both request intensity and scene complexity fluctuate over time. To handle this dynamically varying environment, our approach uses a two-stage decision mechanism: it first performs accuracy-aware filtering to identify suitable device-model candidates that provide accuracy within the acceptable range, and then applies a weighted-sum scoring function over expected latency and energy consumption to select the final execution target. We evaluate the proposed load balancer through extensive experiments on real-world datasets, comparing against widely used baseline strategies. The results indicate that the proposed multi-objective load balancing method halves energy consumption and achieves an 80% reduction in end-to-end latency, while incurring only a modest, up to 10%, decrease in detection accuracy relative to an accuracy-centric baseline.

---

## 论文详细总结（自动生成）

### 异构边缘目标检测系统的多目标负载均衡论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着物联网（IoT）设备的激增，边缘计算成为处理高带宽视频流的主流方案。然而，边缘环境面临三大挑战：
*   **硬件异构性**：系统中同时存在不同性能的单板计算机（如 Raspberry Pi、Jetson Nano）。
*   **模型多样性**：同一任务可由不同参数量和精度的模型（如 YOLOv8 不同版本、SSD）完成。
*   **动态环境**：请求频率和视频场景复杂度（目标数量）随时间剧烈波动。
现有的负载均衡策略往往只关注单一指标（如延迟），忽略了准确率、能耗与延迟之间的复杂权衡。本文旨在设计一种能同时优化这三个目标的动态负载均衡器。

#### 2. 论文提出的方法论
论文提出了一种**两阶段决策机制**，集成在 API 网关中：
*   **核心思想**：将准确率视为硬约束，将延迟和能耗视为优化目标。
*   **关键技术细节**：
    1.  **场景复杂度估计**：利用视频帧的时间连续性，通过上一帧的检测结果估计当前帧的目标数量，避免了额外的计算开销。
    2.  **第一阶段：准确率感知过滤**。根据预设的准确率容忍度（$\Delta mAP$），从所有“设备-模型”对中筛选出满足最低准确率要求的候选集。
    3.  **第二阶段：多目标加权评分**。对候选集中的设备进行评分。评分函数 $J$ 结合了归一化后的**预期延迟**（考虑离线性能分析和实时队列长度）和**预期能耗**。
    4.  **公式逻辑**：$J = \gamma \cdot \text{归一化延迟} + (1 - \gamma) \cdot \text{归一化能耗}$。通过调节权重 $\gamma$，系统可以在节能模式和低延迟模式之间切换。

#### 3. 实验设计
*   **数据集/场景**：使用真实的行人过马路视频流（417 张 JPEG 图像），通过高容量的 YOLOv8x 模型生成伪标签作为基准（Ground Truth）。
*   **实验床（Testbed）**：包含 1 个客户端、1 个网关（RPi 4）和 5 个异构边缘节点（包括 Raspberry Pi 5 配合 TPU/AI HAT、Jetson Orin Nano）。
*   **对比方法（Benchmarks）**：
    *   通用策略：轮询（RR）、随机（RND）、最小连接数（LC）。
    *   单目标极限策略：最低能耗（LE）、最低延迟（LT）、最高准确率（HA）。
*   **评估指标**：平均响应时间、P90 尾部延迟、吞吐量、每请求能耗、平均精度（mAP）。

#### 4. 资源与算力
*   **硬件型号**：
    *   边缘节点：Raspberry Pi 5 (8GB), Jetson Orin Nano (8GB), Coral TPU, Hailo-8 AI HAT。
    *   网关/客户端：Raspberry Pi 4 (4GB)。
*   **算力说明**：论文侧重于**推理阶段**的部署与调度，未涉及模型训练。所有模型性能（延迟、能耗、准确率）均通过离线分析（Profiling）预先获得。
*   **实现工具**：负载均衡逻辑通过 HAProxy 和 Lua 脚本实现，确保微秒级的决策延迟。

#### 5. 实验数量与充分性
*   **实验规模**：测试了从 1 到 15 个并发用户的不同负载水平。
*   **消融/参数实验**：针对权重参数 $\gamma$ 进行了 5 组对比实验（0, 0.25, 0.5, 0.75, 1），验证了模型在不同偏好下的表现。
*   **充分性评价**：实验涵盖了从低负载到高负载的演变，并使用了真实硬件环境而非纯模拟，结果具有较高的客观性和说服力。每组实验运行 3 次取平均值以减少随机误差。

#### 6. 论文的主要结论与发现
*   **性能平衡**：在 $\gamma=0.5$ 的配置下，与“最高准确率（HA）”基准相比，该方法将**能耗降低了 50%**，**端到端延迟减少了 80%**。
*   **准确率损失可控**：在大幅提升效率的同时，准确率（mAP）的下降控制在 **10% 以内**。
*   **动态适应性**：实验证明，通过调整 $\gamma$ 值，系统能有效在“节能优先”和“延迟优先”之间平滑切换，且对准确率的影响较小。

#### 7. 优点
*   **多维度权衡**：首次在异构边缘负载均衡中同时考虑了场景复杂度、硬件异构性和模型准确率。
*   **低开销设计**：利用输出反馈估计目标数量，避免了引入额外的轻量级计数模型，非常适合资源受限的边缘设备。
*   **实用性强**：基于 HAProxy 和 Lua 开发，易于集成到现有的边缘网关架构中。

#### 8. 不足与局限
*   **数据集规模较小**：实验仅使用了 417 张图像的单一视频流，样本多样性（如天气、光照变化）可能不足。
*   **依赖离线分析**：系统性能高度依赖于预先测得的设备性能数据，若硬件老化或环境温度导致性能漂移，静态数据可能失效。
*   **中心化瓶颈**：采用单网关决策架构，在更大规模（成百上千个节点）的集群中，网关可能成为性能瓶颈。
*   **伪标签风险**：使用 YOLOv8x 生成的标签作为基准，其本身可能存在误差，影响最终 mAP 计算的绝对准确性。

（完）
