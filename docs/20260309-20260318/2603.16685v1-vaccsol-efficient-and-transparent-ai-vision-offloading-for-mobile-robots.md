---
title: "vAccSOL: Efficient and Transparent AI Vision Offloading for Mobile Robots"
title_zh: vAccSOL：面向移动机器人的高效且透明的 AI 视觉卸载
authors: "Adam Zahir, Michele Gucciardom Falk Selker, Anastasios Nanos, Kostis Papazafeiropoulos, Carlos J. Bernardos, Nicolas Weber, Roberto Gonzalez"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16685v1"
tags: ["query:aiot"]
score: 9.0
evidence: 具有能量限制的移动机器人AI视觉卸载
tldr: 针对移动机器人在视觉感知中面临的计算资源受限和高功耗挑战，本文提出 vAccSOL 框架。该框架结合了 SOL 神经网络编译器和 vAccel 轻量级执行框架，实现了硬件优化的推理及本地与边缘间的透明任务调度。无需修改应用程序，vAccSOL 即可显著提升推理效率并降低能耗，为受限环境下的机器人视觉任务提供了高效、灵活的解决方案。
selection_source: fresh_fetch
motivation: 移动机器人运行现代视觉模型时面临严苛的计算资源和功耗限制，且现有硬件加速器往往受限于专有软件栈。
method: 提出 vAccSOL 框架，集成 SOL 编译器生成优化推理库，并利用 vAccel 实现任务在机器人本地或边缘基础设施间的透明调度。
result: "在四足机器人测试中，该框架使机器人端功耗降低达 80%，视觉流水线帧率提升最高 24 倍，性能优于或等同于 PyTorch 编译器。"
conclusion: vAccSOL 通过高效的编译优化和灵活的卸载机制，在不改变应用代码的前提下，显著延长了机器人的续航时间并增强了其感知能力。
---

## 摘要
移动机器人越来越多地被部署用于巡检、巡逻和搜救行动，依靠计算机视觉进行感知、导航和自主决策。然而，由于计算资源有限和严格的能量约束，在机载设备上执行现代视觉工作负载具有挑战性。虽然一些平台包含嵌入式加速器，但这些加速器通常与专有软件栈绑定，导致用户定义的工作负载只能在资源受限的伴随计算机上运行。我们提出了 vAccSOL，这是一个用于在异构机器人和边缘平台上高效且透明地执行基于 AI 的视觉工作负载的框架。vAccSOL 集成了两个组件：SOL，一个生成具有最小运行时依赖项的优化推理库的神经网络编译器；以及 vAccel，一个轻量级执行框架，可透明地将推理任务调度到机器人本地或附近的边缘基础设施。这种结合实现了硬件优化的推理和灵活的执行位置分配，且无需修改机器人应用程序。我们在包含商用四足机器人和 12 个深度学习模型（涵盖图像分类、视频分类和语义分割）的真实测试平台上评估了 vAccSOL。与 PyTorch 编译器基准相比，SOL 实现了相当或更好的推理性能。通过边缘卸载，与 PyTorch 相比，vAccSOL 将机器人侧的功耗降低了高达 80%，边缘侧功耗降低了高达 60%，同时将视觉流水线的帧率提高了多达 24 倍，延长了电池供电机器人的运行寿命。

## Abstract
Mobile robots are increasingly deployed for inspection, patrol, and search-and-rescue operations, relying on computer vision for perception, navigation, and autonomous decision-making. However, executing modern vision workloads onboard is challenging due to limited compute resources and strict energy constraints. While some platforms include embedded accelerators, these are typically tied to proprietary software stacks, leaving user-defined workloads to run on resource-constrained companion computers.   We present vAccSOL, a framework for efficient and transparent execution of AI-based vision workloads across heterogeneous robotic and edge platforms. vAccSOL integrates two components: SOL, a neural network compiler that generates optimized inference libraries with minimal runtime dependencies, and vAccel, a lightweight execution framework that transparently dispatches inference locally on the robot or to nearby edge infrastructure. This combination enables hardware-optimized inference and flexible execution placement without requiring modifications to robot applications.   We evaluate vAccSOL on a real-world testbed with a commercial quadruped robot and twelve deep learning models covering image classification, video classification, and semantic segmentation. Compared to a PyTorch compiler baseline, SOL achieves comparable or better inference performance. With edge offloading, vAccSOL reduces robot-side power consumption by up to 80% and edge-side power by up to 60% compared to PyTorch, while increasing vision pipeline frame rate by up to 24x, extending the operating lifetime of battery-powered robots.

---

## 论文详细总结（自动生成）

以下是对论文《vAccSOL: Efficient and Transparent AI Vision Offloading for Mobile Robots》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：移动机器人（如四足机器人、无人机）在执行现代AI视觉任务（如语义分割、视频识别）时，面临**计算资源受限**与**严格功耗约束**的矛盾。
*   **研究动机**：
    *   虽然部分机器人配备了嵌入式加速器（如NVIDIA Jetson），但这些硬件往往被厂商的专有软件栈锁定，用户自定义任务难以调用。
    *   现有的边缘计算卸载方案通常需要修改应用程序代码，且缺乏对异构硬件的高效适配。
    *   需要一种既能优化模型推理效率，又能实现本地与边缘之间“透明”切换的框架，以延长机器人续航并提升感知性能。

### 2. 方法论
论文提出了 **vAccSOL** 框架，由两个核心组件集成：
*   **SOL 神经网络编译器（编译层）**：
    *   将 PyTorch、TensorFlow 或 ONNX 模型转换为专有的高级中间表示（HLIR）。
    *   执行全局图优化（算子融合、常量折叠等）和快速层级自动调优（分钟级完成）。
    *   生成不依赖特定运行库的独立共享库（.so），支持 CPU（MKL/AOCL）和 GPU（cuDNN/cuBLAS）后端。
*   **vAccel 执行框架（运行层）**：
    *   采用客户端-代理（Client-Agent）架构，通过统一的 **GenOp API** 屏蔽底层硬件差异。
    *   **透明性**：应用程序只需调用 `infer()`，由 vAccel 根据配置决定是在机器人本地 CPU 执行，还是通过轻量级 RPC 转发至边缘服务器执行。
    *   **解耦**：无需在边缘端部署复杂的机器人中间件（如 ROS2），仅需部署轻量级代理。

### 3. 实验设计
*   **实验场景**：真实的室内走廊环境，使用 **Unitree Go2 EDU 四足机器人** 进行实时视觉感知测试。
*   **数据集与模型**：涵盖 12 种主流深度学习模型，分为三类任务：
    *   **图像分类**：ResNet50, Swin-T, Swin-S, Swin-V2-B。
    *   **视频动作识别**：R3D-18, R2Plus1D-18, Swin3D-S, Swin3D-B。
    *   **语义分割**：DeepLabV3-ResNet50/101, FCN-ResNet50/101。
*   **基准对比（Benchmark）**：
    *   对比对象：**PyTorch 2.0 (`torch.compile`)**，使用 Inductor 后端。
    *   对比维度：本地 CPU 执行 vs. 边缘 CPU 执行 vs. 边缘 GPU 执行。

### 4. 资源与算力
*   **机器人端（机载计算）**：外挂 mini-PC，配备 **Intel N100 CPU**（4核，3.4 GHz），12 GB RAM。
*   **边缘端（服务器）**：配备 **NVIDIA GeForce RTX 5080 Ti GPU**，Intel Core Ultra 9 275HX CPU（24核，6.5 GHz），32 GB RAM。
*   **网络环境**：通过 Wi-Fi 6 (AX200) 连接至华硕 RT-AX89X 接入点。
*   **训练/编译时长**：文中指出 SOL 的自动调优在分钟级完成，远快于 TVM 等编译器的数小时；实验重点在于推理阶段，未详细说明模型原始训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对 12 个模型在 3 种执行模式（本地、边缘 CPU、边缘 GPU）下进行了详尽测试，每组实验持续 60 秒。
*   **充分性**：实验涵盖了从轻量级 CNN 到重型 Transformer 的多种架构，并测量了端到端延迟、帧率（FPS）以及机器人/边缘两端的实时功耗（利用 Intel RAPL 和 NVIDIA NVML 接口）。
*   **客观性**：通过固定输入尺寸和验证数据集确保了不同后端下的模型准确率一致，对比具有公平性。

### 6. 主要结论与发现
*   **性能提升**：边缘 GPU 卸载使视觉流水线帧率最高提升 **24倍**（如 Swin-V2-B 模型）。
*   **功耗降低**：
    *   **机器人侧**：相比本地执行，边缘卸载可降低机器人端功耗达 **80%**（接近待机功耗）。
    *   **边缘侧**：相比 PyTorch 编译器，SOL 编译的模型在边缘端执行时功耗降低达 **60%**，证明了其生成的二进制文件更高效。
*   **瓶颈分析**：对于语义分割等任务，网络传输开销（大尺寸张量序列化）成为主要瓶颈，有时会抵消 GPU 推理的速度优势。

### 7. 优点
*   **透明卸载**：实现了“代码无感”的卸载，开发者无需为边缘计算重写推理逻辑。
*   **运行时独立性**：SOL 生成的 .so 文件不依赖庞大的深度学习框架（如 libtorch），极大地减小了部署体积。
*   **能效卓越**：不仅关注速度，还深入分析了对机器人续航至关重要的功耗指标。

### 8. 不足与局限
*   **网络依赖性**：实验在稳定的室内 Wi-Fi 6 环境下进行，未探讨在高延迟、丢包或移动网络（如 5G）切换下的鲁棒性。
*   **序列化开销**：vAccel 的通用接口（GenOp）虽然带来了灵活性，但在处理大规模张量时存在一定的序列化延迟，导致部分模型 FPS 低于高度定制化的插件。
*   **动态调度缺失**：目前仍需手动配置执行位置，尚未实现根据网络状况自动切换本地/边缘执行的动态决策机制。

（完）
