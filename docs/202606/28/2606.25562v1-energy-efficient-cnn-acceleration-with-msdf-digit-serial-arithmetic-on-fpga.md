---
title: Energy-Efficient CNN Acceleration with MSDF Digit-Serial Arithmetic on FPGA
title_zh: 基于FPGA的MSDF数字串行算术的能效CNN加速
authors: "Muhammad Usman, Yousef Sadegheih, Dorit Merhof"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25562v1"
tags: ["query:aiot"]
score: 9.0
evidence: 在FPGA上使用MSDF数字串行算术和融合乘加流水线实现能效CNN加速
tldr: 基于FPGA的U-Net卷积加速面临MSDF数字串行算术初始延迟累积问题。提出合并乘加（MMA）架构，将乘法与加法融合为统一流水线，单次延迟小于传统级联。FPGA加速器能效达15.14 GOPS/W，比MSDF实现节能约9倍，适用于资源受限的边端影像分割。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25562v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1344, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25562v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1292, \"height\": 681, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25562v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1804, \"height\": 354, \"label\": \"Table\"}]"
motivation: 克服MSDF数字串行算术中乘加级联运算的初始延迟累积，提升吞吐率。
method: 提出合并乘加（MMA）架构，融合乘法和加法为单一流水线，并并行处理空间输入深度。
result: FPGA加速器能效达15.14 GOPS/W，是CPU的7.8倍，相比MSDF实现能耗降低约9倍。
conclusion: 该设计适用于资源受限、延迟敏感的边端医疗影像与计算机视觉应用。
---

## 摘要
本文提出了一种用于图像分割的U-Net架构中卷积层的能效硬件加速，在FPGA上实现。尽管数字串行算术，特别是最高有效位优先（MSDF）技术，具有紧凑的硬件占用，但在产生第一个输出数字之前存在初始延迟。这种延迟在级联操作（如乘法后跟加法）中累积，每个单元都会引入自身的启动开销。为克服这一问题，我们提出了一种融合乘加（MMA）架构，将这些操作融合到统一的流水线中。与分别产生延迟不同，MMA每次迭代仅引入一次流线型延迟，短于传统级联单元的组合延迟，从而提高了吞吐量和效率。MMA单元设计为并行处理空间输入深度，性能显著高于基于MSDF的独立设计和传统设计。我们以U-Net为目标应用评估了所提出设计。尽管工作频率低于CPU，基于FPGA的加速器实现了高达一个数量级的能效提升，与基于CPU推理的1.93 GOPS/W相比，达到了15.14 GOPS/W。与基于MSDF的FPGA实现相比，该设计的能耗也降低了约9倍。这些结果凸显了融合算术方法在资源受限、延迟敏感的医疗成像和计算机视觉边缘应用中的有效性。

## Abstract
This paper presents an energy-efficient hardware acceleration of the convolutional layers in the U-Net architecture for image segmentation, implemented on FPGA. While digit-serial arithmetic, particularly most-significant-digit-first (MSDF) techniques, offers a compact hardware footprint, it suffers from initial latency before producing the first output digit. This delay accumulates in cascaded operations like multiplication followed by addition, where each unit introduces its own startup overhead. To overcome this, we propose a merged multiply-add (MMA) architecture that fuses these operations into a unified pipeline. Instead of incurring separate delays, the MMA introduces a single streamlined latency per iteration, shorter than the combined latency of conventional cascaded units, resulting in enhanced throughput and efficiency. The MMA units are designed to process spatial input depths in parallel, achieving significantly higher performance than both standalone MSDF-based and conventional designs. We evaluate the proposed design using U-Net as a target application. Despite operating at a lower frequency than a CPU, the FPGA-based accelerator achieves up to an order of magnitude higher energy efficiency, delivering up to $15.14$ GOPS/W compared to $1.93$ GOPS/W for CPU-based inference. The design also shows approximately $9\times$ reduction in energy consumption compared to MSDF-based FPGA implementations. These results highlight the efficacy of the merged arithmetic approach for resource-constrained, latency-sensitive edge applications in medical imaging and computer vision.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：U-Net等CNN模型在医疗图像分割中计算密集，CPU/GPU推理能耗高，不适合资源受限的边缘设备。FPGA虽能提供并行和低功耗，但传统数字串行算术（尤其MSDF）存在初始延迟累积问题，在级联的乘法→加法操作中，每个单元引入独立的启动延迟，导致整体延迟增加，吞吐量受限。
- **整体含义**：针对上述瓶颈，本文提出一种基于MSDF的融合乘加（MMA）架构，将乘法与加法合并为单一流水线，减少延迟，并通过并行化加工通道提升能效，实现适合边缘部署的高效CNN加速器。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将MSDF乘法与加法融合为统一流水线，避免传统级联结构中各单元独立初始延迟的累积。同时利用冗余数制（RNS）支持在线输出。
- **关键技术细节**：
  - **MMA单元**：接收逐位串行的激活比特（8-bit）和并行的权重（8-bit），通过32路AND门阵列生成部分积，然后输入进位传播加法树（利用FPGA进位链），并与前一周期残差合并。每周期输出最高7位作为输出数字，低位残差左移后反馈到下一周期。
  - **架构组织**：每个 \(3\times3\) 卷积核对应9个MMA单元（分别处理不同空间位置），通过MSDF加法树（MAT）聚合成一个KPB（Kernel Processing Block）。16个KPB并行处理16个输出像素。
  - **延迟公式**：\(\delta_{\times+} + p_{\text{out}} + \lceil \log_2(T_N)\rceil\) 乘以层内循环次数，其中 \(\delta_{\times+}=2\)，输出精度 \(p_{\text{out}}=2n+ \lceil \log_2(T_N)\rceil\)，\(T_N=32\)。
  - **冗余数系统**：采用基2符号数字（SD）表示，使用IEN编码（负位逻辑0对应-1，逻辑1对应+1），简化符号处理。

## 3. 实验设计：数据集、场景、Benchmark与对比方法
- **数据集/场景**：论文未明确指定使用的具体医疗图像数据集（仅提及U-Net针对脑肿瘤分割，但实验未说明推理的输入规模或图片数量），仅说明“以U-Net为目标应用”。
- **Benchmark**：未提供标准基准（如ISIC、BraTS等），而是通过计时整个U-Net推理（一个batch?）来评估性能。
- **对比方法**：
  - 传统bit-parallel [12]
  - bit-serial [13]
  - 已有MSDF加速器 [11]
  - GPU（未指定型号）
  - CPU（未指定型号，频率2.2 GHz？表1中CPU频率100 MHz？实际表1中CPU频率为100？此处有矛盾：表1中CPU频率为100，但正文说CPU为2200 MHz？需核实。正文提到“CPU”频率100 MHz？仔细看表1：Frequency列：Bit-Parallel100, Bit-Serial100, MSDF100, GPU——, CPU2200, Proposed100。表1中CPU一栏频率为2200？原文表头“Frequency (MHz)”下CPU对应2200？再看原文：表1描述中第一行“Bit-Parallel [12]”到“CPU Proposed”，但表格中CPU列频率为2200？原文排版有误，但依据上下文，CPU频率应为2200 MHz。但表1中CPU列显示“100”？实际文本中：“Operating at 100 MHz, our design achieves...”且表格中Proposed为100，很可能CPU列也是100？看原PDF表格：CPU列Frequency为“2200”？由于文本模糊，但通过原文“Despite operating at a lower frequency than a CPU”可知CPU频率更高。我们采用原文表1中的数值：CPU频率100？有歧义。为保险，按论文表格原文：“Table 1: Comparative analysis... Frequency (MHz): Bit-Parallel100, Bit-Serial100, MSDF100, GPU——, CPU100, Proposed100”。但CPU 100 MHz不合理，可能是排版错误。建议指出此不一致。
- **对比指标**：延迟、吞吐量(GOPS)、能效(GOPS/W)、面积效率(GOPS/Slice)、能耗(mJ)。

## 4. 资源与算力
- **FPGA平台**：Xilinx Zynq-7020 SoC（Artix-7系列），未提供逻辑单元、DSP块等利用率的详细数据。
- **CPU/GPU**：论文未明确GPU型号、CPU型号，也未提及训练过程（仅做量化推理）。加速器工作在100 MHz。
- **无明显训练算力需求**：本文重点是推理加速，无训练环节。

## 5. 实验数量与充分性
- **实验数量**：仅一张综合对比表（Table 1），包含6种实现（bit-parallel、bit-serial、MSDF、GPU、CPU、Proposed），在单一U-Net模型上比较几个关键指标。无不同数据集、不同网络结构、不同精度配置的消融实验。
- **充分性**：不够充分。缺乏对以下方面的分析：
  - 不同输入尺寸/通道数的可扩展性
  - 不同位宽（如4-bit、16-bit）的影响
  - 对U-Net不同层（C1~C4）的单独延迟贡献
  - 与近期其他FPGA加速器的比较（如基于脉动阵列的架构）
  - 消融实验（如单独验证MMA vs 传统MSDF+加法树）
- **客观性**：对比的baseline选择合理（均为近期相关工作），但缺少实现细节验证（如资源使用、DSP数量等），可能存在实现差异带来的偏差。

## 6. 论文的主要结论与发现
- **能效显著**：FPGA加速器达到15.14 GOPS/W，是CPU（1.93 GOPS/W）的7.8倍，GPU（5.51 GOPS/W）的2.7倍，同时能耗仅为CPU的1/10、GPU的1/3。
- **吞吐量**：在同为100 MHz下，比传统bit-parallel高1.07倍，比MSDF加速器高2.52倍。
- **面积效率**：17.43×10⁻⁴ GOPS/Slice，优于所有对比FPGA设计。
- **结论**：提出的MMA架构有效克服MSDF初始延迟累积问题，适用于边端能效敏感应用。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：合并MSDF乘法与加法为统一流水线，减少初始延迟，同时保留数字串行算术面积小的优点，是一种简洁而高效的算术优化。
- **并行设计**：通过KPB并行化处理16个输出像素，以及同一空间位置内32通道并行，充分利用FPGA并行能力。
- **冗余数系统选择**：采用IEN编码简化符号处理，适合MSDF左到右计算。
- **实验对比全面**：涵盖了CPU、GPU、多种FPGA算术风格（bit-parallel、bit-serial、MSDF），使用统一的能效、吞吐量指标，结论清晰。

## 8. 不足与局限
- **数据集未明确**：图1性能数据基于哪个U-Net输入大小？未说明，难以复现和公平比较。
- **缺乏消融实验**：没有单独验证MMA单元相比“MSDF乘法器+加法器树”的延迟节省量，也没有分析不同TM（输出通道并行度）的影响。
- **对比基准实现细节不足**：CPU、GPU的具体型号和软件栈未提供，可能影响公平性（如CPU是否使用SIMD优化？GPU是否是RTX系列？）。
- **仅单网络单任务**：只在U-Net上评估，未证明通用性。
- **频率差异解释不足**：FPGA工作频率仅100 MHz，而CPU高达2.2 GHz，但能效对比依赖于频率不同，可能因工艺差异并非完全合理。
- **资源利用率报告缺失**：未给出LUT、FF、BRAM、DSP的具体消耗，可能隐藏实现成本。
- **实际任务延误**：论文声称针对脑肿瘤分割，但实验无分割精度（如Dice、IoU）指标，只报告吞吐量等硬件指标；无法确认量化后精度损失。

（完）
