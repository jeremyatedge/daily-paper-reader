---
title: "Efficient Network Inference via Hardware-Aware Architecture Search, Model Pruning & Quantization"
title_zh: 通过硬件感知架构搜索、模型剪枝与量化实现高效网络推理
authors: "Lucas Heublein, Mark Deutel, Axel Plinge, Felix Ott"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23210v1"
tags: ["query:aiot"]
score: 9.0
evidence: 硬件感知神经架构搜索、剪枝和量化用于资源受限嵌入式系统的高效推理
tldr: 嵌入式GNSS干扰监测需要在资源受限硬件上实时处理大量IQ样本，但深度网络的高性能与低部署成本存在矛盾。本文结合迭代结构化剪枝、训练后静态量化和硬件感知零样本NAS，以MCUNet为基线压缩并优化网络。在GNSS干扰数据集上，模型体积和计算量显著降低，分类与表征性能保持。实验在iMXRT1062 MCU、Raspberry Pi Zero 2W和Pi 5上验证，为实时干扰监测提供了实用的紧凑模型开发指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23210v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 915, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 709, \"height\": 303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 743, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 925, \"height\": 606, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1876, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 926, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23210v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 931, \"height\": 454, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23210v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 312, \"label\": \"Table\"}]"
motivation: 嵌入式GNSS干扰监测需在实时处理中平衡深度网络的预测性能与硬件资源限制。
method: 采用迭代结构化剪枝、训练后静态量化及硬件感知零样本NAS，以MCUNet为基线进行压缩与架构搜索。
result: 在GNSS干扰数据集上，模型尺寸和计算复杂度大幅降低，分类与表征性能稳定；三个嵌入式平台验证有效。
conclusion: 该组合策略为资源受限平台上的实时GNSS干扰监测模型设计提供了实用指导。
---

## 摘要
嵌入式全球导航卫星系统（GNSS）干扰监测需要快速且内存高效的推理，以实时处理大量原始同相和正交（IQ）样本。同时，需要表达力日益增强的深度神经网络（DNN）来在多样化的信号条件下进行稳健的干扰分类和表征。这就在预测性能与在资源受限硬件上的可部署性之间形成了根本性的矛盾。本文研究了利用迭代结构化剪枝、训练后静态量化和硬件感知零样本神经架构搜索（NAS）实现GNSS干扰表征的高效网络推理。以MCUNet作为紧凑基线，我们分析了模型压缩和自动化架构优化如何影响模型大小、计算复杂度和内存使用，同时保持任务性能。在涵盖分类和广义表征的GNSS干扰数据集上的实验表明，将压缩与硬件感知设计相结合对嵌入式部署具有益处。我们的结果为在嵌入式平台（iMXRT1062 MCU、树莓派Zero 2W和树莓派5）上开发用于实时GNSS干扰监测的紧凑机器学习（ML）模型提供了实用指南。

## Abstract
Embedded global navigation satellite system (GNSS) interference monitoring requires fast and memory-efficient inference to process large volumes of raw in-phase and quadrature (IQ) samples in real time. At the same time, increasingly expressive deep neural networks (DNNs) are needed for robust interference classification and characterization across diverse signal conditions. This creates a fundamental tension between predictive performance and deployability on resource-constrained hardware. In this paper, we investigate efficient network inference for GNSS interference characterization using iterative structured pruning, post-training static quantization, and hardware-aware zero-shot neural architecture search (NAS). Starting from MCUNet as a compact baseline, we analyze how model compression and automated architecture optimization affect model size, computational complexity, and memory usage while maintaining task performance. Experiments on a GNSS interference dataset, covering both classification and generalized characterization, show the benefits of combining compression and hardware-aware design for embedded deployment. Our results provide practical guidance for developing compact machine learning (ML) models for real-time GNSS interference monitoring on embedded platforms (iMXRT1062 MCU, Raspberry Pi Zero 2W, and Raspberry Pi 5).

---

## 论文详细总结（自动生成）

# Efficient Network Inference via Hardware-Aware Architecture Search, Model Pruning & Quantization 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：嵌入式GNSS干扰监测需要在资源受限的硬件上实时处理大量IQ样本，而日益先进的深度神经网络（DNN）在提升分类/表征性能的同时，与嵌入式平台的存储、计算、能耗约束存在根本矛盾。如何在保持预测性能的前提下，实现模型的高效部署？
- **研究背景**：GNSS接收机在定位、导航、授时等关键基础设施中至关重要，干扰监测需要快速处理高维度谱图。现有工作或依赖专用硬件（如Google Edge TPU），或仅考虑单一压缩手段，缺乏将剪枝、量化与硬件感知架构搜索联合优化的系统性研究。
- **整体含义**：本文系统探索了迭代结构化剪枝、训练后静态量化、硬件感知零样本NAS三种技术组合，以MCUNet为基线，在GNSS干扰分类（7类）和细粒度表征（311类）两个任务上验证了压缩与自动化架构设计对嵌入式部署的可行性，并提供了跨平台（MCU、SBC）的实用指南。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
通过联合优化网络架构和压缩策略（剪枝率、量化位宽），在满足目标硬件资源约束（RAM、ROM、FLOPs）的前提下，最大化模型预测性能。采用“先压缩再搜索”两条分支（见图1）：
- **左分支**：对MCUNet基线应用迭代结构化剪枝 + 训练后静态量化，研究单一全局剪枝率的影响。
- **右分支**：使用零样本NAS（PrototypeNAS）同时搜索CNN骨干网络、超级块结构与剪枝配置，并利用零样本代理指标（MeCo、NASWOT、SNIP、ZiCo）在无需训练的条件下评估候选架构，最后选出5个最佳模型训练并部署。

### 关键技术细节
1. **迭代结构化剪枝**：不是一次性剪枝，而是分多个阶段逐步移除卷积层中的整个滤波器/线性层中的整行，从而避免稀疏张量，无需专用硬件支持。剪枝率从0%到90%以10%步进。
2. **训练后静态量化**：将权重从浮点数映射到无符号8位整数（仿射映射，含可学习的scale和zero-point），使用200个校准样本确定映射参数。
3. **硬件感知零样本NAS（PrototypeNAS）**：
   - **搜索空间**：包含MobileNetV2、PrototypeMbedNet、ResNet、SqueezeNet等骨干网络，以及可调超级块和剪枝配置。
   - **优化目标**：最大化零样本代理指标的集成，同时最小化FLOPs，并加入RAM/ROM约束。
   - **零样本代理**：MeCo（特征图相关性）、NASWOT（激活空间可分性）、SNIP（参数敏感性）、ZiCo（梯度变异系数）。
   - **流程**：多目标优化获得Pareto前沿 → 超体积子集选择 → 选出Top-5候选 → 训练100轮 → 评估。
4. **训练配置**：
   - MCUNet剪枝：SGD，学习率0.01（第60/80轮衰减0.1），动量0.9，batch size 64，100轮。
   - PrototypeNAS模型：SGD，学习率0.001，动量0.9，batch size 48，100轮。

## 3. 实验设计：数据集、基准、对比方法

### 数据集
- **Flexiband-7**：7类干扰分类（无干扰、Chirp、跳频、噪声、调制、多音、脉冲），共114081训练样本 + 30097测试样本（80/20划分）。
- **Flexiband-311**：311类细粒度表征（包含干扰类型、调制方式、扫频轮廓、速率、带宽等参数），样本量相同，类别分布不均（如Pulsed类105个，Modulated类仅9个）。
- **输入**：512×512的FFT谱图（IQ采样率40 MHz，中心频率1.57542 GHz，时长3ms）。

### 基准
- **基线模型**：MCUNet（未剪枝，592925参数）。
- **对比对象**：
  - 不同剪枝率（0%~90%的MCUNet）。
  - PrototypeNAS选出的5个候选模型（分别基于MobileNetV2、PrototypeMbedNet、ResNet、SqueezeNet等）。
  - 浮点精度 vs. 8位量化精度。
  - 在不同嵌入式平台（iMXRT1062 MCU/Teensy、Raspberry Pi Zero 2W、Raspberry Pi 5）上的能耗和延迟。

### 对比方法
- 未明确列出其他论文方法作为直接对比，主要比较自身不同配置（剪枝率、NAS候选、量化前后）的性能与资源开销。部分相关工作中提到了LiteJam、GAC-KAN等紧凑模型，但未在实验中与之比较。

## 4. 资源与算力

- **GPU信息**：论文**未明确说明**使用的GPU型号、数量及训练总时长。仅提到训练轮数（100 epoch）和batch size，但未给出硬件配置。
- **训练成本**：PrototypeNAS本身无需训练即可筛选架构（零样本），仅最终5个候选需要训练，且训练时间因架构而异，但未提供具体数值。
- **部署资源**：给出了三个嵌入式平台上的能量（mJ）和延迟（ms）测量结果，但未说明测量时的功率细节（仅提及各平台典型功耗：RasPi 5 ~5.1W，Zero 2W ~1.8W，Teensy ~0.48W）。

## 5. 实验数量与充分性

### 实验数量
- **剪枝实验**：10个剪枝率（0%~90% step 10%），每个在分类任务上报告参数数和准确率。
- **NAS实验**：PrototypeNAS产生大量候选（图中可见众多点），最终选出5个模型，在分类和表征两个任务上分别训练并评估。
- **量化实验**：对5个NAS模型分别报告浮点精度和量化精度。
- **部署实验**：对3个模型（索引0、3、4）在3个嵌入式平台上测量能量和延迟，共9组数据。
- **总实验组数**：约10（剪枝）+ 2×5（NAS模型两个任务）+ 3×3（部署）= 29组核心结果，外加消融分析（如代理指标对比、剪枝对层参数分布的影响等）。

### 充分性与公平性
- **优点**：覆盖了两种任务难度（7类 vs 311类），验证了剪枝、量化、NAS各自及组合的效果；使用了三个不同算力级别的嵌入式平台，提供了实际部署数据。
- **不足**：
  - 缺乏与现有紧凑模型（如LiteJam、GAC-KAN）的直接对比，使得绝对性能优势难以评估。
  - 仅使用了自建室内数据集，未在公开GNSS干扰基准（如JSNR、Jamming分类数据集）上验证，泛化性存疑。
  - 训练配置（学习率、优化器）未对不同架构进行调优，可能未达到各自最优性能。
  - 剪枝实验仅针对MCUNet，未在其他骨干网络上进行剪枝对比。
  - 量化仅使用200个校准样本，可能不充分（尤其对于311类任务），但未分析校准集大小的影响。
  - 部署实验仅测了3个模型，且未说明为何选择模型0、3、4而非全部5个。

## 6. 论文的主要结论与发现

1. **MCUNet剪枝有效性**：70%剪枝率下准确率仅从99.24%降至98.28%（分类任务），参数从592K降至82K；80%剪枝仍保持97.62%；90%剪枝才大幅下降至93.57%。表明网络存在大量结构冗余。
2. **NAS的收益**：PrototypeNAS找到的模型在相似或更低FLOPs下，分类准确率接近100%，表征任务约95%，显著优于同FLOPs下的固定剪枝MCUNet。量化后大部分模型精度损失极小（模型2略显敏感）。
3. **资源权衡**：不同NAS候选在计算量、ROM、RAM上差异巨大（从几十MFLOPs到2.5GFLOPs），但并非最贵的模型精度最高，体现了硬件感知搜索的价值。
4. **部署可行性**：即使512×512的输入，三个平台均能运行（RasPi 5延迟约161ms，Zero 2W约667ms，Teensy约1752ms）。虽然无法实现3ms的连续监测，但可支持每秒约5次的周期性检查，足以检测多数干扰。
5. **能耗悖论**：更小的系统（如Teensy）不一定能耗更低，因为延迟过长导致总能量与大型系统相当或更高（Teensy约588mJ，Zero 2W约877mJ，RasPi 5约560mJ），但空间/成本限制可能仍倾向小系统。

## 7. 优点

- **系统性与实用性**：将剪枝、量化、NAS三者有机结合，并考虑了硬件资源约束（RAM、ROM），输出可直接部署的紧凑模型，具有工程指导意义。
- **零样本NAS的引入**：避免了对大量候选架构的完整训练，大幅降低搜索成本，适合资源有限的场景。
- **多平台实测**：从MCU到SBC的三种典型嵌入式平台均进行了能耗和延迟测量，结果具现实参考价值。
- **细粒度表征任务**：311类的难度高于常见分类，更能体现压缩对复杂任务的性能影响。
- **结构化剪枝**：与元素级剪枝相比，无需稀疏运算支持，更适配现有硬件和框架。

## 8. 不足与局限

- **对比缺失**：未与现有轻量级GNSS干扰模型（如LiteJam、GAC-KAN）进行直接比较，无法证明方法的相对优势。
- **数据集局限性**：仅在单一室内环境自采数据集上实验，缺乏多场景、多接收机、多种干扰功率/信噪比下的泛化验证。
- **训练配置未优化**：所有NAS候选采用相同超参数（lr=0.001，100 epoch），未针对具体架构进行调优，可能低估了某些模型的潜力。
- **量化校准集偏小**：仅200张样本，对于311类任务可能不足以覆盖所有类别，量化精度评估可靠性存疑。
- **剪枝策略局限性**：全局统一剪枝率，未探索逐层敏感度差异化剪枝，可能进一步减少性能损失。
- **缺乏推理延迟的实时性分析**：未讨论实际应用场景中的抖动、批处理、流水线等优化，仅给出单次延迟平均值与标准差。
- **能耗测量方法简略**：仅说明使用Joulescope和GPIO toggling，但未提供测量电路细节或误差来源分析。
- **可复现性不足**：未公开代码、预训练模型或数据集，无法独立验证结果。

（完）
