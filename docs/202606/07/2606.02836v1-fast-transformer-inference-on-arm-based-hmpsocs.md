---
title: Fast Transformer Inference on ARM-Based HMPSoCs
title_zh: 基于ARM的HMPSoC上快速Transformer推理
authors: "Hang Xu, Yixian Shen, Thanassis Giannetsos, Anuj Pathania"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02836v1"
tags: ["query:aiot"]
score: 9.0
evidence: 在ARM嵌入式板上实现Transformer推理加速高达3倍
tldr: "基于ARM的HMPSoC设备上Transformer推理面临延迟高、缺乏原生支持的问题。本文在ARM Compute Library中新增Transformer内核，实现纯CPU推理相比现有方案加速达3倍。进一步提出CPU-GPU协同推理策略，将访存密集型操作放在CPU、计算密集型放在GPU，在最小开销下将延迟再降低15.72%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02836v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1602, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1740, \"height\": 922, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 738, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 911, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 914, \"height\": 416, \"label\": \"Figure\"}]"
motivation: Transformer模型在资源受限的边缘设备上部署困难，ARM-CL框架缺乏对Transformer推理的原生支持。
method: 在ARM-CL中实现多个Transformer新内核，并设计CPU-GPU协同执行策略，按操作类型分配处理器。
result: "扩展ARM-CL实现Transformer推理加速最高3倍；协同推理相比单处理器最优方案再降延迟15.72%。"
conclusion: 本工作为ARM边缘设备提供高效Transformer推理方案，验证了异构协同计算的潜力。
---

## 摘要
Transformer模型在机器学习任务中树立了新的性能标准。然而，在资源受限的边缘设备上进行无云端、片上Transformer推理的资源密集型部署仍然具有挑战性。ARM计算库（ARM-CL）框架在基于ARM的边缘设备上提供低延迟CNN推理，但缺乏对Transformer推理的支持。在本工作中，我们在ARM-CL中实现了多个新的Transformer内核，以支持原生Transformer执行。与基于ARM的嵌入式板上的最先进CPU/GPU实现相比，我们扩展的ARM-CL实现了高达三倍的Transformer推理加速。此外，为边缘设备提供动力的异构多处理器片上系统（HMPSoC）同时提供嵌入式CPU和GPU。我们引入了协同CPU-GPU Transformer推理，它在CPU上执行内存密集型操作，同时利用GPU进行高度可并行化、计算密集型操作。这种协同执行开销极小，与ARM-CL上最佳单处理器推理相比，进一步将Transformer推理延迟降低了高达15.72%。

## Abstract
Transformer models have set new performance standards for machine learning (ML) tasks. However, their resource-intensive deployment on resource-constrained edge devices for cloud-free, on-chip transformer inference remains challenging. The ARM Compute Library (ARM-CL) framework provides low-latency CNN inference on ARM-based edge devices but lacks support for transformer inference. In this work, we implement several new transformer kernels in ARM-CL to support native transformer execution. Our extended ARM-CL achieves up to three times faster transformer inference compared to state-of-the-art CPU/GPU implementations on an ARM-based embedded board.   Furthermore, heterogeneous multi-processor system-on-chips (HMPSoCs) powering edge devices provide both embedded CPUs and GPUs. We introduce cooperative CPU-GPU transformer inference, which executes memory-intensive operations on the CPU while utilizing the GPU for highly parallelizable, compute-intensive operations. This cooperative execution, implemented with minimal overhead, further reduces transformer inference latency by up to 15.72% compared to the best single-processor inference on ARM-CL.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：Transformer 模型在资源受限的边缘设备（如 ARM 嵌入式平台）上进行片上推理时，面临高延迟和缺乏原生支持的问题。尽管 ARM Compute Library (ARM-CL) 在 CNN 推理上性能优异，但它不包含 Transformer 所需的运算内核（如自注意力、层归一化等）。
- **背景**：现有方案如 TVM 和 ExecuTorch 无法充分利用 ARM Mali GPU，存在性能瓶颈；且没有针对 Transformer 的 CPU-GPU 协同执行研究。
- **意义**：实现低延迟、无云端依赖的 Transformer 推理对于边缘 AI 应用（隐私、响应速度、可靠性）至关重要。

## 2. 方法论
- **核心思想**：在 ARM-CL 中新增 Transformer 原生内核，并利用 HMPSoC 的 CPU 和 GPU 性能异质性，设计层切换协同策略：将访存密集型层（Embedding、SDPA、Add&Norm）交由 CPU 执行，将计算密集型层（Attention Linear、FF）交由 GPU 执行。
- **关键技术细节**：
  - 实现四个新内核：Embedding、Multi-Head Attention (MHA) 中的线性变换、Scaled Dot-Product Attention (SDPA)、Layer Normalization。
  - CPU 内核使用 NEON SIMD 指令优化，GPU 内核使用 OpenCL 并行计算（如 tiled MMUL）。
  - 利用 HMPSoC 支持的共享内存空间，在 CPU-GPU 层切换时采用“多目标共享张量”，避免显式 memcpy 开销。
  - 采用“upscale”方式：将 CPU 张量提升为 GPU 张量，分配在可被 CPU 直接访问的 GPU 内存区域。
  - 根据微观基准测试（不同层在 CPU 和 GPU 上的运行时间比例 TCP_U/T_GPU）决定各层的处理器分配。
- **算法流程**（文字描述）：
  - 输入模型结构描述（DAG），包含操作和连接关系。
  - 配置阶段：为每个层分配处理器（CPU 或 GPU），在层切换处创建共享张量。
  - 运行阶段：按顺序执行各层，连续同处理器层使用单目标张量；切换处理器时，通过共享张量直接访问，无需 memcpy。

## 3. 实验设计
- **硬件平台**：Khadas VIM 3 BASIC，搭载 Amlogic A331D HMPSoC（12 nm，ARM big.Little CPU：四核 Cortex-A73 2.2GHz + 双核 Cortex-A53 1.8GHz，GPU：四核 Mali G52 0.8GHz），2 MB L2 缓存（CPU），128 KB L2 缓存（GPU），DRAM 2–4 GB，运行 Ubuntu 22.04。
- **基准模型**：五种 Transformer 架构：BERT-base、DistilBERT、MobileBERT、SqueezeBERT、GPT-2。均使用默认隐藏维度，固定 token 长度 32（对比实验）或可变长度（微观基准）。SqueezeBERT 需额外实现卷积内核。
- **对比方法**：
  - 单处理器：CPU 推理 vs GPU 推理 vs TVM v0.18、ExecuTorch（仅 CPU）。
  - 多处理器：CPU-GPU 层切换 vs 最佳单处理器（CPU 或 GPU）。
- **评估指标**：推理延迟（毫秒）。

## 4. 资源与算力
- **训练资源**：论文未涉及训练，只关注推理。没有提及任何 GPU 训练型号、数量或时长。
- **推理硬件**：嵌入式 ARM CPU 和 Mali GPU，不涉及高性能 GPU。实验在单一嵌入式开发板上完成。

## 5. 实验数量与充分性
- **实验组数**：
  - **微观基准测试**：覆盖不同 hidden dimension (d_model=192~960) 和 token 长度 (L=16~512)，共约 6×5=30 种组合，分析 5 种层的性能比。
  - **单处理器对比**：对 5 种模型，分别测试 CPU/GPU 在 TVM、ExecuTorch、ARM-CL 上的延迟，共约 5×3=15 组数据（不含 ExecuTorch GPU）。
  - **多处理器对比**：对 5 种模型，比较纯 CPU、纯 GPU、CPU-GPU 层切换的延迟，共 5×3=15 组。
  - **消融分析**：通过微观基准和层分配解释性能差异，但无显式消融实验（如移除共享内存、不同分配策略等）。
- **充分性与公平性**：
  - 对比了主流的编译器（TVM）和框架（ExecuTorch），但 TVM 版本较旧（v0.18）；ARM-CL 版本为 v21.02。
  - 实验在一个平台上完成，结论可能受平台特征影响（如 GPU L2 缓存大小）。
  - 微观基准覆盖了关键参数，但未验证实时性、功耗、精度等指标。
  - 总体来说，实验设计合理，但样本量有限，且缺乏跨平台验证。

## 6. 主要结论与发现
- 扩展的 ARM-CL 在 CPU-only 和 GPU-only 推理上比 TVM 平均加速 2.34× 和 2.23×，比 ExecuTorch（CPU）也有提升。
- CPU-GPU 层切换推理相比最佳单处理器（某些模型为 GPU，某些为 CPU）延迟降低最高 15.72%，平均 10.95%。
- GPU 在长序列（L≥128）时性能下降，归因于 L2 缓存不足（128 KB vs CPU 2 MB）。
- 嵌入式 CPU 更适合访存密集层（Embedding、SDPA、Add&Norm），GPU 更适合计算密集层（Attention Linear、FF）。

## 7. 优点
- **方法创新**：首次在 ARM-CL 中实现 Transformer 原生内核，并设计了低开销的 CPU-GPU 协同推理策略。
- **硬件利用**：充分利用 HMPSoC 的共享内存机制，避免显式数据拷贝，减少通信开销。
- **普适性**：提出的层分配原则可推广到其他 HMPSoC 平台。
- **开源贡献**：代码在 MIT 许可下公开，便于复现和扩展。
- **实验设计**：微观基准测试深入揭示了层级性能异质性，为分配决策提供了依据。

## 8. 不足与局限
- **硬件平台单一**：仅在一款开发板（Amlogic A331D）上测试，GPU L2 缓存较小（128 KB），可能限制长序列下的 GPU 收益。结论对其他 ARM 平台（如更大的 GPU 缓存）泛化性存疑。
- **未涉及模型压缩**：未结合量化、剪枝或蒸馏等压缩技术，仅针对全精度模型。
- **缺乏能耗和精度评估**：只关注延迟，未测量功耗或验证推理精度是否完全保持（虽然内核数学等价，但浮点实现可能引入细微差异）。
- **对比基线有限**：与 TVM 和 ExecuTorch 对比，但未与更优化的手写内核或 ONNX Runtime 等比较。
- **实验数量不足**：仅 5 种模型、固定 token 长度（32）下的主要对比，长序列性能只通过微观基准展示，未在完整模型上验证。
- **同步开销**：层切换时虽然无 memcpy，但仍需同步 CPU-GPU 执行，论文未详细分析同步对端到端延迟的影响。
- **仅推理，不涉及训练**：论文聚焦推理，但未探讨该方案在训练阶段的适用性。

（完）
