---
title: "OpenEye: A Scalable Open-Source Hardware Accelerator for DNNs"
title_zh: OpenEye：一种可扩展的开源深度神经网络硬件加速器
authors: "Denis Lebold, Hendrik Wöhrle"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01450v1"
tags: ["query:aiot"]
score: 9.0
evidence: 面向嵌入式平台的可扩展稀疏感知FPGA硬件加速器
tldr: 深度神经网络推理计算复杂度高，嵌入式硬件加速面临可扩展性挑战。OpenEye提出一种可参数化的FPGA加速器架构，基于处理元件簇和流式数据流，原生支持稀疏权重与激活。在Xilinx ZU19EG上实现多种配置，路由开销近线性增长，性能与资源消耗取得良好权衡。该开源设计可灵活适配不同资源约束，为高效可扩展的DNN加速提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01450v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1533, \"height\": 909, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01450v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1569, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01450v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1600, \"height\": 882, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01450v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1595, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01450v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1681, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01450v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1344, \"height\": 1064, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01450v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1723, \"height\": 1384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01450v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1480, \"height\": 596, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01450v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1260, \"height\": 1068, \"label\": \"Table\"}]"
motivation: 高效执行深度神经网络推理亟需可扩展且资源高效的FPGA加速器，以应对嵌入式平台的计算和资源限制。
method: 提出OpenEye，基于可参数化处理元件簇和流式数据流架构，原生支持稀疏权重与激活，实现近线性路由开销可扩展。
result: 在Xilinx ZU19EG上实现多种配置，卷积、全连接等操作显示良好性能-资源权衡，可扩展性接近线性。
conclusion: OpenEye作为开源可扩展加速器，通过参数化设计适应不同资源约束，为嵌入式DNN加速提供高效方案。
---

## 摘要
深度神经网络推理的计算复杂性日益增加，对嵌入式平台上高效硬件加速提出了重大挑战，尤其是在资源消耗和可扩展性方面。本文提出了OpenEye，一种可扩展且支持稀疏性的基于FPGA的硬件加速器，旨在高效执行常见神经网络操作，如卷积、全连接层和池化。OpenEye基于高度可参数化的架构，由通过基于流的数据流互连的处理单元簇组成。本文详细解释了加速器的内部操作，包括数据移动、缓冲策略、控制逻辑以及簇与处理单元之间的协调。该架构原生支持稀疏权重和激活，能够在不进行不必要计算或内存访问的情况下高效处理稀疏数据。OpenEye的一个关键设计特性是其可扩展性：可以改变簇和处理单元的数量，以适应不同的性能和资源约束。该设计实现了路由和互连开销随处理单元数量增加而接近线性增长，这对于在大规模FPGA器件上保持效率至关重要。为了评估不同设计点的可扩展性，在Xilinx ZU19EG FPGA上实现了多种不同簇大小和处理单元大小的OpenEye配置。使用代表性的神经网络操作，包括卷积、全连接和池化层，分析资源利用率、执行延迟和可扩展性行为。结果表明，在所探索的配置中，性能与资源消耗之间存在有利的权衡。

## Abstract
The increasing computational complexity of deep neural network inference poses significant challenges for efficient hardware acceleration on embedded platforms, particularly with respect to resource consumption and scalability. This work presents OpenEye, a scalable and sparsity-aware FPGA-based hardware accelerator designed to efficiently execute common neural network operations such as convolutions, dense layers, and pooling.   OpenEye is based on a highly parameterizable architecture composed of clusters of processing elements interconnected by a streaming-based dataflow. The paper provides a detailed explanation of the internal operation of the accelerator, including data movement, buffering strategies, control logic, and the coordination between clusters and PEs. The architecture natively supports sparse weights and activations, enabling the efficient processing of sparse data without unnecessary computations or memory accesses.   A key design property of OpenEye is its scalability: the number of clusters and processing elements can be varied to adapt the accelerator to different performance and resource constraints. The design achieves a near-linear scaling of routing and interconnect overhead with increasing PE counts, which is essential for maintaining efficiency on large FPGA devices.   To evaluate scalability across different design points, multiple OpenEye configurations with varying cluster and PE sizes were implemented on a Xilinx ZU19EG FPGA. Representative neural network operations, including convolutional, fully connected, and pooling layers, were used to analyze resource utilization, execution latency, and scalability behavior. The results show favorable trade-offs between performance and resource consumption across the explored configurations.

---

## 论文详细总结（自动生成）

# 论文总结：OpenEye: A Scalable Open-Source Hardware Accelerator for DNNs

## 1. 核心问题与研究背景
- **研究动机**：深度神经网络（DNN）推理计算复杂度日益增长，在嵌入式平台上实现高效硬件加速面临资源消耗和可扩展性的双重挑战。现有加速器评估常忽视数据移动（而非计算）带来的延迟与能耗，且多数ASIC级设计抽象了接口与内存成本，难以反映FPGA实现的实际开销。
- **核心问题**：如何设计一种**可扩展、稀疏感知、且明确考虑数据传输和内存访问开销**的FPGA硬件加速器，使其能在不同资源约束下高效执行卷积、全连接、池化等常见操作。
- **整体含义**：OpenEye是一种开源、参数化、基于处理单元（PE）簇的流式数据流架构，通过近线性扩展的路由开销和原生稀疏支持，桥接理想化计算评估与真实系统性能之间的差距，为嵌入式平台提供实用可行的DNN加速方案。

## 2. 方法论：核心思想与关键技术细节
- **整体架构**：
  - 采用**串行前端 + 并行后端**拓扑：前端通过AXI/Wishbone接口负责外部数据传输与配置；后端由多个可互连的OpenEye簇组成，各簇包含**PE簇**、激活函数单元、可配置全局缓冲及专用路由器。
  - PE簇为**二维可缩放阵列**（独立X、Y维度），内部PE通过专用总线传输激活（IACT总线）、权重（水平传播）和部分和（PSUM，垂直累积）。
- **稀疏处理机制**：
  - 输入激活和权重以稀疏形式传输，分别编码到独立的地址RAM和数据RAM，实现零值跳跃，避免不必要的计算与访存。
  - 稀疏编码方案借鉴Eyeriss v2的压缩稀疏列格式，但OpenEye在每PE内部使用专用稀疏解码硬件。
- **路由器与数据流**：
  - 每个簇内配置**独立路由器**：激活可自由路由（支持对角线传输实现任意步长），权重仅水平通信，部分和仅垂直双向通信。
  - 路由器采用**ready/enable握手协议**实现同步，配置由顶层控制逻辑动态调整以适应不同网络层映射。
- **PE内部设计**：
  - 每个PE包含乘法-累加单元，支持SIMD参数化扩展（增加乘法器/加法器）以提高吞吐量。
  - PSUM存储使用**Live Value Table (LVT)** 技术，以双端口RAM模拟多端口访问，适配FPGA内存约束。
- **可扩展性保证**：
  - 簇和PE的数量可独立配置，路由与互连开销**近线性增长**（经验证无超级线性开销），使架构能适应从小型到大型FPGA器件。

## 3. 实验设计
- **数据集与基准（Benchmark）**：
  - 使用**MNIST**手写数字识别数据集，输入为28×28灰度图像。
  - 测试网络架构：3个卷积层（3×3 kernel，same padding，通道16→32→32）、2个最大池化层（2×2，stride 2）、2个全连接层（32→10）。网络约2.13百万次操作/推理，8-bit量化。
- **硬件平台**：
  - Xilinx ZU19EG FPGA，时钟频率200 MHz。
- **对比方法**：
  - **无直接端到端性能对比其他加速器**。论文在Table 1中与Eyeriss、Eyeriss v2、EIE、Cnvlutin、FINN-R、Pflow、VCONV等进行了特性比较（可扩展性、稀疏支持、内存感知、接口感知等），但实验部分仅评估OpenEye自身不同配置，未在同一基准上跑其他加速器。
- **实验设置**：
  - 改变**簇行数（CLUSTER ROWS）** 和**PE X/Y维度**（即PE X-Axis和PE Y-Axis），共记录多种配置（见表3：3种PE形状×多个簇行数）。
  - 测量资源利用率（CLB、BRAM、DSP）、执行时间（数据发送、处理、总时间）及吞吐量（MOPS Processing / Total）。

## 4. 资源与算力
- **算力描述**：文中未使用GPU，而是FPGA资源。所有配置**时钟200 MHz**，采用Xilinx ZU19EG FPGA（属于Xilinx UltraScale+系列）。
- **资源消耗示例**：最大配置（CLUSTER ROWS=8，PE X=4，PE Y=4）使用约4035 CLB、85 BRAM、135 DSP（从Figure 5读取近似值）。
- **未提及**：训练时长、GPU型号或数量，因为OpenEye专注于**推理加速**，网络已预训练并量化。

## 5. 实验数量与充分性
- **实验组数**：表3列出了12组配置（3种PE形状 × 4种簇行数），每组测量了数据发送时间、处理时间、总时间、MOPS等。Figure 5展示资源利用随簇行数的线性趋势。
- **充分性分析**：
  - **优势**：覆盖了不同PE阵列形状和规模，验证了资源近线性扩展和性能瓶颈（数据传输成为主导），结果清晰。
  - **局限**：
    - 仅使用**单一MNIST网络**，未在更复杂模型（如ImageNet分类、目标检测）上验证；
    - **未与其他加速器进行直接性能对比**（如与Eyeriss v2在相同网络和FPGA上的运行结果），仅特性对比；
    - 未做**消融实验**（如关闭稀疏支持、改变量化精度）；
    - 未评估**能量效率**（虽有提及但未提供实测功率/能耗数据）；
    - 未分析不同稀疏度对性能的影响。
  - **客观性**：实验设计合理，但结论需在更广泛网络和硬件条件下验证。

## 6. 主要结论与发现
1. **资源近线性扩展**：CLB、BRAM、DSP使用量随簇行数严格线性增长，无路由拥塞或BRAM碎片化导致的拐点，验证了架构的可预测扩展能力。
2. **吞吐量受数据传输限制**：处理时间（MOPS Processing）接近理想线性，但总时间（MOPS Total）增益递减，因为数据发送时间占比随簇数增加而增大，通信成为瓶颈。
3. **PE Y维度收益有限**：对于以3×3卷积为主的网络，增加垂直方向PE仅在全连接层有显著收益，整体贡献小，强调网络拓扑与架构匹配的重要性。
4. **资源效率良好**：在8-bit量化下，最大配置（8簇×4×4 PE）总吞吐量约73 MOPS（处理吞吐量约92 MOPS），资源占用适中。

## 7. 优点
- **开源与可移植性**：完全开源，设计参数化，可面向不同FPGA或ASIC平台定制。
- **稀疏支持全面**：同时处理激活和权重稀疏，且通过独立地址/数据RAM实现低开销解码。
- **灵活的簇间路由**：支持对角线激活流，无需硬件改动即可实现任意步长，优于Eyeriss v2的固定网格拓扑。
- **真实开销评估**：明确量化了数据传输和接口成本，避免理想化评估，更贴近实际部署。
- **近线性扩展验证**：通过实测证明了路由和互连无额外开销，对大规模FPGA设计有指导意义。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅测试一个MNIST网络（约2M操作），未在更大、更复杂网络（ResNet、YOLO等）上验证。
  - 未提供能量/功耗测量，虽然提及能量效率，但缺乏实测数据。
  - 未进行消融实验：如关闭稀疏支持、不同量化精度（8-bit vs. 16-bit）、不同数据流映射策略。
- **缺乏与其他加速器的公平对比**：特性对比（Table 1）虽全面，但缺少在相同FPGA和网络下的性能基准测试，无法直接证明优势。
- **评估指标单一**：主要关注吞吐量和资源，缺少延迟分解（如路由延迟、内存带宽利用率）及端到端推理延迟的详细分析。
- **应用限制**：
  - 稀疏编码可能增加控制逻辑开销，在极低稀疏度下收益可能下降（文中未分析）；
  - 架构依赖完整特征图片上存储，对于超大模型可能需要频繁外部访存，削弱优势；
  - 当前仅实现8-bit量化，未支持更低位宽或混合精度。
- **可扩展性上限未探索**：最大配置为8簇×16PE（实际PE数有限），未测试更大规模（如16簇、32簇）是否仍保持线性，不确定路由拥堵是否在更高密度下出现。

（完）
