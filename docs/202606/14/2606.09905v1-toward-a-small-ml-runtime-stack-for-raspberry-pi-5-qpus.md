---
title: Toward a Small ML Runtime Stack for Raspberry Pi 5 QPUs
title_zh: 面向树莓派5 QPU的小型机器学习运行时栈
authors: "Yiannis Hadjiyianni, Panagiotis Michelakis, Dimitrios Stamoulis"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.09905v1"
tags: ["query:aiot"]
score: 9.0
evidence: 在树莓派QPU上构建机器学习运行时栈，加速边缘AI
tldr: Raspberry Pi 5的VideoCore VII QPU常被闲置，本文为其构建了以QPU优先的ML运行时栈。基于py-videocore7汇编库，实现了分块矩阵乘法、GEMM卷积、单头注意力等核心算子，并利用smul24指令支持整数计算。打包INT16输入与INT32累加使吞吐量较NumPy提升近两个数量级，在多种操作上均优于PyTorch和NumPy。结果表明，QPU可作为边缘AI模型加速的实用基板。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09905v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09905v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1246, \"height\": 383, \"label\": \"Table\"}]"
motivation: 充分利用Raspberry Pi 5的QPU算力，为边缘端ML推理提供高效加速方案。
method: 基于py-videocore7汇编库，构建包含分块矩阵乘法、GEMM卷积、单头注意力和smul24整数执行的可重用QPU运行时栈。
result: 密集整数内核吞吐量较NumPy高近两个数量级，min/max、池化、卷积、注意力等操作均优于PyTorch和NumPy。
conclusion: Raspberry Pi QPU可作为边缘AI推理的实用执行基板，具备显著性能优势。
---

## 摘要
我们提出了一种面向树莓派5的VideoCore VII QPU的以QPU优先的机器学习运行时栈，该栈构建在py-videocore7汇编库之上。该系统包括可复用的分块矩阵乘法基元、基于GEMM的卷积、单头注意力风格核心、持久化执行器以及基于smul24指令的整数执行。对于密集整数内核，打包的INT16输入与INT32累加相比NumPy实现了近两个数量级的吞吐量提升。在多种操作（min/max、池化、卷积、注意力）中，我们报告了比PyTorch和NumPy都更优的性能。我们的初步结果表明，树莓派QPU可以作为一种实用的执行基元，用于加速边缘侧的AI模型执行。

## Abstract
We present a QPU-first ML runtime stack for Raspberry Pi 5's VideoCore VII QPU, built on top of the py-videocore7 assembly library. The system comprises reusable tiled matrix-multiplication substrate, GEMM-backed convolution, a single-head attention-style core, persistent executors, and integer execution based on smul24 instructions. For dense integer kernels, packed INT16-input with INT32 accumulation achieves nearly two orders of magnitude higher throughput over NumPy. Across operations (min/max, pooling, convolution, attention), we report improved performance over both PyTorch and NumPy. Our preliminary results indicate that Raspberry QPUs can serve as a practical execution substrate towards accelerating AI model execution at the edge.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：Raspberry Pi 5 集成的 VideoCore VII QPU（四核图形处理单元）在边缘 AI 场景中极少被用作机器学习执行设备，现有推理引擎（如 ONNX、Ollama）仅针对 ARM CPU，缺乏端到端的 QPU 运行时栈。
- **动机**：QPU 拥有空闲的并行计算能力，若能结构化利用，可显著加速边缘侧 AI 模型推理，填补底层 QPU 可编程性与可复用推理运行时之间的空白。
- **背景**：Raspberry Pi 在边缘/IoT 系统广泛部署，其 QPU 算力尚未被充分挖掘。

### 2. 论文提出的方法论

- **核心思想**：构建以 QPU 优先的 ML 运行时栈，基于 py-videocore7 库（Python 端汇编生成、内核加载与调度），实现可复用的分块执行基元，并将整数计算作为优选路径。
- **关键技术细节**：
  - **分块矩阵乘法基元**：可复用的 tiled GEMM 实现，通过降低（lowering）将卷积转化为 GEMM-backed 卷积。
  - **单头注意力核心**：形式为 \( O = (QK^\top)V \) 的点积注意力，直接映射到 QPU。
  - **持久化执行器**：内核和设备缓冲区一次性分配，缓存 uniform 和调度元数据，实现多次重复执行（节省一次性设置时间）。
  - **整数执行优化**：利用 VideoCore VII 的 `smul24` 指令，强制符合算术约束的输入范围；实现打包 INT16 输入 + INT32 累加，以在有限硬件资源下获得更高吞吐。
  - **运行时约束传播**：操作符层面根据整数范围确定可行的分块、打包和累加方案。
- **算法/流程说明（文字）**：  
  1. 预处理阶段：为每个算子（GEMM、卷积、注意力）生成分块配置，适配 QPU 硬件限制。  
  2. 编译阶段：py-videocore7 将 Python 描述的内核转为 QPU 汇编并加载。  
  3. 执行阶段：持久执行器直接调度缓存的内核，避免重复设置；测量分为“一次性设置 + 稳态执行（QPU-C）”和“仅执行（QPU-E）”两个模式。

### 3. 实验设计

- **数据集/场景**：未明确指定具体数据集，所有性能测量基于合成数据（不同尺寸的矩阵、随机输入）。测试了 LeNet 模型（端到端 CNN）。
- **基准测试（Benchmark）**：密集整数 GEMM（矩阵大小 256、512、768、1024）；算子级性能（Min/Max、平均池化、最大池化、Conv2D、注意力核心）；LeNet 全模型推理。
- **对比方法**：NumPy（CPU 基线）和 PyTorch（CPU 推理）。PyTorch 仅作为参考，未使用 GPU 或量化加速。
- **测量指标**：
  - GEMM：GOPS（每秒十亿次操作）
  - 算子级：Min/Max 和池化用 GiB/s，卷积和注意力用 GOPS
  - 区分 “QPU-C”（缓存稳态执行）和 “QPU-E”（仅执行，不含设置开销）

### 4. 资源与算力

- **硬件平台**：Raspberry Pi 5 的 VideoCore VII QPU（12 核心），未使用任何外部 GPU 或加速卡。
- **算力详情**：该 QPU 峰值性能未知，但通过实验显示 INT32 GEMM 可达 20.49 GOPS，打包 INT16 可达 21.67 GOPS。
- **训练时长**：论文未涉及模型训练，仅关注推理加速，因此无训练时间和 GPU 数量信息。

### 5. 实验数量与充分性

- **实验组数**：约 10 组以上，包括：
  - 4 种矩阵大小的 GEMM 测试（表 1）
  - 7 种算子（Min/Max INT32/INT16、AvgPool、MaxPool、Conv2D 三种精度、Attention 两种精度）的对比（表 2）
  - 1 个 LeNet 端到端模型测试
- **充分性评估**：
  - **积极方面**：覆盖了核心计算原语和典型算子，区分了缓存与仅执行模式，提供了与 NumPy 和 PyTorch 的对比。
  - **不足**：仅包含一个简单 CNN 模型（LeNet），未验证更复杂模型（如 ResNet、BERT）；未探索不同数据规模对性能的影响（仅单一尺寸？表格中卷积、注意力尺寸未说明）；未与 CPU 上的 TFLite/ONNX Runtime 等成熟推理引擎对比；缺乏对浮点精度的全面分析（FP32 性能相对较弱）。
  - **客观性**：对比方法 NumPy 和 PyTorch 均为 CPU 基线，QPU 性能提升明显，但公平性受限于 PyTorch 版本和编译选项未提及。

### 6. 论文的主要结论与发现

- **QPU 作为边缘 AI 加速基元可行**：在密集整数运算上，打包 INT16 输入 + INT32 累加 GEMM 较 NumPy 最高提升 **94.38×**（矩阵大小 1024）。
- **算子级性能优势**：INT32 平均池化可达 2.62 GiB/s（NumPy 0.72，PyTorch 0.69）；INT32 卷积和注意力分别达 18.60 GOPS 和 14.41 GOPS（仅执行模式），远超 CPU 基线。
- **持久执行器有效**：缓存稳态（QPU-C）与仅执行（QPU-E）性能差距很小，说明一次性设置开销能被有效分摊。
- **LeNet 端到端**：12 核 INT32 管道达到 4.08 GOPS，是 NumPy (0.83 GOPS) 的约 **5 倍**。

### 7. 优点

- **创新性**：首次为 Raspberry Pi 5 QPU 构建完整的 ML 运行时栈，填补了该领域空白（本科生主导）。
- **设计复用性**：分块矩阵乘法作为通用基元，被卷积和注意力算子继承，降低开发成本。
- **整数计算优化**：利用狭小硬件指令（smul24）和打包技术实现了接近两个数量级的加速。
- **测量方法论**：清晰区分一次性设置、缓存稳态和仅执行开销，为性能分析提供了可靠框架。
- **开源潜在影响**：基于 py-videocore7 构建，结果可复现，有望推动边缘端低功耗 AI 推理新路径。

### 8. 不足与局限

- **实验覆盖不充分**：
  - 仅测试简单模型（LeNet），未验证更大规模 CNN 或 Transformer（如 BERT、LLM）。
  - 缺乏对多种输入分辨率、批量大小的消融实验。
  - 未与 TFLite/ONNX Runtime 或 TVM 等边缘推理引擎对比。
- **偏差风险**：
  - 仅报告性能最优的整数模式，对浮点模式性能相对较弱（FP32 卷积甚至低于 PyTorch？表2中 FP32 Conv2D QPU-C 16.25 GOPS 对比 PyTorch 12.74，但 QPU-E 19.87，需要确认单位一致，但注意 PyTorch 的 FP32 卷积 12.74 GOPS 低于 QPU，而 QPU 优势不明显）。
  - 矩阵乘法中打包 INT16 对精度影响未讨论，可能不适合所有模型。
  - 只评估了吞吐量（GOPS/GiB/s），未报告延迟、功耗或模型准确率损失。
- **应用限制**：QPU 编程复杂，py-videocore7 库仍较底层，难以扩展到完整算子集合；树莓派 5 QPU 仅有 12 核心，内存带宽有限，不适合超大模型。
- **可复现性**：论文属于“正在进行中”（work-in-progress）的短期研讨会论文，缺少代码仓库链接和详细配置参数。

（完）
