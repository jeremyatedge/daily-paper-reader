---
title: Accuracy-Configurable Floating-Point Multiplier Design for SRAM-Based Compute-in-Memory
title_zh: 基于SRAM存内计算的精度可配置浮点乘法器设计
authors: "Yiqi Zhou, Junhao Lu, Jiale Yu, Zhuo Xu, Yang He, Yue Yuan, Shan Shen, Daying Sun"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08430v1"
tags: ["query:aiot"]
score: 9.0
evidence: 适用于SRAM存内计算的精度可配置浮点乘法器，支持能效边缘AI
tldr: "传统数字存内计算框架主要支持整数运算，直接集成IEEE 754浮点单元会带来高面积和功耗开销。本文基于OpenACM框架设计了一种精度可配置的浮点乘法器，通过尾数分段近似降低硬件成本。后仿真结果显示逻辑面积减少69%、功耗降低72%且无延迟开销，在图像处理和ResNet-18推理中精度损失可忽略。该工作为SRAM存内计算系统提供了高效的编译器集成浮点支持。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08430v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 816, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08430v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 270, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08430v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 873, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08430v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1688, \"height\": 996, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08430v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08430v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 662, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08430v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 929, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08430v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 809, \"height\": 582, \"label\": \"Table\"}]"
motivation: 现有数字存内计算框架缺乏编译器集成且精度可配置的浮点计算支持，直接集成IEEE 754单元开销过高。
method: 在OpenACM框架中实现精确IEEE 754乘法器基线，并提出基于尾数分段的近似乘法器。
result: "后仿真获得69%逻辑面积减少和72%功耗节省，无延迟开销；图像处理与ResNet-18推理精度损失可忽略。"
conclusion: 编译器集成近似浮点乘法是SRAM存内计算实现高效可配置浮点支持的实用方法。
---

## 摘要
数字存内计算（DCiM）减少了数据搬运，已成为高能效边缘AI的一种有前景的解决方案。然而，现有大多数DCiM框架仍主要针对整数或定点运算，对编译器集成且精度可配置的浮点计算支持有限。直接将传统IEEE 754浮点单元集成到高密度SRAM基DCiM阵列中，会导致高昂的面积和功耗开销。为应对这一挑战，本文提出了一种集成到OpenACM框架中的精度可配置浮点乘法器，用于基于SRAM的DCiM。首先实现了一个精确兼容IEEE 754的乘法器作为基线，随后提出了一种基于尾数分割的近似乘法器，以降低硬件开销同时保持数值保真度。布局后结果显示，与精确浮点设计相比，逻辑面积减少高达69%，功耗节省72%，且无延迟开销。在图像处理任务和ResNet-18推理上的评估进一步表明精度退化可忽略不计。这些结果表明，编译器集成的近似浮点乘法是在基于SRAM的DCiM系统中实现高效、可配置浮点支持的一种实用方法。浮点乘法器代码可在https://github.com/ShenShan123/OpenACM获取。

## Abstract
Digital Compute-in-Memory (DCiM) reduces data movement and has become a promising solution for energy-efficient edge AI. However, most existing DCiM frameworks still primarily target integer or fixed-point arithmetic, and provide limited support for compiler-integrated and accuracy-configurable floating-point computation. Directly integrating conventional IEEE 754 floating-point units into dense SRAM-based DCiM arrays, however, incurs high area and power overhead. To address this challenge, this work presents an accuracy-configurable floating-point multiplier integrated into the OpenACM framework for SRAM-based DCiM. An exact IEEE~754-compliant multiplier is first implemented as a baseline, and a mantissa-segmentation-based approximate multiplier is then proposed to reduce hardware cost while preserving numerical fidelity. Post-layout results show up to 69% logic area reduction and 72% power savings over exact floating-point designs without delay overhead. Evaluations on image processing tasks and ResNet-18 inference further demonstrate negligible accuracy degradation. These results indicate that compiler-integrated approximate floating-point multiplication is a practical approach for enabling efficient and configurable floating-point support in SRAM-based DCiM systems. The Floating-Point Multiplier is available on https://github.com/ShenShan123/OpenACM

---

## 论文详细总结（自动生成）

# 基于SRAM存内计算的精度可配置浮点乘法器设计——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有数字存内计算（DCiM）框架（如OpenACM、AutoDCIM等）主要支持整数/定点运算，缺乏编译器集成且精度可配置的浮点计算支持。直接将传统IEEE 754浮点单元集成到高密度SRAM基DCiM阵列中，会导致面积和功耗开销过高，难以在能效受限的边缘AI场景下扩展。
- **整体含义**：为了在SRAM DCiM系统中实现高效、灵活的浮点运算，需要一种既能降低硬件开销又能保持可接受精度的近似浮点乘法器，并将其作为编译器中的一等设计组件，从而支持系统级的精度-功耗-面积（PPA）协同优化。

## 2. 论文提出的方法论

### 核心思想
- 在OpenACM框架中集成精确IEEE 754浮点乘法器作为基线，并提出一种基于**尾数分割（mantissa segmentation）** 的精度可配置近似浮点乘法器（AFPM），通过控制分割宽度n实现细粒度的精度-效率权衡。

### 关键技术细节
- **精确乘法器**：遵循标准IEEE 754乘法流水线（符号生成、指数累加与偏置校正、尾数乘法、归一化、舍入与溢出检测）。
- **近似乘法器架构**：
  - 将两个输入尾数Mx、My分为高有效段（A、C）和低有效段（B、D），配置宽度n可调。
  - 部分乘积项AC（最高权重）精确计算；AD和BC根据低有效段是否全零条件性执行，并引入轻量移位补偿；BD项（权重最低）直接省略。
  - 最终近似尾数乘积公式：Mx·My ≈ (A×C)<<n + δAD(A×D) + δBC(B×C) + Ecomp（Ecomp为补偿项）。
  - 累加器宽度优化为3n bits；归一化后零填充至23 bits以符合FP32格式。
  - **低精度模式（ACL5）**：仅使用高n bits的位与结果近似高次乘积，累加宽度进一步降至n bits，适用于极低功耗场景。
- **编译器集成**：将精确/近似浮点乘法器作为OpenACM框架中的可配置算子，暴露精度选择为编译优化旋钮。

## 3. 实验设计

### 使用的数据集/场景
- **硬件评估**：使用FreePDK45工艺库，通过OpenROAD工具链进行布局后PPA分析（100MHz时钟，0.5pF输出负载）。
- **图像处理任务**：
  - **图像融合**：三组测试（Lake & Mandril, Jetplane & Boat, Cameraman & Lake），评价指标为峰值信噪比（PSNR）。
  - **边缘检测**：Boat、Cameraman、Jetplane三张标准测试图，同样使用PSNR。
- **CNN推理**：ResNet-18在CIFAR-10数据集上训练（标准浮点），推理时替换乘法器为近似设计，评估Top-1和Top-5准确率，以及MRED（均值相对误差距离）和NMED（归一化均值误差距离）。

### Benchmark与对比方法
- **对比方法**：
  - 精确乘法器（Exact）
  - 提出的近似设计：ACL5、AC4-4、AC5-5、AC6-6（n=4,5,6）
  - 现有近似乘法器：MMBS5-7（基于尾数分割，[7]）、CSS12-18（静态分割，[6]）、NC/LPC/HPC（对数近似，[5]）
- **对比维度**：延迟、逻辑面积、SRAM面积、总面积、功耗、MRED、NMED、PSNR、Top-1/5准确率。

## 4. 资源与算力

- **文中未明确提及GPU型号、数量或训练时长**。硬件PPA评估基于ASIC设计流程（OpenROAD + FreePDK45），不涉及GPU训练；神经网络训练采用标准浮点精度，但未说明具体训练硬件。仅提到CNN推理时替换乘法器进行评估，训练过程未使用近似运算。

## 5. 实验数量与充分性

- **实验组数**：
  - 后仿真PPA：不同SRAM尺寸（16×8, 32×16, 64×32）下对比了多个近似配置（7种自己设计 + 6种MMBS/CSS + 3种NC/LPC/HPC），覆盖面积、功耗、延迟等指标。
  - 图像处理：3种融合测试 + 3种边缘检测，共6组实验。
  - CNN推理：ResNet-18在CIFAR-10上，对比了所有近似配置和基线，共约11组（不含精确）。
  - **无显式消融实验**（例如仅去除BD项、仅改变补偿策略等），但通过不同n值（AC4-4,5-5,6-6）展示了分割宽度的影响。
- **充分性与公平性**：对比了多种现有近似乘法器（三种主流类型），横跨硬件和算法指标，实验较为全面。但缺少更大规模数据集（如ImageNet）和更复杂网络（如Transformer）的验证；仅测试了推理，未评估训练精度影响；未对近似乘法器在DCiM阵列中的实际布局密度效应进行系统性分析。

## 6. 论文的主要结论与发现

- **硬件收益**：提出的近似乘法器（如ACL5）相比精确乘法器，逻辑面积减少78.4%，功耗降低82.1%，且关键路径延迟不变（由SRAM访问决定）。
- **数值精度**：AC4-4相比HPC（高精度对数近似）MRED降低80.4%，NMED降低79.3%；图像处理PSNR普遍优于同类分割方法（MMBS、CSS）。
- **CNN推理**：AC4-4至AC6-6在ResNet-18上Top-1准确率与精确结果一致（均为87.15%左右），无退化；低精度模式ACL5仅下降约1.5个百分点但仍可接受。
- **总体结论**：编译器集成的近似浮点乘法是实现SRAM DCiM高效浮点支持的实用方法，能提供细粒度的精度-效率权衡，且易于系统级优化。

## 7. 优点

- **编译器集成**：首次将精度可配置浮点乘法器作为OpenACM框架的原生算子，支持自动化的硬件-软件协同探索。
- **架构创新**：基于尾数分割的条件性执行+轻量补偿，在极低开销下维持高精度；低精度模式进一步扩展适用场景。
- **实验覆盖**：从硬件PPA到图像处理再到神经网络推理，多层次验证，对比多种现有AFPM，结论可靠。
- **开源**：代码已在GitHub发布，便于复现和扩展。

## 8. 不足与局限

- **应用范围限制**：仅评估了ResNet-18和CIFAR-10，未在大型数据集（如ImageNet）或更深/新型网络（如ViT、BERT）上验证，泛化性待确认。
- **训练场景缺失**：论文仅研究推理阶段，未考虑近似乘法器对训练收敛性和最终精度的影响。若用于片上学习或微调，其数值误差可能放大。
- **缺乏全面消融**：对每个近似决策（如忽略BD、条件执行AD/BC、补偿策略）的单独贡献未做消融实验；低精度模式ACL5的原理与效果未与更激进的对数方法对比。
- **工艺与功耗模型**：仅基于FreePDK45（45nm）工艺，未在更先进节点（如28nm, 7nm）验证，跨工艺迁移性未知。功耗评估未包含SRAM阵列本身的变化。
- **只涉及浮点乘法**：未考虑加法、累加等操作，实际DCiM需要完整的浮点运算单元组合，整体系统级开销可能不同。
- **误差与硬件开销曲线**：虽然给出了不同n值的结果，但未详尽展示所有n的连续变化曲线，设计空间探索的粒度可能不够细。

（完）
