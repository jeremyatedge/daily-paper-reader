---
title: "S2Act: Simple Spiking Actor"
title_zh: S2Act：简单脉冲 Actor
authors: "Ugur Akcal, Seung Hyun Kim, Mikihisa Yuasa, Hamid Osooli, Jiarui Sun, Ribhav Sahu, Mattia Gazzola, Huy T. Tran, Girish Chowdhary"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15725v1"
tags: ["query:aiot"]
score: 9.0
evidence: 用于功耗受限机器人的轻量级脉冲神经网络框架
tldr: 针对移动机器人受限于功耗和计算资源的问题，本文提出了一种轻量级脉冲神经网络（SNN）强化学习框架 S2Act。该方法通过将 LIF 神经元的速率响应近似为 ReLU 激活函数，简化了训练过程并解决了梯度消失问题。实验证明，S2Act 在多智能体随机环境及 Intel Loihi 硬件部署中表现优异，显著提升了推理效率和任务性能，为 SNN 策略的实际应用提供了新思路。
selection_source: fresh_fetch
motivation: 现有的 SNN 强化学习方法在复杂随机环境中面临超参数敏感和梯度信号不一致的挑战，难以在受限硬件上高效部署。
method: 提出 S2Act 框架，通过构建近似速率型 SNN 的 Actor-Critic 模型，利用兼容激活函数训练后将权重迁移至物理 LIF 神经元。
result: 在多智能体夺旗和泊车任务中，S2Act 在任务性能和实时推理方面均优于基准模型，并成功在 Intel Loihi 硬件上实现部署。
conclusion: S2Act 证明了通过参数塑形使 SNN 近似传统激活函数，可以有效简化 SNN 强化学习的训练并提升其在真实机器人平台上的部署效率。
---

## 摘要
脉冲神经网络 (SNN) 和受生物启发的学习机制在移动机器人领域极具吸引力，因为车载神经网络策略的大小和性能受限于功耗和计算预算。现有的 SNN 方法，如群体编码、奖励调制以及混合人工神经网络 (ANN)-SNN 架构，已展现出良好的效果；然而，由于 SNN 对超参数的敏感性以及梯度信号的不一致，它们在复杂且高度随机的环境中面临挑战。为了应对这些挑战，我们提出了简单脉冲 Actor (S2Act)，这是一个计算轻量级的框架，通过三个步骤使用 SNN 部署强化学习 (RL) 策略：(1) 基于速率型脉冲神经元的近似网络构建 Actor-Critic 模型；(2) 使用兼容的激活函数通过梯度训练网络；(3) 将训练好的权重转换为速率型漏电积分发放 (LIF) 神经元的物理参数，用于推理和部署。通过全局调整 LIF 神经元参数，使其基于速率的响应近似于 ReLU 激活，S2Act 有效地缓解了梯度消失问题，同时预先约束 LIF 响应曲线减少了对复杂的 SNN 特定超参数调优的依赖。我们在两个捕捉多机器人交互复杂性的多智能体随机环境（夺旗和停车）中演示了我们的方法，并使用英特尔的 Loihi 类脑硬件在物理 TurtleBot 平台上部署了训练好的策略。我们的实验结果表明，在几乎所有考虑的场景中，S2Act 在任务性能和实时推理方面均优于相关的基准方法，突显了其在基于 SNN 的 RL 策略的快速原型设计和高效现实世界部署方面的潜力。

## Abstract
Spiking neural networks (SNNs) and biologically-inspired learning mechanisms are attractive in mobile robotics, where the size and performance of onboard neural network policies are constrained by power and computational budgets. Existing SNN approaches, such as population coding, reward modulation, and hybrid artificial neural network (ANN)-SNN architectures, have shown promising results; however, they face challenges in complex, highly stochastic environments due to SNN sensitivity to hyperparameters and inconsistent gradient signals. To address these challenges, we propose simple spiking actor (S2Act), a computationally lightweight framework that deploys an RL policy using an SNN in three steps: (1) architect an actor-critic model based on an approximated network of rate-based spiking neurons, (2) train the network with gradients using compatible activation functions, and (3) transfer the trained weights into physical parameters of rate-based leaky integrate-and-fire (LIF) neurons for inference and deployment. By globally shaping LIF neuron parameters such that their rate-based responses approximate ReLU activations, S2Act effectively mitigates the vanishing gradient problem, while pre-constraining LIF response curves reduces reliance on complex SNN-specific hyperparameter tuning. We demonstrate our method in two multi-agent stochastic environments (capture-the-flag and parking) that capture the complexity of multi-robot interactions, and deploy our trained policies on physical TurtleBot platforms using Intel's Loihi neuromorphic hardware. Our experimental results show that S2Act outperforms relevant baselines in task performance and real-time inference in nearly all considered scenarios, highlighting its potential for rapid prototyping and efficient real-world deployment of SNN-based RL policies.

---

## 论文详细总结（自动生成）

以下是对论文《S2Act: Simple Spiking Actor》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在移动机器人领域，车载计算资源和功耗预算非常有限。脉冲神经网络（SNN）因其事件驱动和稀疏计算的特性，在类脑硬件（如 Intel Loihi）上具有极高的能效比，是理想的选择。
*   **核心问题**：现有的 SNN 强化学习（RL）方法在复杂、高度随机的环境中面临三大挑战：
    1.  **训练不稳定**：SNN 对超参数极度敏感。
    2.  **梯度消失**：脉冲动力学与标准反向传播不兼容。
    3.  **架构复杂**：如群体编码（Population Coding）等方法会导致参数量剧增，难以在资源受限的硬件上部署。
*   **研究目的**：提出一种简单、轻量且易于训练的 ANN-to-SNN 转换框架，旨在实现高效的机器人策略部署。

### 2. 方法论：核心思想与技术细节
S2Act 采用“离线训练、在线部署”的模式，核心流程分为三步：
*   **第一步：架构设计（Soft-ReLLIF）**
    *   核心思想是调整漏电积分发放（LIF）神经元的全局参数（如膜电容 $C_m$、时间常数 $\tau_m$、阈值 $V_{th}$ 等），使其**发放率响应曲线近似于 ReLU 激活函数**。
    *   引入平滑项构建 **Soft-ReLLIF** 激活函数，使其在训练阶段可微，从而直接利用成熟的人工神经网络（ANN）训练管线。
*   **第二步：策略训练**
    *   使用 **PPO（近端策略优化）** 算法在仿真环境中训练 Actor-Critic 模型。Actor 和 Critic 网络均使用基于速率的 Soft-ReLLIF 神经元。
*   **第三步：权重迁移与部署**
    *   训练完成后，将 Actor 网络的权重直接迁移到物理 LIF 神经元中。
    *   通过缩放神经元活动范围，使其适配类脑硬件（Loihi）的物理约束，实现从速率模型到脉冲模型的无缝转换。

### 3. 实验设计
*   **实验场景**：
    1.  **1v1 夺旗（CtF）**：离散状态/动作空间，对手为随机行走，测试基础学习能力。
    2.  **2v2 夺旗（CtF）**：多智能体对抗环境，对手为防御型巡逻策略，测试协作与复杂决策能力。
    3.  **泊车环境（Parking）**：连续状态/动作空间，包含物理动力学和随机干扰。
    4.  **真机部署**：使用 TurtleBot3 机器人和 Intel Loihi 硬件在 12'×12' 的场地进行实地验证。
*   **对比基准（Benchmarks）**：
    *   **PopSAN**：基于群体编码的脉冲 Actor 网络。
    *   **Hybrid SNN**：结合了 ANN 特征提取和 SNN 决策的混合模型。
    *   **RSNN**：基于循环脉冲神经网络的方法。

### 4. 资源与算力
*   **训练算力**：使用 GPU 进行训练（未指明具体型号），框架基于 PyTorch。
*   **训练时长（MTrT）**：
    *   S2Act 在 1v1 夺旗任务中平均训练时长为 **2.4 小时**（PopSAN 为 8.3 小时）。
    *   在 2v2 夺旗任务中为 **5.0 小时**（PopSAN 为 15.7 小时）。
*   **推理硬件**：Intel Loihi (Kapoho Bay USB 形式)。
*   **能耗测量**：使用 USB 功率计测量 Loihi 功耗，使用 `pyJoules` 测量 CPU/GPU 功耗。

### 5. 实验数量与充分性
*   **实验规模**：每个场景均使用 3 个不同的随机种子进行训练，并在训练完成后进行 **1000 次** 独立评估。
*   **充分性评价**：
    *   **覆盖面广**：涵盖了离散与连续控制、单智能体与多智能体、仿真与真机。
    *   **对比公平**：对所有基准方法进行了广泛的参数搜索，以确保对比的是各方法的最佳性能。
    *   **客观性**：不仅关注成功率，还详细统计了推理延迟（MInT）、每帧能耗（EpI）和训练效率。

### 6. 主要结论与发现
*   **性能领先**：S2Act 在所有测试场景中的成功率均优于或持平于最强基准（PopSAN），且训练速度快 2-3 倍。
*   **高能效比**：在 Loihi 上的推理能耗显著低于 GPU 推理（例如在夺旗任务中，Loihi 约为 0.033J/inf，而 GPU 约为 0.079J/inf）。
*   **鲁棒的迁移性**：通过简单的神经活动缩放，S2Act 能够克服硬件量化误差和热漂移，成功实现从仿真到真机的零样本（Zero-shot）迁移。

### 7. 优点与亮点
*   **极简性**：摒弃了复杂的群体编码，仅用两层隐藏层（每层 64 神经元）就实现了复杂对抗任务。
*   **梯度友好**：通过 Soft-ReLLIF 设计，巧妙解决了 SNN 训练中的梯度消失问题。
*   **硬件原生**：直接针对类脑硬件特性设计，避免了复杂的辅助损失函数或复杂的神经元模型。
*   **首创性**：实现了首个在类脑芯片上运行的、基于 ANN-to-SNN 转换的多智能体对抗 RL 策略演示。

### 8. 不足与局限
*   **速率编码延迟**：基于速率的 SNN 需要一定的时间窗口进行积分，这在追求极低延迟的连续控制任务中可能存在瓶颈。
*   **量化误差**：在 Loihi 硬件部署时，由于位宽限制（Quantization），性能较仿真环境有轻微下降。
*   **观测假设**：目前假设环境是完全可观测的，且神经元参数固定，未引入在线学习或可塑性机制，限制了其在部分观测或动态变化环境中的自适应能力。

（完）
