---
title: "GLANCE: Gaze-Led Attention Network for Compressed Edge-inference"
title_zh: GLANCE：用于压缩边缘推理的注视引导注意力网络
authors: "Neeraj Solanki, Hong Ding, Sepehr Tabrizchi, Ali Shafiee Sarvestani, Shaahin Angizi, David Z. Pan, Arman Roohi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15717v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于边缘推理的超高效注视估计和注意力引导检测
tldr: 针对AR/VR设备对实时目标检测的极高功耗和延迟要求，本文提出GLANCE框架。该框架受生物视觉启发，采用两阶段流程：首先利用可微分无权重神经网络通过内存查找实现超低功耗视线估计，随后根据视线引导ROI区域进行选择性目标检测。实验表明，该方法在显著降低计算量和能耗的同时，提升了检测精度，满足了边缘端严苛的推理需求。
selection_source: fresh_fetch
motivation: AR/VR系统需要亚10毫秒的延迟和极低功耗，传统均匀处理方式难以满足边缘端实时目标检测的需求。
method: 提出一种结合无权重神经网络视线追踪与注意力引导ROI检测的两阶段流水线，通过内存查找替代高能耗的乘加运算。
result: "在Arduino Nano上实现了低于10ms的延迟，减少了40-50%的计算量和65%的能耗，且COCO数据集mAP达到48.1%。"
conclusion: 证明了以内存为中心的架构结合显式注意力建模，在资源受限的穿戴式平台上比传统均匀处理更具效率和精度优势。
---

## 摘要
AR/VR 系统中的实时目标检测面临严峻的计算限制，要求在紧凑的功耗预算内实现低于 10 毫秒的延迟。受生物中央凹视觉的启发，我们提出了一种两阶段流水线，将用于超高效注视估计的可微分无权重神经网络与注意力引导的感兴趣区域（ROI）目标检测相结合。我们的方法通过内存查找而非乘加运算进行注视追踪，消除了算术密集型操作，在每帧仅需 393 次 MAC 和 2.2 KiB 内存的情况下，实现了 8.32° 的角度误差。注视预测引导对关注区域的选择性目标检测，将计算负担降低了 40-50%，能耗降低了 65%。在 Arduino Nano 33 BLE 上部署后，我们的系统在 COCO 数据集上实现了 48.1% 的 mAP（关注对象上为 51.8%），同时保持了低于 10 毫秒的延迟，通过将通信时间缩短 177 倍，满足了严苛的 AR/VR 需求。与全局 YOLOv12n 基准模型（对小、中、大目标的准确率分别为 39.2%、63.4% 和 83.1%）相比，基于 ROI 的方法在相同设置下分别达到了 51.3%、72.1% 和 88.1%。这项工作表明，对于资源受限的穿戴式平台，具有显式注意力建模的以内存为中心的架构比统一处理方式具有更高的效率和准确性。

## Abstract
Real-time object detection in AR/VR systems faces critical computational constraints, requiring sub-10\,ms latency within tight power budgets. Inspired by biological foveal vision, we propose a two-stage pipeline that combines differentiable weightless neural networks for ultra-efficient gaze estimation with attention-guided region-of-interest object detection. Our approach eliminates arithmetic-intensive operations by performing gaze tracking through memory lookups rather than multiply-accumulate computations, achieving an angular error of $8.32^{\circ}$ with only 393 MACs and 2.2 KiB of memory per frame. Gaze predictions guide selective object detection on attended regions, reducing computational burden by 40-50\% and energy consumption by 65\%. Deployed on the Arduino Nano 33 BLE, our system achieves 48.1\% mAP on COCO (51.8\% on attended objects) while maintaining sub-10\,ms latency, meeting stringent AR/VR requirements by improving the communication time by $\times 177$. Compared to the global YOLOv12n baseline, which achieves 39.2\%, 63.4\%, and 83.1\% accuracy for small, MEDium, and LARGE objects, respectively, the ROI-based method yields 51.3\%, 72.1\%, and 88.1\% under the same settings. This work shows that memory-centric architectures with explicit attention modeling offer better efficiency and accuracy for resource-constrained wearable platforms than uniform processing.

---

## 论文详细总结（自动生成）

### GLANCE：用于压缩边缘推理的注视引导注意力网络——论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
AR/VR 设备（如 Apple Vision Pro、Meta Quest Pro）需要在极低的功耗（数瓦）和极低的延迟（<10ms）下，同时处理场景理解、目标检测和注视追踪。然而，现有的目标检测器（如 YOLO 系列）通常对整个视野进行均匀计算，这与人类视觉系统（中央凹仅占 2° 视角但消耗绝大部分神经资源）的生物特性相悖。论文的核心问题是如何在资源极度受限的边缘设备上，利用注视信号引导计算资源，消除全帧处理的冗余，实现高效的实时检测。

#### 2. 方法论：核心思想与关键技术
GLANCE 提出了一个两阶段的“注视引导”流水线：
*   **超高效注视估计（DWN）：** 采用**可微分无权重神经网络（Differentiable Weightless Neural Networks）**。
    *   **核心思想：** 放弃传统的卷积和乘加（MAC）运算，改用基于内存查找表（LUT）的推理。
    *   **流程：** 输入眼部图像 -> 非线性变换（tanh）与池化 -> **温度计编码（Thermometer Encoding）**将连续特征转为二进制地址 -> 访问 LUT 获取存储值 -> 线性头输出 3D 注视向量。
*   **注意力引导的 ROI 形成：**
    *   将注视点投影到图像平面，根据注视不确定性动态选择 ROI 尺寸（48/64/80 像素）。
    *   **ROI 联合马赛克（Union-of-ROIs Mosaic）：** 将多个 ROI 区域合并为一个单一的作物（Crop），减少传感器数据传输和检测器的重复计算。
*   **时空优化策略：**
    *   **时间联合（Temporal Union）：** 维护一个 K 帧的注意力窗口，平滑检测结果并提高召回率。
    *   **运动感知稳定：** 利用惯性测量单元（IMU）数据，在检测器运行间隙对 ROI 进行旋转重投影和运动补偿（膨胀处理）。

#### 3. 实验设计
*   **数据集：**
    *   **注视估计：** 使用 **MPIIGaze** 数据集，遵循“留一法”（LOPO）协议评估跨人泛化能力。
    *   **目标检测：** 使用 **COCO** 验证集，评估不同尺寸（小、中、大）目标的检测精度。
*   **Benchmark 与对比方法：**
    *   **注视估计对比：** iTracker、Dilated-Net、CA-Net、SAZE 等深度学习模型。
    *   **目标检测对比：** 以全局 **YOLOv12n** 作为基准（Baseline）。
*   **硬件部署：** 使用 **Arduino Nano 33 BLE**（微控制器）作为边缘端，**NVIDIA RTX 3090** 作为主机端。

#### 4. 资源与算力
*   **边缘端（DWN）：** 仅需 **393 次 MAC** 运算和 **2.2 KiB** 内存（8位精度），参数量约 9.6K。
*   **主机端：** 使用 NVIDIA RTX 3090 GPU 进行 YOLOv12n 推理。
*   **通信优化：** 通过仅传输 ROI 区域，通信时间缩短了 **177 倍**。
*   **训练细节：** 使用 Adam 优化器，学习率 $3 \times 10^{-3}$，Batch Size 为 64，DWN 训练包含梯度裁剪。

#### 5. 实验数量与充分性
*   **实验覆盖：** 论文进行了注视估计的精度对比、不同 ROI 数量（N=1 到 50）对检测精度的影响实验、不同 ROI 尺寸的消融实验，以及在三种不同微控制器（MSP430, Arduino, STM32）上的性能模拟。
*   **客观性：** 采用了标准的 COCO 评估协议和 MPIIGaze 跨人验证，实验设计较为严谨。通过分尺寸（Small/Medium/Large）讨论检测精度，展示了 ROI 方法在小目标上的显著优势。

#### 6. 主要结论与发现
*   **效率提升：** GLANCE 将计算负担降低了 40-50%，能耗降低了 65%，实现了亚 10 毫秒的端到端延迟。
*   **精度优势：** 在 COCO 数据集上，基于 ROI 的方法在小目标（51.3% vs 39.2%）和中目标（72.1% vs 63.4%）上的表现显著优于全局 YOLOv12n 基准。
*   **内存中心架构的潜力：** 证明了在极低功耗设备上，基于内存查找的 DWN 是替代传统 MAC 密集型 CNN 的可行方案。

#### 7. 优点
*   **生物启发式设计：** 成功模拟了人类视觉的中央凹特性，将注意力机制显式化。
*   **极低资源占用：** DWN 的内存和计算需求极低，甚至可以在极其简陋的 MSP430 单片机上运行。
*   **系统级优化：** 不仅优化了算法，还考虑了传感器传输、IMU 补偿和时空一致性，具有很强的工程落地价值。

#### 8. 不足与局限
*   **注视精度权衡：** DWN 的平均角度误差（8.32°）明显高于重型深度学习模型（约 4°），虽然通过增加 ROI 覆盖范围进行了补偿，但在极高精度要求的场景下可能受限。
*   **静态数据集模拟：** 目标检测实验主要在静态的 COCO 数据集上通过模拟扫描路径完成，在真实动态视频流中的表现（如快速运动导致的伪影）仍需更多实测数据支持。
*   **应用限制：** 该系统依赖于额外的眼动追踪摄像头，增加了硬件设计的复杂性。

（完）
