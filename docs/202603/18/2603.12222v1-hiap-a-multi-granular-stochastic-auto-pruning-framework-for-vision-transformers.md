---
title: "HiAP: A Multi-Granular Stochastic Auto-Pruning Framework for Vision Transformers"
title_zh: HiAP：一种用于视觉 Transformer 的多粒度随机自动剪枝框架
authors: "Andy Li, Aiden Durrant, Milan Markovic, Georgios Leontidis"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12222v1"
tags: ["query:aiot"]
score: 9.0
evidence: 针对边缘设备视觉 Transformer 的自动剪枝框架
tldr: 针对Vision Transformers在边缘设备部署受限的问题，本文提出HiAP框架。该框架采用连续松弛技术，通过在宏观（注意力头、FFN块）和微观（内部维度、神经元）多个粒度引入随机Gumbel-Sigmoid门控，实现端到端的自动化剪枝。HiAP无需手动设置层级稀疏目标，能同时优化内存带宽和计算开销，在ImageNet上取得了优异的精度与效率平衡。
selection_source: fresh_fetch
motivation: 现有的ViT结构化剪枝方法通常局限于单一粒度且依赖复杂的多阶段流程，难以在满足稀疏预算的同时兼顾内存和计算效率。
method: 提出一种多粒度随机自动剪枝框架，利用Gumbel-Sigmoid门控在宏观和微观层面同时优化子网络结构，并结合FLOPs惩罚项进行端到端训练。
result: 在ImageNet数据集上，HiAP为DeiT-Small等模型发现了高效架构，达到了与复杂多阶段方法相当甚至更优的精度-效率帕累托前沿。
conclusion: HiAP简化了ViT的部署流程，通过多粒度联合优化证明了自动化、端到端剪枝在提升模型运行效率方面的有效性。
---

## 摘要
视觉 Transformer 需要大量的计算资源和内存带宽，严重限制了它们在边缘设备上的部署。虽然最近的结构化剪枝方法成功降低了理论 FLOPs，但它们通常在单一结构粒度上运行，并依赖于具有事后阈值处理的复杂多阶段流水线，以满足稀疏度预算。在本文中，我们提出了分层自动剪枝（HiAP），这是一种连续松弛框架，可在单个端到端训练阶段发现最优子网络，而无需手动的重要性启发式方法或预定义的逐层稀疏度目标。HiAP 在多个粒度上引入了随机 Gumbel-Sigmoid 门控：宏观门控用于剪枝整个注意力头和 FFN 块，微观门控用于选择性地剪枝头内维度和 FFN 神经元。通过同时优化这两个层级，HiAP 解决了加载大矩阵带来的内存受限开销以及数学运算带来的计算受限问题。HiAP 使用结合了结构可行性惩罚和解析 FLOPs 的损失函数，自然地收敛到稳定的子网络。在 ImageNet 上的广泛实验表明，HiAP 有机地发现了高效架构，并为 DeiT-Small 等模型实现了具有竞争力的准确率-效率帕累托前沿，在显著简化部署流程的同时，达到了复杂多阶段方法的性能。

## Abstract
Vision Transformers require significant computational resources and memory bandwidth, severely limiting their deployment on edge devices. While recent structured pruning methods successfully reduce theoretical FLOPs, they typically operate at a single structural granularity and rely on complex, multi-stage pipelines with post-hoc thresholding to satisfy sparsity budgets. In this paper, we propose Hierarchical Auto-Pruning (HiAP), a continuous relaxation framework that discovers optimal sub-networks in a single end-to-end training phase without requiring manual importance heuristics or predefined per-layer sparsity targets. HiAP introduces stochastic Gumbel-Sigmoid gates at multiple granularities: macro-gates to prune entire attention heads and FFN blocks, and micro-gates to selectively prune intra-head dimensions and FFN neurons. By optimizing both levels simultaneously, HiAP addresses both the memory-bound overhead of loading large matrices and the compute-bound mathematical operations. HiAP naturally converges to stable sub-networks using a loss function that incorporates both structural feasibility penalties and analytical FLOPs. Extensive experiments on ImageNet demonstrate that HiAP organically discovers highly efficient architectures, and achieves a competitive accuracy-efficiency Pareto frontier for models like DeiT-Small, matching the performance of sophisticated multi-stage methods while significantly simplifying the deployment pipeline.

---

## 论文详细总结（自动生成）

### 论文总结：HiAP —— 用于视觉 Transformer 的多粒度随机自动剪枝框架

#### 1. 核心问题与整体含义（研究动机和背景）
视觉 Transformer (ViT) 虽然性能卓越，但其巨大的计算开销（FLOPs）和内存带宽需求限制了其在边缘设备上的部署。现有的结构化剪枝方法存在两个主要局限：
*   **粒度单一：** 多数方法仅针对微观结构（如神经元）或宏观结构（如注意力头），忽略了现代硬件的瓶颈往往在于内存带宽（加载大矩阵的开销），而非单纯的计算量。
*   **流程复杂：** 通常依赖多阶段流水线（搜索、剪枝、微调），且需要手动设置启发式阈值或预定义每一层的稀疏目标。
**HiAP 的目标**是提出一种端到端的自动剪枝框架，通过多粒度联合优化，在单一训练阶段内自动发现最优子网络。

#### 2. 方法论
HiAP 核心思想是将剪枝建模为一个**单阶段、预算感知的可微分搜索问题**：
*   **分层门控机制 (Hierarchical Gating)：**
    *   **宏观门控 (Macro-gates)：** 控制整个注意力头 (Attention Heads) 和 FFN 块的保留与跳过，直接减少内存访问开销。
    *   **微观门控 (Micro-gates)：** 在保留的宏观结构内部，选择性剪枝头内维度 (Intra-head dimensions) 和 FFN 神经元，精细化调整计算量。
*   **关键技术细节：**
    *   **Gumbel-Sigmoid 松弛：** 将离散的剪枝决策转化为连续可微的变量，允许通过梯度下降进行端到端优化。
    *   **可微分成本建模：** 将网络总成本分解为静态开销和由门控控制的动态成本（MACs），并直接计入损失函数。
    *   **损失函数设计：** $L_{total} = L_{task} + \lambda_{macro} L_{macro} + \lambda_{micro} L_{micro} + L_{feasibility}$。其中 $L_{feasibility}$（可行性惩罚）用于防止“层坍塌”（即某一层被完全剪掉导致梯度断裂）。
    *   **单阶段退火策略：** 训练过程中，Gumbel-Sigmoid 的温度参数 $\tau$ 从 2.0 逐渐退火至 0.5，使门控从随机分布平滑过渡到确定性的二值决策。

#### 3. 实验设计
*   **数据集：** ImageNet-1K（大规模验证）、CIFAR-10（受控消融实验）。
*   **基准模型 (Benchmark)：** DeiT-Small (ImageNet)、ViT-Tiny (CIFAR-10)。
*   **对比方法：** 包括 WDPruning、S2ViT、ViT-Slim、GOHSP、NViT、UPDP 等多种 SOTA 结构化剪枝方法。

#### 4. 资源与算力
*   **训练配置：** 使用 AdamW 优化器，学习率 $5 \times 10^{-5}$，全局 Batch Size 为 256。
*   **时长：** ImageNet 和 CIFAR-10 均训练 200 个 Epoch。
*   **硬件说明：** 论文提到在“单 GPU”上进行了 CIFAR-10 的推理延迟分析（从 5.57ms 降至 3.86ms），但**未明确说明** ImageNet 训练阶段使用的具体 GPU 型号（如 A100 或 V100）及具体 GPU 数量。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从小型数据集到大规模 ImageNet 的迁移，验证了框架的扩展性。
*   **消融实验：** 深入探讨了宏观与微观惩罚比例（如 2:1, 5:1, 1.5:1）对帕累托前沿的影响，并可视化了剪枝动态过程。
*   **公平性：** 对比了相同 MACs 预算下的准确率，并与手动启发式方法（如 $\ell_1$ 范数排名）进行了对比。实验设计较为全面，能够客观反映自动化搜索的优势。

#### 6. 主要结论与发现
*   **性能优异：** 在 ImageNet 上，HiAP 将 DeiT-Small 的计算量减少约 33% (3.1G MACs)，同时保持 79.1% 的 Top-1 准确率，达到了与复杂多阶段方法相当的水平。
*   **自动分布稀疏度：** 观察发现 HiAP 会优先移除深层的 FFN 块（宏观剪枝），并对浅层进行微观精修，这种异构拓扑是手动设计难以达到的。
*   **硬件加速：** 理论上的 MACs 减少能直接转化为实际的设备提速（约 1.44x 加速），证明了物理提取子网络的有效性。

#### 7. 优点
*   **流程极简：** 实现了“搜索即训练”，无需事后微调或复杂的图分析工具。
*   **多粒度协同：** 同时考虑了计算受限（Compute-bound）和内存受限（Memory-bound）的开销。
*   **鲁棒性：** 引入可行性惩罚项，有效解决了可微分剪枝中常见的层坍塌问题。

#### 8. 不足与局限
*   **优化目标偏差：** 目前主要优化目标是预估的 MACs，而非直接针对特定硬件的实测延迟（Latency）或功耗，实际加速比可能因硬件架构和算子实现而异。
*   **实验覆盖：** 尚未在更大规模的模型（如 ViT-Large）或非分类任务（如检测、分割）上进行广泛验证。
*   **超参数依赖：** 虽然减少了启发式规则，但仍需手动调节 $\lambda_{macro}$ 和 $\lambda_{micro}$ 的比例来平衡不同粒度的稀疏度。

（完）
