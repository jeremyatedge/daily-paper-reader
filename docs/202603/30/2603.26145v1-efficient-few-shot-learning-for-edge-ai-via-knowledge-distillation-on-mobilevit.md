---
title: Efficient Few-Shot Learning for Edge AI via Knowledge Distillation on MobileViT
title_zh: 基于 MobileViT 知识蒸馏的边缘 AI 高效小样本学习
authors: "Shuhei Tsuyuki, Reda Bensaid, Jérémy Morlier, Mathieu Léonardon, Naoya Onizawa, Vincent Gripon, Takahiro Hanyu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26145v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘AI的高效少样本学习与知识蒸馏
tldr: 针对边缘计算中数据稀缺和资源受限的挑战，本文提出一种基于知识蒸馏的MobileViT预训练方法，旨在提升少样本学习性能。通过将大型教师模型的泛化能力迁移至轻量化学生模型，该方法在MiniImageNet基准上显著提升了分类准确率，同时大幅降低了参数量、计算复杂度和功耗，为边缘AI部署提供了高效实用的解决方案。
selection_source: fresh_fetch
motivation: 边缘AI应用在低数据场景下亟需兼顾高效率、低延迟与低功耗的少样本学习模型。
method: 利用知识蒸馏技术将大规模教师模型的泛化能力迁移到轻量级的MobileViT骨干网络中进行预训练。
result: "相比ResNet12基准，该模型在提升准确率的同时减少了69%的参数和88%的计算量，并在Jetson平台上降低了37%的能耗。"
conclusion: 该研究证明了通过知识蒸馏优化轻量化视觉Transformer是实现在边缘设备上高效部署少样本学习的有效路径。
---

## 摘要
高效且具有适应性的深度学习模型是深度学习研究的一个重要领域，这主要源于边缘设备对高效模型的需求。小样本学习（Few-shot learning）使得深度学习模型能够在低数据量的情况下使用，这种能力在收集大规模标注数据集成本高昂或不切实际的现实应用中备受青睐。这一挑战在边缘场景中尤为突出，因为这些场景可能面临连接受限、需要低延迟响应或受到严格能耗限制的情况。我们提出并评估了一种针对边缘计算设计的 MobileViT 骨干网络预训练方法。具体而言，我们采用了知识蒸馏技术，将大规模教师模型的泛化能力迁移到轻量级学生模型中。在 MiniImageNet 基准测试中，与 ResNet12 基准模型相比，该方法在单样本（one-shot）和五样本（five-shot）分类上的准确率分别提高了 14% 和 6.7%，同时将模型参数量减少了 69%，计算复杂度（FLOPs）降低了 88%。此外，我们将所提模型部署在 Jetson Orin Nano 平台上，并直接在电源处测量了功耗，结果显示动态能耗降低了 37%，延迟仅为 2.6 毫秒。这些结果表明，所提方法是为边缘 AI 硬件部署小样本学习模型的一种极具前景且实用的解决方案。

## Abstract
Efficient and adaptable deep learning models are an important area of deep learning research, driven by the need for highly efficient models on edge devices. Few-shot learning enables the use of deep learning models in low-data regimes, a capability that is highly sought after in real-world applications where collecting large annotated datasets is costly or impractical. This challenge is particularly relevant in edge scenarios, where connectivity may be limited, low-latency responses are required, or energy consumption constraints are critical. We propose and evaluate a pre-training method for the MobileViT backbone designed for edge computing. Specifically, we employ knowledge distillation, which transfers the generalization ability of a large-scale teacher model to a lightweight student model. This method achieves accuracy improvements of 14% and 6.7% for one-shot and five-shot classification, respectively, on the MiniImageNet benchmark, compared to the ResNet12 baseline, while reducing by 69% the number of parameters and by 88% the computational complexity of the model, in FLOPs. Furthermore, we deployed the proposed models on a Jetson Orin Nano platform and measured power consumption directly at the power supply, showing that the dynamic energy consumption is reduced by 37% with a latency of 2.6 ms. These results demonstrate that the proposed method is a promising and practical solution for deploying few-shot learning models on edge AI hardware.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Few-Shot Learning for Edge AI via Knowledge Distillation on MobileViT》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限的边缘 AI 设备上，实现高精度且低功耗的小样本学习（Few-Shot Learning, FSL）。
*   **研究动机**：
    *   边缘设备通常面临计算资源有限、能耗敏感及网络连接不稳定的挑战。
    *   传统 FSL 研究多基于 ResNet 等重型骨干网络，不适合边缘部署。
    *   现有的轻量级模型在小样本场景下的泛化能力往往不足，准确率难以满足实际应用需求。

### 2. 方法论
*   **核心思想**：结合混合架构（MobileViT）的结构优势与知识蒸馏（KD）的泛化能力迁移，构建一个既轻量又强大的边缘 FSL 骨干网络。
*   **关键技术细节**：
    *   **骨干网络选择**：采用 **MobileViT-XXS**。该架构融合了 CNN 的局部特征提取能力和 Vision Transformer (ViT) 的全局上下文建模能力，相比纯 CNN 架构具有更好的领域无关表示能力。
    *   **知识蒸馏预训练**：使用大规模预训练模型 **DINOv2 (ViT-S/14)** 作为教师模型。
    *   **蒸馏策略**：采用简单的特征对齐策略，通过 **均方误差（MSE）损失函数** 使学生模型（MobileViT）的特征嵌入尽可能接近教师模型，避免了复杂的超参数调节。
    *   **FSL 框架**：基于 **EASY** 框架，在推理阶段使用最近类均值（NCM）分类器，通过支持集（Support Set）生成的原型进行分类。

### 3. 实验设计
*   **数据集**：
    *   核心基准：**MiniImageNet**（包含 100 个类，每类 600 张图）。
    *   泛化性测试：在 ImageNet、Caltech101、OxfordPets、Flowers102、Food101、FGVCAircraft、EuroSAT 和 ImageNet-A 等 8 个额外数据集上进行了评估。
*   **Benchmark 与对比方法**：
    *   基准模型：使用 ResNet12 的 EASY 框架。
    *   对比骨干网络：EfficientNet (B0–B3)、MobileNetV3 (Small/Large)。
    *   消融对比：对比了有无知识蒸馏（KD）的 MobileViT 性能。

### 4. 资源与算力
*   **训练阶段**：文中提到模型训练了 100 个 epoch，batch size 为 376，学习率为 0.01/0.001。**未明确说明训练所使用的具体 GPU 型号和数量**。
*   **部署与测试阶段**：
    *   硬件平台：**Jetson Orin Nano**（设置为 15W 功率模式）。
    *   测量工具：使用 Monolithe 1 平台，通过 5 kHz 高频测量板直接从电源输入端监测电压和电流，而非依赖软件估算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 9 个不同领域的数据集，进行了 1-shot 和 5-shot 的全面对比。
    *   每项 FSL 推理均执行 10,000 次任务，并使用 5 个随机种子取平均值及 95% 置信区间。
    *   进行了骨干网络选型实验（对比了 3 个系列多个尺寸的模型）。
*   **充分性评价**：实验设计较为充分，不仅有算法精度的对比，还包含了边缘 AI 关注的延迟、吞吐量、动态功耗和每帧能耗等硬件指标，实验过程客观且具有实际参考价值。

### 6. 主要结论与发现
*   **精度提升**：在 MiniImageNet 上，相比 ResNet12 基准，1-shot 准确率提升了 **14%**，5-shot 提升了 **6.7%**。
*   **效率飞跃**：参数量减少了 **69%**，计算量（FLOPs）降低了 **88%**。
*   **硬件表现**：在 Jetson 平台上，MobileViT-XXS 的推理延迟仅为 **2.6 ms**（ResNet12 为 3.6 ms），动态能耗降低了 **37%**，吞吐量达到 392 ips。
*   **蒸馏价值**：知识蒸馏显著增强了轻量级模型在小样本场景下的泛化能力，使其在多数跨域数据集上表现优于标准预训练。

### 7. 优点
*   **软硬结合**：不仅关注算法改进，还通过物理电力测量验证了模型在真实边缘硬件上的节能效果。
*   **架构优势**：证明了混合架构（CNN+ViT）在处理小样本数据时比纯 CNN 具有更好的全局特征捕捉能力。
*   **实用性强**：提出的蒸馏方案简单稳定，无需复杂的超参数调节，易于在工业界推广。

### 8. 不足与局限
*   **教师模型依赖**：性能高度依赖于强大的教师模型（DINOv2），若在完全无监督或特定垂直领域（如医学、遥感）缺乏优质教师模型，效果可能受限。
*   **训练成本**：虽然推理高效，但知识蒸馏过程需要大型教师模型参与，预训练阶段的算力需求较高。
*   **特定场景失效**：在某些特定数据集（如 EuroSAT 卫星图像）上，蒸馏后的模型表现略逊于基准，说明该通用蒸馏策略在极特殊领域可能存在负迁移风险。

（完）
