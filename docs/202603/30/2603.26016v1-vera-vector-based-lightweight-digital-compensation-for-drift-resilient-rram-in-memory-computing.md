---
title: "VeRA+: Vector-Based Lightweight Digital Compensation for Drift-Resilient RRAM In-Memory Computing"
title_zh: VeRA+：用于抗漂移 RRAM 存内计算的基于向量的轻量级数字补偿
authors: "Weirong Dong, Kai Zhou, Zhen Kong, Zhengke Yang, Quan Cheng, Haoyuan Li, Junkai Huang, Jun Lan, Yida Li, Masanori Hashimoto, Longyang Lin"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26016v1"
tags: ["query:aiot"]
score: 9.0
evidence: 高能效RRAM存内计算
tldr: 针对RRAM存算一体架构中电导漂移导致的长期精度下降问题，本文提出VeRA+轻量级数字补偿框架。该方法通过复用共享投影矩阵并为每个漂移级别引入两个紧凑向量，结合离线训练的漂移感知调度算法，实现了无需重写或在线重训的漂移补偿。实验表明，VeRA+在显著降低存储开销的同时，能有效维持长达十年的模型精度，并通过实测RRAM数据验证了其有效性。
selection_source: fresh_fetch
motivation: 解决RRAM器件电导漂移导致的存算一体系统长期推理精度衰减，以及现有补偿方案存储开销大或依赖在线重训的问题。
method: 采用基于向量的轻量级补偿机制，通过共享投影矩阵和每层仅两个漂移特定向量，配合离线训练的调度算法实现精度修复。
result: "在模拟十年漂移后保留了高达99.77%的初始精度，且存储开销较传统BN校准方法降低了三个数量级以上。"
conclusion: VeRA+为实现长寿命、高可靠性的RRAM存算一体系统提供了一种低开销且实用的数字化漂移补偿方案。
---

## 摘要
基于 RRAM 的存内计算 (IMC) 具有极高的能效，但受限于电导漂移，这会严重降低长期准确率。现有的方法，包括重训练、噪声感知训练以及基于批归一化 (BN) 的校准，要么需要 RRAM 重写，要么需要巨大的存储开销，或者依赖于在线修正。我们提出了 VeRA+，这是一个轻量级的漂移补偿框架，它重用共享投影矩阵，并且针对每个漂移级别仅引入两个紧凑的漂移特定向量。一种漂移感知调度算法离线训练一小部分 VeRA+ 参数，并随时间推移选择合适的参数集，无需任何片上重训练或数据回放。在模拟十年的漂移后，VeRA+ 保留了高达 99.77% 的无漂移准确率，且与基于 BN 的校准相比，存储开销降低了三个数量级以上。为了在真实的器件行为下验证 VeRA+，我们从制造的 1T1R RRAM 器件的测量结果中提取了一周的漂移统计数据，并利用这些数据模拟真实的漂移权重。在这些实测的漂移条件下，VeRA+ 实现了接近无漂移基准的准确率，为 RRAM-IMC 的长期抗漂移能力提供了一种高效且实用的解决方案。

## Abstract
RRAM-based in-memory computing (IMC) offers high energy efficiency but suffers from conductance drift that severely degrades long-term accuracy. Existing approaches including retraining, noise-aware training, and Batch Normalization (BN)-based calibration either require RRAM rewriting, demand large storage overhead, or rely on online correction. We propose VeRA+, a lightweight drift compensation framework that reuses shared projection matrices and introduces only two compact drift-specific vectors per drift level. A drift-aware scheduling algorithm offline-trains a small set of VeRA+ parameters and selects the appropriate set over time without any on-chip retraining or data replay. VeRA+ preserves up to 99.77% of the drift-free accuracy after ten years of simulated drift and reduces storage overhead by more than three orders of magnitude compared with BN-based calibration. To validate VeRA+ under realistic device behavior, we extract one-week drift statistics from measurements on our fabricated 1T1R RRAM devices and use them to simulate realistic drifted weights. Under these measured drift conditions, VeRA+ achieves accuracy close to the drift-free baseline, providing an efficient and practical solution for long-term drift resilience in RRAM-IMC.

---

## 论文详细总结（自动生成）

### VeRA+：用于抗漂移 RRAM 存内计算的基于向量的轻量级数字补偿论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于阻变存储器（RRAM）的存内计算（IMC）虽然能效极高，但面临严重的**电导漂移（Conductance Drift）**问题。随着时间推移，RRAM 单元的电导值会发生不可预测的变化，导致预训练模型在部署数小时、数天或数年后精度大幅下降。
*   **研究动机**：现有的补偿方案存在明显缺陷：
    *   **重训练/重写**：RRAM 写入速度慢、功耗高且寿命有限。
    *   **噪声感知训练**：难以覆盖长期漂移的大范围变化。
    *   **BN 校准**：需要存储大量训练数据（约 5%）并在片上进行在线统计计算，存储和计算开销巨大。
*   **整体含义**：本文旨在提出一种无需重写 RRAM、无需在线重训、且存储开销极低的数字化补偿框架，以实现 RRAM-IMC 系统的长期可靠运行。

#### 2. 论文提出的方法论：VeRA+
*   **核心思想**：借鉴参数高效微调（PEFT）的思想，在 RRAM 存储固定权重（主干）的基础上，利用 SRAM 存储极少量的补偿参数，通过数字电路修正漂移引起的误差。
*   **关键技术细节**：
    *   **混合架构**：采用 RRAM-IMC 处理主干计算，SRAM-IMC 处理轻量级补偿计算。
    *   **向量化随机矩阵自适应（VeRA+）**：
        *   **共享投影**：所有层和所有漂移级别共享一对随机正交投影矩阵 $A_R$ 和 $B_R$。
        *   **层/漂移特定向量**：为每个漂移阶段的每一层仅引入两个紧凑的缩放向量 $b$ 和 $d$。
        *   **计算公式**：$W_{eff} X = W_0 X + b \odot [B_R (d \odot (A_R X))]$。其中 $W_0 X$ 在 RRAM 中完成，其余在 SRAM 中完成。
    *   **CNN 优化设计**：针对卷积神经网络，采用 1×1 卷积核方案生成补偿权重，并利用大矩阵切片技术实现跨层共享，大幅降低了参数量和计算量。
    *   **漂移感知调度算法**：采用指数级时间步长监测精度，仅在精度跌破阈值时才离线训练并分配一组新的补偿向量。

#### 3. 实验设计
*   **数据集与模型**：
    *   **图像分类**：CIFAR-10/100（ResNet-20/32）、ImageNet-1k（ResNet-50）。
    *   **自然语言处理**：QQP、SST-5（BERT-base/large）。
*   **Benchmark（对比方法）**：
    *   无补偿的纯 RRAM 方案。
    *   **LoRA**（低秩自适应）。
    *   **VeRA**（原始向量自适应方案）。
    *   **基于 BN 的校准**（SOTA 漂移补偿方法）。
*   **漂移模型**：
    *   **模拟模型**：IBM Analog AI Kit 的统计漂移模型（模拟长达 10 年）。
    *   **实测模型**：基于作者团队制造的 180nm 1T1R RRAM 器件，提取一周的真实漂移数据进行验证。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到每个漂移级别的补偿向量仅需离线训练 3 个 epoch（batch size 为 64），暗示该方法的训练开销极低。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型 CNN（ResNet-20）到大型 Transformer（BERT-large）的多种架构，并在多个不同难度的任务上进行了验证。
*   **消融实验**：对低秩维度 $r$ 进行了详细分析（$r=1$ 到 $8$），探讨了精度与开销的平衡。
*   **充分性评价**：实验设计较为充分。不仅有长达 10 年的时间跨度模拟，还引入了真实硬件测量数据，增强了结果的说服力。对比维度包括精度、参数量、计算量（Ops）、芯片面积和能效，评估非常全面。

#### 6. 论文的主要结论与发现
*   **精度恢复**：在模拟 10 年漂移后，VeRA+ 能将 ResNet-20 在 CIFAR-100 上的精度从 9.1% 恢复至 66.9%（接近无漂移基准）。
*   **极低开销**：相比于 BN 校准，VeRA+ 的存储开销降低了 **1000 倍以上**（从 7.5 MB 降至 5.15 KB）。
*   **硬件友好**：在 22nm 工艺下，VeRA+ 仅引入约 3.5% 的面积开销和 4.5% 的能效损失，远优于 LoRA 和原始 VeRA。
*   **结构鲁棒性**：发现 Transformer 模型比 CNN 对电导漂移具有更强的内在鲁棒性。

#### 7. 优点
*   **极度轻量化**：通过共享投影矩阵和 1×1 卷积方案，将补偿参数压缩到了极致。
*   **无需重写 RRAM**：完全通过数字侧的轻量级计算解决模拟侧的漂移问题，延长了器件寿命。
*   **实测验证**：使用了自研 RRAM 芯片的测量数据进行闭环验证，证明了方案在真实非均匀漂移环境下的有效性。

#### 8. 不足与局限
*   **混合架构依赖**：该方案依赖于 SRAM-IMC 和数字逻辑的支持，对于纯模拟 RRAM 存算芯片可能需要额外的电路修改。
*   **离线训练依赖**：补偿向量的有效性高度依赖于离线训练时所采用漂移模型的准确性。如果实际环境中的漂移特性与模型偏差较大，补偿效果可能受限。
*   **空间误差补偿有限**：采用 1×1 卷积核方案虽然节省资源，但可能无法完美补偿具有复杂空间相关性的电导误差。

（完）
