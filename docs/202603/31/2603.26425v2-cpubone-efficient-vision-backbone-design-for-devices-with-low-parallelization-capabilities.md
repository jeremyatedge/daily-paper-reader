---
title: "CPUBone: Efficient Vision Backbone Design for Devices with Low Parallelization Capabilities"
title_zh: CPUBone：针对低并行化能力设备的视觉骨干网络高效设计
authors: "Moritz Nottebaum, Matteo Dunnhofer, Christian Micheloni"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26425v2"
tags: ["query:aiot"]
score: 9.0
evidence: 嵌入式系统的高效视觉骨干网络设计
tldr: 针对CPU等低并行化设备，现有视觉骨干网络优化不足。本文提出CPUBone系列模型，通过研究分组卷积和减小卷积核大小对CPU推理效率的影响，在降低计算量（MACs）的同时保持高硬件执行效率。实验证明CPUBone在多种CPU设备上实现了领先的精度与速度平衡，并能有效应用于检测和分割任务。
selection_source: fresh_fetch
motivation: 现有视觉模型多针对高并行硬件优化，而CPU需要一种能平衡计算量与硬件执行效率的特定设计。
method: 通过引入分组卷积和优化卷积核大小，在减少计算成本的同时确保模型在CPU上的高硬件利用率。
result: CPUBone在多种CPU设备上达到了最先进的速度-精度平衡，并在目标检测和语义分割任务中表现优异。
conclusion: 针对CPU特性设计的视觉骨干网络能显著提升低并行化设备上的推理性能。
---

## 摘要
最近关于视觉骨干网络架构的研究主要集中在优化具有高并行处理能力的硬件平台的效率。这一类别越来越多地包括手机和嵌入式 AI 加速模块等嵌入式系统。相比之下，CPU 无法以同样的方式并行化操作，因此模型受益于一种特定的设计理念，即通过保持较高的每秒乘加操作数（MACpS）来平衡操作量（MACs）和硬件执行效率。为此，我们研究了针对标准卷积的两种改进，旨在降低计算成本：分组卷积和减小卷积核尺寸。虽然这两种改进都大幅减少了推理所需的总 MACs，但维持低延迟仍需保持硬件效率。我们在多种 CPU 设备上的实验证实，这些改进成功地在 CPU 上保留了较高的硬件效率。基于这些见解，我们推出了 CPUBone，这是一个针对基于 CPU 推理进行优化的新型视觉骨干模型系列。CPUBone 在广泛的 CPU 设备上实现了最先进的速度-准确度权衡（SATs），并能有效地将其效率迁移到目标检测和语义分割等下游任务中。模型和代码可在 https://github.com/altair199797/CPUBone 获取。

## Abstract
Recent research on vision backbone architectures has predominantly focused on optimizing efficiency for hardware platforms with high parallel processing capabilities. This category increasingly includes embedded systems such as mobile phones and embedded AI accelerator modules. In contrast, CPUs do not have the possibility to parallelize operations in the same manner, wherefore models benefit from a specific design philosophy that balances amount of operations (MACs) and hardware-efficient execution by having high MACs per second (MACpS). In pursuit of this, we investigate two modifications to standard convolutions, aimed at reducing computational cost: grouping convolutions and reducing kernel sizes. While both adaptations substantially decrease the total number of MACs required for inference, sustaining low latency necessitates preserving hardware-efficiency. Our experiments across diverse CPU devices confirm that these adaptations successfully retain high hardware-efficiency on CPUs. Based on these insights, we introduce CPUBone, a new family of vision backbone models optimized for CPU-based inference. CPUBone achieves state-of-the-art Speed-Accuracy Trade-offs (SATs) across a wide range of CPU devices and effectively transfers its efficiency to downstream tasks such as object detection and semantic segmentation. Models and code are available at https://github.com/altair199797/CPUBone.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《CPUBone: Efficient Vision Backbone Design for Devices with Low Parallelization Capabilities》** 的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉骨干网络（Backbone）大多针对 GPU、NPU 等具有高并行处理能力的硬件进行优化。然而，在许多实际应用场景中，CPU 仍是主要的计算单元。
*   **核心痛点**：CPU 的并行化能力有限，导致那些在 GPU 上表现优异的模型（通常具有高 MACs 但高并行度）在 CPU 上运行缓慢。
*   **核心理念**：论文指出，评估 CPU 效率不能仅看 MACs（乘加运算数），而应关注 **MACpS**（每秒执行的 MAC 数）。研究目标是设计一种在降低 MACs 的同时，能保持高 MACpS 的 CPU 专用架构。

### 2. 方法论
*   **核心思想**：通过对标准卷积进行微调（分组和减小核尺寸），在不损失硬件执行效率的前提下显著降低计算成本。
*   **关键技术细节**：
    *   **分组卷积（Grouping）**：将 `groups` 参数设为 2。公式显示 MACs 与分组数成反比（$M \propto 1/groups$）。实验发现 `groups=2` 在 CPU 上能维持与不分组卷积相近的 MACpS，从而有效降低延迟。
    *   **减小卷积核（Kernel Size）**：将常用的 3x3 卷积核减小为 2x2。这能减少约 56% 的 MACs，且实验证明 2x2 卷积在 CPU 上的硬件效率（MACpS）甚至优于 3x3。
    *   **混合 Block 设计**：
        *   **GrFuMBConv**（分组融合 MBConv）：适用于通道数 < 256 的层，利用融合操作提高小通道下的执行效率。
        *   **GrMBConv**（分组非融合 MBConv）：适用于通道数 ≥ 256 的层，避免宽通道下融合操作导致的效率下降。
    *   **宏观架构**：借鉴 LowFormer，在最后两个阶段引入轻量化注意力机制（LowFormer Attention），并用最近邻上采样替换转置卷积以进一步提速。

### 3. 实验设计
*   **数据集**：
    *   图像分类：ImageNet-1K。
    *   目标检测：COCO 2017（使用 RetinaNet 框架）。
    *   语义分割：ADE20K（使用 Semantic FPN 框架）。
*   **Benchmark 与对比方法**：
    *   对比了 FasterNet, RepViT, MobileOne, EfficientMod, FastViT, EffFormerV2 等多种近年来的轻量化 SOTA 模型。
*   **测试设备**：
    *   ARM CPU：Raspberry Pi 5, Google Pixel 4/7 Pro, Snapdragon 8 Elite。
    *   Intel CPU：Intel Xeon W-2125。
    *   GPU（对比用）：Nvidia Titan RTX。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，训练 320 个 epoch（含 20 个 warmup epoch），采用 Cosine 学习率衰减。
*   **算力资源**：文中提到使用了 Leonardo 超算中心（CINECA）的资源进行实验，但**未明确给出**具体的 GPU 型号数量、单次训练的总时长或总算力消耗（如 TFLOPS）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从基础分类到下游检测、分割任务的全面验证。
    *   针对不同 CPU 架构（ARM vs Intel）和不同移动端设备进行了广泛测试。
    *   **消融实验**：非常详尽，分别验证了分组数（2, 4, 8）、卷积核大小（2x2 vs 3x3）、注意力机制组件、以及融合/非融合块对性能和延迟的影响。
*   **客观性**：通过引入 MACpS 指标，客观解释了为什么某些模型在 GPU 上快但在 CPU 上慢，实验对比具有较强的说服力。

### 6. 主要结论与发现
*   **CPU 专特性**：分组卷积（groups=2）和 2x2 卷积核在 CPU 上能保持极高的硬件效率，但在 GPU 上会导致 MACpS 剧烈下降（最高下降 80%），证明了“硬件感知设计”的必要性。
*   **性能领先**：CPUBone 系列（B0-B3）在多种 CPU 平台上均实现了比 FasterNet 和 RepViT 更优的速度-准确度权衡（SAT）。
*   **下游任务**：在检测和分割任务中，CPUBone 在保持相似精度的前提下，推理速度比同类模型快 3 到 4 倍。

### 7. 优点
*   **指标创新**：强调 MACpS 而非单纯的 MACs，为低并行度硬件的模型设计提供了清晰的指导原则。
*   **实用性强**：针对不同通道宽度动态切换 Block 类型，充分利用了 CPU 的缓存和计算特性。
*   **跨平台表现**：在嵌入式 ARM（树莓派）和桌面端 Intel CPU 上均表现出一致的优越性。

### 8. 不足与局限
*   **硬件局限性**：该模型是为 CPU “偏科”设计的，在 GPU 上的表现极差，不适合跨硬件（CPU+GPU）混合部署的场景。
*   **感受野风险**：大量使用 2x2 卷积核虽然提升了速度，但可能会限制模型的感受野，论文通过仅在深层使用 2x2 来缓解此问题，但在极大规模场景下的表现尚待验证。
*   **算力细节缺失**：未详细披露训练所需的具体算力资源，不便于其他研究者评估复现成本。

（完）
