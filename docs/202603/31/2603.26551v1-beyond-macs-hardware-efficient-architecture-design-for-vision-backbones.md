---
title: "Beyond MACs: Hardware Efficient Architecture Design for Vision Backbones"
title_zh: 超越 MACs：视觉骨干网络的硬件高效架构设计
authors: "Moritz Nottebaum, Matteo Dunnhofer, Christian Micheloni"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26551v1"
tags: ["query:aiot"]
score: 10.0
evidence: 边缘设备视觉骨干网的硬件高效架构设计
tldr: 本研究指出传统的MACs指标在衡量边缘设备执行效率时存在局限性，并以此为基础提出了硬件高效的视觉骨干网络LowFormer。该网络通过精简的宏观与微观设计，引入了轻量级的Lowtention机制替代传统多头自注意力。LowFormer在ImageNet及目标检测、语义分割等下游任务中表现优异，在多种硬件平台上实现了显著的加速，为高效视觉模型设计提供了新思路。
selection_source: fresh_fetch
motivation: 传统的MACs指标无法准确预测边缘设备上的实际执行时间，导致模型设计在硬件效率上存在偏差。
method: 提出了LowFormer系列网络，其核心是采用轻量化的Lowtention机制和精简的架构设计来提升硬件执行效率。
result: LowFormer在ImageNet及多种下游任务上取得了优于现有SOTA模型的结果，并在不同硬件平台上实现了显著的推理加速。
conclusion: 优化视觉骨干网络应超越MACs指标，LowFormer证明了结合硬件特性设计的架构能兼顾高性能与高效率。
---

## 摘要
视觉骨干网络在现代计算机视觉中发挥着核心作用。提高其效率直接惠及广泛的下游应用。为了衡量效率，许多文献依赖 MACs（乘加运算次数）作为执行时间的预测指标。在本文中，我们通过实验证明了该指标的局限性，特别是在边缘设备的背景下。通过对比常见架构设计元素的 MAC 计数和执行时间，我们确定了高效执行的关键因素，并为优化骨干网络设计提供了见解。基于这些见解，我们提出了 LowFormer，一个新颖的视觉骨干网络系列。LowFormer 具有精简的宏观和微观设计，其中包括 Lowtention，这是一种多头自注意力（Multi-Head Self-Attention）的轻量级替代方案。Lowtention 不仅被证明更高效，而且在 ImageNet 上取得了更优异的结果。此外，我们还提出了 LowFormer 的边缘 GPU 版本，可以进一步提高其在边缘 GPU 和桌面 GPU 上的基准速度。我们通过在较小的图像分类数据集上进行评估，并将其应用于目标检测、语义分割、图像检索和视觉目标跟踪等多个下游任务，证明了 LowFormer 的广泛适用性。与最近的最先进骨干网络相比，LowFormer 模型在各种硬件平台上始终实现了显著的加速。代码和模型可在 https://github.com/altair199797/LowFormer/blob/main/Beyond_MACs.md 获取。

## Abstract
Vision backbone networks play a central role in modern computer vision. Enhancing their efficiency directly benefits a wide range of downstream applications. To measure efficiency, many publications rely on MACs (Multiply Accumulate operations) as a predictor of execution time. In this paper, we experimentally demonstrate the shortcomings of such a metric, especially in the context of edge devices. By contrasting the MAC count and execution time of common architectural design elements, we identify key factors for efficient execution and provide insights to optimize backbone design. Based on these insights, we present LowFormer, a novel vision backbone family. LowFormer features a streamlined macro and micro design that includes Lowtention, a lightweight alternative to Multi-Head Self-Attention. Lowtention not only proves more efficient, but also enables superior results on ImageNet. Additionally, we present an edge GPU version of LowFormer, that can further improve upon its baseline's speed on edge GPU and desktop GPU. We demonstrate LowFormer's wide applicability by evaluating it on smaller image classification datasets, as well as adapting it to several downstream tasks, such as object detection, semantic segmentation, image retrieval, and visual object tracking. LowFormer models consistently achieve remarkable speed-ups across various hardware platforms compared to recent state-of-the-art backbones. Code and models are available at https://github.com/altair199797/LowFormer/blob/main/Beyond_MACs.md.

---

## 论文详细总结（自动生成）

### 论文总结：超越 MACs：视觉骨干网络的硬件高效架构设计

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于指出：**MACs（乘加运算次数）并非衡量视觉骨干网络在实际硬件（尤其是边缘设备）上运行速度的可靠指标**。
*   **研究动机**：许多研究致力于降低 MACs 以提升效率，但由于内存访问成本（MAC）和硬件并行度的差异，MACs 较低的模型在实际设备上的延迟（Latency）反而可能更高。
*   **背景**：随着视觉模型在机器人、自动驾驶和移动设备上的广泛应用，开发者迫切需要一种能真实反映硬件执行效率的设计准则，而非仅仅追求理论计算量的减少。

#### 2. 方法论：核心思想与关键技术
论文通过实验分析了卷积、注意力机制在不同分辨率和通道数下的硬件表现，并据此提出了 **LowFormer** 架构：
*   **硬件效率准则**：
    *   **深度卷积（DWConv）的低效性**：在边缘设备上，DWConv 的内存交换开销大，实际执行效率远低于标准卷积。
    *   **融合 MBConv（Fused MBConv）**：在通道数较少（<256）的早期阶段，使用融合卷积替代传统的“逐点-深度-逐点”结构更快。
    *   **分辨率 vs 通道**：高分辨率层的硬件效率通常低于高通道数层，因此应将计算量集中在后期低分辨率阶段。
*   **核心组件 - Lowtention**：
    *   一种轻量化的自注意力机制。在执行缩放点积注意力（SDA）之前，通过卷积进行**空间下采样**和**通道压缩**，显著降低了注意力机制随分辨率增加而产生的二次方复杂度。
*   **宏观设计**：
    *   前三个阶段采用纯卷积结构且层数较少；后两个阶段引入 Lowtention。
    *   针对边缘 GPU（如 Jetson）提出了 **Edge 变体（E1-E3）**，通过移除内存受限的 MLP 层或减少深度来进一步提升并行效率。

#### 3. 实验设计
*   **数据集/场景**：
    *   **图像分类**：ImageNet-1K（核心基准）、Oxford-IIIT-Pets、Stanford Cars、Flowers-102。
    *   **下游任务**：COCO 2017（目标检测）、ADE20K（语义分割）、GPR1200（图像检索）、GOT10K/LaSOT 等（视觉目标跟踪）。
*   **Benchmark 与对比方法**：
    *   对比了包括 MobileOne、FastViT、RepViT、EfficientMod、EdgeViT、BiFormer 等在内的多种最新（SOTA）轻量级视觉骨干网络。
*   **硬件平台**：涵盖了服务器 GPU (A40)、桌面 GPU (Titan RTX)、边缘 GPU (Jetson TX2)、移动端 (iPhone 13) 和嵌入式 CPU (树莓派 5)。

#### 4. 资源与算力
*   **训练细节**：在 ImageNet-1K 上训练 320 个 epoch，使用 AdamW 优化器，采用余弦退火学习率。
*   **算力设备**：文中提到使用 Nvidia A40 进行吞吐量测试，Nvidia Titan RTX 进行延迟测试。
*   **训练规模**：LowFormer-B3 的 Batch Size 设为 2400，B2 为 850。具体的总训练时长（如 GPU-hours）未在文中明确标出，但给出了详细的超参数配置。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   **基础性能测试**：5 种不同规模的 LowFormer 基准模型（B0-B3）及 3 种 Edge 变体。
    *   **消融实验**：针对 MBConv 融合、Lowtention 的下采样/通道压缩、MLP 的移除等关键设计做了逐一验证。
    *   **跨任务验证**：涵盖了从分类到跟踪的 5 大类视觉任务。
*   **充分性与公平性**：实验在多种硬件架构（GPU, ARM CPU, NPU）上运行，并使用了统一的测量协议（如 TorchScript, ONNX, TensorRT），对比基准涵盖了 2023-2025 年间的主流模型，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **超越 MACs**：LowFormer 在 MACs 高于某些竞争对手的情况下，实际推理速度却快得多（例如在 Jetson TX2 上比同精度的模型快 2-3 倍）。
*   **Lowtention 的有效性**：通过降低 SDA 的输入维度，不仅提升了速度，还因卷积引入的归纳偏置提升了 ImageNet 的准确率。
*   **硬件定制化**：针对边缘 GPU 移除 MLP 层能显著提升速度，因为 MLP 在此类硬件上往往受限于内存带宽而非计算能力。

#### 7. 优点
*   **实战导向**：不迷信理论指标，直接针对真实硬件延迟进行优化，对工业界部署具有极高参考价值。
*   **全能型骨干**：在分类、检测、分割、检索和跟踪任务中均表现出极强的通用性和效率优势。
*   **开源贡献**：提供了完整的代码和多种硬件平台下的预训练模型。

#### 8. 不足与局限
*   **移动端优化偏差**：针对边缘 GPU（Jetson）优化的 Edge 变体在移动端（iPhone NPU/GPU）上并未表现出同样的加速效果，说明不同硬件架构间的优化策略难以完全通用。
*   **参数量较大**：为了换取速度，LowFormer 的参数量（Params）通常高于同等精度的模型，这可能在存储空间受限的极小嵌入式系统中成为限制因素。
*   **时间线疑问**：论文标注日期为 2026 年，可能存在元数据错误或为前瞻性研究。

（完）
