---
title: "MotiMem: Motion-Aware Approximate Memory for Energy-Efficient Neural Perception in Autonomous Vehicles"
title_zh: MotiMem：面向自动驾驶车辆能效神经感知的运动感知近似内存
authors: "Haohua Que, Mingkai Liu, Jiayue Xie, Haojia Gao, Jiajun Sun, Hongyi Xu, Handong Yao, Fei Qiao"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27108v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对能效感知的软硬件协同设计
tldr: "针对自动驾驶中高分辨率传感器带来的内存墙和高能耗问题，本文提出MotiMem，一种软硬件协同设计的近似内存接口。它利用时间相干性，通过轻量级2D运动传播识别感兴趣区域（RoI），并结合混合稀疏感知编码（自适应反转和截断）来降低总线切换活动。实验表明，该方法在保持约93%检测精度的同时，减少了43%的内存接口动态能耗，显著优于JPEG和WebP等传统编解码器。"
selection_source: fresh_fetch
motivation: 自动驾驶中高分辨率传感器导致的数据传输能耗已超过计算能耗，且传统图像压缩方法缺乏语义感知，无法有效优化总线切换活动。
method: 提出MotiMem接口，利用2D运动传播动态识别RoI，并采用混合稀疏感知编码方案通过自适应反转和截断来诱导位级稀疏性。
result: "在多个主流数据集和16个检测模型上的实验显示，MotiMem在保留约93%精度的前提下，将内存接口动态能耗降低了约43%。"
conclusion: MotiMem通过结合运动感知和稀疏编码，在自动驾驶感知的能效与精度之间建立了优于传统方法的帕累托前沿。
---

## 摘要
高分辨率传感器对于鲁棒的自动驾驶感知至关重要，但给电池受限的电动汽车带来了严重的“内存墙”挑战。在这些系统中，数据传输能耗通常超过了计算能耗。传统的图像压缩并不适用，因为它们缺乏语义感知，且优化目标是存储空间而非总线切换活动。我们提出了 MotiMem，一种软硬件协同设计的接口。利用时间相干性，MotiMem 使用轻量级二维运动传播（2D Motion Propagation）来动态识别感兴趣区域（RoI）。与此相辅相成的是，一种混合稀疏感知编码方案利用自适应反转和截断来诱导位级稀疏性。在 nuScenes、Waymo 和 KITTI 数据集上使用 16 种检测模型进行的广泛实验表明，MotiMem 在保留约 93% 的目标检测准确率的同时，将内存接口动态能耗降低了约 43%，建立了一个显著优于 JPEG 和 WebP 等标准编解码器的新帕累托前沿。

## Abstract
High-resolution sensors are critical for robust autonomous perception but impose a severe memory wall on battery-constrained electric vehicles. In these systems, data movement energy often outweighs computation. Traditional image compression is ill-suited as it is semantically blind and optimizes for storage rather than bus switching activity. We propose MotiMem, a hardware-software co-designed interface. Exploiting temporal coherence,MotiMem uses lightweight 2D Motion Propagation to dynamically identify Regions of Interest (RoI). Complementing this, a Hybrid Sparsity-Aware Coding scheme leverages adaptive inversion and truncation to induce bitlevel sparsity. Extensive experiments across nuScenes, Waymo, and KITTI with 16 detection models demonstrate that MotiMem reduces memory-interface dynamic energy by approximately 43 percent while retaining approximately 93 percent of the object detection accuracy, establishing a new Pareto frontier significantly superior to standard codecs like JPEG and WebP.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MotiMem** 的软硬件协同设计接口，旨在解决自动驾驶车辆中高分辨率传感器带来的“内存墙”与高能耗挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在自动驾驶系统中，高分辨率相机（4K/8K）产生海量数据，导致数据在传感器、总线与内存之间的移动能耗甚至超过了计算本身的能耗。
*   **研究背景**：
    *   **内存墙瓶颈**：电池受限的边缘设备难以承受高带宽数据传输带来的动态功耗。
    *   **传统压缩缺陷**：JPEG、WebP 等标准编解码器是“语义盲”的（对背景和关键目标一视同仁），且优化目标是存储体积而非硬件层面的**位翻转活动（Switching Activity）**。
    *   **高熵问题**：标准压缩后的比特流通常具有高随机性，导致总线切换频繁，反而增加了动态功耗。

### 2. 方法论
MotiMem 采用了一种**闭环、语义感知**的近似内存接口设计，核心思想是利用视频的时间相干性进行非均匀精度存储。
*   **运动感知 RoI 预测**：
    *   利用上一帧（$T-1$）的检测结果，通过轻量级的 **2D 运动传播模型**（恒定速度模型）预测当前帧（$T$）的目标位置。
    *   对预测框进行膨胀处理（Margin $\delta$），生成块粒度（如 16x16）的**感兴趣区域（RoI）掩码**。
*   **混合稀疏感知编码（Hybrid Sparsity-Aware Coding）**：
    *   **RoI 路径**：保持高保真度。通过**自适应反转**减少高位（MSB）中“1”的密度，并将反转标志位嵌入到像素的最低位（LSB）中，实现近乎无损的存储。
    *   **背景路径**：执行激进的**截断**（仅保留前 $k$ 位，通常 $k=4$）和自适应反转。
*   **位级优化逻辑**：通过减少比特流中“1”的密度（Bit-1 density）和切换率，直接降低内存接口的动态功耗（$P \propto \alpha CV^2f$）。

### 3. 实验设计
*   **数据集**：使用了自动驾驶领域三大主流数据集：**nuScenes**、**Waymo** 和 **KITTI**。
*   **Benchmark（基准）**：以原始 8-bit 未压缩图像作为精度上限。
*   **对比方法**：
    *   **标准编解码器**：JPEG (Q10, Q50, Q100)、WebP (Q10, Q50, Q100)、无损 JPEG2000。
    *   **消融实验**：Global $k=4$（全局统一截断，无 RoI 引导）、Uniform 4-bit（无反转优化的均匀量化）。
*   **检测模型**：覆盖了 16 种主流模型，包括 **YOLO 系列**（v5, v8, v9, v10, v11, YOLO26）和 Transformer 架构的 **RT-DETR**。

### 4. 资源与算力
*   **硬件环境**：实验在配备 Intel Core Ultra 9 285K CPU、128GB RAM 和 **NVIDIA RTX 6000 Pro GPU** 的工作站上完成。
*   **软件实现**：使用 PyTorch 进行功能性仿真。
*   **算力说明**：文中未明确提及具体的训练时长或总算力消耗（单位如 TFLOPS），因为该研究侧重于推理阶段的接口编码模拟，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：跨 3 个数据集、16 个模型，进行了数百组推理实验。
*   **充分性**：
    *   **多维度评估**：不仅评估了感知精度（mAP），还引入了归一化位密度（NBD）作为硬件能耗的线性代理指标。
    *   **闭环验证**：RoI 预测基于前一帧的真实模型检测输出而非真值（Ground Truth），真实模拟了部署环境。
    *   **灵敏度分析**：对关键参数 $k$（保留位数）进行了从 1 到 7 的全扫描，确定了 $k=4$ 的帕累托最优解。

### 6. 主要结论
*   **能效显著**：MotiMem 将内存接口的动态能耗代理指标降低了约 **43%**。
*   **精度保留**：在大幅节能的同时，平均保留了约 **93%** 的目标检测精度（mAP）。
*   **帕累托前沿**：相比 JPEG 和 WebP，MotiMem 在“精度-能耗”权衡曲线上表现极佳，证明了语义感知分配优于全局统计压缩。

### 7. 优点
*   **软硬协同创新**：将高层语义（RoI）与底层硬件物理特性（位翻转功耗）紧密结合。
*   **极低开销**：运动预测和位级编码均为轻量级操作，不涉及复杂的光流计算或深度学习编解码，适合实时系统。
*   **即插即用**：作为内存接口层，无需修改下游感知模型的权重即可直接应用。

### 8. 不足与局限
*   **物理验证缺失**：能耗节省目前基于解析模型和位密度代理指标，尚未在真实的 FPGA 或 ASIC 芯片上进行物理功耗实测。
*   **冷启动与突发性**：依赖上一帧信息，在系统启动首帧或物体突然高速进入画面时，RoI 预测可能失效，需依赖全局备份机制。
*   **任务覆盖**：目前主要验证了 2D 目标检测，对于语义分割、深度估计或 3D 点云感知的适用性尚待进一步研究。

（完）
