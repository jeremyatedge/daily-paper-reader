---
title: "Expert Streaming: Accelerating Low-Batch MoE Inference via Multi-chiplet Architecture and Dynamic Expert Trajectory Scheduling"
title_zh: 专家流：通过多芯粒架构和动态专家轨迹调度加速低批次 MoE 推理
authors: "Songchen Ma, Hongyi Li, Weihao Zhang, Yonghao Tan, Pingcheng Dong, Yu Liu, Lan Liu, Yuzhong Jiao, Xuejiao Liu, Luhong Liang, Kwang-Ting Cheng"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27624v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘AI MoE推理的多芯粒架构
tldr: 针对边缘AI中低批次MoE推理面临的内存限制和负载不均问题，本文提出了一种适用于多芯粒架构的并行范式FSE-DP。该方法通过在高速D2D链路上调度细粒度的专家流和动态轨迹，实现了计算与通信的自适应重叠及负载均衡。实验表明，该方案在显著提升推理速度的同时，大幅降低了片上内存需求，为边缘端高效部署大模型提供了新思路。
selection_source: fresh_fetch
motivation: 边缘设备在进行低批次MoE推理时，面临片上内存受限、负载严重不均以及模型卸载导致的访存瓶颈。
method: 提出完全分片专家数据并行（FSE-DP），通过在多芯粒架构上调度细粒度专家流和动态轨迹来优化计算与通信重叠。
result: "该方法相比现有技术实现了1.22至2.00倍的推理加速，并节省了高达78.8%的片上内存空间。"
conclusion: FSE-DP证明了利用多芯粒互连和细粒度调度可以有效解决MoE在边缘端的部署难题。
---

## 摘要
混合专家模型（MoE）是边缘 AI 低批次推理中一种极具前景的方法。然而，设备端部署通常面临片上内存受限和严重的工作负载不平衡问题；普遍采用的卸载（offloading）技术进一步导致了片外内存访问瓶颈。此外，MoE 的稀疏性和动态门控机制使分布式策略向更细的粒度转变，并引入了运行时调度方面的考量。近年来，高 die-to-die 带宽的芯粒互连为多芯粒系统通过细粒度调度解决工作负载不平衡和卸载瓶颈创造了新机遇。在本文中，我们提出了全分片专家数据并行（Fully Sharded Expert Data Parallelism, FSE-DP），这是一种专门为多芯粒加速器上的低批次 MoE 推理设计的并行化范式。FSE-DP 通过在高带宽 D2D 链路上沿动态轨迹编排细粒度的互补专家流，实现了自适应的计算-通信重叠和负载均衡。随之而来的数据流复杂性通过一套极简的、硬件友好的虚拟化规则和轻量级调度算法得以控制。我们的方法相比最先进的基准模型实现了 1.22 到 2.00 倍的加速，并节省了高达 78.8% 的片上内存。

## Abstract
Mixture-of-Experts is a promising approach for edge AI with low-batch inference. Yet, on-device deployments often face limited on-chip memory and severe workload imbalance; the prevalent use of offloading further incurs off-chip memory access bottlenecks. Moreover, MoE sparsity and dynamic gating shift distributed strategies toward much finer granularity and introduce runtime scheduling considerations. Recently, high die-to-die bandwidth chiplet interconnects have created new opportunities for multi-chiplet systems to address workload imbalance and offloading bottlenecks with fine-grained scheduling. In this paper, we propose Fully Sharded Expert Data Parallelism, a parallelization paradigm specifically architected for low-batch MoE inference on multi-chiplet accelerators. FSE-DP attains adaptive computation-communication overlap and balanced load by orchestrating fine-grained, complementary expert streams along dynamic trajectories across high-bandwidth D2D links. The attendant dataflow complexity is tamed by a minimal, hardware-amenable set of virtualization rules and a lightweight scheduling algorithm. Our approach achieves 1.22 to 2.00 times speedup over state-of-the-art baselines and saves up to 78.8 percent on-chip memory.

---

## 论文详细总结（自动生成）

以下是对论文《Expert Streaming: Accelerating Low-Batch MoE Inference via Multi-chiplet Architecture and Dynamic Expert Trajectory Scheduling》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向边缘端（如 AI PC、机器人、自动驾驶）迁移，混合专家模型（MoE）因其“激活参数少、模型容量大”的特性成为首选。然而，在边缘场景下，MoE 推理面临三大挑战：
*   **片上内存受限**：SRAM 成本高，难以容纳巨大的 MoE 权重，导致频繁的权重卸载（Offloading）。
*   **外部访存瓶颈**：低批次（Low-batch）推理下权重重用率极低，DDR 带宽成为性能瓶颈。
*   **动态负载不均**：MoE 的专家激活具有明显的“长尾效应”，不同专家处理的 Token 数量差异巨大，导致计算资源闲置或通信拥塞。
**研究动机**：利用新兴的多芯粒（Multi-chiplet）架构和高带宽 Die-to-Die（D2D）互连技术（如 UCIe），将通信带宽转化为架构资源，通过细粒度的专家流调度来解决上述瓶颈。

### 2. 论文提出的方法论：FSE-DP
论文提出了一种名为 **全分片专家数据并行（FSE-DP）** 的并行化范式，核心思想是将整个芯粒阵列视为一个统一的池化缓冲区。

*   **核心思想**：不再将专家固定在某个芯粒上，而是将专家权重切分为微切片（Micro-slices），让权重在计算过程中沿着动态预设的轨迹在芯粒间“流动”（Streaming）。
*   **关键技术细节**：
    *   **微切片流（Micro-slice Flow）**：将专家权重进一步细分，实现计算与 D2D 通信的深度流水化。芯粒在计算当前微切片的同时，接收下一个微切片并转发已完成的微切片。
    *   **配对加载策略（Paired-load Policy）**：根据专家热度（Token 激活数）进行排序，将“热专家”（计算密集）与“冷专家”（通信密集）配对执行，以平衡计算与 DDR 加载时间。
    *   **令牌缓冲（Token Buffering）**：针对极冷专家，允许请求在当前层暂时挂起，等待后续迭代累积更多 Token 后再处理，以提高带宽利用率。
    *   **虚拟化规则（Virtualization Rules）**：提出了一套硬件友好的抽象规则（如“收到即计算、计算即转发”），使复杂的动态数据流能够自动在硬件调度器中实现，无需复杂的软件编程。
*   **算法流程**：调度器根据 Token 局部性和资源可用性，动态规划每个专家的执行轨迹（Trajectory），并实时更新芯粒的空闲状态。

### 3. 实验设计
*   **数据集/场景**：使用语言建模基准测试 **Wikitext-2** 和 **C4**，并在动机分析中使用了 **WinoGrande**。
*   **模型覆盖**：评估了四种代表性 MoE 模型：**Phi-3.5-MoE** (16专家)、**Yuan2.0-M32** (32专家)、**DeepSeek-MoE** (64专家) 和 **Qwen3-30B-A3B** (128专家)。
*   **Benchmark（对比方法）**：
    *   **EP (Expert Parallelism)**：传统的专家并行方法。
    *   **Hydra**：一种针对多芯粒系统优化的软件-硬件协同方案（SOTA）。
*   **测试配置**：针对不同输入 Token 数（16, 64, 256, 1024）进行测试，模拟低批次边缘推理环境。

### 4. 资源与算力
*   **硬件原型**：论文基于一颗 **已流片的 2×2 5nm MCM 测试芯片** 进行评估。该芯片集成了符合 UCIe 标准的高速 D2D 互连。
*   **算力规格**：每个计算芯粒包含 2048 个 MAC，运行频率 800 MHz，峰值算力为 4.865 TOPS。
*   **模拟器**：使用周期精确（Cycle-accurate）模拟器，并利用测试芯片的实测数据进行校准，以评估更大规模（如 3×3, 4×4）的阵列。
*   **训练/推理时长**：论文侧重于推理加速，未提及模型训练时长。

### 5. 实验数量与充分性
*   **实验维度**：涵盖了单层 MoE 延迟、端到端吞吐量、片上内存占用、利用率波动、设计空间探索（DSE）以及扩展性分析。
*   **消融实验**：设计了 5 组消融配置（A1-A5），逐一验证了微切片流、配对加载、虚拟化规则和令牌缓冲对性能的贡献。
*   **充分性评价**：实验设计非常充分且客观。通过真实芯片数据校准模拟器，保证了结果的可信度；对比了不同规模和专家数量的模型，证明了方法的通用性。

### 6. 论文的主要结论与发现
*   **性能提升**：FSE-DP 相比 EP 和 Hydra 实现了 **1.22-2.00 倍** 的推理加速。
*   **内存节省**：通过消除冗余存储，节省了高达 **78.8%** 的片上内存，使 32MB 以内的 SRAM 即可高效运行大型 MoE 模型。
*   **负载均衡**：动态轨迹调度显著降低了计算利用率的波动，使系统能更好地应对 MoE 的长尾激活分布。
*   **扩展性**：在 4×4 阵列中，FSE-DP 的利用率下降远小于传统方法，证明其更适合大规模芯粒集成。

### 7. 优点（亮点）
*   **软硬协同创新**：不仅提出了算法，还设计了专门的硬件调度器（RTL 已合成，面积仅 0.43 $mm^2$）。
*   **打破传统范式**：创造性地用“专家流”代替“Token 转发”，充分利用了 D2D 带宽这一硬件红利。
*   **自适应性强**：虚拟化规则允许系统在无需人工干预的情况下，自动适应动态变化的 Token 负载。

### 8. 不足与局限
*   **硬件依赖性**：该方法高度依赖高带宽、低延迟的 D2D 互连（如 UCIe），在传统总线或低速互连的芯片上效果有限。
*   **控制开销**：当微切片划分过细时，硬件调度器的控制开销和包头开销会增加，可能导致性能边际效应递减。
*   **应用限制**：主要针对低批次（Low-batch）推理优化，在云端超大批次（High-batch）场景下，传统的专家并行可能已经足够高效。
*   **QoS 风险**：令牌缓冲策略虽然提高了效率，但会引入额外的请求延迟，需在系统吞吐量与单请求延迟（QoS）之间进行权衡。

（完）
