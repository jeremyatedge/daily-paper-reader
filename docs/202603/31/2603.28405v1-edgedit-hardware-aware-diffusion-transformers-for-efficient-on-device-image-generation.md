---
title: "EdgeDiT: Hardware-Aware Diffusion Transformers for Efficient On-Device Image Generation"
title_zh: EdgeDiT：面向高效端侧图像生成的硬件感知扩散 Transformer
authors: "Sravanth Kodavanti, Manjunath Arveti, Sowmya Vajrala, Srinivas Miriyala, Vikram N R"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28405v1"
tags: ["query:aiot"]
score: 10.0
evidence: 移动端NPU上扩散Transformer的硬件感知优化
tldr: 针对扩散Transformer（DiT）在边缘设备上计算开销巨大的问题，本文提出了EdgeDiT。这是一种专为移动端NPU设计的硬件感知生成式Transformer系列模型。通过硬件感知优化框架，系统性地识别并修剪了对移动数据流负担较重的结构冗余。EdgeDiT在保持生成质量的同时，显著减少了参数量和计算量，实现了1.65倍的端侧加速，为在移动设备上部署大规模生成式模型提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的扩散Transformer模型因巨大的计算复杂度和内存需求，难以在资源受限的移动端NPU上实现高效部署。
method: 采用硬件感知优化框架，针对移动端数据流特性，系统性地识别并修剪DiT骨干网络中的结构冗余。
result: "模型参数减少20-30%，FLOPs降低36-46%，端侧推理延迟缩减至原来的1.65倍，且保持了优异的图像生成质量。"
conclusion: EdgeDiT在生成质量与推理速度之间达到了更优的平衡，为将大规模基础模型迁移至移动端提供了可扩展的蓝图。
---

## 摘要
扩散 Transformer (DiT) 在高保真图像合成领域树立了新的基准；然而，其巨大的计算复杂度和内存需求阻碍了在资源受限的边缘设备上的本地部署。在本文中，我们介绍了 EdgeDiT，这是一系列专为移动神经网络处理器 (NPU)（如高通 Hexagon 和苹果神经网络引擎 ANE）设计的硬件高效型生成式 Transformer。通过利用硬件感知优化框架，我们系统地识别并剪枝了 DiT 骨干网络中对移动数据流负担特别重的结构冗余。我们的方法产生了一系列轻量级模型，在不牺牲原始 Transformer 架构的缩放优势或表达能力的情况下，实现了参数量减少 20-30%、FLOPs 降低 36-46% 以及端侧延迟降低 1.65 倍。广泛的基准测试表明，与优化的移动端 U-Net 和原生 DiT 变体相比，EdgeDiT 在 Frechet Inception Distance (FID) 和推理延迟之间提供了更优的帕累托最优权衡。通过直接在设备上实现响应迅速、私密且离线的生成式 AI，EdgeDiT 为将大规模基础模型从高端 GPU 迁移到用户掌中提供了一个可扩展的蓝图。

## Abstract
Diffusion Transformers (DiT) have established a new state-of-the-art in high-fidelity image synthesis; however, their massive computational complexity and memory requirements hinder local deployment on resource-constrained edge devices. In this paper, we introduce EdgeDiT, a family of hardware-efficient generative transformers specifically engineered for mobile Neural Processing Units (NPUs), such as the Qualcomm Hexagon and Apple Neural Engine (ANE). By leveraging a hardware-aware optimization framework, we systematically identify and prune structural redundancies within the DiT backbone that are particularly taxing for mobile data-flows. Our approach yields a series of lightweight models that achieve a 20-30% reduction in parameters, a 36-46% decrease in FLOPs, and a 1.65-fold reduction in on-device latency without sacrificing the scaling advantages or the expressive capacity of the original transformer architecture. Extensive benchmarking demonstrates that EdgeDiT offers a superior Pareto-optimal trade-off between Frechet Inception Distance (FID) and inference latency compared to both optimized mobile U-Nets and vanilla DiT variants. By enabling responsive, private, and offline generative AI directly on-device, EdgeDiT provides a scalable blueprint for transitioning large-scale foundation models from high-end GPUs to the palm of the user.

---

## 论文详细总结（自动生成）

这是一份关于论文《EdgeDiT: Hardware-Aware Diffusion Transformers for Efficient On-Device Image Generation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散 Transformer (DiT) 虽然在图像生成质量上达到了 SOTA，但其巨大的计算量和内存需求使其难以在资源受限的移动端设备（如手机）上本地运行。
*   **研究动机**：
    *   **云端局限性**：依赖云端推理存在隐私泄露风险、网络延迟和高能耗问题。
    *   **硬件不匹配**：传统的模型压缩指标（如 FLOPs）并不能完全反映在移动端 NPU（如高通 Hexagon 或 Apple ANE）上的真实延迟，因为 NPU 对特定算子（如 GEMM）有优化偏好。
    *   **目标**：开发一种硬件感知的优化框架，在保持生成质量的同时，实现 DiT 在移动端的高效部署。

### 2. 方法论
EdgeDiT 采用了一种结合硬件感知搜索与知识蒸馏的系统化方法：
*   **硬件感知架构手术（Search Space）**：
    *   **块删除（Block Removal）**：将每两个连续的 DiT 层替换为一个层，以减少深度。
    *   **MLP 比例修改**：将 FFN 块的扩展比例从 4 降低到 2。
    *   **隐藏维度缩减**：将内部投影维度从 1152 降低到 512，构建低秩表示。
*   **特征级知识蒸馏 (FwKD)**：
    *   采用“分而治之”策略，让轻量级的“代理块（Surrogates）”学习模仿原始 DiT-XL/2 教师模型对应块的特征输出。这种局部蒸馏高度可并行，显著降低了训练成本。
*   **多目标贝叶斯优化 (MOBO)**：
    *   在硬件对齐的搜索空间中，利用 MOBO 寻找生成性能（FID 分数）与硬件效率（端侧延迟）之间的帕累托最优解。
*   **端到端微调**：
    *   确定最优架构后，进行最后的端到端训练，使模型参数完全适应扩散任务。

### 3. 实验设计
*   **数据集**：ImageNet 256×256（类别条件图像生成）。
*   **基准模型 (Benchmarks)**：DiT-XL/2（原始基准）、DiT-S/2、B/2、L/2 等变体。
*   **对比设备**：
    *   **Apple A18 Pro NPU** (iPhone 16 Pro Max)。
    *   **Qualcomm 8850 NPU** (Samsung Galaxy S25 Ultra)。
*   **评估指标**：FID（图像质量）、参数量、GMACs、GFLOPs 以及真实的端侧推理延迟（ms）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 GPU** 进行训练。
*   **训练时长**：
    *   教师模型使用 DiT-XL/2 的 400K 迭代检查点。
    *   选定的 EdgeDiT 候选模型在 ImageNet 上进行了额外的 **100K 迭代**端到端训练。
*   **效率提升**：通过特征级蒸馏，避免了对搜索空间中数以亿计的候选架构进行全量训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了敏感性分析，验证了块删除（2合1比3合1更好）和 MLP 比例选择的合理性。
    *   对比了 EdgeDiT 系列（EdgeDiT 1 到 6）在不同分辨率（256x256 和 512x512）下的表现。
    *   进行了消融实验，证明了特征级知识蒸馏（FwKD）对维持图像质量的关键作用。
*   **充分性评价**：实验设计较为客观，涵盖了当前市场上最主流的两大移动端芯片平台。通过帕累托前沿分析展示了模型在性能与速度之间的平衡，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：EdgeDiT 在参数量减少 **20-30%**、FLOPs 降低 **36-46%** 的情况下，端侧延迟缩减了 **1.65 倍**（三星 S25U）和 **1.45 倍**（iPhone 16PM）。
*   **质量不减**：在 ImageNet 256x256 任务上，EdgeDiT 的 FID 分数（约 12.3-12.4）甚至优于仅训练了 400K 次的原始 DiT-XL/2 基准（16.23），证明了蒸馏和架构优化的有效性。
*   **硬件敏感性**：实验证明减少算术计算量（FLOPs）并不总是等比例地转化为延迟降低，强调了硬件感知搜索的必要性。

### 7. 优点
*   **硬件对齐**：不只是理论上的压缩，而是针对特定 NPU 架构进行优化，具有极强的工程落地价值。
*   **高效搜索**：引入特征级蒸馏作为代理评估，极大地解决了 Transformer 架构搜索中算力消耗巨大的痛点。
*   **帕累托优化**：提供了多个版本的模型（EdgeDiT 1-6），允许开发者根据设备性能灵活选择。

### 8. 不足与局限
*   **教师模型单一**：主要基于 DiT-XL/2 进行优化，尚未在更新的架构（如 SiT 或 MDT）上广泛验证其通用性。
*   **训练迭代次数较少**：由于算力限制，端到端训练仅进行了 100K 次，若增加训练时长，性能可能还有进一步提升空间。
*   **任务覆盖面**：目前主要集中在类别条件生成（Class-conditional），对于更复杂的文生图（Text-to-Image）任务尚未提供详细的实验数据。
*   **未来工作**：论文提到将搜索空间扩展到 AR/VR/XR 等更多边缘设备是未来的方向。

（完）
