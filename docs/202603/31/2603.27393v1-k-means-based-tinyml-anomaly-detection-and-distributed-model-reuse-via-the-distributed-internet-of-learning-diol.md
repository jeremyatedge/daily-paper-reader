---
title: K-Means Based TinyML Anomaly Detection and Distributed Model Reuse via the Distributed Internet of Learning (DIoL)
title_zh: 基于 K-Means 的 TinyML 异常检测及通过分布式学习物联网 (DIoL) 实现的分布式模型重用
authors: "Abdulrahman Albaiz, Fathi Amsaad"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27393v1"
tags: ["query:aiot"]
score: 10.0
evidence: MCU上的TinyML异常检测与分布式模型重用
tldr: 本研究针对资源受限的微控制器（MCU），提出了一种基于K-Means的轻量级异常检测模型，并引入分布式学习网络（DIoL）实现模型复用。通过“一次训练，到处共享”（TOSE）策略，模型能以文本形式在设备间传输。在冰箱功耗监测实验中，该系统成功实现了跨设备推理，在保持检测一致性的同时，显著降低了计算开销，为大规模TinyML部署提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决资源受限的微控制器在异常检测中重复训练成本高以及模型难以在异构设备间高效共享的挑战。
method: 开发了基于K-Means的轻量级模型，并利用分布式学习网络（DIoL）将训练好的模型导出为可移植的文本表示以供其他设备直接复用。
result: 原型测试显示，该方法在保持推理性能和运行时间不变的前提下，实现了极低的解析开销和跨设备的一致性检测。
conclusion: 该框架证明了“一次训练，到处共享”模式在嵌入式设备中的可行性，为低成本、可扩展的TinyML应用奠定了基础。
---

## 摘要
本文提出了一种轻量级 K-Means 异常检测模型和分布式模型共享工作流，专为资源受限的微控制器 (MCU) 设计。该系统利用小型冰箱电器的真实功率测量数据，在设备端进行特征提取、聚类和阈值估计，以识别电器的异常行为。为了避免在每个设备上重新训练模型，我们引入了分布式学习物联网 (DIoL)，它允许将在一个 MCU 上训练的模型导出为可移植的文本表示，并直接在其他设备上重用。一个双设备原型通过真实电器案例研究证明了“一次训练，到处共享” (TOSE) 方法的可行性，其中设备 A 训练模型，设备 B 在不进行重新训练的情况下执行推理。实验结果表明，独立运行与基于 DIoL 的运行在异常检测行为上保持一致，解析开销可以忽略不计，且推理运行时间完全相同。所提出的框架为在嵌入式设备群中实现可扩展、低成本的 TinyML 部署提供了可能。

## Abstract
This paper presents a lightweight K-Means anomaly detection model and a distributed model-sharing workflow designed for resource-constrained microcontrollers (MCUs). Using real power measurements from a mini-fridge appliance, the system performs on-device feature extraction, clustering, and threshold estimation to identify abnormal appliance behavior. To avoid retraining models on every device, we introduce the Distributed Internet of Learning (DIoL), which enables a model trained on one MCU to be exported as a portable, text-based representation and reused directly on other devices. A two-device prototype demonstrates the feasibility of the "Train Once, Share Everywhere" (TOSE) approach using a real-world appliance case study, where Device A trains the model and Device B performs inference without retraining. Experimental results show consistent anomaly detection behavior, negligible parsing overhead, and identical inference runtimes between standalone and DIoL-based operation. The proposed framework enables scalable, low-cost TinyML deployment across fleets of embedded devices.

---

## 论文详细总结（自动生成）

这是一份关于论文《K-Means Based TinyML Anomaly Detection and Distributed Model Reuse via the Distributed Internet of Learning (DIoL)》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限的微控制器（MCU）上实现高效的异常检测，并解决设备间模型无法共享、导致每个设备都必须重复进行高能耗训练的问题。
*   **研究动机**：
    *   **计算成本高**：在 MCU 上从零训练模型非常耗电且耗时。
    *   **缺乏标准**：目前 TinyML 缺乏在设备间存储和重用模型的标准化机制。
    *   **现有方案不适用**：传统的联邦学习（Federated Learning）依赖服务器和频繁的网络通信，不适合离线或低功耗的独立嵌入式环境。

### 2. 核心方法论
*   **核心思想**：提出“一次训练，到处共享”（TOSE）原则，通过**分布式学习物联网（DIoL）**框架，将在一个设备上训练好的轻量级模型导出为可移植的文本文件，供其他同类设备直接加载使用。
*   **关键技术细节**：
    *   **K-Means 聚类**：选择 $k=3$（对应压缩机关闭、稳定运行、高负载三种状态）。使用欧几里得距离计算异常得分。
    *   **特征工程**：提取 5 个功率特征：均方根（RMS）、滚动平均值、标准差、RMS 斜率、压缩机开启持续时间。
    *   **DIoL 工作流**：
        1.  **设备 A（训练端）**：执行特征提取、K-Means 训练、阈值估计（基于 95 百分位），生成 `MODEL.TXT`。
        2.  **可移植格式**：`MODEL.TXT` 包含聚类中心、特征缩放参数（均值/标准差）和异常阈值。
        3.  **设备 B（推理端）**：通过 microSD 卡读取该文件，跳过训练阶段，直接进行推理。

### 3. 实验设计
*   **实验场景**：小型冰箱（Mini-fridge）的功耗监测，识别如压缩机运行时间过长、循环异常或意外停机等行为。
*   **硬件平台**：STM32F446RE 微控制器、ACS712 电流传感器、microSD 卡模块。
*   **数据集**：采集了 14 天的真实功率数据，共 43,285 条记录。
*   **Benchmark（基准对比）**：
    *   对比了作者之前开发的基于 **Z-Score** 的检测器。
    *   对比了“独立训练推理”与“通过 DIoL 共享模型推理”的性能差异。

### 4. 资源与算力
*   **硬件**：STM32F446RE（基于 ARM Cortex-M4 核心）。
*   **训练算力**：在 MCU 上训练 8,600 个样本（约 20% 的数据）耗时 **59,328 毫秒**（约 59 秒）。
*   **推理算力**：处理 43,285 条记录耗时 **58,518 毫秒**（约 58.5 秒），平均每秒处理约 734 条记录。
*   **内存占用**：运行在 sub-100 KB 的内存限制内，使用固定大小数组，无动态内存分配。

### 5. 实验数量与充分性
*   **实验规模**：使用了 14 天的连续监测数据，包含 4 万余条特征记录，对于 MCU 级别的研究较为充分。
*   **实验设计**：通过双设备原型（设备 A 训练，设备 B 推理）验证了 DIoL 流程。
*   **客观性**：实验展示了 DIoL 推理与原生推理在运行时间（58,514ms vs 58,518ms）和检测结果上几乎完全一致，证明了模型迁移的无损性。但实验目前仅局限于单一类型的电器（冰箱）。

### 6. 主要结论与发现
*   **可行性验证**：证明了在 MCU 上进行全流程（训练+推理+序列化）K-Means 异常检测是可行的。
*   **DIoL 优势**：通过共享 `MODEL.TXT`，第二台设备可以完全跳过 59 秒的训练过程，且推理表现与原设备一致。
*   **性能表现**：K-Means 相比 Z-Score 提供了更平滑的决策边界，且两者在 MCU 上的运行开销相当。

### 7. 优点与亮点
*   **轻量级共享机制**：提出了一种极其简单的文本化模型表示方式，非常适合资源受限的嵌入式系统。
*   **离线分布式学习**：DIoL 框架不依赖云端或持续的网络连接，通过物理介质（如 SD 卡）即可实现知识迁移。
*   **端到端实现**：不仅有算法模拟，还完成了从传感器采样到模型导出的完整硬件闭环。

### 8. 不足与局限
*   **设备多样性不足**：实验仅在同型号的 STM32 和同一种电器上完成，未测试在异构硬件或不同品牌冰箱上的泛化能力。
*   **安全性缺失**：目前的 `MODEL.TXT` 是明文存储，缺乏模型完整性校验和加密机制，容易受到篡改攻击。
*   **共享介质局限**：目前依赖 microSD 卡手动转移模型，在大规模设备群中的自动化部署能力有待提升。

（完）
