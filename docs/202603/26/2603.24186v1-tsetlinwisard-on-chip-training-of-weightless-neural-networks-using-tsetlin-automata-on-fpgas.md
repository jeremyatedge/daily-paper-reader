---
title: "TsetlinWiSARD: On-Chip Training of Weightless Neural Networks using Tsetlin Automata on FPGAs"
title_zh: TsetlinWiSARD：在 FPGA 上利用 Tsetlin 自动机实现无权神经网络的片上训练
authors: "Shengyu Duan, Marcos L. L. Sartori, Rishad Shafik, Alex Yakovlev"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24186v1"
tags: ["query:aiot"]
score: 9.0
evidence: FPGA上的片上训练与低复杂度推理
tldr: 针对边缘计算对高效片上训练的需求，本文提出TsetlinWiSARD，将Tsetlin自动机（TA）引入无权重神经网络（WNN）。该方法通过概率性反馈驱动的迭代优化，克服了传统WiSARD一键式训练易过拟合的缺陷。在FPGA上实现的硬件架构不仅达到了最先进的精度，且训练速度比传统方法快1000倍以上，同时在资源、延迟和功耗方面显著优于现有的机器学习加速器，为低功耗边缘学习提供了新方案。
selection_source: fresh_fetch
motivation: 传统无权重神经网络的一键式存储学习机制容易导致过拟合且精度受限，限制了其在高效片上训练中的应用。
method: 结合Tsetlin自动机与WiSARD架构，利用概率性反馈实现迭代优化，并设计了专用的FPGA片上训练硬件架构。
result: "实验显示训练速度提升超1000倍，且相比其他FPGA加速器，资源减少22%、延迟降低93.3%、功耗降低64.2%。"
conclusion: TsetlinWiSARD成功平衡了硬件效率与学习精度，是实现低功耗、高性能边缘设备片上训练的有效途径。
---

## 摘要
边缘端对适应性、隐私和安全性的需求日益增长，不断推动着具备片上训练和推理能力的新一代机器学习（ML）算法的前沿发展。无权神经网络（WNN）就是这样一种基于查找表的简单神经元结构的算法。因此，与严重依赖乘累加操作的深度神经网络相比，它具有低延迟、低复杂性推理等架构优势。然而，传统的 WNN 依赖于基于记忆的一次性训练，这要么导致过拟合和精度下降，要么需要繁琐的训练后调整，限制了其在高效片上训练中的有效性。在这项工作中，我们提出了 TsetlinWiSARD，这是一种利用 Tsetlin 自动机（TA）实现概率性、反馈驱动学习的 WNN 训练方法。它通过迭代优化克服了 WiSARD 一次性训练的过拟合问题，同时保持了简单、连续的二进制反馈，以实现高效的片上训练。我们方法的核心是一个基于现场可编程门阵列（FPGA）的训练架构，该架构在显著提高硬件效率的同时，提供了最先进的精度。与传统的 WiSARD 实现相比，我们的方法使训练速度提高了 1000 倍以上。此外，与实现其他机器学习算法的基于 FPGA 的训练加速器相比，我们证明了资源使用量减少了 22%，延迟降低了 93.3%，功耗降低了 64.2%。

## Abstract
Increasing demands for adaptability, privacy, and security at the edge have persistently pushed the frontiers for a new generation of machine learning (ML) algorithms with training and inference capabilities on-chip. Weightless Neural Network (WNN) is such an algorithm that is principled on lookup table based simple neuron structures. As a result, it offers architectural benefits, such as low-latency, low-complexity inference, compared to deep neural networks that depend heavily on multiply-accumulate operations. However, traditional WNNs rely on memorization-based one-shot training, which either leads to overfitting and reduced accuracy or requires tedious post-training adjustments, limiting their effectiveness for efficient on chip training.   In this work, we propose TsetlinWiSARD, a training approach for WNNs that leverages Tsetlin Automata (TAs) to enable probabilistic, feedback-driven learning. It overcomes the overfitting of WiSARD's one-shot training with iterative optimization, while maintaining simple, continuous binary feedback for efficient on-chip training. Central to our approach is a field programmable gate array (FPGA)-based training architecture that delivers state-of-the-art accuracy while significantly improving hardware efficiency. Our approach provides over 1000x faster training when compared with the traditional WiSARD implementation of WNNs. Further, we demonstrate 22% reduced resource usage, 93.3% lower latency, and 64.2% lower power consumption compared to FPGA-based training accelerators implementing other ML algorithms.

---

## 论文详细总结（自动生成）

### 论文总结：TsetlinWiSARD —— 基于 FPGA 的 Tsetlin 自动机无权神经网络片上训练

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：边缘计算对实时模型自适应、隐私保护和安全性要求日益增高，迫切需要具备片上训练（On-chip Training）能力的机器学习算法。
*   **核心问题**：传统的深度神经网络（DNN）依赖大量浮点运算和梯度计算，在资源受限的边缘硬件（如 FPGA）上训练能耗极高。无权神经网络（WNN，如 WiSARD）虽具有低延迟、无乘法器的优势，但其传统的“一次性记忆”训练机制容易导致过拟合，且缺乏有效的迭代优化手段。
*   **论文目标**：提出 TsetlinWiSARD，通过引入 Tsetlin 自动机（TA）实现概率性、反馈驱动的迭代学习，旨在解决 WNN 的过拟合问题，并实现极高性能的 FPGA 片上训练。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：将 Tsetlin 自动机（TA）作为 WiSARD 查找表（LUT）中每个条目的学习单元。TA 通过内部状态的转移（增加/减少）来决定该条目输出 0 还是 1，从而将“硬记忆”转变为“概率性学习”。
*   **关键技术细节**：
    *   **TA 组织**：每个 $n$ 输入的 LUT 对应 $2^n$ 个 TA。TA 状态位于中点以上输出 1，以下输出 0。
    *   **反馈机制**：当预测错误时触发反馈。正确类别的判别器中，对应条目的 TA 状态以概率 $P$ 增加（强化）；错误类别的判别器中，对应条目的 TA 状态以概率 $P$ 减少（抑制）。
    *   **硬件架构**：利用 FPGA 的分布式 RAM（LUTRAM）存储 TA 状态，使用行波进位加法器（Ripple-carry adder）实现状态更新。
    *   **随机性实现**：使用线性反馈移位寄存器（LFSR）产生伪随机数，以极低的硬件开销实现概率反馈控制。

#### 3. 实验设计
*   **数据集**：使用了 6 个基准数据集，涵盖图像识别、生物信号和传感器数据：MNIST、Fashion-MNIST (FMNIST)、Kuzushiji-MNIST (KMNIST)、EMG（手势识别）、HAR（人类活动识别）、GPS（手势相位分割）。
*   **对比方法（Benchmark）**：
    *   **算法对比**：标准 WiSARD、B-bleaching（一种改进的 WNN 训练方法）、Tsetlin Machine (TM)。
    *   **硬件对比**：Hashed WNN（现有的 WNN 加速器）、LogicWiSARD、BTHOWeN、DTM（Tsetlin Machine 加速器）、F-CNN（卷积神经网络加速器）。

#### 4. 资源与算力
*   **硬件平台**：Xilinx Zynq XC7Z020 FPGA (PYNQ-Z1)。
*   **算力与耗时**：
    *   **训练延迟**：每个样本仅需 6.4 $\mu$s（包含数据传输和训练/推理）。
    *   **功耗**：动态功耗约为 1.56 W。
    *   **加速比**：比现有的基于处理器的 WiSARD 训练方案快 1000 倍以上。
    *   **资源占用**：TsetlinWiSARD-150 模型使用了 33,596 个 LUT 和 25,927 个 FF。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的数据集上进行了测试，并针对不同模型规模（150 LUTs 和 300 LUTs）进行了对比。
*   **消融与参数研究**：详细分析了 LUT 数量、每个 LUT 的输入位数、TA 状态数以及反馈概率 $P$ 对准确率的影响。
*   **评价**：实验设计较为充分，涵盖了从算法准确率到硬件效率（面积、延迟、功耗）的全方位对比，客观展示了该方法在边缘端训练的优势。

#### 6. 主要结论与发现
*   **准确率提升**：TsetlinWiSARD 在所有测试数据集上均显著优于传统的 WiSARD 和 B-bleaching 方法。
*   **硬件效率极高**：相比于 TM 加速器和 CNN 加速器，TsetlinWiSARD 在资源减少 22% 的情况下，训练延迟降低了 93.3% 以上，功耗降低了 64.2% 以上。
*   **TA 效率**：相比于 Tsetlin Machine，TsetlinWiSARD 在达到同等或更高准确率时，所需的 TA 数量减少了约三分之一。

#### 7. 优点（亮点）
*   **算法创新**：首次将 TA 引入 WiSARD 框架，解决了 WNN 长期存在的迭代训练难题。
*   **硬件友好**：完全摒弃了乘法器和梯度计算，利用 FPGA 原生的 LUT 结构实现存储与计算的深度融合。
*   **极致性能**：实现了真正的“处理器无关”（Processor-free）片上训练，速度极快，非常适合实时边缘应用。

#### 8. 不足与局限
*   **绝对精度差距**：虽然优于传统 WNN，但与最先进的深度学习模型（如大型 CNN）在复杂任务上的准确率仍有一定差距。
*   **硬件资源限制**：在小型 FPGA（如 XC7Z020）上，模型规模受限于 LUTRAM 的数量，难以部署超大规模模型。
*   **随机性偏差**：片上使用的 LFSR 产生的伪随机性在极短时间内可能存在分布不均，导致训练结果与离线软件训练（使用真随机数）相比有轻微的精度损失。

（完）
