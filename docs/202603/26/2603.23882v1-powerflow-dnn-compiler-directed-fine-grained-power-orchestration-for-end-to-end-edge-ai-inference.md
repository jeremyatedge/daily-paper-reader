---
title: "PowerFlow-DNN: Compiler-Directed Fine-Grained Power Orchestration for End-to-End Edge AI Inference"
title_zh: PowerFlow-DNN：面向端到端边缘 AI 推理的编译器导向细粒度功耗编排
authors: "Paul Chen, Jeongeun Kim, Wenbo Zhu, Yuanhan Li, Shunyao Huang, Chenjie Weng, Christopher Torng"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23882v1"
tags: ["query:aiot"]
score: 10.0
evidence: 编译器引导的边缘AI推理功耗编排
tldr: 针对边缘AI推理中的能效挑战，本文提出PowerFlow-DNN框架。该框架采用编译器导向的方法，通过对DVFS和电源门控进行细粒度编排，解决了层间耦合导致的调度空间爆炸问题。在满足实时性约束的前提下，它能自动搜索端到端的最小能量功率状态序列。在TSMC 40nm工艺下的评估显示，该方法在处理海量调度空间时，能显著降低能耗并提升求解效率。
selection_source: fresh_fetch
motivation: 传统的层级功率管理忽略了层间耦合和转换开销，且在受限硬件资源下组合调度空间巨大，难以实现全局最优能效。
method: 提出一种编译器导向的框架，将截止日期约束下的周期性推理建模为统一的层间功率调度问题，并利用结构化层状态图进行高效优化。
result: "在代表性边缘网络上实现了高达37%的节能效果，结果接近ILP最优解（差距仅0.68%），且通过轻量级剪枝实现了2.14倍的求解加速。"
conclusion: PowerFlow-DNN证明了通过编译器层面的细粒度功率编排，可以在复杂的硬件约束下有效平衡边缘AI推理的性能与功耗。
---

## 摘要
边缘 AI 系统通常在严格的能量和体积限制下运行，要求在有限的电池容量下实现极高的效率，且随着智能能力需求的提升，这些要求变得愈发苛刻。先前文献表明，包括动态电压频率调整（DVFS）和电源门控在内的细粒度功耗编排能够带来显著的能效收益，不容忽视，但仍存在尚未探索的挑战。我们观察到，由于功耗控制决策的层间耦合，层级方法会产生意外的开销，并且在有限电压轨和转换开销等实际约束下联合管理这些机制会导致组合调度空间迅速增长。为此，我们提出了 PowerFlow-DNN，这是一个用于超低功耗加速器中端到端功耗状态编排的编译器导向框架。通过将截止时间约束下的实时周期性推理构建为统一的层间功耗调度问题，我们的框架能够自动发现符合截止时间要求的最小能量功耗状态调度方案，同时兼顾端到端的层间影响。我们在采用 TSMC 40nm 工艺的 DNN 加速器 VLSI 实现上评估了该框架。在代表性的边缘网络中，我们展示了 PowerFlow-DNN 在离散化公式下发现了近乎最优的解，实现的能量消耗与精确的整数线性规划（ILP）基准相比误差在 0.68% 以内；与没有功耗编排的激进基准相比，能量消耗降低了高达 37%。尽管在超过 10^160 种可能的功耗状态分配的组合调度空间中进行推理，但该框架运行在结构化的分层状态图上，从而实现了高效优化，并通过轻量级剪枝实现了高达 2.14 倍的求解器加速。

## Abstract
Edge AI systems often operate under stringent energy and volume constraints that demand extreme efficiency under limited battery capacity, with requirements worsening as intelligent capability demands advance. Prior literature suggests that fine-grained power orchestration, including DVFS and power gating, enables significant energy efficiency benefits that cannot be left unexploited, while still exhibiting unexplored challenges. We observe that layer-level approaches incur unintended overheads due to inter-layer coupling of power control decisions, and that jointly managing these mechanisms under practical constraints such as limited voltage rails and transition overheads leads to a rapidly growing combinatorial schedule space. To address this, we propose PowerFlow-DNN, a compiler-directed framework for end-to-end power-state orchestration in ultra-low-power accelerators. By constructing a rigorous problem formulation for deadline-constrained, real-time, periodic inference as a unified inter-layer power-scheduling problem, our framework enables automated discovery of energy-minimal power-state schedules that adhere to a deadline while accounting for end-to-end, inter-layer impacts. We evaluate the framework on a DNN accelerator VLSI implementation in TSMC 40nm technology. Across representative edge networks, we show that PowerFlow-DNN discovers near-optimal solutions under the discretized formulation and achieves energy within 0.68\% of the exact ILP oracle, reducing energy by up to 37\% compared to an aggressive baseline without power orchestration, while reasoning over a combinatorial schedule space of over $10^{160}$ possible power-state assignments, yet operating on a structured layered state graph that enables efficient optimization, achieving up to 2.14$\times$ solver speedup via lightweight pruning.

---

## 论文详细总结（自动生成）

以下是对论文《PowerFlow-DNN: Compiler-Directed Fine-Grained Power Orchestration for End-to-End Edge AI Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：边缘 AI 系统在极度受限的电池容量和体积下，如何通过细粒度的功耗管理（如 DVFS 和电源门控）实现极致能效。
*   **研究动机**：
    *   **层间耦合挑战**：传统的功耗管理多采用层级（Layer-level）独立决策，忽略了层与层之间由于共享电压轨和转换开销（Transition Overhead）导致的耦合，导致局部最优而非全局最优。
    *   **调度空间爆炸**：在有限的电压轨和复杂的转换约束下，联合优化 DVFS 和电源门控会导致组合调度空间呈指数级增长（文中提到超过 $10^{160}$ 种可能），手动调优或简单启发式算法难以应对。
    *   **硬件约束**：边缘设备往往由于面积限制，只能提供极少数的电压轨（Voltage Rails），这限制了电压调整的灵活性。

### 2. 方法论
*   **核心思想**：提出 PowerFlow-DNN 框架，将端到端 DNN 推理建模为一个**受限的最短路径优化问题**，通过编译器在编译阶段自动发现满足实时性（Deadline）约束的最小能量功耗状态序列。
*   **关键技术细节**：
    *   **统一建模**：将 DVFS（针对计算/存储域）和电源门控（针对高漏电的 RRAM 存储库）统一在功耗状态空间中。
    *   **分层状态图（Layered State Graph）**：将推理过程表示为状态图，节点为各层的功耗状态，边表示层间转换的能量和延迟开销。
    *   **$\lambda$-DP 算法**：采用基于拉格朗日松弛的动态规划算法。通过引入乘子 $\lambda$ 平衡能量与延迟（目标函数变为 $E + \lambda T$），在离散空间中高效搜索最优解。
    *   **编译器导向剪枝**：
        *   **结构剪枝（Structure Pruning）**：移除被帕累托支配（Pareto-dominated）的状态，即在能量、延迟和漏电方面均无优势的状态。
        *   **边际效用跳转（Marginal-Utility Jump）**：利用边际效用递减法则指导 $\lambda$ 的搜索方向。

### 3. 实验设计
*   **技术栈**：基于 TSMC 40nm LP 工艺的 VLSI 实现，包含脉动阵列加速器、RRAM 权重存储和 SRAM 缓冲区。
*   **测试负载（Workloads）**：涵盖了不同类型的边缘网络：SqueezeNet 1.1、MobileNetV3-Small、ResNet18、MobileViT-xxs。
*   **对比基准（Benchmarks）**：
    1.  **Baseline**：无功耗优化的常规加速器。
    2.  **+Gating**：仅开启细粒度存储电源门控。
    3.  **+Greedy**：采用层级贪婪 DVFS 启发式算法（不考虑层间耦合）。
    4.  **+Gating+Greedy**：结合上述两者。
    5.  **ILP Oracle**：整数线性规划精确解（用于小规模实例的验证）。

### 4. 资源与算力
*   **硬件模拟**：使用周期精确的性能模型估算延迟，结合后综合（Post-synthesis）门级功耗分析提取能量数据。
*   **算力说明**：论文未明确提及训练这些模型所用的 GPU 资源，因为其核心贡献在于**推理阶段的编译器优化工具链**。该工具链（求解器）运行在通用 CPU 上，在数秒内即可完成复杂网络的调度搜索。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种主流模型进行了全网络评估，涵盖了卷积、深度可分离卷积、残差结构和注意力机制。
*   **维度覆盖**：
    *   横向对比了不同推理速率（FPS）下的能效。
    *   纵向分析了电压轨数量（1 到 5 条）对节能效果的影响。
    *   进行了转换开销敏感度分析和求解器扩展性实验。
*   **充分性评价**：实验设计较为全面，通过与 ILP 精确解对比证明了算法的近优性（误差 < 0.68%），且考虑了真实的 VLSI 布局布线开销，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **节能显著**：相比无优化基准，PowerFlow-DNN 最高可降低 **37%** 的能耗；相比贪婪启发式算法，仍有 **2-7%** 的额外收益。
*   **电压轨收益递减**：增加电压轨数量可提升能效，但超过 3 条后收益迅速减小。优化电压轨的选择比单纯增加数量更重要。
*   **域分离的重要性**：将计算域和存储域独立控制可显著扩大节能空间（约 11%）。
*   **求解效率**：通过结构剪枝，求解器实现了高达 **2.14 倍** 的加速，能够处理超大规模的搜索空间。

### 7. 优点
*   **垂直整合**：从编译器算法到架构设计，再到 40nm VLSI 落地，实现了全栈式的验证。
*   **严谨的数学建模**：将复杂的硬件约束转化为清晰的优化问题，并给出了高效的近似求解方案。
*   **现实约束考虑**：充分考虑了电压转换的延迟和能量惩罚，这在以往的学术研究中常被简化。

### 8. 不足与局限
*   **适用场景受限**：目前主要针对**确定性的周期性推理**（如固定帧率的视频处理），对于突发性或非确定性的推理任务（如事件驱动型）适用性待考证。
*   **静态调度**：所有决策在编译时完成，无法应对运行时的动态波动（如环境温度变化导致的延迟波动）。
*   **工艺节点**：实验基于 40nm 工艺，虽然原理通用，但在更先进工艺（如 7nm 及以下，漏电占比更高）下的表现需进一步验证。

（完）
