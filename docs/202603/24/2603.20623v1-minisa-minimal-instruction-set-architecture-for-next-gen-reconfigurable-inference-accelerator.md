---
title: "MINISA: Minimal Instruction Set Architecture for Next-gen Reconfigurable Inference Accelerator"
title_zh: MINISA：面向下一代可重构推理加速器的极简指令集架构
authors: "Jianming Tong, Devansh Jain, Yujie Li, Charith Mendis, Tushar Krishna"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20623v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于可重构AI推理加速器的极简指令集架构
tldr: "针对现代可重构AI加速器因细粒度控制导致的巨大片外指令获取开销，本文提出MINISA指令集。它基于改进的FEATHER+架构，以虚拟神经元为控制粒度，仅需四条核心指令即可实现灵活的数据流映射与布局设置。实验表明，MINISA显著降低了指令流量，消除了高达96.9%的取指停顿周期，在多种AI及非AI负载中实现了显著的性能提升。"
selection_source: fresh_fetch
motivation: 细粒度微控制带来的片外配置信息获取开销已成为制约可重构加速器性能的主要瓶颈。
method: 提出以虚拟神经元为粒度的最小指令集，通过三条布局指令和一条映射指令简化硬件控制并支持动态数据流。
result: 在50个GEMM负载中，指令流量降低了35倍至40万倍，端到端性能提升最高达31.6倍。
conclusion: MINISA在保持硬件灵活性的同时极大地降低了控制成本，是下一代可重构推理加速器的有效架构方案。
---

## 摘要
现代可重构 AI 加速器依赖于丰富的映射和数据布局灵活性，以在矩阵乘法、卷积以及 AI 之外的新兴应用中保持高利用率。然而，通过细粒度的微控制来暴露这种灵活性，会导致从片外存储器获取配置位的控制开销过高。本文提出了 MINISA，这是一种极简指令集，它以虚拟神经元（VN）为粒度对可重构加速器进行编程。VN 是保留硬件灵活性最粗的控制粒度，也是避免不必要控制成本最细的粒度。首先，我们介绍了 FEATHER+，它是对 FEATHER 的适度改进，消除了运行时数据流/布局协同切换所需的冗余片上复制，并支持在执行前无法获取输入和权重数据以进行离线布局处理的动态情况。随后，MINISA 将 FEATHER+ 的控制抽象为三条针对输入、权重和输出 VN 的布局设置指令，以及一条用于设置数据流的映射指令。这在保留 FEATHER+ 支持的合法映射和布局空间的同时，减少了控制和指令占用空间。我们的结果表明，在涵盖 AI (GPT-oss)、全同态加密 (FHE) 和零知识证明 (ZKP) 的 50 个不同规模的 GEMM 工作负载下，MINISA 将片外指令流量的几何平均值降低了 35 倍至 4x10^5 倍。这消除了消耗 96.9% 微指令周期的取指停顿，使 16x256 FEATHER+ 的端到端加速比高达 31.6 倍。我们的代码：https://github.com/maeri-project/FEATHER/tree/main/minisa。

## Abstract
Modern reconfigurable AI accelerators rely on rich mapping and data-layout flexibility to sustain high utilization across matrix multiplication, convolution, and emerging applications beyond AI. However, exposing this flexibility through fine-grained micro-control results in prohibitive control overhead of fetching configuration bits from off-chip memory. This paper presents MINISA, a minimal instruction set that programs a reconfigurable accelerator at the granularity of Virtual Neurons (VNs), the coarsest control granularity that retains flexibility of hardware and the finest granularity that avoids unnecessary control costs. First, we introduce FEATHER+, a modest refinement of FEATHER, that eliminates redundant on-chip replication needed for runtime dataflow/layout co-switching and supports dynamic cases where input and weight data are unavailable before execution for offline layout manipulation. MINISA then abstracts control of FEATHER+ into three layout-setting instructions for input, weight, and output VNs and a single mapping instruction for setting dataflow. This reduces the control and instruction footprint while preserving the legal mapping and layout space supported by the FEATHER+. Our results show that MINISA reduces geometric mean off-chip instruction traffic by factors ranging from 35x to (4x10^5)x under various sizes under 50 GEMM workloads spanning AI (GPT-oss), FHE, and ZKP. This eliminates instruction-fetch stalls that consume 96.9% of micro-instruction cycles, yielding up to 31.6x end-to-end speedup for 16x256 FEATHER+. Our code: https://github.com/maeri-project/FEATHER/tree/main/minisa.

---

## 论文详细总结（自动生成）

以下是对论文《MINISA: Minimal Instruction Set Architecture for Next-gen Reconfigurable Inference Accelerator》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代可重构 AI 加速器（如 FEATHER）虽然能通过灵活的数据流和布局切换来适配多样化的算子（如 CNN、Transformer、全同态加密 FHE、零知识证明 ZKP），但其**控制开销（Control Overhead）**巨大。
*   **研究动机**：现有的可重构架构通常采用细粒度的微代码（Micro-code）控制，这导致指令流极其庞大。在扩展到大规模阵列时，从片外获取指令的带宽压力会导致严重的取指停顿（Stall），甚至消耗高达 96.9% 的执行周期。论文旨在寻找一种既能保留硬件灵活性，又能极大地压缩指令体积的抽象层。

### 2. 方法论
论文提出了 **FEATHER+ 硬件架构** 和 **MINISA 指令集**：
*   **FEATHER+ 架构改进**：
    *   引入**全对全（All-to-all）分布网络**，取代点对点连接，消除了片上缓冲区的冗余数据复制。
    *   支持动态权重和输入切换，不再要求权重必须离线预排序。
    *   增加了输出缓冲区到静态缓冲区的反馈链路，支持连续层间的无缝衔接。
*   **核心思想：虚拟神经元（Virtual Neuron, VN）抽象**：
    *   将硬件原子操作（$A_H$ 元素的点积）定义为软件控制的最小单位（VN）。
    *   **MINISA 指令集**：仅包含 8 条核心指令。
        *   **布局指令（Set*VNLayout）**：通过 3 位编码定义张量在缓冲区中的 4 级划分和排序。
        *   **映射指令（ExecuteMapping/Streaming）**：使用 6 个参数（起始索引、步长、组大小等）描述 VN 在 PE 阵列上的空间分布和时间流向。
*   **编译流程**：开发了 FEATHER+ Mapper，采用“映射优先、布局随后”的搜索策略，在保证无银行冲突（Bank Conflict）的前提下，自动生成最优的 MINISA 指令序列。

### 3. 实验设计
*   **数据集与场景**：涵盖了 50 个通用矩阵乘法（GEMM）负载，分为三大领域：
    1.  **AI 推理**：GPT-oss (20B 参数规模)。
    2.  **全同态加密 (FHE)**：包括 Bootstrapping 过程中的算子。
    3.  **零知识证明 (ZKP)**：数论变换 (NTT) 相关算子。
*   **Benchmark 与对比方法**：
    *   **Micro-instruction (Baseline)**：FEATHER 原有的细粒度微指令控制模式。
    *   **工业级硬件**：NVIDIA RTX 5090 GPU 和 Google TPU v6e-8。
    *   **阵列规模扩展**：从 4x4 扫频至 16x256 的不同 PE 阵列尺寸。

### 4. 资源与算力
*   **硬件实现**：使用 TSMC 28nm 工艺进行逻辑综合（PnR），评估面积和功耗。
*   **仿真环境**：采用周期精确的 RTL 模型验证正确性，并结合分析模型进行大规模扩展评估。
*   **算力对比**：在相同功耗预算（约 575W）下，对比了 64 个 FEATHER+ 实例与 8 个 TPU v6e 核心。
*   **编译开销**：在 MacBook Pro (M5 Pro) 上，针对 50 个负载进行映射搜索仅需约 17 分钟。

### 5. 实验数量与充分性
*   **实验规模**：进行了 50 组不同形状的 GEMM 实验，覆盖了从规则到极端不规则的张量维度。
*   **消融实验**：针对阵列宽度（AW）和高度（AH）进行了独立的可扩展性分析。
*   **充分性评价**：实验设计非常充分，不仅对比了指令压缩率，还深入分析了取指停顿对端到端延迟的影响，并提供了与顶级商用加速器的性能功耗比对比，客观地展示了架构在处理不规则负载时的优势。

### 6. 主要结论与发现
*   **指令压缩**：MINISA 将片外指令流量降低了 **35 倍至 400,000 倍**。
*   **性能提升**：通过消除取指停顿，在 16x256 规模下实现了高达 **31.6 倍** 的端到端加速。
*   **架构优势**：相比 GPU 和 TPU，FEATHER+ 在处理不规则形状（如维度不匹配硬件对齐要求）时，性能分别提升了 **23.7 倍** 和 **7.8 倍**，这归功于其 VN 级的灵活重构能力。
*   **硬件成本**：FEATHER+ 引入的灵活性仅增加了不到 **7%** 的面积和功耗开销。

### 7. 优点
*   **抽象层定义精准**：找准了“虚拟神经元”这一平衡灵活性与控制成本的黄金分割点。
*   **通用性强**：不仅适用于传统 AI，还高效支持了计算密集且形状怪异的密码学负载（FHE/ZKP）。
*   **系统性完整**：从硬件改进、指令集设计到编译器 Mapper 搜索算法，提供了一套完整的解决方案。

### 8. 不足与局限
*   **算子覆盖面**：虽然 GEMM 是核心，但对于非线性算子或极度稀疏算子的支持细节讨论较少。
*   **互连网络扩展性**：全对全（All-to-all）分布网络在阵列规模进一步扩大（如超过 16x256）时，其面积和布线复杂度的增长可能会成为新的瓶颈。
*   **依赖静态调度**：目前的 Mapper 仍属于静态搜索，对于某些极度动态、形状在运行时剧烈变化的场景，可能需要更复杂的动态调度支持。

（完）
