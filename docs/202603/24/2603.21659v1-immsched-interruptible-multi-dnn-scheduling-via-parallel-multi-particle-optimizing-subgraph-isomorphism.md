---
title: "IMMSched: Interruptible Multi-DNN Scheduling via Parallel Multi-Particle Optimizing Subgraph Isomorphism"
title_zh: IMMSched：通过并行多粒子优化子图同构实现的可中断多 DNN 调度
authors: "Boran Zhao, Hetian Liu, Zihang Yuan, Yanbin Hu, Wenzhe Zhao, Tian Xia, Pengju Ren"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21659v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘加速器的可中断多DNN调度优化
tldr: 针对边缘加速器上多DNN任务到达时间不可预测导致的调度难题，本文提出IMMSched。它将多DNN调度建模为NP难的子图同构问题，并创新性地结合多粒子优化与Ullmann算法，通过概率连续松弛方案实现并行化，消除了传统方法的串行依赖。配合硬件架构中的量化调度和全局控制器，IMMSched显著降低了调度延迟和功耗，实现了不可预测任务的实时中断调度。
selection_source: fresh_fetch
motivation: 现有的多DNN调度框架难以处理不可预测的任务到达，且受限于CPU离线调度的高开销和子图同构问题的计算复杂性。
method: 提出一种并行子图同构方法，结合多粒子优化、Ullmann算法及概率连续松弛方案，并辅以硬件层面的量化调度与全局控制。
result: 实验表明IMMSched在调度延迟和能耗上实现了数量级的降低，支持边缘加速器上的实时任务执行。
conclusion: IMMSched通过高效的并行优化算法解决了边缘计算中多DNN实时中断调度的瓶颈。
---

## 摘要
对具有不可预测任务到达时间的多 DNN 工作负载的需求日益增长，凸显了在边缘加速器上进行可中断调度的必要性。然而，现有的抢占式框架通常假设任务到达时间已知，并依赖基于 CPU 的离线调度，这会产生沉重的运行时开销，且难以处理不可预测的任务到达。更糟糕的是，先前的研究表明，多 DNN 调度需要在有限时间内解决大型有向无环图上的 NP 难子图同构问题，这极具挑战性。为了解决这一问题，我们提出了 IMMSched，这是一种并行子图同构方法，它将多粒子优化与基于概率连续松弛方案的 Ullmann 算法相结合，消除了先前工作中的串行数据依赖。最后，硬件架构中的量化调度方案和全局控制器进一步结合了多粒子结果，以进行共识引导的探索。评估结果表明，IMMSched 在调度延迟和功耗方面实现了数量级的降低，从而能够在边缘加速器上实时执行不可预测的 DNN 任务。

## Abstract
The growing demand for multi-DNN workloads with unpredictable task arrival times has highlighted the need for interruptible scheduling on edge accelerators. However, existing preemptive frameworks typically assume known task arrival times and rely on CPU-based offline scheduling, which incurs heavy runtime overhead and struggles to handle unpredictable task arrivals. Even worse, prior studies have shown that multi-DNN scheduling requires solving an NP-hard subgraph isomorphism problem on large directed acyclic graphs within limited time, which is extremely challenging. To tackle this, we propose IMMSched, a parallel subgraph isomorphism method that combines Multi-Particle Optimization with the Ullmann algorithm based on a probabilistic continuous-relaxation scheme, eliminating the serial data dependencies of previous works. Finally, a quantized scheduling scheme and a global controller in the hardware architecture further combine multi-particle results for consensus-guided exploration. Evaluations demonstrate that IMMSched achieves orders-of-magnitude reductions in scheduling latency and energy consumption, enabling real-time execution of unpredictable DNN tasks on edge accelerators.

---

## 论文详细总结（自动生成）

### IMMSched：通过并行多粒子优化子图同构实现的可中断多 DNN 调度论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：在自动驾驶、AR/VR等边缘计算场景中，多DNN任务的到达时间往往是不可预测的（如突发的避障指令或用户交互）。
*   **核心问题**：现有的抢占式调度框架（如PREMA, Planaria等）大多假设任务到达时间已知，并依赖CPU进行离线串行调度。这种方式在面对不可预测任务时，调度延迟极高（有时甚至超过任务执行时间），且多DNN调度本质上是复杂的NP难子图同构问题，难以在边缘侧实时解决。
*   **整体含义**：本文旨在通过利用DNN加速器自身的计算能力，实现一种低延迟、高能效的实时“可中断”调度方案，以应对边缘侧突发的紧急任务。

#### 2. 方法论
*   **核心思想**：将调度问题建模为子图同构搜索，并利用加速器原有的矩阵/向量计算单元并行化求解。
*   **关键技术细节**：
    *   **并行子图匹配算法**：结合了**粒子群优化（PSO）**与**Ullmann算法**。PSO负责全局并行探索，Ullmann负责可行性验证。
    *   **概率连续松弛方案**：针对Ullmann算法的离散特性导致PSO搜索不稳定的问题，引入连续松弛矩阵 $S$，将离散匹配转化为连续优化过程，显著提高了收敛稳定性。
    *   **量化调度方案**：将调度算法中的变量统一量化为8位定点数，使其能直接在DNN加速器的定点MAC单元上运行，无需浮点运算。
    *   **硬件适配**：在现有加速器架构中加入轻量级的**全局控制器**，用于融合多粒子搜索结果并引导探索方向；同时对PE（处理单元）进行微调，支持向量求和与索引输出。
    *   **抢占策略**：采用“单核抢占率”策略，优先抢占执行时间余量（Slack）最大的低优先级任务，以确保高优先级任务实时执行且不违反原任务的截止日期（DDL）。

#### 3. 实验设计
*   **测试场景与数据集**：
    *   **Simple（简单）**：MobileNetV2, ResNet50, UNet（常用于AR/VR）。
    *   **Middle（中等）**：EfficientNet, NASNet, PNASNet（常用于神经架构搜索）。
    *   **Complex（复杂）**：DeepSeek-7B, Qwen-7B, Llama-3-8B（代表大语言模型）。
*   **硬件平台**：模拟了边缘侧（64 MACs）和云端（128 MACs）两种加速器配置。
*   **对比方法（Benchmarks）**：
    *   **基于层级时间调度（LTS）**：PREMA, CD-MSA, Planaria, MoCA。
    *   **基于分块空间调度（TSS）**：IsoSched（目前最先进的抢占式调度方案）。
*   **评估指标**：加速比（Speedup）、延迟受限吞吐量（LBT）、能效比（Energy Efficiency）。

#### 4. 资源与算力
*   **硬件建模**：使用 Verilog 实现引擎和片上网络（NoC），采用 Synopsys Design Compiler 在 **FreePDK 45nm** 工艺下进行综合。
*   **模拟工具**：使用 CACTI-P 建模 SRAM 功耗和面积，使用 McPAT 1.3 建模 NoC。
*   **算力说明**：论文未提及具体的训练时长（因为该方法是调度算法而非模型训练），但明确了加速器运行频率为 700MHz。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从传统CNN到最新LLM（Llama-3, DeepSeek）的9种主流模型，并在边缘和云端两种硬件规格下进行了交叉验证。
*   **充分性评价**：实验设计较为充分。不仅对比了传统的LTS方法，还与最新的TSS方法（IsoSched）进行了深度对比。通过对不同复杂度工作负载的测试，证明了该方法在处理大规模、复杂拓扑结构任务时的优越性。

#### 6. 主要结论与发现
*   **性能提升**：相比于LTS类方法，IMMSched 实现了平均 **27.9x 至 81.4x** 的加速比；相比于最先进的 TSS 方法 IsoSched，加速比提升了 **1.6x**。
*   **吞吐量与能效**：在延迟受限吞吐量（LBT）上，IMMSched 比 IsoSched 提升了 **3.4x**；在能效比上，提升了 **3.43x**。
*   **实时性**：通过并行化子图匹配，IMMSched 将调度延迟降低了几个数量级，使得在任务到达瞬间完成重新调度成为可能。

#### 7. 优点
*   **创新性强**：首次提出支持在线、可中断的多DNN调度，解决了不可预测任务到达的痛点。
*   **软硬协同优化**：通过连续松弛算法解决了离散搜索难题，并巧妙复用了加速器现有的定点计算资源，硬件开销极小。
*   **适用性广**：实验证明该方法不仅适用于小型CNN，也能有效处理参数量巨大的LLM任务。

#### 8. 不足与局限
*   **工艺节点较旧**：实验基于 45nm 工艺建模，虽然在学术论文中常见，但与当前 7nm/5nm 的先进边缘芯片相比，功耗和面积的绝对值参考意义有限。
*   **全局控制器开销**：虽然提到硬件改动轻量，但未详细给出全局控制器在复杂多核架构下的面积占比和布线复杂度的定量分析。
*   **应用限制**：该方法依赖于 TSS（分块空间调度）架构，对于仅支持 LTS（层级时间调度）的旧式加速器可能难以直接移植。

（完）
