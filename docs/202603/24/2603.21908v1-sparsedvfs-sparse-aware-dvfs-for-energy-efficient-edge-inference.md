---
title: "SparseDVFS: Sparse-Aware DVFS for Energy-Efficient Edge Inference"
title_zh: SparseDVFS：面向高效能边缘推理的稀疏感知 DVFS 框架
authors: "Ziyang Zhang, Zheshun Wu, Jie Liu, Luca Mottola"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21908v1"
tags: ["query:aiot"]
score: 10.0
evidence: 用于高能效边缘推理的稀疏感知动态电压频率调整
tldr: 针对边缘设备上深度学习推理的高能耗问题，本文提出SparseDVFS框架。该框架利用算子稀疏性作为频率调节的核心指标，区分计算密集型和访存密集型算子。通过离线建模确定最优频率三元组、运行时图划分合并算子以摊销切换开销，以及统一协同调度引擎消除硬件冲突并隐藏延迟，在保证性能的同时显著提升了能效。
selection_source: fresh_fetch
motivation: 传统的DVFS在模型级粒度过粗，而算子级粒度因频繁的硬件切换延迟导致严重的性能下降。
method: 该框架通过离线稀疏性建模、运行时算子超块合并以及统一协同调度引擎，实现了感知稀疏性的细粒度频率调节。
result: "实验结果显示，SparseDVFS在边缘设备上实现了平均78.17%的能效提升，并保持了14%的优异成本收益比。"
conclusion: SparseDVFS证明了结合算子稀疏性感知与延迟摊销技术可以有效解决边缘推理中的能效与性能平衡难题。
---

## 摘要
在功耗敏感的边缘设备上部署深度神经网络 (DNN) 面临着严峻挑战。虽然动态电压频率调节 (DVFS) 被广泛用于能效优化，但传统的模型级调节往往过于粗糙，难以捕捉推理内部的变化，而细粒度的算子级调节则因显著的硬件切换延迟而导致严重的性能下降。本文提出了 SparseDVFS，这是一个专为高效能边缘推理设计的细粒度、稀疏感知 DVFS 框架。我们的核心见解是，算子稀疏度是硬件频率调制的关键指标。通过区分计算密集型的稠密算子和访存密集型的稀疏算子，系统可以应用专门的频率三元组来最大化能效。为了克服切换开销和组件干扰，SparseDVFS 包含了三项关键创新：(1) 离线建模器，通过白盒时间线分析建立算子稀疏度与最优频率三元组 (CPU/GPU/EMC) 之间的确定性映射；(2) 运行时图分区器，利用贪心合并启发式算法将算子聚合为超块，通过延迟摊销约束平衡调节粒度与 DVFS 切换延迟；(3) 统一协同调节器，采用频率统一调节引擎 (FUSE) 和前瞻指令队列，以消除独立控制器之间的拮抗效应并隐藏硬件转换延迟。广泛的评估表明，SparseDVFS 与现有最先进的解决方案相比，平均实现了 78.17% 的能效提升，同时保持了 14% 的优异成本收益比。

## Abstract
Deploying deep neural networks (DNNs) on power-sensitive edge devices presents a formidable challenge. While Dynamic Voltage and Frequency Scaling (DVFS) is widely employed for energy optimization, traditional model-level scaling is often too coarse to capture intra-inference variations, whereas fine-grained operator-level scaling suffers from prohibitive performance degradation due to significant hardware switching latency. This paper presents SparseDVFS, a fine-grained, sparse-aware DVFS framework designed for energy-efficient edge inference. Our key insight is that operator sparsity is a primary metric for hardware frequency modulation. By distinguishing between compute-bound dense operators and memory-bound sparse operators, the system can apply specialized frequency triplets to maximize energy efficiency. To overcome switching overheads and component interference, SparseDVFS incorporates three key innovations: (1) an offline modeler that established a deterministic mapping between operator sparsity and optimal frequency triplets (CPU/GPU/EMC) via white-box timeline analysis; (2) a runtime graph partitioner that utilizes a greedy merging heuristic to aggregate operators into super-blocks, balancing scaling granularity and DVFS switching latency through a latency amortization constraint; and (3) a unified co-governor that employs a frequency unified scaling engine (FUSE) and a look-ahead instruction queue to eliminate antagonistic effects between independent controllers and hide hardware transition latencies. Extensive evaluations show that SparseDVFS achieves an average 78.17% energy efficiency gain over state-of-the-art solutions while maintaining a superior 14% cost-gain ratio.

---

## 论文详细总结（自动生成）

### SparseDVFS：面向高效能边缘推理的稀疏感知 DVFS 框架论文总结

#### 1. 核心问题与研究动机
在边缘设备（如 NVIDIA Jetson）上部署深度神经网络（DNN）时，能效是核心挑战。现有的动态电压频率调整（DVFS）策略存在两极分化：
*   **模型级 DVFS**：粒度太粗，无法捕捉模型内部不同层（算子）之间的负载波动。
*   **算子级 DVFS**：粒度极细，但硬件频率切换延迟（通常 5-10ms）往往超过了轻量级算子的执行时间，导致切换开销抵消了节能收益。
*   **研究发现**：算子的**稀疏性（Sparsity）**决定了其是计算密集型还是访存密集型。稀疏算子在低频下运行通常不会显著降低性能，但能大幅节能。

#### 2. 方法论
SparseDVFS 提出了一种**块级（Block-level）**的稀疏感知 DVFS 框架，包含三个核心组件：
*   **离线建模器 (Offline Modeler)**：采用白盒时间线分析和热感知功耗模型。它通过物理公式建立算子稀疏度、算术强度与最优频率三元组（CPU/GPU/内存控制器 EMC）之间的确定性映射，避免了黑盒模型（如强化学习）的高额离线训练成本。
*   **运行时图分区器 (Runtime Graph Partitioner)**：使用贪心合并算法将相邻算子聚合成“超块（Super-blocks）”。核心约束是**延迟摊销约束**：只有当超块的预计执行时间远大于硬件切换延迟的 $N$ 倍时才进行频率调整，从而平衡调节精度与切换开销。
*   **统一协同调节器 (Unified Co-governor)**：
    *   **FUSE 策略**：将 CPU、GPU 和内存频率视为耦合向量统一调节，消除独立控制器之间的拮抗效应（如 GPU 忙时 CPU 自动降频导致的预取延迟）。
    *   **前瞻指令队列 (Look-ahead)**：在当前块执行时，提前发出下一块的频率调整指令，利用计算时间掩盖硬件转换延迟。

#### 3. 实验设计
*   **硬件平台**：NVIDIA Jetson Orin Nano (8GB DRAM)。
*   **测试模型**：涵盖 CNN（ResNet-18/101）和 Transformer（ViT-B16/L16）架构。
*   **数据集**：ImageNet-2012（用于稀疏性分析）、DOTA-v1.0 和 VisDrone 2019（用于消融实验）。
*   **对比基准 (Benchmarks)**：
    1.  **Default DVFS**：Linux 内核默认调节器。
    2.  **nvpmodel (MAX-N)**：全速运行模式。
    3.  **GearDVFS**：基于深度强化学习的模型级 DVFS。
    4.  **Ascend-DVFS**：基于遗传算法的算子级细粒度 DVFS。

#### 4. 资源与算力
*   **推理设备**：NVIDIA Jetson Orin Nano，具有 Ampere 架构 GPU 和 ARM Cortex CPU。
*   **算力消耗**：论文未明确提及离线分析的具体训练/分析时长，但强调了其物理模型极其轻量（仅 32 字节），远小于基于神经网络的预测器（如 nn-Meter 的 856MB），适合边缘端实时运行。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括端到端延迟、功耗分布、能效增益、成本收益比、热稳定性分析以及针对超块聚合因子 $N$ 的敏感度分析。
*   **充分性**：实验覆盖了不同类型的模型（CNN vs Transformer）和不同的应用场景（分类 vs 检测数据集），并进行了详细的消融实验来验证 FUSE 策略和 CPU 锁定的有效性。实验设计客观，对比了从系统默认到最先进的学术方案，具有较强的说服力。

#### 6. 主要结论与发现
*   **能效提升**：SparseDVFS 相比默认 DVFS 实现了平均 **78.17%** 的能效提升。
*   **性能平衡**：在大幅节能的同时，相比最高性能模式（MAX-N）仅增加了约 12.8% 的延迟，且成本收益比（Cost-Gain Ratio）仅为 **14%**，远优于其他方案。
*   **热稳定性**：由于平均功耗更低，SparseDVFS 能显著延迟热节流（Thermal Throttling）的发生，保持更稳定的帧率。
*   **切换开销**：通过超块合并，ResNet-101 的切换延迟从 10.81ms 降至 1.45ms。

#### 7. 优点
*   **稀疏性驱动**：敏锐捕捉到稀疏性对硬件瓶颈（计算 vs 访存）的转换作用。
*   **开销感知**：通过“延迟摊销”解决了细粒度 DVFS 在实际硬件上难以落地的痛点。
*   **白盒模型**：相比黑盒 AI 模型，物理建模更具解释性且资源占用极低。
*   **系统级协同**：考虑了 CPU-GPU-EMC 的联动，解决了异构芯片内部的资源竞争问题。

#### 8. 不足与局限
*   **设备依赖性**：离线建模器仍需要针对特定硬件进行初步剖析（Profiling），跨设备迁移需要重新校准物理参数。
*   **稀疏模式细化**：目前主要将稀疏性视为标量比率，未深入区分结构化稀疏与非结构化稀疏对内存访问模式的不同影响（尽管讨论部分提到了这一点）。
*   **应用范围**：主要针对确定性计算图的 DNN 推理，对于具有高度动态分支或条件执行的复杂模型，图分区的效果可能受限。

（完）
