---
title: Optimizing Feature Extraction for On-device Model Inference with User Behavior Sequences
title_zh: 针对用户行为序列的端侧模型推理特征提取优化
authors: "Chen Gong, Zhenzhe Zheng, Yiliu Chen, Sheng Wang, Fan Wu, Guihai Chen"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21508v1"
tags: ["query:aiot"]
score: 9.0
evidence: 优化设备端模型推理和特征提取
tldr: 针对移动端模型推理中特征提取效率低下的瓶颈，本文提出了AutoFeature引擎。该引擎通过将特征提取流程抽象为有向无环图（DAG），利用图优化技术消除冗余操作，并结合高效缓存机制减少连续推理间的重复计算。在搜索、视频和电商等工业级场景的评估显示，AutoFeature显著降低了端到端推理延迟，在不影响模型精度的前提下提升了用户体验。
selection_source: fresh_fetch
motivation: 识别出从原始应用日志中提取特征已成为移动端模型推理中被忽视的性能瓶颈。
method: 提出AutoFeature引擎，通过图抽象、图优化融合冗余节点以及高效缓存机制来加速特征提取过程。
result: 在五项工业级移动服务中，AutoFeature将端到端模型执行延迟降低了1.33倍至4.53倍。
conclusion: 优化特征提取流程是提升移动端模型推理性能的关键，AutoFeature在不损失精度的情况下实现了显著的加速。
---

## 摘要
机器学习模型被广泛集成到现代移动应用中，用于分析用户行为并提供个性化服务。确保低延迟的端侧模型执行对于维持高质量的用户体验至关重要。虽然先前的研究主要集中在利用给定输入特征加速模型推理，但我们发现了实际端侧模型执行流水线中一个被忽视的瓶颈：从原始应用日志中提取输入特征。在这项工作中，我们通过分析并消除不同模型特征之间以及连续模型推理之间的冗余提取操作，探索了特征提取优化的新方向。随后，我们介绍了 AutoFeature，这是一个旨在加速端侧特征提取过程且不损害模型推理准确性的自动化特征提取引擎。AutoFeature 包含三个核心设计：(1) 图抽象，将不同输入特征的提取工作流表示为一个有向无环图；(2) 图优化，识别并融合图中不同特征之间的冗余操作节点；(3) 高效缓存，以最大限度地减少连续模型推理之间重叠原始数据的操作。我们实现了 AutoFeature 的系统原型，并将其集成到涵盖搜索、视频和电子商务领域的五个工业级移动服务中。在线评估表明，AutoFeature 在白天将端到端端侧模型执行延迟降低了 1.33 倍至 3.93 倍，在夜间降低了 1.43 倍至 4.53 倍。

## Abstract
Machine learning models are widely integrated into modern mobile apps to analyze user behaviors and deliver personalized services. Ensuring low-latency on-device model execution is critical for maintaining high-quality user experiences. While prior research has primarily focused on accelerating model inference with given input features, we identify an overlooked bottleneck in real-world on-device model execution pipelines: extracting input features from raw application logs. In this work, we explore a new direction of feature extraction optimization by analyzing and eliminating redundant extraction operations across different model features and consecutive model inferences. We then introduce AutoFeature, an automated feature extraction engine designed to accelerate on-device feature extraction process without compromising model inference accuracy. AutoFeature comprises three core designs: (1) graph abstraction to formulate the extraction workflows of different input features as one directed acyclic graph, (2) graph optimization to identify and fuse redundant operation nodes across different features within the graph; (3) efficient caching to minimize operations on overlapping raw data between consecutive model inferences. We implement a system prototype of AutoFeature and integrate it into five industrial mobile services spanning search, video and e-commerce domains. Online evaluations show that AutoFeature reduces end-to-end on-device model execution latency by 1.33x-3.93x during daytime and 1.43x-4.53x at night.

---

## 论文详细总结（自动生成）

这篇论文针对移动端机器学习（On-device ML）中一个长期被忽视的性能瓶颈——**特征提取**，提出了系统性的优化方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业级移动应用（如抖音、淘宝）中，端侧模型推理的输入通常是动态的用户行为序列。研究发现，**特征提取过程（从原始 SQLite 日志到模型输入特征）占用了端到端执行延迟的 61%-86%**，而传统的推理加速技术（如量化、剪枝）无法解决这一瓶颈。
*   **研究动机**：
    *   **特征冗余性**：同一模型内的不同特征往往依赖重叠的行为数据（如“过去1小时”和“过去24小时”的点击行为）。
    *   **执行冗余性**：连续的推理请求之间存在大量重叠的时间窗口，导致重复的数据检索和解码。
    *   **资源受限**：移动端内存和存储空间有限，简单的全量缓存或云端预计算方案不可行。

### 2. 方法论：AutoFeature 引擎
AutoFeature 通过将特征提取抽象为有向无环图（FE-graph）并进行多维度优化：
*   **图抽象（Graph Abstraction）**：将提取过程分解为四个原子操作：`Retrieve`（检索）、`Decode`（解码）、`Filter`（过滤）和 `Compute`（计算）。
*   **图优化（Graph Optimization）**：
    *   **内部分区（Intra-feature Partition）**：将特征提取链分解为更细粒度的子链，避免因条件合并导致处理无关数据。
    *   **间融合（Inter-feature Fusion）**：识别并融合不同特征间的冗余节点。
    *   **层次化过滤算法**：在融合节点后，利用时间戳的有序性，将过滤复杂度从 $O(len \times num\_features)$ 降低到 $O(len + num\_features)$。
*   **高效缓存机制（Efficient Caching）**：
    *   将缓存决策建模为**背包问题**，旨在给定内存预算下最大化计算节省。
    *   **贪婪策略**：根据“效用-成本比”（Utility-to-Cost ratio）优先缓存高频使用且解码昂贵的行为属性。

### 3. 实验设计
*   **应用场景**：涵盖了搜索（关键词预测 KP、搜索排序 SR）、视频（内容预加载 CP、视频推荐 VR）和电商（商品推荐 PR）五个工业级移动服务。
*   **数据集/用户**：选取了 10 名真实匿名用户，收集了其在 2 天内三个典型时段（中午、傍晚、深夜）的真实行为轨迹。
*   **Benchmark/对比方法**：
    *   **w/o AutoFeature**：工业标准特征提取流程（基准）。
    *   **消融版本**：仅融合（w/ Fusion）、仅缓存（w/ Cache）。
    *   **云端基准**：Decoded Log（存储解码后的日志）、Feature Store（特征商店）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长，因为该研究侧重于端侧推理引擎的系统优化而非模型训练。
*   **后端引擎**：使用了字节跳动的 **ByteNN** 作为推理后端，并与开源的 **MNN** 进行了对比。
*   **开销评估**：离线优化耗时仅 1.23-3.32 毫秒，在线缓存额外内存占用低于 100 KB。

### 5. 实验数量与充分性
*   **实验规模**：进行了端到端延迟测试、消融实验、存储开销对比、以及针对推理频率和特征冗余度的敏感性分析。
*   **充分性评价**：实验设计较为**充分且客观**。通过 KS 检验验证了测试用户与海量真实用户行为模式的一致性；涵盖了多个业务领域，证明了方案的通用性；对比了存储成本，指出了云端方案在移动端的不可行性。

### 6. 主要结论与发现
*   **显著加速**：AutoFeature 将端到端模型执行延迟降低了 **1.33x 至 4.53x**，使总延迟普遍降至 20ms 以下（低于人类感知的 30ms 阈值）。
*   **时段差异**：夜间的加速效果优于白天，因为夜间用户行为更密集，冗余消除的空间更大。
*   **存储优势**：相比于云端预计算方案（会导致日志体积膨胀 2.6x-2.8x），AutoFeature 几乎不增加存储负担，避免了因应用体积过大导致的用户流失。

### 7. 优点
*   **首创性**：首次揭示并系统解决了端侧模型执行中“特征提取”这一关键瓶颈。
*   **工业实用性**：方案不改变模型结构，可无缝集成到现有推理引擎（如 TFLite, MNN）中。
*   **隐私友好**：所有操作均在本地完成，符合数据隐私保护趋势。
*   **算法精巧**：通过层次化过滤和贪婪缓存策略，在极低系统开销下实现了高性能。

### 8. 不足与局限
*   **组织架构限制**：由于模型开发和基础设施优化通常由不同团队负责，目前无法实现“模型-引擎”的深度协同设计（如为了速度牺牲极小精度）。
*   **边缘情况**：对于极度不活跃的用户，由于其日志数据极少，特征提取本身开销就很低，AutoFeature 的加速效果会减弱。
*   **未来挑战**：随着未来端侧模型（如端侧大模型）变得越来越大，推理时间可能会重新占据主导，届时特征提取优化的占比将下降。

（完）
