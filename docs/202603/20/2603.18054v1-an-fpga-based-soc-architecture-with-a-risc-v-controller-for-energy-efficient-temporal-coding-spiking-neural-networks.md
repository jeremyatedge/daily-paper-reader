---
title: An FPGA-Based SoC Architecture with a RISC-V Controller for Energy-Efficient Temporal-Coding Spiking Neural Networks
title_zh: 一种基于 FPGA 的 SoC 架构，配备 RISC-V 控制器，用于高能效时间编码脉冲神经网络
authors: "Mohammad Javad Sekonji, Ali Mahani, Maryam Mirsadeghi, Mahdi Taheri"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.18054v1"
tags: ["query:aiot"]
score: 10.0
evidence: 集成RISC-V和能效脉冲神经网络核心的FPGA SoC架构
tldr: 针对边缘AI对低功耗脉冲神经网络（SNN）的需求，本文提出一种基于FPGA的SoC架构。该架构集成RISC-V控制器与事件驱动SNN核心，采用时间编码和二值化权重，通过位运算替代乘法器，并引入脉冲时间排序器以跳过无用事件。该设计在Artix-7 FPGA上实现了显著的内存缩减和低延迟推理，为实时神经形态计算提供了高效、可扩展的硬件平台。
selection_source: fresh_fetch
motivation: 解决SNN在FPGA硬件实现中面临的计算负担重、内存占用大及灵活性不足等挑战。
method: 提出一种集成RISC-V控制器的SoC架构，利用二值化权重位运算、脉冲排序和事件跳过技术优化时间编码SNN。
result: "在Artix-7 FPGA上实现了16倍的权重内存缩减，并在MNIST和FashionMNIST数据集上分别达到97.0%和88.3%的准确率。"
conclusion: 该自包含的SoC设计为低功耗边缘设备的实时神经形态推理提供了一个高效且可扩展的解决方案。
---

## 摘要
脉冲神经网络 (SNN) 具有高能效和事件驱动计算的特点，是低功耗边缘人工智能的理想选择。然而，由于计算量大、内存占用高以及灵活性有限，其在 FPGA 上的硬件实现面临挑战。本文提出了一种用于时间编码 SNN 的紧凑型片上系统 (SoC) 架构，将 RISC-V 控制器与事件驱动的 SNN 核心集成在一起。该架构利用二值化权重，通过位运算替代乘法器，包含一个用于活动脉冲的脉冲时间排序器，并跳过无信息事件以减少计算量。该架构完全在 Xilinx Artix-7 FPGA 上运行，实现了高达 16 倍的权重内存缩减，并降低了计算开销和延迟，在 MNIST 和 FashionMNIST 数据集上分别达到了 97.0% 和 88.3% 的准确率。这种自包含设计为边缘端的实时类脑推理提供了一个高效、可扩展的平台。

## Abstract
Spiking Neural Networks (SNNs) offer high energy efficiency and event-driven computation, ideal for low-power edge AI. Their hardware implementation on FPGAs, however, faces challenges due to heavy computation, large memory use, and limited flexibility. This paper proposes a compact System-on-Chip (SoC) architecture for temporal-coding SNNs, integrating a RISC-V controller with an event-driven SNN core. It replaces multipliers with bitwise operations using binarized weights, includes a spike-time sorter for active spikes, and skips noninformative events to reduce computation. The architecture runs fully on a Xilinx Artix-7 FPGA, achieving up to 16x memory reduction for weights and lowering computational overhead and latency, with 97.0% accuracy on MNIST and 88.3% on FashionMNIST. This self-contained design provides an efficient, scalable platform for real-time neuromorphic inference at the edge.

---

## 论文详细总结（自动生成）

### 论文总结：一种基于 FPGA 的 SoC 架构，配备 RISC-V 控制器，用于高能效时间编码脉冲神经网络

#### 1. 核心问题与整体含义（研究动机和背景）
脉冲神经网络（SNN）因其生物启发性的事件驱动计算和高能效，被视为边缘人工智能（Edge AI）的理想选择。然而，在 FPGA 等硬件上实现 SNN 面临三大挑战：
*   **计算负担重**：高精度权重和复杂的突触运算消耗大量资源。
*   **内存占用大**：存储大规模神经元权重需要极高的片上存储空间。
*   **灵活性不足**：现有的神经形态加速器（如 TrueNorth、Loihi）架构固定，难以适应不同的模型配置。
本文旨在提出一种**紧凑型片上系统（SoC）架构**，通过集成 RISC-V 控制器和优化的 SNN 核心，解决边缘设备在实时推理中的能效、内存和灵活性平衡问题。

#### 2. 核心方法论
论文提出了一种软硬件协同设计的 SoC 架构，主要包含以下关键技术：
*   **RISC-V 控制器集成**：采用基于 RV32I 指令集的 RISC-V 处理器负责系统配置、内存管理和数据流调度，支持标准 GNU 编译工具链，无需重新综合硬件即可快速重构参数。
*   **时间编码（Temporal Coding）**：使用“首次脉冲时间”（TTFS）编码方式，将像素强度转换为脉冲延迟，简化了信息表示。
*   **二值化权重（BS4NN）与位运算**：将权重简化为二值（±1），从而用简单的加减法和位运算替代昂贵的乘法器（DSP），显著降低了逻辑资源消耗。
*   **脉冲排序器（Spike Sorter）**：引入排序单元按时间顺序处理活动脉冲，并跳过无信息的事件，从而降低推理延迟。
*   **时间复用神经元模块**：通过时间复用技术减少了近 50% 的逻辑资源使用，同时保持了高精度。
*   **灵活性支持**：架构不仅支持二值化 SNN（BS4NN），还通过可配置模块支持多比特定点权重模型（如 S4NN）。

#### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 Fashion-MNIST（服装分类）。
*   **硬件平台**：Xilinx Artix-7 FPGA。
*   **Benchmark（对比对象）**：
    *   **Spiker (2022)**：一种针对 FPGA 优化的 SNN 加速器。
    *   **Artix-7 (2025) 相关研究**：最新的开源低端 FPGA SNN 框架。
    *   **软件基准**：对比了 BS4NN 和 S4NN 的软件模拟准确率。
*   **对比维度**：准确率、功耗、延迟、吞吐量（FPS）、资源占用（LUT、FF、BRAM、DSP）。

#### 4. 资源与算力
*   **推理端（FPGA）**：使用 Xilinx Artix-7 系列 FPGA。
    *   **功耗**：在 MNIST 上仅为 72mW，在 Fashion-MNIST 上为 158mW。
    *   **频率**：运行频率约为 163-167 MHz。
    *   **资源占用**：MNIST 任务（600-10 网络）占用约 14k LUT 和 15k FF。
*   **训练端**：论文明确指出模型采用**离线训练**（Offline Training），训练过程在软件环境中完成，训练好的权重通过 RISC-V 控制器加载至 FPGA 片上内存。文中未详细说明软件训练所使用的具体 GPU 型号或时长。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个主流基准数据集（MNIST, Fashion-MNIST）进行了测试，并分别实现了两种不同的网络规模（600-10 和 1000-10）。
*   **对比实验**：进行了二值化模型（BS4NN）与非二值化模型（S4NN）的横向对比，验证了架构的灵活性。
*   **充分性评价**：实验涵盖了准确率、资源、功耗和延迟等关键工业指标，对比了近年来的同类硬件实现。虽然数据集相对基础（未涉及 ImageNet 等大型数据集），但对于定位在“边缘低功耗”和“Artix-7 等低端 FPGA”的研究来说，实验设计是充分且客观的。

#### 6. 主要结论与发现
*   **高准确率**：在 MNIST 上达到 97.0%，在 Fashion-MNIST 上达到 88.3%，完全匹配软件模拟水平。
*   **极高能效**：相比之前的 FPGA 实现，功耗大幅降低（从 381mW 降至 72mW），且无需使用 DSP 资源。
*   **内存优化**：通过二值化权重，内存需求缩减了高达 16 倍。
*   **系统自包含性**：集成的 RISC-V 控制器使得该 SoC 能够独立运行，无需依赖外部处理器进行实时推理。

#### 7. 优点
*   **软硬结合的灵活性**：RISC-V 的引入使得系统在不改变硬件电路的情况下，能够灵活调整网络参数和处理逻辑。
*   **极致的资源优化**：通过二值化权重和时间复用神经元，极大地节省了 FPGA 的逻辑资源和存储资源，适合极低成本的 FPGA 芯片。
*   **事件驱动的效率**：脉冲排序和无效事件跳过机制有效降低了动态功耗和推理延迟。

#### 8. 不足与局限
*   **数据集复杂度有限**：实验仅限于 MNIST 类简单图像，尚未在更复杂的视觉任务（如 CIFAR-10 或目标检测）上验证其扩展性。
*   **单脉冲限制**：采用 TTFS 编码意味着每个神经元在每个推理周期只发一次脉冲，虽然高效，但可能限制了处理复杂时序动态信息的能力。
*   **缺乏片上学习**：目前仅支持离线训练后的推理，不支持在线学习（On-chip Learning），这在一定程度上限制了其在需要持续适应环境的边缘场景中的应用（作者将其列为未来工作）。

（完）
