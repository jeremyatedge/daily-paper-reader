---
title: Mitigating the Bandwidth Wall via Data-Streaming System-Accelerator Co-Design
title_zh: 通过数据流式系统-加速器协同设计缓解带宽墙
authors: "Qunyou Liu, Marina Zapater, David Atienza"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19057v1"
tags: ["query:aiot"]
score: 9.0
evidence: Transformer推理的系统-加速器协同设计
tldr: 针对Transformer推理中带宽受限的“带宽墙”问题，本文提出了一种系统与加速器协同设计方案。通过引入MatrixFlow加速器，利用分页流式数据流和计算与传输的显式重叠，结合Gem5-AcceSys全系统模拟器优化互连带宽。研究表明，采用分页流式处理和流水线重叠，而非增加片上SRAM，是提升Transformer推理效率的关键，在标准PCIe环境下即可达到接近HBM的性能。
selection_source: fresh_fetch
motivation: Transformer模型的推理性能主要受限于数据移动和互连带宽，而非单纯的算力，导致传统加速器难以发挥效能。
method: 提出MatrixFlow加速器，采用分页对齐的块矩阵乘法和流水线化DMA调度，并开发Gem5-AcceSys模拟器进行全系统协同设计。
result: "在BERT和ViT模型上，该方案比纯CPU快22倍，比现有加速器快5-8倍，且PCIe设计能达到HBM性能的80%。"
conclusion: 分页流式传输和计算传输重叠是解决现实系统约束下Transformer推理瓶颈的最有效手段。
---

## 摘要
Transformer 彻底改变了自然语言处理和计算机视觉领域的 AI，但其巨大的计算和内存需求给硬件加速带来了重大挑战。在实践中，端到端吞吐量通常受限于分页数据移动和互连带宽，而非原始的 MAC 数量。本研究提出了一种用于 Transformer 推理的统一系统-加速器协同设计方法，通过分页流式数据流以及计算与传输的显式重叠，共同优化矩阵加速器及其系统集成。在硬件方面，我们引入了 MatrixFlow，这是一种松耦合的 16x16 脉动阵列加速器，采用基于 4 KB 分块的页对齐分块矩阵乘法方法、约 20 KB 的小型片上缓冲区，以及 DMA、计算和 DMA 输出的流水线调度，以高效利用互连带宽。在系统方面，我们开发了 Gem5-AcceSys，它是 gem5 全系统模拟器的扩展，用于探索 PCIe 等标准互连以及包括直接内存（Direct Memory）、直接缓存（Direct Cache）和设备内存（Device Memory）模式在内的可配置内存层次结构，并考虑了 SMMU/TLB 效应。我们在多种数据类型和系统设置下，通过对 BERT 和 ViT 等代表性 Transformer 模型进行 gem5 模拟，评估了该协同设计。结果显示，与仅使用 CPU 的基准相比，端到端加速比高达 22 倍，且比最先进的松耦合和紧耦合加速器提升了 5 到 8 倍。我们进一步证明，标准的基于 PCIe 的主机内存设计可以达到设备端 HBM 性能的约 80%。总之，在现实的系统约束下，分页流式传输和流水线重叠，而非大型本地 SRAM，是实现高效 Transformer 推理最有效的手段。

## Abstract
Transformers have revolutionized AI in natural language processing and computer vision, but their large computation and memory demands pose major challenges for hardware acceleration. In practice, end-to-end throughput is often limited by paged data movement and interconnect bandwidth rather than raw MAC count. This work proposes a unified system-accelerator co-design approach for transformer inference that jointly optimizes a matrix accelerator and its system integration through paged streaming dataflows and explicit overlap of compute and transfer. On the hardware side, we introduce MatrixFlow, a loosely coupled 16x16 systolic-array accelerator with a page-aligned block matrix multiplication method using 4 KB tiles, a small on-chip buffer of about 20 KB, and a pipelined schedule of DMA, compute, and DMA-out to utilize interconnect bandwidth efficiently. On the system side, we develop Gem5-AcceSys, an extension of the gem5 full-system simulator that explores standard interconnects such as PCIe and configurable memory hierarchies including Direct Memory, Direct Cache, and Device Memory modes with SMMU/TLB effects. We evaluate the co-design using gem5 simulations on representative transformer models including BERT and ViT across multiple data types and system setups. Results show up to 22x end-to-end speedup over a CPU-only baseline and 5x to 8x gains over state-of-the-art loosely and tightly coupled accelerators. We further show that a standard PCIe-based host-memory design can achieve about 80 percent of the performance of on-device HBM. Overall, paged streaming and pipeline overlap, rather than large local SRAMs, are the most effective levers for efficient transformer inference under realistic system constraints.

---

## 论文详细总结（自动生成）

这篇论文由洛桑联邦理工学院（EPFL）等机构的研究人员撰写，重点探讨了如何通过硬件加速器与系统架构的协同设计来解决 Transformer 模型推理中的“带宽墙”问题。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 模型（如 BERT、ViT）虽然性能卓越，但其巨大的计算量和内存需求导致推理瓶颈。研究发现，端到端吞吐量往往受限于**分页数据移动（Paged Data Movement）**和**互连带宽（Interconnect Bandwidth）**，而非单纯的算力（MAC 数量）。
*   **研究背景**：传统的加速器设计往往关注算力提升或增加片上缓存（SRAM），但忽略了与主机系统（CPU、PCIe、SMMU）的集成开销。在实际系统中，DMA 设置、PCIe 延迟和地址转换（TLB 缺失）可能占据总运行时间的 40% 以上。
*   **整体含义**：本文主张“系统-加速器协同设计”，通过优化数据流使其与系统分页机制对齐，利用流式传输和流水线重叠来缓解带宽瓶颈。

### 2. 论文提出的方法论
核心思想是**MatrixFlow 加速器**与 **Gem5-AcceSys 模拟平台**的深度耦合：
*   **MatrixFlow 硬件设计**：
    *   采用 16×16 的脉动阵列（Systolic Array）。
    *   **极小片上缓存**：仅使用约 20 KB 的 SRAM（分为 3 个 4 KB 的分块），远小于传统加速器（通常为数百 KB）。
    *   **分页对齐流式数据流**：将矩阵划分为 4 KB 的分块（Tile），与操作系统的标准内存页大小一致，从而最大化 DMA 传输效率并减少 SMMU/TLB 转换开销。
*   **Gem5-AcceSys 模拟系统**：
    *   扩展了 gem5 全系统模拟器，增加了对 PCIe 3.0/4.0/5.0/6.0 互连、多通道 DMA 引擎和系统内存管理单元（SMMU/IOMMU）的精确建模。
    *   支持三种访问模式：**直接内存（DM）**、**直接缓存（DC）**和**设备内存（DevMem）**。
*   **软件/算法优化**：
    *   开发了轻量级驱动程序，实现 DMA 传输与脉动阵列计算的**双缓冲流水线重叠**。
    *   优化矩阵布局（如 B 矩阵的行条带化），避免跨页的非连续访问。

### 3. 实验设计
*   **数据集与模型**：
    *   **微内核**：64×64 到 2048×2048 的不同精度（INT8/16/32, FP16/32）矩阵乘法。
    *   **完整模型**：BERT-Base/Large（NLP 任务）和 ViT-Base/Large/Huge（视觉任务）。
*   **基准测试（Benchmarks）与对比方法**：
    *   **CPU 基准**：单核 ARM CPU、多线程 CPU（OpenMP, 256 线程）、ARM Neon SIMD 优化版。
    *   **加速器对比**：
        *   **SMAUG**：最先进的松耦合 DNN 加速器模拟框架。
        *   **TiC-SAT**：最先进的紧耦合（集成在 CPU 指令集内）脉动阵列加速器。
*   **测试场景**：涵盖了不同的 PCIe 带宽（2 GB/s 到 128 GB/s）、不同的内存技术（DDR3/4/5, GDDR6, HBM2）以及不同的数据放置位置。

### 4. 资源与算力
*   **模拟环境**：论文基于 **gem5** 进行全系统周期精确模拟，而非在物理 GPU 上训练。
*   **硬件合成**：为了获得准确的面积和功耗数据，研究者使用 **TSMC 28nm** 工艺对 MatrixFlow 进行了逻辑合成。
    *   16×16 INT8 阵列在 1 GHz 下的峰值算力为 512 GOPS。
    *   合成面积约为 0.186 $mm^2$，动态功耗约为 84.55 mW。
*   **训练时长**：文中未提及模型训练时长，因为研究重点在于**推理（Inference）阶段**的架构仿真。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括：
    *   不同矩阵规模和精度的性能扫描。
    *   PCIe 数据包大小（64B 到 4096B）对效率影响的消融实验。
    *   内存带宽 vs. 延迟的敏感度分析。
    *   地址转换（SMMU）开销的定量分析。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了算力，还深入分析了非计算部分（Non-GEMM）对整体性能的影响，并探讨了在不同互连带宽下，主机内存方案何时能超越昂贵的设备端 HBM 方案。

### 6. 论文的主要结论与发现
*   **性能飞跃**：MatrixFlow 在端到端推理上比单核 CPU 快 **22 倍**，比 SOTA 紧耦合加速器（TiC-SAT）快 **8 倍**，比松耦合加速器（SMAUG）快 **5 倍**。
*   **带宽胜过缓存**：在现实系统约束下，通过分页流式传输和流水线重叠，即使只有 20 KB 的极小 SRAM，也能达到极高的利用率。
*   **互连的重要性**：标准的 PCIe 方案如果配置得当（如 Gen6 x16），可以达到专用 HBM 内存性能的 **80%**。
*   **瓶颈转移**：随着 GEMM 被大幅加速，非 GEMM 操作（如 Softmax、LayerNorm）和系统控制开销（约占 24%）成为新的瓶颈。

### 7. 优点
*   **全系统视角**：跳出了单纯的加速器微架构设计，将 PCIe、SMMU 和驱动程序开销纳入考量，更符合工业界真实部署场景。
*   **资源效率极高**：证明了通过聪明的调度和对齐，可以用极小的硅片面积（极小 SRAM）换取极高的性能。
*   **开源贡献**：开发的 Gem5-AcceSys 为学术界提供了一个研究标准互连和系统集成开销的强大工具。

### 8. 不足与局限
*   **模拟局限性**：所有结论均基于 gem5 模拟，虽然具有周期精确性，但仍需物理芯片的最终验证。
*   **模型局限性**：主要针对 Transformer 结构，对于非矩阵乘法主导的轻量级模型（如某些 CNN 或单层 MLP），其优势可能不明显。
*   **非 GEMM 瓶颈**：论文虽然识别了非 GEMM 操作的瓶颈，但 MatrixFlow 本身并未对这些操作（如 Softmax）提供专门的硬件加速，仍依赖主机 CPU。

（完）
