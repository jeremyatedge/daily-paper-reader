---
title: "LiteAtt: Secure and Seamless IoT Services Using TinyML-based Self-Attestation as a Primitive"
title_zh: LiteAtt：使用基于 TinyML 的自验证作为原语的安全且无缝的物联网服务
authors: "Varun Kohli, Biplab Sikdar"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19727v1"
tags: ["query:aiot"]
score: 8.0
evidence: 基于 TinyML 的物联网安全自验证
tldr: LiteAtt 是一种针对物联网微控制器（MCU）的自验证框架，旨在应对日益增长的软件攻击。它利用 Arm TrustZone 中的 TinyML 技术，在设备本地对 SRAM 占用情况进行快速评估，从而实现了无需远程验证者的去中心化信任机制。该框架在保护用户隐私的同时，以极低的延迟和能耗实现了高精度的固件完整性验证，为电池供电的物联网设备提供了安全保障。
selection_source: fresh_fetch
motivation: 传统的依赖远程验证者的固件验证方式难以满足现代物联网设备对去中心化和隐私保护的需求。
method: 在 Arm TrustZone 中部署 TinyML 模型，通过分析 SRAM 运行指纹实现设备本地的固件完整性自验证。
result: "实验显示该框架准确率达 98.7%，延迟仅 1.29ms，能耗极低，适用于 Cortex-M 系列设备。"
conclusion: LiteAtt 为物联网设备提供了一种高效、安全且无需外部验证者的去中心化信任根解决方案。
---

## 摘要
随着物联网（IoT）成为关键基础设施、智慧城市和消费者网络不可或缺的一部分，针对构成此类网络的微控制器（MCU）的软件攻击数量不断增加。运行时固件验证（即固件完整性验证）已变得至关重要，先前的工作主要集中在轻量级物联网 MCU 上，将验证任务卸载给具备能力的远程验证器。然而，现代物联网设备具有大容量闪存和易失性存储器、设备端 TinyML 推理以及可信执行环境（TEE）。利用这些功能，本文提出了一种名为 LiteAtt 的无验证器混合自验证（SA）框架，该框架基于在物联网 MCU 的 Arm TrustZone 中执行 TinyML，用于对物联网固件的 SRAM 占用情况进行快速的设备端评估。LiteAtt 向物联网网络中的泛在智能和去中心化信任迈出了一步。它消除了验证时对固件副本的需求，并通过利用孪生设备训练 TinyML 模型来保护用户 SRAM 数据的隐私。该框架在从真实设备收集的 SRAM 验证数据集上实现了 98.7% 的平均准确率、99.33% 的 F1 分数、98.72% 的 TPR 和 97.45% 的 TNR。LiteAtt 的运行延迟为 1.29ms，能耗为 42.79uJ，运行时内存开销最高为 32KB，适用于电池供电的 Arm Cortex-M 设备。本文还针对该协议在双向身份验证、机密性、完整性、SRAM 隐私以及防御重放和冒充攻击方面提供了安全性分析。此外，还讨论了实际部署场景和未来工作。

## Abstract
As the Internet of Things (IoT) becomes an integral part of critical infrastructure, smart cities, and consumer networks, there has been an increase in the number of software attacks on the microcontrollers (MCUs) that constitute such networks. Runtime firmware attestation, i.e., the verification of a firmware's integrity, has become instrumental, and prior work focuses on lightweight IoT MCUs, offloading the verification task to capable remote verifiers. However, modern IoT devices feature large flash and volatile memory, on-device TinyML inference, and Trusted Execution Environments (TEE). Leveraging these capabilities, this paper presents a verifier-less, hybrid Self-Attestation (SA) framework called LiteAtt, which is based on TinyML execution in the Arm TrustZone of an IoT MCU for quick, on-device evaluation of the IoT firmware's SRAM footprint. LiteAtt takes a step towards ubiquitous intelligence and decentralized trust in IoT networks. It eliminates the need for firmware copies for attestation, and protects the privacy of user SRAM data by leveraging twin devices to train the TinyML models. The proposed framework achieves an average accuracy of 98.7%, F1 score of 99.33%, TPR of 98.72%, and TNR of 97.45% on SRAM attestation datasets collected from real devices. LiteAtt operates with a latency of 1.29ms, an energy consumption of 42.79uJ, and a runtime memory overhead of up to 32KB, which is suitable for battery-operated Arm Cortex-M devices. A security analysis is provided for the protocol regarding mutual authentication, confidentiality, integrity, SRAM privacy, and defense against replay and impersonation attacks. Practical deployment scenarios and future works are also discussed.

---

## 论文详细总结（自动生成）

这是一份关于论文《LiteAtt: Secure and Seamless IoT Services Using TinyML-based Self-Attestation as a Primitive》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着物联网（IoT）设备深入关键基础设施，针对微控制器（MCU）的固件篡改和运行时攻击（如恶意软件注入）日益增多。
*   **传统方案的局限**：现有的远程验证（Remote Attestation, RA）依赖一个强大的外部“验证者”来检查“证明者”（IoT设备）的状态。这导致了高通信延迟、隐私泄露（需上传SRAM数据）以及对中心化设施的依赖。
*   **研究目标**：论文提出了 **LiteAtt**，旨在利用现代 MCU 的硬件特性（如 Arm TrustZone TEE 和 TinyML 推理能力），实现一种**去中心化、无验证者、保护隐私**的自验证（Self-Attestation, SA）框架，使设备能自主证明其固件完整性。

### 2. 方法论：核心思想与关键技术
LiteAtt 的核心思想是在设备的受信任执行环境（TEE）中运行一个轻量级的机器学习模型，实时监测 SRAM 的运行指纹。
*   **核心流程**：
    1.  **SRAM 指纹提取**：将 SRAM 的数据段（.data 和 .bss）划分为块，计算每块的平均值进行降维聚合，形成特征向量。
    2.  **TinyML 模型（Autoencoder）**：在 TEE 内部部署一个经过 int8 量化的自编码器（AE）。该模型仅在正常固件的 SRAM 轨迹上训练。
    3.  **自适应阈值选择**：根据验证集重构误差的分布（P95-P99 分位数），动态计算检测阈值，以平衡检出率（TPR）和误报率（TNR）。
    4.  **隐私保护训练**：利用“物理/数字孪生设备”生成训练数据，无需接触用户的实际运行数据，且推理过程完全在 TEE 隔离区完成。
    5.  **LiteAtt 协议**：设计了一个四次握手的双向认证协议，结合随机数（Nonce）、时间戳和 AES-CBC 加密，确保验证报告的机密性、完整性和防重放攻击。

### 3. 实验设计
*   **硬件平台**：使用了三种典型的 Arm Cortex-M 系列开发板：Arduino Portenta C33（支持 TrustZone）、Portenta H7 和 Nano 33 BLE Sense。
*   **数据集**：涵盖了 4 个主要数据集，包含 23 种不同的固件（如 AES 加密、传感器监控、电机控制、Swarm 机器人集群任务等）。
*   **恶意样本**：通过修改正常固件的控制流、函数逻辑或数据段，生成了超过 136 万个恶意 SRAM 样本。
*   **Benchmark 与对比**：
    *   **模型对比**：对比了 2 层 MLP、3 层 MLP 和 4 层 CNN 架构。
    *   **方法对比**：与传统的软件验证（SWATT）、硬件辅助验证（TyTan）以及最新的基于 ML 的远程验证（SAFE-IoT, Swarm-Net）在延迟、精度和架构上进行了对比。

### 4. 资源与算力
*   **训练端**：模型在 PC 端使用 TensorFlow 2.17 和 Python 3.11 训练，随后通过 TFLite Micro 进行 int8 量化。文中**未明确说明**具体使用的 GPU 型号或训练时长，但考虑到 AE 模型极其轻量（仅几层全连接），训练开销通常极低。
*   **推理端（MCU）**：
    *   **内存**：峰值内存开销约 16.8KB - 32KB。
    *   **存储**：量化后的模型文件仅 3KB - 12KB。
    *   **算力消耗**：在 200MHz 的 Cortex-M33 上，推理延迟低至 0.08ms - 0.86ms。

### 5. 实验数量与充分性
*   **实验规模**：测试了 23 种固件，正常样本 10,380 个，恶意样本 1,368,290 个。这种非对称的样本量设计模拟了现实中恶意攻击的多样性。
*   **充分性**：
    *   涵盖了单节点和集群（Swarm）两种物联网部署模式。
    *   进行了跨硬件平台的性能评估（C33 vs H7 vs Nano）。
    *   提供了基于博弈论的安全性形式化分析。
*   **客观性**：通过自适应阈值选择（而非手动调优）来评估性能，增加了实验结果的可信度。

### 6. 主要结论与发现
*   **高性能**：平均准确率达到 **98.7%**，F1 分数 **99.33%**。
*   **极低开销**：在 Portenta C33 上，整个自验证流程（含聚合、推理、加密）的总延迟仅为 **1.29ms**，能耗仅 **42.79 μJ**。
*   **隐私与安全**：成功实现了在不泄露 SRAM 原始数据的情况下完成验证，且能有效防御重放攻击和冒充攻击。
*   **架构优势**：相比于传统的远程验证（延迟通常在秒级或百毫秒级），LiteAtt 实现了数量级的速度提升。

### 7. 优点（亮点）
*   **去中心化信任**：摆脱了对远程验证者的依赖，非常适合无人机群（UAV Swarms）或低轨卫星等无法实时联网的场景。
*   **TEE 与 TinyML 的结合**：将 ML 模型置于 TrustZone 中，既保证了检测的智能性，又保证了检测逻辑本身不被篡改。
*   **孪生设备策略**：巧妙地解决了物联网安全中“数据获取难”和“隐私保护”之间的矛盾。

### 8. 不足与局限
*   **统计学安全限制**：LiteAtt 提供的是统计学上的安全保证（依赖模型准确率），而非绝对的密码学证明。如果攻击者能构造出 SRAM 指纹极度接近正常状态的对抗性样本，可能存在漏报风险。
*   **侧信道攻击**：虽然论文假设 TEE 是安全的，但未深入探讨针对 TEE 的侧信道攻击（如功耗分析或时序攻击）对 LiteAtt 的潜在威胁。
*   **覆盖范围**：目前主要针对 SRAM 的数据段（Data Section），对于堆（Heap）和栈（Stack）的动态变化监测仍有待加强，因为这些区域的非确定性更高，模型训练更复杂。

（完）
