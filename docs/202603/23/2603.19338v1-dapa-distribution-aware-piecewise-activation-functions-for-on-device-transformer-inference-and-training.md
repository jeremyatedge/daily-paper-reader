---
title: "DAPA: Distribution Aware Piecewise Activation Functions for On-Device Transformer Inference and Training"
title_zh: DAPA：面向端侧 Transformer 推理与训练的分布感知分段激活函数
authors: "Maoyang Xiang, Bo Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19338v1"
tags: ["query:aiot"]
score: 9.0
evidence: 适用于设备端Transformer推理的硬件友好型激活函数
tldr: 针对端侧Transformer推理和训练中非线性激活函数资源消耗大的问题，本文提出了一种分布感知的分段激活函数（DAPA）。该方法利用预激活数据的分布特性，在概率较高区域分配更精细的分段，并结合分布加权均方误差进行量化。实验表明，DAPA在保持模型性能的同时，显著提升了计算速度并降低了硬件资源占用，是高效的硬件友好型方案。
selection_source: fresh_fetch
motivation: 传统的非线性激活函数在端侧设备上计算复杂且资源消耗大，严重限制了Transformer模型的部署效率和能效。
method: 提出DAPA方法，通过非均匀分段逼近数据分布的高概率区域，并利用分布加权均方误差进行量化以优化硬件部署。
result: 实验显示DAPA将GELU计算速度提升了16倍，DSP资源占用降低了16倍，且在ViT和GPT-2模型上维持了优异性能。
conclusion: DAPA通过结合数据分布特性与分段线性近似，为端侧Transformer的高效推理与训练提供了一种极具竞争力的激活函数替代方案。
---

## 摘要
非线性激活函数在端侧推理和训练中起着至关重要的作用，因为它们不仅消耗大量的硬件资源，还对系统性能和能效产生重大影响。在这项工作中，我们通过利用激活前数据的分布，提出了一种适用于 Transformer 架构的可微分且硬件友好的激活函数——分布感知分段激活（DAPA）。DAPA 采用非均匀分段近似，将更精细的段分配给分布的高概率区域，从而提高了相比于先前分段线性方法的泛化能力。所得近似值使用分布权重均方误差（Distribution-Weighted Mean Square Error）进行进一步量化，以降低硬件部署的延迟和资源占用。我们的 HLS 实现表明，DAPA 将 GELU 计算速度提升了 16 倍，并将 DSP 利用率降低了 16 倍，同时在视觉 Transformer 和 GPT-2 模型中保持了相当或更好的性能。

## Abstract
Non-linear activation functions play a pivotal role in on-device inference and training, as they not only consume substantial hardware resources but also impose a significant impact on system performance and energy efficiency. In this work, we propose Distribution-Aware Piecewise Activation (DAPA), a differentiable and hardware-friendly activation function for Transformer architectures by exploiting the distribution of pre-activation data. DAPA employs a non-uniform piecewise approximation that allocates finer segments to high-probability regions of the distribution, improving generalizability over prior piecewise linear methods. The resulting approximation is further quantized using Distribution-Weighted Mean Square Error to reduce latency and resource utilization for hardware deployment. Our HLS implementation demonstrates that DAPA speeds up GELU computation by 16$\times$ and decreases DSP utilization by 16$\times$ while maintaining comparable or better performance across vision Transformers and GPT-2 models.

---

## 论文详细总结（自动生成）

### 论文总结：DAPA —— 面向端侧 Transformer 推理与训练的分布感知分段激活函数

#### 1. 核心问题与研究动机
*   **核心问题**：在端侧设备（如边缘 AI 芯片、FPGA）上，Transformer 模型中的非线性激活函数（如 GELU、Softmax）是计算瓶颈。虽然矩阵乘法可以高度并行化，但非线性函数的复杂计算（如指数、除法）往往会导致流水线停顿，消耗大量硬件资源（DSP、LUT）并增加延迟。
*   **研究动机**：现有的近似方法（如分段线性近似）通常以最小化均方误差（MSE）为目标。然而，MSE 假设输入数据在整个量化范围内是均匀分布的，这忽略了神经网络中预激活数据（Pre-activation）往往集中在特定区域的特性。这导致硬件资源被浪费在极少出现的输入区域，而高频区域的近似精度不足。

#### 2. 方法论
*   **核心思想**：提出 **DAPA（Distribution-Aware Piecewise Activation）**，通过感知输入数据的概率密度函数（PDF），在数据分布密集的区域分配更细致的分段，在稀疏区域分配粗略分段。
*   **关键技术细节**：
    *   **DWMSE（分布加权均方误差）**：引入一种新的损失函数，将误差平方按输入数据的概率密度 $p(x)$ 进行加权。实验证明 DWMSE 与模型最终性能的相关性远高于传统 MSE。
    *   **非均匀分段策略**：利用累积分布函数（CDF）的逆函数 $F^{-1}$，将概率质量等分为 $N$ 份，从而确定分段的节点（Knots）。这意味着每个分段在统计上被触发的频率是相等的。
    *   **加权最小二乘法（WLS）**：在每个分段内，通过求解 WLS 问题来确定线性近似的系数（斜率和截距）。
    *   **DWMSE 引导的量化**：提出一种 16 位定点量化方案，根据 DWMSE 设定的误差预算，自动迭代选择整数位和分数位的精度。
    *   **可微性**：DAPA 不仅近似函数本身，还近似其导数，使其能够直接用于模型的端侧训练和微调。

#### 3. 实验设计
*   **数据集与场景**：
    *   **计算机视觉（CV）**：在 ImageNet-1K 上评估了 ViT (Tiny/Small/Base)、DeiT 和 Swin Transformer。
    *   **自然语言处理（NLP）**：在 WikiText-2 上评估了 GPT-2，在 GLUE 基准测试上评估了 BERT。
*   **Benchmark 与对比方法**：
    *   **基准**：PyTorch 官方 FP32 实现。
    *   **对比方法**：基于 MSE 的近似方法、PEANO-ViT、SwiftTron 以及其他针对 Transformer 优化的非线性算子方案。
*   **硬件评估**：使用 Vitis HLS 2025.1 在 200MHz 频率下进行 FPGA 综合，对比了资源占用（DSP、FF、LUT）和延迟。

#### 4. 资源与算力
*   **硬件实现**：使用了 Xilinx FPGA 相关的 HLS 工具进行评估。
*   **训练算力**：论文提到了从头开始训练（Train from scratch）ViT 模型以验证 DAPA 的收敛性，但**未明确说明**具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了当前主流的多种 Transformer 变体（ViT, Swin, GPT, BERT），并在 CV 和 NLP 两个大领域进行了验证。
*   **消融与鲁棒性实验**：
    *   分析了用于估计分布的样本数量（M）对精度的影响，证明仅需少量样本（如 16 张图像）即可获得稳定的分布估计。
    *   对比了不同分段数量（N=4, 8, 16）下的性能表现。
*   **充分性评价**：实验设计较为全面，不仅验证了推理精度，还验证了训练收敛性及硬件实现的效率，对比基准涵盖了近年来的前沿工作，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **精度保持**：DAPA(16) 在几乎所有模型上都达到了与 FP32 基准相当的精度，且显著优于基于 MSE 的近似方法。
*   **硬件效率**：相比于传统的定点 GELU 实现，DAPA 将 DSP 利用率降低了 **16 倍**，计算速度提升了 **16 倍**；在 Softmax 单元中，DSP 占用降低了 **48 倍**。
*   **训练能力**：使用 DAPA 替代标准 GELU 进行从头训练，模型收敛速度一致，且在某些 ViT 变体上取得了略高的最终准确率（如 ViT-Small 提升 0.65%）。

#### 7. 优点
*   **分布感知**：巧妙地将统计分布引入分段线性近似，解决了硬件资源分配不均的问题。
*   **软硬协同**：不仅提供了数学上的近似方法，还给出了具体的 HLS 硬件架构设计，具有很强的实用价值。
*   **通用性**：方案可推广至多种非线性函数（GELU, Exp, Sigmoid 等）和多种 Transformer 架构。

#### 8. 不足与局限
*   **分布依赖性**：虽然实验显示少量样本即可估计分布，但如果推理时的实际数据分布与离线统计的分布发生剧烈偏移（Out-of-distribution），近似精度可能会受到影响。
*   **定点限制**：目前主要针对 16 位定点环境设计，对于更低比特（如 Int8）或更复杂浮点格式的适配讨论较少。
*   **硬件灵活性**：虽然分段节点是离线计算的，但若要在运行时动态调整分布感知参数，可能会增加额外的硬件控制逻辑开销。

（完）
