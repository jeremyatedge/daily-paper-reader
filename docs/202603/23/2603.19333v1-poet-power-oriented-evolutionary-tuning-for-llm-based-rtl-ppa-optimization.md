---
title: "POET: Power-Oriented Evolutionary Tuning for LLM-Based RTL PPA Optimization"
title_zh: POET：面向功耗的基于大语言模型的 RTL PPA 优化进化调优
authors: "Heng Ping, Peiyu Zhang, Zhenkun Wang, Shixuan Li, Anzhe Cheng, Wei Yang, Paul Bogdan, Shahin Nazarian"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19333v1"
tags: ["query:aiot"]
score: 9.0
evidence: 使用大模型进行面向功耗的硬件RTL优化
tldr: "本研究针对大语言模型在RTL代码优化中面临的幻觉导致的功能错误及多目标PPA权衡难题，提出了POET框架。该框架通过差分测试生成测试平台以确保100%的功能正确性，并引入以功耗为导向的进化机制，利用非支配排序和功耗优先排名策略，在无需手动调参的情况下引导搜索向低功耗Pareto前沿演进。实验表明，POET在40个设计中均实现了最优功耗和完全的功能正确性。"
selection_source: fresh_fetch
motivation: 旨在解决LLM在RTL优化中的幻觉问题，并系统性地在PPA多目标空间中优先实现功耗降低。
method: 采用基于差分测试的验证流水线确保功能一致性，并结合LLM驱动的进化算法进行功耗优先的Pareto搜索。
result: "在RTL-OPT基准测试的40个设计中，POET均达到了100%的功能正确性，并取得了最优的功耗优化结果。"
conclusion: POET通过结合确定性验证与进化搜索，为基于LLM的硬件设计自动化提供了一种高效且可靠的功耗优化方案。
---

## 摘要
将大语言模型（LLM）应用于 RTL 代码优化以改进功耗、性能和面积（PPA）面临两个关键挑战：一是在 LLM 存在幻觉的情况下确保优化设计的功能正确性；二是在多目标 PPA 权衡空间中系统地优先考虑降低功耗。我们提出了 POET（Power-Oriented Evolutionary Tuning），这是一个应对上述挑战的框架。针对功能正确性，POET 引入了一种基于差分测试的测试平台（testbench）生成流水线，将原始设计视为功能参考模型（oracle），利用确定性仿真生成黄金参考（golden references），从而在验证过程中消除 LLM 幻觉。针对 PPA 优化，POET 采用了一种 LLM 驱动的进化机制，结合非支配排序、功耗优先的层内排名以及比例幸存者选择，在无需手动调整权重的情况下，引导搜索向 Pareto 前沿的低功耗区域靠拢。在包含 40 个不同 RTL 设计的 RTL-OPT 基准测试上进行的评估表明，POET 实现了 100% 的功能正确性，在所有 40 个设计中均获得了最佳功耗，并在面积和延迟改进方面具有竞争力。

## Abstract
Applying large language models (LLMs) to RTL code optimization for improved power, performance, and area (PPA) faces two key challenges: ensuring functional correctness of optimized designs despite LLM hallucination, and systematically prioritizing power reduction within the multi-objective PPA trade-off space. We propose POET (Power-Oriented Evolutionary Tuning), a framework that addresses both challenges. For functional correctness, POET introduces a differential-testing-based testbench generation pipeline that treats the original design as a functional oracle, using deterministic simulation to produce golden references and eliminating LLM hallucination from the verification process. For PPA optimization, POET employs an LLM-driven evolutionary mechanism with non-dominated sorting, power-first intra-level ranking, and proportional survivor selection to steer the search toward the low-power region of the Pareto front without manual weight tuning. Evaluated on the RTL-OPT benchmark across 40 diverse RTL designs, POET achieves 100% functional correctness, the best power on all 40 designs, and competitive area and delay improvements.

---

## 论文详细总结（自动生成）

### POET: 面向功耗的基于大语言模型的 RTL PPA 优化进化调优论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在集成电路设计中，将大语言模型（LLM）用于寄存器传输级（RTL）代码优化面临两大挑战：
*   **功能正确性难题：** LLM 固有的“幻觉”倾向常导致生成的优化代码出现逻辑错误。在 RTL-OPT 基准测试中，即使是 DeepSeek-R1 也有超过 25% 的尝试引入了错误。
*   **多目标 PPA 权衡：** 功耗（Power）、性能（Performance/Delay）和面积（Area）之间存在复杂的权衡。现有方法通常采用手动调整权重的加权和函数，难以系统性地优先降低功耗，且无法有效探索非凸的 Pareto 前沿。
**POET 框架**旨在通过自动化验证流水线和以功耗为导向的进化算法，在保证 100% 功能正确性的前提下，实现系统性的低功耗 RTL 优化。

#### 2. 方法论：核心思想与关键技术
POET 由两个核心组件构成：
*   **基于差分测试的测试平台生成（Differential-Testing-Based Testbench Generation）：**
    *   **核心思想：** 将原始（虽非最优但功能正确）的 RTL 设计视为“功能参考模型（Oracle）”。
    *   **流程：** 1. LLM 提取原始设计的规格说明；2. LLM 生成测试激励（输入向量）；3. 使用确定性仿真器（如 Icarus Verilog）在原始设计上运行激励，捕获“黄金输出”；4. 组装成包含断言的测试平台。这种方法避免了让 LLM 直接推理时序逻辑输出，从而消除了验证环节的幻觉。
*   **面向功耗的进化优化引擎：**
    *   **进化算子：** 引入了 Improve（针对弱项）、Refactor（重构）、Explore（架构探索）、Simplify（简化）、Fusion（融合）和 Crossover（交叉）六类算子，并结合 UCB 算法进行自适应算子选择。
    *   **幸存者选择策略：** 采用改进的 NSGA-II 算法。首先进行**非支配排序**划分 Pareto 等级；然后在同一等级内按**功耗升序排名**；最后通过**比例槽位分配**（Proportional Slot Allocation）保留各等级代表，既优先低功耗又维持种群多样性。

#### 3. 实验设计
*   **数据集/基准测试：** 使用 **RTL-OPT** 基准测试，包含 40 个专家设计的 RTL 模块，涵盖算术电路、控制逻辑和数据通路。
*   **对比方法：** 
    1.  **I/O Prompting：** 直接提示 LLM 进行优化。
    2.  **Chain-of-Thought (CoT)：** 引导 LLM 进行逐步推理优化。
    3.  **REvolution：** 现有的基于加权和适应度函数的进化框架。
*   **评估工具：** 使用 Yosys 进行逻辑综合，OpenSTA 进行时序和功耗分析（基于 NanGate 45nm 工艺库）。

#### 4. 资源与算力
*   **模型：** 实验使用 **GPT-4o-mini** 作为后端 LLM。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架属于推理侧的智能体（Agentic）框架，其开销主要集中在 LLM API 调用次数（每个任务总调用次数与基线对齐）和 EDA 工具的仿真/综合运行时间上。

#### 5. 实验数量与充分性
*   **实验规模：** 在 40 个不同的 RTL 设计上进行了全量对比实验。
*   **消融实验：** 针对差分测试、进化机制、功耗排名、比例分配和领域知识等 5 个关键组件分别进行了消融研究。
*   **充分性评价：** 实验设计较为充分。通过 40 组不同复杂度的电路验证了通用性，消融实验证明了各模块的必要性，案例研究（Case Study）展示了优化轨迹，整体评估客观且公平。

#### 6. 主要结论与发现
*   **功能正确性：** POET 达到了 **100% 的功能正确率**（40/40），显著优于 REvolution (90%)、CoT (82.5%) 和 I/O (72.5%)。
*   **功耗优化：** 在所有 40 个设计中，POET 均取得了**最佳的功耗结果**。
*   **综合性能：** 在获得最佳功耗的同时，POET 在 72.5% 的设计中取得了最佳面积，在 67.5% 的设计中取得了最佳延迟。
*   **发现：** 进化机制是优化的核心驱动力，而差分测试是确保优化结果可用的基石。

#### 7. 优点
*   **可靠性高：** 通过差分测试巧妙地解决了硬件描述语言生成中的幻觉问题，无需人工编写测试平台。
*   **无需调参：** 采用非支配排序和功耗优先排名，避免了传统方法中繁琐的 PPA 权重手动调优。
*   **多样性保护：** 比例分配策略防止了算法过早收敛到局部最优，保留了潜在的优化路径。

#### 8. 不足与局限
*   **EDA 工具依赖：** 优化循环高度依赖 Yosys/OpenSTA 等工具的反馈，综合过程可能成为大批量优化时的性能瓶颈。
*   **设计规模限制：** 实验主要集中在模块级 RTL（RTL-OPT），对于超大规模 SoC 级设计的处理能力和 LLM 上下文窗口限制尚未深入探讨。
*   **成本考虑：** 进化算法涉及多次迭代和 LLM 调用，在大规模应用时可能产生较高的 API 使用成本。

（完）
