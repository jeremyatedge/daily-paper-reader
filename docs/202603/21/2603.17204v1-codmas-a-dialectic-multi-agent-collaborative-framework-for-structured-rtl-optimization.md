---
title: "CODMAS: A Dialectic Multi-Agent Collaborative Framework for Structured RTL Optimization"
title_zh: CODMAS：一种用于结构化 RTL 优化的辩证多智能体协作框架
authors: "Che-Ming Chang, Prashanth Vijayaraghavan, Ashutosh Jadhav, Charles Mackin, Vandana Mukherjee, Hsinyu Tsai, Ehsan Degan"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17204v1"
tags: ["query:aiot"]
score: 8.0
evidence: 针对功耗、性能和面积的自动化 RTL 优化
tldr: CODMAS是一个针对RTL代码优化的多智能体协作框架，通过辩证推理、领域感知生成和确定性评估来提升芯片设计的PPA。它引入了Articulator和Hypothesis Partner等智能体进行逻辑推演，并配合编码与评估智能体实现自动化优化。在RTLOPT基准测试中，该框架显著降低了关键路径延迟和功耗，同时提高了代码的功能正确性。
selection_source: fresh_fetch
motivation: 传统的RTL优化过程复杂且易出错，需要一种能够结合结构化推理与领域知识的自动化方法来提升PPA。
method: 提出CODMAS框架，利用辩证智能体进行计划制定与结果预测，并结合领域特定编码智能体和评估智能体进行Verilog代码的迭代优化。
result: "在RTLOPT基准上，流水线优化使关键路径延迟降低约25%，门控时钟优化使功耗降低约22%，且功能失效显著减少。"
conclusion: 结构化的多智能体辩证推理能有效增强自动化RTL优化能力，并具备扩展至更复杂设计任务的潜力。
---

## 摘要
优化寄存器传输级（RTL）代码是电子设计自动化（EDA）中提高功耗、性能和面积（PPA）的关键步骤。我们提出了 CODMAS（通过辩证多智能体系统进行的协作优化），这是一个将结构化辩证推理与领域感知代码生成及确定性评估相结合的框架，旨在实现 RTL 优化的自动化。CODMAS 的核心是两个辩证智能体：受“小黄鸭调试法”启发的阐述者（Articulator），负责阐明逐步转换计划并揭示潜在假设；以及假设伙伴（Hypothesis Partner），负责预测结果并调和预期与实际行为之间的偏差，以指导有针对性的改进。这些智能体指导领域特定编码智能体（DCA）生成架构感知的 Verilog 修改，并指导代码评估智能体（CEA）验证语法、功能和 PPA 指标。我们引入了 RTLOPT，这是一个包含 120 个 Verilog 三元组（未优化、已优化、测试平台）的基准测试集，用于流水线化和时钟门控转换。在专有和开源大语言模型（LLM）上，与强提示词和智能体基线相比，CODMAS 在流水线化方面实现了约 25% 的关键路径延迟降低，在时钟门控方面实现了约 22% 的功耗降低，同时减少了功能和编译失败。这些结果表明，结构化多智能体推理可以显著增强自动化 RTL 优化，并扩展到更复杂的设计和更广泛的优化任务。

## Abstract
Optimizing Register Transfer Level (RTL) code is a critical step in Electronic Design Automation (EDA) for improving power, performance, and area (PPA). We present CODMAS (Collaborative Optimization via a Dialectic Multi-Agent System), a framework that combines structured dialectic reasoning with domain-aware code generation and deterministic evaluation to automate RTL optimization. At the core of CODMAS are two dialectic agents: the Articulator, inspired by rubber-duck debugging, which articulates stepwise transformation plans and exposes latent assumptions; and the Hypothesis Partner, which predicts outcomes and reconciles deviations between expected and actual behavior to guide targeted refinements. These agents direct a Domain-Specific Coding Agent (DCA) to generate architecture-aware Verilog edits and a Code Evaluation Agent (CEA) to verify syntax, functionality, and PPA metrics. We introduce RTLOPT, a benchmark of 120 Verilog triples (unoptimized, optimized, testbench) for pipelining and clock-gating transformations. Across proprietary and open LLMs, CODMAS achieves ~25% reduction in critical path delay for pipelining and ~22% power reduction for clock gating, while reducing functional and compilation failures compared to strong prompting and agentic baselines. These results demonstrate that structured multi-agent reasoning can significantly enhance automated RTL optimization and scale to more complex designs and broader optimization tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CODMAS** 的多智能体协作框架，旨在通过大语言模型（LLM）实现硬件描述语言（RTL/Verilog）的自动化优化。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在芯片设计中，优化寄存器传输级（RTL）代码以提升功耗、性能和面积（PPA）指标是一项高度依赖专家经验、耗时且易出错的任务。
*   **研究动机**：虽然 AI 在逻辑综合和布局布线中已有应用，但在 RTL 级优化（如流水线化和时钟门控）方面，现有的 LLM 方法往往缺乏对设计意图的深刻理解，且难以处理复杂的架构变换。此外，缺乏高质量的 RTL 优化基准数据集也限制了该领域的研究。

### 2. 方法论：核心思想与技术细节
CODMAS 采用了一种**辩证多智能体协作**模式，将优化过程分为“推理”和“执行”两个层次：
*   **辩证推理层（Dialectic Agents）**：
    *   **阐述者（Articulator）**：受“小黄鸭调试法”启发，负责将优化目标分解为具体的步骤计划，并明确设计假设。
    *   **假设伙伴（Hypothesis Partner）**：进行预测性推理，在代码生成前预估优化效果，并在反馈阶段分析实际结果与预期之间的偏差。
*   **执行层（Executor Agents）**：
    *   **领域特定编码智能体（DCA）**：利用“领域知识注入（DKI）”技术（包括专家提示词、少样本示例和基于 Pyverilog 提取的数据流图），生成具体的 Verilog 代码。
    *   **代码评估智能体（CEA）**：调用确定性 EDA 工具（Icarus Verilog, Yosys, GTKWave）进行语法检查、功能仿真和 PPA 指标提取。
*   **算法流程**：系统形成一个闭环，CEA 的反馈（错误信息或 PPA 数据）会返回给辩证智能体，触发计划调整和假设修正，直到达到优化目标或迭代上限。

### 3. 实验设计
*   **数据集（RTLOpt）**：作者构建了一个包含 **120 个 Verilog 三元组**（原始代码、优化后代码、测试平台）的基准测试集，涵盖算术、控制和混合模块，专注于**流水线化（Pipelining）**和**时钟门控（Clock Gating）**两种优化任务。
*   **对比方法（Baselines）**：
    *   **提示词工程**：Zero-Shot Prompting。
    *   **推理框架**：CoDes、ReAct、Reflexion。
    *   **专用流水线**：LLM-VeriPPA。
*   **测试模型**：涵盖了专有模型（GPT-4o, GPT-3.5-turbo）和开源模型（Llama-3, DeepSeek-v2.5, Granite-34B, CodeLLaMA-34B）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **64 核 CPU** 和 **NVIDIA A100 GPU** 的服务器上运行。
*   **训练/推理时长**：文中未明确给出总运行时间，但提到每项实验重复 5 次以确保统计显著性。由于该框架主要基于推理和调用 EDA 工具，算力消耗主要集中在 LLM API 调用和 Yosys 综合仿真上。

### 5. 实验数量与充分性
*   **实验规模**：使用了 120 个设计案例，在 6 种不同的 LLM 上进行了测试，并对比了 5 种基线方法。
*   **消融实验**：作者进行了详细的消融实验，分别验证了辩证智能体（DA）、领域知识注入（DKI）和评估智能体（CEA）的贡献。
*   **充分性评价**：实验设计较为充分，通过多轮迭代（最多 8 轮）展示了性能提升的收敛过程。使用了配对 t 检验（p < 0.01）来证明结果的统计学意义，体现了实验的客观性和公平性。

### 6. 主要结论与发现
*   **PPA 显著提升**：在 GPT-4o 上，CODMAS 使流水线设计的关键路径延迟降低了约 **25.5%**，使时钟门控设计的功耗降低了约 **21.7%**。
*   **可靠性增强**：相比基线方法，CODMAS 显著降低了失败率（FR），将失败率控制在 **30% 以下**，而传统方法往往超过 40%-50%。
*   **模型普适性**：即使是较弱的开源模型，在 CODMAS 框架下也能获得明显的性能增益。
*   **迭代价值**：前 3 轮迭代通常能获得最大的性能提升，后续迭代收益递减。

### 7. 优点
*   **结构化推理**：通过“阐述者”和“假设伙伴”的辩证互动，模拟了人类工程师的思考过程，提高了优化的可解释性。
*   **确定性反馈**：将 EDA 工具集成到智能体循环中，确保了生成的代码不仅在语法上正确，而且在功能和物理指标上是经过验证的。
*   **新基准贡献**：RTLOpt 数据集的发布填补了 RTL 优化领域缺乏标准化 Benchmark 的空白。

### 8. 不足与局限
*   **优化类型有限**：目前仅专注于流水线和时钟门控，尚未扩展到重定时（Retiming）、资源共享或状态机重构等其他复杂优化。
*   **规模扩展性**：对于超大规模的工业级设计，频繁调用 EDA 工具进行综合可能会面临严重的运行时间瓶颈。
*   **验证深度**：功能验证依赖于测试平台（Testbench），而非穷尽式的形式验证（Formal Verification），可能存在极端情况下的功能偏差。
*   **数据集规模**：120 个案例虽然在同类研究中已属可观，但对于全面覆盖工业界多样的编码风格仍显不足。

（完）
