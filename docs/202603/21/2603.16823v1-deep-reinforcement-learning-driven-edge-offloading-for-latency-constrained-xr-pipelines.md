---
title: Deep Reinforcement Learning-driven Edge Offloading for Latency-constrained XR pipelines
title_zh: 深度强化学习驱动的延迟受限 XR 流水线边缘卸载
authors: "Sourya Saha, Saptarshi Debroy"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16823v1"
tags: ["query:aiot"]
score: 9.0
evidence: 边缘辅助XR的电池感知执行管理
tldr: 针对扩展现实（XR）应用在能耗受限设备上的严苛延迟需求，本文提出一种基于深度强化学习的边缘卸载框架。该框架综合考虑执行位置、负载质量、延迟要求及电池动态，通过轻量级策略在动态网络中做出在线决策。研究显著延长了设备续航并保证了高延迟达标率，为XR系统的延迟与能源权衡提供了有效解决方案。
selection_source: fresh_fetch
motivation: 现有XR卸载方案多关注平均性能，忽视了实时延迟要求与设备电池寿命之间的持续交互关系。
method: 提出一种基于轻量级深度强化学习（DRL）的电池感知执行管理框架，动态调整执行位置、工作负载质量和延迟。
result: "相比于延迟最优的本地执行，该方法将设备电池寿命延长了高达163%，且在各种网络条件下均保持了较高的运动到光子（MTP）延迟达标率。"
conclusion: 通过显式管理延迟与能源的权衡，该框架能有效提升边缘辅助XR系统的持久性和实时响应能力。
---

## 摘要
沉浸式扩展现实 (XR) 应用引入了对延迟敏感的工作负载，这些负载在能源和电池受限的设备上运行时必须满足严苛的实时响应性要求，这使得终端设备与附近边缘服务器之间的执行位置决策成为一个基础性的系统挑战。现有的自适应执行和计算卸载方法通常优化平均性能指标，未能充分捕捉闭环 XR 工作负载中实时延迟要求与设备电池寿命之间的持续交互。在本文中，我们提出了一种用于边缘辅助 XR 系统的电池感知执行管理框架，该框架综合考虑了执行位置、工作负载质量、延迟要求和电池动态。我们设计了一种基于轻量级深度强化学习策略的在线决策机制，该机制能够在动态网络条件下持续调整执行决策，同时保持较高的动作到光子 (motion-to-photon) 延迟合规性。实验结果表明，与延迟最优的本地执行相比，所提方法在稳定网络条件下可将预计的设备电池寿命延长高达 163%，同时保持超过 90% 的动作到光子延迟合规性。即使在网络带宽显著受限的情况下，该合规性也不会低于 80%，从而证明了在沉浸式 XR 系统中显式管理延迟与能耗权衡的有效性。

## Abstract
Immersive extended reality (XR) applications introduce latency-critical workloads that must satisfy stringent real-time responsiveness while operating on energy- and battery-constrained devices, making execution placement between end devices and nearby edge servers a fundamental systems challenge. Existing approaches to adaptive execution and computation offloading typically optimize average performance metrics and do not fully capture the sustained interaction between real-time latency requirements and device battery lifetime in closed-loop XR workloads. In this paper, we present a battery-aware execution management framework for edge-assisted XR systems that jointly considers execution placement, workload quality, latency requirements, and battery dynamics. We design an online decision mechanism based on a lightweight deep reinforcement learning policy that continuously adapts execution decisions under dynamic network conditions while maintaining high motion-to-photon latency compliance. Experimental results show that the proposed approach extends the projected device battery lifetime by up to 163% compared to latency-optimal local execution while maintaining over 90% motion-to-photon latency compliance under stable network conditions. Such compliance does not fall below 80% even under significantly limited network bandwidth availability, thereby demonstrating the effectiveness of explicitly managing latency-energy trade-offs in immersive XR systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Deep Reinforcement Learning-driven Edge Offloading for Latency-constrained XR pipelines》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限（电池、算力、散热）的扩展现实（XR）设备上，既满足严苛的实时响应要求（运动到光子延迟 MTP < 20-30ms），又最大化设备的电池寿命。
*   **研究动机**：
    *   **本地执行**：延迟低但功耗极高，导致电池迅速耗尽。
    *   **边缘卸载**：降低了设备功耗，但引入了不稳定的网络延迟，易导致 MTP 违规。
    *   **现有局限**：以往研究多关注平均延迟或吞吐量，忽视了 XR 闭环流水线中实时延迟合规性与电池动态之间的持续交互。

### 2. 方法论
*   **核心思想**：提出一个**电池感知（Battery-aware）**的执行管理框架，利用深度强化学习（DRL）在动态环境下实时调整执行配置。
*   **关键技术细节**：
    *   **系统模型**：将 XR 任务（以视觉惯性里程计 VIO 为例）抽象为可配置流水线。控制变量包括：图像质量（3 级）、惯性测量单元（IMU）采样率（3 级）和执行模式（本地或卸载），共 18 种组合。
    *   **MDP 建模**：
        *   **状态空间 ($s_t$)**：包括电池剩余电量 (SoC)、瞬时功耗、网络往返时间 (RTT)、可用带宽和当前 MTP 延迟。
        *   **动作空间 ($a_t$)**：选择上述 18 种配置之一。
        *   **奖励函数 ($R_t$)**：由三部分组成：MTP 延迟惩罚（优先级最高）、功耗惩罚和电池寿命激励。
    *   **算法流程**：采用轻量级的 **Deep Q-Network (DQN)**。控制器每秒运行一次，根据当前系统状态推断最优配置。该策略支持**在线学习**，无需预先离线训练，能够直接适应运行时的环境变化。

### 3. 实验设计
*   **实验平台**：基于开源 XR 研究平台 **ILLIXR** 构建端到端原型。
*   **硬件环境**：客户端和边缘服务器均配备 8 核 CPU、32GB RAM 和 NVIDIA RTX 2000 Ada GPU。
*   **对比方法 (Benchmarks)**：
    1.  **LOCAL**：始终在本地执行（最高质量）。
    2.  **OFFLOAD**：始终卸载到边缘（最高质量）。
    3.  **GREEDY**：基于即时奖励的贪婪策略（无长期规划）。
    4.  **THRESHOLD**：基于带宽阈值的启发式规则。
*   **测试场景**：
    *   **稳定网络**：固定带宽。
    *   **动态网络**：带宽在 1Gbps 到 1Mbps 之间循环波动，模拟真实环境中的网络干扰。

### 4. 资源与算力
*   **算力设备**：使用了 **NVIDIA RTX 2000 Ada GPU**。
*   **模型规模**：DQN 采用多层感知机（MLP）结构，输入层 5 维，隐藏层为 128-128，输出层 18 维，总参数量仅为 19,602 个。
*   **运行开销**：
    *   **推理延迟**：中位数仅 157 $\mu s$。
    *   **训练开销**：包含梯度更新的 P95 延迟为 3.945 ms（在 1s 的决策周期内占比极小）。
    *   **功耗开销**：DRL 插件仅增加约 0.9W（4%）的系统功耗，远低于其节省的功耗。

### 5. 实验数量与充分性
*   **实验规模**：针对稳定和动态网络分别进行了长时运行测试（直至电池耗尽或任务结束）。
*   **充分性**：
    *   每个 DRL 实验均使用 3 个不同的随机种子重复运行，并给出了方差包络线。
    *   进行了详细的**超参数敏感性分析**（如折扣因子 $\gamma$、探索率 $\epsilon$、惩罚权重 $\lambda$）。
    *   进行了**消融实验**，对比了不同决策间隔（0.5s, 0.75s, 1s）的影响。
*   **评价**：实验设计较为客观，涵盖了从能耗、延迟到策略收敛性的多维度评估，对比基准选择合理。

### 6. 主要结论与发现
*   **电池寿命**：相比于本地执行，RL-Adaptive 策略将预计电池寿命延长了 **163%**（从 16 分钟延长至 42 分钟）。
*   **延迟合规性**：在稳定网络下保持了 **90.8%** 的 MTP 合规率；在极端波动的网络下，合规率仍保持在 **83.8%**，远优于纯卸载模式（27.7%）。
*   **策略表现**：DRL 代理学会了在带宽低时切换到本地执行，并在带宽高时利用边缘卸载，同时动态降低传感器质量以平衡功耗。

### 7. 优点
*   **多目标协同**：成功将电池动态、感知质量和实时延迟整合进统一的优化框架。
*   **轻量化与实用性**：模型极小，推理极快，且支持在线学习，非常适合部署在嵌入式 XR 设备上。
*   **鲁棒性**：在带宽波动三个数量级的极端情况下，依然能通过动态降级和模式切换维持系统可用性。

### 8. 不足与局限
*   **延迟保证的经验性**：该方法提供的是概率性的延迟合规，而非确定性的硬实时保证（Hard Real-time Guarantee）。
*   **场景单一性**：实验主要基于 VIO 任务，未涵盖复杂的渲染卸载或多用户竞争边缘资源的场景。
*   **设备偏差**：实验使用的 RTX 2000 Ada 虽为移动版 GPU，但与真正的轻量级 AR 眼镜（如使用骁龙 AR2 平台）在功耗特性上仍有差异。

（完）
