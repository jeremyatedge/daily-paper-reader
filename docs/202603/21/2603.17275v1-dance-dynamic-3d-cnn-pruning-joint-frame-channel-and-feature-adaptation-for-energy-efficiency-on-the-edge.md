---
title: "DANCE: Dynamic 3D CNN Pruning: Joint Frame, Channel, and Feature Adaptation for Energy Efficiency on the Edge"
title_zh: DANCE：动态 3D CNN 剪枝：面向边缘端能效的帧、通道和特征联合自适应
authors: "Mohamed Mejri, Ashiqur Rasul, Abhijit Chatterjee"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17275v1"
tags: ["query:aiot"]
score: 10.0
evidence: 针对边缘端能效的动态3D CNN剪枝
tldr: 针对3D CNN在边缘设备上能效不足的问题，本文提出DANCE框架。该框架通过输入感知的动态剪枝，结合激活变异性放大（AVA）和自适应激活剪枝（AAP）技术，实现了对视频帧、通道和特征的细粒度调整。在保持性能的同时，显著降低了计算量和内存访问，在移动平台上实现了高达2.22倍的加速和1.47倍的能效提升。
selection_source: fresh_fetch
motivation: 现代3D CNN无法根据输入样本的计算复杂度动态调整，导致在边缘设备上能耗过高。
method: 采用两步走策略：先通过AVA增加神经元激活方差，再利用轻量级控制器AAP根据首层输出统计量动态剪枝帧、通道和特征。
result: 在Jetson Nano和Snapdragon平台上分别实现1.37倍和2.22倍加速，能效比现有技术提升达1.47倍。
conclusion: DANCE框架通过细粒度的动态适配，为3D CNN在资源受限的边缘设备上实现高效部署提供了有效方案。
---

## 摘要
现代卷积神经网络（CNN）是视频和图像处理的核心工具，但无法根据输入样本的计算复杂度进行动态调整以最小化能耗。在本研究中，我们提出了 DANCE，这是一种针对 3D CNN 的细粒度、输入感知动态剪枝框架，旨在以微乎其微甚至零性能损失为代价实现能效最大化。在所提出的两步法中，第一步称为激活变异性放大（AVA），通过重新训练 3D CNN 模型来增加全网神经元激活幅度的方差，从而便于在各种输入场景下做出剪枝决策。第二步称为自适应激活剪枝（AAP），通过训练一个轻量级激活控制器网络，根据网络第一层输出的统计信息，动态剪枝 3D 卷积层的帧、通道和特征（各层独立调整）。我们的方法通过在卷积层中引入稀疏性，显著减少了乘加（MAC）运算和内存访问。在 NVIDIA Jetson Nano GPU 和 Qualcomm Snapdragon 8 Gen 1 平台上的硬件验证显示，分别实现了 1.37 倍和 2.22 倍的加速，与现有最先进技术相比，能效提升高达 1.47 倍。

## Abstract
Modern convolutional neural networks (CNNs) are workhorses for video and image processing, but fail to adapt to the computational complexity of input samples in a dynamic manner to minimize energy consumption. In this research, we propose DANCE, a fine-grained, input-aware, dynamic pruning framework for 3D CNNs to maximize power efficiency with negligible to zero impact on performance. In the proposed two-step approach, the first step is called activation variability amplification (AVA), and the 3D CNN model is retrained to increase the variance of the magnitude of neuron activations across the network in this step, facilitating pruning decisions across diverse CNN input scenarios. In the second step, called adaptive activation pruning (AAP), a lightweight activation controller network is trained to dynamically prune frames, channels, and features of 3D convolutional layers of the network (different for each layer), based on statistics of the outputs of the first layer of the network. Our method achieves substantial savings in multiply-accumulate (MAC) operations and memory accesses by introducing sparsity within convolutional layers. Hardware validation on the NVIDIA Jetson Nano GPU and the Qualcomm Snapdragon 8 Gen 1 platform demonstrates respective speedups of 1.37X and 2.22X, achieving up to 1.47X higher energy efficiency compared to the state of the art.

---

## 论文详细总结（自动生成）

### 论文总结：DANCE - 动态 3D CNN 剪枝框架

#### 1. 核心问题与研究动机
*   **核心问题**：现代 3D CNN（用于视频处理）计算开销巨大，且无法根据输入样本的复杂度动态调整计算量，导致在资源受限的边缘设备上能效低下。
*   **研究动机**：现有的剪枝技术多为静态剪枝或针对 2D CNN 的粗粒度动态剪枝（如仅剪枝通道）。3D 视频数据具有时间（帧）、空间（特征）和深度（通道）三个维度的冗余，亟需一种能够同时在这三个维度进行细粒度、输入感知适配的动态剪枝方案，以在保证精度的前提下最大化边缘端的能效。

#### 2. 方法论
DANCE 采用两步走策略来实现输入感知的动态剪枝：
*   **第一步：激活变异性放大 (AVA)**
    *   **核心思想**：在剪枝前，通过重新训练模型来拉大神经元激活幅度的差距，使“重要”和“不重要”的特征更易区分。
    *   **技术细节**：引入了帧、通道、特征三个层级的注意力模块。通过在损失函数中加入方差项 $\sigma^2_{AVA}$，强制模型在训练过程中增加激活张量在各维度上的分布方差。
    *   **数学原理**：证明了最大化标准差等同于最大化 Hoyer 稀疏度度量，从而在不损失性能的情况下诱导稀疏性。
*   **第二步：自适应激活剪枝 (AAP)**
    *   **核心思想**：训练一个极轻量级的控制器，根据网络第一层的输出统计特性，为后续所有层生成动态剪枝阈值。
    *   **算法流程**：
        1.  提取第一层卷积的输出，通过 3D 平均池化和前馈网络（FFN）生成全局阈值 $\theta$。
        2.  使用直通估计器（STE）和类似 Gumbel-Softmax 的软门控机制解决剪枝不可导的问题。
        3.  根据阈值动态跳过不重要的帧、通道和特征图中的像素点。

#### 3. 实验设计
*   **数据集**：使用了视频识别领域的标准基准数据集 **UCF101** 和 **HMDB51**。
*   **基准模型 (Backbones)**：采用了 **R(2+1)D** 和 **C3D** 两种主流 3D 架构。
*   **对比方法**：
    *   静态剪枝方法：KGS、KGRC、DCP、FP、TP、MDP 等。
    *   针对 3D 的特定压缩方法：如基于频率域的压缩。
*   **硬件验证**：在 **NVIDIA Jetson Nano** (GPU) 和 **Snapdragon 8 Gen 1** (移动端 CPU) 上进行了实测。

#### 4. 资源与算力
*   **推理硬件**：
    *   NVIDIA Jetson Nano (4GB GPU)。
    *   Qualcomm Snapdragon 8 Gen 1 (Samsung S22)。
*   **算力优化**：针对 GPU 开发了时间/通道剪枝内核，针对 CPU 开发了基于 **ARMv9 Neon SIMD** 指令集的 FP16 定制内核。
*   **训练资源**：论文**未明确说明**训练这些模型所使用的具体服务器 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个大型数据集和两种不同的模型架构，进行了详尽的对比实验。
*   **消融实验**：专门验证了 AVA 步骤的必要性、AAP 控制器的开销以及不同剪枝维度（帧 vs 通道 vs 特征）对总压缩率的贡献。
*   **客观性**：通过与多种 SOTA（最先进）方法在相同数据集上的剪枝倍率与精度损失对比，证明了其优越性。实验设计考虑了硬件实际表现（延迟和能耗），而非仅停留在理论 FLOPs 上，具有较高的说服力。

#### 6. 主要结论与发现
*   **性能提升**：在 UCF101 上，R(2+1)D 模型实现了 **4.0倍** 的剪枝率，且精度反而提升了 1.55%；C3D 模型实现了 **3.49倍** 剪枝且无精度损失。
*   **硬件加速**：在 Jetson Nano 上实现 **1.37倍** 加速和 **1.47倍** 能效提升；在骁龙 8 Gen 1 上实现平均 **2.22倍** 的推理加速。
*   **输入相关性**：实验发现计算量与视频的空域/时域复杂度（通过 DCT 能量比衡量）高度相关，证明了动态适配的有效性。
*   **维度贡献**：通道剪枝对减少开销的贡献最大，其次是特征和帧。

#### 7. 优点
*   **多维度细粒度**：不同于以往只做通道剪枝的研究，DANCE 实现了帧、通道、特征的三位一体动态适配。
*   **AVA 预处理**：创新性地提出通过放大激活方差来辅助剪枝，解决了动态剪枝中阈值难确定的痛点。
*   **硬件友好**：不仅有理论指标，还针对边缘端 CPU/GPU 编写了定制化内核，验证了实际落地可行性。

#### 8. 不足与局限
*   **全局阈值限制**：为了降低控制器开销，AAP 使用了跨层的全局阈值，这可能限制了每一层根据自身特性进行最优剪枝的潜力。
*   **首层依赖**：剪枝决策高度依赖第一层卷积的输出，如果首层受到噪声干扰，可能会导致错误的剪枝决策影响全网。
*   **训练复杂性**：采用两步训练法（AVA 训练 + AAP 训练），相比端到端的单次训练增加了训练流程的复杂程度。

（完）
