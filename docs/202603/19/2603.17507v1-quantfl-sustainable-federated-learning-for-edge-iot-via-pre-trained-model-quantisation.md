---
title: "QuantFL: Sustainable Federated Learning for Edge IoT via Pre-Trained Model Quantisation"
title_zh: QuantFL：通过预训练模型量化实现边缘物联网的可持续联邦学习
authors: "Charuka Herath, Yogachandran Rahulamathavan, Varuna De Silva, Sangarapillai Lambotharan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17507v1"
tags: ["query:aiot"]
score: 9.0
evidence: 通过量化实现边缘物联网的可持续联邦学习
tldr: 针对物联网设备在联邦学习中因频繁上行传输导致的高能耗和碳足迹问题，本文提出 QuantFL 框架。该框架利用预训练模型初始化来支持激进且轻量级的量化方案，通过桶量化技术显著减少通信开销，且无需复杂的误差反馈机制。实验表明，QuantFL 在保持高精度的同时大幅降低了通信比特数，为受限的物联网网络提供了一种可持续的绿色训练方案。
selection_source: fresh_fetch
motivation: 联邦学习在物联网应用中面临高昂的通信能耗和碳排放挑战，而预训练模型在降低微调能耗方面的潜力尚未被充分挖掘。
method: 提出 QuantFL 框架，利用预训练初始化带来的参数更新集中性，采用内存高效的桶量化实现激进压缩，避免了高能耗的误差反馈计算。
result: "在 MNIST 和 CIFAR-100 数据集上，QuantFL 减少了约 40% 的总通信量（上行链路减少可达 80% 以上），且在严格带宽限制下精度优于未压缩基准。"
conclusion: QuantFL 为电池受限的物联网设备提供了一种实用且环保的联邦学习方案，有效平衡了通信效率与模型性能。
---

## 摘要
联邦学习（FL）在物联网（IoT）设备上实现了隐私保护的智能化，但由于频繁上行传输的高能耗，产生了显著的碳足迹。尽管预训练模型在边缘设备上已日益普及，但其在降低微调能耗方面的潜力尚未得到充分探索。在这项工作中，我们提出了 QuantFL，这是一个可持续的联邦学习框架，它利用预训练初始化来实现激进且计算轻量级的量化。我们证明了预训练能自然地集中更新统计信息，使我们能够使用内存高效的分桶量化（bucket quantisation），而无需复杂误差反馈机制带来的高能耗开销。在 MNIST 和 CIFAR-100 数据集上，QuantFL 在严格的带宽预算下，将总通信量减少了 40%（全精度下行链路时总比特数减少约 40%；上行链路或下行链路量化时减少 ≥80%），同时达到或超过了未压缩的基准模型；BU 在比特数减少几个数量级的情况下，分别实现了 89.00%（MNIST）和 66.89%（CIFAR-100）的测试准确率。我们还考虑了上行和下行链路的成本，并对量化水平和初始化进行了消融实验。QuantFL 为受电池限制的物联网网络上的可扩展训练提供了一种实用的“绿色”方案。

## Abstract
Federated Learning (FL) enables privacy-preserving intelligence on Internet of Things (IoT) devices but incurs a significant carbon footprint due to the high energy cost of frequent uplink transmission. While pre-trained models are increasingly available on edge devices, their potential to reduce the energy overhead of fine-tuning remains underexplored. In this work, we propose QuantFL, a sustainable FL framework that leverages pre-trained initialisation to enable aggressive, computationally lightweight quantisation. We demonstrate that pre-training naturally concentrates update statistics, allowing us to use memory-efficient bucket quantisation without the energy-intensive overhead of complex error-feedback mechanisms. On MNIST and CIFAR-100, QuantFL reduces total communication by 40\% ($\simeq40\%$ total-bit reduction with full-precision downlink; $\geq80\%$ on uplink or when downlink is quantised) while matching or exceeding uncompressed baselines under strict bandwidth budgets; BU attains 89.00\% (MNIST) and 66.89\% (CIFAR-100) test accuracy with orders of magnitude fewer bits. We also account for uplink and downlink costs and provide ablations on quantisation levels and initialisation. QuantFL delivers a practical, "green" recipe for scalable training on battery-constrained IoT networks.

---

## 论文详细总结（自动生成）

以下是对论文《QuantFL: Sustainable Federated Learning for Edge IoT via Pre-Trained Model Quantisation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）虽然能保护物联网（IoT）设备的隐私，但面临严重的**通信瓶颈**。由于需要频繁上传巨大的模型更新，导致高昂的能耗和碳足迹，这对于电池受限的边缘设备是致命的。
*   **研究动机**：现有的压缩方法（如误差反馈 EF）往往增加了本地计算和内存负担。作者发现，随着边缘设备上预训练模型的普及，可以利用预训练带来的“参数更新集中性”来简化压缩过程。
*   **核心目标**：提出一种“绿色”且可持续的 FL 框架，在不牺牲精度的前提下，通过极低成本的量化技术大幅减少上行链路通信量。

### 2. 方法论：核心思想与关键技术
**QuantFL** 的核心思想是：**预训练初始化 + 激进的桶量化（Bucket Quantisation）**。
*   **核心逻辑**：预训练使模型处于较好的参数空间，导致每轮的本地更新（$\Delta w$）范围更小、方差更低。这种统计特性使得简单的标量量化就能达到极高的保真度，从而无需复杂的误差反馈机制。
*   **关键技术细节**：
    *   **桶均匀量化（BU）**：将每层更新值的范围划分为等宽的 $L$ 个桶。
    *   **桶分位数量化（BQ）**：根据更新值的经验分布划分等质量的桶，适应峰值分布。
    *   **索引传输与码本刷新**：客户端每轮仅发送桶索引（Index），而桶边界（Codebook）则以较长周期 $T$ 进行异步刷新，从而分摊通信成本。
    *   **中点解码**：服务器接收索引后，取对应桶的中点值进行模型聚合，这是均方误差（MSE）最优的解码方式。

### 3. 实验设计
*   **数据集**：MNIST（简单任务，浅层 CNN）和 CIFAR-100（复杂任务，ResNet-18）。
*   **Benchmark（基准）**：
    *   **Baseline FedAvg**：全精度、无预训练的原始联邦平均算法。
    *   **QSGD**：经典的随机量化方法。
*   **对比维度**：量化水平 $L \in \{64, 128\}$、有无预训练（NP 代表从零训练）、数据异构性（Dirichlet 分布 $\alpha$ 扫描）。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算设置，包括 **NVIDIA RTX 6000 GPU (48GB VRAM)** 和 **Intel Core i9-10980 处理器**。
*   **训练细节**：模拟 100 个客户端，每轮随机选择 10 个，每个客户端本地训练 2 个 Epoch。
*   **说明**：文中明确给出了硬件型号，但未详细列出总训练时长（以小时计的具体数值）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个代表性数据集，并针对量化阶数、预训练影响、数据非独立同分布（Non-IID）程度进行了多组消融实验。
*   **充分性评价**：实验设计较为充分。作者不仅对比了准确率，还详细计算了上行和下行链路的比特数成本，并引入了 Dirichlet 异构性测试（$\alpha=1.0, 0.5, 0.1$），证明了 BU 方法在极端数据偏斜下的鲁棒性。对比方法（QSGD）也是该领域的标准基准，实验结果具有客观性。

### 6. 主要结论与发现
*   **通信大幅减少**：QuantFL 在保持或超过基准精度的情况下，总通信量减少了约 **40%**。若仅看上行链路，比特数减少可达 **80% 以上**（从 32 位降至约 6 位）。
*   **预训练的红利**：预训练初始化显著提升了量化后的模型稳定性。在相同比特预算下，预训练版本的准确率远高于从零训练（NP）的版本。
*   **BU 优于 BQ**：在 CIFAR-100 等复杂任务中，桶均匀量化（BU）表现最稳健，而桶分位数量化（BQ）和 QSGD 在非 IID 数据下精度下降明显。
*   **精度表现**：在 MNIST 上达到 89.00%，在 CIFAR-100 上达到 66.89%，均处于帕累托前沿（Pareto frontier）。

### 7. 优点
*   **高效节能**：去除了复杂的误差反馈（Error Feedback），降低了 IoT 设备的内存占用和计算能耗，符合“绿色 AI”趋势。
*   **简单易部署**：基于标量量化，算法逻辑简单，易于在资源受限的微控制器上实现。
*   **鲁棒性强**：实验证明其在 Non-IID 数据环境下依然能保持高性能。

### 8. 不足与局限
*   **数据集覆盖有限**：目前仅在图像分类任务（MNIST/CIFAR）上验证，缺乏自然语言处理（NLP）或时间序列等其他 IoT 常见任务的验证。
*   **下行链路假设**：部分实验假设下行链路为全精度，虽然文中提到了下行量化方案，但大规模测试仍以上行为主。
*   **模型规模限制**：实验使用的 ResNet-18 属于中等规模，对于超大规模模型（如 Transformer 类）的量化效果和码本刷新频率的影响尚待进一步研究。
*   **预训练依赖**：该方法高度依赖于高质量预训练模型的可用性，若特定 IoT 任务缺乏相关预训练模型，其优势将减弱。

（完）
