---
title: "RAMP: Reinforcement Adaptive Mixed Precision Quantization for Efficient On Device LLM Inference"
title_zh: RAMP：面向高效端侧大语言模型推理的强化自适应混合精度量化
authors: "Arpit Singh Gautam, Saurabh Jha"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17891v1"
tags: ["query:aiot"]
score: 9.0
evidence: 针对设备端大模型推理的混合精度量化
tldr: RAMP 是一种基于强化学习（Soft Actor-Critic）的自适应混合精度量化框架，旨在优化大语言模型在边缘设备上的推理效率。它通过学习每层的最优位宽分配，并结合 Scale Folding 预处理技术，在极低位宽下保持高精度。该方法具有极强的泛化能力，支持跨模型规模的零样本迁移，在显著降低模型体积的同时，性能优于传统的均匀量化方法。
selection_source: fresh_fetch
motivation: 传统的均匀位宽量化在模型精度与推理效率之间难以达到最优平衡，且缺乏针对不同层敏感度的自适应分配机制。
method: 采用 Soft Actor-Critic 强化学习框架，结合 11 维特征嵌入和 Scale Folding 预处理技术，实现每层位宽的动态分配。
result: 在 Llama 2 7B 上以 3.65 位宽实现了优于 4 位 AWQ 的精度，且训练好的策略可直接零样本迁移至 13B 和 Mistral 模型。
conclusion: RAMP 证明了量化敏感性主要由架构决定，通过自适应混合精度分配，可以在极低资源消耗下保留大模型绝大部分的推理性能。
---

## 摘要
训练后量化对于在资源受限的硬件上部署大语言模型（LLMs）至关重要，然而目前最先进的方法对各层强制执行统一的位宽，导致精度与效率的权衡并非最优。我们提出了 RAMP（强化自适应混合精度），这是一个离策（off-policy）Soft Actor Critic 框架，通过学习逐层位宽分配，在全局比特预算下最小化困惑度。该策略基于激活统计量、权重属性和结构描述符的 11 维嵌入进行条件化，实现了跨模型家族和规模的零样本迁移。为了实现稳定的 4 比特以下量化，我们引入了尺度折叠（Scale Folding），这是一种通过逐通道缩放和归一化层补偿将激活离群值迁移至权重的预处理技术。一种具有非对称惩罚和预算悬崖的质量优先奖励机制驱动了模型的快速收敛。在 Llama 2 7B 上，RAMP 在 3.68GB（有效位宽 3.65 bits）下实现了 5.54 的困惑度，在模型大小上优于统一 4 比特 AWQ（3.90 GB 下为 5.60）和 GPTQ 约 6%，在质量上提升了 1% 至 3%。关键的是，仅在 Llama 2 7B 上训练的策略可以零样本泛化至 Llama 2 13B 和 Mistral 7B，且表现往往优于针对特定目标的训练，这支持了量化敏感性主要由架构决定的假设。HALO 流水线将分配方案导出为 GGUF 格式，以便在 CPU、GPU 和边缘设备上进行无算子（kernel-free）推理，保留了 FP16 常识推理性能的 99.5%。

## Abstract
Post training quantization is essential for deploying large language models (LLMs) on resource constrained hardware, yet state of the art methods enforce uniform bit widths across layers, yielding suboptimal accuracy efficiency trade offs. We present RAMP (Reinforcement Adaptive Mixed Precision), an off policy Soft Actor Critic framework that learns per layer bit width assignments to minimize perplexity under a global bit budget. The policy conditions on an 11 dimensional embedding of activation statistics, weight properties, and structural descriptors, enabling zero shot transfer across model families and scales. To enable stable sub 4 bit quantization, we introduce Scale Folding, a preconditioning technique that migrates activation outliers into weights via per channel scaling and normalization layer compensation. A quality prioritized reward with asymmetric penalties and budget cliffs drives rapid convergence. On Llama 2 7B, RAMP achieves 5.54 perplexity at 3.68GB (3.65 effective bits), outperforming uniform 4 bit AWQ (5.60 at 3.90 GB) and GPTQ by 6% in size and 1% to3% in quality. Critically, a policy trained only on Llama 2 7B generalizes zero shot to Llama 2 13B and Mistral 7B, often surpassing target specific training, supporting the hypothesis that quantization sensitivity is primarily architectural. The HALO pipeline exports allocations to GGUF format for kernel free inference on CPUs, GPUs, and edge devices, retaining 99.5% of FP16 commonsense reasoning performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RAMP**（Reinforcement Adaptive Mixed-Precision）的强化学习框架，旨在解决大语言模型（LLM）在端侧设备部署时的存储与推理效率问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代 LLM（如 Llama-3, Mistral）参数量巨大，面临“内存墙”瓶颈，难以在消费级 GPU 或边缘设备上运行。
*   **研究动机**：
    *   **均匀量化的局限性**：现有的训练后量化（PTQ）方法（如 GPTQ, AWQ）通常对所有层采用统一位宽，忽略了不同层对量化噪声的敏感度差异。
    *   **缺乏迁移性**：现有的混合精度搜索方法通常需要针对每个模型进行昂贵的重新优化，无法跨模型规模或家族复用。
    *   **硬件部署难题**：混合精度推理往往因算子碎片化导致实际速度反而慢于均匀量化。

### 2. 方法论：核心思想与关键技术
RAMP 将量化位宽分配建模为一个**序列决策问题**，通过强化学习自动寻找最优方案。
*   **核心思想**：利用 **Soft Actor-Critic (SAC)** 算法学习一个策略，根据层的结构和统计特征分配位宽（3-6 bits），在满足全局比特预算的前提下最小化模型困惑度（Perplexity）。
*   **关键技术细节**：
    *   **11 维层嵌入（State Space）**：策略输入包括激活值统计（最大值、重要性）、权重统计（均值、标准差）、结构描述符（层深度、输入输出维度、层类型）及上下文特征。通过归一化处理，使策略具备跨模型规模的通用性。
    *   **尺度折叠（Scale Folding）**：为了支持稳定的 3-bit 量化，该技术通过逐通道缩放将激活值中的离群点（Outliers）迁移到权重中，并补偿归一化层，从而平滑激活分布。
    *   **质量优先奖励函数**：设计了非对称奖励机制，对困惑度上升（精度下降）给予重罚，并设置“预算悬崖”惩罚，强制模型在预算内寻找最优精度。
    *   **HALO 部署流水线**：将学习到的分配方案映射到标准的 GGUF 格式，利用 `llama.cpp` 的成熟算子实现跨平台（CPU/GPU/Apple Silicon）的无损推理。

### 3. 实验设计
*   **数据集**：使用 **WikiText-2** 进行困惑度评估和校准；使用 **PIQA, HellaSwag, WinoGrande, ARC** 等常识推理任务评估下游性能。
*   **模型范围**：Llama-2 (7B, 13B), Llama-3 (8B), Mistral-7B。
*   **Benchmark 与对比方法**：
    *   **基准**：FP16（全精度上限）。
    *   **对比方法**：RTN（简单量化）、GPTQ、AWQ、SmoothQuant、QuIP#、SqueezeLLM 等最先进的 PTQ 方法。

### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA RTX PRO 5000 Blackwell 和 A100 GPU。
*   **训练时长**：
    *   SAC 算法表现出极高的样本效率，通常在 **120-150 个 Episode** 内收敛。
    *   在 Llama-2-7B 上，SAC 训练仅需 **6 小时**，而对比算法 PPO 需要 48 小时。
    *   校准过程（特征提取）仅需约 2 分钟。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **50 多组**核心实验，涵盖了不同模型家族、不同规模以及零样本迁移测试。
*   **充分性**：
    *   包含了详尽的**消融实验**（如尺度折叠的影响、奖励函数设计、SAC vs PPO）。
    *   进行了**零样本迁移实验**：在 Llama-2-7B 上训练的策略直接应用于 13B 和 Mistral-7B，验证了策略的泛化能力。
    *   实验设计客观公平，采用了统一的评估框架（lm-evaluation-harness）和标准的 GGUF 部署环境。

### 6. 主要结论与发现
*   **帕累托最优**：RAMP 在相同模型大小下实现了更低的困惑度。例如在 Llama-2-7B 上，3.65 bits 的 RAMP 优于 4-bit 的 AWQ（体积减小 6%，质量提升 1%）。
*   **零样本迁移成功**：证明了量化敏感性主要由**模型架构**（如 Transformer 的层角色）决定，而非具体的参数数值。在 7B 上训练的策略在 13B 上甚至优于直接在 13B 上训练的效果。
*   **结构敏感性规律**：策略倾向于给输出投影层和最后几层分配更高精度，而对中间的 MLP 层进行激进压缩。
*   **性能保留**：在大幅压缩体积的同时，保留了 FP16 原始推理性能的 99.5%。

### 7. 优点与亮点
*   **泛化性极强**：首次实现了量化策略的跨模型迁移，避免了每个新模型都要重新搜索的成本。
*   **生产就绪**：通过 HALO 流水线直接对接 GGUF 格式，解决了混合精度量化在实际部署中的算子加速难题。
*   **稳定性高**：Scale Folding 技术有效解决了 4-bit 以下量化容易崩溃的问题。
*   **高效节能**：相比云端 A100 部署，RAMP 量化模型在端侧运行可减少约 66-75% 的碳排放。

### 8. 不足与局限
*   **架构覆盖有限**：目前仅在 Decoder-only 的 Transformer 架构（Llama, Mistral）上验证，尚未测试 Encoder-Decoder 或 MoE 架构。
*   **位宽粒度**：目前仅支持层级（Layer-wise）的位宽分配，尚未深入到更细粒度的 Head 或 Channel 级别。
*   **离散限制**：仅支持离散的整数位宽（3, 4, 5, 6），不支持分数位宽（如 3.5-bit）。
*   **静态分配**：位宽分配在推理前固定，尚未实现根据输入内容动态调整精度的“动态量化”。

（完）
